// src/ui/types.ts
//
// BP-018 `## Public interface`, verbatim: `export type Tone = 'ok' | 'warn'
// | 'bad' | 'neutral' | 'accent'`. "Series colours are `chart-you` and
// `chart-rival` only; `Tone` is for state and is never used for a series."
export type Tone = "ok" | "warn" | "bad" | "neutral" | "accent";
