---
description: System · Design system work - previews, components, tokens
---
Invoke the design-guardian subagent for: $ARGUMENTS
(e.g. "preview WO-004", "propose component DataGrid", "audit UI drift",
"status of token backfill"). Return artifacts, registry diffs, and open
questions.

A preview run ends with a published page, not a file path: the sheet under
`design/previews/WO-###.html`, the artifact URL and version logged in the
work order and carried into components.md, and the URL returned for the
owner to answer on (constitution rule 7.3). The sign-off itself is not
yours to record — the librarian reads the owner's word back off that page
and writes the `Signed-off:` line.
