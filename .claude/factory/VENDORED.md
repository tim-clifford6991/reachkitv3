# SDLC Factory — vendored copy

Doctrine **0.11.1** · source `tim-clifford6991/sdlc-factory` @ `e4c6015` · generated 2026-09-02.

This directory and the `agents/`, `commands/`, `skills/` links beside it are
**generated** by `factory-vendor.sh` (run from `factory-upgrade.sh`). Never edit
here — a fix belongs in the central repository, then re-vendor. The two patched
strings (`${CLAUDE_PLUGIN_ROOT}` → this directory) are the only deltas from source.
