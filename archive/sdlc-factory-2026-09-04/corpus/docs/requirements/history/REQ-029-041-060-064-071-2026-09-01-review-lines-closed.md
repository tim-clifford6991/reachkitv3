# REQ-029, REQ-041, REQ-060, REQ-064, REQ-071 — open review lines closed, 2026-09-01 (requirements-analyst)

Answers the librarian's refusal of 2026-09-01
(`REQ-029-041-060-064-071-2026-09-01-librarian-refusal.md`). Seven `REVIEW(...)`
lines were open across the five; all seven are now gone — four folded into
criterion text, three deleted as stale against the text they objected to. No
status was raised: all five stay `in-review`, and approval is the owner's.

## REQ-029 — `REVIEW(gap)` folded, then deleted

Criterion 3's degraded sentence had no stated end. It now stands while the most
recent completed measurement of the site is the one that fell short, and stops
when a later measurement of that site completes in full — the founder's own late
pass or a weekly re-measurement alike. Derived from BP-036 decision 3 ("The
degraded sentence is a projection of the current report, never a stored flag"),
stated as the customer-observable half; the current-report pointer and
`readCurrentReport` stay BP-036's.

## REQ-041 — `REVIEW(ambiguity: criterion 4)` folded, then deleted

Criterion 4 now closes the scope its own last sentence opened: the rivals'
absolute counts and the customer's own number in criteria 10 and 11 are values
shown alongside for comparison, carry no goal, and render without one. Derived
from BP-038 decision 1 ("'Headline number' is defined by the `headline` slot,
not by a list"). The four goals and their pinned values — including
`pages_published = 30`, the owner's ruling of 2026-08-31 — stay in BP-038 and
BP-005: REQ-041's own non-goal places the goal *value* outside this requirement,
and restating it here would mint rule 2.4's second copy.

## REQ-060 — `REVIEW(ambiguity)` folded, then deleted

Criteria 2 and 4 said "is told in one written line" with no surface. Criterion 2
now names the page's own record as the surface and the whole of the telling —
the line REQ-056 criterion 6 puts in place of a live address — and states that a
WordPress draft delivery sends no mail. That is derived, not chosen: REQ-062's
non-goal takes a WordPress draft out of the verification whose completion is the
only occasion REQ-062 criterion 5 sends on, and REQ-064 criterion 6 admits a
mail only on an occasion a requirement states. Criterion 4's line is placed on
the same record.

## REQ-064 — two conflict lines and one gap line deleted as stale

None survives as a `blocked-by` edge; each was checked against the requirement's
current text, and each objects to text REQ-064 no longer carries.

1. `REVIEW(conflict with REQ-057)` — claimed the unsuppressible-mail exception
   "is stated only in this requirement's non-goals, not in criterion 6".
   Criterion **7** carries it in terms: "except the single occasion REQ-057
   criterion 7 makes unsuppressible, where that mail is the whole of the
   customer's telling that a page is going live and no interval in which to stop
   it exists". The line also numbers the recurring-mail promise as criterion 6;
   it is criterion 7. Stale against a renumbering, and against the fold that
   already happened.
2. `REVIEW(conflict with REQ-074, REQ-079, REQ-025)` — its second half ("no
   acceptance criterion in this corpus states which mails may be sent at all")
   is false: criterion 6 states it, and only the *register of kinds* moved to
   the non-goals. Its first half is live but is a defect in the citing files,
   not in REQ-064: REQ-079's stale quotation is fixed in the same pass; REQ-074
   and REQ-077 carry the identical stale quotation and REQ-025 criterion 6 cites
   "REQ-064 criterion 6" for a kind name that now sits in REQ-064's non-goals.
   All three are `approved` and outside this pass's scope; reported for a
   follow-up rather than edited here.
3. `REVIEW(gap)` — feared a partly measured week carrying both criterion 1's
   line and criterion 3's "nothing to report". Criterion 3 already excepts it:
   "or the week was only partly measured (REQ-065 criterion 4), when criterion
   1's line naming which sections could not be measured stands in its place. In
   neither of those cases does the mail say there was nothing to report."

REQ-064's own text is unchanged by this pass. Its non-goal register lists eight
kinds under "It registers at least", which stays true of the nine BP-016 holds;
it was left rather than extended, to avoid deepening a second copy of a table
the mail blueprint owns.

## REQ-071 — `REVIEW(untestable: criteria 12 and 13)` narrowed, then deleted

The limb "each affected number carries its goal in place of a change" had no
pass condition for a rival distance or a point on a charted series, neither of
which has a goal (REQ-041 criterion 4 gives one only to a headline number).
Criteria 12 and 13 now state the promise in two parts, both testable for every
number they cover: no number, distance or point is shown with a change figure
across the change, and where the number is one that carries a goal, the goal is
what it carries in place of that change. The promise is narrowed, not withdrawn:
nothing that was forbidden before is permitted now.

## Downstream

- REQ-029 criterion 3 — WO-152 quotes it verbatim; the quoted fragment survives
  and the criterion is longer. Re-extract.
- REQ-041 criterion 4 — WO-156, WO-160, WO-161 quote it verbatim; same. Re-extract.
- REQ-060 criterion 2 — WO-237 quotes it verbatim; same. Re-extract.
- REQ-071 criteria 12 and 13 — WO-089 and WO-092 quote the changed limb verbatim
  and their rows no longer match; WO-090, WO-091 and WO-093 quote REQ-071
  elsewhere. Re-extract across WO-089…WO-093.
