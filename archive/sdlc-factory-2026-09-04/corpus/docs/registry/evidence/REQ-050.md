# Evidence — REQ-050 (the generation hard rules a draft must pass to be queued)

Source: `BUILD.md` v1.1, 2026-08-28 (in this repo, at `/BUILD.md`).

- §8, preamble, verbatim: "Hard rules, enforced in code, not prompts:"
- §8, rule 1, verbatim: "**Grounded**: ≥1 verifiable fact from the customer's
  own live pages, carried with its source URL + read date, rendered as the
  highlight in the draft view."
- §8, rule 2, verbatim: "**No invented people**: no generated
  bylines/bios/personas. Customer identity or none."
- §8, rule 3, verbatim: "**Not a doorway**: answers the target question before
  naming the product (checked: first 300 chars contain no brand mention)."
- §8, rule 5, verbatim: "**Near-duplicate gate**: ≥85% similarity vs the
  customer's published set = never queued."
- §8, rule 6, verbatim: "No rival metrics invented; every rival claim links its
  public source."
- §14, verbatim: "We make pages **structurally citable**; we never engineer
  content to steer what an assistant recommends (May-2026 spam policy line — no
  prompt-shaped tricks, no hidden instructions, in any generated page)."
- §14.2, verbatim: "No invented authors (§8)."
- §14.3, verbatim: "Near-duplicate gate before queueing (§8)."

Rule 4 of the same list — the do-not-claim filter, and the recovery path every
rule on this list shares — is REQ-053's; its evidence sits with that
requirement, not here (rule 2.4).

Why this sits here and not in the requirement: which pipeline stage enforces
which rule, and the model each stage runs on, is implementation the REQ must not
carry (rule 2.1). REQ-050 states only what must be true of a draft before the
customer can be shown it.

No implementation exists to cite: this repo carries no `src/`
(`00-project.md`, "Greenfield. No source code on disk yet").
