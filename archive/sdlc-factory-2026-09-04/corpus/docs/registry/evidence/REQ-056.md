# Evidence — REQ-056 (publish states, idempotency, ceilings and the stop)

Source: `BUILD.md` v1.1, 2026-08-28 (in this repo, at `/BUILD.md`).

- §9, verbatim: "**State machine** (exact, idempotent):" followed by the diagram
  `planned → generating → in_review → approved → publishing → published`, with
  `↓ veto → skipped`, `↓ fail → failed → retry ×3 → needs_attention`, and
  `published → unpublished (always available)`.
- §9, verbatim: "Publishing idempotent by `(draft_id, destination)` — a retry can
  never create a second post."
- §9, verbatim: "Failed publish: back in the queue with a written reason; expired
  credential is a **state** (reconnect prompt, queue holds), not an error loop."
- §9, verbatim: "Every published page records: opportunity id, target query,
  measurement date, approved-vs-autopilot, live URL."
- §9, verbatim: "Autopilot hard limits regardless of settings: ≤1 publish/day,
  ≤8/week; **Fix never automates**; pause is one click and instant."
- §14.7, verbatim: "Autopilot rate limits independent of the monthly cap (§9)."
- §4.6, day panel, verbatim: "planned → Move/Skip" — the source for the
  `planned`→`skipped` transition the §9 diagram leaves implicit.

The other move the diagram leaves implicit — a generation that produced no draft
fit for review — is not sourced here: it comes from REQ-053 criterion 3 and
REQ-092 criterion 1, cited in the requirement itself.

Why this sits here and not in the requirement: the state names are the
customer-visible contract and stay in the REQ; the diagram, the idempotency key
`(draft_id, destination)` and the cap constants are source evidence a REQ must
not carry (rule 2.1).

No implementation exists to cite: this repo carries no `src/`
(`00-project.md`, "Greenfield. No source code on disk yet").
