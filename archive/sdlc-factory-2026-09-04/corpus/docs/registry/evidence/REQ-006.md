# Evidence — REQ-006 (what the free report's AI matrix is made of)

Source: `BUILD.md` v1.1, 2026-08-28 (in this repo, at `/BUILD.md`).

- §6.2, verbatim: "We do **no own inference** for AI visibility — all AI-answer
  data is DataForSEO data, three tiers, and the MVP uses the cheap two plus a
  free one".
- §6.2, tier table, AI Overviews row, verbatim: "`ai_overview` item + per-domain
  `references`, returned **inside** organic SERPs we already buy | **0¢ extra** |
  **The free report's AI matrix** (12 question-SERPs) and every paid target SERP.
  Never set `load_async_ai_overview`".
- §6.2, ChatGPT (LLM Scraper) row, verbatim: "**Paid only** — battery engine 1
  (std)". Perplexity row, verbatim: "**Deferred to v1.1**".
- §6.2, verbatim: "The free path makes **zero** AI Optimization API calls."
- §6.3, verbatim: "Brand mentions/citations = string match over references."

Why this sits here and not in the requirement: which vendor endpoint supplies
the matrix, and at what price, is vendor evidence a REQ must not carry (rule
2.1). REQ-006 states only what the customer must be able to rely on — the
denominator, the muted no-answer cell, the named citations, and the claim being
scoped to Google's AI answers rather than to assistants we never queried.
