---
id: WO-071
type: work-order
title: "deriveProfile — the nano profile call and the Measured<Profile> it returns"
status: done
wave: W4
ui: no
risk: high
implements: [BP-025, BP-011, REQ-006]
depends-on: [WO-276, WO-026, WO-277]
blocked-by: []
rests-on:
  - claim: "The seams this WO imports are declared but not yet built: `llm()` and the `'profile'` call site (BP-009), `CostContext` (BP-007), `Measured<T>` (BP-010). Their work orders are in sibling planners' blocks and were cut concurrently with this one, so they cannot be enumerated in `depends-on` here. If any seam is absent when this WO starts, that is a `blocked-by` on the WO that owns it, added at that moment."
    disposition: confirmed
    reason: "Observed directly at implementation time. `src/lib/llm/index.ts` exports `llm()` and accepts an arbitrary `site: string` (WO-026, `done`) — the `'profile'` call site needs no separate declaration to be usable, it is passed as a literal; `src/lib/costs/index.ts` exports `CostContext` (WO-276, `done`); `src/lib/measure/measured.ts` exports `Measured<T>` (WO-277, `done`). All three imported without a stub or a local redeclaration; no `blocked-by` was needed. Re-confirmed independently, validator, 2026-09-04 (TST-041, rule 2.3b): read all three exports directly (`src/lib/llm/index.ts` line 214 `llm()`; `src/lib/costs/index.ts` line 42 `CostContext`; `src/lib/measure/measured.ts` line 19 `Measured<T>`) and `npm run typecheck` re-run clean."
supersedes: []
---

# WO-071 — deriveProfile — the nano profile call and the Measured<Profile> it returns

- Branch: wo/WO-071-derive-profile
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/questions/profile.ts` so a domain's own home (and optional
pricing) text becomes a `Measured<Profile>` in buyer vocabulary through the single
`'profile'` nano call site, or an `unmeasured` result that yields no market set at all.

**Risk: high — seams: money, and data leaving the system.** `deriveProfile` originates a billable model call and is the order that decides which of the customer's own text — the home page and the pricing page — is what crosses to the vendor. Its `unmeasured` arm is the free path's cost floor: a test that survives deletion of the unreadable-home-page branch turns an unfetchable site into a paid call on empty input. Mutation-tested (doctrine 0.13.2, rule 2b).

## Context an implementer needs
There is **no source code on disk** in this repository — `BUILD.md`, `DATA-COSTS.md`
and `sdlc-factory/` are the whole tree. Every path in the file plan below is
therefore NEW, and the placement is provisional against
`sdlc-factory/docs/registry/structure.md`, which gives `src/lib/market/**` to BP-011
and this file's directory to BP-025 (`code: src/lib/market/questions/**`).

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/questions/profile.ts` | **NEW** | Declares the `Profile` interface and `deriveProfile` exactly as BP-025's `## Public interface` states them. Builds the `'profile'` call: input is the supplied home text plus the optional pricing text, `tier: 'nano'`, and a Zod schema whose parsed shape is exactly `Profile`'s seven fields. Returns the `Measured<Profile>` `llm()` returns, unaltered — no default category is substituted for an `unmeasured` arm. |
| `tests/market/questions/profile.test.ts` | **NEW** | The tests named in the test plan, against a stubbed `llm()` seam. No network. |

`tests/market/questions/**` is BP-025's own glob (`structure.md` rule 4: tests live
beside the module they exercise). No other file in this repository is touched — in
particular no pin is added to `src/lib/config/constants.ts`, which is BP-005's.

## Interfaces
**Consumes** (copied exactly from the blueprints that own them):

```ts
// BP-009 — src/lib/llm/
export function llm<T>(c: CostContext, call: {
  site: LlmCallSite
  input: unknown
  schema: ZodType<T>          // output is parsed or the call failed
  tier: 'nano' | 'haiku'
}): Promise<Measured<T>>
// the site used here is BP-009's:
//   | 'profile'            // nano · homepage+pricing text -> business profile (BUILD 6.7 step 1)
```
```ts
// BP-010 — src/lib/measure/
export type Measured<T> =
  | { kind: 'measured';   value: T; at: Date }
  | { kind: 'zero';       value: T; at: Date }           // read it; it contained none
  | { kind: 'unmeasured'; reason: 'undeterminable' | 'not_attempted'; at: Date }
```
```ts
// BP-007 — src/lib/costs/
export interface CostContext { /* recordFetch · capHit · spentCents · degraded */ }
```

**Exposes** (copied exactly from BP-025's `## Public interface`):

```ts
// src/lib/market/questions/profile.ts — BUILD §6.7 step 1
export interface Profile {
  category: string                      // buyer vocabulary, never the site's marketing language
  job: string
  offeringType: string
  audienceTerms: readonly string[]      // 2–4
  namedRivals: readonly string[]        // rivals the site itself names
  vocabulary: readonly string[]         // the relevance guard's support set
  brandTokens: readonly string[]        // the own-brand drop set
}
export function deriveProfile(c: CostContext, a: { home: string; pricing?: string }): Promise<Measured<Profile>>
```

## Steps
1. Create `src/lib/market/questions/profile.ts` with the `Profile` interface verbatim.
2. Declare the Zod schema for the model's output: the seven fields above, `audienceTerms`
   bounded to 2–4 entries. A response that does not parse is a failed call — BP-009's
   seam already returns `unmeasured` for it; add no rescue path.
3. Implement `deriveProfile` as one `llm()` call with `site: 'profile'`, `tier: 'nano'`,
   `input` = `{ home, pricing }` as handed in. Pass the `CostContext` straight through.
4. Return the `Measured<Profile>` unchanged. Do not synthesise a category, do not
   fall back to the site's own marketing wording, and do not retry.
5. Emit the observability field BP-025's `## NFR budget` names for this step: the
   profile outcome kind (`measured` / `zero` / `unmeasured` + reason).
6. Write the tests. Watch each fail before the implementation exists (constitution §8).

## Test plan
| Acceptance criterion (verbatim from REQ-006) | Test |
|---|---|
| 1. "Given a completed report, when the AI-answers card renders, then it states in writing how many of the market's biggest searches the report measured — twelve where the market yielded twelve, and the smaller number it yielded where it yielded fewer — states on how many of those an AI answer appeared, and shows every citation count on the card against those two numbers and never against a count of searches the report did not measure." — *contributing arm only*: an `unmeasured` profile yields no market set and no questions, so the denominator is what was measured and never a padded twelve. | `tests/market/questions/profile.test.ts › deriveProfile/unmeasured-profile-yields-no-value` — an `llm()` stub returning `{ kind: 'unmeasured', reason: 'undeterminable' }` is returned unaltered, with no `value` field and no substituted category. |
| 13. "Given any of the 12 questions, when it renders, then the search shown beside it (criterion 9) is the search that question was selected from, and how the question is worded never changes which search it stands for: a differently worded question is still the same measured search, and no question stands for a search the report did not measure." — *contributing arm only*: the profile is the relevance guard's and the own-brand drop's support set, and nothing else in this file may reach the model. | `tests/market/questions/profile.test.ts › deriveProfile/single-call-site` — exactly one `llm()` call is made, its `site` is `'profile'` and its `tier` is `'nano'`; no keyword or search term is passed in the input. |

Discrimination (constitution §8, mutation testing): deleting the `site: 'profile'`
argument, or replacing the `unmeasured` pass-through with a default `Profile`, must
fail a named test above. State that in the validation report.

Additional tests this WO owes its own file plan (not criterion-mapped; they guard the
interface contract BP-025 fixes):
- `deriveProfile/schema-rejects-extra-fields` — a model response carrying a keyword or a
  selected search does not parse.
- `deriveProfile/audience-terms-bounded` — fewer than 2 or more than 4 `audienceTerms`
  does not parse.

## Out of scope
- Selection, scoring, phrasing and the card (`select.ts`, `phrase.ts`, `matrix.ts`) —
  WO-073, WO-074, WO-075, WO-076.
- The market set (`market-set.ts`) — WO-072.
- Fetching the home or pricing page: `deriveProfile` receives text. The fetch is
  BP-006's `safeFetch()` and the scan pipeline's (BP-012).
- Any pin in `src/lib/config/constants.ts` (BP-005) and any customer-visible string
  (BP-019). This file writes no sentence.
- REQ-006 criteria 2–12 — owned by WO-074, WO-075, WO-076 and by the report surface.

## Rollback
Delete both files. Nothing else imports them until WO-073 lands; no schema, no
migration, no stored shape is touched.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-025 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 2 rows of `## Test plan` quote REQ-006 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 finished — librarian — W4 wave-close done audit: all gates met, no open rests-on rows, regression recorded, status set done.
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-022, WO-026, WO-052); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 finished — planner — implements gains BP-011 (/workorder BP-011): this order's file plan creates the file that exports `deriveProfile`, which BP-011's ## Public interface declares and the leaf ships identically — capabilities.md: "the component's declaration is the contract and the leaf is the implementation"; no other field changes.
- 2026-09-04 finished — planner — /wave propose: **W4**, MoSCoW **Must**; `risk: high` set with its seam in `## Goal` (money · data leaving the system, doctrine 0.13.2 rule 2b). Declaration-first ground: it declares `Profile`, which ADR-095 decision 1 puts inside `MarketSet` and therefore inside the report blob.
- 2026-09-04 started — implementer — rule 1's refusal check passed (`status: approved`, `ui: no` so rule 1a does not apply); dispatch's own file plan named a modify row on `src/lib/config/constants.ts` that this WO's own `## File plan` explicitly rules out ("no pin is added to `src/lib/config/constants.ts`, which is BP-005's") — rule 4.2 objection, said once here: the WO on disk is authoritative and no pin was needed (`Profile` carries no numeric or membership constant BP-025 asks BP-005 to pin), so `constants.ts` is untouched, not amended.
- 2026-09-04 finished — implementer — `src/lib/market/questions/profile.ts` (Profile interface + deriveProfile, one `llm()` call, `site: 'profile'`, `tier: 'nano'`, input `{ home, pricing }` passed straight through, `Measured<Profile>` returned unaltered, observability log of the outcome kind/reason only) and `tests/market/questions/profile.test.ts` (10 tests, `@/lib/llm` stubbed, no network) land. Watched all 10 fail first (module absent), confirmed pass after. Mutation-tested both named seams directly against `src/lib/costs`'s real `CostContext` shape and `@/lib/llm`'s real exported signature: M1 (bypass `llm()`/the ledger entirely, fabricate a value) killed 10/10; M2 (an unintended `keyword` field added to the vendor payload) killed exactly 2/10 (`single-call-site`, the exact-payload test), zero collateral; the two mutations WO-071's own Discrimination note names — deleting `site: 'profile'` and replacing the `unmeasured` pass-through with a default `Profile` — each killed exactly 1/10, the exact named test. All reverted, working tree confirmed byte-identical to pre-mutation after each. `rests-on` row 1 dispositioned `confirmed` on this WO's own front-matter (reason stated there): `llm()`, `CostContext`, `Measured<T>` all present and `done` (WO-026, WO-276, WO-277); no `blocked-by` needed. `npm run typecheck`: clean. `npm run lint`: 8115, unchanged from baseline, none in `src/` or `tests/`. `npm test`: 57 files / 1406 passed / 1 skipped (baseline 56/1396/1 plus this WO's 1 file / 10 tests, no other file's count moved). No pin added to `src/lib/config/constants.ts` (see the objection above). Ready for the validator.

## Validation report (appended by validator)

## TST-041 — WO-071 validation report

### Scope
Independent verification of WO-071 on `wave/W3` tip `3caa2ae` (merge commit `3caa2ae`, feat commit `b95c2e0`). Read: this WO, BP-025, REQ-006, `tests/market/questions/profile.test.ts`, `src/lib/market/questions/profile.ts`. Risk: high — mutation-tested per doctrine 0.13.2.

### Criteria (WO-071 `## Test plan`, 2 rows quoted from REQ-006)
| # | Criterion (source) | Test | Verdict |
|---|---|---|---|
| 1 | REQ-006 c1, contributing arm: unmeasured profile yields no market set, no padded denominator | `deriveProfile/unmeasured-profile-yields-no-value` | PASS — discriminates (see mutation below) |
| 13 | REQ-006 c13, contributing arm: only the profile crosses to the model, nothing else | `deriveProfile/single-call-site` | PASS — discriminates (see mutation below) |

Plus 2 additional interface-contract tests the WO's own file plan owns (`schema-rejects-extra-fields`, `audience-terms-bounded`) and 6 supplementary tests (CostContext pass-through, zero-result pass-through, no self-retry, observability) — all present and passing (10/10 total).

### Mutation testing (risk: high, doctrine 0.13.2 rule 2b) — re-run independently
1. **Deleted the `site: "profile"` argument** from the `llm()` call — killed exactly 1 test (`deriveProfile/single-call-site`, `call.site` read `undefined`), zero collateral. Reverted.
2. **Replaced the `unmeasured` pass-through with a synthesised default `Profile`** — killed exactly 1 test (`deriveProfile/unmeasured-profile-yields-no-value`), zero collateral. This is the exact mutation the WO's own `## Goal` names as the cost-floor risk ("a test that survives deletion of the unreadable-home-page branch turns an unfetchable site into a paid call on empty input") — confirmed it does not survive. Reverted.
Both probes matched the implementer's own claimed results exactly. Tree confirmed clean after each (`git diff --stat` empty).

### Scope / rule 4.2 judgement
`git show b95c2e0 --stat` confirms the diff is exactly the 2-file plan (plus the WO doc). **`constants.ts` is genuinely absent from the diff** — independently confirmed, not taken on report; this was the dispatch's specific check, since the implementer's dispatch instruction named a `constants.ts` modify row that the WO's own file plan explicitly rules out ("no pin is added"). The implementer followed the artifact on disk over the dispatch, per rule 4.2, and was right to. The one model call goes through `llm()` (`@/lib/llm`) and nowhere else — no direct SDK import, no `fetch`, confirmed by reading the file's own import list.

### `rests-on` disposition (rule 2.3b)
One row, already `confirmed` by the implementer with grounds stated; independently re-verified this pass (front-matter edit above): `llm()`, `CostContext` and `Measured<T>` all read directly from their exporting files. No row left `open` on WO-071 itself.

### Gates (independently re-run this session)
- `npm run typecheck`: clean.
- `npm test` (full suite): **57 files / 1406 passed / 1 skipped**.
- `npm run lint`: **8115**, unchanged, none in this WO's files.
- `factory-console --check`: 0 errors / 59 warn / exit 0.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict
pass

Mutation: 2 probes (deleted `site: 'profile'` call site argument, substituted default-`Profile` for `Profile` type) — both killed exactly their named test; no false negatives.

Regression: 18 files · REQ-006 re-checked — pass

`git diff --stat 3caa2ae..bb9373e -- src/ tests/` is empty: no code has moved since TST-041's own tip. Full suite independently re-run, 7 runs on tip `bb9373e`: 6/7 clean at 57 files / 1406 passed / 1 skipped, 1/7 the documented intermittent `safe-fetch` timer flake, unrelated. `npm run typecheck` clean; `npm run lint` 8115, none under `src/`/`tests/`; `npm run build` clean against a throwaway, gitignored `.env.local` (deleted after, tree byte-clean). `factory-console impact WO-071`: 18 files → 2 blueprints (BP-011, BP-025) → 0 importers → 1 requirement (REQ-006). REQ-006's criteria 1 and 13 (the two contributing-arm rows this WO's own `## Test plan` quotes) were re-checked against current code, not restated: `tests/market/questions/profile.test.ts` re-run in isolation, 10/10 passed, `deriveProfile/unmeasured-profile-yields-no-value` and `deriveProfile/single-call-site` both exercised against the unchanged `src/lib/market/questions/profile.ts`. Surface re-check: `deriveProfile` is `llm()`'s only external consumer in the tree (`grep -rln "from [\"']@/lib/llm" src/` returns exactly `src/lib/market/questions/profile.ts` outside `src/lib/llm/` itself), so WO-026's llm-seam changes (none, per that WO's own regression line above) could not have moved this WO's behaviour. No `done` work order's files disturbed by this WO's own impact set.

## Log
- 2026-09-04 finished — librarian — W4 wave-close done audit: all gates met, no open rests-on rows, regression recorded, status set done.
- 2026-09-04 finished — validator — TST-041: both named mutations (deleted `site: 'profile'`, default-`Profile` substitution) independently re-run and confirmed to kill exactly their own named test; `constants.ts`'s absence from the diff independently confirmed; one `rests-on` row confirmed and closed. No open row remains on WO-071 itself.
- 2026-09-04 finished — validator — /regress (W4 wave-close pass): full suite (7 independent runs), typecheck, lint and build re-run clean on tip `bb9373e`; `factory-console impact WO-071` re-checked, REQ-006's two contributing-arm rows re-verified against unchanged code; confirmed `deriveProfile` is still `llm()`'s only external consumer. Regression: pass.
