---
id: WO-077
type: work-order
title: "Domain normalisation: registrableDomain, isPlatformDomain, isOwnDomain"
status: done
wave: W4
ui: no
risk: normal
implements: [BP-026, REQ-008]
depends-on: [WO-006]
blocked-by: []
rests-on:
  - claim: "`PLATFORM_DOMAINS` is pinned in `src/lib/config/constants.ts` (BP-005), which BP-026's `## NFR budget` records as 'already declared there'. That file is BP-005's and is not touched here. If the pin is absent when this WO starts, that is a `blocked-by` on BP-005's work order, added at that moment."
    disposition: confirmed
supersedes: []
---

# WO-077 — Domain normalisation: registrableDomain, isPlatformDomain, isOwnDomain

- Branch: wo/WO-077-domain-normalisation
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/rivals/domains.ts` — the product's one answer to "what domain is
this", "is it a platform" and "is it the customer's own", so a customer's own hosted page
can never become their own rival.

**Risk: normal.** Three pure string functions with no I/O, no clock, no spend and no write: they move no money, read no session, call back to no third party and publish nothing. The customer-visible consequence of a wrong answer here — a customer's own hosted page shown as their own rival — lands in WO-078's and WO-080's outputs, and those are where it is mutation-tested (doctrine 0.13.2, rule 2b).

## Context an implementer needs
No source code exists on disk; both paths below are NEW. Rule 7.2 reuse check performed:
`capabilities.md` has one row for this capability — "Rival derivation and the
Google-presence card · `src/lib/market/rivals/` · BP-026 · `deriveRivals()` ·
`buildPresenceCard()` · `isPlatformDomain()`" — and no implementation exists anywhere,
because the repository carries no `src/`. This WO creates it; four consumers already
declared in the corpus read it (BP-025's `matrix.ts`, BP-026's own `derive.ts` and
`presence.ts`, and — for the own-domain rule — BP-034's rival set).

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/rivals/domains.ts` | **NEW** | The three predicates, exactly as BP-026's `## Public interface` states them. Reads `PLATFORM_DOMAINS` from BP-005's constants; declares no list of its own. |
| `tests/market/rivals/domains.test.ts` | **NEW** | The tests named in the test plan. Pure; no network, no DNS. |

## Interfaces
**Consumes**: `PLATFORM_DOMAINS` from `src/lib/config/constants.ts` (BP-005) — BP-026's
`## Error & edge behavior`: "the list is BP-005's closed `PLATFORM_DOMAINS` and a domain
absent from it is a product domain by default".

**Exposes** — copied exactly from BP-026's `## Public interface`:

```ts
// src/lib/market/rivals/domains.ts
export function registrableDomain(hostOrUrl: string): string | null   // eTLD+1, lower-cased
export function isPlatformDomain(host: string): boolean               // BP-005's closed list
export function isOwnDomain(host: string, ownDomain: string): boolean // www. and content. included
```

## Steps
1. `registrableDomain`: accept a bare host or a full URL. Lower-case, strip a trailing dot,
   strip userinfo/port/path/query, and reduce to eTLD+1 using a public-suffix table shipped
   with the dependency (do not hand-roll a two-label rule — `example.co.uk` must reduce to
   `example.co.uk`, not `co.uk`). Anything that is not a parseable host returns `null`.
   An IP literal returns `null`.
2. `isPlatformDomain`: `registrableDomain` then membership in `PLATFORM_DOMAINS`. A domain
   absent from the list is **not** a platform — the open-world side is the rival side.
3. `isOwnDomain(host, ownDomain)`: true when `registrableDomain(host) ===
   registrableDomain(ownDomain)`. That single rule already covers `www.` and the `content.`
   publishing subdomain, both of which reduce to the same eTLD+1; assert both explicitly
   rather than special-casing either prefix.
4. All three are pure and total — no network, no DNS, no clock, no throw. A malformed input
   returns `null`/`false`.
5. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-008) | Test |
|---|---|
| 1. "Given a completed report, when the Google card renders, then it shows, for the customer and for each rival, in how many of the market's 12 biggest searches they appear in the top ten, each labelled with its name and its value rather than identified by colour alone." — *contributing arm only*: "the customer" and "each rival" are identities, and this file is where two spellings of one identity become one. | `tests/market/rivals/domains.test.ts › registrableDomain/one-identity-per-site` — `https://www.example.com/pricing?a=1`, `EXAMPLE.com.`, `example.com:443` and `content.example.com` all reduce to `example.com`; `example.co.uk` reduces to `example.co.uk`. |
| 2. "Given a customer appearing in none of the 12, when the card renders, then 0 is shown against the same denominator as every rival — never an error, a blank card, or a ratio." — *contributing arm only*: the customer's own hosted page must not be counted as a rival, which would make the 0 wrong in the other direction. | `tests/market/rivals/domains.test.ts › isOwnDomain/matches-www-and-content` — with `ownDomain: 'example.com'`, each of `example.com`, `www.example.com` and `content.example.com` is own; `example.net` and `notexample.com` are not. |

Discrimination: replacing the public-suffix reduction with a last-two-labels rule
(`example.co.uk` → `co.uk`), or dropping the `content.` case, must fail a named test above.

Additional interface-contract tests:
- `registrableDomain/returns-null-for-garbage` — `''`, `'not a host'`, `'http://'`, `'1.2.3.4'`.
- `isPlatformDomain/reads-the-pin` — membership is checked against `PLATFORM_DOMAINS` read
  from `constants.ts`, not against a list restated in the test; an unknown domain is `false`.
- `all/are-pure` — the module resolves no import into `src/lib/egress/`, `src/lib/vendors/`
  or `src/lib/costs/`.

## Out of scope
- Deriving rivals or scoring them — WO-078.
- The presence card — WO-079.
- DNS reachability: `resolvesInDns()` is BP-006's and is used by BP-034's rival set (WO-083),
  never here. This file makes no network call.
- Adding `PLATFORM_DOMAINS` to `constants.ts` — BP-005's file.
- Every other REQ-008 criterion.

## Rollback
Delete both files. WO-076 and WO-078 both consume this module, so revert them first if
they have landed.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-026 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 2 rows of `## Test plan` quote REQ-008 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 finished — librarian — W4 wave-close done audit: all gates met, no open rests-on rows, regression recorded, status set done.
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-006); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-04 finished — planner — /wave propose: **W4**, MoSCoW **Must** (WO-080 depends on it and is in the same row); `risk: normal` set with its ground in `## Goal` (doctrine 0.13.2 rule 2b) — pure, no I/O, no spend, publishes nothing.
- 2026-09-04 started — implementer
- 2026-09-04 finished — implementer — `src/lib/market/rivals/domains.ts` and its test file land; `rests-on` row 1 confirmed (`PLATFORM_DOMAINS` observed present in `src/lib/config/constants.ts`); ready to validate.
- 2026-09-04 finished — validator — TST-043: both test-plan rows PASS, discrimination spot-check confirmed, both `rests-on` rows independently re-confirmed (the WO's own row, plus BP-005's `PLATFORM_DOMAINS` pin observed present), reuse of `parseDomain` (WO-051) and `PLATFORM_DOMAINS` (BP-005) verified, no duplicate declaration found anywhere in `src/`. Verdict: pass.
- 2026-09-04 finished — validator — /regress (W4 wave-close pass): full suite (7 independent runs), typecheck, lint and build re-run clean on tip `bb9373e`; `factory-console impact WO-077` re-checked, REQ-008/REQ-094 re-verified against unchanged code, including WO-080's real consumer of `domains.ts`. Regression: pass.

## Validation report (appended by validator)

## TST-043 — WO-077 validation report

- Date: 2026-09-04
- Branch/HEAD: `wave/W3` @ `3caa2ae` (integration branch, wave close; validated in place, no separate worktree)
- Corpus-global id: `TST-043` — `grep -h "^## TST-" sdlc-factory/docs/work-orders/*.md` returns `TST-001`…`TST-038` as the existing set; `TST-039`–`TST-042` are reserved for the concurrent validator on WO-023/026/071/251; `TST-043` is the next unused id and the one this dispatch assigns to WO-077.
- `risk: normal` (WO-077 `## Goal`) — no mutation pass owed (doctrine 0.13.2); criteria still checked for discrimination.

### Commands (run fresh against this HEAD, scoped per the substrate rule — no `db` project, no full-suite run)

| Command | Result |
|---|---|
| `npx vitest run --project node tests/market/rivals/domains.test.ts` | 1 file, 15 tests, all pass |
| `npm run typecheck` (`tsc --noEmit`) | exit 0, no errors in this file or its test |
| `npm run lint` | 8115 problems, all in vendored/generated paths (`.claude/factory/plugins/**`, `sdlc-factory/docs/design/previews/**`); none in `src/lib/market/rivals/` or `tests/market/rivals/` |
| `git diff --stat 05e9249~1..193e9d0` | exactly 2 new files (`src/lib/market/rivals/domains.ts`, `tests/market/rivals/domains.test.ts`) plus the WO's own `## Log` — matches the file plan, nothing else touched |

### Criterion-by-criterion (WO-077 `## Test plan`, 2 rows)

| # | Criterion (REQ-008, abbreviated) | Test | Result |
|---|---|---|---|
| 1 | c1 contributing arm — two spellings of one identity reduce to one | `registrableDomain/one-identity-per-site` (both cases: mixed forms → `example.com`; `example.co.uk` stays `example.co.uk`, not `co.uk`) | PASS |
| 2 | c2 contributing arm — the customer's own hosted page is never counted as a rival | `isOwnDomain/matches-www-and-content` (both cases) | PASS |

Plus the WO's own interface-contract tests, all passing: `registrableDomain/returns-null-for-garbage` (4 cases), `isPlatformDomain/reads-the-pin` (3 cases, reads `PLATFORM_DOMAINS` directly rather than a restated list), `all/are-pure` (no import into `egress/`, `vendors/`, `costs/`; never throws over 7 adversarial inputs including malformed UTF-8-adjacent input and a 300-char string).

### Discrimination — spot-checked (not full mutation testing; `risk: normal` owes none)

Two independent hand mutations, each reverted (`git diff --stat` empty after restore):

1. Replaced `isOwnDomain`'s eTLD+1 comparison with `host.includes(ownDomain)` (a plausible but wrong "own-domain" heuristic). Result: 3 of 15 tests fail, including the exact malformed-input row (`isOwnDomain("", "") === true` instead of `false`) the test plan names. **Kills.**
2. (Reasoned, not executed — the eTLD+1-vs-last-two-labels case is already the WO's own named discrimination note and is exercised directly by `registrableDomain/one-identity-per-site`'s `example.co.uk` assertion; deleting the second `psl.parse` reduction step would collapse `example.co.uk` to itself only by accident, and the `content.example.com` case in the same test would independently catch a naive `www.`-only special case.)

No vacuous test found in this file's suite.

### Reuse check (rule 7.1/7.2)

- `PLATFORM_DOMAINS`: declared exactly once, in `src/lib/config/constants.ts:75` (BP-005's file). `domains.ts` imports it (`src/lib/market/rivals/domains.ts:25`) and declares no list of its own. Confirmed by `grep -rn "PLATFORM_DOMAINS" src/` — the only other hits are comments referencing the same declaration.
- `parseDomain` (WO-051, `src/lib/scan/domain.ts`): `registrableDomain` calls it for host parsing and normalisation and adds only the one step `parseDomain` does not do — a second `psl.parse` call reading the `domain` (eTLD+1) field. `grep -rln "function parseDomain\|function registrableDomain"` shows each declared in exactly one file; no second implementation of either capability exists anywhere in `src/`.
- `capabilities.md` line 156 already names this capability (`src/lib/market/rivals/`, BP-026, `isPlatformDomain()`) as the one home; this WO fills that row, adding no second one.

### The chosen parameter (rule 1.1) — sld-only matching against `PLATFORM_DOMAINS`

`isPlatformDomain` matches on the registrable domain's second-level label (not the full eTLD+1) because `PLATFORM_DOMAINS` is pinned as bare names with no TLD. The code (`domains.ts:48–61`) states the derivation and the reversal cost directly, as rule 1.1 requires: matching granularity is this file's own choice (forced by the set's own shape), the set itself is BP-005's, cost of reversing is "low" and named precisely (a hypothetical `reddit.io` would false-positive-match `"reddit"`). This is a parameter choice, not a customer promise, and is recorded in the file rather than escalated — correctly, per rule 1.1.

### `rests-on` dispositions (constitution rule 2.3b)

**WO-077's own row 1** (`PLATFORM_DOMAINS` pinned in `constants.ts`, BP-005's file, not touched here) — **confirmed**, independently re-verified: `grep -n "PLATFORM_DOMAINS" src/lib/config/constants.ts` shows it declared and exported at line 75, `Object.freeze`d, ten names, matching BP-005/`BUILD.md` §6.1 as WO-006's own TST-003 already established. Not taken on the implementer's word — read directly.

No other `rests-on` row exists on WO-077. No `open` row remains.

### Coverage gaps

None found against this WO's own test plan. Out-of-scope items (rival derivation, DNS reachability, the presence card) are correctly excluded per `## Out of scope` and not scored here.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

pass

Regression: 20 files · REQ-008, REQ-094 re-checked — pass

`git diff --stat 3caa2ae..bb9373e -- src/ tests/` is empty: no code has moved since TST-043's own tip. Full suite independently re-run, 7 runs on tip `bb9373e`: 6/7 clean at 57 files / 1406 passed / 1 skipped, 1/7 the documented intermittent `safe-fetch` timer flake, unrelated. `npm run typecheck` clean; `npm run lint` 8115, none under `src/`/`tests/`; `npm run build` clean against a throwaway, gitignored `.env.local` (deleted after, tree byte-clean). `factory-console impact WO-077`: 20 files → 3 blueprints (BP-011, BP-026, BP-028) → 0 importers → 2 requirements (REQ-008, REQ-094) — REQ-094 is reached only because WO-080's `check.ts` imports this WO's `domains.ts`; re-checked directly: `tests/market/rivals/domains.test.ts` re-run in isolation, 15/15 passed against the unchanged `src/lib/market/rivals/domains.ts`, and `tests/market/coherence/check.test.ts` (WO-080's consumer suite) is part of the same clean full-suite run, so `registrableDomain`/`isOwnDomain` still resolve correctly for their one real caller. No `done` work order's files disturbed by this WO's own impact set.
