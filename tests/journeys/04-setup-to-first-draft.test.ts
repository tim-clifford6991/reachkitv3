// tests/journeys/04-setup-to-first-draft.test.ts — BUILD §3, §4.3
//
// Journey: three decisions → deep pass → first draft already in the calendar (JN-002)
// Stub. Replace `describe.todo` with the end-to-end path the moment its
// surfaces exist; scripts/drift-audit.mjs reports this file as TODO until then.
import { describe } from "vitest";

describe.todo("three decisions → deep pass → first draft already in the calendar (JN-002)");
