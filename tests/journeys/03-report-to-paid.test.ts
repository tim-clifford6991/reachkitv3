// tests/journeys/03-report-to-paid.test.ts — BUILD §3, §13
//
// Journey: Start ReachKit → Checkout → webhook provisions → magic link → /setup (JN-002)
// Stub. Replace `describe.todo` with the end-to-end path the moment its
// surfaces exist; scripts/drift-audit.mjs reports this file as TODO until then.
import { describe } from "vitest";

describe.todo("Start ReachKit → Checkout → webhook provisions → magic link → /setup (JN-002)");
