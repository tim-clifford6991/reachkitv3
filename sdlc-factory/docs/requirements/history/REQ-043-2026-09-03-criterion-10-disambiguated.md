# REQ-043 — history — 2026-09-03

## Fold: criterion 10 disambiguated to the shared-line reading

**Trigger.** Research-introduced `REVIEW(ambiguity)` finding (branch
`research/ux-simplification`, merged `4034084`) on criterion 10.

**Finding.** "Every measured value shown in the detail carries the date of
the measurement it came from" read two ways that differ by five rendered
dates: per-value (the day panel's five "Why this page" rows plus REQ-096
criterion 4's rival band each carrying their own copy of the same Monday's
date) or as one shared statement (`BUILD.md` §4.6's "one dim provenance
line"). The finding ruled the per-value reading out on `00-project.md`
standing law 2 ("meaning over data") and left the choice to the analyst,
stating no promise changes either way — both readings state the same
provenance.

**Disposition: folded.** Settled from this requirement's own Source citation
in the Rationale, which already names the day panel's design as carrying
"one provenance line" (`BUILD.md` §4.6, cited verbatim in REQ-043's
Rationale). Criterion 10 rewritten from "every measured value ... carries the
date" to "one written line states the date they were measured, and that date
is not repeated separately beside each value" — closing the ambiguity in the
direction REQ-043 already cited, without adding or removing any promise.

**What was not changed.** Every other criterion, the rationale, non-goals,
placement (JN-003 steps 1, 2, 8, 9), status (`approved`, unchanged — no
promise moved, so the gate does not reopen).

**Review.** Ambiguity fold, derivable from REQ-043's own cited source (rule
1.3); round one only, self-run per `skills/review-rounds/SKILL.md` — the
rewrite is a closer transcription of the same cited "one provenance line",
not a new decision, so no second round is owed.
