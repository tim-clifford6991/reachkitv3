---
id: REQ-041
type: requirement
title: "Overview answers whether the work is working, and what needs the customer today"
status: draft
depends-on: [REQ-065, REQ-093]
blocked-by: []
rests-on:
  - claim: "Ranking data for the customer's root domain includes rows for pages served on the hosted `content.` subdomain, so pages ReachKit publishes register as the customer's own wins on the growth chart."
    disposition: open
supersedes: []
---

# REQ-041 — Overview answers whether the work is working, and what needs the customer today

- Priority: Must

## User story
As a paying customer, I want the first screen I land on to tell me whether the
work is paying off and whether anything is waiting on me, so that a five-minute
visit is enough to stay in control.

## Rationale
The single question a subscriber asks every week is "is this working?", and the
default screen must answer it from measured values rather than assertion, while
surfacing anything blocked on the customer instead of letting it sit unseen.
Source: `BUILD.md` §4.5 (growth chart of searches-you-appear-in by week; "max one
headline number per module; every value carries its delta or its goal, never
bare"; a 7-day strip and up to two alerts).

## Acceptance criteria
1. Given a signed-in customer, when they open the app, then Overview is the
   screen they land on.
2. Given at least one completed weekly measurement, when Overview renders, then
   it shows how many searches the customer appears in over time, with the current
   value labelled and carrying the date it was measured.
3. Given no weekly measurement has completed yet, when Overview renders, then in
   place of the chart it carries one written line saying that the first
   measurement has not run and the date it is due.
4. Given a headline number on Overview — the single number a module leads with,
   of which every module shows exactly one and never two — when it is rendered,
   then it carries either its change since the previous measurement or the goal it
   is moving toward, and never appears bare. Every headline number has a goal: a
   fixed value the product defines for that number, the same for every customer,
   shown with the number and what reaching it means. Where there is no previous
   measurement to compare against, the number carries its goal. A value a module
   shows alongside its headline number, for comparison or context, is not itself a
   headline number and this criterion does not bind it.
5. Given pages awaiting the customer's review, or items that cannot proceed
   without them, when they open Overview, then at most two are named, each with a
   single control that takes them straight to it, and where more exist the screen
   states how many remain and where to see them.
6. Given the current day of the week, when the customer opens Overview, then they
   can see which days of this week are done, which is today, and which are still
   to come.
7. Given a week Overview shows whose re-measurement did not run, or ran without
   completing every measurement, when Overview renders, then that week carries the
   account REQ-065 gives it: for a week that did not run, one written line that
   this week has not been measured and the day the next one is due (REQ-065
   criterion 3, "this week has not been measured"); for a week measured only in
   part, the values that were measured shown with their date and the rest stated
   as not measured (REQ-065 criterion 4, "what was not measured is stated"). In
   neither case is an earlier week's value labelled with that week's date.

## Non-goals
- A second score, or any number that answers no question the customer has.
- Model-written text anywhere on this screen — REQ-093 owns that line for the
  whole interface.
- Module order, chart geometry, tile widths and colour — design system.
- Drill-down analytics beyond the Calendar.
- The goal value each headline number carries — a pinned product constant
  (`00-project.md`, "`src/lib/config/constants.ts` | **Every** pinned number in
  the specification"), not a per-customer setting; criterion 4 fixes only that
  every headline number has one and never renders bare.

## Open questions

- [ ] REVIEW(ambiguity: criterion 4): "headline number" is never defined here, so which of Overview's rendered numbers the never-bare promise and the new goal obligation bind is unsettled — and the goal half now makes that scope load-bearing, because the build must pin one constant per headline number and cannot know how many. It decides directly whether REQ-042 criterion 3's rivals' absolute counts and the customer's own number must each carry a goal, or whether they are outside criterion 4 and may render bare.
