---
id: REQ-077
type: requirement
title: "Changing the address you sign in with never locks you out"
status: draft
depends-on: [REQ-064, REQ-076]
blocked-by: []
rests-on: []
supersedes: []
---

# REQ-077 — Changing the address you sign in with never locks you out

- Priority: Should

## User story
As a paying customer whose email address has changed, I want to move my account
to the new address safely, so that a mistyped address cannot leave me locked
out of a product I am still paying for.

## Rationale
ReachKit has no passwords — the email address is the account, so moving it is
moving the only key that exists, and one mistyped character must never be able
to lock a paying customer out of a product with no support desk to call: the
new address proves itself before the old one stops working, and until it does
the customer can see the change waiting, undo it, or correct it (BUILD.md §4.7,
"**Account** (name, email, magic-link note, change email, sign out)"; §1,
Supabase "magic-link auth").

## Acceptance criteria
1. Given a customer on Settings, when they open the account card, then their
   name and the address they sign in with are shown, alongside one written line
   saying ReachKit sends a sign-in link rather than using a password, and one
   saying invoices and receipts go to the address held in the billing portal
   and are changed there (REQ-076 criterion 2), not here.
2. Given the customer submits a new email address, when that address is not
   already a ReachKit account and is a valid address, then a sign-in link is
   sent to it and the old address keeps working until that link is used; when
   it is already an account or is not a valid address, then no link is sent,
   nothing changes, and one written line says why.
3. Given the link sent to the new address is used, when it succeeds, then only
   the new address can sign in, every mail ReachKit itself sends goes to it,
   and one `account` mail (REQ-064) goes to the old address saying the account
   now signs in at a different address and this one no longer can.
4. Given a change is pending, when the customer opens the account card, then it
   shows the address awaiting confirmation and lets them cancel the pending
   change or submit a different address; and the link expires 24 hours after it
   is sent, after which the pending change lapses and the account is unchanged.
5. Given the customer signs out, when they do, then that session ends and
   returning requires a fresh sign-in link.

## Non-goals
- Passwords, social sign-in, SSO or two-factor authentication.
- More than one person on an account: no seats, invitations or roles.
- Transferring an account to a different person or company.
- Changing the domain the site is measured and published under, which Settings
  does offer (REQ-070 criterion 1) and which is a different key from the one
  this requirement moves — what that change does is REQ-071's.
- Account recovery when both addresses are inaccessible.
- Changing the address Stripe invoices and receipts go to, which the customer
  does in the billing portal (REQ-076 criterion 2).
- Whether a mail may be sent at all. The rule is REQ-064 criterion 6's — a mail
  is legal when "a requirement names the occasion it was sent on and states
  whether the customer may stop that mail" — and the eight kind names there are
  its registry, which "records which kinds exist, and is not itself the test of
  what is allowed". Criterion 3 above supplies the occasion; `account` is the
  registry kind it carries; adding a kind is a change to REQ-064, not to this
  requirement.

## Open questions
