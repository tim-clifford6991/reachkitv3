---
id: REQ-097
type: requirement
title: "Stripe owns billing end to end — ReachKit renders no billing value and sends no billing mail"
status: approved
depends-on: [REQ-020, REQ-021, REQ-024, REQ-064, REQ-070]
blocked-by: [REQ-076, BP-060, BP-055]
rests-on:
  - claim: "Stripe's own billing surface, configured for this product, reaches all five of the things criterion 1 routes there. Two of the five are unverified in this corpus and BP-060 carries each as an open `rests-on` row — the VAT number (REQ-076's own row, carried forward) and the billing address invoices and receipts are sent to. If either is refuted, the promise that leg carries — REQ-076 criterion 9 for the VAT number, REQ-076 criterion 2 for the address — has no surface anywhere in the product, and this requirement is rewritten rather than patched. The other three legs — the card, the invoices and cancelling — are BUILD.md §13's and §4.7's, and BP-060 raises no row for them."
    disposition: undischargeable
    reason: "Architect's wave-close read, 2026-09-04 (rule 2.3b). This row is a roll-up of rows whose homes are elsewhere and it says so: of the five legs, three raise no assumption at all and the two that do are BP-060 row 1 (the VAT number) and BP-060 row 2 (the billing address), both still `open` and both dischargeable there. Nothing observed at this requirement settles it; it is discharged when those two are (rule 2.4 — one claim, one home)."
supersedes: []
---

# REQ-097 — Stripe owns billing end to end — ReachKit renders no billing value and sends no billing mail

- Priority: Must

## User story
As a paying customer, I want every part of paying — the card, the invoices, the
billing details and cancelling — to happen on Stripe's own surfaces and every
mail about a payment to come from Stripe, so that the company holding my billing
is the one showing it to me and ReachKit never shows me a figure that is out of
date or writes to me about a payment it did not take.

## Rationale
What must be true for the customer is that there is one place money lives and it
is not ReachKit: a billing figure ReachKit renders is a second copy of a fact
Stripe holds, and a second copy is what goes stale in front of a paying customer.
The owner's statement of 2026-09-02, the corpus clauses this requirement is cut
against, the backward pass and the choices taken under rule 1.1 are in
`registry/evidence/REQ-097.md`. It contradicts approved artifacts and declares
that as `blocked-by` rather than overwriting them (rule 2.3): **REQ-076 criteria
1, 3, 5 and 7** — the four billing values Settings shows, the screen shown at the
moment cancellation is confirmed, and the billing details Settings still offers
after the paid-through date — and the two approved blueprints that publish those
same behaviours as interfaces, **BP-060** (`billingSummary`'s `nextInvoice` and
`card`, and `cancelSubscription`, whose comment reads "Returns the exact date
access ends so the confirming screen can state it") and **BP-055** (the `ACTIONS`
tuple's `'invoices'` and `'cancel'`). REQ-070 and REQ-024 are `depends-on`, not
conflicts: REQ-070's criteria 1 and 2 are ceilings — "nothing beyond this list is
offered" — and criterion 1 offers strictly less than they permit; REQ-024's mails
on a completed payment are the one exception criterion 4 names. Both open
questions must be ruled before this or REQ-076's criteria can be approved.
Placement: JN-004 step 5. What it reaches: no code — this repo carries no `src/`
and the design preview app carries no billing surface — and BP-016's `MAIL_KINDS`
and BP-017's `portalLink` are confirmed rather than moved; each clause is cited in
the evidence file's backward pass.

## Acceptance criteria
1. Given a customer on Settings, when they open billing (REQ-076 criterion 1),
   then they are offered a control that takes them to Stripe's own billing
   surface, and that surface is where every one of the five things money touches
   is done — the card, the invoices, the billing address, the VAT number, and
   cancelling. Given a customer anywhere else in ReachKit, when they look for any
   of the five, then the only thing ReachKit offers for it is a control to that
   same one destination: ReachKit offers no separate control per item and
   presents no field, form, stepper, cancellation control, confirmation step or
   consequence screen for any of them.
2. Given a customer who has gone to that surface to do any of the five, when they
   come back to ReachKit — having done it, or having done nothing — then they
   return to Settings still signed in, as REQ-076 criterion 2 promises for the
   four it names and so, here, for cancelling.
3. Given a founder acting on an offer to subscribe — the report's pricing card,
   or any other price surface (REQ-021) — or a cancelled customer resuming
   (REQ-076 criterion 6), when a payment is to be taken from them, then ReachKit
   presents no card field, no payment form and no billing form of its own at any
   point on that path: every step that asks the customer for anything about
   paying happens on Stripe's own checkout or on the billing surface criterion 1
   names.
4. Given Stripe records a billing event against a customer's record — a charge
   taken, an invoice issued or upcoming, a renewal paid or failed, a card
   expiring or changed, a refund, a chargeback, a cancellation — when it does,
   then ReachKit sends no mail on that occasion: the mails REQ-024 states on a
   completed payment, and the two notices REQ-076 criterion 11 states on
   ReachKit's own clock, are the only ReachKit mails a payment lies behind. Given
   any mail ReachKit sends, when the customer reads it, then neither its subject
   nor its body states an amount charged or due, an invoice, a renewal date, the
   card, or that a payment failed — the only payment fact a ReachKit mail carries
   is what REQ-024 states: that a completed payment succeeded, and what it did or
   did not open.
5. Given a customer who wants the date or amount of their next invoice, which
   card is on file, or what they have been invoiced before, when they go to find
   out, then no ReachKit surface states any of those values — a screen or a mail
   alike — and Settings offers criterion 1's control in their place.
6. Given criterion 1's billing surface or criterion 3's checkout cannot be
   produced — Stripe unreachable, or a session refused for any other reason —
   when the customer or the founder acts on the control, then they are told in
   writing, on the screen they were on, that billing cannot be reached, that they
   may try again, and one way to reach a person; nothing about their plan or
   their account changes, a signed-in customer stays signed in, and no ReachKit
   field, form, payment step or cancellation control stands in its place.

## Non-goals
- Resuming: the control and the promise are REQ-076 criterion 6's and this
  requirement does not move them; criterion 3 adds only that no ReachKit form
  takes its payment.
- The plan itself. There is one plan and no change to it (REQ-076's non-goal), so
  criterion 1 routes nothing about the plan to Stripe.
- What a cancelled customer keeps and until when, the paid-through date, and the
  access gate the weekly and daily loops read — REQ-076.
- The two written notices a customer with hosted pages is owed before serving
  stops: REQ-076 criterion 11 states their occasions, their count and their
  channel. Criterion 4 names them only to place them on ReachKit's side of the
  line.
- Which fields Stripe's surfaces are configured to offer and where each sits —
  REQ-076 criterion 2's non-goal already places that with the blueprint.
- The price and the plan name: €49/mo is a public product fact stated on every
  price surface (REQ-021, REQ-022 criterion 1), not a value Stripe holds about one
  customer, and criterion 5 does not reach either.
- What checkout records against the billing record — REQ-022. What a founder may
  be asked at checkout and what may be asked before payment — REQ-020 criteria 1
  and 6; criterion 3 adds only that no ReachKit-rendered payment or billing form
  stands anywhere on that path.
- The webhook that opens an account from a completed payment, and everything a
  founder who has paid is owed until they are inside — REQ-024. Provisioning is
  ReachKit's and this boundary does not move it.
- Refunds, chargebacks and failed renewals, deferred past launch by owner ruling
  (REQ-076): criterion 4 states only that ReachKit sends no mail on them.
- Anything on Stripe's own surfaces — their wording, layout, fields or mail.
- Which kinds of mail exist and where that register lives: REQ-064's non-goal
  assigns it to the mail blueprint, and the examples of Stripe's own mail it names
  illustrate criterion 4's list rather than bounding it. Criterion 4 adds no
  kind, removes none and changes no row.

## Open questions
- [ ] REQ-076 criterion 1 promises Settings shows four things: the plan and
      price, the date and amount of the next invoice, the card on file, and a way
      to reach invoices. Criteria 1 and 5 above keep the plan and its price
      readable in ReachKit and withhold the other three, offering the control in
      their place. Which holds — Settings reads Stripe and shows the next invoice
      date and the card, or Settings shows the plan, the price and the control and
      nothing else? (owner: user)
- [ ] REQ-076 criteria 3 and 7, and BUILD §4.7's line "cancelling keeps
      everything running until {date}", put three statements on a ReachKit screen
      at the moment cancellation is confirmed: the exact date access ends, that
      the export stays open with no end date, and the day hosted pages stop being
      served. Criterion 1 leaves no such screen. Does ReachKit state those three
      before handing the customer to Stripe, or afterwards in an `account` mail
      sent when the cancellation reaches ReachKit (which criterion 4 would then
      name as a further exception), or does the line go? (owner: user)

> One page (rule 2.1). Changelog lives in `requirements/history/`, not here.
> Evidence lives in `registry/evidence/REQ-097.md`.
