# Evidence — REQ-071 (changing the domain, the market or the rival set)

Source: `BUILD.md` v1.1, 2026-08-28 (in this repo, at `/BUILD.md`).

- §4.7, Settings, verbatim: "**Your market** (chip + Edit + \"changing this
  rebuilds the search set and the 12 questions next Monday\") · **Competitors**
  (chips ×5, add/remove)".
- §6.7 step 5, verbatim: "On the paid side the customer confirms the category at
  setup, may edit it in Settings, and the question set **freezes after
  confirmation** so week-on-week movement is comparable; it re-derives only when
  the category changes."
- §6.1, caps table, verbatim: "| `COMPETITORS_MAX` | 5 |".
- §6.6, verbatim: "**The cold-start law: every derivation in the product must
  work for a domain that ranks for nothing**".
- §6.6, verbatim: "**the customer can always type a rival manually** — a
  cold-start founder knows their competitors even when no dataset does."
- §17 (out of scope), verbatim: "community outreach of any kind."

The weekly event these changes take effect on is REQ-065's; its own cadence
evidence (`BUILD.md` §11, "`weekly/refresh` | Mon 06:00 UTC") sits with that
requirement, not here (rule 2.4).

Why this sits here and not in the requirement: which BUILD section and which
config constant fixes the five-competitor ceiling is source evidence a REQ must
not carry (rule 2.1). REQ-071 states only what the customer may change, when the
change takes effect, and what the change may never be made to look like.

No implementation exists to cite: this repo carries no `src/`
(`00-project.md`, "Greenfield. No source code on disk yet").
