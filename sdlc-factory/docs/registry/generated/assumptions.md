<!-- GENERATED by scripts/build-registry.mjs from front-matter under sdlc-factory/docs/{requirements,blueprints,decisions,work-orders,feedback,journeys}/.
     DO NOT HAND-EDIT — edits here are silently overwritten on the next `factory-console registry` and `factory-console registry --check` fails on drift.
     To change what this file says, change the front-matter of the artifact it describes and regenerate. -->

# Assumptions

`rests-on` is projected from 282 of 444 nodes — the rest are silent on it. This is rule 5.5's own example: a generated file whose source field is empty everywhere looks identical to a healthy one unless it says so.

Every `rests-on` entry across the graph, grouped by disposition. `claim` and `accepted-by` are projected verbatim from the artifact's own front-matter (stored data, not extracted prose); the argument for the disposition stays in the artifact's body.

## open

| Artifact | Claim | Accepted by | See |
|---|---|---|---|
| ADR-002 | Manual handling of removal requests at the published address can meet REQ-002 criterion 2's five-working-day promise at v1 volumes; there is no automation behind it and no queue the product exposes | undefined | sdlc-factory/docs/decisions/ADR-002.md |
| ADR-010 | Every surface the product renders or sends is reachable from a repo-relative path glob — src/app/**, src/app/(hosted)/**, src/lib/mail/** and src/ui/** — so a surface added later is in scope without anyone adding it to a list. A surface rendered from a path outside those globs is invisible to every suite this decision creates. | undefined | sdlc-factory/docs/decisions/ADR-010.md |
| ADR-020 | A maintained public-suffix list is available to the build (the ICANN/Mozilla PSL is the de facto one), so 'has a public suffix' is decidable without a network call at request time | undefined | sdlc-factory/docs/decisions/ADR-020.md |
| ADR-051 | Supabase RLS policies can be written so that every request-scoped read through `db()` excludes rows belonging to a user with a non-null `deleted_at`, including reads that join through `sites`, `drafts` and `publications`, without each caller adding a predicate | undefined | sdlc-factory/docs/decisions/ADR-051.md |
| ADR-052 | A Stripe Price created with `tax_behavior: 'inclusive'` continues to charge the same gross amount if automatic tax is later enabled on the account, deriving the tax from within the amount rather than adding to it | undefined | sdlc-factory/docs/decisions/ADR-052.md |
| ADR-052 | A Stripe Price's `tax_behavior` cannot be changed after creation, so a price created `exclusive` must be replaced and every existing subscription migrated to correct it | undefined | sdlc-factory/docs/decisions/ADR-052.md |
| ADR-070 | BP-015's node implementing REQ-056 c2 leaves `needs_attention -> generating` reachable only from a customer action, and no scheduler, retry or job holds a reference to that edge. | undefined | sdlc-factory/docs/decisions/ADR-070.md |
| ADR-071 | The node cut from REQ-065 exposes, per site and week, whether that week was not measured at all (c3) or measured only in part (c4), and which measurements the partial week holds. BP-051 distinguishes `no_week` from `not_measured` from that distinction alone. | undefined | sdlc-factory/docs/decisions/ADR-071.md |
| ADR-072 | The node cut from REQ-065 exposes, per site and week, whether that week was not measured at all (c3) or measured only in part (c4), and which measurements the partial week holds. BP-051 distinguishes `no_week` from `not_measured` from that distinction alone. Carried forward unchanged from ADR-071. | undefined | sdlc-factory/docs/decisions/ADR-072.md |
| ADR-072 | Nothing in the product ever fetches a published page's address after the 24-hour check, so no later observation can contradict a `page_not_found` cause. REQ-062's and REQ-063's non-goals both state it and REQ-065 criterion 1's weekly re-measurement visits no page; this is what makes Decision 5's terminality safe rather than merely stated. It is an assumption about every future node under BP-015 and BP-012, not only about the two that exist. | undefined | sdlc-factory/docs/decisions/ADR-072.md |
| ADR-080 | A post created by us in a customer's WordPress carries a marker we can find again on a later request, so a retry whose first outcome was never recorded finds the existing post instead of creating a second one. The mechanism is BP-048's; that one exists at all is assumed here. | undefined | sdlc-factory/docs/decisions/ADR-080.md |
| ADR-081 | The WordPress REST API lets an application-password user with `edit_posts` set an existing post's status back to `draft` on a post that user's own credential created, without any other field of that post changing. BP-048's `rests-on` already assumes the write path for creation and for the SEO fields; that the same path can set status back is assumed here and is not verified in this corpus. | undefined | sdlc-factory/docs/decisions/ADR-081.md |
| ADR-081 | A publication row records whether ReachKit itself made the page live at that destination, so the two populations this ruling separates are decidable from stored data and never from a re-read of the customer's site. `publications.live_url` is null for every WordPress delivery today (BP-048's data-model delta), so the discriminator cannot be `live_url` and BP-045 must carry an explicit one — see Decision 2. | undefined | sdlc-factory/docs/decisions/ADR-081.md |
| ADR-082 | The WordPress REST API lets an application-password user with `edit_posts` set an existing post's status back to `draft` on a post that user's own credential created, without any other field of that post changing. Carried forward unchanged from ADR-081; BP-048's `rests-on` already assumes the write path for creation and for the SEO fields, and that the same path can set status back is assumed here and is not verified in this corpus. | undefined | sdlc-factory/docs/decisions/ADR-082.md |
| ADR-082 | A publication row records whether ReachKit itself made the page live at that destination, so the two populations this ruling separates are decidable from stored data and never from a re-read of the customer's site. Carried forward unchanged from ADR-081. `publications.live_url` is null for every WordPress delivery (BP-048's data-model delta), so the discriminator cannot be `live_url` and BP-045 carries an explicit one. | undefined | sdlc-factory/docs/decisions/ADR-082.md |
| ADR-082 | A WordPress REST request can tell 'the post is no longer there' (a 404 for a post id we hold) apart from 'the site could not be reached' (DNS failure, connection reset, timeout, 5xx) reliably enough to choose between the `already_gone` and `unreachable` arms. The two arms carry different promises to the customer — nothing is theirs to remove, versus the post may still be live — so a mapping that collapses them would make one of REQ-056 criterion 16's outcomes unreachable. BP-048's `errors.ts` is where the mapping lives; nothing in this corpus verifies that a misconfigured host cannot answer 404 for an unreachable site. | undefined | sdlc-factory/docs/decisions/ADR-082.md |
| ADR-083 | Every WordPress site a customer can connect lets ReachKit give the posts it creates there something the customer can list posts by in their own WordPress admin and can tell from posts they wrote themselves, under the application password they gave. Inherited verbatim in substance from REQ-060's own `rests-on` row, which the requirements-analyst raised and could not discharge. Which attribute that is, and whether every install and every credential permits one, is unverified in this corpus. | undefined | sdlc-factory/docs/decisions/ADR-083.md |
| ADR-083 | An application-password user holding `edit_posts` — the capability BP-048 already assumes for creating a post and writing its SEO fields — can also assign a tag term to the post it is creating, and can cause that term to exist if it does not. WordPress's default role map is what this rests on and nothing in this corpus verifies it; if it is refuted, the stamp needs a capability BP-048 does not assume and this decision's mechanism changes while its shape (two marks, never one) does not. | undefined | sdlc-factory/docs/decisions/ADR-083.md |
| ADR-083 | A term assigned to a post at creation is visible to the customer in their own wp-admin post list, and filterable there, without a plugin. This is what makes the stamp answer REQ-060 criterion 6 at all; unverified here. | undefined | sdlc-factory/docs/decisions/ADR-083.md |
| ADR-084 | The WordPress REST API lets an application-password user create a post with `status: 'publish'` in a single request and returns that post's public permalink in the same response, so a delivery is live at a known address with no second call. Nothing in this repository has spoken to a WordPress install (`00-project.md`, 'Greenfield. No source code on disk yet'), so this is assumed and not verified here. | undefined | sdlc-factory/docs/decisions/ADR-084.md |
| ADR-084 | An application-password user that holds `edit_posts` but not `publish_posts` — WordPress's Contributor role in the default map — can still create a post, and WordPress silently holds it at `draft` or `pending` rather than honouring `status: 'publish'` or refusing the request. This is the whole ground for Decision 3; if it is refuted and such a request is refused outright, Decision 3's probe becomes belt-and-braces rather than the only thing standing between the owner's ruling and a silently drafted post, and its shape does not change. | undefined | sdlc-factory/docs/decisions/ADR-084.md |
| ADR-084 | The WordPress REST API lets an application-password user with `edit_posts` set an existing post's status back to `draft` on a post that user's own credential created, without any other field of that post changing. Carried forward unchanged from ADR-082, which carried it from ADR-081. Unverified in this corpus. | undefined | sdlc-factory/docs/decisions/ADR-084.md |
| ADR-084 | A WordPress REST request can tell 'the post is no longer there' (a 404 for a post id we hold) apart from 'the site could not be reached' (DNS failure, connection reset, timeout, 5xx) reliably enough to choose between ADR-082's `already_gone` and `unreachable` arms. Carried forward unchanged from ADR-082; BP-048's `errors.ts` is where the mapping lives and nothing in this corpus verifies that a misconfigured host cannot answer 404 for an unreachable site. | undefined | sdlc-factory/docs/decisions/ADR-084.md |
| ADR-084 | A publication row records whether ReachKit itself made the page live at that destination, so ADR-082's two unpublish populations stay decidable from stored data and never from a re-read of the customer's site. Carried forward from ADR-082 and now standing on a *different* footing: `publications.live_url` is no longer null for WordPress rows, so the wrong derivation `live_url != null` now over-classifies where it used to under-classify. Decision 4. | undefined | sdlc-factory/docs/decisions/ADR-084.md |
| ADR-085 | A page-address fetch can tell an unambiguous 404 or 410 from every other answer reliably enough to choose between `page_not_found` and `could_not_confirm`. This is ADR-084's fourth `rests-on` row, raised there for a WordPress post-id fetch through `BP-048`'s `errors.ts`, and it is now load-bearing for `BP-049`'s fetch of the page's own public address as well — a different call, against a different host, with the same failure mode. Nothing in this repository has fetched a customer's page (`00-project.md`, 'Greenfield. No source code on disk yet'); a misconfigured host that answers 404 for a site that is in fact unreachable would put a page in the wrong arm, and the arms have opposite consequences (Decision 4). | undefined | sdlc-factory/docs/decisions/ADR-085.md |
| ADR-085 | REQ-062 criterion 4's `page_not_found` and REQ-063 criterion 6's `page_not_found` cause are the same event recorded once — BP-049 writes it and BP-051 reads it — so the corpus never derives 'no page was found' twice from two fetches. True by construction here because there is only ever one fetch; it stops being true the moment anything else looks at a published page's address, which REQ-062's and REQ-063's non-goals both forbid. | undefined | sdlc-factory/docs/decisions/ADR-085.md |
| ADR-086 | An application-password user that holds `edit_posts` but not `publish_posts` can be told apart from one that can publish, over the REST API, without creating a post — the probe ADR-084 Decision 3 names as `canPublish(cfg)`. Carried forward from ADR-084's second `rests-on` row, on which this file's whole occasion depends. Nothing in this repository has spoken to a WordPress install (`00-project.md`, 'Greenfield. No source code on disk yet'). | undefined | sdlc-factory/docs/decisions/ADR-086.md |
| ADR-091 | The console's file -> blueprint derivation (rule 5.6) resolves a file that matches several nodes' `code:` globs to the node with the most specific match, or reports it under each. Where it reports the file under each without ranking, this decision still fixes which node is answerable, and the overlap is a reporting artefact rather than an unowned or doubly-owned file. | undefined | sdlc-factory/docs/decisions/ADR-091.md |
| ADR-092 | A lint rule can be expressed over import specifiers that resolve inside `src/lib/publish/`, so the directory partial order in Decision 3 is machine-checkable rather than review-checkable. `structure.md` rule 6 already asserts the module-level version of this ('a cycle between `src/lib/` modules is a build failure'), and BP-001 decision 2 already places a cross-module lint invariant in the root toolchain. No such rule exists yet in a repository with no code; the work order that first creates `src/lib/publish/types.ts` is where it lands. | undefined | sdlc-factory/docs/decisions/ADR-092.md |
| BP-001 | Vercel serves the App Router deployment and the Inngest endpoint from one build, so a job and a page share one process image and one set of environment bindings | undefined | sdlc-factory/docs/blueprints/BP-001.md |
| BP-002 | Supabase's managed `auth.users` is the identity of record and the product's own `users` row is a profile keyed to it, so deleting an account means deleting both | undefined | sdlc-factory/docs/blueprints/BP-002.md |
| BP-003 | Inngest (or Vercel cron plus a queue) delivers at-least-once with a durable retry and a per-function concurrency limit, so every job below is written idempotent rather than exactly-once | undefined | sdlc-factory/docs/blueprints/BP-003.md |
| BP-004 | A single Vercel deployment can accept wildcard `content.{customer-domain}` CNAMEs and route by Host header without a per-customer deployment or certificate step the customer must take | undefined | sdlc-factory/docs/blueprints/BP-004.md |
| BP-008 | Labs `ranked_keywords` on a root domain returns rows for pages served on the `content.` subdomain; if it does not, hosted pages' wins are invisible to the growth chart and the client must query with subdomain inclusion or track the subdomain as a second target (BUILD §16 milestone 2 is the first measurement) | undefined | sdlc-factory/docs/blueprints/BP-008.md |
| BP-016 | Six of ADR-040's nine template directories — `magic-link`, `draft-ready`, `published`, `weekly`, `setup-reminder`, `account` — have no leaf `code:` glob, so `src/lib/mail/templates/<kind>/**` resolves to **this node** by the module anchor (structure.md rule 2). That is the arrangement ADR-040 Alternative 3 rejected, and this node is carrying it by default rather than by decision. It is not fixed here: ADR-040 point 2 assigns each directory to the node owning that kind's occasions, and for five of the six that node is a leaf of another component (BP-046, BP-049, BP-051/BP-050, BP-061, BP-033), so applying it literally would create five new instances of structure.md rule 2b where the corpus has one — restructuring with no file on disk (rule 2.2a). `account`'s occasions span REQ-024, REQ-074, REQ-076, REQ-077 and REQ-079, so ADR-040's own rule names five owners and picks none. Assessed 2026-09-02 by the architect; the trigger to resolve it is the first template file, and the resolution is an ADR amending ADR-040 point 2. Nothing is blocked: no file exists and no work order writes one. | undefined | sdlc-factory/docs/blueprints/BP-016.md |
| BP-017 | Stripe's customer portal can be configured so the customer adds or corrects a tax ID (VAT number) themselves; BUILD.md §13 names the portal only for card and cancellation and puts the VAT field at Checkout, so this is asserted from outside the corpus (REQ-076's own rests-on row) | undefined | sdlc-factory/docs/blueprints/BP-017.md |
| BP-017 | Stripe Checkout collects and stores the billing-address country on every purchase with no extra configuration (REQ-022's rests-on row) | undefined | sdlc-factory/docs/blueprints/BP-017.md |
| BP-020 | Every string a surface renders can be reached from a repo-relative path glob over src/app/**, src/app/(hosted)/**, src/lib/mail/** and src/ui/**, so the conformance suite of ADR-010 enumerates surfaces added later without being told about them. A surface rendered from a path outside those globs is invisible to the suite. | undefined | sdlc-factory/docs/blueprints/BP-020.md |
| BP-020 | Model-produced text reaches a surface only through a bounded set of stored columns — a draft's body, title, slug and description, an opportunity's proposed title and slug, and a question's wording — so the suite can name the fields whose flow into a surface it must gate. | undefined | sdlc-factory/docs/blueprints/BP-020.md |
| BP-021 | Every place a value or a module can sit is reachable from the App Router tree under src/app/**, src/app/(hosted)/** and the mail templates under src/lib/mail/**, so PLACES can be made total over the rendered surface by enumeration rather than by memory. A place rendered only inside a client-only modal that no route renders is invisible to that enumeration. | undefined | sdlc-factory/docs/blueprints/BP-021.md |
| BP-021 | One canonical cold-start fixture — a domain with no measured ranking and no AI citation of its own — can drive every engine end to end without a vendor call, because BP-007's cache seam and BP-008's closed endpoint list can both be stubbed at the module boundary. Without that, REQ-091 criterion 1 is only assertable against live vendors. | undefined | sdlc-factory/docs/blueprints/BP-021.md |
| BP-021 | Each place whose wording a sibling requirement already fixes (REQ-041 c3, REQ-043 c3/c4/c5, REQ-065 c3/c4, REQ-010 c1) contributes exactly one CopyKey and no place gains a second account. REQ-091's own non-goal makes this a design obligation on this node; nothing in the corpus proves the set of such places is closed. | undefined | sdlc-factory/docs/blueprints/BP-021.md |
| BP-022 | The address a report names for a removal request is an owner-written string; BP-019's copy registry holds it under one key and this node renders it. No value for it exists in the corpus yet | undefined | sdlc-factory/docs/blueprints/BP-022.md |
| BP-022 | A person, not the product, writes the `domain_blocks` row that removes a report, and does so inside REQ-002 criterion 2's five working days; ADR-002 carries the same claim for the operation as a whole | undefined | sdlc-factory/docs/blueprints/BP-022.md |
| BP-022 | Vercel presents the visitor's address in `x-forwarded-for`, so the network key BP-023 counts by is derivable at the edge without a platform-specific header this node invents | undefined | sdlc-factory/docs/blueprints/BP-022.md |
| BP-023 | The free battery — own fetches plus 12 live SERP calls plus the suggestion call — completes within 60 s at the p95 of vendor response time; `BUILD.md` §16 milestone 3 is the first measurement of this chain. Inherited from REQ-003's own row, restated here only because this node holds the deadline that acts on it | undefined | sdlc-factory/docs/blueprints/BP-023.md |
| BP-023 | Counting by network address means several founders behind one office or mobile network share one hourly allowance and one in-flight slot. Inherited from REQ-003 | undefined | sdlc-factory/docs/blueprints/BP-023.md |
| BP-023 | `x-forwarded-for`'s first entry is the visitor's address on the deployment platform, so the network key is derivable without trusting a client-supplied header | undefined | sdlc-factory/docs/blueprints/BP-023.md |
| BP-023 | BP-005 gains `FREE_RESCAN_WINDOW_D: 7`, `FAILURE_COOLDOWN_H: 24`, `DAILY_WINDOW_H: 24` and `HOURLY_WINDOW_H: 1`; the first two are `BUILD.md` §6.4's figures and pins are BP-005's by `structure.md` rule 5, so this node cannot declare them itself | undefined | sdlc-factory/docs/blueprints/BP-023.md |
| BP-029 | A free report's stored blob is immutable for the scan it belongs to, so the offer a visitor saw when they submitted is the offer the giveaway is written from; only a newer current scan for the same domain can change what is on offer between render and submit | undefined | sdlc-factory/docs/blueprints/BP-029.md |
| BP-029 | Resend reports a hard bounce or a permanent rejection distinguishably from a transient failure, so criterion 8's 24-hour retry window is spent on retriable failures rather than on an address that will never accept mail | undefined | sdlc-factory/docs/blueprints/BP-029.md |
| BP-030 | Stripe Checkout collects and stores the billing-address country on every purchase with `billing_address_collection: 'required'` and no further configuration (REQ-022's own rests-on row; BUILD.md §13 'collect the buyer's country (Stripe does automatically)') | undefined | sdlc-factory/docs/blueprints/BP-030.md |
| BP-030 | Stripe's `tax_id_collection` never blocks or delays completion of a Checkout Session on the basis of a tax ID's registry status, so REQ-022 criterion 7's 'no check stands between them and their purchase' holds with the field enabled | undefined | sdlc-factory/docs/blueprints/BP-030.md |
| BP-030 | A Stripe account can be configured so that no presentment-currency conversion (Adaptive Pricing or equivalent) is applied to a EUR price for a buyer outside the euro area, which REQ-022 criterion 4 requires | undefined | sdlc-factory/docs/blueprints/BP-030.md |
| BP-030 | A tax ID collected by `tax_id_collection` at Checkout is stored against the Stripe customer created by that session, and is rendered on every invoice Stripe issues for that customer thereafter — not only on the first. REQ-022 criterion 6's second half is 'what they enter appears on their billing record and their invoices', plural and ongoing, and this node writes the number once at checkout and never again. Three adjacent rows on this node and BP-060 cover the collection, the portal's edit path and the billing address; none covered this, and it is the half the criterion is actually about | undefined | sdlc-factory/docs/blueprints/BP-030.md |
| BP-031 | A site address given at setup is enough to run the onboarding deep pass with no prior free scan (REQ-021's own rests-on row) | undefined | sdlc-factory/docs/blueprints/BP-031.md |
| BP-032 | Stripe delivers `checkout.session.completed` for a completed subscription Checkout Session at least once, and a session that never completed produces no such event — so the signature-verified webhook is a sufficient as well as a necessary provisioning trigger (BUILD.md §13, 'Webhook (signature-verified, the only provisioning path)') | undefined | sdlc-factory/docs/blueprints/BP-032.md |
| BP-033 | A founder reaching /setup always has an open account behind their payment (REQ-024 criterion 6), so the screen never has to render for a payment with no user row | undefined | sdlc-factory/docs/blueprints/BP-033.md |
| BP-034 | `competitors_domain` returns a measured-empty set, not an error, for a domain with no ranked presence — the setup card's only rival source when no report backs the account. If it errors instead, the card shows `seeking → none_found` on a failure it cannot distinguish from a true empty, and the two must be split. `BUILD.md` §16 milestone 2 is the first point at which it can be measured. | undefined | sdlc-factory/docs/blueprints/BP-034.md |
| BP-036 | The onboarding deep pass completes fast enough that a founder will wait on a progress screen rather than leave (inherited from REQ-029; nothing in the corpus measures it, and the ten-minute release is the design that makes the claim non-load-bearing) | undefined | sdlc-factory/docs/blueprints/BP-036.md |
| BP-038 | Ranking data for the customer's root domain includes rows for pages served on the hosted content. subdomain, so pages ReachKit publishes register as the customer's own wins on the growth chart (inherited from REQ-041) | undefined | sdlc-factory/docs/blueprints/BP-038.md |
| BP-038 | Ten ranked searches is the point below which a rival-to-customer ratio misleads more than it informs (inherited from REQ-041; BUILD.md 6.6 pins the threshold, nothing measures it) | undefined | sdlc-factory/docs/blueprints/BP-038.md |
| BP-039 | For every post ReachKit delivered to a customer's WordPress, an address inside that WordPress that opens that post for whoever is signed in there can be formed **from what ReachKit recorded at delivery** — the site's base URL and the post id. Carried from REQ-043's own `rests-on` row, which the 2026-09-01 fold corrected: the assumption is not about the application password the customer gave, which is not the credential the signed-in customer arrives with. Where none can be formed for a post, criterion 12's second way cannot be offered and the detail says so in its place. | undefined | sdlc-factory/docs/blueprints/BP-039.md |
| BP-040 | BP-008's `rankedKeywords()` reports a domain's total ranked count, not merely the number of rows it fetched. Where it reports only rows fetched, a count equal to the row cap is a floor and not a count, and this node treats it as `unmeasured` for criterion 9's bar rather than as a value at the cap. | undefined | sdlc-factory/docs/blueprints/BP-040.md |
| BP-040 | BP-011 exports the deterministic intent classifier behind `selectTwelve()` (`BUILD.md` §6.7 step 3's `intentWeight`) as a callable, so the ranking formula's intent term is the same classification the twelve searches were selected by rather than a second copy of it (rule 2.4). | undefined | sdlc-factory/docs/blueprints/BP-040.md |
| BP-041 | The onboarding deep pass, on the pinned battery, can yield 30 qualifying unused opportunities for a customer who ranks for nothing. No yield has been measured; `BUILD.md` §16 milestone 6 is the first point at which it can be. Carried from REQ-047's parent requirement REQ-095 because this node is where the target 30 is spent and where a systematic shortfall would first be visible in `pursueDepth`'s stop reason. | undefined | sdlc-factory/docs/blueprints/BP-041.md |
| BP-041 | The three written lines criteria 3, 5 and 6 require are owner-owed and not yet written. This node emits `SupplyNotice` handles; until BP-019's copy registry holds the strings, no surface can render the telling REQ-095 promises. | undefined | sdlc-factory/docs/blueprints/BP-041.md |
| BP-042 | BP-010 stores, or can re-read from `fetches`, the rendered text of every page it measured for a site, so REQ-050 c9's comparison set ('every page on the customer's own site the product has published or measured') is available without a fresh fetch of the customer's site at generation time. | undefined | sdlc-factory/docs/blueprints/BP-042.md |
| BP-042 | The private-figure register (REQ-050 c3) is buildable from the stored report blob and the site's opportunities alone — every ranking position, search volume, visibility and answer count the product holds for a site is in one of those two places. | undefined | sdlc-factory/docs/blueprints/BP-042.md |
| BP-043 | BP-015's `becomesPublishable()` consults this node's `claimRecheckOutstanding()` before every hand-off — veto expiry, customer approval, publish time, automatic retry and customer-started retry alike. REQ-053 c5's promise that no approval, schedule or retry can put a page carrying a forbidden claim on the customer's site is enforced at that one call site and nowhere else; nothing in this node can compel it. | undefined | sdlc-factory/docs/blueprints/BP-043.md |
| BP-043 | The settings write path that records a change to `sites.do_not_claim` calls `sweepOutstandingRechecks()` for that site. Correctness does not rest on this — the hash guard holds without it — only timeliness does. | undefined | sdlc-factory/docs/blueprints/BP-043.md |
| BP-045 | Postgres row-level locking on the `sites` row plus a unique index on `(draft_id, destination_id)` is sufficient serialisation for the publish claim; no advisory lock or external mutex is needed because at most one publish per site per calendar day is ever due | undefined | sdlc-factory/docs/blueprints/BP-045.md |
| BP-047 | A published page's question-and-answer section is available structurally on the draft row (`drafts.meta.faq`, written by BP-014 at generation) rather than only as prose inside `body_md`, so `FAQPage` markup is emitted from data and never from a render-time parse of markdown | undefined | sdlc-factory/docs/blueprints/BP-047.md |
| BP-048 | A WordPress site with Yoast or RankMath installed exposes, over the REST API to an application-password user with `edit_posts`, a writable path for that plugin's SEO title and description on a post — whether by registered post meta, by a plugin-supplied namespace under `/wp-json/`, or not at all. Which of those holds, and under which plugin versions, is unverified in this corpus. | undefined | sdlc-factory/docs/blueprints/BP-048.md |
| BP-048 | A post created by us carries a marker we can find again on a later request — post meta written at creation and readable back by the same application-password user — so a retry whose first outcome was never recorded finds the existing post instead of creating a second one | undefined | sdlc-factory/docs/blueprints/BP-048.md |
| BP-048 | Every WordPress site a customer can connect lets ReachKit give the posts it creates there something the customer can list posts by in their own wp-admin and can tell from posts they wrote themselves, under the application password they gave. Raised by the requirements-analyst on REQ-060 and carried here undischarged: this repository has never spoken to a WordPress install and there is nothing in it to discharge it with. Where an install permits none, REQ-060 criterion 6 cannot be met there and REQ-079 criterion 6's mail has no place to name — which is REQ-060's own stated consequence (ADR-083 Decision 4). | undefined | sdlc-factory/docs/blueprints/BP-048.md |
| BP-048 | An application-password user holding `edit_posts` can assign a `post_tag` term to the post it is creating, and can cause that term to exist if it does not. WordPress's default role map is what ADR-083 Decision 1 rests on in choosing a tag over a category; nothing in this corpus verifies it. | undefined | sdlc-factory/docs/blueprints/BP-048.md |
| BP-048 | The WordPress REST API lets an application-password user create a post with `status: 'publish'` in a single request and returns that post's public permalink in the same response, so a delivery is live at a known address with no second call. ADR-084's first `rests-on` row, carried here because this node is where the call is made. Unverified in this corpus. | undefined | sdlc-factory/docs/blueprints/BP-048.md |
| BP-048 | An application-password user that holds `edit_posts` but not `publish_posts` can still create a post, and WordPress silently holds it at `draft` or `pending` rather than honouring `status: 'publish'` or refusing the request. ADR-084's second `rests-on` row, and the whole ground for `canPublish`. If it is refuted and such a request is refused outright, the probe becomes belt-and-braces rather than the only thing standing between the owner's ruling and a silently drafted post, and its shape does not change. | undefined | sdlc-factory/docs/blueprints/BP-048.md |
| BP-049 | A fetch of a page's own public address can tell an unambiguous 404 or 410 from every other answer reliably enough to choose between REQ-062 criterion 4's second and third outcomes. This is ADR-084's fourth `rests-on` row, raised there for a WordPress post-id fetch, and it is load-bearing here for a different call against a different host (ADR-085). A misconfigured host answering 404 for a site that is in fact unreachable puts a page in the arm that stops its verdicts forever (ADR-072). | undefined | sdlc-factory/docs/blueprints/BP-049.md |
| BP-049 | A page served by a customer's CMS carries its own identity in server-rendered markup — a canonical link or `og:url` resolving to the fetched address, or failing both a `<title>` matching the published title — even where its body is withheld behind client JavaScript. This is what lets `could_not_confirm` ("what came back was not the page ReachKit published") be decided separately from `aiReadable: false` (a page whose body a script-less reader cannot see). Nothing in this corpus has fetched a customer's page; if it is refuted for some CMS, a JavaScript-gated page reads as not-confirmed instead of as failing the one check that exists to catch it. | undefined | sdlc-factory/docs/blueprints/BP-049.md |
| BP-051 | No node under BP-015 writes `publications.verdict` (BUILD.md §10). This node is the single home of a page's verdict and keeps a row per page per week; a column holding one latest verdict would be the second copy and could not answer REQ-063 c6's 'the date of the last verdict it did receive'. | undefined | sdlc-factory/docs/blueprints/BP-051.md |
| BP-051 | The four verdict words and the one written line criterion 6 requires are owner-owed and not yet written. This node emits handles; until BP-019's copy registry holds the strings, no surface and no mail can render a verdict. | undefined | sdlc-factory/docs/blueprints/BP-051.md |
| BP-051 | An `unblock` acceptance whose `Barrier` is absent from `WeekMeasurements.gatesCleared` has **no declared `NotJudgeableCause`**. `evaluateAcceptance` produces two causes and both are read from the other two maps (`positions`, `namesCustomer`); WO-204's file plan asserts exactly those two, and its own test row expects an absent barrier to be "undecided rather than failing" — undecided as what is not stated anywhere. Two readings are open and neither is derivable from REQ-063 or REQ-047 as they stand: it is a partly measured week (`not_measured`, no row, REQ-065 c4) or it is a sixth cause. Raised 2026-09-02 by the architect against this node's own interface; not decided here, because the choice changes which pages carry a permanent `not_judgeable` row and that is REQ-063 c6's promise, not a parameter. Nothing is blocked: no work order writes the `gate_cleared` branch yet. | undefined | sdlc-factory/docs/blueprints/BP-051.md |
| BP-052 | A rival's total count of searches it appears in is readable beside the 100 rows `rankedKeywords` fetches for it, so a band can be cut at 500 from a 100-row call. Origin: REQ-096's own open row. If refuted, `RivalSize.rankedCount` caps at 100, the `far` band becomes unreachable, and REQ-041's how-far-ahead ratio is refuted with it — the fix is shared with that requirement, not owned here. | undefined | sdlc-factory/docs/blueprints/BP-052.md |
| BP-053 | Resend accepts one request carrying both an HTML body and a plain-text alternative, so criterion 5's guarantee is a property of the request this node builds rather than of a second send | undefined | sdlc-factory/docs/blueprints/BP-053.md |
| BP-054 | The three stop facts REQ-092 names — a spend cap reached, the kill switch engaged, a step that failed — are each readable per site and per day from records the corpus already provides: BP-007's cap-hit ledger over `fetches`, BP-003's `killSwitchEngaged()`, and BP-012's `status: 'degraded' | 'failed'` on a scan. If any of the three cannot be read that way, a stop is unobservable and REQ-092 criterion 3 cannot be met without a new migration topic, which `structure.md` rule 3's closed topic list does not admit. | undefined | sdlc-factory/docs/blueprints/BP-054.md |
| BP-054 | The date the work is expected to resume is derivable without an owner ruling: a spend cap resumes at the start of the next cap window, a failed step at the next run of its job, and an engaged kill switch promises no time at all. Nothing in the corpus states the cap window's reset, so the first of the three rests on a fact BP-007 must expose. | undefined | sdlc-factory/docs/blueprints/BP-054.md |
| BP-054 | Pages prepared before a stop survive it because they are rows BP-015's state machine owns, and a stop writes no state transition. REQ-092 criterion 5 is therefore a property of BP-015's transition table rather than of this node; this node asserts it and does not implement it. | undefined | sdlc-factory/docs/blueprints/BP-054.md |
| BP-058 | The moment a customer last signed in is readable through BP-017 (Supabase auth's own `last_sign_in_at`), so REQ-074 criterion 6's condition — the customer has not signed in since the destination broke — can be evaluated without minting a column on BP-017's `users` topic | undefined | sdlc-factory/docs/blueprints/BP-058.md |
| BP-058 | `draft/generate` is invoked once per active site per day even on a day where supply is exhausted and no draft is written, so the once-per-breakage mail of criterion 6 has a daily occasion without a seventh job id | undefined | sdlc-factory/docs/blueprints/BP-058.md |
| BP-060 | Stripe's customer portal can be configured so that a customer adds or corrects the tax ID (VAT number) on their own billing record there. BUILD.md §13 names the portal only for card and cancellation and puts the VAT ID field at Checkout; criterion 2 rests on it (REQ-076's own rests-on row, carried forward unchanged) | undefined | sdlc-factory/docs/blueprints/BP-060.md |
| BP-060 | Stripe's customer portal can be configured to change the billing address invoices and receipts are sent to, which REQ-076 criterion 2 and REQ-077 criterion 1 together make the only surface offering it | undefined | sdlc-factory/docs/blueprints/BP-060.md |
| BP-060 | Cancelling a Stripe subscription with `cancel_at_period_end` leaves `current_period_end` unchanged, so `paid_through` needs no separate arithmetic (REQ-076 criterion 3's 'the exact date their access ends') | undefined | sdlc-factory/docs/blueprints/BP-060.md |
| BP-061 | Supabase's magic-link auth can be driven so that the one-time token is delivered through BP-016's `sendEmail()` shell rather than Supabase's own mailer, which REQ-064's one-shell rule and BUILD.md §12 require of every mail the product sends | undefined | sdlc-factory/docs/blueprints/BP-061.md |
| BP-063 | The grounding passages REQ-050 criterion 1 keeps unchanged are stored in `drafts.grounded_fact` and nowhere else, so purging that column is the whole of what REQ-079 criterion 7 requires of them | undefined | sdlc-factory/docs/blueprints/BP-063.md |
| REQ-003 | The free battery — own fetches plus 12 live SERP calls plus the suggestion call — completes within 60 s at the p95 of vendor response time. BUILD §16 milestone 3 is the first measurement of this chain, and is therefore also the point at which tightening the promise below 60 s is reconsidered — see `registry/evidence/REQ-003.md`, 'Why not 10 seconds (owner considered and ruled, 2026-08-31)' | undefined | sdlc-factory/docs/requirements/REQ-003.md |
| REQ-003 | Visitors are counted by network address, so several founders behind one shared network share one hourly allowance and one in-flight slot | undefined | sdlc-factory/docs/requirements/REQ-003.md |
| REQ-015 | Visitors are counted by network address, so several founders behind one shared network share one hourly allowance and one in-flight slot | undefined | sdlc-factory/docs/requirements/REQ-015.md |
| REQ-021 | A site address given at setup is enough to run the onboarding deep pass with no prior free scan | undefined | sdlc-factory/docs/requirements/REQ-021.md |
| REQ-022 | Stripe Checkout collects and stores the billing-address country on every purchase without extra configuration (BUILD §13) | undefined | sdlc-factory/docs/requirements/REQ-022.md |
| REQ-023 | Stripe Checkout collects and stores the billing-address country on every purchase without extra configuration (BUILD §13) | undefined | sdlc-factory/docs/requirements/REQ-023.md |
| REQ-023 | Stripe's customer portal can add or correct a customer's tax ID after checkout. No in-corpus source establishes it: BUILD.md §13 states only 'VAT ID field on' at Checkout and 'Portal for card/cancel'. Criterion 4's correction route was placed there by owner ruling (2026-08-29) from knowledge outside the corpus; the vendor behaviour itself is unverified (rule 1.2). | undefined | sdlc-factory/docs/requirements/REQ-023.md |
| REQ-029 | The onboarding deep pass completes fast enough that a founder will wait on a progress screen rather than leave | undefined | sdlc-factory/docs/requirements/REQ-029.md |
| REQ-041 | Ranking data for the customer's root domain includes rows for pages served on the hosted `content.` subdomain, so pages ReachKit publishes register as the customer's own wins on the growth chart. | undefined | sdlc-factory/docs/requirements/REQ-041.md |
| REQ-041 | Ten ranked searches is the point below which a rival-to-customer ratio misleads more than it informs, so the ratio module stays locked until the customer reaches it. | undefined | sdlc-factory/docs/requirements/REQ-041.md |
| REQ-042 | Ten ranked searches is the point below which a rival-to-customer ratio misleads more than it informs, so the ratio module stays locked until the customer reaches it. | undefined | sdlc-factory/docs/requirements/REQ-042.md |
| REQ-043 | For every post ReachKit delivered to a customer's WordPress, ReachKit can form, from what it recorded at delivery, an address inside that WordPress that opens that post for whoever is signed in there. Whether every install permits one is unverified in this corpus — REQ-060 carries the parallel assumption for finding those posts in bulk. Where none can be formed for a post, criterion 12's second way through cannot be offered and the detail says so in its place. | undefined | sdlc-factory/docs/requirements/REQ-043.md |
| REQ-047 | A top-10 containing a domain that ranks for no more than the greater of 500 searches and five times the customer's own count marks a target a small site can realistically win, and leaves a non-empty winnable set for a customer at zero. | undefined | sdlc-factory/docs/requirements/REQ-047.md |
| REQ-047 | A domain ranking for no more than the greater of 100 searches and twice the customer's own count reads to the customer as a site roughly their own size, which is what the word Winnable promises them. The floor is 100 rather than 500 because at 500 this expression and criterion 9's are equal for every customer ranking below 250 searches, so every queued target banded Winnable and the Reach band was unreachable for the cold-start founder. | undefined | sdlc-factory/docs/requirements/REQ-047.md |
| REQ-049 | A top-10 containing a domain that ranks for no more than the greater of 500 searches and five times the customer's own count marks a target a small site can realistically win, and leaves a non-empty winnable set for a customer at zero. | undefined | sdlc-factory/docs/requirements/REQ-049.md |
| REQ-049 | A domain ranking for no more than the greater of 100 searches and twice the customer's own count reads to the customer as a site roughly their own size, which is what the word Winnable promises them. The floor is 100 rather than 500 because at 500 this expression and criterion 1's are equal for every customer ranking below 250 searches, so every queued target banded Winnable and the Reach band was unreachable for the cold-start founder. | undefined | sdlc-factory/docs/requirements/REQ-049.md |
| REQ-050 | Three hundred characters is the opening a reader judges a page by, so a page that reaches the end of it without naming the brand is answering the search rather than funnelling the reader. | undefined | sdlc-factory/docs/requirements/REQ-050.md |
| REQ-050 | Eighty-five per cent similarity, by one fixed measure applied to every comparison, is the point at which two pages compete with each other rather than each earning its own place. | undefined | sdlc-factory/docs/requirements/REQ-050.md |
| REQ-052 | Three hundred characters is the opening a reader judges a page by, so a page that reaches the end of it without naming the brand is answering the search rather than funnelling the reader. | undefined | sdlc-factory/docs/requirements/REQ-052.md |
| REQ-054 | Eighty-five per cent similarity, by one fixed measure applied to every comparison, is the point at which two pages compete with each other rather than each earning its own place. | undefined | sdlc-factory/docs/requirements/REQ-054.md |
| REQ-060 | Every WordPress site a customer can connect lets ReachKit give the posts it creates there something the customer can list posts by in their own WordPress admin and can tell from posts they wrote themselves, under the application password they gave. Which attribute that is, and whether every install and every credential permits one, is unverified in this corpus; where a connected site permits none, criterion 6 cannot be met and REQ-079 criterion 6's mail has no place to name. | undefined | sdlc-factory/docs/requirements/REQ-060.md |
| REQ-076 | Stripe's customer portal can be configured so that a customer adds or corrects the tax ID (VAT number) on their own billing record there. Nothing in this corpus establishes that capability: BUILD.md §13 names the portal only for card and cancellation, and puts the VAT ID field at Checkout. Criterion 2 rests on it (rule 1.2 — the vendor behaviour is asserted from outside the corpus and is unverified in it). | undefined | sdlc-factory/docs/requirements/REQ-076.md |
| REQ-079 | A connected WordPress site permits one place that brings every post ReachKit created there up together, so criterion 6's mail has a place to name — the same assumption REQ-060 carries for its criterion 6, unverified in this corpus. Where a site permits none, criterion 6's mail says so and still carries its counts; the customer is then left with pages live on their own domain that no product surface can point them to. | undefined | sdlc-factory/docs/requirements/REQ-079.md |
| REQ-095 | The onboarding deep pass, on the pinned battery (12 question SERPs, up to 13 target SERPs, rival ranked rows, two suggestion calls, up to 25 measured pages), can yield 30 qualifying unused opportunities for a customer who ranks for nothing — the default customer under the cold-start law. No yield has been measured; `BUILD.md` §16 milestone 6 ('Real supply fills the calendar; empty days honest') is the first point at which it can be. | undefined | sdlc-factory/docs/requirements/REQ-095.md |
| REQ-095 | Seven unused opportunities — one week of pages — is the count at which a customer must be told supply is short, because the weekly re-measurement (REQ-065 criterion 1) is the only event that replenishes supply and cannot arrive sooner than the following Monday. | undefined | sdlc-factory/docs/requirements/REQ-095.md |
| REQ-096 | A rival's total count of searches it appears in is available from the same monthly rival measurement the how-far-ahead ratio already rests on, and is not limited to the rows that measurement fetches — the contrast the product already promises (a rival at 12,400 against a customer at 25) is unreadable otherwise. No implementation has verified it; `BUILD.md` §16 milestone 2 is the first point at which it can be. | undefined | sdlc-factory/docs/requirements/REQ-096.md |
| REQ-096 | The two counts that separate the three bands — 100 searches or twice the customer's own, and 500 searches or five times the customer's own — read to a customer as the difference between a rival roughly their size, one bigger but still catchable, and one out of their league. Derivation and reversal cost: `registry/evidence/REQ-096.md`. | undefined | sdlc-factory/docs/requirements/REQ-096.md |
| REQ-097 | Stripe's own billing surface, configured for this product, reaches all five of the things criterion 1 routes there. Two of the five are unverified in this corpus and BP-060 carries each as an open `rests-on` row — the VAT number (REQ-076's own row, carried forward) and the billing address invoices and receipts are sent to. If either is refuted, the promise that leg carries — REQ-076 criterion 9 for the VAT number, REQ-076 criterion 2 for the address — has no surface anywhere in the product, and this requirement is rewritten rather than patched. The other three legs — the card, the invoices and cancelling — are BUILD.md §13's and §4.7's, and BP-060 raises no row for them. | undefined | sdlc-factory/docs/requirements/REQ-097.md |
| REQ-099 | A demo video of the running product will exist and be hosted somewhere the page can embed it. Nothing in this corpus produces, hosts or budgets one, and no milestone in BUILD §16 names it; criterion 5 rests on it, and criterion 6 is what holds the page together if it is refuted. | undefined | sdlc-factory/docs/requirements/REQ-099.md |
| WO-002 | `sdlc-factory/docs/design/tokens.md` and `components.md` are `draft`/empty. Rule 7.3's preview cannot be signed until the design-guardian has registered BP-018's tokens and components; this work order's `ui: yes` is what holds it until then. | undefined | sdlc-factory/docs/work-orders/WO-002.md |
| WO-007 | BP-019's `BAND_LABELS`, `SCORE_BANDS` and `SEVERITY` exist in `src/lib/presentation/` before the ADR-001 disjointness block can run. The nine words they carry were ruled by the owner on 2026-08-31 and are recorded in BP-019 decision 6, so the words are no longer the obstacle — but no work order in the corpus carries `implements: [BP-019]`, so those three maps have no author. Reported with this block's return; the block is written to assert, not to skip, and fails loudly until they exist | undefined | sdlc-factory/docs/work-orders/WO-007.md |
| WO-009 | `fetches` is one of `BUILD.md` §10's nine but `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic, and BP-007's `## Data model delta` fully specifies the table including its unique key and its index. Derivation (rule 1.1): the baseline creates the eight tables no other node owns and BP-007's own migration (WO-021) creates `fetches`, so one table has one writer. Reversal cost: moving one `create table` between two migrations that both run once, before any data exists — cheap today, a data migration later, which is why it is recorded now. | undefined | sdlc-factory/docs/work-orders/WO-009.md |
| WO-009 | `publications` carries no `verdict` column, `BUILD.md` §10's key-column list notwithstanding: BP-002's data-model delta states the exclusion and BP-051 decision 1 holds the reasoning. The baseline is written without it and BP-051's `page_verdicts` is the home of a page's standing. | undefined | sdlc-factory/docs/work-orders/WO-009.md |
| WO-010 | `users.deleted_at` does not exist until BP-063's `*_users_erasure_*.sql` lands. The policy condition ADR-051 requires is therefore written here against a column this migration adds a nullable placeholder for, or the policies are re-issued by BP-063's migration. The implementer takes the first: the baseline gains no column, this policy migration adds `users.deleted_at timestamptz` as its own precondition, and BP-063 owns every rule about setting it. | undefined | sdlc-factory/docs/work-orders/WO-010.md |
| WO-015 | The engine function each definition calls — `runScan()` (BP-012) — does not exist in this repo and is outside this block. The definitions are written against BP-012's `## Public interface` verbatim and are tested with that function doubled. The jobs cannot run end-to-end until BP-012's own work orders land. | undefined | sdlc-factory/docs/work-orders/WO-015.md |
| WO-016 | `generateDraft()` (BP-014), `transition()`, `publish()` and `verifyLive()` (BP-015) do not exist in this repo and are outside this block. The definitions are written against those nodes' `## Public interface` verbatim and tested with the functions doubled. | undefined | sdlc-factory/docs/work-orders/WO-016.md |
| WO-017 | The five due-work queries `account/maintenance` calls — `paymentsAwaitingSignIn()`, `paymentsWithoutAccounts()` (BP-032), `sitesDueHostingEndNotice()`, `sitesDueHostingStop()` (BP-060) and `accountsDueForPurge()` (BP-063) — and `advanceSequences()` (BP-029) do not exist in this repo and are outside this block. The tick is written against those nodes' public interfaces verbatim and tested with all six doubled. | undefined | sdlc-factory/docs/work-orders/WO-017.md |
| WO-021 | This migration creates `fetches`, which `BUILD.md` §10 lists among its nine baseline tables. WO-009 deliberately leaves it out: `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic and BP-007's `## Data model delta` fully specifies the table, so one table has one writer. The derivation and its reversal cost are recorded on WO-009. | undefined | sdlc-factory/docs/work-orders/WO-021.md |
| WO-023 | `Measured<T>` is BP-010's (`src/lib/measure/`, per `registry/capabilities.md`) and does not exist in this repo. BP-008's whole public interface returns it. This work order declares a local type-only import from `@/lib/measure` and cannot type-check until BP-010's work order lands; the type is consumed, never re-declared here (rule 7.1). The type fan-out check the planner would normally run — make the type edit alone, run the project's type-check, take the erroring files as the plan — could not be run: the repo holds no TypeScript to check. | undefined | sdlc-factory/docs/work-orders/WO-023.md |
| WO-024 | BP-008's own open assumption stands and is not discharged here: whether Labs `ranked_keywords` on a root domain returns rows for pages served on the `content.` subdomain. `BUILD.md` §16 milestone 2 is the first measurement, so this work order neither confirms nor refutes it and adds no subdomain handling on speculation. | undefined | sdlc-factory/docs/work-orders/WO-024.md |
| WO-026 | `Measured<T>` is BP-010's (`src/lib/measure/`, per `registry/capabilities.md`) and does not exist in this repo. `llm()` returns it. The type is consumed by a type-only import and never re-declared here (rule 7.1); this work order cannot type-check until BP-010's work order lands. The planner's type fan-out check could not be run: the repo holds no TypeScript to check. | undefined | sdlc-factory/docs/work-orders/WO-026.md |
| WO-028 | BP-004's `code:` glob is `src/app/(hosted)/**`, and its leaf BP-047 globs five narrower paths inside it — `layout.tsx`, `resolve-host.ts`, `[slug]/**`, `robots.ts`, `sitemap.ts` — which is every concrete file BP-004's public interface names. Under `structure.md` rule 2 the narrower glob owns each of them, so what remains to BP-004 itself is the container scaffold: the unknown-Host 404 and the cache-invalidation policy. This work order is carved to exactly that remainder so it shares no file with BP-047's work orders, which another planner cut. | undefined | sdlc-factory/docs/work-orders/WO-028.md |
| WO-028 | `sdlc-factory/docs/design/tokens.md` and `components.md` are `Status: draft` and empty. Rule 7.3's preview cannot be signed until the design-guardian registers BP-018's tokens and components; this work order's `ui: yes` is what holds it until then. | undefined | sdlc-factory/docs/work-orders/WO-028.md |
| WO-029 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds: "no approved requirement states the design system, which is why `satisfies` is empty and the M1 work orders hang off this node directly." Every acceptance criterion below is therefore quoted from BP-018 and `BUILD.md` §2.1; none is inherited from a requirement, and none was invented to fill the gap. Reported with this block's return. | undefined | sdlc-factory/docs/work-orders/WO-029.md |
| WO-029 | `sdlc-factory/docs/design/tokens.md` is `Status: draft` with an empty table and states in its own body that all UI work orders are blocked while it is. The token VALUES are not missing from the corpus — `BUILD.md` §2.1 states them exactly — so this work order transcribes them from there. What is missing is the design-guardian's registration of them in `tokens.md`, which rule 7.3's preview sign-off depends on. `ui: yes` is what holds this work order until that lands. | undefined | sdlc-factory/docs/work-orders/WO-029.md |
| WO-029 | `BUILD.md` §2.1's dark block states `--ok`, `--warn`, `--bad` and adds `/* + matching -bg/-line at 12%/28% alpha */`. The six dark `-bg`/`-line` values are therefore DERIVED at those two alphas from the three stated hues (rule 1.1), not invented and not carried over from light. Each derived value carries a comment quoting that clause. Reversal: six CSS lines. | undefined | sdlc-factory/docs/work-orders/WO-029.md |
| WO-030 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Every acceptance criterion below is quoted from BP-018 and `BUILD.md` §2.3; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-030.md |
| WO-030 | `sdlc-factory/docs/design/tokens.md` is `Status: draft` and empty; rule 7.3's preview sign-off waits on the design-guardian registering the type scale alongside the colour tokens. `ui: yes` holds this work order until then. | undefined | sdlc-factory/docs/work-orders/WO-030.md |
| WO-031 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2 and §2.5; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-031.md |
| WO-031 | `sdlc-factory/docs/design/components.md` is an empty registry with no rows. BP-018 decision 1 makes entry in that file the precondition of export: "a new component enters `sdlc-factory/docs/design/components.md` as `proposed` with its data contract and states before it can be exported." The eight components below must be registered there by the design-guardian — with the data contract and states this work order's preview shows — before this work order can be signed off. The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-031.md |
| WO-032 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-032.md |
| WO-032 | The seven components below must be registered in `sdlc-factory/docs/design/components.md` — an empty table today — by the design-guardian before this work order can be signed off (BP-018 decision 1). The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-032.md |
| WO-033 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2 and §2.5; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-033.md |
| WO-033 | Both components must be registered in `sdlc-factory/docs/design/components.md` with their data contract and states before this work order can be signed off (BP-018 decision 1). The planner may not write that file. **Partly discharged 2026-08-31**: the design-guardian registered both — `Sidebar` and `AiDotMatrix` — with their contracts and states. Both rows read `Status: proposed`, and `components.md` fixes that a row reaches `approved` only "when a preview carrying that component is signed off, and not before"; no preview exists yet. What stands is the sign-off, not the registration. | undefined | sdlc-factory/docs/work-orders/WO-033.md |
| WO-034 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2 and §2.5; none is inherited from a requirement. REQ-043 and REQ-092 govern what a calendar day SAYS; those are BP-039's and BP-054's, and this component holds no such string. | undefined | sdlc-factory/docs/work-orders/WO-034.md |
| WO-034 | Both components must be registered in `sdlc-factory/docs/design/components.md` with their data contract and states before sign-off (BP-018 decision 1). The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-034.md |
| WO-035 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.4; none is inherited from a requirement. REQ-008 criterion 6 (rival strength is never an alarm) is named by BP-018's own error behaviour and is enforced here as a TYPE constraint — the criterion itself is discharged where the rival series renders with its data (BP-038, BP-052), so it is cited, not inherited. | undefined | sdlc-factory/docs/work-orders/WO-035.md |
| WO-035 | The three charts must be registered in `sdlc-factory/docs/design/components.md` with their data contract and states before sign-off (BP-018 decision 1). The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-035.md |
| WO-036 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.4; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-036.md |
| WO-036 | Both charts must be registered in `sdlc-factory/docs/design/components.md` with their data contract and states before sign-off (BP-018 decision 1). The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-036.md |
| WO-037 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018, `BUILD.md` §2 and constitution rule 7.3; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-037.md |
| WO-037 | The registry conformance test reads `sdlc-factory/docs/design/components.md`, which is an empty table today. Until the design-guardian registers the nineteen exports WO-031 to WO-036 ship, that test fails by design — it is the check that rule 7.3 is met, and a green run before registration would mean the check does not work. The implementer runs it, records the failing list, and does not weaken it. | undefined | sdlc-factory/docs/work-orders/WO-037.md |
| WO-041 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31: the repo root holds only BUILD.md, DATA-COSTS.md, REQUIREMENTS-CONSOLIDATION.md, factory.config.json, CLAUDE.md and sdlc-factory/). Every path in this file plan is NEW and its placement is provisional against a repo with no code. The TypeScript project, its path resolution and the `*.test.ts` runner are established by work orders outside WO-041..WO-050; this WO creates none of them and adds no repo-root file. | undefined | sdlc-factory/docs/work-orders/WO-041.md |
| WO-042 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-042.md |
| WO-042 | BP-010's `Measured<T>` and BP-019's `renderMeasured()` are consumed here by import and are created by work orders outside WO-041..WO-050. This WO cannot be implemented before both symbols exist at their owning nodes' public entry points; the implementer stops and says so if they do not (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-042.md |
| WO-043 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-043.md |
| WO-043 | Model-produced text reaches a surface only through the seven stored columns `GeneratedColumn` names (BP-020 rests-on row 3). A model string reaching a surface from any other column is outside this module's sink and is invisible to it; WO-044's flow gate is scoped to the same seven and inherits the same limit. | undefined | sdlc-factory/docs/work-orders/WO-043.md |
| WO-044 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31), so the four governed globs match zero files on the day this suite lands and it would pass vacuously. This WO therefore ships a synthetic fixture surface tree carrying known violations, so the sweep discriminates from the first run rather than from the first screen. | undefined | sdlc-factory/docs/work-orders/WO-044.md |
| WO-044 | Every surface the product renders or sends is reachable from `src/app/**`, `src/app/(hosted)/**`, `src/lib/mail/**` and `src/ui/**` (ADR-010 rests-on; BP-020 rests-on row 2). A surface rendered from a path outside those globs is invisible to this suite. | undefined | sdlc-factory/docs/work-orders/WO-044.md |
| WO-044 | This suite is recorded, not enforced (constitution rule 3.3, ADR-010 point 6): no CI configuration, no branch protection and no pre-commit hook exists in this repository at cut time. A failing sweep blocks no merge until a platform decision creates one. This WO creates no such configuration. | undefined | sdlc-factory/docs/work-orders/WO-044.md |
| WO-045 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-045.md |
| WO-045 | The five seeded places are the ones BP-021 rests-on row 3 names by clause (REQ-041 c3, REQ-043 c3/c4/c5, REQ-065 c3/c4, REQ-010 c1). That row states in terms that nothing in the corpus proves the set of such places is closed, so PLACES is seeded, not complete; every later surface node appends its own places and WO-047's sweep fails an unregistered place by name. | undefined | sdlc-factory/docs/work-orders/WO-045.md |
| WO-046 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-046.md |
| WO-046 | One canonical cold-start fixture can drive every engine end to end without a vendor call, because BP-007's cache seam and BP-008's closed endpoint list can both be stubbed at the module boundary (BP-021 rests-on row 2). Without that, REQ-091 criterion 1 is only assertable against live vendors. | undefined | sdlc-factory/docs/work-orders/WO-046.md |
| WO-046 | BP-010 to BP-015's engine entry points are created by work orders outside WO-041..WO-050 and none exists on disk at cut time. The stage table this WO ships therefore drives zero engines on the day it lands and reports that count; each engine's own work order wires its stage in. The implementer stops and says so if that wiring is expected here (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-046.md |
| WO-046 | This suite is recorded, not enforced (constitution rule 3.3, ADR-010 point 6): no CI configuration, no branch protection and no pre-commit hook exists in this repository at cut time. | undefined | sdlc-factory/docs/work-orders/WO-046.md |
| WO-047 | No source tree exists on disk at cut time (verified 2026-08-31), so the App Router tree holds zero routes and this sweep would pass vacuously. This WO therefore ships a synthetic fixture route tree carrying known violations, so the sweep discriminates from its first run rather than from the first screen. | undefined | sdlc-factory/docs/work-orders/WO-047.md |
| WO-047 | Every place a value or a module can sit is reachable from the App Router tree under src/app/** and src/app/(hosted)/** (BP-021 rests-on row 1). A place rendered only inside a client-only modal that no route renders is invisible to this enumeration. | undefined | sdlc-factory/docs/work-orders/WO-047.md |
| WO-047 | This suite is recorded, not enforced (constitution rule 3.3, ADR-010 point 6): no CI configuration, no branch protection and no pre-commit hook exists in this repository at cut time. | undefined | sdlc-factory/docs/work-orders/WO-047.md |
| WO-048 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-048.md |
| WO-048 | The three stop facts REQ-092 names are each readable per site and per day from records the corpus already provides — BP-007's cap-hit ledger over `fetches`, BP-003's `killSwitchEngaged()`, and BP-012's `status: 'degraded' | 'failed'` on a scan (BP-054 rests-on row 1). This WO consumes the three as a plain `facts` argument assembled by BP-001's adapter and does not read them itself; if any of the three is not readable that way, BP-054 Decisions 2 is the decision to revisit. | undefined | sdlc-factory/docs/work-orders/WO-048.md |
| WO-048 | The date the work is expected to resume is derivable without an owner ruling: a spend cap resumes at the start of the next cap window, a failed step at the next run of its job, and an engaged kill switch promises no time at all (BP-054 rests-on row 2). Nothing in the corpus states the cap window's reset, so this WO derives `resumes` from a window boundary handed in by the caller rather than computing one. | undefined | sdlc-factory/docs/work-orders/WO-048.md |
| WO-049 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-049.md |
| WO-049 | Every line this WO returns is an owner-owed CopyKey seeded by WO-041. Until the owner supplies each string, `copy()` throws naming the key rather than rendering a blank, so this module is complete in mechanism and incomplete in words. The tests assert the key each statement reaches, never the sentence. | undefined | sdlc-factory/docs/work-orders/WO-049.md |
| WO-050 | No source tree exists on disk at cut time (verified 2026-08-31), so the enumerated route set is empty and this sweep would pass vacuously. This WO therefore ships a synthetic fixture route tree carrying known violations, so the sweep discriminates from its first run rather than from the first screen. | undefined | sdlc-factory/docs/work-orders/WO-050.md |
| WO-050 | Pages prepared before a stop survive it because they are rows BP-015's state machine owns, and a stop writes no state transition (BP-054 rests-on row 3). REQ-092 criterion 5 is a property of BP-015's transition table; this suite asserts it and implements none of it, so the assertion is only as strong as BP-015's table being reachable from a test. | undefined | sdlc-factory/docs/work-orders/WO-050.md |
| WO-050 | This suite is recorded, not enforced (constitution rule 3.3, ADR-010 point 6): no CI configuration, no branch protection and no pre-commit hook exists in this repository at cut time. A failing sweep blocks no merge until a platform decision creates one. | undefined | sdlc-factory/docs/work-orders/WO-050.md |
| WO-051 | A maintained public-suffix list is available to the build (the ICANN/Mozilla PSL is the de facto one), so 'has a public suffix' is decidable without a network call at request time — ADR-020's own row, inherited because this WO is where it is first spent | undefined | sdlc-factory/docs/work-orders/WO-051.md |
| WO-051 | The Next.js + TypeScript scaffold (package.json, tsconfig.json, next.config.ts) exists before this WO runs. It is BP-001's, not this node's — `structure.md` puts `next.config.ts` in BP-001's globs — and this repo carries none of it today | undefined | sdlc-factory/docs/work-orders/WO-051.md |
| WO-052 | The Next.js + TypeScript scaffold (package.json, tsconfig.json) exists before this WO runs, with `strict` and exhaustiveness checking on. It is BP-001's and BP-005's, not this node's; this repo carries none of it today | undefined | sdlc-factory/docs/work-orders/WO-052.md |
| WO-055 | The closed list of AI reader user-agents the `blockedReaders` count is computed over is pinned once in BP-005 as `AI_READER_AGENTS` (ADR-022, amended by ADR-090, which cites REQ-059 criterion 4 for the membership). This WO reads the pin and populates nothing; a build with the list empty fails BP-005's own pins test | undefined | sdlc-factory/docs/work-orders/WO-055.md |
| WO-055 | `scans` exists with `domain`, `report jsonb`, `score numeric null`, `drivers jsonb` and the `unique (domain) where is_current` partial index before this migration runs. That table is BP-002's baseline and BP-012's; this repo carries no `supabase/` directory today | undefined | sdlc-factory/docs/work-orders/WO-055.md |
| WO-056 | `scans` exists with `domain`, `tier`, `status(running/done/degraded)`, `created_at`, `is_current`, `supersedes_scan_id`, `correction_state` and `stopped_reason` before this migration runs. That table and those columns are BP-002's baseline and BP-012's; this repo carries no `supabase/` directory today | undefined | sdlc-factory/docs/work-orders/WO-056.md |
| WO-056 | The partial unique index `unique (domain) where is_current` is BP-012's and already exists, so a `failed` scan never becoming current holds by the index rather than by application logic | undefined | sdlc-factory/docs/work-orders/WO-056.md |
| WO-057 | `x-forwarded-for`'s first entry is the visitor's address on the deployment platform, so the network key is derivable without trusting a client-supplied header. BP-022 and BP-023 both carry this row; it is spent here | undefined | sdlc-factory/docs/work-orders/WO-057.md |
| WO-057 | Counting by network address means several founders behind one office or mobile network share one hourly allowance and one in-flight slot. Inherited from REQ-003's own row | undefined | sdlc-factory/docs/work-orders/WO-057.md |
| WO-057 | BP-005 gains `FREE_RESCAN_WINDOW_D: 7`, `FAILURE_COOLDOWN_H: 24`, `DAILY_WINDOW_H: 24` and `HOURLY_WINDOW_H: 1`, and `env` binds `IP_HASH_SALT` and `KILL_SWITCH` (`BUILD.md` §15). Pins and bindings are BP-005's by `structure.md` rule 5; this WO imports them and declares none | undefined | sdlc-factory/docs/work-orders/WO-057.md |
| WO-059 | The free battery — own fetches plus 12 live SERP calls plus the suggestion call — completes within 60 s at the p95 of vendor response time; `BUILD.md` §16 milestone 3 is the first measurement of this chain. Inherited from REQ-003's own row | undefined | sdlc-factory/docs/work-orders/WO-059.md |
| WO-059 | BP-007's `withCostContext` / `CostContext.capHit()` exists and is bound to `CAPS.FREE_C` (12¢) before this WO runs. The cost seam is BP-007's; this repo carries no `src/lib/costs/` today | undefined | sdlc-factory/docs/work-orders/WO-059.md |
| WO-059 | BP-005 pins `TIMING.reportTargetS: 60`, `TIMING.reportCeilingS: 90` and `CAPS.FREE_C: 12`. `structure.md` rule 5 puts every number in `constants.ts`; this WO imports them and declares none | undefined | sdlc-factory/docs/work-orders/WO-059.md |
| WO-060 | BP-005 pins `TIMING.progressHeartbeatS: 30`. `structure.md` rule 5 puts every number in `constants.ts`; this WO imports it and declares none | undefined | sdlc-factory/docs/work-orders/WO-060.md |
| WO-061 | BP-005 pins `FREE_RESCAN_WINDOW_D: 7` and `FAILURE_COOLDOWN_H: 24`. `structure.md` rule 5 puts every number in `constants.ts`; this WO imports them and declares none | undefined | sdlc-factory/docs/work-orders/WO-061.md |
| WO-062 | `src/middleware.ts` lists every path in this node on its public allow-list, so no session is read on this route. The middleware is BP-001's; this repo carries no `src/` today | undefined | sdlc-factory/docs/work-orders/WO-062.md |
| WO-063 | The deployment platform does not buffer or terminate a server-sent-events response inside `TIMING.progressHeartbeatS`, so a heartbeat every 30 s keeps the connection open for the 90 s a scan may last. This is a platform property, not a code one, and `BUILD.md` §16 milestone 3 is the first run that tests it | undefined | sdlc-factory/docs/work-orders/WO-063.md |
| WO-064 | The missing-pages and unquotable-pages counts reach the stored report blob as `Measured<number>` values. **Partly discharged 2026-08-31**: BP-012 decision 3 declares `StoredReport`, so this WO imports that type and defines no local reader interface. What stands is narrower — that BP-013's opportunity supply lands on the blob as two `Measured<number>` counts this node can read; `StoredReport`'s declaration names sections, not those two scalars, so the field each count arrives on is still the assembling node's to fix | undefined | sdlc-factory/docs/work-orders/WO-064.md |
| WO-064 | BP-005 pins `SEVERITY_THRESHOLDS` with the per-problem boundaries BP-027 decision 1 fixes. The pin is declared in BP-005's public interface; `structure.md` rule 5 forbids this node declaring the numbers, so this WO imports them | undefined | sdlc-factory/docs/work-orders/WO-064.md |
| WO-065 | BP-005's `AI_READER_AGENTS` holds the closed list of AI reader user-agent tokens (ADR-022, amended by ADR-090, whose membership is REQ-059 criterion 4's own approved text, asserted in `tests/pins.test.ts` by quotation). This WO reads the pin and populates nothing; naming an agent from memory would invent a fact about the world (rule 1.2) | undefined | sdlc-factory/docs/work-orders/WO-065.md |
| WO-066 | BP-019's `copy()` and `renderMeasured()` and BP-018's registered components (`Card`, `Badge`, `Collapse`, `Btn`, `Divider`) exist before this WO runs. Both are other nodes'; this repo carries no `src/lib/presentation/` and no `src/ui/` today | undefined | sdlc-factory/docs/work-orders/WO-066.md |
| WO-067 | The address a report names for a removal request is an owner-written string. BP-019's copy registry holds it under one key and this node renders it. **No value for it exists anywhere in the corpus** — it is owner-owed and outstanding. This WO creates the reference and the test; it writes no address, and `env.OWNER_EMAILS` (`BUILD.md` §15) is a deployment binding for owner accounts, not a published removal address, so substituting it would invent a customer fact (rule 1.2) | undefined | sdlc-factory/docs/work-orders/WO-067.md |
| WO-067 | A person, not the product, writes the `domain_blocks` row that removes a report, and does so inside REQ-002 criterion 2's five working days. BP-005 pins `RETENTION_D.removalSlaWorkingDays: 5`; no code in this WO enforces the window, because no code can — the operation is manual, and ADR-002 carries the same claim for the operation as a whole | undefined | sdlc-factory/docs/work-orders/WO-067.md |
| WO-068 | The AI-answers card, the Google-presence card, the free-page card and the pricing card are sibling nodes' modules. This WO renders them as named slots and supplies none of them, so the report is composable before those nodes ship | undefined | sdlc-factory/docs/work-orders/WO-068.md |
| WO-069 | `src/middleware.ts` puts `/scan/{domain}` and `/api/scan/**` on BP-001's public allow-list, so no session is read on any path in this node. The middleware is BP-001's; this repo carries no `src/` today | undefined | sdlc-factory/docs/work-orders/WO-069.md |
| WO-069 | No sitemap the product publishes names a report address. The sitemaps are BP-004/BP-047's (`src/app/(hosted)/**`); this WO asserts the absence from this side and cannot enforce it from inside BP-022 | undefined | sdlc-factory/docs/work-orders/WO-069.md |
| WO-070 | BP-019's `copy()` holds the landing headline, the field label, the submit label and one written line per `DomainProblem`. All are the owner's strings and this WO renders keys; `BUILD.md` §3 quotes a headline ('See what AI tells buyers about your market — and write your way in.') but a quotation in the build specification is not a copy-registry value, and minting one here would be writing customer-visible copy this node has no right to (constitution §1) | undefined | sdlc-factory/docs/work-orders/WO-070.md |
| WO-071 | The seams this WO imports are declared but not yet built: `llm()` and the `'profile'` call site (BP-009), `CostContext` (BP-007), `Measured<T>` (BP-010). Their work orders are in sibling planners' blocks and were cut concurrently with this one, so they cannot be enumerated in `depends-on` here. If any seam is absent when this WO starts, that is a `blocked-by` on the WO that owns it, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-071.md |
| WO-072 | The seams this WO imports are declared but not yet built: `keywordSuggestions()` and `SuggestionRow` (BP-008), `CostContext` (BP-007), `Measured<T>` (BP-010). Their work orders are in sibling planners' blocks, cut concurrently, so they cannot be enumerated in `depends-on`. If a seam is absent when this WO starts, that is a `blocked-by` on the WO that owns it, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-072.md |
| WO-073 | `SELECTION.intentWeights` — `{ decision: 3; solution: 3; problem: 2; informational: 1 }` — is pinned in `src/lib/config/constants.ts` (BP-005), which `sdlc-factory/docs/registry/capabilities.md` records as already declaring a `SELECTION` block. That file is BP-005's and is not touched here. If the block is absent when this WO starts, that is a `blocked-by` on BP-005's work order, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-073.md |
| WO-074 | `SELECTION: { volumeFloorPerMonth: 50; intentWeights: { decision: 3; solution: 3; problem: 2; informational: 1 }; minDecision: 4; minSolution: 3; maxRivalBrand: 3; maxHowTo: 2 }` and `BATTERY.QUESTIONS` (12) are pinned in `src/lib/config/constants.ts` (BP-005), which `capabilities.md` records as already declaring `SELECTION`. That file is BP-005's and is not touched here. | undefined | sdlc-factory/docs/work-orders/WO-074.md |
| WO-075 | BP-009's `LlmCallSite` union already carries `'question-phrasing'`, so this WO adds no call site to `src/lib/llm/` (BP-009's file). If the member is absent when this WO starts, that is a `blocked-by` on BP-009's work order, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-075.md |
| WO-076 | `SerpResult` (BP-008) carries the `ai_overview` item with its `references` payload — BP-008's own interface comment states 'SerpResult carries organic top-10 AND the ai_overview item with its reference domains — the free AI matrix rides here at 0c extra'. This WO reads that payload and buys nothing. If the shipped `SerpResult` does not carry it, that is a `blocked-by` on BP-008's work order, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-076.md |
| WO-077 | `PLATFORM_DOMAINS` is pinned in `src/lib/config/constants.ts` (BP-005), which BP-026's `## NFR budget` records as 'already declared there'. That file is BP-005's and is not touched here. If the pin is absent when this WO starts, that is a `blocked-by` on BP-005's work order, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-077.md |
| WO-078 | `BATTERY.COMPETITORS_MAX` (5) is pinned in `src/lib/config/constants.ts` (BP-005), which BP-026's `## NFR budget` records as 'already declared there'. That file is BP-005's and is not touched here. | undefined | sdlc-factory/docs/work-orders/WO-078.md |
| WO-079 | `SERP_LOCATION` is pinned in `src/lib/config/constants.ts` (BP-005) and BP-008 exposes no per-call locale parameter — BP-026's `## Error & edge behavior`: 'Every figure on the card comes from a call made at `SERP_LOCATION` (Google US / `en`). BP-008 exposes no per-call locale parameter, so criterion 8 cannot be broken by this node.' If BP-008 ships a locale parameter after all, criterion 8 stops being structurally held and this WO needs a runtime assertion instead. | undefined | sdlc-factory/docs/work-orders/WO-079.md |
| WO-083 | `BATTERY.COMPETITORS_MAX` (5) is pinned in `src/lib/config/constants.ts` (BP-005), which BP-034's `## NFR budget` records as 'already declared there'. That file is BP-005's and is not touched here. | undefined | sdlc-factory/docs/work-orders/WO-083.md |
| WO-085 | `competitors_domain` returns a measured-empty set, not an error, for a domain with no ranked presence — the setup card's only rival source when no report backs the account. If it errors instead, the card shows `seeking → none_found` on a failure it cannot distinguish from a true empty, and the two must be split. `BUILD.md` §16 milestone 2 is the first point at which it can be measured. | undefined | sdlc-factory/docs/work-orders/WO-085.md |
| WO-085 | `PRICE_BOOK.COMPETITORS_DOMAIN_COST_C` (1.5¢) and `BATTERY.COMPETITORS_MAX` (5) are pinned in `src/lib/config/constants.ts` (BP-005), which BP-034's `## NFR budget` records as 'both already declared there'. That file is BP-005's and is not touched here. | undefined | sdlc-factory/docs/work-orders/WO-085.md |
| WO-086 | `RIVAL_SIZE_BANDS { nearFloor: 100, nearMultiple: 2, middleFloor: 500, middleMultiple: 5 }` is pinned in `src/lib/config/constants.ts` (BP-005), which BP-052's `## NFR budget` records as 'already declared there'. That file is BP-005's and is not touched here. | undefined | sdlc-factory/docs/work-orders/WO-086.md |
| WO-087 | A rival's total count of searches it appears in is readable beside the 100 rows `rankedKeywords` fetches for it, so a band can be cut at 500 from a 100-row call. Origin: REQ-096's own open row. If refuted, `RivalSize.rankedCount` caps at 100, the `far` band becomes unreachable, and REQ-041's how-far-ahead ratio is refuted with it — the fix is shared with that requirement, not owned here. | undefined | sdlc-factory/docs/work-orders/WO-087.md |
| WO-087 | `RANKED_RIVAL_ROWS` (100) and `CACHE_WINDOWS_D.rival` (30 days) are pinned in `src/lib/config/constants.ts` (BP-005), which BP-052's `## NFR budget` records as 'both already declared there'. That file is BP-005's and is not touched here. | undefined | sdlc-factory/docs/work-orders/WO-087.md |
| WO-088 | The written line criterion 6 requires — that the rival is far beyond what they could catch and why the distance will not move — is a customer-visible string and therefore the owner's (constitution §1). It lives in BP-019's copy registry and does not exist yet. Narrowed 2026-08-31: the three band words this row was previously bracketed with are ruled and recorded in BP-019's `BAND_LABELS.rivalSize` (BP-019 decision 6), and the nine owner-owed words that ruling settled are the six band words and REQ-009 criterion 8's three severity words — criterion 6's sentence is not among them and stays outstanding on its own. It blocks the surface that renders the offer, never this module: this WO creates the offer's state and its destination handle only, no string is written here and none is invented. | undefined | sdlc-factory/docs/work-orders/WO-088.md |
| WO-091 | The weekly schedule — Monday, at `WEEKLY_DUE_HOUR_LOCAL`, site-local due-ness (ADR-060) — is REQ-065 criterion 1's and lives with the job runner (BP-003) and BP-005's constants. BP-056's `## NFR budget` states 'Pins needed from BP-005: none new'. `effectiveOn` reads that schedule rather than restating it; if the two ever disagree the customer is shown one date and given another, which criterion 6 forbids. | undefined | sdlc-factory/docs/work-orders/WO-091.md |
| WO-092 | REQ-071 carries an open `REVIEW(untestable)` line on the limb 'each affected number carries its goal in place of a change' in criteria 12 and 13: the goal it cites (REQ-041 criterion 4) is defined only for a headline number on Overview, so a rival distance or a charted point has no goal to carry. BP-056 decision 3 bounds this module to supplying the change marker and no change figure for those numbers, and leaves the REVIEW line with the requirement for the analyst to rule. If the ruling widens the limb, it widens `markers.ts` and nothing else. | undefined | sdlc-factory/docs/work-orders/WO-092.md |
| WO-101 | BP-019's copy registry exposes copy(key, vars) and generatedLabel() as this renderer's only source of sentences; until BP-020 ships them the implementer stubs them behind the BP-019 signature and no string is authored in this module | undefined | sdlc-factory/docs/work-orders/WO-101.md |
| WO-103 | BP-018's theme tokens are reachable from a server-rendered email string — i.e. the shell can resolve named token values at build or render time without a browser, so the mail frame uses named tokens rather than hard-coded hex | undefined | sdlc-factory/docs/work-orders/WO-103.md |
| WO-103 | BP-053 decision 2's 'rejected at the type level for every other kind' is expressible inside the declared `composeMail` signature — the parameter's type narrowed on `kind` so that `measurement` is required for `'weekly'` and absent for the rest — with no field renamed and no second entry point. If the compiler cannot express it within that signature, the rejection degrades to a runtime refusal, which is a finding for the architect on BP-053, not a choice this order makes. | undefined | sdlc-factory/docs/work-orders/WO-103.md |
| WO-104 | Every requirement `MAIL_KINDS` names in `occasionsFrom` is `approved` when this order validates. On disk today ten of the eleven are; REQ-074 is `status: in-review`, so `register-legality.test.ts` fails on the `account` row until the owner approves it — a true finding under BP-053 decision 4 ('retiring a requirement fails the mail test before it fails a customer'), not a defect in the test. | undefined | sdlc-factory/docs/work-orders/WO-104.md |
| WO-105 | Resend accepts one request carrying both an HTML body and a plain-text alternative, so criterion 5's guarantee is a property of the request this node builds rather than of a second send | undefined | sdlc-factory/docs/work-orders/WO-105.md |
| WO-106 | REQ-064 criterion 7 as written carries its own exception — 'except the single occasion REQ-057 criterion 7 makes unsuppressible' — so the `suppressible: false` parameter this WO ships is the requirement's text and not a ruling on REQ-064's first open REVIEW line, which names criterion 6. If /decide instead removes the exception, this WO loses one parameter, one branch and one test. | undefined | sdlc-factory/docs/work-orders/WO-106.md |
| WO-107 | BP-002's baseline ships users.notify jsonb with default {} before this WO merges; this node owns the column's shape and meaning but no migration (BP-059 decision 4), so a baseline that omits the column blocks this WO at implementation time rather than at planning time | undefined | sdlc-factory/docs/work-orders/WO-107.md |
| WO-109 | BP-002's baseline creates the `leads` table (BUILD.md §10 keys it to scan_id) and `email_suppressions` before this migration runs; this WO adds columns and indexes to `leads` under the `leads` token and specifies `email_suppressions` under the `suppressions` token, and a baseline that ships neither table blocks it at implementation time | undefined | sdlc-factory/docs/work-orders/WO-109.md |
| WO-110 | A free report's stored blob is immutable for the scan it belongs to, so the offer a visitor saw when they submitted is the offer the giveaway is written from; only a newer current scan for the same domain can change what is on offer between render and submit | undefined | sdlc-factory/docs/work-orders/WO-110.md |
| WO-115 | Resend reports a hard bounce or a permanent rejection distinguishably from a transient failure, so criterion 8's 24-hour retry window is spent on retriable failures rather than on an address that will never accept mail | undefined | sdlc-factory/docs/work-orders/WO-115.md |
| WO-117 | A Stripe Price created with `tax_behavior: 'inclusive'` continues to charge the same gross amount if automatic tax is later enabled on the account, deriving the tax from within the amount rather than adding to it | undefined | sdlc-factory/docs/work-orders/WO-117.md |
| WO-117 | A Stripe Price's `tax_behavior` cannot be changed after creation, so a price created `exclusive` must be replaced and every existing subscription migrated to correct it | undefined | sdlc-factory/docs/work-orders/WO-117.md |
| WO-118 | Stripe Checkout collects and stores the billing-address country on every purchase with `billing_address_collection: 'required'` and no further configuration (REQ-022's own rests-on row; BUILD.md §13 'collect the buyer's country (Stripe does automatically)') | undefined | sdlc-factory/docs/work-orders/WO-118.md |
| WO-118 | Stripe's `tax_id_collection` never blocks or delays completion of a Checkout Session on the basis of a tax ID's registry status, so REQ-022 criterion 7's 'no check stands between them and their purchase' holds with the field enabled | undefined | sdlc-factory/docs/work-orders/WO-118.md |
| WO-118 | A Stripe account can be configured so that no presentment-currency conversion (Adaptive Pricing or equivalent) is applied to a EUR price for a buyer outside the euro area, which REQ-022 criterion 4 requires | undefined | sdlc-factory/docs/work-orders/WO-118.md |
| WO-119 | Stripe Checkout collects and stores the billing-address country on every purchase with `billing_address_collection: 'required'` and no further configuration (REQ-022's own rests-on row; BUILD.md §13 'collect the buyer's country (Stripe does automatically)') | undefined | sdlc-factory/docs/work-orders/WO-119.md |
| WO-119 | A tax ID collected by `tax_id_collection` at Checkout is stored against the Stripe customer and rendered on every invoice Stripe issues for that customer, which is the half of REQ-022 criterion 6 no test in this corpus can exercise. No blueprint in this block carries this row: BP-030's three rests-on rows cover the country, the non-blocking behaviour of `tax_id_collection` and presentment currency, and none of them covers what reaches an invoice. | undefined | sdlc-factory/docs/work-orders/WO-119.md |
| WO-123 | A site address given at setup is enough to run the onboarding deep pass with no prior free scan (REQ-021's own rests-on row) | undefined | sdlc-factory/docs/work-orders/WO-123.md |
| WO-125 | Stripe delivers `checkout.session.completed` for a completed subscription Checkout Session at least once, and a session that never completed produces no such event — so the signature-verified webhook is a sufficient as well as a necessary provisioning trigger (BUILD.md §13, 'Webhook (signature-verified, the only provisioning path)') | undefined | sdlc-factory/docs/work-orders/WO-125.md |
| WO-126 | Stripe delivers `checkout.session.completed` for a completed subscription Checkout Session at least once, and a session that never completed produces no such event — so the signature-verified webhook is a sufficient as well as a necessary provisioning trigger (BUILD.md §13, 'Webhook (signature-verified, the only provisioning path)') | undefined | sdlc-factory/docs/work-orders/WO-126.md |
| WO-128 | Stripe delivers `checkout.session.completed` for a completed subscription Checkout Session at least once, and a session that never completed produces no such event — so the signature-verified webhook is a sufficient as well as a necessary provisioning trigger (BUILD.md §13, 'Webhook (signature-verified, the only provisioning path)') | undefined | sdlc-factory/docs/work-orders/WO-128.md |
| WO-132 | Stripe's customer portal can be configured so that a customer adds or corrects the tax ID (VAT number) on their own billing record there. BUILD.md §13 names the portal only for card and cancellation and puts the VAT ID field at Checkout; criterion 2 rests on it (REQ-076's own rests-on row, carried forward unchanged) | undefined | sdlc-factory/docs/work-orders/WO-132.md |
| WO-132 | Stripe's customer portal can be configured to change the billing address invoices and receipts are sent to, which REQ-076 criterion 2 and REQ-077 criterion 1 together make the only surface offering it | undefined | sdlc-factory/docs/work-orders/WO-132.md |
| WO-133 | Cancelling a Stripe subscription with `cancel_at_period_end` leaves `current_period_end` unchanged, so `paid_through` needs no separate arithmetic (REQ-076 criterion 3's 'the exact date their access ends') | undefined | sdlc-factory/docs/work-orders/WO-133.md |
| WO-136 | Supabase's magic-link auth can be driven so that the one-time token is delivered through BP-016's `sendEmail()` shell rather than Supabase's own mailer, which REQ-064's one-shell rule and BUILD.md §12 require of every mail the product sends | undefined | sdlc-factory/docs/work-orders/WO-136.md |
| WO-140 | WO-267's `supabase/migrations/00000000000002_rls.sql` adds `users.deleted_at timestamptz null` before this order's migration applies — the first branch of WO-267's third `rests-on` row, the one its implementer takes. If the other branch is what lands, this migration regains the `add column` and BP-063's migration re-issues the policies. | undefined | sdlc-factory/docs/work-orders/WO-140.md |
| WO-142 | `UnpublishResult`'s `{ ok: false; reason: FailureReason }` arm — a publication whose unpublish did not complete at all, as against `unreachable`, which ADR-082 Decision 3a makes `ok: true` because the page did reach `unpublished` — is named by neither REQ-079 criterion 4 nor BP-063's `UnpublishAllResult`. This work order routes it into `stillLive` (see the file plan's chosen-under-rule-1.1 note): the page is still live at its destination, and the only other counter the result carries is `takenDown`, so the alternative is that a live page is reported in neither and disappears from the one sentence criterion 4 promises about it. Cost of reversing: one arm in one switch and one test row. | undefined | sdlc-factory/docs/work-orders/WO-142.md |
| WO-143 | A connected WordPress site permits one place that brings every post ReachKit created there up together, so criterion 6's mail has a place to name — REQ-079's own `rests-on` row and BP-048's, unverified in this corpus, and answered per destination by BP-058's `stamp_capable` (ADR-083). Where a site permits none, `place` is `null`, the sentence says so and still carries its count, and the customer is left with pages live on their own domain that no product surface can point them to. That sentence is owner-owed and is BP-063's first Open question; this work order renders whatever the registry holds and mints none. | undefined | sdlc-factory/docs/work-orders/WO-143.md |
| WO-143 | `publications.unpublish_outcome` records, per publication, which of `UnpublishResult`'s five arms the run produced (BP-045's column, ADR-082 Decision 6 carried forward by ADR-084), so the four counts this mail carries are read from the run's own outcomes and no population is re-derived here (rule 2.4). The count for an arm with no rows is not zero — the key is absent from the map. | undefined | sdlc-factory/docs/work-orders/WO-143.md |
| WO-144 | The grounding passages REQ-050 criterion 1 keeps unchanged are stored in `drafts.grounded_fact` and nowhere else, so purging that column is the whole of what REQ-079 criterion 7 requires of them | undefined | sdlc-factory/docs/work-orders/WO-144.md |
| WO-146 | A founder reaching /setup always has an open account behind their payment (REQ-024 criterion 6), so the submit path never has to handle a payment with no user row | undefined | sdlc-factory/docs/work-orders/WO-146.md |
| WO-147 | Every setup copy key on this screen is an owner-owed string in BP-019's registry; this WO creates the keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-147.md |
| WO-149 | A founder reaching /setup always has an open account behind their payment (REQ-024 criterion 6), so no reminder is scheduled for a payment with no user row | undefined | sdlc-factory/docs/work-orders/WO-149.md |
| WO-150 | The onboarding deep pass completes fast enough that a founder will wait on a progress screen rather than leave (inherited from REQ-029; nothing in the corpus measures it, and the ten-minute release is the design that makes the claim non-load-bearing) | undefined | sdlc-factory/docs/work-orders/WO-150.md |
| WO-152 | The written sentences this notice selects are owner-owed strings in BP-019's copy registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-152.md |
| WO-153 | Every stage name and every line on this screen is an owner-owed string in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-153.md |
| WO-155 | Every line the frame states — the not-measured line, each no-publish line — is an owner-owed string in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-155.md |
| WO-156 | Ranking data for the customer's root domain includes rows for pages served on the hosted content. subdomain, so pages ReachKit publishes register as the customer's own wins on the growth chart (inherited from REQ-041) | undefined | sdlc-factory/docs/work-orders/WO-156.md |
| WO-156 | The four head copy keys are owner-owed strings in BP-019's registry; this WO creates the keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-156.md |
| WO-157 | The not-yet-measured line and every week-account line are owner-owed strings in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-157.md |
| WO-158 | Ten ranked searches is the point below which a rival-to-customer ratio misleads more than it informs (inherited from REQ-041; BUILD.md 6.6 pins the threshold, nothing measures it) | undefined | sdlc-factory/docs/work-orders/WO-158.md |
| WO-159 | The rival-module line keys — including the one the cold-start arm must not use — are owner-owed strings in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-159.md |
| WO-160 | Each goal's what-reaching-it-means line is an owner-owed string in BP-019's registry; this WO creates the keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-160.md |
| WO-161 | The what-reaching-it-means line behind GOALS.pages_published.meansKey is an owner-owed string in BP-019's registry; this WO renders the key and tests it, never the string | undefined | sdlc-factory/docs/work-orders/WO-161.md |
| WO-162 | Every alert line, the overflow line and the supply lines are owner-owed strings in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-162.md |
| WO-163 | The four head lines and every line this screen renders are owner-owed strings in BP-019's registry; this WO composes keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-163.md |
| WO-165 | Every empty-date line is an owner-owed string in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-165.md |
| WO-166 | `pageRecordFor(draftId)` and the whole `PageRecord` it returns are BP-045's `src/lib/publish/record/`, added to the capability index on 2026-09-01 and cut by **no work order in the corpus** as of 2026-09-02 — a grep of all 246 work orders for `PageRecord`, `pageRecordFor` and `record/index.ts` returns nothing. It falls in the BP-045 block (WO-206 to WO-239 and the new ids WO-261 to WO-270), not this one. If it is absent when work starts, the implementer adds nothing under `src/lib/publish/` — it logs `failed` naming the missing read and stops (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-166.md |
| WO-167 | Every stage label and every empty-date line is an owner-owed string in BP-019's registry; this WO renders keys and tests them, never the strings | undefined | sdlc-factory/docs/work-orders/WO-167.md |
| WO-169 | The two authorship labels and every claim-state line are owner-owed strings in BP-019's registry; this WO creates keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-169.md |
| WO-172 | The two authorship labels, the four claim-state lines and the do-nothing line are owner-owed strings in BP-019's registry; this WO renders keys and tests them, never the strings | undefined | sdlc-factory/docs/work-orders/WO-172.md |
| WO-173 | The unsaved-change line and the copy-out labels are owner-owed strings in BP-019's registry; this WO renders keys and tests them, never the strings | undefined | sdlc-factory/docs/work-orders/WO-173.md |
| WO-177 | The not-measured, partial and complete week lines are owner-owed strings in BP-019's registry; this WO returns variants and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-177.md |
| WO-179 | The market-effect line, the publishing-outcome line and every refusal line are owner-owed strings in BP-019's registry; this WO returns keys and their tests, never the strings | undefined | sdlc-factory/docs/work-orders/WO-179.md |
| WO-180 | Every panel label, the danger zone's consequence statements and each refusal line are owner-owed strings in BP-019's registry; this WO renders keys and tests them, never the strings | undefined | sdlc-factory/docs/work-orders/WO-180.md |
| WO-181 | BP-002's baseline migration has created `sites` and `scans` and installed the RLS default-deny policy this table's policies extend; `src/lib/db/index.ts` exports `db()`. Neither exists on disk and both are another node's work order. | undefined | sdlc-factory/docs/work-orders/WO-181.md |
| WO-181 | BP-010 exports `Measured<T>` from `src/lib/measure/`. The type is imported here and not redeclared (rule 2.4). | undefined | sdlc-factory/docs/work-orders/WO-181.md |
| WO-182 | BP-052 writes `report.rivalSizes = RivalSize[]` into the scan blob, each entry carrying `rankedCount` for a rival domain; BP-052's own open row records that a count at the 100-row fetch cap may be a floor rather than a count, which is the condition `rankedCountFrom` coerces on. | undefined | sdlc-factory/docs/work-orders/WO-182.md |
| WO-183 | BP-005 exports `EFFORT_BY_TYPE` and `FIT_WEIGHT` from `src/lib/config/constants.ts` with the values BP-040 decision 3 derives, and `SELECTION.intentWeights` for BP-011's classifier. | undefined | sdlc-factory/docs/work-orders/WO-183.md |
| WO-183 | The site's `Profile` that `rankOpen` passes to every row is readable from the site's current scan without a vendor call or a second derivation. BP-025's data-model delta states `report.market = { profile, suggestions, totalVolume }`; BP-012's `StoredReport`, which is the declared shape of that blob, states `market: Measured<SuggestionRow[]>` and carries no profile anywhere. One of the two is wrong and neither is this work order's to edit — see the note under Interfaces for the implementer's stop condition. | undefined | sdlc-factory/docs/work-orders/WO-183.md |
| WO-184 | The customer's own domain and their own measured ranked count for the scan are readable from the scan record, so `keyword_page`'s absence test and `ownRanked` need no fresh fetch. | undefined | sdlc-factory/docs/work-orders/WO-184.md |
| WO-185 | BP-010 exposes, per site and scan, the customer's own measured pages with the on-page facts the three shortfall kinds need — the page's own position for a search, its word count, and when its content last changed — and the robots/`noindex`/login-wall/JS-only/AI-agent-block findings the five `Barrier` values name. Every one of those is a stored measurement (REQ-093 c4); none is composed here. | undefined | sdlc-factory/docs/work-orders/WO-185.md |
| WO-185 | BP-005 exports `AI_READER_AGENTS` for the `blocked_ai_agent` barrier. | undefined | sdlc-factory/docs/work-orders/WO-185.md |
| WO-186 | BP-007's `CostContext` is open for the pass (`withCostContext` with cap `DEEP` or `WEEKLY`), so `capHit()` is answerable inside this call and the `opportunity-typing` spend lands inside that cap rather than a budget line of its own. | undefined | sdlc-factory/docs/work-orders/WO-186.md |
| WO-186 | BP-009's `llm()` accepts the `opportunity-typing` call site at tier `haiku` and returns `Measured<T>` parsed against the schema this WO passes. | undefined | sdlc-factory/docs/work-orders/WO-186.md |
| WO-188 | BP-002's baseline has created `sites` and `scans` and installed the default-deny RLS policy; `db()` is exported from `src/lib/db/index.ts`. | undefined | sdlc-factory/docs/work-orders/WO-188.md |
| WO-188 | BP-012 records a completion time per scan that `exhaustedSince` falls back to for a site that has never held an open opportunity. | undefined | sdlc-factory/docs/work-orders/WO-188.md |
| WO-189 | BP-005 exports `SUPPLY_TARGET_DEPTH = 30` from `src/lib/config/constants.ts`, asserted in `tests/pins.test.ts`. | undefined | sdlc-factory/docs/work-orders/WO-189.md |
| WO-189 | BP-007's `CostContext` is open for the pass and `capHit()` answers true once the pass's cap is spent, which is the only source of the `pass_ended_early` stop. | undefined | sdlc-factory/docs/work-orders/WO-189.md |
| WO-189 | BP-017 exports `hasActiveAccess()`; a site without active access takes no top-up (REQ-065 c5's shape). | undefined | sdlc-factory/docs/work-orders/WO-189.md |
| WO-189 | BP-003's `scan/run` (deep) calls `pursueDepth` and `weekly/refresh` calls `topUp`; neither call site is created by this WO. | undefined | sdlc-factory/docs/work-orders/WO-189.md |
| WO-190 | BP-005 exports `SUPPLY_SHORT_BELOW = 7` from `src/lib/config/constants.ts`, asserted in `tests/pins.test.ts`. | undefined | sdlc-factory/docs/work-orders/WO-190.md |
| WO-190 | The three written lines criteria 3, 5 and 6 require are owner-owed and not yet written (BP-041's second `rests-on`). This WO emits handles; until BP-019's copy registry holds the strings, no surface can render the telling REQ-095 promises. | undefined | sdlc-factory/docs/work-orders/WO-190.md |
| WO-191 | BP-002's baseline has created `sites` and the default-deny RLS policy, and `db()` is exported from `src/lib/db/index.ts`. `sites` records the business name, the domain, `voice_text` and `do_not_claim`. | undefined | sdlc-factory/docs/work-orders/WO-191.md |
| WO-191 | BP-012 exposes `StoredReport` and WO-181 exposes `Opportunity`, both referenced by `SiteRuleInputs` as the two places every figure the product measured for a site is held (BP-042's third `rests-on`). | undefined | sdlc-factory/docs/work-orders/WO-191.md |
| WO-192 | BP-006's `safeFetch` outcome for the customer's own page that day is available to the caller, so the grounding rule can check the passage against the content fetched that day without fetching again itself. | undefined | sdlc-factory/docs/work-orders/WO-192.md |
| WO-192 | Every ranking position, search volume, visibility and answer count the product holds for a site is in the stored report blob or the site's opportunities (BP-042's third `rests-on`); the register is built from those two and nothing else. | undefined | sdlc-factory/docs/work-orders/WO-192.md |
| WO-193 | `sites` records the business name and account-holder name the draft's `attribution` is written from, so "the customer themselves" is a stored value the check compares against rather than an entity it recognises. | undefined | sdlc-factory/docs/work-orders/WO-193.md |
| WO-194 | BP-005 exports `MACHINE_ADDRESS_PATTERNS: readonly RegExp[]` from `src/lib/config/constants.ts`, non-empty and asserted in `tests/pins.test.ts`. The battery decides the forms it lists and no others — BP-042's `undischargeable` `rests-on`, carried here because this is where the limit is felt. | undefined | sdlc-factory/docs/work-orders/WO-194.md |
| WO-195 | BP-005 exports `SHINGLE_SIZE = 5` and `NEAR_DUPLICATE_MAX = 0.85` from `src/lib/config/constants.ts`, asserted in `tests/pins.test.ts`. | undefined | sdlc-factory/docs/work-orders/WO-195.md |
| WO-196 | BP-043's `claimCheck` is available at `src/lib/generate/claims/` and returns `ClaimVerdict`; the `do_not_claim` arm of the battery is evaluated by calling it, and this node holds no second implementation of it (rule 7.1). | undefined | sdlc-factory/docs/work-orders/WO-196.md |
| WO-197 | `sites.voice_text` is the one field the customer writes and changes (REQ-070's Settings card, BP-001's surface); it is read here at generation time and is not written by this node. | undefined | sdlc-factory/docs/work-orders/WO-197.md |
| WO-198 | BP-009's `llm()` accepts the `brief`, `outline`, `draft` and `answerability` call sites at the tiers `BUILD.md` §6.3 prices, and returns `Measured<T>` parsed against the schema passed. | undefined | sdlc-factory/docs/work-orders/WO-198.md |
| WO-198 | BP-006's `safeFetch` reads a page of the customer's own live site under the SSRF, DNS, size and robots policy; the grounding read makes no other outbound call. | undefined | sdlc-factory/docs/work-orders/WO-198.md |
| WO-198 | BP-007's `capHit()` is answerable between steps inside the open `CAP_DRAFT` context. | undefined | sdlc-factory/docs/work-orders/WO-198.md |
| WO-199 | BP-010's `readMeasuredText` returns the rendered text of every page this engine measured for a site, so the `measured` half of the comparison set needs no fresh fetch at generation time (BP-042's second `rests-on`). | undefined | sdlc-factory/docs/work-orders/WO-199.md |
| WO-199 | BP-015 owns every state transition; this WO returns a result and performs none. `needs_attention` is reached by BP-015's own edge, and ADR-070 point 3 forbids any scheduler taking `needs_attention -> generating`. | undefined | sdlc-factory/docs/work-orders/WO-199.md |
| WO-199 | BP-003's `draft/generate` job calls this function the evening before the publish date; the job registration is not created by this WO. | undefined | sdlc-factory/docs/work-orders/WO-199.md |
| WO-200 | `sites.do_not_claim` holds the customer's recorded claims as a jsonb array, written by REQ-070's Settings card (BP-001's surface); this node reads it and never writes it. | undefined | sdlc-factory/docs/work-orders/WO-200.md |
| WO-200 | BP-009's `llm()` accepts the `claim-check` call site at tier `nano` and parses the response against the schema passed; a response that does not parse arrives as `unmeasured`. | undefined | sdlc-factory/docs/work-orders/WO-200.md |
| WO-201 | BP-015's `becomesPublishable()` calls `claimRecheckOutstanding(draftId)` before every hand-off — veto expiry, customer approval, publish time, automatic retry and customer-started retry alike. REQ-053 c5's promise is enforced at that one call site and nothing in this node can compel it (BP-043's first `rests-on`). | undefined | sdlc-factory/docs/work-orders/WO-201.md |
| WO-201 | The settings write path that records a change to `sites.do_not_claim` calls `sweepOutstandingRechecks()` for that site. Correctness does not rest on this — the hash guard holds without it — only timeliness does (BP-043's second `rests-on`). | undefined | sdlc-factory/docs/work-orders/WO-201.md |
| WO-201 | BP-005 exports `CLAIM_RECHECK_SWEEP_MAX = 25` from `src/lib/config/constants.ts`. | undefined | sdlc-factory/docs/work-orders/WO-201.md |
| WO-202 | BP-015's node implementing REQ-056 c2 leaves `needs_attention -> generating` reachable only from a customer action, and no scheduler, retry or job holds a reference to that edge (ADR-070's `rests-on`). This function cannot compel that; it only refuses to authorise a second automatic attempt. | undefined | sdlc-factory/docs/work-orders/WO-202.md |
| WO-203 | No node under BP-015 writes `publications.verdict` (`BUILD.md` §10). This node is the single home of a page's verdict and keeps a row per page per week; a column holding one latest verdict would be the second copy and could not answer REQ-063 c6's "the date of the last verdict it did receive" (BP-051's first `rests-on`). This WO therefore does not create that column, and a migration elsewhere that adds it is a rule 7.1 conflict to be reported, not worked around. | undefined | sdlc-factory/docs/work-orders/WO-203.md |
| WO-203 | BP-050 exposes, per site and week, whether that week was not measured at all (REQ-065 c3) or measured only in part (c4), and which measurements the partial week holds, so a `WeekMeasurements` can be built from it without a fresh measurement (ADR-072's first `rests-on`, carried from ADR-071). The *shape* is no longer assumed: BP-051 decision 5 declares `WeekMeasurements` in its own `## Public interface` as of 2026-08-31. | undefined | sdlc-factory/docs/work-orders/WO-203.md |
| WO-203 | `CheckId` — `'reachable' | 'indexable' | 'sitemap' | 'aiReadable'` — is declared by BP-049 in `src/lib/publish/verify/verify.ts` and is importable from a module under `src/lib/opportunities/`. `VerifyNote`'s `checks_failed` arm names it and this work order declares no second copy of it (rule 2.4). The direction `src/lib/opportunities -> src/lib/publish` is not the cycle `structure.md` rule 6 forbids; the forbidden direction here is `src/lib/scan -> src/lib/opportunities` (BP-051 decision 5). | undefined | sdlc-factory/docs/work-orders/WO-203.md |
| WO-203 | BP-051's `## Public interface` code fence, as it stands on disk on 2026-09-02, splices the `VerifyNote` declaration into the middle of the `WeekStanding` union, so `WeekStanding`'s `not_judgeable`, `not_measured` and `no_week` arms read as arms of `VerifyNote`. The two unions are separated in the `## Interfaces` block below, and every member, comment and arm is otherwise copied character-for-character. Reported to the architect rather than resolved here; the blueprint is not this agent's to edit (constitution 4). If the architect's correction differs from the separation below, this work order is re-extracted from it. | undefined | sdlc-factory/docs/work-orders/WO-203.md |
| WO-204 | BP-005 exports `TOO_EARLY_WEEKS = 3` from `src/lib/config/constants.ts`, asserted in `tests/pins.test.ts`. | undefined | sdlc-factory/docs/work-orders/WO-204.md |
| WO-204 | `NotJudgeableCause` has **five** members since 2026-09-01 (ADR-072 Decision 5a) and this pure function produces **two** of them: `search_untracked` and `question_left_set`, both read as an absent key in `WeekMeasurements`. The other three are the caller's, from stored state this function's declared inputs do not carry — `domain_changed` needs the publication's own domain to compare `WeekMeasurements.domain` against, `unpublished` needs the publication's state (REQ-056 c7), and `page_not_found` is BP-049's recorded outcome and is never derived anywhere (ADR-085). BP-051's doc comment on `evaluateAcceptance` still reads "which of the four causes stops it being decidable" and its `WeekMeasurements.domain` comment reads "Compared with the publication's"; neither the count nor that comparison is reachable from the declared signature. The signature is copied unchanged below and the split of work is stated in the file plan; reported to the architect rather than resolved here. | undefined | sdlc-factory/docs/work-orders/WO-204.md |
| WO-205 | BP-050's `accountForWeek` distinguishes a week that was not measured at all from one measured only in part, and names which parts are unmeasured; `no_week` and `not_measured` are derived from that distinction alone (ADR-072's first `rests-on`, carried from ADR-071). | undefined | sdlc-factory/docs/work-orders/WO-205.md |
| WO-205 | BP-049 records, per publication, the whole `VerifyOutcome` of the one check at 24 hours — `found` with its four `Measured<boolean>`, `page_not_found`, or `could_not_confirm` — each carrying its own `checkedAt`, and `dispositionFor` returns it. This node reads that record and never re-derives, re-fetches or retries it (ADR-085 Decision 3). WO-234 writes it; the shape is declared in BP-045's `src/lib/publish/types.ts` (WO-206) and re-exported by BP-049. | undefined | sdlc-factory/docs/work-orders/WO-205.md |
| WO-205 | A prior `not_judgeable` row for a publication is answerable by one indexed read on `page_verdicts` before any evaluation is attempted, which is what makes ADR-072 Decision 5c's terminality-by-row rather than terminality-by-flag implementable at this node's NFR budget (one bulk insert per site per week, ≤8 pages). | undefined | sdlc-factory/docs/work-orders/WO-205.md |
| WO-205 | BP-003's `weekly/refresh` calls `judgeWeek` after the site's re-measurement; the job registration is not created by this WO. | undefined | sdlc-factory/docs/work-orders/WO-205.md |
| WO-206 | `DraftView` carries the ten fields BP-046 requires of it — `id`, `siteId`, `state`, `vetoDeadline`, `approvedAt`, `approvedBy`, `hasUnsavedEdit`, `claimRecheckOutstanding`, `told` and the `GoverningPair` in force — and BP-014's draft row is the source of the last four; this work order declares the shape, it does not populate it. | undefined | sdlc-factory/docs/work-orders/WO-206.md |
| WO-206 | `Measured<T>` is importable into `src/lib/publish/types.ts` from `src/lib/measure/` (BP-010) without creating a cycle: `src/lib/measure/` imports nothing from `src/lib/publish/`. structure.md rule 6 forbids a cycle between `src/lib/` modules, and BP-045's leaf rule is narrower — `types.ts` imports nothing from `src/lib/publish/`, which an import from `src/lib/measure/` does not violate. Asserted by this work order's dependency test, not assumed. | undefined | sdlc-factory/docs/work-orders/WO-206.md |
| WO-209 | The columns `drafts.transitions jsonb not null default '[]'` and `drafts.publishable_since timestamptz null` are added by BP-014's migration file on the `*_drafts*.sql` topic (structure.md rule 3), which this work order may not write. They are assumed present at implementation time. | undefined | sdlc-factory/docs/work-orders/WO-209.md |
| WO-210 | The columns `drafts.transitions jsonb not null default '[]'` and `drafts.publishable_since timestamptz null` are added by BP-014's migration file on the `*_drafts*.sql` topic (structure.md rule 3), which this work order may not write. They are assumed present at implementation time. | undefined | sdlc-factory/docs/work-orders/WO-210.md |
| WO-215 | The columns `drafts.approved_at`, `drafts.approved_by`, `drafts.told jsonb`, `drafts.veto_token_hash` and `drafts.veto_token_expires_at` are added by BP-014's migration file on the `*_drafts*.sql` topic (structure.md rule 3), which this work order may not write. They are assumed present at implementation time. | undefined | sdlc-factory/docs/work-orders/WO-215.md |
| WO-215 | `claimRecheckOutstanding` is supplied on `DraftView` by BP-014's claim-check work (REQ-053 criterion 5, which fixes when one stands outstanding and how it clears); this predicate is its one call site and computes none of it itself. | undefined | sdlc-factory/docs/work-orders/WO-215.md |
| WO-216 | The columns `drafts.approved_at`, `drafts.approved_by`, `drafts.told jsonb`, `drafts.veto_token_hash` and `drafts.veto_token_expires_at` are added by BP-014's migration file on the `*_drafts*.sql` topic (structure.md rule 3), which this work order may not write. They are assumed present at implementation time. | undefined | sdlc-factory/docs/work-orders/WO-216.md |
| WO-217 | The columns `drafts.approved_at`, `drafts.approved_by`, `drafts.told jsonb`, `drafts.veto_token_hash` and `drafts.veto_token_expires_at` are added by BP-014's migration file on the `*_drafts*.sql` topic (structure.md rule 3), which this work order may not write. They are assumed present at implementation time. | undefined | sdlc-factory/docs/work-orders/WO-217.md |
| WO-218 | `sites.timezone` is BP-017's column — its `## Data model delta` ('`sites` — as §10, plus `timezone`'), migrated by WO-272 under `*_sites_timezone_*.sql` as nullable with no default — and this order's `adoptBrowserTimezone` is its first-sign-in writer, called by WO-273's adapter from BP-001's account shell. The column is null only between provisioning (WO-126) and that first authenticated request, an interval in which nothing that reads it runs, so no read path here falls back to the server's zone and `readPublishingSettings` treats a null zone as an error, never a default. The row's earlier reading — the column as BP-002's declared addition, populated by no named order — was refuted by BP-002's own index text (WO-272's provenance note) and by BP-057's `## Data model delta` as amended 2026-09-03. | undefined | sdlc-factory/docs/work-orders/WO-218.md |
| WO-220 | `drafts.told` (BP-046's column, on BP-014's `*_drafts*.sql` topic) exists at implementation time; this work order clears it and writes no migration. | undefined | sdlc-factory/docs/work-orders/WO-220.md |
| WO-227 | The moment a customer last signed in is readable through BP-017 (Supabase auth's own `last_sign_in_at`), so REQ-074 criterion 6's condition can be evaluated without minting a column on BP-017's `users` topic. | undefined | sdlc-factory/docs/work-orders/WO-227.md |
| WO-227 | `draft/generate` is invoked once per active site per day even on a day where supply is exhausted and no draft is written, so the once-per-breakage mail has a daily occasion without a seventh job id. | undefined | sdlc-factory/docs/work-orders/WO-227.md |
| WO-230 | A published page's question-and-answer section is available structurally on the draft row (`drafts.meta.faq`, written by BP-014 at generation) rather than only as prose inside `body_md`, so `FAQPage` markup is emitted from data and never from a render-time parse of markdown. | undefined | sdlc-factory/docs/work-orders/WO-230.md |
| WO-237 | A post created by us carries a marker we can find again on a later request — post meta written at creation and readable back by the same application-password user — so a retry whose first outcome was never recorded finds the existing post instead of creating a second one. | undefined | sdlc-factory/docs/work-orders/WO-237.md |
| WO-237 | The WordPress REST API lets an application-password user create a post with `status: 'publish'` in a single request and returns that post's public permalink in the same response, so a delivery is live at a known address with no second call. ADR-084's first `rests-on` row, carried here because this node is where the call is made. Nothing in this repository has spoken to a WordPress install (`00-project.md`, 'Greenfield. No source code on disk yet'). | undefined | sdlc-factory/docs/work-orders/WO-237.md |
| WO-237 | An application-password user that holds `edit_posts` but not `publish_posts` can still create a post, and WordPress silently holds it at `draft` or `pending` rather than honouring `status: 'publish'` or refusing the request. ADR-084's second `rests-on` row, and the whole ground for the status assertion on the create response. If it is refuted and such a request is refused outright, that assertion becomes belt-and-braces rather than the only thing standing between the owner's ruling and a silently drafted post, and its shape does not change. | undefined | sdlc-factory/docs/work-orders/WO-237.md |
| WO-237 | ADR-083's stamp — the `post_tag` term the create call carries, `canStamp`, `WORDPRESS_STAMP_SLUG` and `stampApplied` — is cut by no work order anywhere in this corpus (verified 2026-09-02 by grep over all 245 work orders: `canStamp`, `stampCapability`, `stamp_capable`, `post_tag` and `WORDPRESS_STAMP_SLUG` appear in none of them). BP-048 requires the term to be written **in the create call and never afterwards**, so it cannot be added by a later work order without becoming 'a later write into their site', which REQ-060 criterion 6 forbids in terms — which is what BP-048 means by the work-order sequence being load-bearing rather than a preference. This work order declares `stampApplied` because BP-048's interface does, and does **not** implement the stamp; see the stop condition in `## Steps`. | undefined | sdlc-factory/docs/work-orders/WO-237.md |
| WO-238 | A WordPress site with Yoast or RankMath installed exposes, over the REST API to an application-password user with `edit_posts`, a writable path for that plugin's SEO title and description on a post — whether by registered post meta, by a plugin-supplied namespace under `/wp-json/`, or not at all. Which of those holds, and under which plugin versions, is unverified in this corpus. | undefined | sdlc-factory/docs/work-orders/WO-238.md |
| WO-239 | The WordPress REST API lets an application-password user with `edit_posts` set an existing post's status back to `draft` on a post that user's own credential created, without any other field of that post changing. BP-048's `rests-on` already assumes the write path for creation and for the SEO fields; that the same path can set status back is assumed here and is not verified in this corpus. Carried forward from ADR-082, which carried it from ADR-081. | undefined | sdlc-factory/docs/work-orders/WO-239.md |
| WO-239 | A WordPress REST request can tell 'the post is no longer there' (a 404 for a post id we hold) apart from 'the site could not be reached' (DNS failure, connection reset, timeout, 5xx) reliably enough to choose between the `already_gone` and `unreachable` arms. ADR-082's fourth `rests-on`, carried here because this module is where the two arms are taken. Nothing in this corpus verifies that a misconfigured host cannot answer 404 for an unreachable site; the mapping itself is WO-236's `errors.ts`. | undefined | sdlc-factory/docs/work-orders/WO-239.md |
| WO-239 | The written lines the customer reads — `publish.wordpress.namedForRemoval`, the was-live line, the `already_gone` line, the `unreachable` line and the retry action's label — are BP-019/BP-020's and their strings are the owner's (ADR-082 and ADR-084, Open questions). Nothing in this work order blocks on any of them: it returns an `UnpublishResult` handle and composes no sentence. | undefined | sdlc-factory/docs/work-orders/WO-239.md |
| WO-241 | BP-001's delegation table names "BP-031's offer state, BP-030's checkout control, BP-005's price" behind this surface. Two of the three resolve: BP-031 declares `offerTerms()` and BP-030 declares `createCheckoutSession()` and `PRICE_COPY_KEYS`. **BP-005 declares no price.** Its `## Public interface` pins vendor costs (`PRICE_BOOK`), caps and windows and carries no €49; the amount a customer reads is REQ-022 criterion 1's, rendered from the `price.amount` copy key (the owner's string), and the amount checkout charges is Stripe's, addressed by `env.STRIPE_PRICE_ID` in BP-030's `CHECKOUT_PARAMS`. This WO renders the key and passes no amount; nothing here mints a number. Reported to the architect | undefined | sdlc-factory/docs/work-orders/WO-241.md |
| WO-241 | The nine sentences this surface speaks — BP-030's three `PRICE_COPY_KEYS` and BP-031's six offer keys — are the owner's under constitution §1 and no value for them exists in the corpus. WO-041 seeds all nine into `src/lib/presentation/copy/keys/offer.ts` as owner-owed, so `copy()` throws naming the key rather than rendering a blank. This surface therefore cannot render until the owner writes them: the intended blocking point, and it blocks copy, not structure | undefined | sdlc-factory/docs/work-orders/WO-241.md |
| WO-242 | BP-029's `captureLead(a: { scanId: string; email: string })` exists in `src/lib/mail/leads/capture.ts` before this adapter can be wired. That module is BP-029's and is cut by another block; this WO calls it and implements none of it. Its `{ captured: false; reason: 'unavailable' }` arm is REQ-003 criterion 10's and is mapped here to a written line by key, never to a vendor message | undefined | sdlc-factory/docs/work-orders/WO-242.md |
| WO-243 | BP-028's `correctionOffer`, `nextCorrectionState` and `ReportFacts`, and BP-012's `runScan({ correctionOf })`, exist before this adapter can be wired. Both are other nodes' and are cut by other blocks; this WO calls them and implements neither the state machine nor the re-measurement | undefined | sdlc-factory/docs/work-orders/WO-243.md |
| WO-243 | The corrected category the reader submits is a value BP-028's node validates. This adapter passes it through as an opaque string and neither checks it against a category list nor normalises it: no blueprint gives this route a vocabulary to check against, and inventing one here would be a second implementation of a check BP-028 owns | undefined | sdlc-factory/docs/work-orders/WO-243.md |
| WO-244 | BP-062's `exportEverything(siteId)` and BP-061's `currentSession()` exist before this adapter can be wired. Both are other nodes' and are cut by other blocks; this WO calls them and builds no archive | undefined | sdlc-factory/docs/work-orders/WO-244.md |
| WO-245 | BP-045's `transition(draftId, to, by, reason?)` and `Actor`, and BP-046's `becomesPublishable`, exist before these adapters can be wired. Both are BP-015's leaves and are cut by other blocks; this WO calls `transition` and implements no guard, no state and no publish | undefined | sdlc-factory/docs/work-orders/WO-245.md |
| WO-245 | Draft ownership is checked here as `currentSession().siteId === draft.siteId`, the same check WO-170's `PATCH /api/drafts/{id}` adapter makes. No blueprint declares a shared `assertOwnsDraft` helper, and two adapters making one check is the second copy rule 7.1 warns about; this WO makes the check inline and reports the missing helper rather than minting one in a route file, which BP-001 decision 1 forbids ('an adapter that grows logic is a review defect') | undefined | sdlc-factory/docs/work-orders/WO-245.md |
| WO-246 | BP-017's `handleStripeWebhook(rawBody, signature)` exists before this adapter can be wired, and performs signature verification inside itself. That module is BP-017's and is cut by another block; this WO reads the raw body and the signature header and hands both over, and holds no secret, no verification and no event dispatch | undefined | sdlc-factory/docs/work-orders/WO-246.md |
| WO-247 | BP-029's `applyOptOutToken(token)` and `optOutTokenFor(email)` exist before this surface can be wired; the token is signed and has no expiry, and `applyOptOutToken` is idempotent. Both are BP-029's and are cut by another block; this WO calls the first and implements no suppression, no sequence sweep and no token format | undefined | sdlc-factory/docs/work-orders/WO-247.md |
| WO-247 | The two sentences this surface speaks — `optout.confirmed` and `optout.invalid` — are the owner's under constitution §1 and no value for them exists in the corpus. WO-041 seeds both into `src/lib/presentation/copy/keys/mail.ts` as owner-owed, so `copy()` throws naming the key rather than rendering a blank. The page therefore cannot render until the owner writes them: the intended blocking point, and it blocks copy, not structure | undefined | sdlc-factory/docs/work-orders/WO-247.md |
| WO-248 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31: the repo root holds only BUILD.md, DATA-COSTS.md, REQUIREMENTS-CONSOLIDATION.md, factory.config.json, CLAUDE.md and sdlc-factory/). Every path in this file plan is NEW and its placement is provisional against a repo with no code. The TypeScript project, its path resolution and the `*.test.ts` runner are established by other work orders; this WO creates none of them and adds no repo-root file. | undefined | sdlc-factory/docs/work-orders/WO-248.md |
| WO-248 | The thirteen strings these three maps resolve to are written by WO-041 into `src/lib/presentation/copy/keys/bands.ts` and are not owner-owed there (WO-041 Steps 5: 'None of the thirteen is in `OWNER_OWED`: they have values, so `copy()` returns them'). This WO declares keys only and asserts each resolves; if any of the thirteen is missing or empty when this WO runs, the failure is WO-041's and the implementer says so rather than seeding a string here (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-248.md |
| WO-248 | The handle sets are the engines' and are fixed elsewhere: `winnable | reach | not-yet` is BP-040's `Winnability`, `near | middle | far` is BP-011's rival-size handle set, `invisible | hard-to-find | findable | dominant` is BP-010's `BandHandle`, and `low | mid | high` is BP-027's `Severity`. This WO restates each union in the map's own type because the map must be total over it; a handle set that changes at its owning node without changing here is a compile error at the `Record<>`, which is the intended coupling. | undefined | sdlc-factory/docs/work-orders/WO-248.md |
| WO-249 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW except the one Modify row, and its placement is provisional against a repo with no code. This WO creates no repo-root file. | undefined | sdlc-factory/docs/work-orders/WO-249.md |
| WO-249 | The whole argument of this WO rests on TypeScript's discriminated-union narrowing being on: `strict`, `strictNullChecks` and no `any` on the `Measured<T>` import path. Without them the `unmeasured` arm does not lose its `value` field on narrowing and the central guarantee below degrades from a compile error to a convention. The tsconfig is BP-001's and does not exist today; the implementer stops and says so if `strict` is off (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-249.md |
| WO-249 | The two sentences REQ-004 criteria 6 and 9 require — the line naming what could not be determined, and the line stating the scan stopped early — are customer-visible strings and therefore the owner's (constitution §1). They stand **owner-owed**: this WO seeds two empty keys for them so callers have a key to pass, and `copy()` throws naming the key rather than rendering a blank (WO-041's chosen representation). Until the owner writes them, `renderMeasured` is complete in mechanism and throws loudly on the unmeasured arm; every test below asserts the key reached, never the sentence. | undefined | sdlc-factory/docs/work-orders/WO-249.md |
| WO-250 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. This WO creates no repo-root file and no lint configuration. | undefined | sdlc-factory/docs/work-orders/WO-250.md |
| WO-250 | All four leaf modules expose their surface through an `index.ts` that re-exports 'the symbols listed under Exposes and nothing else': `copy/index.ts` (WO-041), `generated/index.ts` (WO-043), `place/index.ts` (WO-045), `stopped/index.ts` (WO-049). This barrel re-exports those four entry points rather than reaching into a leaf's internal files, so a leaf can move a symbol between its own files without touching this one. If any of the four ships without an `index.ts`, the implementer stops and says so rather than importing the leaf's internals (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-250.md |
| WO-250 | No import path alias (`@/lib/presentation`) is assumed. The barrel is a relative-path module; whether consumers reach it by alias is BP-001's tsconfig decision and is not this WO's to make. | undefined | sdlc-factory/docs/work-orders/WO-250.md |
| WO-251 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-251.md |
| WO-251 | `OnPageFacts` has no declared shape anywhere in the corpus. BP-010 names it in `measureDomain`'s return type and BP-012's `StoredReport` carries it as `onPage: OnPageFacts // BP-010`; six work orders (WO-184, WO-185, WO-186, WO-189, WO-191, WO-192) copy that line verbatim and none declares the type either. The shape in `## Interfaces` below is derived by this work order from BP-010's own module sentence — "the parsers (headings, answer blocks, evidence density, schema, OG)" — and from `BUILD.md` §5's named sub-measures, and is flagged for the architect to fold into BP-010's `## Public interface` (constitution rule 1.1: chosen, recorded, reversible). Reversal cost: one interface, no stored data — the blob is `jsonb` and no migration names these fields. | undefined | sdlc-factory/docs/work-orders/WO-251.md |
| WO-251 | BP-005's `constants.ts` carries the direct-answer character window. BP-010 decision 2 assigns it there by name — "the coefficients that are numbers (band thresholds, the answerability floor, the direct-answer character window) are pins in BP-005" — and `BUILD.md` §5 fixes its value: "`directAnswers` = question headings whose first block is 40–320 visible chars ÷ all headings × 100". BP-005's `## Public interface` as it stands on disk declares no such pin, so WO-006 will not have written one. See the stop condition under `## Interfaces`. | undefined | sdlc-factory/docs/work-orders/WO-251.md |
| WO-252 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-252.md |
| WO-252 | BP-005's `constants.ts` carries the answerability floor. BP-010 decision 2 assigns it there by name and `BUILD.md` §5 fixes its value: "Answerability = shape of the home + measured pages, 0–100, floored at 1". BP-005's `## Public interface` as it stands on disk declares no pin carrying it. Same stop condition as WO-251's, restated under `## Interfaces` because this file is where the floor is applied. | undefined | sdlc-factory/docs/work-orders/WO-252.md |
| WO-252 | The remaining coefficients of `BUILD.md` §5 — 25, 0.55, 0.45 and 4 in SearchPresence, 0.4 and 0.6 in AIPresence, and the cube root — are **formula shape, not pins**, and belong in this file. BP-010 decision 2 names exactly three things as BP-005's ("band thresholds, the answerability floor, the direct-answer character window") and puts the rest here: "the formula's shape is code here, covered by a fixture suite per driver". Chosen on that sentence (rule 1.1); reversal is moving six numbers into `constants.ts` and one line into `tests/pins.test.ts`. | undefined | sdlc-factory/docs/work-orders/WO-252.md |
| WO-253 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-253.md |
| WO-253 | `measureDomain` cannot measure `aiPresence`. BP-010's declared signature takes `{ domain, tier, pages? }` and BP-010 `depends-on: [BP-005, BP-006, BP-008]` — not BP-011 — while `BUILD.md` §5's AIPresence is a rate over the twelve SERPs' AI-overview references, and the twelve searches are BP-011's and are selected from a profile derived from the home text this call has only just read. There is no arm of the declared signature that reaches them. This work order returns `aiPresence` as `{ kind: 'unmeasured', reason: 'not_attempted' }` and the pipeline fills it at the `scoring` stage from WO-252's `aiPresenceOf` over SERPs it already bought (WO-256). Chosen and recorded under rule 1.1; reversal is one parameter on BP-010's signature (`serps?`) and one call site. | undefined | sdlc-factory/docs/work-orders/WO-253.md |
| WO-253 | BP-007's `fetches.payload` holds the raw HTML of every own-fetch, so `readMeasuredText` re-reads bytes already bought rather than re-fetching. BP-010 decision 4 states it — "BP-007's `fetches` row already holds the payload (`ledger + cache + raw store in one`), and this function is the read of it keyed by the site's scans" — and WO-021's migration gives `fetches` the `payload jsonb` column and the `scan_id` index the read uses. If the shipped `recordFetch` stores a parsed projection rather than the document, `readMeasuredText` has nothing to return and this is a `blocked-by` on WO-022 added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-253.md |
| WO-254 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/` and no `supabase/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-254.md |
| WO-254 | Three blueprints describe the blob's `market` section differently and this work order follows BP-012. BP-012 declares `market: Measured<SuggestionRow[]>`; BP-025's `## Data model delta` declares `report.market = { profile, suggestions, totalVolume }`; BP-056's `## Data model delta` reads `report.market.category`, which is a field of BP-025's `Profile` and reachable under neither of the other two shapes. BP-012 is followed because its decision 3 is the corpus's only declaration of the type and states the ownership ground — "This node owns it because it assembles the blob and is its only writer" — and because rule 2.4 makes the other two the second and third copies, not the home. The consequence is that **the blob carries no `Profile`**, which is the fact WO-183's stop condition turns on; that stop condition is carried forward unchanged (see `## Interfaces`). Resolving the three-way disagreement is the architect's, on BP-012, BP-025 and BP-056; it is reported with this block's return. | undefined | sdlc-factory/docs/work-orders/WO-254.md |
| WO-254 | BP-011's `## Data model delta` says it "Writes the `questions`, `market`, `rivals` and **`coherence`** sections of `scans.report jsonb`", and `StoredReport` has no `coherence` field — it carries `correctionState: CorrectionState` (BP-028's value domain) and nothing holding a `CoherenceVerdict`. Following BP-012 verbatim means a coherence verdict has nowhere in the blob to live. Reported rather than fixed: widening `StoredReport` is BP-012's author's, not this work order's. | undefined | sdlc-factory/docs/work-orders/WO-254.md |
| WO-254 | `OnPageFacts` is declared by WO-251 and by no blueprint; `SerpResult` is declared by WO-023 in `src/lib/vendors/dataforseo/types.ts` and by no blueprint, though BP-012's `StoredReport` attributes it to BP-026 in a comment. Both are imported by this file at the path its author ships it at. If either ships under a different name, this is a `blocked-by` on that work order added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-254.md |
| WO-255 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/` and no `supabase/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-255.md |
| WO-255 | `assembleReport` and `storeCurrentReport` are internal names this work order mints. BP-012's `## Public interface` declares the type and the reader and names no writer, while its decision 1 requires one — "a single transaction that inserts the new scan and flips the pointer" — and its decision 3 names the node "the only writer". Two names for two things the blueprint describes and does not name (constitution §1: internal names are the system's; rule 1.1: chosen, recorded). Reversal is a rename; nothing outside `src/lib/scan/` imports either. | undefined | sdlc-factory/docs/work-orders/WO-255.md |
| WO-256 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/` and no `supabase/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-256.md |
| WO-256 | BP-023's `stages.ts` declares the **read** side of the progress stream (`progress(scanId): AsyncIterable<StageEvent>`, consuming `dbAdmin()`) and no write side, and no blueprint or work order in the corpus declares where the stage state it reads is stored. This work order adds two columns to the `scans` row under BP-012's own `scans` topic and writes them (rule 1.1: chosen, recorded, reversible — drop two columns). `BUILD.md` §10's "one report blob per scan, no per-section tables" is the ground for a column pair rather than a `scan_stages` table. If WO-060 ships a declared writer instead, this work order uses it and drops the columns. | undefined | sdlc-factory/docs/work-orders/WO-256.md |
| WO-256 | `runScan` on the free tier **adopts** the `scans` row BP-023's `claimFreeScanSlot` already inserted rather than inserting a second one. BP-023 calls that row "the `scans` row that *is* the in-flight record", WO-058's `## Out of scope` says "The scan pipeline the claimed id feeds — BP-012's `runScan`, another node's", and BP-012's declared signature takes no `scanId`. Chosen and recorded (rule 1.1); reversal is one optional field on BP-012's signature and one call site. A free-tier call that finds no running row for the domain refuses with `status: 'failed'` and spends nothing — a free scan is never started outside admission control. | undefined | sdlc-factory/docs/work-orders/WO-256.md |
| WO-256 | `measureDomain` returns `aiPresence` as `unmeasured / not_attempted` (WO-253's second `rests-on` row), so this pipeline fills it at the `scoring` stage from WO-252's `aiPresenceOf` over the SERPs stage 5 already bought, and hands the completed `Drivers` to BP-024's `verdictOf`. Nothing is re-bought: the SERPs are in memory from the stage that bought them. | undefined | sdlc-factory/docs/work-orders/WO-256.md |
| WO-257 | No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code. | undefined | sdlc-factory/docs/work-orders/WO-257.md |
| WO-257 | `rankOpen(siteId)` already excludes `family = 'fix'` and every `unblock`, and returns `Ranked[]` in descending score (BP-040 `## Public interface`; WO-183's file plan for `src/lib/opportunities/rank/open.ts`). `nextForDay` therefore re-filters nothing and re-ranks nothing — it reads the head and loads that one row. If the shipped `rankOpen` does not hold those exclusions, the filter belongs in it and not here, and this is a `blocked-by` on WO-183 added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-257.md |
| WO-257 | WO-183 carries a stop condition for the BP-012/BP-025 disagreement about where the site's `Profile` is stored, and `rankOpen` is unreachable until it is discharged. That condition is unchanged by WO-254, which ships BP-012's `StoredReport` verbatim and therefore stores no `Profile`. This work order inherits the stop through its `depends-on` edge and adds nothing to it: an implementer who finds `rankOpen` unimplemented stops on WO-183, not here, and does not write a second ranking. | undefined | sdlc-factory/docs/work-orders/WO-257.md |
| WO-258 | `pageRecordFor(draftId)` and the `PageRecord` it returns are BP-045's `src/lib/publish/record/`, added to the capability index on 2026-09-01 and cut by **no work order in the corpus** as of 2026-09-02 — a grep of all 246 work orders for `PageRecord`, `pageRecordFor` and `record/index.ts` returns nothing. It falls in the BP-045 block (WO-206 to WO-239 and the new ids WO-261 to WO-270), not this one. This work order needs only the **type**, which BP-045 declares in `src/lib/publish/types.ts` (WO-206), and constructs its own fixtures; if the type is absent when work starts, the implementer adds nothing under `src/lib/publish/` — it logs `failed` naming the missing declaration and stops (constitution rule 4.2). | undefined | sdlc-factory/docs/work-orders/WO-258.md |
| WO-258 | `PageRecord.address.offered === false` — REQ-056 criterion 6's `never_made_live` arm — is **unreachable in production since ADR-084 Decision 1**, which makes every page created live at every destination, and REQ-043 criterion 12 names no refusal ground for it: its three grounds are `unpublished_by_us`, `page_not_found` and `no_admin_address`, and none of them is 'ReachKit never made this page live'. This work order therefore gives that input **no runtime arm**: `wayAsVisitor` narrows on it and closes with `const _never: never`, so the day the arm gains members the file stops compiling rather than defaulting into an offered way with an undefined address. Reported to the architect: it is either criterion 12's fourth ground or BP-045's arm to retire, and neither is a planner's to choose. | undefined | sdlc-factory/docs/work-orders/WO-258.md |
| WO-259 | An address of the form `<baseUrl>/wp-admin/post.php?post=<postId>&action=edit` opens that post inside the customer's own WordPress for whoever is signed in there, on every install a customer can connect. This is REQ-043's own `rests-on` row and BP-039's second — 'for every post ReachKit delivered to a customer's WordPress, an address inside that WordPress that opens that post for whoever is signed in there can be formed from what ReachKit recorded at delivery — the site's base URL and the post id' — narrowed here to the one concrete template this work order builds. Nothing in this repository has spoken to a WordPress install (`00-project.md`, 'Greenfield. No source code on disk yet'), so `wp-admin` sitting at that relative path, and `post.php` taking those two parameters, are assumed and not verified here. Where an install serves its admin elsewhere the address 404s inside the customer's own site, which is the failure this template's cost-of-reversing names. | undefined | sdlc-factory/docs/work-orders/WO-259.md |
| WO-261 | A fetch of a page's own public address can tell an unambiguous 404 or 410 from every other answer reliably enough to choose between REQ-062 criterion 4's second and third outcomes. This is ADR-084's fourth `rests-on` row, raised there for a WordPress post-id fetch, and it is load-bearing here for a different call against a different host (ADR-085's first row). A misconfigured host answering 404 for a site that is in fact unreachable puts a page in the arm that stops its verdicts forever (ADR-072). Nothing in this repository has fetched a customer's page (`00-project.md`, 'Greenfield. No source code on disk yet'). | undefined | sdlc-factory/docs/work-orders/WO-261.md |
| WO-261 | A page served by a customer's CMS carries its own identity in server-rendered markup — a canonical link or `og:url` resolving to the fetched address, or failing both a `<title>` matching the published title — even where its body is withheld behind client JavaScript. BP-049's second `rests-on`, carried here because this module is where the split is implemented. If it is refuted for some CMS, a JavaScript-gated page reads as `not_our_page` instead of as failing the one check that exists to catch it — which is the safe direction: it asserts nothing. | undefined | sdlc-factory/docs/work-orders/WO-261.md |
| WO-262 | `publications.verify jsonb` can carry a `siteCondition` field beside the serialised `VerifyOutcome` without a column of its own, and the most recent non-null one for a destination is readable in one indexed query over that jsonb. BP-049 decision 5 fixes the shape; the index that makes the read one query is this work order's to choose and lands on WO-208's migration topic, which this work order may not write — see `## Out of scope`. | undefined | sdlc-factory/docs/work-orders/WO-262.md |
| WO-263 | The three surfaces that state a published page's standing — the day panel (BP-039), the draft view (BP-044) and Overview (BP-001) — call `pageRecordFor` rather than re-deriving liveness from a column. This work order ships the read model and the type that makes the re-derivation unnecessary; it cannot make those surfaces call it, and the work orders that build them are not this pass's. Until they do, REQ-062 criterion 7's promise holds by construction only for callers of this function. | undefined | sdlc-factory/docs/work-orders/WO-263.md |
| WO-263 | The two sentences REQ-056 criterion 6's address-label pair needs — `record.address.publiclyReadableAt` and `record.address.wasPublishedAt` — and the never-made-live line `record.address.neverMadeLive` are owner-owed customer-visible strings (REQ-093, constitution §1's rights table) and are not written anywhere in this corpus. This work order names the three keys and supplies the address; it mints no sentence and does not block on them, because every assertion below is on a key. | undefined | sdlc-factory/docs/work-orders/WO-263.md |
| WO-264 | An application-password user that holds `edit_posts` but not `publish_posts` — WordPress's Contributor role in the default map — can be told apart from one that can publish, over the REST API, **without creating a post**. ADR-084's second `rests-on` row and ADR-086's first, and the whole ground for this probe. Nothing in this repository has spoken to a WordPress install (`00-project.md`, 'Greenfield. No source code on disk yet'). If it is refuted and such a request is refused outright, the probe becomes belt-and-braces rather than the only thing standing between the owner's ruling and a silently drafted post, and its shape does not change. | undefined | sdlc-factory/docs/work-orders/WO-264.md |
| WO-266 | REQ-074 gains a criterion for the destinations list in the `failing`-because-cannot-publish state. REQ-060 criterion 7 fixes what the customer must and must not be told there and narrowed its own non-goal to REQ-074 criterion 1 accordingly, so REQ-074 today covers only the expired-credential occasion. Until that criterion exists this work order renders the occasion from REQ-060 criterion 7 alone — enough to build, and not enough for the destinations list to own the promise (rule 5.7). BP-058's third `rests-on` and ADR-086's second; raised by the requirements-analyst on 2026-09-01 and not closed. Recorded here, not as a `blocked-by`: REQ-074 is silent on this occasion, and silence is a gap, not a conflict (rule 2.3). | undefined | sdlc-factory/docs/work-orders/WO-266.md |
| WO-266 | The sentence the customer reads in this occasion, and the label of the `reconnect_other_account` action, are owner-owed customer-visible strings and are not written anywhere in this corpus (ADR-084's and ADR-086's Open questions, both open). This work order names the key, the reason and the action and mints no sentence; every assertion below is on a key or a union member, so nothing here blocks on them — but the destinations list cannot render this occasion to a customer until the owner supplies them. | undefined | sdlc-factory/docs/work-orders/WO-266.md |
| WO-268 | **No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2 and §2.5; none is inherited from a requirement. | undefined | sdlc-factory/docs/work-orders/WO-268.md |
| WO-268 | `sdlc-factory/docs/design/components.md` is an empty registry with no rows. BP-018 decision 1 makes entry in that file the precondition of export: "a new component enters `sdlc-factory/docs/design/components.md` as `proposed` with its data contract and states before it can be exported." The eight components below must be registered there by the design-guardian — with the data contract and states this work order's preview shows — before this work order can be signed off. The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-268.md |
| WO-268 | The seven components below must be registered in `sdlc-factory/docs/design/components.md` — an empty table today — by the design-guardian before this work order can be signed off (BP-018 decision 1). The planner may not write that file. | undefined | sdlc-factory/docs/work-orders/WO-268.md |
| WO-273 | BP-033's `SETUP_INCOMPLETE_ALLOWLIST` admits `/api/account/*` as its text on disk states, so the post lands for a founder whose setup is unfinished — BP-057: 'a founder who signs in and abandons setup has still signed in'. The list is BP-033's data; this order adds no entry to it and holds if the entry stands. | undefined | sdlc-factory/docs/work-orders/WO-273.md |

## undischargeable

| Artifact | Claim | Accepted by | See |
|---|---|---|---|
| ADR-093 | No approved requirement promises that the product works at every screen size. Checked 2026-09-02 over all of `requirements/`: the words mobile, responsive, breakpoint, screen size and phone appear in none of them, and the single narrow-viewport clause in the corpus is REQ-040 criterion 5, which promises only that the three destinations stay reachable. This ADR therefore derives its scope from an owner ruling and from `BUILD.md` §4, not from an approved promise — and the promise itself is owed (see Consequences, 'What the analyst owes'). | undefined | sdlc-factory/docs/decisions/ADR-093.md |
| BP-042 | The machine-address rule (REQ-050 c8) is enforced by a pinned, deterministic pattern battery. The battery catches the forms it lists; no coverage rate over unseen text is claimed or measured, and `BUILD.md` §16 milestone 6 is the first point at which one could be. | undefined | sdlc-factory/docs/blueprints/BP-042.md |
| WO-269 | ADR-093 decision 1 fixes exactly two boundaries; `design/tokens.md` §2b (closed 2026-09-02 under rule 1.1) mints four — `--breakpoint-sm` 640, `-md` 768, `-lg` 1024, `-xl` 1280. This order maps the two boundaries from decision 1's own structural facts: `compact|medium` is where the sidebar returns (`--breakpoint-lg`) and `medium|wide` is where the day panel sits beside the grid (`--breakpoint-xl`), because those are the two facts decision 1's table uses to define the bands. `--breakpoint-sm` and `--breakpoint-md` are widths the suite does not probe; whether they are content-dimension steps in decision 1's sense or a fourth and fifth band is the architect's to rule, and reversing this mapping costs two entries in `BAND_MIN` and nothing else. | undefined | sdlc-factory/docs/work-orders/WO-269.md |
| WO-269 | Today `src/app/` holds only `.gitkeep`, so the route sweep enumerates zero routes and only the canary exercises the browser. Three things are observed the first time a route exists (WO-002 and the first leaf): that the app boots for the suite under `tests/setup.ts`'s network refusal and WO-005's boot-time env validation; that Playwright's own transport to Chromium (a child-process pipe, not `http.request`) does not trip that refusal; and what an `(account)` route renders to the anonymous visitor the suite is — REQ-020 criterion 5's address prompt, which is the document measured until a session fixture exists. | undefined | sdlc-factory/docs/work-orders/WO-269.md |

## refuted

| Artifact | Claim | Accepted by | See |
|---|---|---|---|
| WO-275 | vitest 4.1.11's per-project `test.fileParallelism: false` (a `ProjectConfig` field — `ProjectConfig = Omit<InlineConfig, NonProjectOptions | "sequencer" | "deps">`, and `fileParallelism` is not a member of the `NonProjectOptions` union that excludes root-only options — `node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts`) actually scopes serialization to the files of the one project it is set on, while files in the other three projects (`node`, `ui`, `layout`) keep scheduling concurrently with each other and with that project. Confirmed at the type level only; not yet observed at runtime. | undefined | sdlc-factory/docs/work-orders/WO-275.md |

## confirmed

| Artifact | Claim | Accepted by | See |
|---|---|---|---|
| ADR-022 | The membership of the list — which AI reader user-agent tokens exist and are worth naming — is stated in the corpus after all: REQ-059 criterion 4 (approved) names six by name for the robots policy ReachKit serves, and decision 4 below makes that set and the counted set one set. `tests/pins.test.ts` asserts the constant against that clause by quotation; nothing is populated from memory (rule 1.2). Discharged by ADR-090, which is also where the reasoning lives | undefined | sdlc-factory/docs/decisions/ADR-022.md |
| ADR-060 | Every active site stores a time zone (REQ-073 criterion 1, defaulting to the one the browser reported at first sign-in), so a site-local Monday is always computable | undefined | sdlc-factory/docs/decisions/ADR-060.md |
| ADR-086 | REQ-074 gains a criterion for the destinations list in this occasion. REQ-060 criterion 7 fixes what the customer must and must not be told and narrowed its own non-goal to REQ-074 criterion 1 accordingly, leaving REQ-074 covering only the expired-credential occasion. Until that criterion exists, BP-058 renders this occasion from REQ-060 criterion 7 alone — which is enough to build and not enough to be the destinations list's own promise (rule 5.7). Raised by the requirements-analyst in `requirements/history/REQ-056-057-060-2026-09-01-adr-084-review-round-two-folded.md`; nobody held REQ-074 that pass and this file does not edit requirements. **Discharged 2026-09-02: REQ-074 criterion 8 exists (`requirements/history/REQ-074-2026-09-01-cannot-publish-criterion-and-jn-004-step.md`) and states the promise for any destination, not for WordPress alone. It ratifies this file's Decisions 1, 2 and 3 clause for clause — the `failing` state, the connection-is-live line, the three prohibitions and the action that leads to a different account — and adds one limb this file did not fix: a re-entry of the stored credential leaves the destination not reading as working. BP-058 renders that limb with no new branch and records how (its `## Error & edge behavior`). This row is discharged, not amended: no Decision in this file changes and its `status: accepted` stands (rule 2.2b).** | undefined | sdlc-factory/docs/decisions/ADR-086.md |
| ADR-090 | REQ-059 criterion 4's six named agents are the whole of the set, and not merely the subset the hosted robots policy must permit. This follows inside this corpus from ADR-022 decision 4 — 'the agents named and allowed on content.{customer-domain} are exactly AI_READER_AGENTS' — which makes the hosted set and the counted set one set by construction. If a later requirement names an AI reader the hosted policy does not permit, that decision, not this one, is what needs revisiting. | undefined | sdlc-factory/docs/decisions/ADR-090.md |
| ADR-093 | A layout engine is available wherever the conformance suite of Decision 6 runs — a real browser, not jsdom. In jsdom every box metric is 0, so `scrollWidth <= clientWidth` and every containment assertion in Decision 6 holds for every element on every page, and the suite reports a clean run over a corpus it never measured. No test runner exists on disk yet; the work order that first creates `tests/ui/layout/` is where this is discharged, and Decision 6 point 5's canary is what discharges it. | undefined | sdlc-factory/docs/decisions/ADR-093.md |
| ADR-093 | Nothing in this repository enforces a failing suite (rule 3.3). Checked 2026-09-02: no `.github/workflows`, no `.husky`, no `core.hooksPath`, no non-sample hook in `.git/hooks`. Until one exists a layout failure is recorded, not enforced — the same platform gap ADR-010 point 6 records for the presentation sweeps. | undefined | sdlc-factory/docs/decisions/ADR-093.md |
| BP-003 | Drift assessment, 2026-09-03 (constitution §6; rule 2.2a — record the assessment, move nothing): the only code under this node's `code:` today is `src/jobs/.gitkeep`, placed by b172edf (`feat(WO-001): Next.js toolchain, build configuration and root config files`); `tests/jobs/` does not exist. WO-001's validation report puts that file in scope deliberately — File scope: `.gitkeep` under `src/app`, `src/jobs`, `src/lib`, `src/ui`, `supabase` — required by the WO's own test — and records its criterion-2 mutation as discriminating (Deleted `src/ui/.gitkeep` and the now-empty dir → failed). A placeholder contradicts nothing this node declares: the `## Public interface` union `type JobId` of exactly seven ids, the one-file-per-job layout under `## Module / boundary` and decision 4's single platform file are all still unwritten, and WO-016 (`approved`, `implements: [BP-003]`) is the order that writes them. The code and the blueprint are compatible and neither is the wrong source of truth. This row is discharged for that commit only; a later commit under these globs that names no work order implementing this node is a fresh drift, re-read against this node from scratch. | undefined | sdlc-factory/docs/blueprints/BP-003.md |
| BP-003 | `tests/jobs/**` stays in `code:` as a forward declaration although the directory does not exist (rule 1.1, ruled 2026-09-03). Derivation: rule 5.6's `code: []` clause is for a node that governs no code, not for a glob whose directory is still empty; the blueprint-writing skill has container nodes anchor directories and this node is a container; WO-016's file plan names `tests/jobs/content-jobs.test.ts` as NEW; and 84 of the corpus's 86 blueprint test globs point at directories that do not exist yet, so dropping this one would make this node the single exception to a corpus-wide convention and would be reinstated the day WO-016 lands. Cost of reversing: one front-matter line — but the console derives file → blueprint from it, so its absence would be felt exactly when WO-016's tests arrive with no governing node. Re-observed by WO-016's validator when those tests exist. | undefined | sdlc-factory/docs/blueprints/BP-003.md |
| BP-020 | Every value in COPY is an owner-supplied string. Nine were outstanding at the time this node was cut and no agent could fill them: the three winnability band words and the three rival-size band words ADR-001 routes into BAND_LABELS, and REQ-009 criterion 8's three severity words. The four score band words were not outstanding — REQ-004 criterion 1 states them (Invisible, Hard to find, Findable, Dominant) and the registry transcribes them. Discharged 2026-08-31: the owner ruled all nine and they are recorded in BP-019's decision 6, which also records that ADR-001's disjointness assertion passes on them. | undefined | sdlc-factory/docs/blueprints/BP-020.md |
| BP-024 | The four band words — Invisible, Hard to find, Findable, Dominant — are `BUILD.md` §5's and REQ-004 criterion 1's, so they are transcribed rather than chosen; they reach a screen only through BP-019's `SCORE_BANDS`, and this node emits the four internal handles | undefined | sdlc-factory/docs/blueprints/BP-024.md |
| BP-024 | The closed list of AI reader user-agents the blocked-readers count is computed over is pinned once in BP-005 by ADR-022. Discharged by ADR-090: REQ-059 criterion 4 (approved) states the membership and `tests/pins.test.ts` asserts the constant against that clause by quotation, so nothing is populated from memory. ADR-022's own row for this claim already read `confirmed` and this one lagged it | undefined | sdlc-factory/docs/blueprints/BP-024.md |
| BP-027 | The three severity words are the owner's (constitution §1, customer-visible strings). This node emits the ordered handles `low | mid | high`; BP-019's `SEVERITY` triple holds the keys and the owner holds the words. Discharged 2026-08-31: the owner ruled them, most to least severe — Critical, Worth fixing, Minor — recorded in BP-019 against `high`, `mid` and `low` so the tuple stays ascending in the count, which is what makes REQ-009 c8's monotonicity a property of its order. | undefined | sdlc-factory/docs/blueprints/BP-027.md |
| BP-027 | The verbatim unblock lines a founder pastes are derived from one pinned, closed list of AI reader user-agents (ADR-022). Discharged by ADR-090, which makes REQ-059 criterion 4 (approved) the membership statement and `tests/pins.test.ts` the assertion against that clause by quotation; nothing is populated from memory. ADR-022's own row for this claim already read `confirmed` and this one lagged it | undefined | sdlc-factory/docs/blueprints/BP-027.md |
| BP-027 | BP-005 gains `SEVERITY_THRESHOLDS` as pinned per-problem step boundaries, since `structure.md` rule 5 puts every number in `constants.ts` and this node cannot declare them itself. Discharged: BP-005's public interface declares `SEVERITY_THRESHOLDS` with this node's decision 1 boundaries and cites this node as the asker | undefined | sdlc-factory/docs/blueprints/BP-027.md |
| BP-037 | The customer's time zone is captured once and stored as one preference and every scheduled time the shell states is expressed in it (REQ-073 criterion 1) | undefined | sdlc-factory/docs/blueprints/BP-037.md |
| BP-039 | The customer's time zone is captured once and stored as one preference, so a calendar day begins and ends in the customer's own local day (REQ-073 criterion 1) | undefined | sdlc-factory/docs/blueprints/BP-039.md |
| BP-040 | The three words a customer reads for `winnable | reach | not-yet` are owner-owed and were not written when this node was cut. Discharged 2026-08-31: the owner ruled Winnable, Reach and Not yet; they are in BP-019's `BAND_LABELS` (ADR-001) and surfaces can render a band. The rendered third term is `Not yet`, without the hyphen the internal handle carries — see BP-019 decision 6. | undefined | sdlc-factory/docs/blueprints/BP-040.md |
| BP-050 | Every date and week boundary stated to a customer is stated in the customer's own stored time zone (REQ-073 criterion 3) | undefined | sdlc-factory/docs/blueprints/BP-050.md |
| BP-051 | BP-015's `DestinationAdapter` exposes whether a destination serves the page publicly. REQ-063 c1's population is pages published to such a destination. Discharged 2026-08-31: `servesPublicly` is declared on the adapter in BP-015's interface and in BP-045's `types.ts`, where the shape actually lives; it was missing from the second and has been added. **Re-read 2026-09-01 (ADR-084): the field is now `true` at both destinations, so the population includes every WordPress page — the seam held and no rule here changed. The clause that used to end this row, 'a WordPress delivery the customer must publish themselves is outside it', is refuted, and the non-goal it cited has been deleted from REQ-063.** | undefined | sdlc-factory/docs/blueprints/BP-051.md |
| BP-052 | The three customer-facing words for `near`, `middle` and `far` are the owner's and did not exist when this node was cut (REQ-096 criterion 9, ADR-001). This node emits handles only. Discharged 2026-08-31: the owner ruled `near` → Similar size, `middle` → Larger, `far` → Much larger; they are recorded in BP-019's `BAND_LABELS` and are disjoint from the winnability three, which BP-019 decision 6 checked before recording them. | undefined | sdlc-factory/docs/blueprints/BP-052.md |
| BP-058 | REQ-074 gains a criterion for the destinations list in the `failing`-because-cannot-publish state. REQ-060 criterion 7 fixes what the customer must and must not be told there and narrowed its own non-goal to REQ-074 criterion 1 accordingly, so REQ-074 today covers only the expired-credential occasion. Until that criterion exists this node renders the occasion from REQ-060 criterion 7 alone — enough to build, and not enough for the destinations list to own the promise (rule 5.7). Raised by the requirements-analyst on 2026-09-01; nobody held REQ-074 that pass and the architect does not edit requirements. **Discharged 2026-09-02: REQ-074 criterion 8 exists and carries it, stated for any destination rather than for WordPress alone (`requirements/history/REQ-074-2026-09-01-cannot-publish-criterion-and-jn-004-step.md`). This node's own ancestor now owns the promise its destinations list renders, so rule 5.7 is satisfied for the occasion. Criterion 8 ratifies every clause this node already carried — the `failing` state, the connection-is-live line, the three prohibitions, the action to a different account — and adds one that was not specified anywhere: a re-entry of the stored credential leaves the destination not reading as working, publishes nothing, and tells the customer the account still cannot publish. That clause is now stated under *Error & edge behavior*.** | undefined | sdlc-factory/docs/blueprints/BP-058.md |
| BP-059 | The three toggles are exactly the MAIL_KINDS rows whose stoppability is 'toggle' — draft-ready, published, weekly — as REQ-075's own confirmed rests-on states; a fourth recurring kind would be a change to REQ-075 before it is a change to this node | undefined | sdlc-factory/docs/blueprints/BP-059.md |
| JN-003 | The customer's timezone is captured once and stored as one preference, so 'the evening before', the veto window and 'the next morning' are the customer's own local times throughout this journey — the single stored preference REQ-073 criterion 1 captures with a stated default ('the time zone is the one their browser reported at first sign-in, which they may change here at any time afterwards'). | undefined | sdlc-factory/docs/journeys/JN-003.md |
| REQ-003 | A free visitor has no account and therefore no stated timezone, so every time this requirement tells a visitor is an elapsed duration, never a clock time | undefined | sdlc-factory/docs/requirements/REQ-003.md |
| REQ-015 | A free visitor has no account and therefore no stated timezone, so every time this requirement tells a visitor is an elapsed duration, never a clock time | undefined | sdlc-factory/docs/requirements/REQ-015.md |
| REQ-040 | The customer's timezone is captured once and stored as one preference, and every scheduled time and window the product shows or acts on is expressed in it — the single stored preference REQ-073 criterion 1 captures with a stated default ('the time zone is the one their browser reported at first sign-in, which they may change here at any time afterwards'). | undefined | sdlc-factory/docs/requirements/REQ-040.md |
| REQ-043 | The customer's timezone is captured once and stored as one preference, so a calendar day begins and ends in the customer's own local day, not the server's — the setting REQ-073 criterion 1 records, defaulting to 'the one their browser reported' at first sign-in. | undefined | sdlc-factory/docs/requirements/REQ-043.md |
| REQ-065 | Every date and week boundary this requirement states to a customer — the day the week's measurement was taken, the day the next one is due — is stated in the customer's own stated time zone, the single stored preference REQ-073 criterion 3 governs. | undefined | sdlc-factory/docs/requirements/REQ-065.md |
| REQ-071 | Every customer-facing scheduled time and date in this requirement — the date a re-measurement is due, the date a question set changed, the date pages resume — is stated in the customer's own stated time zone, the single stored preference REQ-073 criterion 3 governs. | undefined | sdlc-factory/docs/requirements/REQ-071.md |
| REQ-073 | Every customer has a stated time zone from first sign-in onward — the browser reports one then and criterion 1 makes it settable afterwards — so every customer-facing scheduled time and date in the product has a zone to be stated in and none falls back to the server's. | undefined | sdlc-factory/docs/requirements/REQ-073.md |
| REQ-075 | The three toggles in BUILD §4.7 are the three recurring emails a paying customer receives — draft-ready, published, weekly — and every other kind in the set REQ-064 registers is transactional or pre-purchase and therefore not togglable. What a toggle reaches is an occasion rather than a whole kind: one occasion of a togglable kind is itself transactional and stays outside these toggles — the draft-ready mail sent under autopilot at a veto window of zero, which is the whole of the customer's telling that a page is going live and which REQ-057 criterion 7 keeps unsuppressible. | undefined | sdlc-factory/docs/requirements/REQ-075.md |
| REQ-076 | Every customer-facing date this requirement states — the next invoice date, the paid-through date access ends on — is stated in the customer's own stated time zone, the single stored preference REQ-073 criterion 3 governs. | undefined | sdlc-factory/docs/requirements/REQ-076.md |
| REQ-080 | Every customer-facing date this requirement states — the date the re-measurement is due, the date pages resume — is stated in the customer's own stated time zone, the single stored preference REQ-073 criterion 3 governs ('it publishes at that hour in the customer's stated time zone'). | undefined | sdlc-factory/docs/requirements/REQ-080.md |
| WO-001 | `structure.md` fixes the top-level directory set but names no owner for the repo's root configuration FILES (package.json, tsconfig.json, eslint/vitest/postcss config, .env.example). They are assigned to BP-001 here because BP-001 already owns `next.config.ts` and the deployable Next.js unit those files configure; rule 7 is not engaged, which governs top-level DIRECTORIES, and the set is unchanged. **Discharged 2026-08-31**: BP-001 decision 2 adopts exactly this assignment and globs all seven files onto this node, and `structure.md` now carries a row for them; the provisional placement this row raised is now the recorded one. | undefined | sdlc-factory/docs/work-orders/WO-001.md |
| WO-001 | `factory.config.json` carries `"schema": null` and no `typeCheck` key, so the project's type-check command is derived here rather than read: `npm run typecheck` -> `tsc --noEmit`, the ecosystem's native compiler invocation for a TypeScript project. It cannot be run before this work order lands, because the repo holds no TypeScript. | undefined | sdlc-factory/docs/work-orders/WO-001.md |
| WO-006 | One value BP-005 decision 3 lists as deliberately undeclared stays undeclared here: the removal address REQ-002 criterion 1 requires the free report to name. It is a customer-visible string and the owner's under the decision-rights table; it is absent, not guessed (rule 1.2). Whether anything records receipt of a removal request is outstanding with it. The other four this row carried were ruled by the owner on 2026-08-31 (BP-005 decision 3, "Discharged 2026-08-31 by owner ruling"): the six band words and the three severity words landed in BP-019's `BAND_LABELS` and `SEVERITY` — not here, decision 2 stands — and `GOAL_VALUES.pages_published` landed here as 30. | undefined | sdlc-factory/docs/work-orders/WO-006.md |
| WO-006 | `AI_READER_AGENTS` and `PLATFORM_DOMAINS` are populated from quoted source clauses — REQ-059 criterion 4 and `BUILD.md` §6.1 respectively — never from memory. `BUILD.md` §6.1's `PLATFORM_DOMAINS` row ends in an ellipsis ("… (closed list)"), so the list it states is not complete on its face; the implementer takes the ten names §6.1 does state and raises the incompleteness rather than inventing an eleventh. | undefined | sdlc-factory/docs/work-orders/WO-006.md |
| WO-013 | BP-003's open question — Inngest versus Vercel cron plus a queue — is taken here, as BP-003 itself directs ("taken at M1 by the first work order under this node"). Choice: **Inngest**. Derivation: `BUILD.md` §1 names it in bold as the choice and the parenthetical as the alternative, and BP-003's `rests-on` already assumes at-least-once delivery with durable retry and per-function concurrency, which Inngest provides and a bare cron does not. It is a platform choice with no customer-visible consequence, so it is the system's under rule 1.1. Reversal cost: the trigger layer only — BP-003 decision 3 makes the state machine the queue, so no job body and no table changes; one adapter file and one route. **Confirmed 2026-08-31**: BP-003 decision 4 records Inngest on this derivation, checked, and states the same reversal cost. | undefined | sdlc-factory/docs/work-orders/WO-013.md |
| WO-013 | BP-003 states the decision entry recording that choice belongs in BP-003's own `## Decisions`. Only the architect may edit a blueprint, so this work order records the derivation above and the entry is owed by the architect (`/decide` or `/sync`). **Discharged 2026-08-31**: BP-003 decision 4, "The job platform is Inngest" — Status: accepted, entry written 2026-08-31, choice taken by this work order as that node directed. Nothing is owed. | undefined | sdlc-factory/docs/work-orders/WO-013.md |
| WO-041 | Nine values in COPY were owner-owed at cut time and no agent could fill them (BP-020 rests-on row 1: the three winnability band words, the three rival-size band words, REQ-009 criterion 8's three severity words). **Discharged 2026-08-31**: the owner ruled all nine and BP-019 decision 6 records them verbatim, so this WO transcribes them from that approved entry into `keys/bands.ts` rather than seeding them empty. Every other seeded key remains owner-owed and empty, and the machinery below is what makes an unfilled key a loud failure instead of a blank. | undefined | sdlc-factory/docs/work-orders/WO-041.md |
| WO-054 | BP-005 gains the four score-band boundaries of `BUILD.md` §5 — 0–24, 25–49, 50–74, 75–100 — as a pin under a name BP-005 chooses. **Discharged 2026-08-31**: BP-005 declares `SCORE_BAND_BOUNDS: { invisible: 0; 'hard-to-find': 25; findable: 50; dominant: 75 }`, named so as not to collide with BP-019's `SCORE_BANDS` copy map, and BP-024's `bandOf` comment now names it. This WO imports that pin and does not create it | undefined | sdlc-factory/docs/work-orders/WO-054.md |
| WO-055 | `RobotsPolicy` has a shape. **Discharged 2026-08-31**: BP-006 declares it — `ok`, `origin`, `readAt`, `disallowsAll`, `disallowedAgents`, `sitemaps`, `absent` — and WO-018 ships the declaration in `src/lib/egress/types.ts`. This WO imports that type and reads `disallowedAgents` and `absent`; it declares no local reader interface | undefined | sdlc-factory/docs/work-orders/WO-055.md |
| WO-061 | BP-012's `readCurrentReport(domain): Promise<StoredReport | null>` exists before this WO runs, and `StoredReport` carries the fields the control table reads. **Discharged 2026-08-31**: BP-012 decision 3 declares `StoredReport`, and the three fields this table reads are on it by name — `measuredAt`, `complete` and `fromIncompleteRescan`. This WO imports the type and defines no local reader interface; the remaining assumption is only that BP-012's module exists before this one runs, which `depends-on` does not express because the type is another node's | undefined | sdlc-factory/docs/work-orders/WO-061.md |
| WO-066 | The three severity words are the owner's (constitution §1, customer-visible strings). This WO renders BP-019's `SEVERITY` triple by key and emits no word of its own. **Discharged 2026-08-31**: the owner ruled them — `low → "Minor"`, `mid → "Worth fixing"`, `high → "Critical"`, recorded in BP-019 decision 6 — and WO-041 transcribes the three strings into the copy registry, so the keys resolve and this WO no longer holds for copy. It still emits no word of its own | undefined | sdlc-factory/docs/work-orders/WO-066.md |
| WO-068 | No blueprint's `code:` glob covers the report's header-strip markup. **Discharged 2026-08-31**: BP-024's `## Module / boundary` now states it — 'The header strip itself is a registered component (BP-018) composed by BP-022's shell and written through BP-019's `renderMeasured`; no sentence and no band word is written here' — which is exactly what this WO does, in BP-022's `_address/` glob. BP-022's own boundary states the other half: 'this node owns the shell that decides whether a body renders at all, and the order the modules sit in (`BUILD.md` §4.1)'. No file here sits outside a declared glob | undefined | sdlc-factory/docs/work-orders/WO-068.md |
| WO-068 | The four band words reach a screen only through BP-019's `SCORE_BANDS`, and the three driver names through the copy registry. Both are BP-019/BP-020's; this WO renders keys and writes no word. **Confirmed 2026-08-31**: BP-019 declares `SCORE_BANDS` over the four handles with REQ-004 criterion 1's own words — 'transcribed, never chosen' — and WO-041 carries the four strings in `keys/bands.ts`, so the keys resolve | undefined | sdlc-factory/docs/work-orders/WO-068.md |
| WO-080 | `COHERENCE: { minMeasuredSearches: 3; shareDivisor: 4; minAppearances: 2 }` is declared in BP-005's `## Public interface` — verbatim, `export const COHERENCE: { minMeasuredSearches: 3; shareDivisor: 4; minAppearances: 2 }`, under the header 'Added 2026-08-31 (decision 3)' and traced there to 'BP-028 · REQ-094 c2 · BUILD §6.7 step 5'. Confirmed 2026-08-31: BP-028's `## NFR budget` note that it was 'not yet listed in BP-005's interface, reported to the parent' is spent and BP-028 now records the pin as declared. `src/lib/config/constants.ts` is BP-005's file and is not touched here; if the block is absent from that file when this WO starts, that is a `blocked-by` on BP-005's work order, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-080.md |
| WO-082 | `CORRECTION: { perScan: 1; retries: 1; offerMaxAgeDays: 7 }` is declared in BP-005's `## Public interface` — verbatim, `export const CORRECTION: { perScan: 1; retries: 1; offerMaxAgeDays: 7 }`, under the header 'Added 2026-08-31 (decision 3)' and traced there to 'BP-028 · REQ-094 c5–c7'. Confirmed 2026-08-31: BP-028's `## NFR budget` note that it was 'not yet listed in BP-005's interface, reported to the parent' is spent and BP-028 now records the pin as declared. `src/lib/config/constants.ts` is BP-005's file and is not touched here; if the block is absent from that file when this WO starts, that is a `blocked-by` on BP-005's work order, added at that moment. | undefined | sdlc-factory/docs/work-orders/WO-082.md |
| WO-086 | The three customer-facing words for `near`, `middle` and `far` are the owner's (REQ-096 criterion 9, ADR-001) and did not exist when this WO was cut. This node emits handles only. Discharged 2026-08-31: the owner ruled `near` → 'Similar size', `middle` → 'Larger', `far` → 'Much larger', recorded in BP-019's `BAND_LABELS.rivalSize` under the keys `band.rivalSize.near`, `band.rivalSize.middle` and `band.rivalSize.far`. BP-019 decision 6 checked ADR-001 point 3 before recording them: the rendered rival-size set and the rendered winnability set {Winnable, Reach, Not yet} intersect emptily, so `tests/pins.test.ts`' disjointness assertion passes and no longer skips. This WO's plan is unchanged by the ruling — it writes none of the three strings. | undefined | sdlc-factory/docs/work-orders/WO-086.md |
| WO-091 | Every customer-facing scheduled time and date in this requirement — the date a re-measurement is due, the date a question set changed, the date pages resume — is stated in the customer's own stated time zone, the single stored preference REQ-073 criterion 3 governs. | undefined | sdlc-factory/docs/work-orders/WO-091.md |
| WO-147 | The three winnability band words are owner-owed strings rendered through BP-019's registry; this WO creates keys and tests, never strings. Discharged 2026-08-31 by owner ruling — Winnable, Reach, Not yet, recorded in BP-019's BAND_LABELS under band.winnability.{winnable,reach,notYet} | undefined | sdlc-factory/docs/work-orders/WO-147.md |
| WO-154 | The customer's time zone is captured once and stored as one preference and every scheduled time the shell states is expressed in it (REQ-073 criterion 1) | undefined | sdlc-factory/docs/work-orders/WO-154.md |
| WO-161 | The goal value for the Pages published headline number was the owner's and was outstanding (BP-038 decision 2 and its open question). Discharged 2026-08-31 by owner ruling — 30, meaning a month of daily pages. The value is pinned in BP-005's GOAL_VALUES, BP-038's open question is closed, and this tile renders its goal exactly as the other three do. | undefined | sdlc-factory/docs/work-orders/WO-161.md |
| WO-164 | The customer's time zone is captured once and stored as one preference, so a calendar day begins and ends in the customer's own local day (REQ-073 criterion 1) | undefined | sdlc-factory/docs/work-orders/WO-164.md |
| WO-166 | The three winnability band words are owner-owed; this WO emits the handle and never a word, and rendering goes through BP-019's BAND_LABELS registry (ADR-001). Discharged 2026-08-31 by owner ruling — Winnable, Reach, Not yet, under band.winnability.{winnable,reach,notYet} | undefined | sdlc-factory/docs/work-orders/WO-166.md |
| WO-168 | The three winnability band words are owner-owed and this panel renders them through BP-019's BAND_LABELS registry, never with a literal (ADR-001). Discharged 2026-08-31 by owner ruling — Winnable, Reach, Not yet, under band.winnability.{winnable,reach,notYet} | undefined | sdlc-factory/docs/work-orders/WO-168.md |
| WO-174 | Every date and week boundary stated to a customer is stated in the customer's own stored time zone (REQ-073 criterion 3) | undefined | sdlc-factory/docs/work-orders/WO-174.md |
| WO-175 | Every active site stores a time zone (REQ-073 criterion 1, defaulting to the one the browser reported at first sign-in), so a site-local Monday is always computable | undefined | sdlc-factory/docs/work-orders/WO-175.md |
| WO-182 | BP-005's `## Public interface` declares `export const WINNABILITY: { qualifyFloor: 500; qualifyMultiple: 5; nearFloor: 100; nearMultiple: 2 }` and `tests/pins.test.ts` asserts it, so the four numbers this work order needs already have exactly one home and are imported, never re-declared. | undefined | sdlc-factory/docs/work-orders/WO-182.md |
| WO-184 | BP-012's `StoredReport` exposes the sections BP-025 and BP-052 declare they write — `market`, `questions`, `answers`, `rivals`, `rivalSizes` — plus, per target search, the top-ten organic result rows (domain, url, position) the scan bought. Every value this WO reads is one of those; none is composed here (REQ-093 c4). Discharged 2026-08-31 for the five sections this WO reads: BP-012's `## Public interface` declares `StoredReport` with `questions`, `answers`, `serps`, `rivals` and `rivalSizes`, and `serps: readonly Measured<SerpResult>[]` is the per-target top ten. | undefined | sdlc-factory/docs/work-orders/WO-184.md |
| WO-187 | BP-019 renders the day-panel line from the handles this function returns; no word of it is written here (ADR-001). Discharged 2026-08-31: the owner ruled the three winnability words and BP-019's `BAND_LABELS.winnability` now maps `winnable -> band.winnability.winnable -> "Winnable"`, `reach -> band.winnability.reach -> "Reach"`, `not-yet -> band.winnability.notYet -> "Not yet"`. BP-040's third `rests-on` is `confirmed`. Nothing in this work order changes: it still returns the handle and no word. | undefined | sdlc-factory/docs/work-orders/WO-187.md |
| WO-205 | BP-015's `DestinationAdapter.servesPublicly` is the population boundary. Discharged 2026-08-31: the field is declared on the adapter in BP-015's interface and in BP-045's `src/lib/publish/types.ts`, where the shape actually lives. **Re-read 2026-09-01 (ADR-084): it is now `true` at both destinations, so the judged population includes every WordPress page and no rule in this work order changed to admit it.** The clause that used to end this row — that a WordPress delivery the customer must publish themselves is outside the population — is refuted, and the REQ-063 non-goal it rested on has been deleted from the requirement. | undefined | sdlc-factory/docs/work-orders/WO-205.md |
| WO-211 | `sites.timezone` is present and non-null for every site — BP-002's declared addition and REQ-073's own confirmed `rests-on` — so no ceiling computation falls back to the server's zone. | undefined | sdlc-factory/docs/work-orders/WO-211.md |
| WO-259 | The credential the signed-in customer arrives at that address with is **their own WordPress session**, not the application password ReachKit holds. BP-039 states it: 'This panel therefore has no dependency on what the *application password* can do — the credential the signed-in customer arrives with is not ReachKit's.' So what the address opens, and whether the post is editable there, is theirs and is not promised by this work order (REQ-043's non-goal, as rewritten 2026-09-01). | undefined | sdlc-factory/docs/work-orders/WO-259.md |
| WO-267 | `fetches` is one of `BUILD.md` §10's nine but `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic, and BP-007's `## Data model delta` fully specifies the table including its unique key and its index. Derivation (rule 1.1): the baseline creates the eight tables no other node owns and BP-007's own migration (WO-021) creates `fetches`, so one table has one writer. Reversal cost: moving one `create table` between two migrations that both run once, before any data exists — cheap today, a data migration later, which is why it is recorded now. | undefined | sdlc-factory/docs/work-orders/WO-267.md |
| WO-267 | `publications` carries no `verdict` column, `BUILD.md` §10's key-column list notwithstanding: BP-002's data-model delta states the exclusion and BP-051 decision 1 holds the reasoning. The baseline is written without it and BP-051's `page_verdicts` is the home of a page's standing. | undefined | sdlc-factory/docs/work-orders/WO-267.md |
| WO-267 | `users.deleted_at` does not exist until BP-063's `*_users_erasure_*.sql` lands. The policy condition ADR-051 requires is therefore written here against a column this migration adds a nullable placeholder for, or the policies are re-issued by BP-063's migration. The implementer takes the first: the baseline gains no column, this policy migration adds `users.deleted_at timestamptz` as its own precondition, and BP-063 owns every rule about setting it. | undefined | sdlc-factory/docs/work-orders/WO-267.md |
| WO-269 | A real layout engine is what makes this suite measure anything (ADR-093 rests-on row 1, in this order's words): `vitest.config.ts` today routes every file under `tests/ui/**` to jsdom, where every box metric is 0 and every one of checks 1–4 holds on every element of every page. A layout suite dropped into `tests/ui/layout/` without the `layout` project split in this file plan passes vacuously. The canary fixture is what discharges this: the run in which the canary's checks 1–4 do not fail is the run that measured nothing. | undefined | sdlc-factory/docs/work-orders/WO-269.md |
| WO-269 | Nothing in this repository enforces a failing suite (ADR-093 rests-on row 2; ADR-010 point 6). No `.github/workflows`, no hook, no branch protection, and `npm run test:layout` is not part of `npm test`. A layout failure is recorded in the run's output and nowhere else until a platform check exists (rule 3.3). | undefined | sdlc-factory/docs/work-orders/WO-269.md |
| WO-269 | `sdlc-factory/docs/design/components.md` carries no `Surface` row (grepped 2026-09-02: none). BP-018 decision 3 says `design/` owes it. WO-037's two-way registry test will name the gap until it lands; this order does not write under `design/`. | undefined | sdlc-factory/docs/work-orders/WO-269.md |
| WO-270 | `structure.md` rule 7 opens with "A new top-level directory requires an ADR" and, as amended by the librarian's backward pass (commit 1fdab38), admits `.claude/` by a registry edit rather than by an ADR. This order asserts the set exactly as the registry states it and takes that amendment as the authority on the set — the test's job is to agree with `structure.md`, not to rule on how `structure.md` was changed. If an ADR later records the admission, nothing here moves; if the admission is reversed, reversal is one entry removed from `allowed` and the comment above it. | undefined | sdlc-factory/docs/work-orders/WO-270.md |
| WO-272 | WO-267's `topicOf()` resolves a `users`- or `sites`-topic migration whose remaining name segments match no registered sub-token to the topic's owner, BP-017, rather than refusing it. WO-267's naming test states the rule as 'exactly one assigned topic token or one sub-token of an assigned topic; a name carrying none, or two, fails', and this order's two file names each carry one topic token and no sub-token. Derivation (rule 1.1): structure.md rule 3a says a component-level column needs no sub-token ('the sub-token list is open and grows with the leaves'), so no new token is minted here. Reversal cost: a file rename before any data exists, or two rows in rule 3a's list — either is cheap; observable by this order's validator against WO-267's applied test. | undefined | sdlc-factory/docs/work-orders/WO-272.md |
| WO-272 | `sites.timezone` is nullable because the `sites` row exists from provisioning (WO-126) and the zone arrives only at first sign-in (REQ-073 criterion 1: 'the time zone is the one their browser reported at first sign-in'), and REQ-073 forbids a zone the customer did not state; in the interval between, nothing that reads the column runs — WO-211's ceiling check and WO-218's settings read both follow sign-in and setup. WO-211 and WO-218 rest on the column being 'present and non-null for every site'; this order narrows that to every site past first sign-in, and reports that no order in the corpus yet writes the browser's zone at first sign-in. Reversal cost: `set not null` after a backfill — cheap before customers exist. | undefined | sdlc-factory/docs/work-orders/WO-272.md |

## unconfirmed

None.

