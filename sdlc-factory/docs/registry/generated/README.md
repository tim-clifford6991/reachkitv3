# Generated — do not hand-edit

Everything under this directory (`traceability.md`, `orphans.md`,
`blocked.md`, `assumptions.md`, `graph.json`) is projected by the librarian
from every artifact's own front-matter, never written by hand. An index
that is authored can lie; an index that is derived cannot (constitution
rule 5.1) — if one of these files looks wrong, fix the front-matter of the
artifact it was derived from and regenerate, don't edit the file itself.
Each carries its own banner and its own coverage statement (rule 5.5) so a
clean-looking empty view can be told apart from an unpopulated one.
