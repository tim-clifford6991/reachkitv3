---
id: WO-051
type: work-order
title: "The one canonical domain parser — parseDomain, CanonicalDomain, DomainProblem"
status: approved
ui: no
implements: [BP-022, BP-023, REQ-001]
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "A maintained public-suffix list is available to the build (the ICANN/Mozilla PSL is the de facto one), so 'has a public suffix' is decidable without a network call at request time — ADR-020's own row, inherited because this WO is where it is first spent"
    disposition: confirmed
  - claim: "The Next.js + TypeScript scaffold (package.json, tsconfig.json, next.config.ts) exists before this WO runs. It is BP-001's, not this node's — `structure.md` puts `next.config.ts` in BP-001's globs — and this repo carries none of it today"
    disposition: confirmed
supersedes: []
wave: W2
---

# WO-051 — The one canonical domain parser — `parseDomain`, `CanonicalDomain`, `DomainProblem`

- Branch: wo/WO-051-canonical-domain
- Estimate: ≤1 day

## Goal
Ship the product's single domain parser and its branded key, so that every
written form of one domain becomes one value and every malformed value becomes a
named handle, in the module ADR-020 fixes.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`,
> no `package.json` and no `tsconfig.json`. Placement follows
> `sdlc-factory/docs/registry/structure.md` (`src/lib/scan/**` is BP-012's
> container; BP-023 anchors `src/lib/scan/domain.ts` by the narrower glob) and is
> provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/scan/domain.ts` | NEW | The whole public interface below: the branded `CanonicalDomain`, the `DomainProblem` handles, `DomainParse`, and `parseDomain`. Canonicalisation is ADR-020 decision 1, in this order: lowercase; strip scheme; strip userinfo, port, path, query and fragment; strip a leading `www.`; strip a trailing dot; convert internationalised labels to punycode A-labels. Rejections are ADR-020 decision 2's five handles and no others. No DNS call, no fetch, no clock (ADR-020 decision 3). The public-suffix data is loaded once at module scope and held in memory. |
| `tests/scan/free/domain.test.ts` | NEW | The fixture suite named in the test plan. Table-driven: one row per written form, one row per rejection, plus the idempotence and injectivity properties. |

## Interfaces

Consumes: nothing inside this repository. One external data set — a public
suffix list — per this WO's first `rests-on` row.

Exposes (verbatim from BP-023 `## Public interface`, `src/lib/scan/domain.ts`):

```ts
// ── src/lib/scan/domain.ts — ADR-020 ───────────────────────────────────────
/** The product's one domain key. Produced only by `parseDomain`; every
 *  domain-keyed row, URL segment, cache key and counter uses this value. */
export type CanonicalDomain = string & { readonly __canonicalDomain: unique symbol }

export type DomainProblem =
  | 'empty' | 'not_a_hostname' | 'ip_literal' | 'no_public_suffix' | 'too_long'

export type DomainParse =
  | { ok: true;  domain: CanonicalDomain }
  | { ok: false; problem: DomainProblem }

/** Accepts every written form REQ-001 criterion 2 names — scheme or none, `www`
 *  or none, trailing path, any letter case, trailing dot, IDN — and returns one
 *  value for all of them. Does not resolve DNS and does not fetch. */
export function parseDomain(raw: string): DomainParse
```

## Steps
1. Add the public-suffix data source to the build and load it at module scope;
   assert at load that it is non-empty.
2. Write the failing fixture suite first (constitution §8, "watch it fail
   first"): every row of the test plan below, against a `parseDomain` that does
   not yet exist.
3. Implement the canonicalisation in ADR-020 decision 1's order.
4. Implement the five rejections. `too_long` is the label-length / total-length
   bound the hostname grammar fixes; `not_a_hostname` is anything that is not a
   sequence of hostname labels after canonicalisation.
5. Brand the success value. `CanonicalDomain` has exactly one producer — export
   no cast helper, no `asCanonicalDomain`, and no constructor beside
   `parseDomain`.
6. Run the suite; confirm every test that passed before step 3 existed is now
   failing for the right reason and passing after.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-001.md`; line breaks are
> normalised to fit the table and no word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-001 c2 — "Given a visitor who enters a domain in any of its written forms — with or without a scheme, with or without `www`, with a trailing path, in any letter case — when they submit, then they arrive at the single public report address for that domain; no two of those forms produce different addresses or different stored reports." | `domain/parse · one domain, every written form, one key` — a table of at least these forms of one domain: `example.com`, `EXAMPLE.COM`, `www.example.com`, `http://example.com`, `https://WWW.Example.com/pricing?a=1#x`, `example.com.`, `example.com:8443` — asserts all parse `ok` and all yield the identical `CanonicalDomain` string. The address-level half of this criterion is WO-061's `canonicalRedirect` test; the key-level half is here. |
| REQ-001 c3 — "Given a visitor who submits a value that is not a well-formed public domain name — an empty value, an IP literal, or a hostname with no public suffix — when they submit, then one written line names what is wrong with the value and the visitor stays on the landing page with their input intact. A well-formed domain is accepted here whether or not it resolves; a domain that cannot be reached is a failed scan (REQ-003), not a rejected input." | `domain/parse · the three named rejections carry their own handle` — `''` and `'   '` → `empty`; `192.0.2.1`, `[2001:db8::1]`, `http://192.0.2.1/` → `ip_literal`; `localhost`, `myserver`, `example.invalidtld` → `no_public_suffix`. And `domain/parse · a well-formed domain that cannot resolve is accepted` — a domain in a real public suffix that resolves nowhere parses `ok`, and the test asserts by spy that no DNS or network API was called during the parse. The written line itself is BP-019's and is WO-070's / WO-069's to render. |
| REQ-001 c4 — "Given a report address whose domain segment is not a well-formed public domain name, when it is opened, then one written line names what is wrong with it and the visitor is offered the landing field — never a blank page, an unhandled error, or a scan." | `domain/parse · never throws` — a property test over adversarial inputs (empty, 10 000 characters, control characters, lone surrogates, `../`, `%00`, a full URL with credentials) asserting `parseDomain` returns a `DomainParse` for every one of them and throws for none. The rendering half of c4 is WO-069's. |
| REQ-001 c2, injectivity half | `domain/parse · distinct domains never collide` — `example.com`, `www2.example.com`, `example.co.uk`, `xn--e1afmkfd.xn--p1ai` and the unicode form of the last parse to four distinct keys, and the unicode and punycode forms of the same IDN parse to one. |
| REQ-001 c2, stability half | `domain/parse · canonical form is a fixed point` — for every `ok` result, `parseDomain(result.domain)` returns the same value. A parser that is not idempotent produces two keys for one domain on the second hop. |

## Out of scope
- DNS resolution of any kind (ADR-020 decision 3; `resolvesInDns` is BP-006's).
- The 308 redirect policy and any URL construction — BP-022's `canonicalRedirect`,
  WO-061.
- The written line for each `DomainProblem` — a `CopyKey` in BP-019's registry,
  owned by another node.
- Any use of the key: admission (WO-057), the `scans` row (WO-056), the report
  address (WO-069).

## Rollback
Delete both files. Nothing imports them until WO-057 and WO-062 land, and no
migration and no stored data depends on this WO.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022, BP-023 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-001 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-001); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — `src/lib/scan/domain.ts` (`parseDomain`/`CanonicalDomain`/`DomainProblem`) and `tests/scan/free/domain.test.ts` (34 tests, all 5 test-plan rows) land on `wo/WO-051-canonical-domain`; watch-fail-first and a deletion mutation both confirmed against the suite; `npm test` (430/430) and `tsc --noEmit` pass; ready for validation.
- 2026-09-03 finished — validator — TST-017 pass: all 5 test-plan rows independently reproduced and discriminate under 5 targeted mutations; `npm test` 430/430, `npm run lint` clean on the 3 WO files, `tsc --noEmit` and `npm run build` clean; `psl.d.ts` deviation verified independently (real failure without the stub, real failure of the inline-`declare module` alternative, stub narrowly scoped to `parse` only) and accepted as build plumbing under rule 2.5, not a fourth file plan entry; one coverage gap found and stated (the `too_long`/`not_a_hostname` `DomainProblem` classification is exercised by the adversarial suite but never asserted by value — confirmed by mutation — inert against this WO's own test-plan rows, all of which quote REQ-001 verbatim and pass). Both `rests-on` rows confirmed (this WO and, upstream, ADR-020's identical row). `ui: no` — no Placement line required.

## Validation report (appended by validator)

## TST-017 — WO-051 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-051`, branch `wo/WO-051-canonical-domain`, implementer HEAD `40a7ec9` (single commit on top of `aa50c59`). Working tree clean before and after this pass (files touched by this report — `WO-051.md`, `ADR-020.md` — are this pass's own edits).
- Corpus-global id: `TST-017` — next unused after a corpus-wide scan (`grep -rn "^## TST-" sdlc-factory/docs/work-orders/*.md`; highest existing is `TST-016`, WO-002's re-validation).

### Scope check

`git show 40a7ec9 --stat` (the implementer's one commit) touches exactly: `package.json`, `package-lock.json` (the `psl` dependency), `sdlc-factory/docs/work-orders/WO-051.md` (this WO's own `## Log`), `src/lib/scan/domain.ts`, `src/lib/scan/psl.d.ts`, `tests/scan/free/domain.test.ts`. Nothing outside this WO's declared surface. (The wider `doctrine-0.11.0`-diff I first pulled — `BP-001.md`, `BP-002.md`, `waves.md`, generated registry files, several other WOs' `depends-on` edges — predates the implementer's commit; those landed on this branch from the wave-propose step (`aa50c59`) before implementation started and are not this WO's diff.)

### Criterion-by-criterion (WO's own `## Test plan`, 5 rows)

| # | Acceptance criterion | Test | Result |
|---|---|---|---|
| 1 | REQ-001 c2 — every written form of one domain yields one key | `domain/parse · one domain, every written form, one key` (7 forms, all `ok`, all identical `CanonicalDomain`) | PASS — reproduced (7/7 `ok`, 1 distinct key = `example.com`); discriminates: mutating the `www.`-strip step to a no-op turns the "identical key" assertion red (1 test failed) while the rest of the suite unaffected |
| 2 | REQ-001 c3 — empty / IP literal / no-public-suffix each carry their own handle, and a well-formed-but-unresolvable domain is still accepted with no DNS/network call | `domain/parse · the three named rejections carry their own handle` + `domain/parse · a well-formed domain that cannot resolve is accepted` | PASS — reproduced (8 rejection rows + 1 no-DNS-call row, all as specified); discriminates: inverting the `ip_literal` check flips 25/34 tests red (cascades through the fixed-point and injectivity suites too, since IP literals stop being rejected); removing the `empty` check flips exactly the 2 `empty`-row assertions red, both to `not_a_hostname` |
| 3 | REQ-001 c4 — `parseDomain` never throws, for any input | `domain/parse · never throws` (7 adversarial inputs: empty, 10k chars, control chars, lone surrogate, path traversal, `%00`, full URL with credentials) | PASS — reproduced (7/7 return a `DomainParse`, 0 throws); the function's own top-level `try/catch` is defence-in-depth per its own comment, not exercised by any of these 7 inputs (all resolve through the normal control flow to a `DomainParse`), so this row shows the URL-based parse itself is total over the sampled adversarial space, not that the catch clause fires |
| 4 | REQ-001 c2, injectivity half — distinct domains never collide, IDN unicode/punycode forms of one domain do collide | `domain/parse · distinct domains never collide` (4 distinct + 1 IDN-unicode dup) | PASS — reproduced (4 distinct keys, unicode/punycode IDN forms equal); discriminates: inverting `no_public_suffix` (so a listed suffix is rejected and vice-versa) flips 22/34 red, including this row, since `xn--e1afmkfd.xn--p1ai` stops parsing `ok` |
| 5 | REQ-001 c2, stability half — canonical form is a fixed point | `domain/parse · canonical form is a fixed point` (9 inputs, re-parse the output) | PASS — reproduced (9/9); this row is also the one most sensitive to classification-branch mutations (it failed under 2 of the unrelated mutations above, §Mutation summary, because a mutated `ok:false` on an IDN input breaks the re-parse chain before the fixed-point assertion runs) |

### Mutation summary (constitution §8 / step 2's discrimination requirement)

Five independent, targeted mutations against `src/lib/scan/domain.ts`, each reverted before the next:

1. Disable the `www.`-strip (`host = rawHost` unconditionally) → 1/34 red (row 1's "identical key" assertion) — correctly localised, nothing else masks or is masked by it.
2. Invert the `ip_literal` check (`isIP(...) === 0` instead of `!== 0`) → 25/34 red — the widest blast radius, expected: every IP-literal fixture and everything downstream of it (fixed-point, injectivity) depends on IP literals actually being rejected.
3. Remove the `empty` check entirely → 2/34 red, both re-classified to `not_a_hostname` — precisely localised.
4. Invert the `no_public_suffix` check (`if (parsed.listed)` instead of `if (!parsed.listed)`) → 22/34 red.
5. Collapse the `too_long`/`not_a_hostname` branch to always return `"empty"` → **0/34 red — no test discriminates this branch by value.** See Coverage gaps below; not a mutation of anything this WO's own test-plan table names.

All five mutations were independently authored and applied by the validator (not supplied by the implementer), and all five were reverted; `git status` confirms `src/lib/scan/domain.ts` is back to the implementer's committed content after this pass.

### Watch-it-fail-first (independently reproduced, not taken on the implementer's claim)

Reverting `src/lib/scan/domain.ts` to a stub that always returns `{ ok: false, problem: "empty" }` and re-running `tests/scan/free/domain.test.ts` fails the great majority of the 34 tests for the expected reason (assertion mismatch on `ok`/`domain`/`problem`, not a crash or a type error) — confirming the suite was written against a red implementation first, as the implementer's log claims and step 2 of the WO requires.

### Coverage gaps

One, stated precisely per rule 5.5: `DomainProblem` has 5 members (ADR-020 decision 2, WO step 4). The WO's own `## Test plan` — copied verbatim from REQ-001, which names only 3 rejection cases (`empty`, `ip_literal`, `no_public_suffix`) — asserts a specific `problem` value for those 3 only. The other 2 (`too_long`, `not_a_hostname`) are exercised by `domain/parse · never throws`'s adversarial inputs (independently confirmed by direct probe: a 10,000-character input returns `too_long`; control characters, a lone surrogate, `../../etc/passwd` and a `%00` suffix all return `not_a_hostname`) but that test only asserts `typeof result.ok === "boolean"` — it does not check which `problem` fires. Mutation 5 above confirms this precisely: collapsing the entire `too_long`/`not_a_hostname` branch to always return `"empty"` leaves all 34 tests green. A future regression that swaps these two handles, or folds either into a different one, would ship undetected by this suite; downstream, the written line a visitor sees (BP-019's copy registry, WO-069/WO-070's to render) is keyed on the exact `DomainProblem` value, so a swapped handle is a real, user-visible wrong-copy risk once those WOs land — not a hypothetical one. This does **not** fail any of this WO's own 5 test-plan rows, all of which are REQ-001-verbatim and all of which pass under mutation; it is a gap beyond what this WO committed to testing, stated here so WO-069/WO-070's implementer and validator (or a later pass over this file) can close it. Not a reason to fail this WO on its own criteria.

No vacuous test found among the 5 rows that are in scope — none of the 34 tests survive the mutation targeted at the code it's meant to cover.

### The `psl.d.ts` deviation — independently verified, not taken on the implementer's word

1. **`exports` map has no `types` condition.** Read `node_modules/psl/package.json` directly: `"exports": { ".": { "import": "./dist/psl.mjs", "require": "./dist/psl.cjs" } }` plus a root `"types": "types/index.d.ts"` field — no `types` condition inside `exports`. Confirmed independently, not paraphrased from the implementer's comment.
2. **`tsc --noEmit` genuinely fails without the stub.** Moved `src/lib/scan/psl.d.ts` out of the tree and re-ran `tsc --noEmit`: fails with `TS7016: Could not find a declaration file for module 'psl'. ... There are types at '.../psl/types/index.d.ts', but this result could not be resolved when respecting package.json "exports"'` — TypeScript's own message corroborates the exports/types-condition mismatch exactly as the implementer's comment states. Restored the file; `tsc --noEmit` clean again.
3. **The inline-`declare module "psl"` alternative genuinely fails, for the stated reason.** Independently pasted the same ambient block inside `domain.ts` (in place of the standalone file) and re-ran `tsc --noEmit`: fails with `TS2665: Invalid module name in augmentation. Module 'psl' resolves to an untyped module ..., which cannot be augmented.` — confirms the implementer's claim that TS treats an in-file `declare module` as an illegal augmentation of an already-resolved untyped module, not a fabricated constraint. Reverted `domain.ts` to the committed content.
4. **Narrowly scoped, not over-broad.** The real `psl` types (`node_modules/psl/types/index.d.ts`) export `parse`, `get`, `isValid`, `ParsedDomain`, and a generic `ErrorResult<T extends keyof errorCodes>` keyed to a specific error-code enum. `domain.ts` calls only `parse`. The stub declares only `parse`, `ParsedDomain` and a simplified (non-generic, `code: string`) `ParseError` — it omits `get`, `isValid` and the real error-code union entirely. It is a subset of the real surface, scoped to exactly what this module calls, not a wildcard `declare module "psl": any` or a re-declaration of the whole package.
5. **Ruling.** This is build plumbing (rule 2.5): a fact about how TypeScript resolves an untyped npm dependency under this repo's already-fixed `moduleResolution: "bundler"`, not a re-derivation of the WO's spec or a reinterpretation of its behaviour. `CanonicalDomain`, `DomainProblem`, `DomainParse` and `parseDomain`'s signature match BP-023's `## Public interface` block verbatim (diffed directly, no divergence). The file plan's 2-file count is a planning artifact from before `psl`'s specific typing gap was known (the file plan itself was "provisional against a repo with no code" — its own header says so); a third file that carries no behaviour of its own, exists solely so the other two compile under the repo's existing `tsconfig.json`, and is fully explained in both the module's own header comment and the file's own header comment, is not scope creep. Accepted.

### Structure compliance

`src/lib/scan/domain.ts` sits under `src/lib/scan/**` (BP-012's container; BP-023's narrower `code:` glob names it explicitly) and `src/lib/scan/psl.d.ts` sits beside it under the same glob. `tests/scan/free/domain.test.ts` sits under `tests/scan/free/**`, matching BP-023's `code:` front matter (`"tests/scan/free/**"`). `grep -rln "src/lib/scan/domain.ts" sdlc-factory/docs/work-orders/*.md` outside `WO-051.md` finds only `WO-254.md`, which *imports* `CanonicalDomain` (consumption, not a second definition — verified by reading the cited line). `grep -rn "function parseDomain|CanonicalDomain\b" --include="*.ts" src/` outside the two new files: no hits. No duplicate capability, no structure violation.

### ADR-020 canonicalisation order

Decision 1's stated order (lowercase; strip scheme; strip userinfo/port/path/query/fragment; strip leading `www.`; strip trailing dot; convert IDN to punycode) is implemented as: one `new URL(...)` call does lowercase + scheme/userinfo/port/path/query/fragment-strip + IDNA-to-punycode as a single WHATWG operation, *then* `www.`-strip, *then* trailing-dot-strip. This reorders "convert to punycode" earlier in the pipeline than the decision's prose lists it, but the operations commute here — `www.` is pure ASCII so stripping it before or after IDNA conversion is observationally identical, and the test suite's IDN row (`пример.рф` / `xn--e1afmkfd.xn--p1ai`) independently confirms the output is correct either way. Not a defect; a legitimate consequence of using one platform primitive (`URL`) that performs 4 of the decision's 6 steps atomically, exactly as `domain.ts`'s own header comment states.

### `rests-on` disposition (constitution rule 2.3b)

1. **"A maintained public-suffix list is available to the build ... so 'has a public suffix' is decidable without a network call at request time"** — **confirmed**. `psl@1.15.0` bundles the Mozilla/ICANN PSL into its own published package at install time (`node_modules/psl/dist/*`, read directly) and exposes no network call in `parse`/`get`/`isValid`. `domain.ts`'s module-scope smoke assertion (`pslParse("example.com")` must be `listed`) runs on every import; the full suite (430/430, including this WO's own 34/34) only passes if that assertion held. A dedicated test spies on `dns.lookup`, `dns.resolve` and `fetch` across a real parse and asserts zero calls — reproduced directly. This is the row's first spend (WO's own note); disposition set here, and the identical row on ADR-020 (this WO's parent decision) dispositioned `confirmed` alongside it, with the same evidence recorded in `ADR-020.md`.
2. **"The Next.js + TypeScript scaffold ... exists before this WO runs"** — **confirmed**. `package.json`, `tsconfig.json` and `next.config.ts` all present and used: `tsconfig.json` sets `moduleResolution: "bundler"` (read directly, and load-bearing for the `psl.d.ts` deviation above), and `npm run build` (`next build`) reports `Running next.config.ts` and compiles/typechecks/prerenders clean.

Neither row left `open`. No `rests-on` row on BP-022 or BP-023 (the other `implements` edges) concerns domain parsing, the public-suffix list, or the repo scaffold — all four of BP-023's own open rows are about network-key derivation, the free-battery timing budget, and BP-005's pinned constants, none of which this build observes — so none of those are touched here, per the instruction to disposition only what this build observed.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

pass

All 5 of the WO's own test-plan rows independently reproduced and shown to discriminate by targeted mutation (5 mutations, 4 caught with correctly localised blast radius, 1 — outside this WO's own criteria — not caught and stated as a coverage gap rather than left silent). Watch-it-fail-first independently reproduced. `npm test` 430/430, `npm run lint` clean on the three touched source/test files (repo-wide lint failures are pre-existing, outside this WO's diff), `tsc --noEmit` and `npm run build` (`next build`) both clean. The `psl.d.ts` deviation is independently verified at every claimed step — the missing `exports`-`types` condition, the genuine `tsc` failure without the stub, the genuine augmentation-error failure of the inline alternative, and the stub's narrow scope to exactly `parse` — and ruled legitimate build plumbing under rule 2.5, not a fourth file-plan entry or a reinterpretation of the WO's behaviour. BP-023's public interface matches verbatim. Structure compliant, no duplicate capability. Both `rests-on` rows dispositioned `confirmed` here, and the identical upstream row on ADR-020 dispositioned alongside it — none left `open`.

No `Regression:` line follows: that record is `/regress`'s to write, a separate dispatch from this `/validate` pass. This WO does not carry one yet and should not reach `done` until it does.
