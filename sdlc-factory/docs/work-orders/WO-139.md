---
id: WO-139
type: work-order
title: "exportEverything — complete or nothing, and never a check on whether they still pay"
status: approved
ui: no
implements: [BP-062, REQ-078, REQ-076]
depends-on: [WO-267, WO-131, WO-136, WO-138]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-139 — exportEverything — complete or nothing, and never a check on whether they still pay

- Branch: wo/WO-139-export-everything
- Estimate: ≤1 day

## Goal
Stream a complete archive of every page ReachKit wrote for a site, decided
complete before the first byte leaves, to an active, cancelled or lapsed customer
alike — and make the absence of the access check enforceable rather than
remembered.

## File plan
> **Every path below is NEW.** No `src/` exists. Anchored to
> `registry/structure.md` and BP-062's `code:` glob `src/lib/account/export/**`.
> Provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/account/export/archive.ts` | NEW | Exports `exportEverything(siteId)` and `ExportFailure` with BP-062's signatures verbatim. **Resolves the manifest, every page body and every asset byte before the first byte of the zip reaches the response** (decision 2); only then streams. A failure before streaming returns `{ ok: false, reason, lineKey: 'export.failed' }`; a failure during streaming aborts the HTTP response without a zip trailer, so the client sees a broken download rather than a short archive presented as complete. A page whose asset cannot be read fails the **whole** export (`asset_unreadable`) — skipping it is exactly the partial archive c5 forbids. Past 120 s the export fails with `timeout` rather than trickling. Asset bytes are streamed through and never buffered whole; peak heap ≤ 64 MB regardless of site size. |
| `src/lib/account/export/frontmatter.ts` | NEW | Writes each file's front-matter: title, state, publish date, live URL and take-down date, with explicit nulls where they do not apply. **File bodies are the stored `body_md` verbatim** — nothing is regenerated, re-rendered or model-touched at export time; a copy that differs from what was published is a defect. |
| `tests/account/export/archive.test.ts` | NEW | Every page in all four states appears as a Markdown file with its assets; a site with zero pages yields an archive containing the manifest and no page files; an unreadable asset fails the whole export and the retry returns `ok: false` again; a timeout returns `timeout` and no archive; a failure during streaming yields no valid zip trailer; bodies are byte-identical to `body_md`; time to first byte ≤ 10 s for a 500-page fixture. |
| `tests/account/export/frontmatter.test.ts` | NEW | The five fields per file; a never-published page carries explicit nulls; nothing in a file body was re-rendered. |
| `tests/account/export/no-access-gate.test.ts` | NEW | BP-062 decision 1, made enforceable two ways: (a) a source-level assertion that no file under `src/lib/account/export/**` imports from `src/lib/account/billing/**`, the message naming REQ-078 c2; (b) a behavioural case — a site whose `paid_through` is in the past exports a **complete** archive. The mutation case: adding an `hasActiveAccess` call fails (a). **The lint half of the same rule is not written here.** BP-001 decision 2 (2026-08-31) adds BP-062's rule verbatim to `eslint.config.mjs`, which is BP-001's file and another planner's work order; case (a) above is this node's own copy of the check and stays, because a lint config can be skipped in a worktree and case (b) is behavioural anyway (rule 3.3: a refusal blocks only where something enforces it). |

## Interfaces
Exposes (verbatim from BP-062's `## Public interface`):
```ts
export function exportEverything(siteId: string): Promise<
  | { ok: true; archive: ReadableStream<Uint8Array>; filename: string; pages: number }
  | { ok: false; reason: ExportFailure; lineKey: 'export.failed' }
>
export type ExportFailure = 'asset_unreadable' | 'record_unreadable' | 'timeout' | 'internal'
```
Consumes: `buildManifest(siteId)` (WO-138); `currentSession()` (BP-061) at the
route, which is BP-001's; `db()` (BP-002).

Does **not** consume `hasActiveAccess` — BP-062: "**This node never consults
BP-060's access gate** — that is the whole of REQ-078 criterion 2 and there is a
test that asserts the import does not exist."

## Steps
1. Write `tests/account/export/no-access-gate.test.ts` first and watch both halves
   fail.
2. Write `frontmatter.ts`.
3. Write `archive.ts` with resolve-then-stream ordering.
4. Add the failure cases: unreadable asset, timeout, mid-stream abort.
5. Run the mutation check on the import assertion.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-078 c2 — "Given a customer whose subscription is active, cancelled, or past its paid-through date, when they sign in and request an export, then it downloads to them directly and is never withheld on account of subscription state; a lapsed customer reaches it by the sign-in link REQ-076 criterion 5 keeps working." | `tests/account/export/no-access-gate.test.ts › the export module imports nothing from billing`, `› a site past its paid-through date exports a complete archive` |
| REQ-078 c3 — "Given an export is produced, when the customer opens it, then it contains every page ReachKit wrote for them — published, in review, vetoed and failed alike — as Markdown files with their assets, readable without ReachKit." | `tests/account/export/archive.test.ts › all four states appear as Markdown files with their assets`, `› bodies are byte-identical to the stored body_md` |
| REQ-078 c4 — "Given an export containing a page that was published, when the customer reads that page's file, then it carries the page's title, its publish date and the URL it was published at; and where the page is no longer published, the file says so and carries the date it was taken down." (the written-file half; the manifest half is WO-138) | `tests/account/export/frontmatter.test.ts › each file carries title, state, publish date, live URL and take-down date`, `› a never-published page carries explicit nulls` |
| REQ-078 c5 — "Given an export cannot be produced, when the customer requests one, then they are told so in one written line and are not given a partial archive presented as complete." | `tests/account/export/archive.test.ts › an unreadable asset fails the whole export with export.failed`, `› a timeout returns no archive`, `› a mid-stream failure yields no valid zip trailer` |
| REQ-076 c5 — "Given the date has passed, when the customer requests a sign-in link at their address, then it is sent, signing in works, and Settings still offers their content export (REQ-078), their billing details and a way to resume." (the export half; the sign-in half is WO-131 and WO-127, the resume half WO-133) | `tests/account/export/no-access-gate.test.ts › a lapsed customer's export is complete` |

## Out of scope
- `GET /api/export` — BP-001's signed-in download route.
- The sentence behind `export.failed` — BP-019's registry; the owner's words.
- What a **deleted** account can reach: BP-062 is explicit that this is enforced
  in BP-002's row policy, not by a check here (ADR-051 point 3).
- Scheduled, automatic or API-driven export, and mailing the archive — REQ-078
  non-goals.

## Rollback
Delete three source files and three test files. Nothing persists. Do not "roll
back" by adding an access check to the route instead: REQ-078 c2 is the one
promise a departing customer tests, and the import assertion exists to fail if it
reappears anywhere.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-062 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-078, REQ-076 as they stand on disk.
- **Bounded.** 5 files in the file plan (5 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — BP-001 decision 2's import fence recorded — the lint rule lands in BP-001's eslint.config.mjs, and this WO's own source-level and behavioural checks are marked as staying.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-011, WO-131, WO-136); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267

## Validation report (appended by validator)
