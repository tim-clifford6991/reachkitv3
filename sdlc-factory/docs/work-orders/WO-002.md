---
id: WO-002
type: work-order
title: "Route-group skeleton, the root layout and font wiring"
status: approved
ui: yes
implements: [BP-001]
wave: W1
depends-on: [WO-001, WO-029, WO-030, WO-268]
blocked-by: []
rests-on:
  - claim: "`sdlc-factory/docs/design/tokens.md` and `components.md` are `draft`/empty. Rule 7.3's preview cannot be signed until the design-guardian has registered BP-018's tokens and components; this work order's `ui: yes` is what holds it until then."
    disposition: refuted
supersedes: []
---

# WO-002 — Route-group skeleton, the root layout and font wiring

- Branch: wo/WO-002-route-skeleton
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-002.html
- Signed-off: 2026-09-03

## Goal
Create the three-route-group skeleton BP-001 decision 2 fixes and the one root
document every ReachKit-addressed surface renders inside, carrying BP-018's
theme and fonts and no product copy of its own.

## Provenance of this file plan
**This repository holds no `src/`.** Every path below is NEW. Paths are anchored
to `structure.md`'s top-level table and to BP-001's `code:` globs; the plan is
provisional against a repo with no code.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/app/layout.tsx` | NEW | The root document. Sets `<html lang="en">`, applies BP-018's font CSS variables (WO-030) and the three-state theme attribute contract (WO-029), imports `src/ui/theme.css`. Renders `{children}` and nothing else — no navigation, no heading, no string. |
| `src/app/(public)/layout.tsx` | NEW | The public group's layout per BP-001 decision 2. Declares nothing about sessions; renders `{children}`. Exists so the group is the authorisation boundary rather than the route. |
| `src/app/(account)/layout.tsx` | NEW | The account group's layout. Asserts a session is present (the check itself is WO-003's middleware; this layout consumes the result) and renders `{children}`. Holds no shell chrome — BP-037 owns `src/app/(account)/app/layout.tsx`. |
| `src/app/(hosted)/layout.tsx` | — | **NOT this work order's.** BP-047 owns it by the more specific glob (`structure.md` rule 2). |
| `tests/app/layout.test.ts` | NEW | Asserts the root layout renders no literal string, imports `src/ui/theme.css`, and sets the font variables. |
| `tests/app/route-groups.test.ts` | NEW | Asserts the three route-group directories exist and that `(hosted)` carries no layout written by this work order. |

## Interfaces
Consumes (verbatim from BP-018 `## Public interface`):
```ts
export type Tone = 'ok' | 'warn' | 'bad' | 'neutral' | 'accent'
```
and BP-018's module boundary `src/ui/theme.css` (the three-state theming) and
`src/ui/fonts.ts`.

Exposes (verbatim from BP-001 `## Public interface`, the rows this work order
creates the container for):
```
Routes (public)
  GET  /                                landing: one field, one submit
  GET  /pricing                         the scanless price surface
Routes (account, session required)
  GET  /app                             redirects to Overview
```
This work order creates the groups those routes live in; the route files
themselves belong to their leaf nodes (BP-022, BP-037) and to work orders outside
this block.

## Steps
1. Create the three route-group directories.
2. Write `src/app/layout.tsx` importing `src/ui/theme.css` and `src/ui/fonts.ts`.
3. Write the `(public)` and `(account)` group layouts as pass-throughs.
4. Write the two tests; watch the "no literal string" test fail first by adding a
   string, then remove it (constitution §8, "Watch it fail first").

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-001 carries
> `satisfies: []`. Acceptance is quoted from BP-001 and BP-018. REQ-020
> criterion 5 governs what a signed-out request to `/setup` or `/app` may say;
> the enforcing half is WO-003's middleware and the address-prompt surface is
> outside this block, so no criterion is inherited here.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-001 decision 2: "three route groups — `(public)`, `(account)`, `(hosted)` — with the authorisation rule attached to the group rather than to the route" | `tests/app/route-groups.test.ts` |
| BP-001 module boundary: "It does **not** own `src/app/(hosted)/**`, which is BP-004's." | `tests/app/route-groups.test.ts` — fails if this work order writes any file under `(hosted)`. |
| BP-018 decision 2: "no component has a default string" | `tests/app/layout.test.ts` — asserts the rendered root layout contains no text node the work order authored. |
| BP-018 error behaviour: "Every numeral, date, URL, search query and code-like string renders in JetBrains Mono with `tabular-nums`" | `tests/app/layout.test.ts` — asserts both font variables are bound on `<html>`, so the mono family is reachable everywhere. |

## Out of scope
- `src/app/error.tsx` and `src/app/not-found.tsx`: both render product sentences,
  which are BP-019's copy registry, outside this block. Named here so they are
  not written without their copy.
- Every concrete route file. Each belongs to its leaf node.
- The app shell (sidebar, three destinations) — BP-037.

## Rollback
Delete the four NEW files and the three directories. Nothing renders yet, so
there is no customer-visible consequence to reverting.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-001 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan` quote BP-001's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 5 files in the file plan (5 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-031); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-031 → WO-268
- 2026-09-03 signed-off — owner — preview sheet `design/previews/WO-002.html`, https://claude.ai/code/artifact/dd46e79b-068e-418b-ab36-baba9ebe5c30
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — `src/app/layout.tsx`, `src/app/(public)/layout.tsx`, `src/app/(account)/layout.tsx`, `src/app/(hosted)/.gitkeep`, `tests/app/layout.test.ts`, `tests/app/route-groups.test.ts` written; fail-first observed on both new tests (stray `<body>` string, and a `(hosted)/layout.tsx` mutation), both discriminate; `npm test` 395/395, `npm run test:layout` 16/16, `npm run typecheck` clean, `npm run lint` clean on touched files (pre-existing whole-repo lint debt unrelated to this WO, unchanged), `npm run build` clean; ready for validator.
- 2026-09-03 failed — validator — TST-015: criterion 4's test doesn't discriminate removal of `layout.tsx`'s `type.css` import (font-variable binding goes untested; full suite stays green under the mutation) — next: implementer adds a `layoutSource()` regex assertion for the `type.css` import in `tests/app/layout.test.ts`, mirroring the existing `theme.css`/`fonts.ts` checks, then returns for re-validation.

## Validation report (appended by validator)

## TST-015 — WO-002 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-002`, branch `wo/WO-002-route-skeleton`, implementer HEAD `a5f1562`, based on `doctrine-0.11.0` tip `78d74cc`. `git status`: clean, no merge needed (already current with `doctrine-0.11.0`).
- `ui: yes`. **Placement: clean** — WO-002 carries no `## Open questions` heading at all, so zero `REVIEW(placement)` lines stand under it.
- Corpus-global id: `TST-015` — next unused across `sdlc-factory/docs/` (`TST-001`…`TST-014` all taken; `TST-014` was the prior high-water mark).
- Scope, independently confirmed: `git diff --stat doctrine-0.11.0..HEAD` touches exactly `src/app/layout.tsx` (NEW), `src/app/(public)/layout.tsx` (NEW), `src/app/(account)/layout.tsx` (NEW), `src/app/(hosted)/.gitkeep` (NEW), `tests/app/layout.test.ts` (NEW), `tests/app/route-groups.test.ts` (NEW), and this WO's own `## Log` (2 lines). That is 6 files against a 5-file plan — the sixth is `.gitkeep`, addressed under Deviation 3 below. No file outside this list changed.

### 1. Full suite and build — independently re-run

`npm test` (`vitest run --project node --project ui --project db`): **395/395 passed**, matching the implementer's count (the interleaved Postgres `NOTICE`/`ERROR` lines are expected output of constraint-violation tests, not failures). `npm run test:layout`: **16/16 passed**. `npm run typecheck`: clean, no output. `npm run build`: Next.js 16.3.4/Turbopack compiles, typechecks and prerenders clean. `npx eslint` on the 5 touched source/test files (excluding the `.gitkeep`): clean.

### 2. Fail-first, independently reproduced

Reproduced both cases named in the implementer's log, with my own edits:
- Added a stray `"Loading"` string into `<body>{children}</body>` in `layout.tsx` → `tests/app/layout.test.ts`'s "contains no text node this work order authored" test fails immediately (`expected 'Loading' to be ''`). Reverted; 6/6 green.
- Added a `layout.tsx` file under `src/app/(hosted)/` → `tests/app/route-groups.test.ts`'s "(hosted) carries no layout.tsx written by this work order" and "no file besides the placeholder" tests both fail. Reverted; 6/6 green.
- Removed the `src/app/(account)` directory → the "(public) and (account) each carry this work order's pass-through layout" and the `it.each` existence test both fail.
- Removed the `src/app/layout.tsx` import of `src/ui/theme.css` → the file-plan "imports src/ui/theme.css" regex assertion fails.
- Set `<html className="wrong-class">` instead of `fontVariables` → the "`<html>` carries `fontVariables` as its class" test fails.
- Deleted the `--font-mono` declaration from `type.css`'s `.rk-fonts` rule → the "type.css binds --font-ui and --font-mono" test fails.

All six mutations reverted afterward; `git status` clean, full suite restored to 395/395.

### 3. Mutation gap found — criterion 4's test does not discriminate removal of the `type.css` import

One mutation did **not** fail: **removing `import "@/ui/type.css";` from `src/app/layout.tsx`** leaves the entire suite green — `npm test` 395/395, `npm run typecheck` clean, `npm run build` clean, and `tests/app/layout.test.ts` itself 6/6. Reproduced twice, independently.

This matters because it is the exact deviation the implementer's own report flags as load-bearing (Deviation 2, disposed below): `src/ui/fonts.ts` only loads font bytes and exports the class name `"rk-fonts"`; the `--font-ui`/`--font-mono` custom-property *bindings* live solely in `src/ui/type.css`'s `.rk-fonts` rule, and `grep -rn "type.css"` across `src/` and `tests/` shows `src/app/layout.tsx` is the only file in the tree that imports it. If that one import line is deleted, the class `"rk-fonts"` still lands on `<html>` (nothing binds it to any CSS in the compiled output) and, in a real browser, neither font variable would actually be bound — yet no test in the suite would catch the regression.

The test suite proves two things separately — (a) `<html>` carries the class named `fontVariables`, and (b) `type.css`, read directly off disk, has a `.rk-fonts` rule binding both variables — but never proves the third fact that makes (a) and (b) compose into the criterion: that `layout.tsx` actually imports `type.css`. The file-plan-imports `describe` block in `tests/app/layout.test.ts` checks this exact pattern for `theme.css` and for `fonts.ts` (both regex-asserted against `layoutSource()`, both independently reproduced failing when I removed them, §2 above) but has no equivalent row for `type.css`.

This is a vacuous-test finding under constitution §8 / the validator's own charge ("a test that survives deletion of its feature is vacuous and is itself a finding") — not a defect in the shipped code (`type.css` **is** imported today, so the criterion holds as built) but a real, demonstrated gap in the regression protection for acceptance-criterion row 4 of this WO's own test plan.

**Fix:** add one more assertion to the `layoutSource()` `describe` block in `tests/app/layout.test.ts`, of the same shape as the existing `theme.css`/`fonts.ts` checks — `expect(layoutSource()).toMatch(/import\s+["']@\/ui\/type\.css["']/)` — so a future removal of the import fails loudly instead of silently.

### 4. The three flagged deviations — independent scrutiny

**Deviation 1 — "BP-001 decision 2" citation.** Confirmed: BP-001's decisions are currently numbered 1 (HTTP adapters), 2 (root config files — added after this WO was cut), 3 (route groups). The quoted text WO-002 cites — "three route groups — `(public)`, `(account)`, `(hosted)` — with the authorisation rule attached to the group rather than to the route" — matches BP-001's current decision 3 verbatim, character for character. The implementer proceeded correctly on the quoted text, not the stale ordinal, and left a comment in both `(public)/layout.tsx` and `route-groups.test.ts` documenting the drift. This is not a code defect; it is a stale ordinal in three places in **this WO's own text** (the `## Goal` line, the `(public)/layout.tsx` file-plan row, and the `## Test plan` table's first row) that a `/sync` pass should correct — the citation practice constitution rule 5.3 asks for ("the quote is the claim") was followed; only the parenthetical number drifted.

**Deviation 2 — importing `src/ui/type.css` in the root layout.** The implementer's reasoning is independently confirmed correct and necessary: verified by reading `src/ui/fonts.ts` (exports only the string `"rk-fonts"` plus font-byte side-effect imports — no CSS-variable declaration) and `src/ui/type.css` (the `.rk-fonts` rule is the sole binder of `--font-ui`/`--font-mono`, per WO-030's own file plan: "`fonts.ts`... Exports the two CSS variable names the root layout binds" — the binding itself is `type.css`'s). `grep -rn "type.css" src/ tests/` confirms `src/app/layout.tsx` is the only importer of `type.css` in the entire tree. Without this import, criterion row 4 ("both font variables bound on `<html>`") would be false at runtime despite the class name matching, exactly as the implementer argued. The deviation from the file-plan's literal "theme.css and fonts.ts" import list was necessary and is correctly reasoned — see §3 above for the resulting *test*-coverage gap this deviation left behind, which is a separate finding from whether the deviation itself was right (it was).

**Deviation 3 — `src/app/(hosted)/.gitkeep`.** Confirmed both in-scope and convention-matching. `find . -name .gitkeep` (excluding `node_modules`) shows six pre-existing placeholders under `src/` and `sdlc-factory/docs/` (`src/app/.gitkeep`, `src/lib/.gitkeep`, `src/ui/.gitkeep`, `src/jobs/.gitkeep`, `supabase/.gitkeep`, plus the docs-skeleton template's own set) — the mechanism is already the repo's standing convention for tracking an intentionally-empty directory in git, not a new pattern invented for this WO. It is in scope: WO-002 step 1 requires "the three route-group directories" to exist, BP-001's module boundary explicitly excludes `(hosted)/layout.tsx` from this WO (BP-047's), and git has no way to track an empty directory without a placeholder file — so a directory-only deliverable for `(hosted)` cannot be committed any other way. `route-groups.test.ts`'s own test ("(hosted) carries no file besides the placeholder") independently discriminates a stray file appearing there (§2 above). Correctly counted as a 6th file in this report's scope line, not hidden in the 5-file plan.

### Criterion-by-criterion (WO-002 `## Test plan`, 4 rows)

| # | Criterion (source quoted) | Test | Result |
|---|---|---|---|
| 1 | BP-001 decision (current ordinal 3, cited as "2" — Deviation 1): "three route groups — `(public)`, `(account)`, `(hosted)` — with the authorisation rule attached to the group rather than to the route" | `tests/app/route-groups.test.ts` | PASS — independently mutated (directory removal, §2) |
| 2 | BP-001 module boundary: "It does **not** own `src/app/(hosted)/**`, which is BP-004's" | `tests/app/route-groups.test.ts` | PASS — independently mutated (stray `(hosted)/layout.tsx`, §2) |
| 3 | BP-018 decision 2: "no component has a default string" | `tests/app/layout.test.ts` | PASS — independently mutated (stray `<body>` text, §2) |
| 4 | BP-018 error behaviour: "Every numeral... renders in JetBrains Mono with `tabular-nums`" — both font variables bound on `<html>` | `tests/app/layout.test.ts` | **FINDING** — the class-name and CSS-rule halves each discriminate independently (§2), but no test discriminates removal of the `type.css` import that wires them together (§3); the criterion holds in the code as shipped but is not fully protected by the suite |

### Coverage gaps

One, precisely stated per rule 5.5: `tests/app/layout.test.ts` never asserts that `src/app/layout.tsx` imports `src/ui/type.css`, even though that import is the only thing in the tree that makes the `fontVariables` class meaningful. Demonstrated by mutation (§3): deleting the import leaves 395/395, typecheck, build and the file's own 6/6 all green. Fix given in §3.

### Structure compliance

`src/app/layout.tsx`, `src/app/(public)/**`, `src/app/(account)/**` sit under BP-001's own `code:` globs and `structure.md`'s governed-paths table (line 22); `tests/app/**` is BP-001's own `code:` glob too (front matter line 28). No other work order's file plan defines any of these six files as its own creation (WO-001, WO-070, WO-148, WO-273 each only reference `src/app/layout.tsx` / `(account)/layout.tsx` as consumers or later `MODIFY` targets, never as a first `NEW`). No duplicate capability, no structure violation.

### Vacuity

Confirmed no test reads its own source as its entire assertion: `layout.test.ts`'s markup tests render real `react-dom/server` output (`renderToStaticMarkup`) and real `postcss`-parsed CSS; `route-groups.test.ts` checks the real filesystem (`existsSync`/`readdirSync`). One vacuous-test finding stands (§3, the missing `type.css`-import assertion) — the rest of both files discriminate under mutation as shown in §2.

### `rests-on` disposition (constitution rule 2.3b)

1. **"`sdlc-factory/docs/design/tokens.md` and `components.md` are `draft`/empty. Rule 7.3's preview cannot be signed until the design-guardian has registered BP-018's tokens and components; this work order's `ui: yes` is what holds it until then."** — **refuted**. Read both files directly on this branch: `tokens.md` front matter states `Status: ready` (616 lines, populated); `components.md` is populated (431 lines) and carries its own status vocabulary (`proposed → approved`), not `draft`/empty. WO-002's own front matter already carries `Signed-off: 2026-09-03` and a preview URL, and `## Log` records `2026-09-03 signed-off — owner`. The condition this row names as blocking sign-off no longer holds — sign-off already happened, and its prerequisite state (draft/empty registries) is no longer the state on disk.

Neither this WO's own row nor any row it can newly discharge upstream is left `open`. Walking BP-001 (the sole `implements` target): its one `rests-on` row — "Vercel serves the App Router deployment and the Inngest endpoint from one build..." — concerns the Vercel/Inngest deployment topology, which this WO's file plan (three layout files, two tests) does not touch or observe in any way; it is left `open` for the architect's wave-close read or a later work order that actually builds against that shared-process assumption (e.g. the Inngest serve endpoint or a build/deploy config WO), not this one.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

findings

Three of four test-plan rows pass clean under independent mutation (route-group existence, the `(hosted)` boundary, the no-default-string rule — §2). The fourth row's *criterion* holds in the code as shipped — `layout.tsx` does import `type.css`, and the font variables are genuinely bound on `<html>` today — but its *test* does not discriminate the one mutation that would break it (deleting the `type.css` import leaves 395/395 green, §3), which is a vacuous-test finding under constitution §8, not a break of the underlying behaviour. All three deviations the implementer flagged were independently scrutinised: Deviation 1 (stale "decision 2" ordinal) is confirmed harmless — the quoted text matches BP-001's current decision 3 verbatim; Deviation 2 (importing `type.css`) is confirmed correct and necessary, and is in fact the very deviation whose test coverage this report finds thin; Deviation 3 (`(hosted)/.gitkeep`) is confirmed in-scope and convention-matching against six pre-existing placeholders elsewhere in the repo. Full suite (395/395), layout project (16/16), typecheck, build and lint all independently reproduced clean. Scope is exactly the 5-file plan plus the necessary `.gitkeep` (6th file, addressed above) and this WO's own `## Log`. **Next:** the implementer adds one regex assertion to `tests/app/layout.test.ts`'s file-plan `describe` block — `expect(layoutSource()).toMatch(/import\s+["']@\/ui\/type\.css["']/)` — mirroring the existing `theme.css`/`fonts.ts` rows, and this WO returns for re-validation. No other change is required; do not touch the passing criteria or the three deviations, all of which stand as built.

This is the *record's* verdict, not a claim the underlying code is wrong: `type.css` is correctly imported and the criterion is true today. The defect is that the test protecting it can be silently broken later without failing, which is exactly the class of finding rule 8 exists to catch before it ships as a false sense of coverage.
