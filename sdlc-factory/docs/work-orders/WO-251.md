---
id: WO-251
type: work-order
title: "OnPageFacts and the deterministic on-page parse — headings, answer blocks, evidence density, schema, OG, access gates"
status: approved
ui: no
risk: high
wave: W4
implements: [BP-010]
depends-on: [WO-001, WO-006, WO-277]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code."
    disposition: refuted
    reason: "Spent, and its consequence with it — observed by the planner 2026-09-04. `src/` exists: `src/lib/measure/` holds `measured.ts`, `partition.ts`, `score.ts`, `bands.ts` and `verdict.ts` (WO-277, `done`) and `tests/measure/verdict/` holds their six suites, so the placement this row hedged is now read off disk rather than assumed. `src/lib/measure/parse.ts` and `tests/measure/domain/` are still new and still fall to BP-010 under `structure.md` rule 2 — BP-024's `code:` names five files by name and none is `parse.ts`. The three files this plan now also modifies (`src/lib/config/constants.ts`, `tests/config/constants.test.ts`, `src/lib/measure/score.ts`) all exist and were read on this pass."
  - claim: "`OnPageFacts` has no declared shape anywhere in the corpus. BP-010 names it in `measureDomain`'s return type and BP-012's `StoredReport` carries it as `onPage: OnPageFacts // BP-010`; six work orders (WO-184, WO-185, WO-186, WO-189, WO-191, WO-192) copy that line verbatim and none declares the type either. The shape in `## Interfaces` below is derived by this work order from BP-010's own module sentence — \"the parsers (headings, answer blocks, evidence density, schema, OG)\" — and from `BUILD.md` §5's named sub-measures, and is flagged for the architect to fold into BP-010's `## Public interface` (constitution rule 1.1: chosen, recorded, reversible). Reversal cost: one interface, no stored data — the blob is `jsonb` and no migration names these fields."
    disposition: open
  - claim: "BP-005's `constants.ts` carries the direct-answer character window. BP-010 decision 2 assigns it there by name — \"the coefficients that are numbers (band thresholds, the answerability floor, the direct-answer character window) are pins in BP-005\" — and `BUILD.md` §5 fixes its value: \"`directAnswers` = question headings whose first block is 40–320 visible chars ÷ all headings × 100\". BP-005's `## Public interface` as it stands on disk declares no such pin, so WO-006 will not have written one. See the stop condition under `## Interfaces`."
    disposition: confirmed
    reason: "Confirmed as raised, then cleared on the interface half — planner, 2026-09-04. The claim was true at cut time and stayed true through the 2026-09-04 refusal; commit `2fe462e` (`docs(BP-005): declare the scoring pins BP-010 delegates`) added `SCORING { directAnswerCharsMin: 40, directAnswerCharsMax: 320, answerabilityFloor: 1 }` to BP-005's `## Public interface` after `SCORE_BAND_BOUNDS`. Re-verified against source rather than inherited: `BUILD.md` §5 reads \"`directAnswers` = question headings whose first block is 40–320 visible chars\" and \"Answerability = shape of the home + measured pages, 0–100, floored at 1\" — all three values transcribed, none chosen. `src/lib/config/constants.ts` still carries no `SCORING`; landing it there is this order's first file-plan row, not another order's."
supersedes: []
---

# WO-251 — `OnPageFacts` and the deterministic on-page parse

- Branch: wo/WO-251-onpage-parse
- Estimate: ≤1 day

## Goal
Turn one fetched HTML document into the counted facts every driver is computed
from — headings, question-shaped headings, direct-answer blocks, evidence
tokens, structured data, Open Graph tags and the document's own access gates —
deterministically, with no fetch, no clock and no JavaScript execution.

**Risk seam (`risk: high`, doctrine 0.13.2 rule 2b).** A customer-visible
verdict: this order lands `SCORING` and points `factorsOf`'s answerability floor
at it, so the pinned number it moves is one the report's score and band word are
computed from — and the neighbouring `PRESENCE_FLOOR`, equal to it only by
coincidence of value, would move a second driver on the same edit if a later
pass folded them. Nothing else here is a seam: the parse spends nothing, fetches
nothing, publishes nothing and gates no access.

## Provenance of this file plan
**This repository holds no `src/`.** Both paths are NEW. `src/lib/measure/` is
BP-010's row in `structure.md` ("The measurement engine — deterministic parse of
fetched HTML into four measured quantities …") and BP-010's `code:` globs are
`src/lib/measure/**` and `tests/measure/**`. Neither path is claimed by BP-024,
the only leaf under this component, whose `code:` names five files by name
(`measured.ts`, `partition.ts`, `score.ts`, `bands.ts`, `verdict.ts`) and
`tests/measure/verdict/**` — so `parse.ts` and `tests/measure/domain/**` fall to
the component under `structure.md` rule 2.

**Reuse check (rule 7.2).** `capabilities.md` has no row for HTML parsing and no
work order in the corpus creates a parser: the file-plan rows under
`src/lib/measure/` are WO-052 (`measured.ts`), WO-053 (`partition.ts`), WO-054
(`score.ts`, `bands.ts`) and WO-055 (`verdict.ts`), all of which take numbers
already produced and none of which reads HTML. This work order creates the
capability; it does not duplicate one.

**Status: `approved`, self-certified 2026-09-04 (rule 3.2) — the second read.**
The 2026-09-04 read refused this order on exactly one clause: BP-010 decision 2
assigns the direct-answer character window to BP-005, and BP-005's
`## Public interface` declared no pin carrying it. **That clause is cleared.**
Commit `2fe462e` added to BP-005's `## Public interface`, after
`SCORE_BAND_BOUNDS`:

```ts
export const SCORING: {                                    // BP-010 d2 · BUILD §5
  directAnswerCharsMin: 40      // inclusive
  directAnswerCharsMax: 320     // inclusive
  answerabilityFloor: 1
}
```

It matches this order's `## Interfaces` **member for member and name for
name** — the group name `SCORING`, and the three members
`directAnswerCharsMin`, `directAnswerCharsMax`, `answerabilityFloor`, which is
the same spelling WO-252's own stop condition uses ("If
`SCORING.answerabilityFloor` … is absent"). The values were re-verified against
source on this read rather than inherited: `BUILD.md` §5 reads "`directAnswers`
= question headings whose first block is 40–320 visible chars" and
"Answerability = shape of the home + measured pages, 0–100, floored at 1".
All three are transcribed; none is chosen (rule 1.2).

**Grounds for `approved`** — each checked on this read, not carried forward:
- **Authorising ancestor.** Cut from BP-010, `status: approved` (gate 3: "No
  work order from a non-verified blueprint").
- **Criteria.** Every row of `## Test plan` quotes its source verbatim and
  names one test that discriminates; none is paraphrased, and the blockquote
  above the table states why no REQ criterion is inherited (rule 5.4).
- **Bounded.** Six files: one new parser, one new suite, and four small
  modifications (a pinned group, its pins-test block, one `const` becoming an
  import, one guard assertion). ≤1 day stands.
- **Rule 2.6 — one slice, and it does not merge.** The capability is the
  deterministic on-page parse, cut from its pin to its test. WO-252 states its
  goal (the ratios, floors and the saturating curve over these counts) without
  citing this order, and this order states its goal (the counts) without citing
  WO-252 — a one-way `depends-on`, not the mutual citation the merge test
  forbids.
- **No unresolved `blocked-by`.** `blocked-by: [BP-005]` is cleared here;
  `depends-on` gains WO-277, whose `score.ts` the last two rows modify.

**Why the second half of the fix is this order's, and not another's.** The
2026-09-04 refusal named two halves: BP-005's interface gains the pin (the
architect's — landed), "then one modify row on `constants.ts`" (this order's —
folded in below). A one-line pin group is far below the work-order floor (rule
2.6: "'add the field'… is not a work order; it is a step inside one"); giving it
its own order would buy a dispatch, a context pack, a merge and a share of a
wave's validation for four lines. It belongs to the order whose stop condition
fires without it, and this is that order. **Rule 7.1 is not breached:** BP-005
owns the *declaration* — the pin, its value and its comment are BP-005's
`## Public interface` — and this order only transcribes that declaration into
BP-005's file. That is the same move WO-080 makes on `COHERENCE` and WO-077 on
its own group, neither of which adds BP-005 to `implements:`; this order follows
that precedent and keeps `implements: [BP-010]`.

**The second copy this order retires, and the one it must not.** Once
`SCORING.answerabilityFloor` exists, `src/lib/measure/score.ts` (WO-277,
`done`) holds a second copy of a pinned number — `const ANSWERABILITY_FLOOR = 1`
— which is rule 2.4's failure arriving by omission and `structure.md` rule 5's
"a number that appears in two files is wrong". This order mints the pin, so this
order closes the duplicate it mints, in the same commit: one import, one call
site, no behaviour change (1 before, 1 after).

> **`PRESENCE_FLOOR` is not part of that change and must not be folded into
> it.** `score.ts` declares `const PRESENCE_FLOOR = 1` on the line above, and
> the two are equal **by coincidence of value, not by derivation** — `BUILD.md`
> §5 states Presence's floor (`Presence = max(1, √(SearchPresence ×
> AIPresence))`) and Answerability's floor independently, and BP-005's own pin
> comment says so in terms: "`PRESENCE_FLOOR` is deliberately not a fourth
> member, and unifying it with `answerabilityFloor` would be a defect wearing a
> cleanup's clothes." Folding them would make a later edit to Answerability's
> floor silently move Presence's — two drivers moving on one edit. The file plan
> row says leave it, the steps say leave it, and a test-plan row below fails if
> a later pass "tidies" it.


## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/config/constants.ts` | **MODIFY** | Add the `SCORING` group, transcribed verbatim from BP-005's `## Public interface` — group name, three member names, three values and the two `// inclusive` comments — in the same form as its neighbours in that file (`Object.freeze({ … } as const)`, with the source trace comment `// BP-010 d2 · BUILD §5`). Placed after `SCORE_BAND_BOUNDS`, which is where BP-005 declares it. Nothing else in this file changes; no value is chosen here and none is computed (BP-005 `## Module / boundary`: this file imports nothing). |
| `tests/config/constants.test.ts` | **MODIFY** | One added `describe` for `SCORING`, in the form the file's existing `SCORE_BAND_BOUNDS` block uses: the three member names exactly (`Object.keys(...).sort()`), the three values against `BUILD.md` §5 quoted in the comment above them, the group frozen, and — the guard below — that `SCORING` has no presence-floor member and never gains one. |
| `src/lib/measure/parse.ts` | **NEW** | The whole public interface below. `visibleText(html)` strips script, style, template and comment nodes and returns the document's rendered text — no JavaScript is executed and no headless browser is used (BP-010 `## Error & edge behavior`: "No JS execution, no clock in the parse, no locale-dependent collation"). `parseOnPage(html)` returns `OnPageFacts` in one pass over the parsed tree: every `h1`–`h6`; the question-shaped subset, by `BUILD.md` §5's own test — "ends `?` or opens with how/what/why/when/where/which/who/can/do/does/is/are" — matched case-insensitively against the ASCII-lowercased heading text and against no other vocabulary; for each question-shaped heading, whether the first block element after it is inside the direct-answer window read from BP-005; the evidence tokens (numerals, dates, outbound citations) and the visible character count they are counted per 1,000 of; the JSON-LD and microdata types present; the Open Graph property set; and the document's own access gates (a `noindex` in a `<meta name="robots">` or equivalent, and whether that directive names every reader or only some). **Counts only — no score, no ratio, no floor and no coefficient appears in this file**: those are WO-252's. Every field is a plain number, boolean or readonly array; no field is `Measured<T>`, because a parse of a document we successfully read cannot be `unmeasured` and the empty denominators read 0, never null (BP-010 `## Error & edge behavior`). Pure: same input, byte-identical output; no `Date`, no `Intl`, no `toLocaleLowerCase`, no `Math.random`. |
| `tests/measure/domain/parse.test.ts` | **NEW** | The suites named in the test plan, over checked-in HTML fixtures held inline in the file. |
| `src/lib/measure/score.ts` | **MODIFY** | **One line and one call site.** Delete `const ANSWERABILITY_FLOOR = 1;`, import `SCORING` from `../config/constants`, and read `SCORING.answerabilityFloor` at its single call site inside `factorsOf` (`answerability: floorAt(d.answerability, …)`). No behaviour change: the floor is 1 before and 1 after. **`const PRESENCE_FLOOR = 1` on the line above is not touched** — not deleted, not imported, not pointed at `SCORING`, not merged with the other floor — and `presenceOf` keeps reading it; add the one-line comment naming why (equal by coincidence of value; `BUILD.md` §5 states the two floors independently; BP-005's pin comment refuses a fourth member). `floorAt`, `presenceOf`, `ScoreFactors` and the file's exports are otherwise unchanged. |
| `tests/measure/verdict/score.test.ts` | **MODIFY** | Two added assertions, no existing case edited: the answerability floor `factorsOf` applies is the pinned one (assert against `SCORING.answerabilityFloor`, not the literal `1`), and the two-floors guard of the last test-plan row. Every existing case in this suite must still pass unchanged — that is what "no behaviour change" means here. |

## Interfaces

**Consumes:**
```ts
// BP-005 · src/lib/config/constants.ts — the pin BP-010 decision 2 assigns there
export const SCORING: { directAnswerCharsMin: 40; directAnswerCharsMax: 320; answerabilityFloor: 1 }
```

> **Stop condition — the pin is transcribed, never invented.** BP-010 decision
> 2 puts the direct-answer character window in BP-005, and BP-005's
> `## Public interface` now declares it (`2fe462e`, verified 2026-09-04). The
> first file-plan row copies that declaration into `src/lib/config/constants.ts`
> **verbatim from BP-005**, and the implementer reads BP-005 for it — not
> `BUILD.md`, not this file, and not their own arithmetic. If BP-005's
> `## Public interface` no longer declares `SCORING` with those three members
> when this work order starts, or declares values that disagree with
> `BUILD.md` §5's "question headings whose first block is 40–320 visible chars"
> and "floored at 1", the implementer **stops and logs `failed — next: BP-005's
> SCORING declaration and BUILD.md §5 disagree; the value is BP-005's to
> restate, not this order's`**. They do **not** write `40`, `320` or `1` as a
> literal anywhere under `src/lib/measure/` (`structure.md` rule 5: "A number
> that appears in two files is wrong"), and they do not choose a value BP-005
> does not state — that declaration is BP-005's, and one capability has one
> owner (rule 7.1).

**Exposes** — `OnPageFacts` is *derived here*, not copied: no blueprint declares
it (second `rests-on` row). Every field below traces to a sentence in BP-010's
`## Module / boundary` or in `BUILD.md` §5, quoted in the comment beside it.

```ts
/** The counted facts of one fetched document. Named by BP-010's `measureDomain`
 *  return type and by BP-012's `StoredReport` (`onPage: OnPageFacts // BP-010`);
 *  declared here for the first time. Counts only: nothing in this type is a
 *  score, a ratio, a floor or a band. Empty denominators read 0, never null. */
export interface OnPageFacts {
  url: string
  /** `BUILD.md` §5: "question-shaped headings ÷ all headings". */
  headings: number
  /** §5: "question-shaped = ends `?` or opens with how/what/why/when/where/
   *  which/who/can/do/does/is/are". */
  questionShapedHeadings: number
  /** §5: "question headings whose first block is 40–320 visible chars". The
   *  window is BP-005's pin; this field is the count that passed it. */
  directAnswerHeadings: number
  /** §5's evidence density inputs, counted raw. The saturating log curve over
   *  them is WO-252's, not this file's. */
  numerals: number
  dates: number
  outboundCitations: number
  visibleChars: number
  /** BP-010 `## Module / boundary`: "the parsers (headings, answer blocks,
   *  evidence density, schema, OG)". Types and property names as written in the
   *  document, lowercased; no interpretation. */
  schemaTypes: readonly string[]
  openGraphProperties: readonly string[]
  /** The document's own access gates. REQ-004 c7's case — "a home document the
   *  scan read that tells every reader not to index it" — is
   *  `noindex && noindexAppliesToEveryReader`. What that *means* for the score
   *  is WO-252's; what it means for the blocked-readers count is BP-024's. */
  noindex: boolean
  noindexAppliesToEveryReader: boolean
}

/** The document's rendered text. Exported because BP-010 decision 4's
 *  `readMeasuredText` re-reads `fetches.payload` and must produce the same text
 *  the measurement was taken from — a second extractor would make the
 *  comparison set disagree with the measurement (rule 7.2). WO-253 imports
 *  this one. */
export function visibleText(html: string): string

/** Deterministic and total: any string parses, including an empty one, which
 *  yields every count at 0. Never throws. */
export function parseOnPage(a: { url: string; html: string }): OnPageFacts
```

## Steps
1. Land the pin first. Read BP-005's `## Public interface` for the `SCORING`
   declaration and copy it into `src/lib/config/constants.ts` verbatim; add its
   `describe` to `tests/config/constants.test.ts` and run that suite. If BP-005
   and `BUILD.md` §5 disagree, take the stop condition above — do not improvise
   a value.
2. Retire the second copy the pin just created, before writing any new code:
   in `src/lib/measure/score.ts`, replace `const ANSWERABILITY_FLOOR = 1` with
   `SCORING.answerabilityFloor` at its one call site in `factorsOf`. **Leave
   `const PRESENCE_FLOOR = 1` exactly as it is** and leave `presenceOf` reading
   it; the two floors are equal by coincidence of value and `BUILD.md` §5 states
   them independently, so unifying them is a defect, not a cleanup. Run
   `tests/measure/verdict/score.test.ts` and confirm every existing case still
   passes — the whole change is a 1 that used to be local and is now imported.
3. Write `tests/measure/domain/parse.test.ts` and run it; every case must
   fail on a missing module before any of `parse.ts` exists (constitution §8,
   "Watch it fail first").
4. Implement `visibleText` first: strip `<script>`, `<style>`, `<template>` and
   comment nodes, then take the text content. No JavaScript is executed and no
   network is touched.
5. Implement `parseOnPage` as one pass producing every field. The question-shape
   test is the twelve openers and the trailing `?` of `BUILD.md` §5 and no other
   token; add none and drop none.
6. Count the direct-answer headings by measuring the *visible* characters of the
   first block element after each question-shaped heading against the pin's two
   bounds inclusive, so the boundary values 40 and 320 both pass.
7. Count evidence tokens and `visibleChars` from the output of `visibleText`, so
   markup can never inflate a numeral or a date.
8. Read the access gates from the document only. `robots.txt` is BP-006's
   `readRobots` and is not fetched, parsed or consulted here.
9. Mutate to prove the suite discriminates (`risk: high` — this order's
   criteria are mutation-tested at validation): replace the twelve-opener test
   with "ends `?`" alone and confirm the opener fixture fails; replace
   `visibleText` with a raw `innerHTML` and confirm the markup-inflation case
   fails; change `SCORING.answerabilityFloor` to `2` and confirm the score suite
   fails **and that no Presence assertion moves with it** — a Presence failure
   there means the two floors have been folded and the change must be reverted.

## Test plan

> **Criterion source: the blueprint and `BUILD.md`, not a requirement.** BP-010
> carries `satisfies: []` and its status grounds say it was "cut from `BUILD.md`
> §5 and §16 milestone 2, not from one requirement". Its `covers:` list is not
> an authorising ancestor (rule 5.4), so **no REQ criterion is inherited here**
> and none is paraphrased into one. Each row quotes its source verbatim.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-010 `## Error & edge behavior`: "**Determinism is a test, not an aspiration**: the same HTML measured twice produces byte-identical output (`BUILD.md` §16 milestone 2). No JS execution, no clock in the parse, no locale-dependent collation." | `tests/measure/domain/parse.test.ts › the same document parses to a byte-identical value twice` — every fixture is parsed twice and the two results compared by serialisation, once with the process locale set to `tr-TR` (whose dotless-i casing breaks a naive `toLowerCase` on the twelve openers) and once with `TZ` shifted. Plus a source-level assertion that `parse.ts` contains no `Date`, no `Intl`, no `toLocale`, no `Math.random` and no `fetch`. Discriminates: a `toLocaleLowerCase` on the heading text passes the default-locale run and fails the `tr-TR` one. |
| BP-010 `## Error & edge behavior`: "Empty denominators read 0, never null (`BUILD.md` §5): a page with no headings scores `zero`, not `unmeasured`." | `… › a document with no headings yields every count at 0, not null` — a fixture with no `h1`–`h6` returns `headings: 0`, `questionShapedHeadings: 0`, `directAnswerHeadings: 0`; a type-level assertion that no field of `OnPageFacts` admits `null`, `undefined` or a `Measured` arm. Discriminates: widening any field to `\| null` fails to compile. |
| `BUILD.md` §5: "question-shaped = ends `?` or opens with how/what/why/when/where/which/who/can/do/does/is/are" | `… › the question shape is exactly the trailing ? and the twelve openers` — one fixture heading per opener plus one trailing-`?` heading are all counted; a thirteenth opener not on the list ("should …") and a heading with an interior `?` are both not counted. Discriminates: adding a thirteenth opener fails the negative case; dropping one fails its positive case. |
| `BUILD.md` §5: "`directAnswers` = question headings whose first block is 40–320 visible chars ÷ all headings × 100" | `… › the direct-answer window is inclusive at both bounds and read from the pin` — blocks of 39, 40, 320 and 321 visible characters after a question-shaped heading yield 2 of 4 counted; the two bounds are asserted to come from `SCORING`, and a source assertion holds that neither `40` nor `320` appears as a literal in `parse.ts`. |
| `BUILD.md` §5: "`evidenceDensity` = saturating log curve over (numerals + dates + outbound citations per 1k chars)" — **the counts only; the curve is WO-252's** | `… › evidence tokens are counted over rendered text, never over markup` — a fixture whose `<script>` block holds twenty numerals and whose visible text holds three yields `numerals: 3`; `visibleChars` excludes the script. A source assertion holds that `parse.ts` contains no `log`, no `Math.log10` and no division — the curve is not here. |
| BP-010 `## Error & edge behavior`: "A home document read successfully that tells every reader not to index it is a measured 0 for Foundations and forces the score to 0 with its band word — it is never a \"—\" (REQ-004 criterion 7)." — **the fact only; the forcing is WO-252's** | `… › a generic noindex and a targeted one are different facts` — a document with `<meta name="robots" content="noindex">` yields `noindex: true, noindexAppliesToEveryReader: true`; one that disallows a single named agent yields `noindex: true, noindexAppliesToEveryReader: false`; one with neither yields both false. Discriminates: collapsing the two booleans into one makes the targeted fixture indistinguishable from the generic one, which is the difference REQ-004 c7 turns on. |
| BP-010 `## Module / boundary`: "the parsers (headings, answer blocks, evidence density, schema, OG)" | `… › schema and Open Graph are read as written, never interpreted` — a JSON-LD block and a microdata block both contribute their `@type`/`itemtype` names lowercased; an `og:` property set is returned in document order; an unknown type is returned rather than dropped, because dropping it would be a judgement this file is not allowed to make. |
| BP-005 `## Public interface`, `SCORING` (added 2026-09-04): "`directAnswerCharsMin: 40 // inclusive` · `directAnswerCharsMax: 320 // inclusive` · `answerabilityFloor: 1`" | `tests/config/constants.test.ts › BP-005 SCORING (added 2026-09-04) — transcribed, frozen, three members` — the group's key set is exactly those three sorted names, the three values are 40, 320 and 1, and the object is frozen (the file's existing "nothing mutates a pin at runtime" behaviour). Discriminates: changing any value, adding a member or dropping `Object.freeze` fails a named case. |
| BP-005 `## Public interface`, the `SCORING` comment: "**`PRESENCE_FLOOR` is deliberately not a fourth member, and unifying it with `answerabilityFloor` would be a defect wearing a cleanup's clothes.** … It equals the answerability floor today by coincidence of value, not by derivation — §5 states the two floors independently" | Two guards, one on each side of the seam. `tests/config/constants.test.ts › SCORING carries no presence floor` — the key set assertion above is exact, so a fourth member named for Presence fails it. `tests/measure/verdict/score.test.ts › the two floors are separate numbers that happen to be equal` — a source-level assertion that `score.ts` still declares its own `PRESENCE_FLOOR` and that `presenceOf`'s body references `PRESENCE_FLOOR` and not `SCORING`, plus a behavioural pair: with `SCORING.answerabilityFloor` stubbed to 2, `factorsOf`'s `answerability` moves and `presenceOf`'s output does not. Discriminates: the "cleanup" that points `presenceOf` at `SCORING.answerabilityFloor` — which changes no output today, because both are 1 — fails both the source assertion and the stubbed pair. This row exists because that edit reads as tidying and is not. |

Additional tests this work order owns:
- `… › `visibleText` is the one extractor` — a source assertion that no other
  file in this plan re-implements text extraction, and that `visibleText` is
  exported. WO-253's `readMeasuredText` imports it; a second extractor would let
  the generation comparison set disagree with the measurement it came from
  (BP-010 decision 4, rule 7.2).
- `… › the parse never throws` — malformed, truncated, empty and
  non-HTML inputs each return an `OnPageFacts` with zeroed counts rather than
  rejecting; BP-006 already turned every failure into a typed outcome, so a
  throw here would reintroduce the exception path that node removed.

## Out of scope
- Every ratio, floor, coefficient and driver — WO-252. This file produces counts.
- `measureDomain`, the fetches it makes and the vendor rows it buys — WO-253.
- `robots.txt` — BP-006's `readRobots` (WO-020); this file reads the document's
  own gates and nothing at an origin.
- The blocked-readers count over `AI_READER_AGENTS` — BP-024's `verdictOf`
  (WO-055).
- **Choosing** any pinned value. The `SCORING` group is transcribed from
  BP-005's `## Public interface`; see the stop condition.
- Every other constant in `constants.ts`, and every other line of
  `score.ts` — including `PRESENCE_FLOOR`, `presenceOf`, `floorAt`, `bandOf`
  and `verdictOf`, none of which this order may touch.
- `drivers.ts`'s own flooring of Answerability — WO-252's, which reads the
  same pin this order lands and is not blocked by it after this order runs.
- Any migration, any schema and any write: this file has no I/O at all.

## Rollback
Three independent reversals, innermost first. Delete `src/lib/measure/parse.ts`
and `tests/measure/domain/parse.test.ts` — nothing imports them until WO-252 and
WO-253 land, so roll those back first if they have. Restore
`const ANSWERABILITY_FLOOR = 1` in `src/lib/measure/score.ts` and drop the
import (`PRESENCE_FLOOR` was never moved, so nothing about Presence reverses).
Drop the `SCORING` group from `src/lib/config/constants.ts` and its `describe`
— but only if WO-252 has not landed, because it reads the same pin; after that
the pin is shared and removing it is BP-005's decision, not a rollback.

## Log
- 2026-08-31 created — planner
- 2026-09-04 finished — planner — read for self-certification at the owner's ruling and **refused, not flipped** (rule 3.1): the plan is complete and bounded, but BP-010 decision 2 assigns the direct-answer character window to BP-005 and BP-005's `## Public interface` declares no pin carrying it — verified on disk, `src/lib/config/constants.ts` (WO-006, `done`) exports no `SCORING` group. This order's own stop condition fires on contact, so an implementer dispatched today would log `failed` without writing a line. `blocked-by: [BP-005]` recorded as a state (rule 2.3, planner step 4); `status:` stays `draft`; **not in W4**, though the owner's 2026-09-04 ruling named it among the first two — reported in this pass's return. Grounds and what clears it are in `## Provenance of this file plan`.

- 2026-09-04 finished — planner — **re-read against BP-005 as it now stands (`2fe462e`) and certified**: the declared `SCORING` matches this order's `## Interfaces` member for member and name for name, and its three values were re-verified against `BUILD.md` §5 directly rather than inherited. `status: draft → approved` (rule 3.2, grounds in `## Provenance of this file plan`); `blocked-by: [BP-005]` cleared; `rests-on` rows 1 and 3 dispositioned on direct observation (`refuted` — the no-source-tree row is spent; `confirmed` — the pin row, raised true and now cleared), row 2 left `open` for the validator. File plan gains four modify rows: the `constants.ts` transcription and its pins-test block (the second half of the fix this order's own refusal named), and `score.ts`'s `ANSWERABILITY_FLOOR` becoming an import of `SCORING.answerabilityFloor` with a guard suite — the duplicate this order mints is the duplicate this order retires (rule 2.4), and `PRESENCE_FLOOR` is fenced in the file plan, the steps, the out-of-scope list and a test-plan row so no later pass folds the two. `depends-on` gains WO-277. `risk: high` (doctrine 0.13.2 rule 2b): the seam is a customer-visible verdict — this order edits the pinned floor `factorsOf` applies to the score the report shows, and a folded floor moves a number a customer reads with no failing test unless one is written. `wave: W4`, second in the row, per the owner's ruling of 2026-09-04 ("WO-023 and WO-251 first"); WO-024 leaves W4 to hold the cap at eight, reasoned in this pass's return.

- 2026-09-04 started — implementer — dispatched for W4 (second in the wave). Pack read (`factory-console pack WO-251`): 5 artifacts, 6 files. BP-005 read directly (not in the pack's artifact list, but named by this WO's own stop condition and step 1) to transcribe `SCORING` — matches this order's `## Interfaces` and the dispatch's given values member for member.
- 2026-09-04 finished — implementer — all six file-plan rows landed: `SCORING` transcribed into `src/lib/config/constants.ts` (steps 1); `score.ts`'s `ANSWERABILITY_FLOOR` retired to an import of `SCORING.answerabilityFloor`, `PRESENCE_FLOOR` left untouched with its fencing comment (step 2); `src/lib/measure/parse.ts` (new) implements `OnPageFacts`, `visibleText` and `parseOnPage` as a hand-rolled regex/scan parser (no DOM library — none is in the file plan and none is added); `tests/measure/domain/parse.test.ts` (new, 24 cases), `tests/config/constants.test.ts` and `tests/measure/verdict/score.test.ts` gain the pin, guard and parse suites. Watched fail first: the domain suite failed on a missing module with `parse.ts` moved aside before being restored (constitution §8); the two `PRESENCE_FLOOR`-guard assertions were watched to fail against a deliberate fold-the-floors mutation, then reverted. Mutation pass (risk: high, doctrine 0.13.2 rule 2b) run and reverted every time: both boundary sides of the 40/320 window independently discriminate; the pin-to-score flow is confirmed by a `vi.doMock` stub (`SCORING.answerabilityFloor` to 2 moves `factorsOf`'s answerability, leaves `presenceOf` at 1) and by a direct edit to the real pin; the `PRESENCE_FLOOR` fold mutation fails both its source-shape assertion and its behavioural pair; the twelve-opener battery, the raw-innerHTML/attribute-digit inflation case, the generic-vs-targeted `noindex` collapse, the microdata-drop, and the never-throws contract (inner guard and outer catch both removed) each fail their own test and pass restored. Gates: `npm run typecheck` clean; `npm run lint` unchanged at 8115, none in these six files; `npm test` 56 files / 1396 passed / 1 skipped (baseline 55/1360/1), no `safe-fetch` flake seen. `rests-on` row 2 (`OnPageFacts` has no declared blueprint shape) left `open`, as the planner's own note directs — the validator's to disposition; rows 1 and 3 were already dispositioned by the planner. One deviation from the file plan's literal wording, recorded per rule 4.2: `score.ts` imports `SCORING` via the `@/lib/config/constants` alias (matching `bands.ts` and `verdict.ts`, its own directory's sibling files) rather than the relative `../config/constants` the file-plan prose names — an import-style convention choice, not a behaviour change, and it types and runs identically either way.

## Validation report (appended by validator)
