---
id: WO-074
type: work-order
title: "selectTwelve — the deterministic selection, its floor, its collapse and its composition constraints"
status: approved
ui: no
implements: [BP-025, REQ-006]
depends-on: [WO-006, WO-072, WO-073]
blocked-by: []
rests-on:
  - claim: "`SELECTION: { volumeFloorPerMonth: 50; intentWeights: { decision: 3; solution: 3; problem: 2; informational: 1 }; minDecision: 4; minSolution: 3; maxRivalBrand: 3; maxHowTo: 2 }` and `BATTERY.QUESTIONS` (12) are pinned in `src/lib/config/constants.ts` (BP-005), which `capabilities.md` records as already declaring `SELECTION`. That file is BP-005's and is not touched here."
    disposition: open
supersedes: []
---

# WO-074 — selectTwelve — the deterministic selection, its floor, its collapse and its composition constraints

- Branch: wo/WO-074-select-twelve
- Estimate: ≤1 day

## Goal
Complete `src/lib/market/questions/select.ts` with `selectTwelve` — a pure function of a
profile and a market that returns 0…12 searches in a byte-identical order on every run,
padding nothing to reach twelve.

## Context an implementer needs
No source code exists on disk beyond what WO-071, WO-072 and WO-073 land; the two paths
below are NEW files this WO creates or completes. **This WO modifies `select.ts`, the file
WO-073 creates — the two are sequenced (`depends-on`) and must never be run in parallel.**

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/questions/select.ts` | Modify (created by WO-073) | Adds `SelectedSearch` and `selectTwelve`. Uses `classifyIntent`, `intentWeight`, `passesRelevanceGuard` and `stemKey` from the same file — it does not re-derive any of them. |
| `tests/market/questions/select.test.ts` | **NEW** | The tests named in the test plan, including the fixture determinism test BP-025's `## NFR budget` requires ("a fixture test, not an aspiration"). |
| `tests/market/questions/fixtures/market-set.json` | **NEW** | One frozen profile and one frozen ~50-row market, the input of the determinism test. Hand-authored synthetic rows — it is a fixture, not a measurement, and no figure in it is presented anywhere as a vendor result (constitution rule 1.2). |

## Interfaces
**Consumes**: `Profile` (WO-071), `SuggestionRow` (WO-072), and this file's own
`classifyIntent` / `intentWeight` / `passesRelevanceGuard` / `stemKey` (WO-073), plus
`SELECTION` and `BATTERY.QUESTIONS` from `src/lib/config/constants.ts` (BP-005).

**Exposes** — copied exactly from BP-025's `## Public interface`:

```ts
export interface SelectedSearch {
  keyword: string; volume: number; intent: Intent; score: number; rank: number
}
export function selectTwelve(a: { profile: Profile; market: SuggestionRow[] }): SelectedSearch[]
```

and, from BP-011's `## Public interface`, the scoring law this WO implements:

```
/** Deterministic. No model call. score = intentWeight x log10(volume+1), volume
 *  floor 50/mo, own-brand dropped, relevance guard against the profile's
 *  vocabulary, near-duplicate collapse, composition constraints. Returns as many
 *  as the market yielded — never padded to twelve. */
```

## Steps
1. Drop every row whose `volume` is below `SELECTION.volumeFloorPerMonth`.
2. Drop every row whose `classifyIntent` is `'own_brand'`.
3. Drop every row failing `passesRelevanceGuard`.
4. Collapse near-duplicates on `stemKey`, keeping the highest `volume` of each group; on
   an exact volume tie keep the keyword that sorts first, so the collapse is total.
5. Score: `score = intentWeight(keyword, profile) * log10(volume + 1)`.
6. Rank by `score` descending, then `volume` descending, then `keyword` ascending — a total
   order, so no two runs over identical input differ. (Chosen here under rule 1.1 by the
   same reasoning BP-026 decision 3 records for rivals; BUILD §6.7 fixes the score and not
   the order among equals. Cost of reversing: none stored — it changes which of two
   equal-scoring searches is shown.)
7. Apply the composition constraints over the ranked list up to `BATTERY.QUESTIONS`:
   caps `SELECTION.maxRivalBrand` (searches naming one of `profile.namedRivals`) and
   `SELECTION.maxHowTo` (informational how-to shapes) are enforced by skipping a row that
   would exceed them; floors `SELECTION.minDecision` and `SELECTION.minSolution` are
   enforced by promoting the next-ranked row of that intent. **Relax in one fixed order —
   floors first** (BP-025 `## Error & edge behavior`): a floor that cannot be met means the
   market yielded fewer than twelve, which is legal.
8. Assign `rank` 1…n in final order and return. There is **no** code path in this function
   that duplicates a row, repeats a keyword, or appends a row absent from `market`.
9. Emit the observability fields BP-025 names: the selected count and which constraint bound it.
10. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-006) | Test |
|---|---|
| 1. "Given a completed report, when the AI-answers card renders, then it states in writing how many of the market's biggest searches the report measured — twelve where the market yielded twelve, and the smaller number it yielded where it yielded fewer — states on how many of those an AI answer appeared, and shows every citation count on the card against those two numbers and never against a count of searches the report did not measure." | `tests/market/questions/select.test.ts › selectTwelve/short-market-is-a-complete-result` — a market that survives the floor, the drop, the guard and the collapse with 5 rows returns exactly 5 `SelectedSearch`, ranked 1…5, with no sixth row. |
| 8. "Given a completed report, when the questions module renders, then every question the report measured is reachable from the page, with the first four visible and the remainder one action away, and no question is invented, repeated or left standing for an unmeasured search to bring the list up to twelve." — the engine limb ("no question is invented, repeated"); the "first four visible" limb is the report surface's (BP-022). | `tests/market/questions/select.test.ts › selectTwelve/never-pads-and-never-repeats` — a property test over generated markets of size 0…60: the result length is `min(eligible, 12)`, every returned `keyword` is a member of the input `market`, and the returned keywords are pairwise distinct. |
| 12. "Given the set of questions the report measured, when it renders, then no two of them derive from searches that differ only in plural form, word order, punctuation or stop-words — each question stands for a different search." | `tests/market/questions/select.test.ts › selectTwelve/no-two-share-a-stem-key` — over the fixture and over generated markets, `stemKey` of every returned keyword is pairwise distinct; and a market seeded with a plural/word-order/punctuation/stop-word variant pair returns the higher-volume member only. |

Determinism, required by BP-025's `## NFR budget`:
`tests/market/questions/select.test.ts › selectTwelve/byte-identical-on-every-run` — the
frozen fixture selected 100 times, and selected again over a shuffled copy of the same
market rows, yields an identical `SelectedSearch[]` including `rank` and `score`.

Purity, required by BP-025 decision 1:
`selectTwelve/takes-no-cost-context` — the signature accepts exactly one argument object
of `{ profile, market }`, and `select.ts` resolves no import into `src/lib/costs/`,
`src/lib/llm/` or `src/lib/vendors/`.

Discrimination: making the rank comparator partial (dropping the `keyword` tie-break),
padding the result to twelve, or moving the relaxation order to caps-first must each fail
a named test above.

## Out of scope
- Wording the questions — WO-075. Selection returns searches, never text.
- The AI-answers card and its counts — WO-076.
- Adding `SELECTION` or `BATTERY.QUESTIONS` to `constants.ts` — BP-005's file.
- REQ-006 criteria 2–7, 9–11 and 13.

## Rollback
Revert `select.ts` to WO-073's state and delete the two test artefacts. `phrase.ts` has
not landed, so nothing consumes `selectTwelve`.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-025 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-006 as it stands on disk.
- **Bounded.** 3 files in the file plan (2 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-006); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
