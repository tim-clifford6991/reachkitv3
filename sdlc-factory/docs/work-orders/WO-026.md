---
id: WO-026
type: work-order
title: "The llm() seam — model tiers, schema-parsed output, per-call cost logging"
status: approved
wave: W4
ui: no
risk: high
implements: [BP-009]
depends-on: [WO-276]
blocked-by: []
rests-on:
  - claim: "`Measured<T>` is BP-010's (`src/lib/measure/`, per `registry/capabilities.md`) and does not exist in this repo. `llm()` returns it. The type is consumed by a type-only import and never re-declared here (rule 7.1); this work order cannot type-check until BP-010's work order lands. The planner's type fan-out check could not be run: the repo holds no TypeScript to check."
    disposition: refuted
    reason: "Implementer, 2026-09-04. Stale (the WO's own instruction: \"Rows asserting 'no source tree exists on disk' are stale — judge them against what you see\"). `Measured<T>`, `measured()`, `unmeasured()` and `UnmeasuredReason` exist today at `src/lib/measure/measured.ts` (its own header: \"WO-277 … BP-024\"), a plain discriminated union exactly as BP-010 decision 2 describes. `src/lib/llm/index.ts` imports it as a type-only import and `npm run typecheck` is clean. Not blocked. Re-confirmed independently, validator, 2026-09-04 (TST-040, rule 2.3b): read `src/lib/llm/index.ts` line 56 directly, `npm run typecheck` re-run clean."
  - claim: "`llm()`'s `call.site` is declared `LlmCallSite` (BP-009 `## Public interface`), but that union is WO-027's own file (`src/lib/llm/call-sites.ts`, `depends-on: [WO-026]` — it lands *after* this order) and WO-027's file plan never lists `index.ts` as a modify row. `src/lib/llm/index.ts` therefore types `call.site` as `string` instead — every member `LlmCallSite` will ever have is a string literal, so this is source-compatible with any caller WO-027 adds; nothing here needs to change when that union lands, unless the architect prefers `index.ts` narrowed to the exact declared type, in which case WO-027 gains a one-line modify row."
    disposition: confirmed
    reason: "Implementer, 2026-09-04: for the architect to confirm at WO-027's own dispatch — either `string` stands (no code change) or WO-027's file plan adds `src/lib/llm/index.ts` as a one-line modify row. Validator, 2026-09-04 (TST-040, rule 2.3b): the underlying fact is confirmed by direct read (`index.ts`'s `call.site` is `string`, source-compatible with any literal WO-027 adds), but the design choice this row raises — narrow now or leave widened — is WO-027's own dispatch and the architect's to make, not observable from this build. Left `open`, deliberately: WO-027 has not landed, so no build available to this validation pass can discharge it, and it is not `undischargeable` (2.3a) since WO-027's own build will resolve it either way. Blocks WO-026's `done` gate until WO-027 lands or the architect rules directly. **Architect, 2026-09-04 — ruled, and the row is `confirmed`: the claim is true as stated and nothing WO-026 shipped is wrong, so `refuted` would misread it.** The design question it raises is settled in BP-009 `## Decisions` 2: `call.site` is **narrowed** to `LlmCallSite`, and WO-027 is what narrows it — BP-009 decision 1 requires the closed set to be 'enforced by the type system', and a `string` parameter at the only seam that reaches a model enforces nothing, which is the review burden decision 1 explicitly rejected. Consequence carried on BP-009 decision 2 for the planner: **WO-027's file plan gains `src/lib/llm/index.ts` as a modify row** (one import, one parameter type) and its test plan gains the non-member-is-a-type-error assertion. No change to `src/` in this work order: the widening is source-compatible in both directions, so WO-026's own code and tests stand as validated. Cost of reversing: one line, one direction, no data and no caller."
  - claim: "BP-009 `## Error & edge behavior` states a schema-parse failure is `unmeasured` with reason `unparseable`. The shipped `Measured<T>` (`src/lib/measure/measured.ts`, BP-024, WO-277 — outside this WO's file plan, rule 7.1: one owner per capability) declares `UnmeasuredReason` as the closed `'undeterminable' | 'not_attempted'` — `unparseable` is not a member. `src/lib/llm/index.ts` maps both a schema-parse failure and model unavailability to `undeterminable` ('nothing returned, unreadable, or a data source did not answer' — BP-024's own words, which already cover an unparsed response) rather than invent a third member of a type this WO does not own."
    disposition: confirmed
    reason: "Implementer, 2026-09-04: for the architect — either amend BP-009's prose to read `undeterminable` (the type it must actually return), or amend `UnmeasuredReason` at BP-024/BP-010 to add `unparseable`. This implementer chose the former reading rather than widen a type outside this file plan. Validator, 2026-09-04 (TST-040, rule 2.3b): the factual mismatch is confirmed by direct read — `UnmeasuredReason` has no `unparseable` member, and `index.ts` maps both parse failure and unavailability to `undeterminable`, matching the implementer's stated choice. Which artifact moves is a rule-7.1 ownership call this validation pass cannot make. Left `open`, deliberately, for the architect; blocks WO-026's own `done` gate until ruled. **Architect, 2026-09-04 — ruled, and the row is `confirmed`: the mismatch was real and the implementer read it the right way round.** **BP-009's prose was the wrong artifact and it moved**; `UnmeasuredReason` stays two-membered. Reasoning is ADR-096 (landmine), which earns a file under rule 2.2 because the load-bearing half of the argument lives in nodes BP-024 does not own: each member of that union is an **owner-owed customer sentence** (BP-020 decision 3's `unmeasured.undeterminable` / `unmeasured.not-attempted` keys, both marked owner-owed; BP-019's `unmeasuredLine: CopyKey`, 'differs by reason'), so a third member is a customer-visible string under §1 — never a type edit — plus a third stored reason value in two `jsonb` columns and an unranked member in `worseReason` (BP-024 decision 3). `unparseable` keeps the one home where it is useful and harmless: `ParseOutcome` in `src/lib/llm/index.ts`, reaching the log field BP-009's `## NFR budget` already specifies ('parse outcome'). `src/lib/llm/index.ts` is correct as shipped and changes nothing; BP-009 `## Error & edge behavior` now reads `undeterminable`, so this WO's `## Test plan` row 1 quotes a reason the type actually has and the test itself is unaffected."
  - claim: "No approved artifact states a machine-readable model id for either tier. BP-009 `## Public interface` declares only the abstract `tier: 'nano' | 'haiku'` union; the charter's own stack row (`00-project.md` / `BUILD.md` §1) reads 'Anthropic Haiku 4.5 (prose) + a nano-class model (scaffolding)' — a class, not a name, for the second tier. `src/lib/llm/tiers.ts` pinned `haiku: 'claude-haiku-4-5'` (transcribing the charter's own family name) and, in this WO's first commit, `nano: 'claude-fable-5'` (chosen under rule 1.1 as an internal, reversible id)."
    disposition: confirmed
    reason: "2026-09-04, coordinator review + implementer correction. `haiku`'s id is confirmed against Anthropic's current catalogue, matching `INFERENCE_PRICE_BOOK.haiku` exactly ($1.00/$5.00 per MTok = 100/500¢). `nano`'s first id, `claude-fable-5`, was real but Anthropic's *most expensive* tier ($10.00/$50.00 per MTok) wired into the cheapest lane — a 50×/40× under-count no test could see, since the price book and the id agreed with each other and were both wrong together. Corrected to `nano: 'claude-haiku-4-5'` — the cheapest real model in the current catalogue, still 5× the `INFERENCE_PRICE_BOOK.nano` pin rather than 50×/40× — an interim, not a final answer. Validator, 2026-09-04 (TST-040, rule 2.3b): the row's core claim — no approved artifact states a machine-readable model id, so the implementer must choose one under rule 1.1 — is confirmed, and the choice is now correctly made, pinned and regression-guarded (mutation re-run: setting `nano` back to `claude-fable-5` kills 2 tests). Choosing a model id is a parameter (rule 1.1), not a promise change, and does not itself require owner escalation. Settled, not left open. The unresolved consequence — no real model prices at the pin's own figure — is the next row, dispositioned separately."
  - claim: "BP-005's `INFERENCE_PRICE_BOOK.nano` pin (20/125 ¢/MTok, cut from BP-005's own `## Public interface`) matches no model in Anthropic's current catalogue — the cheapest available at any tier is Haiku 4.5 at 100/500 ¢/MTok. With any real model behind the `nano` tier, `llm()`'s ledger under-records nano spend relative to what the vendor actually bills, and `CAPS.FREE_C`'s cap arithmetic (BP-007/WO-276's cap-check machinery) is computed from a price the product cannot actually buy at — the free-scan spend ceiling silently fails open. This is exactly the seam WO-026 was rated `risk: high` for."
    disposition: open
    reason: "Implementer, 2026-09-04: named explicitly as the architect's and the owner's to resolve, not a code change in this work order — a price is the owner's to move under the decision-rights table (rule 1.2, §1), and it changes what a free scan actually costs to run. Validator, 2026-09-04 (TST-040, rule 2.3b): left `open`, not `undischargeable` — the claim (\"no model in Anthropic's current catalogue prices at 20/125¢/MTok\") is in principle dischargeable by a cited, dated vendor-pricing source (constitution §8: \"cite your source — verbatim, with a URL and a date\"), and no such evidence file exists in `registry/evidence/`; this validation session has no browsing tool to produce one and does not take the implementer's uncited catalogue check on report per §8. Record gap, not a code defect — the fix is either a cited `RESEARCH-*.md` evidence file confirming the mismatch, or the owner's ruling on whether to move `INFERENCE_PRICE_BOOK.nano`. Per rule 2.3b a disposition that changes what is promised (the free scan's actual cost) reaches the owner — it does here, via this `open` state naming the architect/owner, not via a validator ruling on the price itself. This is the third `open` row WO-026 carries on itself and blocks its own `done` gate; deliberate, not an oversight. **Architect, 2026-09-04 — the evidence gap the validator named is closed and the row stays `open`, because what is left is a price and a price is the owner's (§1's decision-rights table).** The cited, dated source is now `registry/evidence/RESEARCH-nano-tier-pricing.md` — read its §0 before quoting it: its vendor figures come from **the `claude-api` skill's bundled model table, cached 2026-06-24, read 2026-09-04**, which is a cached table and **not** a fetch of Anthropic's live pricing page; no session in this chain performed one and no URL is quoted, because none was accessed. The finding is sharper than the row as written: §3 of that file shows the pin is not an error at all but a faithful transcription of `DATA-COSTS.md` §1's row 'Nano class (GPT-nano / Flash-Lite) | $0.20 · $1.25' — **a non-Anthropic price**, matching the charter's own stack row, which names Haiku 4.5 but only a *class* for the second tier, and matching `tiers.ts`'s separate `env.NANO_API_KEY` credential. So the undecided thing is **which vendor sells the nano tier**: option A (a second vendor, pin stays 20/125, a second SDK lands behind the seam) or option B (Anthropic's cheapest, pin moves to 100/500, the free scan gains roughly +1.8 to +2.4c per scan and its worst case goes from 9.0c to ~11.4c against an owner-ruled 12c `CAPS.FREE_C`). That is what a free scan costs to run — the owner's, not this architect's, and not answered here. **Not `undischargeable`: one owner ruling ends it.** It continues to block this WO's `done` gate, correctly."
supersedes: []
---

# WO-026 — The llm() seam — model tiers, schema-parsed output, per-call cost logging

- Branch: wo/WO-026-llm-seam
- Estimate: ≤1 day

## Goal
Make every model call go through one function with a declared tier, a declared
cost and a typed output that is parsed or the call failed.

**Risk: high — seams: money, and data leaving the system.** Every model call the product ever makes goes through this one function: its tier, its logged cost against the spend envelope, and the customer's own page text crossing to a third party. A cost-logging test that survives deletion of the logging spends silently, and a schema-parse test that survives deletion lets an unparsed payload through as if it were typed. Mutation-tested (doctrine 0.13.2, rule 2b).

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW, anchored to
`structure.md`'s row for `src/lib/llm/**` and BP-009's `code:` globs.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/llm/tiers.ts` | NEW | The two tiers — `nano` and `haiku` — each bound to its model id and its per-call cost, read from `env` (`ANTHROPIC_API_KEY`, `NANO_API_KEY`) and `constants.ts`. `BUILD.md` §1: "**Anthropic Haiku 4.5** (prose) + a nano-class model (scaffolding)". No third tier. |
| `src/lib/llm/index.ts` | NEW | `llm()`. Runs inside the caller's `CostContext` via `recordFetch`, with the **call site as the ledger row's `source`**. Parses the response against the supplied `ZodType<T>`; a response that does not parse returns `unmeasured` with reason `unparseable` and is retried at most once inside the caller's budget, never silently coerced. Model unavailability returns `unmeasured`; it never throws. |
| `tests/llm/seam.test.ts` | NEW | The tests below. |

## Interfaces
Exposes (verbatim from BP-009 `## Public interface`):
```ts
export function llm<T>(c: CostContext, call: {
  site: LlmCallSite
  input: unknown
  schema: ZodType<T>          // output is parsed or the call failed
  tier: 'nano' | 'haiku'
}): Promise<Measured<T>>
```
Consumes (verbatim from BP-007 `## Public interface`): `CostContext` and its
`recordFetch`. Consumes `LlmCallSite` from WO-027 and `Measured<T>` from BP-010.

## Steps
1. Write `tiers.ts`.
2. Write `llm()` over `recordFetch`, with the call site as `source`.
3. Add the schema parse and the single retry.
4. Write the tests; watch the "unparseable is never coerced" test fail first by
   falling back to the raw string.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-009 carries
> `satisfies: []` and was cut from the charter's stack table and `BUILD.md` §8.
> REQ-093 criterion 5 — that every word in the product's own voice renders when
> the models are unavailable — is discharged by BP-019's copy registry, which
> holds the strings; this seam's part is only that it degrades rather than
> throws. Cited, not inherited.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-009 error behaviour: "A response that does not parse against the schema is `unmeasured` with reason `unparseable`, retried at most once inside the caller's own budget, and never silently coerced." | `tests/llm/seam.test.ts` — a non-conforming response yields `unmeasured`/`unparseable` after exactly two attempts; a coercing fallback fails the test. |
| BP-009 error behaviour: "Model unavailability degrades the caller" | Same file — a transport failure resolves to `unmeasured`, never rejects. |
| BP-009 data-model delta: "every call is ledgered in `fetches` through BP-007 with the call site as its `source`" | Same file — asserts the `fetches` row's `source` equals the call site string, for a success, a parse failure and an unavailability. |
| BP-009 public interface: "`tier: 'nano' \| 'haiku'`" | Same file — asserts the tier union has exactly two members and that each maps to one model id from `tiers.ts`. |
| BP-009 NFR budget: "call site, tier, tokens in and out, cost, duration, parse outcome; never the prompt or the completion in a log" | Same file — asserts the log record's field set exactly, and that neither the prompt nor the completion text appears in it. |
| BP-009 NFR budget: "p95 latency: nano ≤ 3 s, haiku ≤ 20 s" | Same file — asserts each tier's configured timeout matches its budget. |

## Out of scope
- The call-site union and the prompt templates — WO-027.
- Every caller: BP-011, BP-013, BP-014.
- Any judgement about what a model is allowed to decide; that is WO-027's
  construction and BP-011's deterministic scorer.

## Rollback
Delete `src/lib/llm/index.ts`, `tiers.ts` and the test. No model call is possible,
which is the safe direction.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-009 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 6 rows of `## Test plan` quote BP-009's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-04 finished — planner — /wave propose: **W4**, MoSCoW **Must** (WO-071's `Profile` cannot land without it); `risk: high` set with its seam in `## Goal` (money · data leaving the system, doctrine 0.13.2 rule 2b).
- 2026-09-04 started — implementer.
- 2026-09-04 finished — implementer — `src/lib/llm/tiers.ts`, `src/lib/llm/index.ts` and `tests/llm/seam.test.ts` land (3/3 file plan, all NEW), plus the `constants.ts` modify row (`INFERENCE_PRICE_BOOK` transcribed from BP-005, `INFERENCE_TIMEOUT_MS` transcribed from BP-009). 16 new tests, all three mutation-tested seams (spend, tier selection, credential/payload) manually probed and confirmed dying on the matching bug; `npm run typecheck` clean; `npm test` 53/53 files, 1327 passed/1 skipped (baseline 52/1307/1 + this WO's 16 + 4 the pre-existing generic `tests/config/constants.test.ts` picked up automatically for the two new pin groups); `npm run lint` unchanged at 8115, none in this WO's files. Four `rests-on` rows carried into `## rests-on` above (one refuted — `Measured<T>` exists; three left `open` for the architect — `call.site` typing pending WO-027, the `unparseable`/`undeterminable` reason gap against BP-024, and both tiers' model ids, absent from every approved artifact). Ready for `/validate`.
- 2026-09-04 finished — implementer — money-defect correction, coordinator review (prior log line's `/validate` readiness withdrawn): `nano` was pinned to `claude-fable-5` — a real id, but Anthropic's *most expensive* tier ($10.00/$50.00 per MTok) wired into the cheapest lane, against this file's own `INFERENCE_PRICE_BOOK.nano` pin (20/125¢/MTok) — a 50×/40× under-count of every nano call's real cost that no test in `tests/llm/seam.test.ts` could see, because the price book and the id agreed with each other and were both wrong together; with the cap-check machinery (BP-007/WO-276) computing `CAPS.FREE_C`'s ceiling off that price, the free-scan spend ceiling would have silently failed open — precisely the seam this WO was rated `risk: high` for. Corrected: `nano` now points at `claude-haiku-4-5` (same id as `haiku`, the cheapest real model in the current catalogue, verified against it — still 5× the nano pin rather than 50×/40×). `INFERENCE_PRICE_BOOK` left untouched (BP-005's, not this file's to move). A second, deeper finding is not fixed by this: no model in the current catalogue prices at the nano pin's own 20/125¢/MTok — recorded as its own new `open` `rests-on` row, named as the architect's and owner's to resolve, since it changes what a free scan costs. Two tests updated (the swapped-id assertion no longer applies once both tiers share an id; replaced with literal id checks, a price/timeout-divergence check, and an explicit `claude-fable-5` regression guard) and one added (the nano-lane shadow test, mirroring the existing haiku one); tier-selection mutation re-probed and still kills its test on the new id. Gates re-run: `npm run typecheck` clean; `npm test` 53/53 files, 1329 passed/1 skipped (18 of this WO's own, +2 over the earlier log line's 16); `npm run lint` unchanged at 8115, none in this WO's files. Ready for `/validate`.

## Validation report (appended by validator)

## TST-040 — WO-026 validation report

### Scope
Independent verification of WO-026 on `wave/W3` tip `3caa2ae` (merge commit `065b9ed`, including the money-defect fix commit `5fc4e4f`). Read: this WO, BP-009, `tests/llm/seam.test.ts`, `src/lib/llm/{tiers,index}.ts`. Risk: high — mutation-tested per doctrine 0.13.2.

### Criteria (WO-026 `## Test plan`, 6 rows)
| # | Criterion (source) | Test | Verdict |
|---|---|---|---|
| 1 | BP-009: unparseable → `unmeasured`, 1 retry, never coerced | schema-parse describe block, 2 tests | PASS — discriminates |
| 2 | BP-009: model unavailability degrades, never rejects | unavailability describe block | PASS — discriminates |
| 3 | BP-009: `fetches` row's `source` = call site | ledgered describe block, `it.each` over success/parse-failure/unavailability | PASS — discriminates |
| 4 | BP-009: `tier: 'nano' \| 'haiku'`, each maps to one model id | tier→model-id describe block, 5 tests | PASS — discriminates (see mutation below) |
| 5 | BP-009 NFR: log field set, never prompt/completion | observability describe block | PASS — discriminates |
| 6 | BP-009 NFR: p95 nano ≤3s, haiku ≤20s | latency describe block, 2 tests | PASS |

### Mutation testing (risk: high, doctrine 0.13.2 rule 2b) — re-run independently
1. **Regression guard — set `nano` back to `claude-fable-5`** (the exact money defect): killed 2 tests (`the same holds for the nano lane specifically`, and the literal-id test's own `not.toBe("claude-fable-5")` assertion). Confirms the guard genuinely discriminates, not just documents. Reverted.
2. **Bypassed `c.recordFetch` entirely** (called `runAttempts` directly, fabricated a zero-cost `recordFetch`-shaped result): killed 5 tests across the ledger and spend-accounting describes (`calls` stayed empty). Confirms the spend seam is load-bearing, not incidental. Reverted.
3. **Credential-leak mutation** (piping `binding.apiKey` into a second `console.log` call inside `logCall`): this specific live probe was blocked by this sandbox's own runtime classifier before I could observe pass/fail — noted as a limitation of this session, not evidence either way. In its place: verified by direct code reading that `tests/llm/seam.test.ts`'s credential-leak test (a) checks *both* plaintext and base64-encoded forms of *both* fixture keys (learned from WO-023's own near-miss, cited in the test's own header), across a success, a parse failure and an unavailability, and (b) carries a second, dedicated test proving the check is not vacuously passing because the credential went unused (`constructedWith[n].apiKey` asserted equal to the real fixture key per tier). This is the same shape of check WO-023's mutation probe caught failing before it was strengthened; I could not re-run the live probe here, and say so rather than certifying it silently.

### The money defect (dispatch's specific check)
Independently confirmed the defect, the fix, and the guard:
- `git show 6999209:src/lib/llm/tiers.ts` (pre-fix) pinned `nano: "claude-fable-5"`; `src/lib/llm/tiers.ts` (current) pins `nano: "claude-haiku-4-5"`.
- `INFERENCE_PRICE_BOOK.nano` (20/125 ¢/MTok) is untouched by the fix — correctly, since BP-005 owns that pin (rule 7.1) and the model-id fix does not need to move it.
- Mutation 1 above independently confirms the regression guard kills on the exact old id.
- **A second pin in this same wave was checked for the same failure mode** (dispatch's instruction to look for other pins that agree with each other but not the world): `haiku`'s id (`claude-haiku-4-5`) was checked against `INFERENCE_PRICE_BOOK.haiku` (100/500 ¢/MTok = $1.00/$5.00 per MTok) — this matches Anthropic's own published Haiku 4.5 pricing, so this pin is not a second instance of the same bug. No other model-id/price pair exists in this WO's file plan.

### `rests-on` disposition (rule 2.3b) — five rows, all dispositioned this pass
1. **`Measured<T>` existence** — `refuted` (already correctly dispositioned by the implementer as stale); re-confirmed independently (`src/lib/llm/index.ts:56`, `npm run typecheck` clean).
2. **`call.site` typed `string`, not `LlmCallSite`** — left **`open`**, deliberately. The underlying fact is confirmed by direct read; the design choice (narrow now, or let WO-027 add a modify row) is the architect's, at WO-027's own dispatch, which has not happened. Not `undischargeable` — WO-027's own build will resolve it either way.
3. **`unparseable` vs. `undeterminable`** — left **`open`**, deliberately, for the same reason: the factual gap is confirmed (`UnmeasuredReason` has no `unparseable` member), but which artifact moves is a rule-7.1 ownership call outside this file plan and this validation pass.
4. **Model-id choice under rule 1.1** — **`confirmed`**. Choosing a model id is a parameter, not a promise change; both ids are now correctly pinned and the regression guard is confirmed live (mutation 1 above). Settled, not escalated.
5. **`INFERENCE_PRICE_BOOK.nano` matches no real model** — left **`open`**, not `undischargeable`. The claim is in principle dischargeable by a cited, dated vendor-pricing source (constitution §8); no such evidence file exists in `registry/evidence/`, and this validation session has no browsing tool to produce one — I am not certifying the implementer's uncited "verified against Anthropic's current catalogue" on report, per §8's own rule against a paraphrase presented as a citation. This is a **record gap** (a missing evidence artifact), not a code defect. Per rule 2.3b, a disposition that changes what is promised reaches the owner — it does here, through this `open` state naming the architect and owner explicitly, not through a validator ruling on a price, which is not this validator's decision to make.

**Net: WO-026 carries three `open` rows on itself (rows 2, 3, 5) after this pass — deliberate, not an oversight.** Per the `## Definition of done` gate list and rule 2.3b, this blocks WO-026 from `done` until the architect rules on the two type-ownership questions and either the architect/owner rules on the pricing question or a cited evidence file discharges its factual half. None of the three is a defect in the code shipped; all three are legitimate escalations the code correctly declined to resolve unilaterally.

### Gates (independently re-run this session)
- `npm run typecheck`: clean.
- `npm test` (full suite): **57 files / 1406 passed / 1 skipped**.
- `npm run lint`: **8115**, unchanged, none in this WO's files.
- `factory-console --check`: 0 errors / 59 warn / exit 0.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict
pass

The code and tests are correct, complete, and discriminate under live mutation; the money defect is genuinely fixed and regression-guarded. This is a **record finding, not a code finding**: three `rests-on` rows on WO-026 itself are left `open` — two require the architect's ruling (WO-027's `call.site` typing, and `unparseable`'s type ownership), and one requires either the owner's pricing ruling or a cited evidence file this corpus does not yet have. None sends the implementer back to `src/`; all three route to the architect/owner, named above.

Regression: 27 files · REQ-003, REQ-004, REQ-006, REQ-008, REQ-094 re-checked — pass

`git diff --stat 3caa2ae..bb9373e -- src/ tests/` is empty: no code has moved since TST-040's own tip, and this pass's architect log line below (W4 disposition of rows 2/3) touched no file under `src/` or `tests/`, docs only. Full suite independently re-run, 7 runs on tip `bb9373e`: 6/7 clean at 57 files / 1406 passed / 1 skipped, 1/7 the documented intermittent `safe-fetch` timer flake (not attributable to this WO). `npm run typecheck` clean; `npm run lint` 8115, none under `src/`/`tests/`; `npm run build` clean against a throwaway, gitignored `.env.local` (deleted after, tree byte-clean), `NANO_API_KEY` omitted to exercise the `ANTHROPIC_API_KEY` fallback this WO's own `tiers.ts` implements. `factory-console impact WO-026`: 27 files → 13 blueprints → 21 importers → 5 requirements (REQ-003, REQ-004, REQ-006, REQ-008, REQ-094) — the requirement set is reached transitively (through `constants.ts`'s 21 downstream importers), not through `llm/`'s own `satisfies` edge, and none of those requirements' acceptance criteria could have regressed: the code those importers read (`src/lib/config/constants.ts`) is unchanged since TST-040, confirmed by the empty diff above. Surface-specific re-check (dispatch's named risk): the generic `Object.entries(constants)`-driven frozen/throws-on-write suite in `tests/config/constants.test.ts` is dynamic, not a hand-list — re-ran with `--reporter=verbose` and confirmed `INFERENCE_PRICE_BOOK`/`INFERENCE_TIMEOUT_MS` both appear in the auto-generated `is frozen`/`writing to … throws` cases; `grep -rln "INFERENCE_PRICE_BOOK\|INFERENCE_TIMEOUT_MS" src/ tests/` returns only the declaration (`constants.ts`), the one intended consumer (`src/lib/llm/tiers.ts`) and its test — nothing else reads the new pins. No `done` work order's files disturbed: the only `done` order whose governed file this WO's diff touches is WO-006 (`constants.ts`), extended, not broken, per the check above.

## Log
- 2026-09-04 finished — validator — TST-040: money defect independently confirmed fixed and regression-guarded (live mutation); all six test-plan rows discriminate; five `rests-on` rows dispositioned (one refuted, one confirmed, three left open — deliberate, blocking `done` — next: architect rules on WO-027's `call.site` typing and `unparseable`'s type ownership; architect/owner rules on `INFERENCE_PRICE_BOOK.nano` or a cited evidence file is added).
- 2026-09-04 finished — architect — W4 `rests-on` disposition (rule 2.3b, wave close). Rows 2 and 3 **`confirmed`** and ruled: `call.site` is narrowed to `LlmCallSite` by a one-line modify row on WO-027 (BP-009 decision 2 — **planner action, not this WO's**), and `UnmeasuredReason` stays two-membered with BP-009's prose corrected instead (ADR-096, landmine). Row 5 **stays `open` by design**, now pointing at `registry/evidence/RESEARCH-nano-tier-pricing.md` and naming the owner as the one who discharges it: the pin transcribes a non-Anthropic nano-class price and choosing the tier's vendor is choosing what a free scan costs. **This WO remains blocked from `done` on that one row — the honest outcome, not a defect.** No file under `src/` or `tests/` touched; docs only.
- 2026-09-04 finished — validator — /regress (W4 wave-close pass): full suite (7 independent runs), typecheck, lint and build re-run clean on tip `bb9373e`; `factory-console impact WO-026` re-checked, 5 requirements' criteria re-verified against unchanged code; the `constants.ts` pin-isolation surface (`INFERENCE_PRICE_BOOK`/`INFERENCE_TIMEOUT_MS`, generic frozen-suite coverage, sole-consumer check) independently re-confirmed. Regression: pass. Row 5 remains deliberately `open` — unchanged by this pass, still the owner's ruling to make.
