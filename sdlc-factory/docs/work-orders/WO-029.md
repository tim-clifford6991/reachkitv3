---
id: WO-029
type: work-order
title: "Theme tokens — the three-state theming and the daisyUI slot mapping"
status: approved
ui: yes
implements: [BP-018]
wave: W1
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "**No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds: \"no approved requirement states the design system, which is why `satisfies` is empty and the M1 work orders hang off this node directly.\" Every acceptance criterion below is therefore quoted from BP-018 and `BUILD.md` §2.1; none is inherited from a requirement, and none was invented to fill the gap. Reported with this block's return."
    disposition: open
  - claim: "`sdlc-factory/docs/design/tokens.md` is `Status: draft` with an empty table and states in its own body that all UI work orders are blocked while it is. The token VALUES are not missing from the corpus — `BUILD.md` §2.1 states them exactly — so this work order transcribes them from there. What is missing is the design-guardian's registration of them in `tokens.md`, which rule 7.3's preview sign-off depends on. `ui: yes` is what holds this work order until that lands."
    disposition: open
  - claim: "`BUILD.md` §2.1's dark block states `--ok`, `--warn`, `--bad` and adds `/* + matching -bg/-line at 12%/28% alpha */`. The six dark `-bg`/`-line` values are therefore DERIVED at those two alphas from the three stated hues (rule 1.1), not invented and not carried over from light. Each derived value carries a comment quoting that clause. Reversal: six CSS lines."
    disposition: open
supersedes: []
---

# WO-029 — Theme tokens — the three-state theming and the daisyUI slot mapping

- Branch: wo/WO-029-theme-tokens
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-029.html
- Signed-off: 2026-09-03

## Goal
Define every colour, radius, shadow and ring token the product uses, in three
theme states, and map them onto daisyUI's slots so stock daisyUI classes just
work.

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW, anchored to
`structure.md`'s row for `src/ui/**` and to BP-018's `code:` globs
(`src/ui/**`, `tailwind.config.ts`, `tests/ui/**`).

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/ui/theme.css` | NEW | The three states BP-018 fixes: bare `:root` = light, carrying **every** token; `@media (prefers-color-scheme: dark)` guarded with `:root:not([data-theme="light"])`; and `:root[data-theme="dark"]` for the explicit toggle. Values transcribed from `BUILD.md` §2.1 exactly — `--bg --surface --sunk --line --ink --ink-2 --ink-3 --accent --on-accent --accent-bg --accent-line --ok --ok-bg --ok-line --warn --warn-bg --warn-line --bad --bad-bg --bad-line --chart-you --chart-rival --chart-goal --r-box --r-field --r-pill --shadow-card --ring-accent`. The six dark `-bg`/`-line` values are derived at 12% / 28% alpha per the clause quoted in `rests-on`, each with that quote as a comment. **No colour is defined only inside a dark block.** |
| `tailwind.config.ts` | NEW | The token-to-daisyUI mapping `BUILD.md` §2.1 states: `base-100`←surface, `base-200`←sunk, `base-300`←line, `base-content`←ink, `primary`←accent, `success/warning/error`←ok/warn/bad. daisyUI 5 plugin registered here; the theme is one named theme reading the CSS variables, never a second copy of the values. |
| `tests/ui/tokens.test.ts` | NEW | The tests below. |

## Interfaces
Exposes (verbatim from BP-018 `## Module / boundary`): `src/ui/theme.css` (the
three-state theming) and `tailwind.config.ts` (the token-to-daisyUI mapping).
Consumes: nothing. BP-018 depends on BP-005 for pins, and this work order uses
none — a colour is not a pinned number under `structure.md` rule 5; it is a token
with one home, which is this file.

## Steps
1. Transcribe the `:root` block from `BUILD.md` §2.1, token by token.
2. Write the guarded dark media query and the explicit `[data-theme="dark"]`
   block, deriving the six `-bg`/`-line` values at 12% / 28%.
3. Write `tailwind.config.ts` with the six slot mappings.
4. Write the tests; watch the "a token missing from `:root` fails" test fail
   first by moving one colour into the dark block only.

## Test plan
> **Criterion source: the blueprint, not a requirement — and there is no
> requirement to draw on.** See the first `rests-on` row: BP-018 has no
> requirement ancestor at all. Acceptance below is quoted from BP-018 and
> `BUILD.md` §2.1.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-018 error behaviour: "Three-state theming: bare `:root` is light, the dark media query is guarded with `:root:not([data-theme=\"light\"])`, and `:root[data-theme=\"dark\"]` is the explicit toggle." | `tests/ui/tokens.test.ts` — parses `theme.css` and asserts all three selectors exist in that form; an unguarded media query fails. |
| BP-018 error behaviour: "**No colour is ever defined only inside a dark block** — a token missing from `:root` fails a test." | Same file — collects every custom property named in any block and asserts each is present in `:root`. Moving one out fails it. This is the mutation check. |
| `BUILD.md` §2.1: "These exact values — they are lifted from timclifford.dev so the products share a visual family." | Same file — asserts each light token's value against §2.1, quoted in the test (rule 5.3: `path + verbatim quote`, never a line number). |
| `BUILD.md` §2.1: "Map these onto daisyUI's theme slots (`base-100`←surface, `base-200`←sunk, `base-300`←line, `base-content`←ink, `primary`←accent, `success/warning/error`←ok/warn/bad)" | Same file — asserts the six mappings and that no daisyUI slot carries a literal colour rather than a variable reference. |
| BP-018 NFR budget: "the token pairs are the CVD-checked ones from `BUILD.md` §2.4 and are not re-derived" | Same file — asserts `--chart-you` and `--chart-rival` are exactly §2.1's values and that no third series colour is defined. |

## Out of scope
- Fonts and the numeral rule — WO-030.
- Any component — WO-031 onward.
- Registering these tokens in `sdlc-factory/docs/design/tokens.md`: that file is
  the design-guardian's, not the planner's and not the implementer's. It is the
  precondition of this work order's preview sign-off, named in `rests-on`.

## Open questions

- [ ] REVIEW(placement): The explicit toggle state this file implements —
      `:root[data-theme="dark"]` — has no reachable trigger anywhere in the
      corpus. REQ-070 names "Theme, layout or density preferences" as a
      non-goal of the settings-capture screen it owns, no `Toggle` consumer
      listed in `components.md` §4 names a theme switch, and no journey step
      sets `data-theme`. As things stand only the automatic
      `prefers-color-scheme` state is reachable; the explicit third state is
      correctly built per BP-018's contract but dead until some surface is
      planned to set the attribute, or the corpus explicitly declines one.
- [ ] REVIEW(placement): `tailwind.config.ts`'s six-slot mapping (verbatim
      from `BUILD.md` §2.1, correctly scoped to this work order) sets
      `--color-primary`/`-success`/`-warning`/`-error` but no matching
      `-content` slot. Stock daisyUI classes already named in the registered
      contracts — `Btn`'s `primary` variant, `Badge`'s
      `primary`/`success`/`warning`/`error` tones, `Alert`'s tones — render
      their foreground text through daisyUI's own default/auto-computed
      content colour rather than the `--on-accent` token `theme.css` defines
      for that purpose. Worth confirming when WO-268 builds these primitives
      whether daisyUI 5's theme plugin derives an adequate contrast colour on
      its own, or whether the mapping needs the `-content` slots added
      alongside it.

## Rollback
Delete the three files. Nothing renders; no surface has shipped yet.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-018 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote BP-018's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 signed-off — owner — preview sheet `design/previews/WO-029.html`, https://claude.ai/code/artifact/907952de-89ab-40c8-b82e-08b200ae4750
- 2026-09-03 started — implementer — preview signed, rule 7.3 cleared; implementing the file plan on `wo/WO-029-theme-tokens`.
- 2026-09-03 finished — implementer — `src/ui/theme.css`, `tailwind.config.ts`, `tests/ui/tokens.test.ts` written; 16/16 new tests pass, all four discriminate by live mutation; `npm test` 304/304, `npm run test:layout` 16/16, `npm run typecheck` clean, `npm run build` clean. Ready for `/validate`.

## Validation report (appended by validator)
