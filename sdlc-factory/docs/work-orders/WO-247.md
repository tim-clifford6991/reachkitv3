---
id: WO-247
type: work-order
title: "GET /opt-out/{token} — address-wide follow-up suppression, and the one line that says so"
status: draft
ui: yes
implements: [BP-001, REQ-010]
depends-on: [WO-002, WO-003, WO-268, WO-041, WO-112]
blocked-by: []
rests-on:
  - claim: "BP-029's `applyOptOutToken(token)` and `optOutTokenFor(email)` exist before this surface can be wired; the token is signed and has no expiry, and `applyOptOutToken` is idempotent. Both are BP-029's and are cut by another block; this WO calls the first and implements no suppression, no sequence sweep and no token format"
    disposition: open
  - claim: "The two sentences this surface speaks — `optout.confirmed` and `optout.invalid` — are the owner's under constitution §1 and no value for them exists in the corpus. WO-041 seeds both into `src/lib/presentation/copy/keys/mail.ts` as owner-owed, so `copy()` throws naming the key rather than rendering a blank. The page therefore cannot render until the owner writes them: the intended blocking point, and it blocks copy, not structure"
    disposition: open
supersedes: []
---

# WO-247 — `GET /opt-out/{token}` — address-wide follow-up suppression, and the one line that says so

- Branch: wo/WO-247-opt-out
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-247.html
- Signed-off: <date>

## Goal
Apply an opt-out token on arrival and tell the person, in one written line,
that follow-up to their address has stopped — or that the link is not one of
ours — with nothing else on the page and nothing asked of them.

## Provenance of this file plan
**This repository holds no `src/`.** Both paths are NEW. `GET /opt-out/{token}`
is on BP-001's public route list ("address-wide follow-up suppression") and is
anchored by this container's glob `src/app/(public)/**` and by no leaf's.
BP-001's delegation table names the target: "BP-029's address-wide suppression",
behind it REQ-011. WO-004 named this surface as one it does not cut.

**Why `ui: yes`.** BP-001's prose counts this among "six transport-only
adapters", but its own route table lists it under *Routes (public)*, not under
*Adapters (transport only)* — that block names exactly two, the Stripe webhook
and the Inngest endpoint — and this route renders a page a person reads. Rule
7.3 binds "a WO touching customer-visible surface", so it carries a preview and
a sign-off. Marking it `ui: no` on the strength of the prose would let a
customer-visible page ship with no preview, which is the one thing that gate
exists to prevent. Reported with this block's return.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/app/(public)/opt-out/[token]/page.tsx` | NEW | The surface. Server component; reads the `{token}` segment, calls `applyOptOutToken(token)`, and renders a total switch over its two arms: `{ email }` → the confirmation line by key, `{ error: 'invalid' }` → the invalid-link line by key. Nothing else on the page — no control, no form, no field, no address echoed back, no re-subscribe control, no navigation into the product. Renders without a session, a cookie or a payment; composed from registered components (`Card`, `Alert` — `design/components.md`) and holds no string literal a person reads. |
| `tests/app/opt-out/page.test.tsx` | NEW | The suites below. |

## Interfaces

Consumes (verbatim from BP-029 `## Public interface`):
```ts
// src/lib/mail/leads/optout.ts
export function optOutTokenFor(email: string): string                 // signed, no expiry
export function applyOptOutToken(token: string): Promise<
  | { email: string }
  | { error: 'invalid' }>                                             // idempotent
```
Also consumes: BP-019's `copy()` (WO-041) and BP-018's registered components.
`suppressAddress` is **not** consumed: `applyOptOutToken` is the token path and
calls it, and a second caller would be two entry points to one capability.

Exposes (verbatim from BP-001 `## Public interface`):
```
  GET  /opt-out/{token}                 address-wide follow-up suppression
```
`PUBLIC_PATHS` already carries `/opt-out/{token}` (WO-003 writes that allow-list
from BP-001's public route list); this work order does not touch
`src/middleware.ts`.

## Steps
1. Produce the preview artifact at the path in the display bullets above and hold
   in draft until it is signed off (constitution rule 7.3). This WO does not
   proceed to implementation before that.
2. Write the failing suites first, the idempotence case among them.
3. Call `applyOptOutToken` once per request and switch totally over its two arms;
   there is no third rendering and no default arm.
4. Render the confirmation line by key. **Do not render the returned address.**
   The arm carries it because the caller may need it; this page shows it to
   whoever holds the link, which is not necessarily the person who owns it, so
   echoing it would state one person's address to another.
5. Render no control of any kind: the suite enumerates the rendered controls and
   asserts the set is empty.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-010.md`; line breaks are
> normalised to fit the table and no word is changed. **The edge was re-pointed
> on 2026-08-31**: it named REQ-011, which has been `status: superseded` since
> 2026-08-30 — REQ-010 absorbed its criteria verbatim as criteria 9–13 and
> REQ-011's own header says "cite REQ-010, not this file". REQ-011 criterion 3
> and REQ-010 criterion 11 are the same sentence word for word, so the rows
> below change their citation and not one word of their text.
> REQ-010 criteria 9, 10, 12 and 13 — the three touches, the subscribe stop, the
> sequence order and the redelivery cap — are BP-029's `sequence.ts` and are not
> inherited by this surface.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-010 c11 — "Given any of these emails, when it is opened, then it carries a working opt-out, and using it stops all future follow-up email to that address — every remaining touch of the running sequence, every sequence for every other domain, and any sequence a future delivery would otherwise start." | `opt-out/apply · using the link applies the token, once` — the page calls `applyOptOutToken` exactly once with the segment as written and renders the confirmation line on the `{ email }` arm; and `opt-out/apply · a second visit says the same thing` — visiting the same link twice renders the identical page both times and calls nothing that would fail on the second, so the idempotence BP-029 declares reaches the person as one unchanging answer rather than as an error on a link they clicked twice. The suppression itself — every remaining touch, every other domain, every future sequence — is `suppressAddress`'s and is asserted in BP-029's suite. |
| REQ-010 c11, the "working" half | `opt-out/render · one line, no control, no address` — for both arms, the rendered tree holds exactly one written line resolved through `copy()`, zero controls, zero form fields, and does not contain the address returned on the `{ email }` arm; the page renders for a request with no cookie at all. And `opt-out/render · an invalid token is a line, not an error page` — the `{ error: 'invalid' }` arm renders the invalid key, status 200, and never a 404, a blank page or a stack — the same promise BP-001 makes for every public surface. |

## Out of scope
- `applyOptOutToken`'s body, `suppressAddress`, the suppression table and the
  sequence sweep — BP-029's, another block's.
- The opt-out link inside a mail and `optOutTokenFor` — BP-029's and BP-016's.
- Re-subscribing. No requirement in this corpus states it and no control for it
  is rendered here; inventing one would be inventing a behaviour.
- Writing either sentence. They are the owner's; WO-041 seeds the keys.
- `src/middleware.ts` — WO-003 owns the one allow-list.

## Rollback
Delete both files. The link in every follow-up mail 404s, which leaves an opt-out
unreachable — so revert this only together with whatever stops those mails being
sent.

## Self-certification
**Left `status: draft`** on 2026-08-31 by the planner. This work order does not clear §3's gate list, and an honest draft row is the point of rule 3.2's audit clause — a status set by a sweep is what that clause exists to catch.

- **Clause that fails.** One clause, down from two. `optout.confirmed` and `optout.invalid` are owner-owed with no value in the corpus, so the page cannot render. **The other is now fixed:** `implements:` named REQ-011, `status: superseded` since 2026-08-30, and §3 admits no work order from a non-approved ancestor; the edge was re-pointed at REQ-010 on 2026-08-31 and the test-plan rows re-cited at its criterion 11, which carries REQ-011 criterion 3 word for word.
- **What would clear it.** The owner writes the two strings. Nothing else is outstanding.
- **What does hold.** Cut from BP-001 (`status: approved`); 2 files (2 NEW, 0 MODIFY) at ≤1 day; 2 test-plan rows; `## Rollback` states the undo. Both clauses above are the whole of what is missing.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-112); left `status: draft`, reason in ## Self-certification.
- 2026-08-31 finished — planner — `implements:` re-pointed REQ-011 → REQ-010 (REQ-011 superseded 2026-08-30); both test-plan rows re-cited at REQ-010 c11, text unchanged because c11 and REQ-011 c3 are the same sentence; ## Self-certification now names one failing clause, the owner-owed copy, not two. Status stays `draft`.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-031 → WO-268

## Validation report (appended by validator)
