---
id: WO-279
type: work-order
title: "Generated text, the question and its search, and the surface sweep that proves nothing else gets through"
status: done
wave: W3
ui: no
risk: high
implements: [BP-020, REQ-093]
depends-on: [WO-001, WO-041]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code."
    disposition: confirmed
  - claim: "Model-produced text reaches a surface only through the seven stored columns `GeneratedColumn` names (BP-020 rests-on row 3). A model string reaching a surface from any other column is outside this module's sink and is invisible to it; WO-044's flow gate is scoped to the same seven and inherits the same limit."
    disposition: confirmed
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31), so the four governed globs match zero files on the day this suite lands and it would pass vacuously. This WO therefore ships a synthetic fixture surface tree carrying known violations, so the sweep discriminates from the first run rather than from the first screen."
    disposition: confirmed
  - claim: "Every surface the product renders or sends is reachable from `src/app/**`, `src/app/(hosted)/**`, `src/lib/mail/**` and `src/ui/**` (ADR-010 rests-on; BP-020 rests-on row 2). A surface rendered from a path outside those globs is invisible to this suite."
    disposition: undischargeable
  - claim: "This suite is recorded, not enforced (constitution rule 3.3, ADR-010 point 6): no CI configuration, no branch protection and no pre-commit hook exists in this repository at cut time. A failing sweep blocks no merge until a platform decision creates one. This WO creates no such configuration."
    disposition: confirmed
supersedes: [WO-043, WO-044]
---

# WO-279 — Generated text, the question and its search, and the surface sweep that proves nothing else gets through

- Branch: wo/WO-279-generated-text
- Estimate: ≤1 day

## Goal
Ship the only two ways model-produced text reaches a person — bound to the page it belongs to and to whether that page is written or merely proposed, and bound to the search it came from — and the path-glob suite over the surface tree that fails on a product sentence written as a string literal and on a `GeneratedColumn` value reaching a surface any other way.

**Risk: high — seam: data leaving the system.** This is the boundary that decides what model-produced text reaches a person, and the sweep that proves nothing else does — the charter's standing law 3 and BUILD §14's guardrails, which the charter calls features, not policy. A path-glob sweep is a negative-space suite: it passes when it matches nothing, which is the shape that goes vacuous silently, and TST-028 already returned FINDINGS on exactly that gap. Mutation-tested (doctrine 0.13.2, rule 2b).

## Consolidation (rule 2.6)
WO-044 is a test-only order whose whole subject is WO-043's two functions: its
goal names `renderGenerated` and `renderQuestion` and its flow gate is the proof
of WO-043's boundary. WO-043's `## Out of scope` hands that gate to WO-044 by
name. A boundary and the suite that proves it are one capability from interface
to test; splitting them is the horizontal cut along a layer rule 2.6 forbids.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `src/lib/presentation/generated/text.ts` | **NEW** | WO-043 | `GeneratedText`, `GeneratedColumn`, `fromStored()`, `PageIdentity`, `renderGenerated()`, `generatedLabel()`. The only constructor of a `GeneratedText` is `fromStored`, which takes a `GeneratedColumn`; no overload accepts a literal and no widening cast appears in the file. |
| `src/lib/presentation/generated/question.ts` | **NEW** | WO-043 | `renderQuestion()` — returns the ordered pair a surface renders whole. No function in this file returns the wording alone. |
| `src/lib/presentation/generated/index.ts` | **NEW** | WO-043 | The module's public entry point: re-export the symbols listed under Exposes and nothing else. |
| `tests/presentation/generated/text.test.ts` | **NEW** | WO-043 | TST for criteria 1, 2 and 5 as they bind this module. |
| `tests/presentation/generated/question.test.ts` | **NEW** | WO-043 | TST for criterion 3. |
| `tests/presentation/copy/surface-tree.ts` | **NEW** | WO-044 | The file enumerator: walks `src/app/**`, `src/app/(hosted)/**`, `src/lib/mail/**` and `src/ui/**` with `node:fs` recursive read, returns every `.ts`/`.tsx` path found, and returns the counts it walked per glob. Takes a root parameter so the fixture tree can be swept with the same code. **This is the one file enumerator in the corpus; WO-047's route enumerator is a different thing and lives in BP-021's tree.** |
| `tests/presentation/copy/allowlist.ts` | **NEW** | WO-044 | The declared allow-list of non-voice string positions ADR-010 point 1 names — attribute values, `aria-*`, test ids, class names — as an explicit, reviewable data structure with one comment per entry saying why it is not the product's voice. Nothing else is allowed. |
| `tests/presentation/copy/budget.ts` | **NEW** | WO-044 | `export const COPY_SWEEP_BUDGET_MS = 120_000` — BP-020 Decisions 3's parameter, declared here and nowhere else. A comment records that exceeding it is fixed by sharding on the surface glob, never by raising the number. |
| `tests/presentation/copy/string-literal-sweep.test.ts` | **NEW** | WO-044 | Parses each enumerated file with the TypeScript compiler API and fails on a string literal in a JSX text position, in a text-bearing prop, or in a mail template body, unless the position is on the allow-list. Emits the coverage statement. Asserts the wall clock against the budget. |
| `tests/presentation/generated/flow-gate.test.ts` | **NEW** | WO-044 | Fails on a value read from one of `GeneratedColumn`'s seven columns reaching a surface other than through `renderGenerated` or `renderQuestion`; and fails on a `GeneratedText` reaching a slot whose `PageIdentity` is absent (BP-020 `## Error & edge behavior`, tenth bullet). Emits its own coverage statement. |
| `tests/presentation/copy/__fixtures__/surfaces/**` | **NEW** | WO-044 | The synthetic surface tree: one clean file per governed glob that must pass, and one violating file per rule that must be flagged by name — a JSX text literal, a text-bearing prop literal, a mail body literal, an allow-listed attribute literal (must pass), a direct `drafts.body` read into JSX, and a `GeneratedText` rendered with no `PageIdentity`. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-043

**Consumes:** `copy`, `CopyKey` from `src/lib/presentation/copy` (WO-041). The
labels `renderGenerated` returns are the `generated.page.written` and
`generated.page.proposed` keys WO-041 seeds; both are owner-owed.

**Exposes** (verbatim from BP-020 `## Public interface`):
```ts
// ── src/lib/presentation/generated/text.ts ───────────────────────────────────
/** Model output, wrapped at the point it is read out of storage. An unwrapped
 *  string is the product's own voice by construction; there is no widening
 *  cast in this module and no `GeneratedText` constructor that takes a literal. */
export interface GeneratedText { readonly source: 'model'; readonly text: string }
export function fromStored(column: GeneratedColumn, value: string): GeneratedText
export type GeneratedColumn =
  | 'drafts.body' | 'drafts.title' | 'drafts.slug' | 'drafts.description'
  | 'opportunities.proposed_title' | 'opportunities.proposed_slug'
  | 'questions.wording'

/** REQ-093 criterion 2. A written page carries all four fields; a proposed page
 *  carries a title and a slug and **has no description arm at all** — the
 *  requirement admits none, so none is representable. */
export type PageIdentity =
  | { state: 'written';  pageId: string;        title: GeneratedText; slug: GeneratedText
      body: GeneratedText; description?: GeneratedText }
  | { state: 'proposed'; opportunityId: string; title: GeneratedText; slug: GeneratedText }

/** The one sink for model text. It cannot be called without the identity of the
 *  page the text belongs to, and it returns the label and the text as one value,
 *  so a caller cannot render the text and drop the label. */
export function renderGenerated(
  field: GeneratedText,
  page: PageIdentity
): { label: string; text: string; proposed: boolean }

/** BP-019's declared helper, unchanged: the label alone, for a caller that
 *  already holds a `renderGenerated` result and needs its label separately. */
export function generatedLabel(a: { pageTitle: string; written: boolean }): { label: string }

// ── src/lib/presentation/generated/question.ts ───────────────────────────────
/** REQ-093 criterion 3. The wording of one of the twelve questions is admitted
 *  only on the condition that the search it was derived from is shown with it —
 *  so this returns an ordered pair the surface renders whole. There is no
 *  function that returns the wording alone. The *content* of `provenance` is
 *  REQ-006 criterion 9's and is built by that requirement's node; this node
 *  owns only that the pair cannot be split. */
export function renderQuestion(q: {
  wording: GeneratedText
  provenance: string          // supplied by REQ-006's node, already rendered
}): readonly [{ role: 'question'; text: string }, { role: 'provenance'; text: string }]
```

### From WO-044

**Consumes** (test-time only — ADR-010 `## Consequences`: "The suites read other
nodes' source trees. That is a **test-time** relationship and creates no import
and no cycle; no `depends-on` edge is drawn for it"):
- `COPY`, `COPY_META`, `CopyKey`, `OWNER_OWED` from WO-041.
- `GeneratedColumn`, `renderGenerated`, `renderQuestion` from WO-043.
- The source trees under `src/app/**`, `src/app/(hosted)/**`, `src/lib/mail/**`
  and `src/ui/**`, read from disk. No import of any module in those trees.

**Exposes:** `surfaceTree()` and `COPY_SWEEP_BUDGET_MS` to the other two suites
in this block (WO-047 and WO-050 may import the enumerator; neither may copy it).

## Steps

**WO-043's steps** (renumbered; order within the block unchanged):

1. Create `text.ts`. Declare `GeneratedText` with both fields `readonly` and
   `source` fixed to the literal `'model'`. Implement `fromStored(column, value)`
   returning `Object.freeze({ source: 'model', text: value })`. Declare
   `GeneratedColumn` as the seven-member union above, exactly. Do not export any
   other constructor, factory, cast helper or `as GeneratedText` expression.
2. Declare `PageIdentity` as the two-arm union above, exactly. The `proposed` arm
   has **no** `description` field and no `body` field — BP-020 `## Error & edge
   behavior`, seventh bullet.
3. Implement `renderGenerated(field, page)`: `proposed` is
   `page.state === 'proposed'`; `label` is `copy('generated.page.proposed', {
   pageTitle: page.title.text })` on the proposed arm and
   `copy('generated.page.written', { pageTitle: page.title.text })` on the
   written arm; `text` is `field.text` **untruncated** — the decision to truncate
   belongs to the surface (BP-020 `## Error & edge behavior`, tenth bullet). The
   three fields are returned as one value; there is no overload returning `text`
   alone.
4. Implement `generatedLabel(a)` as the label-only helper, delegating to the same
   `copy()` calls step 3 uses. One label producer, not two (rule 7.1).
5. Create `question.ts`. `renderQuestion` returns
   `[{ role: 'question', text: q.wording.text }, { role: 'provenance', text: q.provenance }] as const`.
   The file exports exactly one function. It does not build, alter or validate
   the content of `provenance` — that is REQ-006 criterion 9's node's.
6. Create `index.ts` re-exporting exactly the symbols above.
7. Write both test files. Watch each fail before its source file exists.

**WO-044's steps** (renumbered; order within the block unchanged):

8. Create `surface-tree.ts`. Walk the four globs from a supplied root using
   `node:fs`'s recursive directory read; return `{ files: string[]; perGlob:
   Record<string, number> }`. Skip nothing silently — a directory that does not
   exist yet contributes `0` and is reported as `0`, never omitted (rule 5.5).
9. Create `allowlist.ts` and `budget.ts`.
10. Create the fixture tree under `tests/presentation/copy/__fixtures__/surfaces/`
   mirroring the four glob shapes, with the six files listed in the file plan.
11. Write `string-literal-sweep.test.ts`. Parse each file with
   `ts.createSourceFile` and walk the AST: flag `JsxText` with non-whitespace
   content, a `StringLiteral` or `NoSubstitutionTemplateLiteral` in a JSX
   attribute whose name is text-bearing, and a string literal in a mail template
   body. Compare against `allowlist.ts`. Two describes: one over the fixture tree
   (must flag exactly the known violations, by file and position, and must not
   flag the clean files or the allow-listed attribute) and one over the real
   surface globs (must flag nothing). Emit the coverage statement:
   files walked per glob, literals inspected, violations found, allow-list hits.
12. Write `flow-gate.test.ts`. For each enumerated file, flag any identifier or
   property access naming one of the seven `GeneratedColumn` strings, or a value
   typed `GeneratedText`, that reaches a JSX text position or a text-bearing prop
   without passing through a call to `renderGenerated` or `renderQuestion` in the
   same expression. Same two describes over fixture tree and real tree; same
   coverage statement.
13. Assert the wall clock of each describe against `COPY_SWEEP_BUDGET_MS`.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| 1. "Given any text the product renders or sends to a person — whether or not they are a customer: a verdict, an explanation, a label, a heading, an empty state, an alert, or anything else it says in its own voice — when they read it, then it is a written string in the product and no part of it was produced by a language model, unless criterion 2 or criterion 3 admits it. The kinds named here are illustrative, not a closed list: the criterion holds for every text the product renders or sends, on every surface it speaks on — a screen and a mail alike — including surfaces added after this requirement." | WO-043 | `text.test.ts` → **"a hand-written sentence cannot be laundered into the generated arm"**: `@ts-expect-error` fixtures constructing a `GeneratedText` (a) from an object literal `{ source: 'model', text: 'hello' }` assigned through the module's exported constructor position, (b) via `fromStored('not.a.column', s)`; plus a source-text assertion that the two files contain no `as GeneratedText`, no `as unknown as`, and no exported function other than `fromStored` returning a `GeneratedText`. Discriminates: adding a literal-accepting overload fails it. |
| 2. "Given text a language model produced — the body, title, slug or description of a page ReachKit wrote, or the title or slug of one it has proposed and not yet written — when it appears on any surface the product renders or sends, a screen and a mail alike, to any reader whether or not they are a customer, whoever the page was written or proposed for and whether or not that person is a customer either, including before that page exists, then it appears as that page's content, identified as the page it belongs to and, where the page is not yet written, identified as proposed; and never as the product's own statement about the reader or their market." | WO-043 | `text.test.ts` → **"model text cannot be rendered without its page"**: (a) `@ts-expect-error` fixture calling `renderGenerated(field)` with one argument; (b) `renderGenerated` on a `written` identity returns `proposed === false` and a label produced from `generated.page.written`; on a `proposed` identity returns `proposed === true` and a label from `generated.page.proposed`; (c) the return is a single object carrying `label` and `text` together — a snapshot of `Object.keys` asserts both are present, so a caller cannot receive text without a label; (d) `@ts-expect-error` fixture putting `description` on the `proposed` arm. Discriminates: making `page` optional, or splitting the return, fails it. |
| 2. (same, verbatim as above) | WO-043 | `text.test.ts` → **"the label does not vary with the reader"**: `renderGenerated` has arity 2 and no session, reader or persona argument; identical arguments yield byte-identical results. |
| 3. "Given the wording of one of the twelve tracked questions, which a language model produced, when it renders on any surface — the report and the app alike, and a set re-derived after a category change (REQ-071 criterion 7) as much as the first — then it is admitted on condition that the search it was derived from is shown with it, in the fuller form REQ-006 criterion 9 fixes wherever that requirement governs, and it is never presented as the product's own verdict about the reader or their market." | WO-043 | `question.test.ts` → **"a question's wording cannot be rendered without its provenance"**: (a) `renderQuestion` returns a two-element tuple whose roles are `'question'` then `'provenance'`, in that order; (b) `@ts-expect-error` fixture calling it without `provenance`; (c) a source-text assertion that `question.ts` exports exactly one function and no export returns a bare string. Discriminates: adding a `wordingOnly()` export fails it. |
| 5. "Given the product's language models are unavailable, when anyone opens a ReachKit surface, then every word in the product's own voice renders unchanged, and page content already written still renders from what was stored." | WO-043 | `text.test.ts` → **"stored page content renders with every model unavailable"**: with every export of `src/lib/llm/` replaced by a throwing stub, `renderGenerated(fromStored('drafts.body', stored), writtenIdentity)` returns `stored` unchanged; the module's transitive import graph contains no path under `src/lib/llm/`. |
| 1. "Given any text the product renders or sends to a person — whether or not they are a customer: a verdict, an explanation, a label, a heading, an empty state, an alert, or anything else it says in its own voice — when they read it, then it is a written string in the product and no part of it was produced by a language model, unless criterion 2 or criterion 3 admits it. The kinds named here are illustrative, not a closed list: the criterion holds for every text the product renders or sends, on every surface it speaks on — a screen and a mail alike — including surfaces added after this requirement." | WO-044 | `string-literal-sweep.test.ts` → **"a product sentence written in a surface is flagged by file and position"** (fixture describe) and **"no surface holds a product sentence"** (real describe). Discriminates: deleting the JSX-text rule makes the fixture describe pass with the violation present, which fails the fixture describe's expected-violation list. The "surfaces added after this requirement" clause is carried by the scope being a glob, not a list: `surface-tree.ts` holds no file names. |
| 2. "Given text a language model produced — the body, title, slug or description of a page ReachKit wrote, or the title or slug of one it has proposed and not yet written — when it appears on any surface the product renders or sends, a screen and a mail alike, to any reader whether or not they are a customer, whoever the page was written or proposed for and whether or not that person is a customer either, including before that page exists, then it appears as that page's content, identified as the page it belongs to and, where the page is not yet written, identified as proposed; and never as the product's own statement about the reader or their market." | WO-044 | `flow-gate.test.ts` → **"a stored model column reaches a surface only through renderGenerated"** (fixture + real describes), and **"a GeneratedText reaching a slot with no PageIdentity is flagged"**. Discriminates: the fixture's direct `drafts.body` read must be flagged by file and position; removing the rule fails the fixture describe. |
| 3. "Given the wording of one of the twelve tracked questions, which a language model produced, when it renders on any surface — the report and the app alike, and a set re-derived after a category change (REQ-071 criterion 7) as much as the first — then it is admitted on condition that the search it was derived from is shown with it, in the fuller form REQ-006 criterion 9 fixes wherever that requirement governs, and it is never presented as the product's own verdict about the reader or their market." | WO-044 | `flow-gate.test.ts` → **"questions.wording reaches a surface only through renderQuestion"**: `questions.wording` is one of the seven columns and its only sanctioned sink is `renderQuestion`; a fixture file rendering it through `renderGenerated` is flagged. |

## Out of scope
- The content of a question's provenance line (REQ-006 criterion 9's node).
- The draft-under-review label — REQ-045's, and REQ-093's own non-goal.
- Where a label sits and how generated content is styled or badged — BP-018's.
- The surface-wide flow gate that proves no `GeneratedColumn` value reaches a
- Truncating long model text in a surface slot: `renderGenerated` returns `text`
  untruncated by design.
- Any CI configuration, branch protection or pre-commit hook. ADR-010 point 6 and
  constitution rule 3.3: these suites are recorded, not enforced, and creating
  the enforcement is a platform decision this WO does not make.
- An ESLint rule — ADR-010 alternative 2 rejects it as the gate; it remains
  available later as ergonomics derived from `allowlist.ts`.
- The cold-start route sweep (WO-047) and the stopped-state sweep (WO-050).
- Type-level enforcement at the `src/ui/` boundary — ADR-010 alternative 3
  rejects it on `structure.md` rule 6 grounds.

## Rollback
Delete `src/lib/presentation/generated/**` and the five suites and fixture trees
under `tests/presentation/`. No surface imports them until the AI-answers card
is cut.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-020 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-043, WO-044.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-043, WO-044 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.
- 2026-09-04 started — implementer — dispatched in parallel with WO-018 (`src/lib/egress/`, `tests/egress/`, `eslint.config.mjs`) and WO-276 (`src/lib/costs/`, `tests/costs/`, `supabase/migrations/`) under constitution rule 4.1, on disjoint directories (`src/lib/presentation/generated/`, `tests/presentation/generated/`, `tests/presentation/copy/`).
- 2026-09-04 finished — implementer — `GeneratedText`/`PageIdentity`/`renderGenerated`/`renderQuestion` (WO-043 half) and the string-literal sweep plus the flow gate over the real and fixture surface trees (WO-044 half) built, discriminate (proven by live injection into `src/ui/components/Card.tsx` and `src/app/(public)/page.tsx`, watched fail, reverted), and are green; ready to validate.
- 2026-09-04 failed — validator — TST-028, verdict findings: the string-literal sweep does not detect a written sentence built by concatenation (`{"a " + "b"}`), an undeclared gap distinct from the flow gate's declared lexical-matching limit; and the criterion-1 rule-4.2 substitution's own comment overclaims closure — a `GeneratedText` can be minted by hand via structural typing, with no cast and no second constructor, from any importing file, and rendered through `renderGenerated` undetected — next: implementer, criterion 1 (`tests/presentation/generated/text.test.ts`) and the string-literal sweep (`tests/presentation/copy/string-literal-sweep.test.ts`).
- 2026-09-04 finished — implementer — TST-028's three findings addressed. (1) Closed, not declared: `string-literal-sweep.test.ts` now recurses through a `+`-chain of string literals in JSX children, attributes and mail-body positions (a `BinaryExpression` case added to the AST walk), fixture added, watched fail before the fix, proven to discriminate by live injection into `src/ui/components/Badge.tsx` (reverted). (2) `text.ts`'s module-header and `fromStored` comments corrected to state only what the source-text checks actually close — a cast-based or second-constructor mint inside this module — and no longer claim closure of a structural mint from an external file; a reproduction test (`text.test.ts`, "open hole (TST-028 finding 2)") documents the gap as a passing assertion. Per the architect's ruling (relayed by the coordinator): `GeneratedText` is BP-020's declared interface, so its disposition lives there, not as a second `rests-on` row on this work order (rule 2.4) — none raised here; the approved nominal-brand follow-up is not implemented by this order. (3) Both sweeps gained `assertGovernedCoverage()`: `GOVERNED_GLOBS` asserted against ADR-010's four paths verbatim (independent of file-system state), `src/app`/`src/ui` asserted non-zero, `src/app/(hosted)`/`src/lib/mail` asserted exactly zero and reported, not hidden; proven to discriminate by mutating the glob string and watching both suites fail (reverted). Isolated run (node-project, no db): 4 files / 40 tests passed under load average ~4.6–5.0 on 4 cores (WO-283/WO-281 contention). Full repo: 31 files / 610 passed / 1 skipped. Typecheck and lint clean (lint count unchanged at 8118, all pre-existing outside `src/`/`tests/`). Commit `ccfc1a7` on `wo/WO-279-generated-text`, on top of `47a3ac3`. Ready to re-validate.
- 2026-09-04 re-planned — planner — `risk: high` set (doctrine 0.13.2, rule 2b): data leaving the system — the generated-text boundary and its negative-space sweep. Front-matter and the one-line assessment in `## Goal` are the whole change; no scope, file plan, interface, criterion or test-plan row is touched.
- 2026-09-04 finished — validator — TST-037, verdict pass: re-validated against commit `ccfc1a7`, which lands after TST-028 and directly answers all three of its findings; independently re-verified (not taken on the implementer's report) by mutation-testing each fix back to its failing state (reverting the `BinaryExpression` recursion, re-running the exact structural-mint bypass, mistyping a governed glob) — all three confirmed closed. All 7 test-plan rows now PASS (two were FINDINGS on TST-028); `rests-on` unchanged, non-`open`; upstream BP-020 transcription confirmed already landed correctly. TST-028's verdict does not stand as-is against the current tip — superseded by this report.
- 2026-09-04 finished — validator — `/regress` on TST-037's passing verdict: `Regression:` line appended — 93 files, REQ-093 re-checked, pass; full suite/typecheck/lint/build reproduced clean.
- 2026-09-04 finished — librarian — done audit: gate list all clauses met — TST-037 pass verdict with Regression line, 5 rests-on rows (confirmed/undischargeable) non-open, ui: no (no placement owed), blocked-by: [], commits name WO-279.

## Validation report (appended by validator)

## TST-028 — WO-279 validation report

### TST id
`TST-028` — highest committed corpus-wide (across `doctrine-0.11.0` and
every live worktree's own copy of its work order, checked directly by grep
immediately before writing this report) is `TST-027` (`WO-001.md`... through
the various WOs already validated). No worktree checked — including
WO-276's, named as a concurrent claimant — carried a `TST-0##` heading of
its own at the time of this check. Taken as `TST-028`; if WO-276's validator
also lands `TST-028` first, this id is the one that must renumber, not
WO-276's (checked second, here, explicitly, per the dispatch instruction).

### Scope
WO-279 (`wo/WO-279-generated-text`, commit `47a3ac3`, worktree
`/root/projects/reachkitv3-wt/WO-279`, cut from `doctrine-0.11.0` `9786ef7`,
**not merged**), validated against its own `## Test plan`, `## File plan`,
`## Interfaces`, `## Steps`, `## Out of scope`, its five `rests-on` rows
(2.3b), and the specific hunt this dispatch named: whether the surface-tree
sweep is a glob that matches nothing dressed up as a passing suite (the
pattern WO-018 was returned findings for in this wave). `git diff
9786ef7 47a3ac3 --stat`: exactly 21 files, all `NEW`, all under
`src/lib/presentation/generated/`, `tests/presentation/generated/` or
`tests/presentation/copy/` — matching the file plan's file-for-file count
(3 source files, 4 test files, 3 sweep-support files, 11 fixture files, 21
files against 21). Working tree clean before, during (every injection
below was reverted and `git status --short` confirmed clean after each) and
after this validation.

### The sweep's real coverage — independently reproduced, not taken on report
Ran `tests/presentation/copy/string-literal-sweep.test.ts` and
`tests/presentation/generated/flow-gate.test.ts` directly with
`--reporter=verbose` to surface their own coverage statements (rule 5.5):

- **Fixture tree:** 11 files walked — `{"src/app":7,"src/app/(hosted)":1,
  "src/lib/mail":2,"src/ui":2}` — 11 literals inspected (string-literal
  sweep), 3 violations, 8 allow-list hits. Matches the file plan's stated
  fixture shape.
- **Real tree:** 26 files walked — `{"src/app":5,"src/app/(hosted)":0,
  "src/lib/mail":0,"src/ui":21}` — 37 literals inspected, **0** violations,
  37 allow-list hits (string-literal sweep); the flow gate walks the same
  26/perGlob and finds 0 violations. **Exact match to the implementer's
  claimed counts** (26 files; 5/0/0/21 per glob; 37 literals; 0 violations;
  37 allow-list hits).
- **The two zero-count globs, checked directly on disk, not inferred from
  the count:** `src/app/(hosted)/` exists and contains only `.gitkeep`
  (`ls -la` confirms) — the glob is correct and the directory is genuinely
  empty of `.ts`/`.tsx` files today. `src/lib/mail/` does not exist on disk
  at all (`ls`: "No such file or directory") — `surfaceTree()`'s own code
  (`if (!existsSync(dir) ...) { perGlob[glob] = 0; continue; }`) reports
  `0` rather than omitting the key, matching rule 5.5 and WO-279 rests-on
  row 3's own design intent. Neither zero is a glob-path bug.
- **Does the suite fail closed on a missing/empty governed directory,
  rather than merely reporting success?** Partially. The real-tree describe
  asserts `filesWalked` (the **sum** across all four globs) is greater than
  zero — this is a real, working vacuous-sweep guard at the whole-suite
  level (an empty **repo** would fail this assertion, not report clean),
  and it is genuinely exercised today: `src/app` and `src/ui` together
  supply the 26 files that keep it non-zero while `src/app/(hosted)` and
  `src/lib/mail` sit at 0. But no assertion anywhere in either test file
  checks a **per-glob** count — `perGlob` is only ever logged (rule 5.5's
  coverage statement), never asserted against an expected value. Confirmed
  by grep: `perGlob` appears in both test files only in type signatures and
  `console.log` interpolation, in no `expect(...)`. Practically: if
  `src/app/(hosted)` or `src/lib/mail`'s glob string in `GOVERNED_GLOBS`
  were silently mistyped tomorrow (e.g. `src/lib/mial`), both suites would
  keep passing — `filesWalked` would still be 26 from the other three globs
  correctly walked (or 25, still non-zero), and nothing would name the
  broken glob. This is not the WO-018 pattern (this whole tree is not
  vacuous — it walks 26 real files and does find/flag real violations by
  fixture) but it is the same failure mode at finer grain: the aggregate
  guard cannot see a single governed directory going silently dark. Not
  severe enough on its own to fail this WO (today's two zero counts are
  independently verified correct on disk, and `GOVERNED_GLOBS` is a
  4-line, rarely-touched literal, not a computed path), but real and worth
  recording — a per-glob non-negative/reported-not-omitted check would
  close it cheaply.

### Discrimination — my own injection, in different files than the implementer's
The implementer's own proof used `src/ui/components/Card.tsx` and
`src/app/(public)/page.tsx`. Independently reproduced in different files,
each reverted immediately (`git status --short` clean after every one):

1. **`src/ui/components/Btn.tsx`** — added `<span>This is an injected
   hand-written sentence</span>`. Real-tree describe: **failed**, named
   `{"file":"src/ui/components/Btn.tsx","rule":"jsx-text"}` exactly.
2. **`src/app/(account)/layout.tsx`** — added a `drafts: { body: string }`
   prop rendered raw as `<p>{drafts.body}</p>`. Flow-gate real-tree
   describe: **failed**, named `{"file":"src/app/(account)/layout.tsx",
   "rule":"raw-column-read"}` exactly.

Both discriminate by file and rule, in files the implementer never touched.

### Bypass attempts — the four named in the dispatch, plus three more
| Attempt | File | Result |
|---|---|---|
| JSX attribute not on the 19-entry allow-list (`placeholder="Type your email address here"`) | `Btn.tsx` | **Caught** — `jsx-attribute`, named by file |
| No-substitution template literal as a JSX expression child (`` {`This is a template-literal sentence`} ``) | `Btn.tsx` | **Caught** — `jsx-expression-string`, named by file |
| **String built by concatenation** (`{"This is " + "a concatenated sentence"}`) | `Btn.tsx` | **NOT caught.** Suite passes clean (8/8), 0 violations. `sweepFile`'s `isVoiceLiteral` only matches `ts.isStringLiteral`/`ts.isNoSubstitutionTemplateLiteral` directly in a JSX-expression child; a `BinaryExpression` operand is never inspected. **Finding — see below.** |
| Deeply nested file under a governed glob (`src/app/deep/nested/very/deep/violation.tsx`) | new dir | **Caught** — real-tree file count rose to 27, violation named by full path; recursion is unbounded, not a reach gap |
| Renamed-variable flow-gate bypass (`d.body` instead of `drafts.body`) | `(account)/layout.tsx` | **NOT caught — but this is the declared limit, verified concretely, not a new finding (see next section)** |
| Uppercase extension (`src/ui/components/Weird.TSX`) | new file | **NOT caught** — `SWEPT_EXTENSIONS` is `{".ts",".tsx"}`, case-sensitive on this (Linux) filesystem; a `.TSX` file is invisible to the enumerator. Minor: no file in this shape exists today and Next.js itself would not route one. Noted, not treated as a defect requiring rework. |
| Symlinked subdirectory under a governed glob | new symlink | **NOT caught** — `readdirSync(..., {withFileTypes:true})`'s `Dirent.isDirectory()` uses `lstat`, so a symlinked directory is never walked into. Minor, same severity note as above. |

### The flow gate's declared limit — verified, not merely asserted
WO-279 rests-on row 2 and ADR-010 rests-on row 1 both state, and both
carry `disposition: open` on disk at the time this build was read (checked
directly: `ADR-010.md` front matter, one row, `open`; `BP-020.md` front
matter row 3, `open`) — the flow gate is lexical name-matching
(`drafts.body`, `<x>.title.text`), not type-checked, so a `GeneratedColumn`
read through a differently-named variable is invisible to it. Demonstrated
concretely above: `d.body` (renamed from `drafts.body`, same shape,
same raw unwrapped read reaching JSX) passes the flow gate clean — 9/9,
0 violations. This is exactly the declared gap, exactly as large as
declared, and no larger. **Not a defect** — a genuinely undischargeable
architectural limit of a lexical (non-type-checked) sweep, correctly
recorded as an `open` row rather than silently assumed closed.

### The budget's provenance
`tests/presentation/copy/budget.ts` declares `COPY_SWEEP_BUDGET_MS =
120_000` and attributes it to "BP-020 `## Decisions` entry 3." Checked
directly: `BP-020.md` `## Decisions` entry 3, verbatim: "3. **The 120 s
conformance budget is this node's parameter, not a product pin**... Decision:
120 s, declared once in `tests/presentation/copy/budget.ts`... putting it in
`constants.ts` would make `tests/pins.test.ts` assert a fact about CI
hardware." The number, its home and its rationale were all fixed at the
blueprint level before this WO existed — this WO transcribes, it does not
invent. No rule 2.4 violation.

### The 19-entry `ATTRIBUTE_ALLOWLIST` — narrow, not a rubber stamp
Read every entry. 1 prefix rule (`aria-*`, with its own limitation named in
its `reason` field) + 18 exact-name entries: `className`, `data-testid`,
`data-theme`, `data-surface`, `id`, `htmlFor`, `name`, `type`, `href`,
`src`, `rel`, `target`, `role`, `method`, `action`, `variant`, `lang`,
`key` — 19 total, matching the claim. Every entry carries its own
one-line reason. None composes a customer-visible sentence in this design
system's registered components: `name`/`type`/`role`/`method`/`rel`/
`target` are enum/wire tokens, `href`/`src`/`action` are URLs, `id`/
`htmlFor`/`key` are DOM/React identifiers, `className`/`variant`/
`data-theme` are style tokens, `data-testid`/`data-surface` are test/layout
hooks never rendered to a reader. Attribute detection is fail-closed by
design — every attribute not on this list is presumed voice (confirmed by
the `placeholder` bypass attempt above, caught immediately) — so the list's
narrowness is enforced structurally, not merely by discipline. Judgement:
a real gate, not a catch-all.

### Criterion 1's rule-4.2 substitution — judged, and disproven as written
The implementer's own comment in `text.test.ts` states the structural-typing
gap is closed because "nothing in this module hands back a `GeneratedText`
except `fromStored`, so a hand-written sentence can enter only by an author
writing a cast or a second constructor, both of which the source-text
checks below catch." **This claim is false, demonstrated directly:**

```ts
// No `as GeneratedText`, no `as unknown as`, no call to fromStored.
const handWritten: GeneratedText = {
  source: "model",
  text: "This sentence was never read from storage — the author typed it directly.",
};
const result = renderGenerated(handWritten, { state: "written", pageId: "p", title: handWritten, slug: handWritten, body: handWritten });
```
This compiles with **zero** TypeScript errors (`tsc --noEmit`, isolated),
and renders through `renderGenerated` exactly as if `handWritten.text` had
come from storage (`vitest`: 1/1 pass, `result.text === handWritten.text`).
Neither of the two source-text checks fires (there is no cast and no second
`GeneratedText`-returning export — the object is typed structurally against
the exported interface, from any file that imports the type), and neither
sweep catches it: the flow gate only flags a raw read reaching JSX
**without** passing through `renderGenerated`, and a hand-minted value
passed correctly *through* `renderGenerated` is, by the gate's own design,
indistinguishable from a legitimate one; the string-literal sweep never
sees the literal because it never appears directly in a JSX position.

**Judgement:** the pair of source-text checks plus the real `tsc` error on
`fromStored`'s own two-argument signature correctly close the two mutations
the test's own "Discriminates" note claims (a literal-accepting overload,
an unbounded column string) — those are proven. They do **not** close
criterion 1's central promise ("no part of it was produced by a language
model") against the structural-typing gap the rule-4.2 note itself names,
because that gap requires neither a cast nor a second constructor to
exploit. The substitution was the right call given `GeneratedText` has no
nominal brand (correctly not the implementer's to add unilaterally — that
is a change to BP-020's own interface, ADR-010 alternative 3's territory,
though notably *internal* to `text.ts` and not the `src/ui/` boundary
alternative 3 rejects on cycle grounds), but its own closure claim
overstates what is proven, and no `rests-on` row records the residual gap
the way the flow gate's sibling limit is recorded two paragraphs above it
in the same file.

### Suite / build
- `tests/presentation/generated/**` + `tests/presentation/copy/**` in
  isolation (`--project node`, this WO's own 6 files only): **57/57
  passed**, deterministic across every re-run in this validation, including
  every injection/revert cycle above.
- Full corpus (`npm test` = `vitest run --project node --project ui
  --project db`): **flaky, as flagged in advance** — the pre-existing
  `db`-project race (WO-275 scope gap) plus **live contention with
  concurrently-running WO-276/WO-277 vitest processes on the shared
  Postgres scratch substrate**, confirmed directly: `ps aux` during this
  validation showed `vitest` workers running from
  `/root/projects/reachkitv3-wt/WO-276` and `/root/projects/reachkitv3-wt/
  WO-277` at the same wall-clock moment as this WO's own full-suite runs.
  Counts by invocation:
  - `npm test` (run 1): 8 failed / 584 passed / 14 skipped (606) —
    `tests/db/clients.test.ts`, `tests/account/columns.test.ts`,
    `tests/scan/free/admission-claim.test.ts` affected.
  - `npx vitest run ... --no-file-parallelism` (run 2): 5 failed / 579
    passed / 22 skipped (606) — contention persisted; `--no-file-parallelism`
    only serialises within this invocation, not across worktrees.
  - `npx vitest run --project db --no-file-parallelism` (isolated, run 1):
    2 failed / 90 passed (92) — live `relation "public.scans" does not
    exist` mid-run, i.e. another process's schema reset firing during this
    run.
  - `npx vitest run --project db --no-file-parallelism` (isolated, rerun):
    **4 passed (4 files) / 92 passed (92)** — clean.
  - `npx vitest run --project node tests/account/columns.test.ts
    --no-file-parallelism` (isolated): **1 passed (1 file) / 10 passed
    (10)** — clean.
  - `npm test` (run 3, full, unisolated): 23 failed / 582 passed / 1
    skipped (606) — contention still live (WO-276 in particular still
    running per `ps aux`).
  All failures are `relation "..." does not exist` / row-mismatch errors
  from schema resets racing across worktrees, matching the two known
  conditions named in this dispatch exactly (WO-275's `vitest.config.ts`
  scope gap; the shared substrate). None reproduce against WO-279's own
  files in isolation — reproduced the contention directly (not merely
  cited it) via concurrent `ps aux` evidence. Total accounted every run:
  606, matching the expected 605 passed + 1 skipped when clean (+35 tests,
  +4 files over the 570/1/27 baseline, confirmed by `git diff --stat`
  showing 6 new test files landing 57 new tests here plus this WO's own
  file-count contribution to the 31-file total — the arithmetic in the
  dispatch's own counts holds). Not chased further as a WO-279 defect,
  per this dispatch's own instruction.
- `tsc --noEmit`: clean.
- `eslint .`: **8118 problems (8093 errors, 25 warnings)** — independently
  re-run on `doctrine-0.11.0` (`9786ef7`) itself: identical count, byte
  for byte. `presentation/generated`/`presentation/copy` paths: **zero**
  matches in the lint output. Matches the claim exactly.
- `next build`: failed as first attempted (`env.ts` throws, no
  `.env.local` in this sandbox — the same precedent TST-024/TST-025 set).
  Wrote a throwaway `.env.local` with the 17 schema-valid placeholder
  bindings `src/lib/config/env.ts`'s `zod` schema requires; `npm run
  build` then compiled, typechecked, collected page data and emitted the
  same `ƒ Proxy (Middleware)` route summary as WO-003's own report.
  Deleted immediately after — `git status --short` clean before and after,
  confirming it was never tracked.

### Criterion-by-criterion

| # | Criterion (from) | Test | Verdict |
|---|---|---|---|
| 1 (WO-043) | REQ-093 c1 — no part of any surface text is model-produced unless c2/c3 admit it | `text.test.ts` "a hand-written sentence cannot be laundered..." | **FINDINGS** — see rule-4.2 judgement above: the source-text checks discriminate the two mutations they claim, but do not close the structural-typing gap the same file's comment claims they do |
| 2 (WO-043) | REQ-093 c2 — model text renders only bound to its page | `text.test.ts` "model text cannot be rendered without its page" + "the label does not vary with the reader" | PASS — both reproduced, arity/identity/label-source assertions hold, `@ts-expect-error` fixtures genuinely type-error |
| 3 (WO-043) | REQ-093 c3 — a question's wording renders only with its provenance | `question.test.ts` | PASS — tuple order, required-provenance type error and single-export check all reproduced |
| 5 (WO-043) | REQ-093 c5 — stored content renders unchanged with models unavailable | `text.test.ts` "stored page content renders with every model unavailable" | PASS — no `src/lib/llm/` import (AST-scoped, reproduced), text passes through byte-identical |
| 1 (WO-044) | REQ-093 c1 — the surface-wide string-literal sweep | `string-literal-sweep.test.ts`, both describes | **FINDINGS** — fixture/real describes both reproduced and discriminate for every rule this suite implements, but the concatenation bypass above shows the rule set is incomplete against the criterion's own words ("no part of it was produced by a language model" — read as "no product sentence escapes the registry," concatenation is exactly such an escape) |
| 2 (WO-044) | REQ-093 c2 — the flow gate | `flow-gate.test.ts`, both describes | PASS — reproduced by independent injection in a different file; the lexical-matching limit is declared (`open` row) and verified to be exactly as large as declared, no larger |
| 3 (WO-044) | REQ-093 c3 — `questions.wording` sanctioned only through `renderQuestion` | `flow-gate.test.ts` "questions.wording reaches a surface only through renderQuestion" | PASS — dedicated fixture reproduced, named by file |

All 9 test-plan rows are present, all are backed by tests that discriminate
against the mutations they claim to discriminate against (independently
re-verified by my own injections, not the implementer's). Two of the nine
rows — both under REQ-093 criterion 1 — pass their own stated mutations but
leave a real, verified gap against the criterion's actual scope, undeclared
anywhere in this WO's `rests-on`, in BP-020, or in ADR-010.

### `rests-on` disposition (rule 2.3b)
All five of WO-279's own rows dispositioned on this WO's own front matter
(none left `open` — the terminal-state requirement this rule states):

1. "No source tree... at cut time... placement is provisional" —
   **confirmed**. The build is the observation: 21 files landed across
   exactly the three authorized directories with no placement conflict or
   revision needed: the provisional plan held.
2. "Model text reaches a surface only through the seven `GeneratedColumn`
   names... invisible outside this sink" — **confirmed**. Verified
   concretely, not merely read: `GENERATED_COLUMNS` in `flow-gate.test.ts`
   is exactly these seven pairs, hardcoded; the renamed-variable bypass
   above demonstrates the boundary is exactly where declared, not wider or
   narrower.
3. "Governed globs match zero files at cut time... ships a synthetic
   fixture tree so the sweep discriminates from the first run" —
   **confirmed**. The real tree is no longer empty (26 files) by the time
   of this build, but the fixture tree was built and independently
   confirmed to discriminate (this report's own injection/bypass table)
   regardless — the row's purpose held.
4. "Every surface is reachable from the four globs... a surface outside
   them is invisible" — **undischargeable**. This is a universal claim
   about the whole application's surface set, contingent on every future
   file landing under `structure.md`'s owned paths — no single build can
   prove a negative over files that do not yet exist. Checked what a build
   *can* show: no surface anywhere in the current tree sits outside the
   four globs (full `src/app`, `src/ui`, `src/lib/mail` inventory taken
   directly). That is consistent with, not proof of, the claim.
5. "Suite is recorded, not enforced... no CI/branch-protection/pre-commit
   hook exists... this WO creates no such configuration" — **confirmed**.
   Checked directly: no `.github/workflows`, no `.git/hooks/pre-commit`,
   no `*.yml`/`*.yaml` at the repo root; `git diff --name-only` confirms
   no config file is in this WO's own commit.

**Upstream — BP-020 and ADR-010, which this order `implements`/is decided
by.** WO-279 is the first (and so far only) work order built against either
node, so under rule 2.3b this validator is the first stage able to observe
their matching claims: BP-020 row 3 (same claim as WO-279 row 2 above) would
be **confirmed**; BP-020 row 2 and ADR-010's row (same claim as WO-279 row 4
above) would be **undischargeable**, for the identical reasons stated
against WO-279's own rows. **Not written onto `BP-020.md` or `ADR-010.md`
in this pass** — this dispatch's own scope instruction restricts edits to
"WO-279's own validation section and `## Log`" while six other agents work
concurrently across the corpus, including a planner in `work-orders/`; I am
not able to confirm `blueprints/`/`decisions/` are not being concurrently
touched. Recorded here for the architect/librarian to transcribe at wave
close (constitution rule 2.3b's own fallback: "the rest are dispositioned by
the architect on its own read at wave close").

### Coverage gaps / findings
1. **String-literal sweep does not detect a written sentence built by
   concatenation** (`{"a" + "b"}` or equivalent `BinaryExpression` forms).
   `sweepFile`'s literal check only inspects `ts.isStringLiteral`/
   `ts.isNoSubstitutionTemplateLiteral` nodes directly in a JSX-expression
   child, JSX attribute or mail-body property; a `+`-joined pair of string
   literals in the same position is invisible. Demonstrated live, 8/8 pass
   with the injected sentence present. Undeclared anywhere (`rests-on`,
   BP-020, ADR-010) — unlike the flow gate's lexical-matching limit, which
   is explicitly recorded. Because a raw concatenated sentence also carries
   no `COPY_META` tag, this gap is invisible not just to this sweep but,
   by ADR-010 point 4's own logic ("the registry is the enumerator"), to
   BP-021's and BP-054's laws downstream as well.
2. **Criterion 1's rule-4.2 substitution overclaims closure.** See the
   dedicated section above — a `GeneratedText` can be minted by hand via
   structural typing (no cast, no second constructor) from any file that
   imports the type, and rendered through `renderGenerated` as if it were
   genuine model output, completely undetected by the source-text checks,
   the flow gate or the string-literal sweep.
3. (Minor, not blocking) The real-tree describes assert only the aggregate
   `filesWalked > 0`, never a per-glob count — a silently mistyped entry
   in `GOVERNED_GLOBS` for either of today's two zero-count globs would
   not be caught. Today's two zeros are independently verified correct.
4. (Minor, not blocking) `SWEPT_EXTENSIONS`'s case-sensitive match and
   `readdirSync`'s `lstat`-based `isDirectory()` mean an uppercase-extension
   file or a symlinked subdirectory under a governed glob is invisible to
   the enumerator. No file in either shape exists today; recorded, not
   scored as a defect requiring rework.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass
/ findings / reject."

### Verdict

findings

Nineteen of the WO's twenty-one files, all four sweep-support files, and
seven of nine test-plan rows are exactly what they claim to be: the file
plan is followed to the file (21/21, three authorized directories only),
typecheck/lint/build are clean (lint byte-identical to the `doctrine-0.11.0`
baseline, zero findings in the two new module paths), the real-tree
coverage counts match the implementer's report exactly (26 files, 37
literals, 0 violations, 37 allow-list hits), the 19-entry allow-list is a
real, narrow, fail-closed gate, the budget is correctly transcribed from
BP-020 rather than invented, and the flow gate's declared lexical-matching
limit is exactly as large as declared — all independently reproduced by my
own injections in files the implementer never touched (`Btn.tsx`,
`(account)/layout.tsx`), not merely re-run on the implementer's own.

Two real, non-vacuous, undeclared gaps under REQ-093 criterion 1 keep this
from a clean pass, both found by doing exactly what this dispatch asked —
trying to slip a written sentence past the sweep, and testing the rule-4.2
substitution's own closure claim rather than accepting it: (1) the
string-literal sweep does not catch a sentence built by string
concatenation, silently passing it as if it were a call through `copy()`;
(2) `text.test.ts`'s own comment claims the structural-typing gap is closed
by its source-text checks, and it is not — a hand-typed `GeneratedText`
passes straight through `renderGenerated` from any importing file with no
cast, no second constructor and no test that catches it. Neither is the
WO-018 pattern (an enumerator matching nothing) — the enumerator here is
real and does find real violations — but both are the same failure mode at
finer grain: a rule set that looks exhaustive and is not, undeclared as a
limit the way the flow gate's own sibling gap correctly is.

**What clears this:** (a) extend `string-literal-sweep.test.ts`'s AST walk
to flag a `BinaryExpression` of two literal operands in the same positions
it already checks, or explicitly record the gap as an `open` `rests-on` row
alongside the flow gate's; (b) either add a nominal brand to `GeneratedText`
(purely internal to `text.ts`, no `structure.md` rule-6 cycle — distinct
from ADR-010 alternative 3's rejected `src/ui/`-boundary branding) so a
hand-typed object can no longer satisfy the type, or correct `text.test.ts`'s
comment to stop claiming closure it does not have and record the residual
gap as an `open` `rests-on` row. Both are implementer-scoped fixes against
existing files in this WO's own file plan, not a re-plan.

`rests-on`: four of WO-279's own five rows dispositioned `confirmed`, one
`undischargeable` (row 4, a universal claim no build can close); none left
`open`. Upstream BP-020/ADR-010 matching rows judged in prose above but not
written to their files in this pass, per this dispatch's own scope
restriction — for the architect/librarian to transcribe.

No `ui: yes` — no `Placement:` line owed.

Regression not run — held for `/regress`, per this dispatch's own
instruction, once the sibling branches and the `vitest.config.ts` fix land.

## TST-037 — WO-279 re-validation report (post-TST-028 fix, commit `ccfc1a7`)

### Why a fresh report, not a log line
This dispatch instructed a log-line-only note if TST-028's verdict still
stands. It does not: the code TST-028 validated (`47a3ac3`) is not the
code on `wave/W3` today. A follow-up implementer commit, `ccfc1a7` — "fix
close the concat bypass, correct the mint-safety claim, assert per-glob
coverage" — landed on `wo/WO-279-generated-text` (and is present on
`wave/W3`) directly answering all three of TST-028's findings, logged by
the implementer as "Ready to re-validate," with no validation section
written against it since. That is a materially different build; a log
line asserting the old verdict stands would be false on its face. Per
this dispatch's own instruction ("if WO-279 genuinely needs a fresh
report, use TST-037 and say so") — this is that case.

### Scope
`git diff 47a3ac3 ccfc1a7 --stat`: 5 files — `src/lib/presentation/
generated/text.ts` (comment correction only, no behaviour change),
`tests/presentation/generated/{text,flow-gate}.test.ts`, `tests/
presentation/copy/string-literal-sweep.test.ts`, and one new fixture,
`tests/presentation/copy/__fixtures__/surfaces/src/app/
violation-string-concat.tsx`. All five are inside WO-279's own file plan
(the two Modify-in-place test files, the fixture glob, and `text.ts`
itself, which the WO's own file plan already owns as NEW). No file outside
scope. `risk: high — seam: data leaving the system` (unchanged from
TST-028); mutation testing owed and performed on the three fixes, below.
Scoped to `--project node` runs only, per this dispatch's
substrate-discipline instruction.

### Suite (targeted)
`npx vitest run --project node tests/presentation/generated/
tests/presentation/copy/string-literal-sweep.test.ts`: **4 files, 40
passed.** `npm run typecheck`: clean. `npx eslint src/lib/presentation/
generated/ tests/presentation/generated/ tests/presentation/copy/`: clean,
zero output. Working tree left clean after every mutation (`git
diff --stat` empty after each revert, confirmed).

### TST-028's three findings, re-checked against the fix — not taken on the implementer's own report

**Finding 1 — string-literal sweep missed a `+`-concatenation.**
Read `string-literal-sweep.test.ts` directly: `isVoiceExpression` now
recurses through a `BinaryExpression` chain of `+`-joined literals (lines
85–91), applied at JSX-child, attribute and mail-body positions. **Killed
by mutation**: reverting `isVoiceExpression` to its pre-fix form (literal
check only, no `BinaryExpression` recursion) makes the fixture-tree
"flags exactly the known violations" assertion fail — the injected
`violation-string-concat.tsx` fixture (present, `{"This is " + "a
concatenated sentence"}`) is silently missed, exactly TST-028's original
finding, reproduced live before reverting. **Closed.**

**Finding 2 — the rule-4.2 overclaim in `text.ts`'s comment.**
Read `text.ts` directly: the module header and `fromStored`'s own doc no
longer claim the source-text checks "actually close the structural gap in
practice." They now state, correctly, that those checks close only a
cast-based or second-constructor mint *inside this module's own files*,
and that a hand-typed `GeneratedText` built in any external file bypasses
them entirely — matching what TST-028 demonstrated live. A new describe,
`"REQ-093 c1 — open hole (TST-028 finding 2)"`, reproduces the exact
construction TST-028's validator used (no cast, no second constructor,
built from outside the module, passed through `renderGenerated`) and
**asserts it currently succeeds** — an honest, passing reproduction of the
gap, not a claim of closure. Independently re-ran the exact bypass myself
in a scratch script against the current `text.ts`: still passes through
`renderGenerated` unchanged, confirming the comment's corrected claim is
now accurate (it says the gap exists; it does exist). The gap's
disposition is recorded on **`BP-020.md`**, not restated as a second copy
on this WO (rule 2.4) — read directly: `BP-020.md` `rests-on` row 4
(quoting the exact same claim, `disposition: open`) plus `## Decisions`
entry 7, a nominal-brand fix via a future ADR-012 order. This is the
architect's own transcription per rule 2.3b's fallback, already landed —
not something this validator needs to write. **Correctly handled**: this
is documentation-and-disposition correction, not a code fix, and it is the
right fix — TST-028 itself named this as an acceptable path ("(b) …
correct `text.test.ts`'s comment to stop claiming closure it does not
have and record the residual gap as an `open` `rests-on` row").

**Finding 3 (minor) — no per-glob coverage assertion, only an aggregate.**
Read both suites: `assertGovernedCoverage()` now asserts `GOVERNED_GLOBS`
verbatim against ADR-010's four paths (independent of file-system state)
and asserts `src/app`/`src/ui` non-zero, `src/app/(hosted)`/`src/lib/mail`
exactly zero, reported not hidden. **Killed by mutation**: mistyping
`"src/lib/mail"` → `"src/lib/mial"` in `surface-tree.ts` fails both
suites' `assertGovernedCoverage` call at the `GOVERNED_GLOBS` equality
check, before any file-walking assertion even runs — exactly the silent-
typo scenario TST-028 named as unguarded. **Closed**, and closed more
strongly than the minimum TST-028 asked for (the glob-string assertion
itself, not just a per-directory non-zero check).

### Criterion-by-criterion, re-run against the fixed build

| # | Criterion (from) | Test | Verdict |
|---|---|---|---|
| 1 (WO-043) | REQ-093 c1 | `text.test.ts` | **PASS** (was FINDINGS on TST-028) — the overclaim is corrected, the gap is honestly reproduced as a passing, visible test, and its disposition lives at BP-020 (rule 2.4), which already carries it as `open`/dischargeable via a named follow-up (ADR-012) |
| 2 (WO-043) | REQ-093 c2 | `text.test.ts` | PASS (unchanged, re-run) |
| 3 (WO-043) | REQ-093 c3 | `question.test.ts` | PASS (unchanged, re-run) |
| 5 (WO-043) | REQ-093 c5 | `text.test.ts` | PASS (unchanged, re-run) |
| 1 (WO-044) | REQ-093 c1 (sweep) | `string-literal-sweep.test.ts` | **PASS** (was FINDINGS) — concatenation bypass closed, mutation-confirmed |
| 2 (WO-044) | REQ-093 c2 (flow gate) | `flow-gate.test.ts` | PASS (unchanged, re-run; per-glob assertion added, mutation-confirmed) |
| 3 (WO-044) | REQ-093 c3 (`questions.wording`) | `flow-gate.test.ts` | PASS (unchanged, re-run) |

All 7 rows PASS. The two rows TST-028 held at FINDINGS now PASS on
independent re-verification, not merely on the implementer's own claim.

### The two minor, non-blocking items TST-028 also named (4 in its own list)
Uppercase-extension (`.TSX`) invisibility and symlinked-directory
invisibility (`lstat`-based `isDirectory()`): neither addressed by
`ccfc1a7` (it was not aimed at them) and neither re-checked as newly
broken — no file in either shape exists in the current tree (`find src
-iname "*.TSX"` and a symlink sweep both empty). Unchanged from TST-028's
own assessment: recorded, not scored as a defect.

### `rests-on` disposition (rule 2.3b) — unchanged, re-verified
WO-279's own front matter carries all 5 rows dispositioned (4 confirmed, 1
`undischargeable`, row 4 — the universal "every surface is reachable from
the four globs" claim) since TST-028; none `open`. Re-read directly:
unchanged by `ccfc1a7` (no `rests-on` row was touched by the fix commit,
correctly — the fix is code and test, not assumption bookkeeping). Gate
condition remains met.

**Upstream — BP-020.** TST-028 judged BP-020 rows 2 and 3 in prose without
writing them (scope-restricted at the time). Re-read `BP-020.md` directly
today: the architect has since transcribed both, plus added row 4 (finding
2's structural gap, `open`, dischargeable via ADR-012) — all exactly
matching TST-028's own prose judgement. No further action needed from this
report; the graph is consistent.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict

pass

TST-028's two substantive findings (the concatenation bypass, the rule-4.2
overclaim) and its one minor finding (no per-glob coverage assertion) are
all closed by commit `ccfc1a7`, independently re-verified here — not taken
on the implementer's report — including mutation-testing each of the
three fixes back to the failing state TST-028 originally found (reverting
the `BinaryExpression` recursion, reverting `text.ts`'s comment context by
re-running the exact bypass, mistyping a governed glob). All 7 test-plan
rows now PASS; scope is exactly the WO's own file plan (5/5 files in this
delta, all previously-owned); typecheck and lint clean; all 5 `rests-on`
rows remain non-`open`, unchanged and re-verified; the upstream BP-020
transcription the previous report deferred has since landed correctly.
Finding 2's residual gap (a hand-typed `GeneratedText` bypassing the
module from outside) is real and still open — but it is BP-020's `open`
row, not WO-279's, per rule 2.4, and TST-028 itself named "correct the
comment and record it as an `open` row" as a sufficient closing action,
which is exactly what happened.

Regression: 93 files · REQ-093 re-checked — pass. W3 wave-close pass
(`/regress`): full `npm test` reproduced clean across 4 independent runs
(49 files, 1252 passed/1 skipped each, no flake); `npm run typecheck`
clean; `npm run lint` 8115 problems, matching the established baseline,
none in this WO's own files. `npm run build` clean via a throwaway,
deleted, never-committed `.env.local`. `factory-console impact WO-279`:
93 files → BP-019, BP-020 → 0 importers → REQ-093, correctly stated per
rule 5.5 rather than left silent — this WO's sweep (the string-literal
scan and its fixtures) has no downstream importer of its own; its
REQ-093 criterion (no fabricated customer-facing string escapes the
allowlisted surfaces) re-checked by re-running `tests/presentation/copy/
string-literal-sweep.test.ts` and `tests/presentation/generated/
{flow-gate,question,text}.test.ts` directly, all green. `factory-console
--check`: 0 errors. No earlier-`done` work order disturbed by this
wave's diff.

No `ui: yes` — no `Placement:` line owed.
- 2026-09-04 finished — librarian — done audit: gate list all clauses met — TST-037 pass verdict with Regression line, 5 rests-on rows (confirmed/undischargeable) non-open, ui: no (no placement owed), blocked-by: [], commits name WO-279.
