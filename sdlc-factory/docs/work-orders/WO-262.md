---
id: WO-262
type: work-order
title: "A site's condition — recorded only from an answer that site gave, and never a suppression on a non-answer"
status: draft
ui: no
implements: [BP-049, REQ-062, ADR-085]
depends-on: [WO-001, WO-011, WO-206, WO-208]
blocked-by: []
rests-on:
  - claim: "`publications.verify jsonb` can carry a `siteCondition` field beside the serialised `VerifyOutcome` without a column of its own, and the most recent non-null one for a destination is readable in one indexed query over that jsonb. BP-049 decision 5 fixes the shape; the index that makes the read one query is this work order's to choose and lands on WO-208's migration topic, which this work order may not write — see `## Out of scope`."
    disposition: open
supersedes: []
---

# WO-262 — A site's condition — recorded only from an answer that site gave, and never a suppression on a non-answer

- Branch: wo/WO-262-verify-site-condition
- Estimate: ≤1 day

## Goal
Ship `src/lib/publish/verify/site.ts`: the condition of a site a page sits in — named, dated, stored on the check that learned it and read back for later pages — recorded **only from an answer the site gave**, so no page is ever marked as having failed a check for a condition of its site and no non-answer suppresses anything.

## File plan

> This repository carries no `src/`, no `tests/`, no `supabase/` and no
> `package.json` (verified 2026-09-02). **Every path below is NEW** — none was
> verified to exist, because none can. Placement is anchored to
> `sdlc-factory/docs/registry/structure.md` and to BP-049's `code:` globs
> (`src/lib/publish/verify/**`, `tests/publish/verify/**`), and is provisional
> against a repo with no code: if the implementer finds a file already present
> at one of these paths, they stop and log `failed` naming the drift rather than
> merging into it.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/publish/verify/site.ts` | NEW | `SiteConditionKind`, `SiteCondition`, `siteConditionFor`. **Exactly two kinds** — `publishes_no_sitemap` and `robots_blocks_site` — which is REQ-062 criterion 6 **as narrowed by the 2026-09-01 fold to the sitemap and the robots policy alone**: reachability moved into criterion 4's third outcome and **is not a site condition**. `siteConditionFor(destinationId)` returns the site's condition as of the last check that got an answer from it, derived from the most recent `publications.verify.siteCondition` for that destination — **one indexed read, not a second column and not a second fetch** (BP-049 decision 5). Returns `null` where no check has ever had an answer to record one from — **which is not the same as "the site is fine" and is never rendered as one**. This module holds the type, the read and the predicate that decides whether an observation is recordable at all; the *recording* happens on the check that learned it (WO-234). |
| `tests/publish/verify/site.test.ts` | NEW | A site that answered that it publishes no sitemap yields `publishes_no_sitemap` with the date it was found; a site whose own robots document blocks the permitted crawlers across the whole site yields `robots_blocks_site`. **The non-answer rows — the reason this module exists:** a sitemap request that timed out, one that returned a 5xx, one that returned an unparseable body, and the same three for the robots document, each record **no** condition, and `siteConditionFor` still returns whatever an earlier answering check recorded, or `null` — **never a condition, and never a suppression**. **The read-back rows:** a condition recorded by page A's check is returned for page B on the same destination; the most recent non-null answer wins; a condition recorded on one destination is never returned for another. **The freshness row:** `null` is asserted to be distinguishable from "the site is fine" at the type level — the function returns `SiteCondition | null` and there is no `{ ok: true }` shape a caller could read as a clean bill of health. **The no-second-fetch row:** a module-graph and stub assertion that this module makes **no outbound request of any kind** — it reads a stored observation and nothing else. |

## Interfaces

**Consumes**

- `db()` under RLS — BP-002, WO-011.
- `publications.verify jsonb` — WO-208 and `BUILD.md` §10. The `siteCondition` field lives **inside** that jsonb; no new column and nothing on BP-058's `destinations` row.
- Nothing from `verify.ts` or `answer.ts`. This module is read-side and has no dependency on the fetch.

**Exposes** — copied exactly from BP-049's `## Public interface`:

```ts
// src/lib/publish/verify/site.ts
/** REQ-062 criterion 6, narrowed by the 2026-09-01 fold to the sitemap and the
 *  robots policy alone — reachability moved into criterion 4's third outcome and
 *  is **not** a site condition. Recorded only from an answer the site gave. */
export type SiteConditionKind = 'publishes_no_sitemap' | 'robots_blocks_site'
export interface SiteCondition { kind: SiteConditionKind; foundAt: Date }

/** The site's condition as of the last check that got an answer from it. Derived
 *  from the most recent `publications.verify.siteCondition` for that
 *  destination — one indexed read, not a second column and not a second fetch
 *  (decision 5). `null` where no check has ever had an answer to record one
 *  from, which is not the same as "the site is fine" and is never rendered as
 *  one. */
export function siteConditionFor(destinationId: string): Promise<SiteCondition | null>
```

**"Only from an answer the site gave" is the whole of this module, and the wrong
version is the natural one — ADR-085 Decision 6.** Criterion 6 used to cover
reachability; the 2026-09-01 fold moved that case into criterion 4's third
outcome and added the sentence that a condition is recorded *only from an answer*.
So **any suppression keyed on "the site could not be reached" is wrong twice
over**: the case has moved, and criterion 6 may not fire on a non-answer at all.

The failure mode is worth stating because it is silent and durable. A sitemap
fetch that times out looks, to a naive implementation, exactly like a site that
publishes no sitemap — and recording `publishes_no_sitemap` from it does two
things at once: it tells the customer something about their site that ReachKit
never observed, and, because the condition is read back for **every later page on
that destination**, it **suppresses the sitemap check for every page published
there afterwards**. The site then silently stops being checked, with every screen
still correct. The six non-answer rows in the file plan are what stop that, and
nothing else in the corpus would notice.

**Where the answer did not come, the cost falls on one page and stops there.**
BP-049: that one check is `unmeasured` **for this page alone**, with its reason;
no `SiteCondition` is written; nothing about the site is stated as the customer's
to act on; and **no later page's check is suppressed**. `unmeasured`, never
`false` — nothing anywhere defaults a `Measured<boolean>` to `false` (BP-010).

**`null` is not a clean bill of health.** It means no check has ever had an answer
to record a condition from. The type is `SiteCondition | null` precisely so there
is no third value a surface could read as "we looked and the site is fine" — a
distinction that matters because ReachKit's only look at this site is whatever
the last page's check happened to fetch, and the requirement bounds the condition
to exactly that freshness.

**Stored on the check that found it, not on the destination row.** BP-049
decision 5 rejects a column on `destinations`: it puts a fact this node observes
in a row BP-058 owns, and that node's health check would then read like its
writer. It also rejects a fresh probe when a later page is checked — that page's
own check already fetches the sitemap, and a second probe is a second look at a
site for a purpose REQ-062's non-goal closes ("Going back to a site to see
whether a condition … has been put right").

## Steps
1. Read **ADR-085** Decision 6 and BP-049 decision 5 before writing a line. The wrong implementation of this module is the one a competent engineer writes first.
2. Create `site.ts` with the block above verbatim. **Two kinds and no third** — reachability is not one of them.
3. Implement `siteConditionFor` as one query for the most recent non-null `verify.siteCondition` on that destination. Make **no outbound request** — assert its absence in test.
4. Expose the recordability predicate this module owns — an observation is recordable **only where the document answered and could be read** — and write the six non-answer fixtures against it before implementing it.
5. Return `null` where nothing was ever recorded. Add no third value and no boolean.
6. Write the "an unparseable sitemap records nothing and suppresses nothing for a later page" test first and watch it fail against an implementation that treats a non-answer as `publishes_no_sitemap` (constitution §8). That is the mutation that matters here: it passes every happy-path row and silently disables a check for every later page on the site.
7. Choose the index that makes the read one query, and **report it to the architect for WO-208's migration** rather than writing it — `supabase/migrations/*_publications*.sql` is BP-045's glob under structure.md rule 3 and is not this work order's file.

## Test plan

Each criterion below is inherited verbatim from the requirement named in the
row, reflowed onto one line and otherwise unaltered.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-062 c6** — Given a check that did not pass at a destination ReachKit does not serve, when its outcome is shown, then it says whether what did not pass is this page or the site the page sits in. A site that answered that it publishes no sitemap, or whose own robots policy blocks the crawlers criterion 1 identifies as across that whole site, is recorded as a condition of that site — named, carrying the date it was found, and stated as the customer's own to act on — and no page on that site is marked as having failed a check for that cause, then or on any later page. A condition of the site is recorded only from an answer the site gave: where ReachKit could not reach what it went to read, or could not read what came back, no condition of that site is recorded, nothing about that site is stated as the customer's to act on, that one check is stated as not confirmed for this page alone, and no check is suppressed for any later page. A page absent from a sitemap the site does publish, or blocked where the rest of that site is not, is that page failing and criterion 3 governs it. | `tests/publish/verify/site.test.ts` › "a site that answered that it publishes no sitemap is recorded as a condition of that site, named and dated" · › "a site whose own robots document blocks the permitted crawlers across the whole site is recorded as a condition of that site" · › **"no page on that site is marked as having failed a check for that cause, then or on any later page"** — the condition is returned for a later page's check and that page's affected outcome is `unmeasured` with the reason, never `false` (the `unmeasured` write itself is WO-234's) · › **"where ReachKit could not reach what it went to read, or could not read what came back, no condition is recorded and no check is suppressed for any later page"** — six fixtures: sitemap timeout, sitemap 5xx, sitemap unparseable, robots timeout, robots 5xx, robots unparseable. **That row is the discriminating one**: an implementation keyed on "could not be reached" passes the first three rows and fails only this, and its failure mode is a check silently disabled for every later page on the site. The "a page absent from a sitemap the site does publish is that page failing" limb is WO-234's. |
| **REQ-062 c5** — Given verification has completed, when the customer is told about that page, then one message carries its live address together with the outcome of all four checks, and it is sent on the same occasion whether every check passed or any failed — a failed check is never a reason to send nothing — naming which failed where any did and, where criterion 6 recorded a condition of the site, naming that separately from the page's own outcomes. Where criterion 4 governed instead, that one message carries what criterion 4 recorded, with its date, in place of the four outcomes and never alongside them. | `tests/publish/verify/site.test.ts` › "the condition is a value of its own, with its own date" — `SiteCondition` is a distinct shape carrying `kind` and `foundAt` and is never merged into a list of failed checks, which is what lets the mail name it separately (WO-235). This row carries **only the shape half**; the mail's payload and its keys are WO-235's. |

## Out of scope
- **Recording a condition.** The write happens on the check that learned it — WO-234's `verifyLive`. This module supplies the type, the recordability predicate and the read-back.
- Fetching a sitemap or a robots document — WO-234. **And fetching one on this module's own account is forbidden**, not merely out of scope: a fresh probe when a later page is checked is a second look at a site for a purpose REQ-062's non-goal closes.
- The `publications.verify` column and any index over it — WO-208, on BP-045's migration glob. Step 7 reports the index rather than writing it.
- Any column on `destinations` — BP-058's topic, and BP-049 decision 5 rejects putting this fact there.
- Classifying the page's own answer — WO-261. Reachability is **not** a site condition and this module has no arm for it.
- The four `Measured<boolean>` checks and the `unmeasured` writes that carry a condition's reason — WO-234.
- Every customer-visible string. ADR-085's Open questions record that what criterion 6's site-condition line says, and what a surface says where one check alone could not be confirmed while the other three were measured, are **owner-owed**. This module names two kinds and a date.
- Going back to a site to see whether a recorded condition has been put right — REQ-062's non-goal in terms. A condition is only ever as fresh as the last page checked there, which is exactly what the requirement bounds it to.

## Rollback
Delete both files. WO-234 imports `SiteCondition` and the recordability predicate and WO-235 imports the type; revert this only before either lands, or verification records a condition nothing can read back. Nothing is stored by this module and no migration is involved — the field it reads lives in a jsonb WO-208 already creates, so a revert leaves that field simply unread.

## Self-certification
`status: draft` — set 2026-09-02 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Clause that fails.** Its acceptance table inherits REQ-062 criteria 6 and 5 verbatim, and **REQ-062 is `status: in-review`**. Criterion 6's narrowing to the sitemap and the robots policy, and its "only from an answer the site gave" sentence, are both new in the 2026-09-01 fold and are the whole ground for this module. The text carries zero open `REVIEW(...)` lines after two full review rounds (rule 3.4), so it is **settled** — and it is **not signed**, and §3's gate reads *approved*.
- **What would clear it.** The owner approves REQ-062.
- **What does hold.** Cut from BP-049 (`status: approved`, itself asserted on unsigned text under rule 3.2); ADR-085 — `status: accepted`; 2 files (2 NEW, 0 MODIFY) at ≤1 day; 2 test-plan rows, one of which carries six discriminating fixtures; `## Rollback` states the undo; `site.ts` is a new file inside `src/lib/publish/verify/`, which BP-049 already globs whole, so no `code:` glob changes and no scope conflict arises.

No `blocked-by` is added (rule 2.3).

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-02 created — planner

## Validation report (appended by validator)
