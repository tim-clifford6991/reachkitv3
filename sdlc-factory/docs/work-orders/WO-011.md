---
id: WO-011
type: work-order
title: "The two Postgres clients — db(), dbAdmin() and the generated Database type"
status: approved
ui: no
implements: [BP-002]
depends-on: [WO-005, WO-010]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-011 — The two Postgres clients — db(), dbAdmin() and the generated Database type

- Branch: wo/WO-011-db-clients
- Estimate: ≤1 day

## Goal
Expose exactly two ways to reach Postgres — a request-scoped client that carries
the caller's identity and a server-only administrative one — so no other module
chooses how it reaches the database.

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW, anchored to
`structure.md`'s row for `src/lib/db/**` and BP-002's `code:` globs.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/db/index.ts` | NEW | The two clients, exactly BP-002's public interface. `dbAdmin()` reads `SUPABASE_SERVICE_ROLE` through `env` and throws at import time if evaluated in a client bundle. `db()` is request-scoped and constructed per request. |
| `src/lib/db/types.generated.ts` | NEW | Generated from the applied schema by `supabase gen types typescript`. Checked in, never hand-edited; a header states so. |
| `tests/db/clients.test.ts` | NEW | The tests below. |

## Interfaces
Exposes (verbatim from BP-002 `## Public interface`):
```ts
// src/lib/db/index.ts — the only two ways to reach Postgres
export function db(): SupabaseClient<Database>        // request-scoped, RLS applies
export function dbAdmin(): SupabaseClient<Database>   // server-only, bypasses RLS
                                                      // throws if imported in a client bundle
export type { Database } from './types.generated'
```
Consumes (verbatim from BP-005 `## Public interface`):
```ts
export const env: Readonly<Env>
```

## Steps
1. Generate `types.generated.ts` with `npm run db:types` (the script WO-001
   declares) from the schema WO-009 and WO-010 applied.
2. Write the two clients.
3. Add the client-bundle guard to `dbAdmin()`.
4. Write the tests; watch the "generated types are stale" test fail first by
   adding a column and not regenerating.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-002 carries
> `satisfies: []`.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-002 responsibility: "expose exactly two clients … so no other module chooses how it reaches the database" | `tests/db/clients.test.ts` — asserts the module's exported value set is exactly `db` and `dbAdmin`, and lints every file under `src/` for a direct `createClient(` outside `src/lib/db/`. |
| BP-002 error behaviour: "A `dbAdmin()` import from a client component is a build error, not a runtime one." | Same file — a fixture client component importing `dbAdmin` fails the build; the runtime guard is asserted separately so removing either fails a case. |
| BP-002 NFR budget: "the request-scoped client per request, the admin client as a module singleton in server code only" | Same file — two `db()` calls yield two clients; two `dbAdmin()` calls yield one. |
| BP-002 public interface: "`export type { Database } from './types.generated'`" | Same file — asserts the generated file matches a fresh generation against the applied schema (staleness check). |
| BP-002 NFR budget: "migration name, applied-at and checksum are the only schema facts logged; no row content is ever logged" | Same file — asserts the client wrappers install no query logger that captures rows. |

## Out of scope
- Any query. This work order ships the clients and nothing that uses them.
- Connection pooling configuration beyond the two constructors.

## Rollback
Delete `src/lib/db/**` and `tests/db/clients.test.ts`. The
schema stays; nothing can reach it, which is the safe direction.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-002 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote BP-002's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
