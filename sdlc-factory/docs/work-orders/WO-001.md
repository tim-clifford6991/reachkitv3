---
id: WO-001
type: work-order
title: "Next.js toolchain, build configuration and the repo's root files"
status: approved
ui: no
implements: [BP-001]
wave: W1
depends-on: []
blocked-by: []
rests-on:
  - claim: "`structure.md` fixes the top-level directory set but names no owner for the repo's root configuration FILES (package.json, tsconfig.json, eslint/vitest/postcss config, .env.example). They are assigned to BP-001 here because BP-001 already owns `next.config.ts` and the deployable Next.js unit those files configure; rule 7 is not engaged, which governs top-level DIRECTORIES, and the set is unchanged. **Discharged 2026-08-31**: BP-001 decision 2 adopts exactly this assignment and globs all seven files onto this node, and `structure.md` now carries a row for them; the provisional placement this row raised is now the recorded one."
    disposition: confirmed
  - claim: "`factory.config.json` carries `\"schema\": null` and no `typeCheck` key, so the project's type-check command is derived here rather than read: `npm run typecheck` -> `tsc --noEmit`, the ecosystem's native compiler invocation for a TypeScript project. It cannot be run before this work order lands, because the repo holds no TypeScript."
    disposition: open
supersedes: []
---

# WO-001 — Next.js toolchain, build configuration and the repo's root files

- Branch: wo/WO-001-toolchain
- Estimate: ≤1 day

## Goal
Stand up the greenfield Next.js (App Router) + TypeScript toolchain BP-001 is the
deployable unit of, so that every later work order in the corpus has a compiler,
a test runner, a lint pass and a place to put a file.

## Provenance of this file plan
**This repository holds no `src/`.** Every path below is NEW; none was verified
against an existing file, because there is none to verify against. The paths are
anchored to `sdlc-factory/docs/registry/structure.md` ("Top level"), which is the
only authority on where code lives, and to BP-001's `code:` globs. The plan is
provisional in exactly that sense and in no other.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `package.json` | NEW | Declares the **whole** stack `BUILD.md` §1 fixes, in one file, so no later work order in this block has to touch it and every work order below WO-001 can run beside its siblings: `next`, `react`, `typescript`, `tailwindcss` (v4), `daisyui` (v5), `lucide-react`, `@fontsource/plus-jakarta-sans`, `@fontsource/jetbrains-mono`, `@supabase/supabase-js`, `stripe`, `resend`, `inngest`, `@anthropic-ai/sdk`, `zod`, `vitest`, `eslint` + `eslint-config-next`, `supabase` (CLI, dev). Scripts: `dev`, `build`, `start`, `lint`, `test`, `typecheck` -> `tsc --noEmit`, and `db:types` -> `supabase gen types typescript --local > src/lib/db/types.generated.ts`. **No chart library** (BP-018 NFR: "no chart library, so no runtime dependency ships to the browser for a five-chart inventory"). |
| `tsconfig.json` | NEW | `strict: true`, `noUncheckedIndexedAccess: true`, `moduleResolution: bundler`, path alias `@/*` -> `src/*`, and `include` covering `src/**`, `tests/**` and the root config files — so the type-check boundary covers tests, not only `src/`. |
| `next.config.ts` | NEW | BP-001's own file. Typed config; `serverExternalPackages` for the Supabase admin client; no `ignoreBuildErrors` and no `ignoreDuringBuilds` (a build that hides a type error defeats the pins-first CI order BP-005's NFR budget states). |
| `eslint.config.mjs` | NEW | Flat config extending `next/core-web-vitals` + `@typescript-eslint`. **Four** project rules, each with a comment naming its source — BP-001 decision 2 makes this file "where the corpus's cross-module lint invariants land" and this node answerable for them: (a) no `import` of `src/lib/db` internals outside `src/lib/db` (BP-002 boundary); (b) `src/lib/**` may not import from `src/app/**` (`structure.md` rule 6, "Dependency direction is one-way"); (c) **BP-062 decision 1, verbatim** — "`src/lib/account/export/**` may not import from `src/lib/account/billing/**`", the half that blueprint states *as a lint rule*, with `REQ-078` in the rule's message (BP-062: "an import that looks harmless fails CI with the requirement id in the message"); (d) **ADR-050's lintable half only** — nothing outside `src/lib/account/billing/**` may import the reader of `users.paid_through` or re-derive access from `plan_status`, so the one gate lives where ADR-050 point 3 puts it. BP-006's `fetch(` restriction is added by WO-018, not here. |
| `vitest.config.ts` | NEW | Node environment by default, `jsdom` for `tests/ui/**`; `setupFiles: ['tests/setup.ts']`; `tests/pins.test.ts` ordered first (BP-005 NFR: "the first check in CI, so a drifted constant fails before anything expensive runs"). |
| `postcss.config.mjs` | NEW | Tailwind CSS 4 pipeline only. daisyUI is wired in WO-029, not here. |
| `tests/setup.ts` | NEW | Global test setup: fails the run if a test process would make a real network call, so no test in this corpus can silently reach a vendor. |
| `.env.example` | NEW | Every binding of `BUILD.md` §15 by name, plus `HOSTED_EDGE_CNAME_TARGET` (BP-005's `Env` note). **Names only, every value blank** — a real credential in this file would be a defect. |
| `.gitignore` | MODIFY | Add `node_modules/`, `.next/`, `.env*.local`, `coverage/`. The file exists on disk today. |

## Interfaces
Consumes: nothing.
Exposes: no runtime interface. It exposes the commands every later work order's
test plan invokes: `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`.

## Steps
1. Initialise `package.json` with the dependency set above, pinned to exact
   versions (no `^`) so a pinned corpus is not moved by an install. Declaring the
   whole set here is deliberate: it is the single file every other foundation
   work order would otherwise have to modify, and it is what lets WO-011,
   WO-013, WO-026, WO-029 and WO-030 run in parallel.
2. Write `tsconfig.json`; confirm `npm run typecheck` exits 0 on an empty `src/`.
3. Write `next.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`. For each
   of the four lint rules, write the fixture that must be reported before the
   rule exists, and watch it go unreported (constitution §8).
   **ADR-050's other half is not recast here.** Point 3 of that ADR — "BP-003,
   BP-012, BP-015 and BP-063 call it and compute no access predicate of their
   own" — is enforced by ADR-050 point 4's three mutation tests in
   `tests/account/billing/`, and stays there. "Computes no access predicate" is
   not a lexical property an import rule can decide: a rule claiming to enforce
   it would report nothing on a hand-rolled `paid_through > now()` comparison in
   a job body and would read, in CI, exactly like a rule that holds. A check
   that looks like it holds and does not is worse than an absent one, so this
   file takes the import fence and nothing more.
4. Write `vitest.config.ts` and `tests/setup.ts`; confirm `npm test` exits 0 with
   zero test files.
5. Write `.env.example` from `BUILD.md` §15, names only.
6. Extend `.gitignore`.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-001 carries
> `satisfies: []` — it was cut from `00-project.md` and `BUILD.md` §1, not from
> a requirement — so the acceptance below is quoted from BP-001 and `BUILD.md`,
> and no requirement criterion is inherited or paraphrased.

| Acceptance criterion (source quoted) | Test |
|---|---|
| `BUILD.md` §1: "**Next.js (App Router) + TypeScript**" | `tests/app/toolchain.test.ts` — asserts `package.json` declares `next` and `typescript`, and that `tsconfig.json` sets `strict: true`. |
| `BUILD.md` §1: "Repo shape: standard Next.js." | `tests/app/toolchain.test.ts` — asserts `src/app`, `src/lib`, `src/jobs`, `src/ui`, `supabase`, `tests` exist and that no top-level directory outside `structure.md` rule 7's set (`src`, `supabase`, `tests`, `public`, `sdlc-factory`) is present. |
| `BUILD.md` §15's binding list | `tests/app/env-example.test.ts` — asserts `.env.example` names every binding in §15 plus `HOSTED_EDGE_CNAME_TARGET`, and that every value is empty. Deleting a name fails it. |
| BP-005 NFR budget: "The pins test runs in under a second and is the first check in CI" | `tests/app/toolchain.test.ts` — asserts `vitest.config.ts` orders `tests/pins.test.ts` first. |
| `structure.md` rule 6: "No `src/lib/` module imports from `src/app/`" | `tests/app/lint-rules.test.ts` — lints a fixture importing `@/app/...` from a `src/lib` fixture and asserts the rule reports. Removing the rule fails the test (mutation check). |
| BP-062 decision 1: "`src/lib/account/export/**` may not import from `src/lib/account/billing/**`. Enforced by a lint rule and by a test that…" | `tests/app/lint-rules.test.ts` — lints a fixture under `src/lib/account/export/` importing `src/lib/account/billing/gate` and asserts the rule reports with `REQ-078` in the message. Removing the rule fails it. The export-side behavioural test BP-062 names in the same sentence is that node's, not this one's. |
| ADR-050 decision 1: "`plan_status` … is read by **no** gate, guard, job filter or query that decides whether work runs", and BP-001 decision 2: "nothing outside `src/lib/account/billing/**` may import `users.paid_through`'s reader or re-derive access from `plan_status`" | `tests/app/lint-rules.test.ts` — two fixtures outside `src/lib/account/billing/`: one importing the `paid_through` reader, one importing `plan_status`'s column accessor; both are reported, and the same imports inside `src/lib/account/billing/` are not. The rule's message names ADR-050. **A third fixture is asserted *not* to be reported**: a job body computing `user.paid_through > new Date()` inline with no billing import at all — the rule does not catch it, and this test records that it does not, so nobody reads a green lint pass as ADR-050 point 3 being enforced. That half is ADR-050 point 4's three mutation tests. |

## Out of scope
- ADR-050 point 3's mutation tests (`tests/account/billing/`) — that ADR's and
  that node's. This work order ships the import fence and says in its own test
  what the fence does not reach.
- `tailwind.config.ts` and the daisyUI mapping — BP-018, WO-029.
- `src/app/layout.tsx` and the route groups — WO-002.
- `src/middleware.ts` — WO-003.
- CI workflow files. No `.github/` directory is created here; `structure.md`
  rule 7 fixes the top-level set and adding one needs an ADR from the architect.
  Constitution rule 3.3 therefore applies to this corpus: refusals are recorded,
  not enforced, until something holds them.

## Rollback
Delete every NEW file listed and revert `.gitignore`. Nothing depends on this
work order until WO-002 lands, and no data or schema is touched.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-001 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 7 rows of `## Test plan` quote BP-001's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 9 files in the file plan (8 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended: the root-config ownership `rests-on` row is discharged by BP-001 decision 2, and `eslint.config.mjs` gains the two import fences that decision owes it — BP-062's rule verbatim and ADR-050's lintable half, with a test recording what that half deliberately does not catch.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 started — implementer
- 2026-09-02 failed — implementer — run killed by a 429 rate limit before commit — next: resume from disk, re-establish §8 evidence
- 2026-09-02 finished — implementer — toolchain, config and root files audited against the file plan and re-verified with fresh fail/pass evidence for all three test files and all four lint rules; ready to validate.

## Validation report (appended by validator)
