---
id: WO-032
type: work-order
title: "Registered components, part 2 — Toggle, Steps, Join, Collapse, Input, Divider, Kbd"
status: superseded
superseded-by: WO-268
ui: yes
implements: [BP-018]
depends-on: [WO-031]
blocked-by: []
rests-on:
  - claim: "**No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2; none is inherited from a requirement."
    disposition: open
  - claim: "The seven components below must be registered in `sdlc-factory/docs/design/components.md` — an empty table today — by the design-guardian before this work order can be signed off (BP-018 decision 1). The planner may not write that file."
    disposition: open
supersedes: []
---

# WO-032 — Registered components, part 2 — Toggle, Steps, Join, Collapse, Input, Divider, Kbd

- Branch: wo/WO-032-components-2
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-032.html
- Signed-off: <date>

## Goal
Complete BP-018's registered daisyUI set at fifteen, closing the list so an
unregistered widget has nowhere to be exported from.

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW under BP-018's `code:`
glob `src/ui/**`, except the barrel this work order appends to. It is sequenced
after WO-031 because both write `src/ui/components/index.ts`.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/ui/components/Toggle.tsx` | NEW | daisyUI `toggle`. Label required; no default on/off wording. |
| `src/ui/components/Steps.tsx` | NEW | `steps`. Each step's label is required — this is the component the scan's named stages render through, so it can never show an unlabelled step. |
| `src/ui/components/Join.tsx` | NEW | `join`. |
| `src/ui/components/Collapse.tsx` | NEW | `collapse`. Summary text required. |
| `src/ui/components/Input.tsx` | NEW | `input`. Placeholder and label are required props, never defaulted. |
| `src/ui/components/Divider.tsx` | NEW | `divider`. |
| `src/ui/components/Kbd.tsx` | NEW | `kbd`. Renders through `.num` — it is a code-like string under `BUILD.md` §2.3. |
| `src/ui/components/index.ts` | MODIFY | Appends the seven; the registered set is now exactly the fifteen BP-018 lists. |
| `tests/ui/components-2.test.tsx` | NEW | The tests below. |

## Interfaces
Exposes (verbatim from BP-018 `## Public interface`, completing the first line):
```ts
export { Btn, Card, Badge, Alert, Stat, Tabs, Table, Progress, Toggle, Steps,
         Join, Collapse, Input, Divider, Kbd } from './components'
```
Consumes: `Tone` and the theme and type layers.

## Steps
1. Write the seven components.
2. Append to the barrel and assert the exported set is exactly fifteen.
3. Write the tests; watch the "the set is closed at fifteen" test fail first by
   exporting a sixteenth.

## Test plan
> **Criterion source: the blueprint, not a requirement — and there is no
> requirement to draw on.** BP-018 has no requirement ancestor.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-018 public interface: "Registered components only — daisyUI primitives plus the five allowed customs." | `tests/ui/components-2.test.tsx` — asserts the barrel's export set is exactly the fifteen names BP-018 lists. A sixteenth fails. |
| BP-018 decision 1: "An open components directory — rejected, the first unregistered widget is the one that re-implements a card with different spacing and the design system stops being one." | Same file — asserts every `.tsx` under `src/ui/components/` (excluding `custom/`) is exported from the barrel, so a file cannot exist unregistered. |
| BP-018 decision 2: "no component has a default string" | Same file — for each of the seven, the same no-default assertion WO-031 applies to its eight. |
| `BUILD.md` §2.3: "Every numeral, date, URL, search query and code-like string is JetBrains Mono with `tabular-nums`." | Same file — asserts `Kbd` renders through `.num`. |
| BP-003 / `BUILD.md` §2.5 empty-and-degraded rule as BP-018 states it: "Every label, empty state and tooltip is a required prop" | Same file — asserts `Steps` requires a label per step, so an unlabelled stage cannot render. |

## Out of scope
- The four custom components — WO-033, WO-034.
- The five charts — WO-035, WO-036.
- Registering the components in `design/components.md`; that is the
  design-guardian's file.

## Rollback
Delete the eight new files and revert the barrel to WO-031's eight exports.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-018 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote BP-018's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 9 files in the file plan (8 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — superseded by WO-268 (rule 2.6 merge on `/wave propose`); criteria and file plan carried there verbatim, this order stays on disk as history

## Validation report (appended by validator)
