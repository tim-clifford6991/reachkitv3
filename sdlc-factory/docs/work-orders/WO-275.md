---
id: WO-275
type: work-order
title: "npm test's default file parallelism races tests/db/**'s shared scratch database — scope the fileParallelism boundary to a dedicated vitest project"
status: approved
ui: no
implements: [BP-001]
depends-on: [WO-267]
blocked-by: []
rests-on:
  - claim: "vitest 4.1.11's per-project `test.fileParallelism: false` (a `ProjectConfig` field — `ProjectConfig = Omit<InlineConfig, NonProjectOptions | \"sequencer\" | \"deps\">`, and `fileParallelism` is not a member of the `NonProjectOptions` union that excludes root-only options — `node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts`) actually scopes serialization to the files of the one project it is set on, while files in the other three projects (`node`, `ui`, `layout`) keep scheduling concurrently with each other and with that project. Confirmed at the type level only; not yet observed at runtime."
    disposition: open
supersedes: []
---

# WO-275 — npm test's default file parallelism races tests/db/**'s shared scratch database — scope the fileParallelism boundary to a dedicated vitest project

- Branch: wo/WO-275-npm-test-file-parallelism
- Estimate: ≤1 day (two files, one existing project boundary extended by one)

## Goal
Make `npm test` — the documented gate command (`structure.md`'s root-toolchain
row, BP-001 decision 2) — pass reliably by scoping vitest's file-parallelism
serialization to exactly the three `tests/db/**` files that share the one
live scratch database, instead of leaving the root `test` script racing them
by default or serializing the whole suite to paper over a coupling that
belongs to one directory.

## Provenance

**The defect, as measured (quoted verbatim, not rounded — rule 1.2).**
`package.json`'s `test` script reads `"vitest run --project node --project
ui"` — no `--no-file-parallelism`. On `doctrine-0.11.0` at `a5219d8`, same
tree, same moment: `npx vitest run --project node --project ui
--no-file-parallelism` → **288/288 pass**; `npm test` run 1 → **13 failed /
267 passed / 8 skipped**; run 2 → **17 failed / 263 passed / 8 skipped**;
run 3 → **17 failed / 263 passed / 8 skipped** — "It fails every run, and
*which* tests fail varies between runs — the signature of a shared-resource
race, not a logic bug. Every failure is in `tests/db/**`." Cause, per the
dispatch and confirmed by reading the files themselves: `tests/db/
baseline.test.ts`, `tests/db/rls.test.ts` and `tests/db/clients.test.ts`
each reset and rebuild the same physical `public` schema on the one native
Postgres scratch database (`reachkit_scratch`) WO-267 stood up in place of
`supabase db reset` (Docker unavailable). Each of the three files' own
header comment already states this and already names the fix's future
location: *"`vitest.config.ts` is outside this work order's file plan (rule
2), so the flag is stated here rather than forced in config."* This WO is
that deferred fix landing.

**Why (b) — a dedicated vitest project — over (a) — a root
`--no-file-parallelism` flag.**

- **§8 / rule 3.1 (a gate must discriminate).** (a) also turns `npm test`
  green, but it does so by serializing every file in every project — `node`,
  `ui` and `layout` alongside the three `tests/db/**` files that actually
  need it. A green run under (a) is indistinguishable from a green run
  under a correct, narrow fix; it cannot tell an auditor *where* the
  coupling lives, and it would stay green even if a future, unrelated file
  introduced its own shared-state race elsewhere in the suite, because
  everything is already serial. (b) names the coupling at the one project
  boundary that has it, so the fix itself is the record of which capability
  needed it — exactly the property §8 asks a gate to have.
- **Rule 7.1 (single owner per capability).** The coupling — three files
  sharing one live scratch database — is `tests/db/**`'s, i.e. BP-002's, by
  `structure.md` rule 4 ("tests live beside the module they exercise") and
  by the files' own header comments. The *mechanism* that enforces it does
  not have to live in a BP-002 file to respect that ownership: BP-001's
  root toolchain row already is, per `structure.md`, *"the one place a
  cross-module lint invariant can be enforced"* — `vitest.config.ts` is
  named there for exactly this kind of cross-file scheduling concern.
  Scoping the fix to a `vitest.config.ts` project (plus the one-line
  `package.json` script update to invoke it) keeps the entire diff inside
  BP-001's own `code:` globs and touches zero BP-002 files — one order,
  one BP, no split needed, and no second owner minted for the
  `tests/db/**` capability itself (unlike (a), which would also have kept
  the diff inside BP-001's files, so this point is not what decided
  between them — §8 and rule 2.5 below are).
- **Rule 2.5 (a document does not say what the code will say).** (a) is one
  flag; there is nothing to over-specify. (b) is a project boundary, and
  this WO's `## File plan` states *what* changes (a fourth project scoped
  to the three named files, `fileParallelism: false` on it, the `node`
  project's selection narrowed so the same three files are not double-run,
  `package.json` invoking the new project) without prescribing the exact
  glob syntax or project ordering the implementer writes — that is the
  implementer's "how."
- **Reversal cost, both options.** Near-identical and cheap: (a) is a
  one-line revert; (b) is a two-file revert (drop the fourth project block,
  drop `--project db`). Cost was not the deciding factor; discrimination
  and ownership were.

**Reuse check (rule 7.2).** `vitest.config.ts` already defines the
project-splitting mechanism this WO extends (three existing projects:
`node`, `ui`, `layout`) — no new config file, no second test runner
introduced. `grep -rn "tests/db" sdlc-factory/docs/registry/capabilities.md`
returns nothing: no product capability row governs test-execution
scheduling, so there is no capability to duplicate.

**No other order claims these two files for this change.** `grep -rln
"vitest.config.ts\|package.json" sdlc-factory/docs/work-orders/*.md`
returns WO-001 (created both, `done`) and a number of later, unrelated,
`done`/merged orders that touched one or the other for different reasons;
none states or reserves this fix.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `package.json` | MODIFY | The `test` script gains a fourth `--project` naming the new project `vitest.config.ts` adds (next row), alongside the existing `--project node --project ui`, so `npm test` — unchanged in name and in the arguments an external caller passes it — actually executes `tests/db/**`'s three live-database files (today they run under the `node` project; after this change they must run under the new one, not nowhere). |
| `vitest.config.ts` | MODIFY | Add a fourth project (name it `db`) whose file selection is exactly the three files that share the live scratch database per their own header comments — `tests/db/baseline.test.ts`, `tests/db/rls.test.ts`, `tests/db/clients.test.ts` — with `test.fileParallelism: false` set on that project only, so those three serialize against each other while every other project keeps its current concurrency. Narrow the existing `node` project's file selection so it no longer also matches those same three files (they must run under exactly one project, not two). `tests/db/migration-naming.test.ts` — which "touches no live database and is unaffected" per `tests/db/rls.test.ts`'s own header — is untouched and keeps running under `node`, in parallel with everything else. |

## Interfaces
Consumes: vitest 4.1.11's own `projects` / `test.fileParallelism`
configuration surface (`ProjectConfig`, `node_modules/vitest/dist/chunks/
reporters.d.DtoKVV2s.d.ts`) — `fileParallelism` is not excluded from
per-project config (it is absent from the `NonProjectOptions` union that
`ProjectConfig` omits from `InlineConfig`), so it is a supported,
project-scoped setting, not a root-only one. Exposes: nothing new —
`npm test` keeps its existing name and existing zero-argument invocation
for every external caller (CI, a validator, a developer); only its internal
project composition changes.

## Steps
1. Watch it fail first, re-observed on this branch rather than trusted from
   the dispatch alone (evidence rots): run `npm test` three consecutive
   times on the unmodified tree and record that at least one run is red,
   with a different failing-test set between runs — reproducing the shared-
   resource-race signature, not just "some run failed."
2. Apply the two-file change in `## File plan`.
3. Run `npm test` (exactly that script, no added flags) five consecutive
   times; every run must report the same pass count and 0 failed.
4. Prove the fix is not accidental whole-suite serialization — the
   discriminating half of the stop condition, since (a) would also make
   step 3 green: capture evidence (vitest's own reporter output, or a
   timestamp probe added temporarily to one `node`-project file and one
   `ui`-project file) showing files outside the new `db` project still run
   concurrently with each other and with the `db` project's files; only the
   three named `tests/db/**` files serialize against each other.
5. Regression probe: temporarily undo just the `fileParallelism: false`
   scoping (fold the three files back under `node`'s default concurrent
   scheduling, keeping everything else from step 2) and run `npm test`
   three times, confirming the flaky, varying-by-run `tests/db/**` failures
   return. Restore the step-2 state and confirm green again. This is what
   makes the stop condition discriminate: it would still go red if the
   coupling this WO fixes regressed.
6. `npm run typecheck` and `npx eslint .` (the documented `lint` script) —
   both clean.

## Test plan
> **Criterion source.** BP-001 carries `satisfies: []`; this WO changes no
> product behaviour BP-001's `## Public interface` or `## Data model delta`
> states, only the toolchain's own test-execution discipline. Rows below
> quote the dispatch's measured evidence, `structure.md`'s toolchain row,
> and constitution §8/§10 directly — not a requirement, since none governs
> this defect.

| Acceptance criterion (source quoted) | Test |
|---|---|
| Dispatch evidence: "`npm test` (run 1)" → "13 failed / 267 passed / 8 skipped"; runs 2–3 → "17 failed / 263 passed / 8 skipped" each — `npm test` is red today and must go green. | Step 3 — five consecutive `npm test` runs, 0 failed each. |
| Dispatch evidence: "It fails every run, and *which* tests fail varies between runs — the signature of a shared-resource race, not a logic bug." The fix must remove the race, not mask it. | Step 5's regression probe — reverting only the `fileParallelism` scoping reproduces the varying-failure-set pattern; restoring it returns to green. |
| `structure.md`'s root-toolchain row names `npm test` as one of the commands the toolchain is verified by (BP-001 decision 2) — the command name and invocation an external caller uses must not change. | `git diff` on `package.json` shows the script key (`"test"`) and its zero-argument invocation unchanged from the caller's side; only the `--project` list inside the script value gains one entry. |
| §8: "a gate that does not discriminate is the thing being fixed"; §10: "every acceptance criterion is covered by a test that discriminates." A fix whose only evidence is "green now" does not meet this — (a) would also produce green. | Step 4 — concurrency evidence that only the three named files serialize, everything else stays parallel, so the fix is attributable to the named coupling and not to blanket serialization. |

## Out of scope
- Any change to `tests/db/baseline.test.ts`, `tests/db/rls.test.ts`,
  `tests/db/clients.test.ts` or `tests/db/migration-naming.test.ts`
  themselves, including updating their header comments (which currently
  say the serialization "is stated here rather than forced in config" —
  now stale once this WO lands). Updating that prose is optional follow-up
  cleanup, not required for this WO's stop condition, and left out to keep
  this WO's entire diff inside BP-001's `code:` globs (`package.json`,
  `vitest.config.ts`) with zero touches to BP-002's files (rule 7.1).
- The `layout` project / `test:layout` script — no coupling reported there;
  untouched.
- The native Postgres/PostgREST substrate itself
  (`/root/projects/reachkitv3-wt/substrate`) — this is a test-scheduling
  defect, not a database or substrate defect.
- Installing `server-only` or any other dependency — unrelated.
- Re-validating any other `done` work order — that is `/regress`'s pass,
  triggered separately once this WO merges.

## Rollback
Revert the two-file diff (`package.json`, `vitest.config.ts`). `npm test`
returns to `"vitest run --project node --project ui"` with no dedicated
`db` project — the flaky-red state measured in `## Provenance`, which is
this WO's starting state on `doctrine-0.11.0` today, not a regression
relative to before this WO existed. No production code, migration, or
schema touched.

## Self-certification
`status: approved` — set 2026-09-03 by the planner under constitution rule
3.2: an assertion with its grounds stated, which the librarian audits and
may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-001 — `status: approved`; both
  files in the plan are in its `code:` globs (`structure.md` line 23).
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan`
  quote the dispatch's own measured evidence, `structure.md`'s toolchain
  row, and constitution §8/§10 verbatim; BP-001 carries no requirement of
  its own to cite here (`satisfies: []`).
- **Bounded.** 2 files in the file plan (0 NEW, 2 MODIFY), estimate ≤1 day.
- **Stop condition stated and discriminating.** Steps 3–5 name the test
  (`npm test` × 5), the concurrency evidence that separates this fix from
  option (a), and the regression probe that shows the stop condition would
  still fail if the coupling returned.
- **Merge test (rule 2.6).** This is its own order because its goal and
  stop condition are stated entirely in terms of `npm test`'s own pass/fail
  behaviour and are checkable without citing any other order; WO-267 is
  named in `depends-on` only because it is the order whose files
  (`tests/db/**`) create the coupling being scoped, not because this
  order's stop condition needs anything from WO-267's own file plan.
- **Single owner (rule 7.1).** The entire diff stays inside BP-001's
  `code:` globs; no BP-002 file is touched, so `tests/db/**`'s ownership
  is unchanged.
- **Reuse (rule 7.2).** Extends the existing `projects` array in
  `vitest.config.ts` (WO-001's file); mints no second test-runner
  mechanism. No capability-index row governs test scheduling.

Not asserted here: validation, regression and merge. Those are the `done`
gate's, and the validator's and librarian's to set. This WO's one
`rests-on` row (per-project `fileParallelism` actually isolating
concurrency, confirmed today only at the type level) is left `open` for the
validator to disposition per rule 2.3b, using Step 4's concurrency evidence
as the observation.

## Log
- 2026-09-03 created — planner — routed from a defect report on
  `doctrine-0.11.0` (`a5219d8`): `npm test`, the documented BP-001 gate
  command, is red (measured, run-to-run varying failures, all in
  `tests/db/**`) because it lacks `--no-file-parallelism`, which every
  validator/regression pass in the corpus instead ran manually
  (`npx vitest run --no-file-parallelism`, 288/288). Chose option (b) — a
  dedicated `db` vitest project with `fileParallelism: false`, scoped to
  exactly the three files that share the live scratch database — over
  option (a) — adding the flag to the root `test` script — because (b)
  keeps the gate discriminating (§8: green cannot come from blanket
  serialization masking an unrelated future coupling) while (a) would
  freeze the coupling in place without naming it; both options stay inside
  BP-001's `code:` globs, so ownership (rule 7.1) did not decide between
  them. `tests/db/**` (BP-002's files, per `structure.md` rule 4) is not
  touched by this order.
