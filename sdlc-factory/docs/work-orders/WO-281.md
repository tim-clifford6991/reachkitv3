---
id: WO-281
type: work-order
title: "The bounded scan and its progress stream — two ceilings, one Ending, six named stages, and the wire they reach a visitor on"
status: done
wave: W3
ui: no
risk: high
implements: [BP-022, BP-023, REQ-003, REQ-004]
depends-on: [WO-006, WO-056, WO-267, WO-276]
blocked-by: []
rests-on:
  - claim: "The free battery — own fetches plus 12 live SERP calls plus the suggestion call — completes within 60 s at the p95 of vendor response time; `BUILD.md` §16 milestone 3 is the first measurement of this chain. Inherited from REQ-003's own row"
    disposition: undischargeable
  - claim: "BP-007's `withCostContext` / `CostContext.capHit()` exists and is bound to `CAPS.FREE_C` (12¢) before this WO runs. The cost seam is BP-007's; this repo carries no `src/lib/costs/` today"
    disposition: confirmed
  - claim: "BP-005 pins `TIMING.reportTargetS: 60`, `TIMING.reportCeilingS: 90` and `CAPS.FREE_C: 12`. `structure.md` rule 5 puts every number in `constants.ts`; this WO imports them and declares none"
    disposition: confirmed
  - claim: "BP-005 pins `TIMING.progressHeartbeatS: 30`. `structure.md` rule 5 puts every number in `constants.ts`; this WO imports it and declares none"
    disposition: confirmed
  - claim: "The deployment platform does not buffer or terminate a server-sent-events response inside `TIMING.progressHeartbeatS`, so a heartbeat every 30 s keeps the connection open for the 90 s a scan may last. This is a platform property, not a code one, and `BUILD.md` §16 milestone 3 is the first run that tests it"
    disposition: undischargeable
supersedes: [WO-059, WO-060, WO-063]
---

# WO-281 — The bounded scan and its progress stream — two ceilings, one Ending, six named stages, and the wire they reach a visitor on

- Branch: wo/WO-281-progress-stream
- Estimate: ≤1 day

## Goal
Ship what a visitor watches while a free scan runs: two ceilings neither of which is ever exceeded to finish outstanding work, exactly one `Ending` however the scan ends, the six named stages that advance as work completes rather than on a clock, and the server-sent-events response that carries them and closes after the one ending.

> **Read `decisions/ADR-021.md` before writing a line of this WO.** It is a
> landmine: raising the cap to finish the last call, and computing a partial
> score from the factors that were measured, both read as kindnesses and both
> break a promise a customer can be shown.

**Risk: high — seam: money.** The first of its two ceilings is the spend cap delegated to WO-276's cost context, and ADR-021's first half binds it: the cap is never raised to finish an outstanding call, however close the scan is to complete. A ceiling test that survives deletion of the ceiling costs real money against the charter's cost envelope and shows nothing. Mutation-tested (doctrine 0.13.2, rule 2b).

## Consolidation (rule 2.6)
WO-063's goal — "Serve BP-023's stage stream as server-sent events and nothing
more — one thin adapter" — cannot be stated without WO-060's stream, and WO-060's
`## Out of scope` hands the transport to WO-063 by name. WO-060 consumes WO-059's
`Ending` in its own type, emits it as the terminal event, and WO-059's
`## Out of scope` hands "the stage stream that carries the ending to a visitor" to
WO-060. Three orders, one journey step: JN-001 step 3 from the ceiling that ends
the scan to the wire the visitor reads it on.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `src/lib/scan/ceilings.ts` | NEW | WO-059 | `Ending`, `Bounds`, `withFreeBounds`. `expired()` is `now ≥ startedAt + TIMING.reportCeilingS`; `capHit()` delegates to BP-007's `CostContext`; `stopNow()` returns the first ceiling that fired or `null`. `withFreeBounds` runs `body`, and on either ceiling stops measuring, marks everything outstanding `not_attempted`, and returns `{ result, ending }` with a `report` ending. It exposes no override parameter, no environment escape and no per-scan exemption (ADR-021 decision 1). |
| `tests/scan/free/ceilings.test.ts` | NEW | WO-059 | The ending-totality, deadline, cap and ADR-021 suites named in the test plan. Each ADR-021 assertion carries `ADR-021` in its failure message (that ADR's own consequence). |
| `src/lib/scan/stages.ts` | NEW | WO-060 | `StageName`, `STAGES`, `StageEvent`, `progress`. Six handles, one per dataset boundary of `BUILD.md` §6.3's free-scan list (BP-023 decision 7), ordered. The stream emits `{stage, done:false}` on entry and `{stage, done:true}` on exit, a `heartbeat` at least every `TIMING.progressHeartbeatS`, and exactly one `ending` before closing. A stage the ceilings cut off emits no `done:true`. No word a visitor reads appears in this file — every handle is internal. |
| `tests/scan/free/stages.test.ts` | NEW | WO-060 | The ordering, heartbeat, cut-off and single-ending suites named in the test plan. |
| `src/app/api/scan/[scanId]/progress/route.ts` | NEW | WO-063 | The `GET` handler. Opens an SSE response over WO-060's `progress(scanId)`, serialising each `StageEvent` as one event, closing the stream after the `ending` event. Sets `Cache-Control: no-store` and `X-Robots-Tag: noindex`. Holds no timer, no stage list and no ending logic of its own. |
| `tests/app/scan-address/progress-route.test.ts` | NEW | WO-063 | The transport, heartbeat, termination and unknown-id suites named in the test plan. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-059

Consumes: BP-007's `CostContext` (`capHit()`, `spentCents()`, `degraded()`) and
`withCostContext`; BP-005's `TIMING` and `CAPS.FREE_C`.

Exposes (verbatim from BP-023 `## Public interface`, `src/lib/scan/ceilings.ts`):

```ts
/** REQ-003's "bounded moment", made total. A free scan reaches exactly one. */
export type Ending =
  | { kind: 'report'; complete: true;  stoppedReason: 'complete' }
  | { kind: 'report'; complete: false; stoppedReason: 'time_ceiling' | 'spend_ceiling' }
  | { kind: 'no_report'; stoppedReason: 'failed' }

/** The deadline every stage and every multi-call step re-checks, exactly as
 *  BP-007's `capHit()` is re-checked between calls. */
export interface Bounds {
  expired(): boolean                 // now ≥ startedAt + TIMING.reportCeilingS
  remainingMs(): number
  capHit(): boolean                  // delegates to the CostContext, CAP_FREE
  stopNow(): 'time_ceiling' | 'spend_ceiling' | null
}
export function withFreeBounds<T>(
  a: { scanId: string; startedAt: Date },
  body: (b: Bounds) => Promise<T>
): Promise<{ result: T | null; ending: Ending }>
```

### From WO-060

Consumes: WO-059's `Ending`; WO-056's `scans.status` including `failed`;
BP-002's `dbAdmin()`; BP-005's `TIMING.progressHeartbeatS`.

Exposes (verbatim from BP-023 `## Public interface`, `src/lib/scan/stages.ts`):

```ts
/** Internal handles. Every word a visitor reads is BP-019's (REQ-093 c1). */
export type StageName =
  | 'reading_your_site'        // own fetches: home + detected pricing page
  | 'reading_access_rules'     // robots.txt and the home document's reader rules
  | 'reading_your_market'      // profile + keyword_suggestions → market set + the 12 questions
  | 'checking_your_presence'   // ranked_keywords@50
  | 'asking_the_twelve'        // 12 live organic SERPs, with their AI overviews
  | 'scoring'                  // drivers, score, band, problems, first page
export const STAGES: readonly StageName[]      // exactly the six above, in order

export type StageEvent =
  | { stage: StageName; done: boolean }
  | { heartbeat: true }                                   // ≥ every 30 s
  | { ending: Ending }                                    // terminal; the stream then closes
export function progress(scanId: string): AsyncIterable<StageEvent>
```

`progress` is the narrowing of BP-012's declared
`progress(scanId: string): AsyncIterable<{ stage: StageName; done: boolean }>` —
BP-023's union adds the heartbeat and the ending arms. That refinement is
BP-023's contract and is not re-decided here.

### From WO-063

Consumes: WO-060's `progress`, `StageEvent`, `STAGES`; WO-059's `Ending`.

Exposes (verbatim from BP-022 `## Public interface`):

```
Routes
  GET  /api/scan/{scanId}/progress          SSE; the stream is BP-023's `progress()`
```

## Steps

**WO-059's steps** (renumbered; order within the block unchanged):

1. Write the failing suites first, ADR-021's two prohibitions among them.
2. Implement `Bounds` over an injectable clock so the deadline tests need no real
   waiting; the clock is a parameter of the module, never read from a global
   inside the returned object's methods.
3. Implement `withFreeBounds`: run `body(bounds)`; when `stopNow()` fires,
   abandon outstanding work and return the `report` ending with the matching
   `stoppedReason`. Time is checked first when both fired in the same tick, since
   `TIMING.reportCeilingS` is the bound REQ-003 criterion 5 states absolutely.
4. Implement the `no_report / failed` ending per BP-023 decision 4: reached only
   when no ceiling was reached and every driver ended `unmeasured` with reason
   `undeterminable`. Any `not_attempted` anywhere means a ceiling acted, and a
   ceiling always yields a report.
5. Emit the one ending log line BP-023's NFR budget specifies: `stoppedReason`,
   elapsed ms, cents spent, whether the context degraded. Return none of it from
   `progress()` — no spend figure ever reaches a visitor.
6. Add the source assertions that no override exists.

**WO-060's steps** (renumbered; order within the block unchanged):

7. Write the failing suites first.
8. Declare `StageName` and freeze `STAGES` in order. Assert at module load that
   `STAGES.length === 6` and that every `StageName` member appears exactly once.
9. Implement `progress(scanId)` as an async iterable over the scan's recorded
   stage transitions, emitting entry and exit events per stage.
10. Add the heartbeat: if no event has been emitted for `TIMING.progressHeartbeatS`,
   emit `{ heartbeat: true }`. The heartbeat never advances a stage and never
   carries a stage name.
11. Emit exactly one `{ ending }` and then close the iterable. After the ending,
   the iterable is done — no further event of any kind.
12. On a stage the ceilings cut off, emit no `done: true` for it; the outputs are
   recorded `unmeasured / not_attempted` by the pipeline (WO-277's arm), never 0
   and never simply absent.
13. Emit the one structured log line per stage transition BP-023's NFR budget
   specifies.

**WO-063's steps** (renumbered; order within the block unchanged):

14. Write the failing suite first.
15. Implement the `GET` handler returning a streaming response with
   `text/event-stream`, `no-store` and `noindex`.
16. Serialise each `StageEvent` verbatim as the event payload — no renaming, no
   added field, no percentage.
17. Close the response after the single `ending` event and release the iterator.
18. Handle a disconnected client by cancelling the iterator: no background
   continuation for a visitor who closed the page (REQ-003's non-goal).
19. An unknown or malformed `scanId` responds 404 on this API route — the promise
   that a *report address* never 404s is REQ-001 criterion 5's and binds
   `/scan/{domain}`, not this transport path.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| REQ-003 c5 — "Given a scan still running past 60 seconds — including when the delay is the scanned domain's own server answering slowly — when it reaches 90 seconds, then measuring stops and the visitor is shown the report of everything measured by that point, with the rest reported as unmeasured (REQ-004). No visitor waits on a running scan beyond 90 seconds." | WO-059 | `ceilings/time · the deadline fires at 90 s whatever is blocking` — with the clock advanced to `startedAt + 90 s` while `body` is awaiting a fetch that never resolves, `withFreeBounds` returns `{ ending: { kind: 'report', complete: false, stoppedReason: 'time_ceiling' } }` and the elapsed time never exceeds the ceiling. Repeated with the block inside a multi-call stage, asserting `stopNow()` is re-checked between calls and not only between stages. |
| REQ-003 c11 — "Given a free scan that reaches its spend ceiling while it is running, when it ends, then the ceiling is never exceeded to finish any outstanding measurement, whether or not a driver of the score depends on it: all remaining work is skipped, and the visitor is shown the report of everything measured up to that point, the skipped parts reading as not measured and the score reading as REQ-004 criterion 9 requires, with one written line stating the scan stopped early and the report is incomplete — never an error, a hang, or a refusal." | WO-059 | `ceilings/spend · no call is made after the cap fires` (**must fail first** — this is ADR-021's "let it run 0.4¢ over") — a cost context stubbed at the cap inside a twelve-call stage: the stage makes zero further vendor calls, asserted by call spy; the ending is `{ kind: 'report', complete: false, stoppedReason: 'spend_ceiling' }`; and it is asserted that the run neither threw nor produced a refusal. Failure message names ADR-021. |
| REQ-003 c11, the "whether or not a driver of the score depends on it" clause | WO-059 | `ceilings/spend · a score input is cut off like any other work` (**must fail first** — this is ADR-021's "compute a partial score") — the cap fires part-way through the twelve SERPs, the only source of Presence: no further call is made, the outstanding work is `not_attempted`, and `withFreeBounds` exposes no parameter, flag or environment variable that would let a caller finish it. Asserted by `withFreeBounds.length === 2` and by a source assertion that `src/lib/scan/ceilings.ts` contains no `override`, `raise`, `extra`, `grace` or `allowOverrun` identifier. Failure message names ADR-021. |
| REQ-003 c11, "never an error, a hang, or a refusal" | WO-059 | `ceilings/ending · a ceiling always produces a report` — over both ceilings and over a `body` that throws, that hangs, and that returns partially: the ending is `kind: 'report'` in every ceiling case, and `no_report` is reachable only through the criterion BP-023 decision 4 fixes. |
| REQ-003 c4 — "Given a scan that cannot complete at all, when it ends, then the visitor is told in one written line that the measurement failed and is offered a manual retry (the retry window is REQ-001's)." | WO-059 | `ceilings/ending · failed means nothing was determined` — a run where every driver ended `unmeasured/undeterminable` and no ceiling fired yields `{ kind: 'no_report', stoppedReason: 'failed' }`; a run with any `not_attempted` anywhere yields a `report` ending instead, never `failed`. |
| REQ-004 c9 — "Given a measurement the scan never attempted because it stopped early — at the 90-second ceiling (REQ-003 criterion 5) or the free path's spend ceiling (REQ-003 criterion 11) — when the report renders and the result is stored, then everything depending on it is recorded as "—" and, wherever the report shows it, shown as "—", one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0. Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as "—" on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands." | WO-059 | `ceilings/not_attempted · cut-off work is never zero and never absent` — outstanding work after either ceiling is recorded as `unmeasured/not_attempted` (WO-277's arm), asserted not equal to `zero` and asserted present in the record rather than omitted. |
| REQ-003's non-goal — "No spend, cost or cap figure shown to a visitor." | WO-059 | `ceilings/observability · no cost figure leaves this module toward a surface` — the returned `Ending` is asserted to carry no cents, no cap and no spend field; the cost figures appear only in the log line, asserted by inspecting the emitted log record. |
| REQ-003 c2 — "Given any 100 consecutive free scans of reachable domains … then no more than 5 of those 100 take longer than 60 seconds, and none takes longer than the 90 seconds of criterion 5." | WO-059 | `ceilings/time · the hard stop is absolute over a batch` — 100 simulated runs with vendor latencies drawn from a fixture distribution: none exceeds 90 s. The 60-second p95 half of this criterion is not testable in this WO — it is a claim about real vendor latency and is `BUILD.md` §16 milestone 3's measurement, recorded in this WO's first `rests-on` row. |
| REQ-003 c1 — "Given a scan is running — however it started: a submission on the landing page, a first visit to a shared report address, a re-scan a visitor started from a stored report (REQ-001 criteria 14 and 15), or a re-measurement after a market correction — when the visitor watches the page, then it shows named stages that advance as work completes, never an unlabelled spinner or an indeterminate bar alone." | WO-060 | `stages/order · six named stages, in order, one per dataset boundary` — `STAGES` equals the six handles above in that order; a scan driven through the pipeline emits `{stage, done:false}` then `{stage, done:true}` for each, in order, with no stage repeated and none skipped. And `stages/advance · a stage advances on completed work, not on a clock` — with the clock advanced arbitrarily and no work completing, no `done:true` is emitted; only a completed unit of work produces one. |
| REQ-003 c1, "never an unlabelled spinner or an indeterminate bar alone" | WO-060 | `stages/heartbeat · a slow third party never reads as stuck, and never as progress` — with a stage stalled 95 s, at least three `{heartbeat:true}` events are emitted at ≥30 s intervals, and every one of them is asserted to carry no `stage` field, so a heartbeat cannot be rendered as an advancing stage. |
| REQ-003 c5 — "Given a scan still running past 60 seconds — including when the delay is the scanned domain's own server answering slowly — when it reaches 90 seconds, then measuring stops and the visitor is shown the report of everything measured by that point, with the rest reported as unmeasured (REQ-004). No visitor waits on a running scan beyond 90 seconds." | WO-060 | `stages/cutoff · a cut-off stage emits no completion` — with the time ceiling firing inside `asking_the_twelve`, the stream carries that stage's `done:false` and never its `done:true`, then one `ending` with `stoppedReason: 'time_ceiling'`, and the iterable closes inside the ceiling. |
| REQ-003 c11 — "… all remaining work is skipped, and the visitor is shown the report of everything measured up to that point, the skipped parts reading as not measured and the score reading as REQ-004 criterion 9 requires …" | WO-060 | `stages/cutoff · the spend ceiling ends the stream the same way` — the same assertions with `stoppedReason: 'spend_ceiling'`, and an assertion that no stage after the cut-off emits any event at all. |
| REQ-003 c4 — "Given a scan that cannot complete at all, when it ends, then the visitor is told in one written line that the measurement failed and is offered a manual retry (the retry window is REQ-001's)." | WO-060 | `stages/ending · exactly one ending, then the stream closes` — over all four `Ending` shapes: exactly one `{ending}` event is emitted, it is the last event, and pulling the iterable again yields done. A stream that emits two endings, or an ending followed by a heartbeat, fails. |
| REQ-003's non-goal — "No queue position, percentage estimate, or countdown timer." | WO-060 | `stages/shape · the stream carries no estimate` — a type-level and runtime assertion that `StageEvent` has exactly three arms and that no event carries a percentage, an ETA, a remaining-count or a position field. |
| REQ-003's non-goal — "No spend, cost or cap figure shown to a visitor." | WO-060 | `stages/shape · no cost figure on the stream` — every event emitted across the full suite is asserted to carry no cents, cost, cap or spend field, including the `ending` arm. |
| REQ-003 c1 — "Given a scan is running — however it started … when the visitor watches the page, then it shows named stages that advance as work completes, never an unlabelled spinner or an indeterminate bar alone." | WO-063 | `progress-route/stream · every stage event reaches the client, in order` — driving a fixture scan through all six stages, the SSE body carries the same sequence of `StageEvent`s the iterable produced, in order, with no event added or dropped. |
| REQ-003 c1, "never an unlabelled spinner" | WO-063 | `progress-route/stream · heartbeats pass through unaltered` — a stalled stage produces `{heartbeat:true}` events on the wire at ≥30 s intervals, each asserted to carry no stage field, and the connection stays open across them. |
| REQ-003 c4 — "Given a scan that cannot complete at all, when it ends, then the visitor is told in one written line that the measurement failed and is offered a manual retry (the retry window is REQ-001's)." | WO-063 | `progress-route/stream · the ending is the last event and the stream closes` — for each `Ending` shape, the ending is serialised once, is last, and the response ends; the client observes end-of-stream rather than a hang. |
| REQ-003 c5 — "… No visitor waits on a running scan beyond 90 seconds." | WO-063 | `progress-route/stream · the response ends inside the ceiling` — a scan stopped at the time ceiling closes the SSE response within the ceiling, asserted against a frozen clock. |
| REQ-003's non-goal — "No background continuation for a visitor who closes the page mid-scan." | WO-063 | `progress-route/stream · a disconnect cancels the iterator` — aborting the request cancels the underlying iterable, asserted by spy on its `return`. |
| REQ-003's non-goal — "No spend, cost or cap figure shown to a visitor." | WO-063 | `progress-route/stream · nothing is added on the wire` — every serialised event is asserted equal to the event the iterable produced; a source assertion that `route.ts` contains no percentage, ETA, cents or cap identifier. |

## Out of scope
- `computeScore`'s behaviour on a cut-off factor — WO-277, the third of
  ADR-021's three tests, in its own module.
- BP-007's `capHit()` itself, the ledger and the cache — another node's.
- The written lines for "stopped early" and "measurement failed" — BP-019's.
- The client behaviour that replaces the progress view with the report without a
  reload (REQ-003 c3) — BP-022's, WO-282.
- The words for each stage — BP-019's `CopyKey`s, another node's.
- Driving the stages: the pipeline that does the work is BP-012's `runScan`.
- The client that consumes the stream and swaps the progress view for the report
  (REQ-003 c3) — WO-282.
- The words for each stage — BP-019's, another node's.
- The client that consumes the stream and swaps the progress view for the report
  (REQ-003 c3) — WO-282.
- Driving the stages: the pipeline that does the work is BP-012's `runScan`,
  WO-256.

## Rollback
Delete `src/lib/scan/ceilings.ts`, `src/lib/scan/stages.ts`,
`src/app/api/scan/[scanId]/progress/route.ts` and their three suites. No schema
change, no stored data; WO-282's progress shell is the only consumer and does not
exist until after this WO.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022, BP-023 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-059, WO-060, WO-063.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-059, WO-060, WO-063 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.
- 2026-09-04 started — implementer — dispatched in parallel with WO-018, WO-277 and WO-279 under rule 4.1 (disjoint directories); branched `wo/WO-281-scan-stream` from `wo/WO-276-cost-ledger` (tip `88e951d`), not from the integration branch, per the owner's stacking instruction.
- 2026-09-04 finished — implementer — `src/lib/scan/ceilings.ts`, `src/lib/scan/stages.ts`, `src/app/api/scan/[scanId]/progress/route.ts` and their three suites (36 tests) built, fail-first watched, self-mutated per §8, full suite green (639 passed/1 skipped/32 files with `--no-file-parallelism`); ready for `/validate`.
- 2026-09-04 re-planned — planner — `risk: high` set (doctrine 0.13.2, rule 2b): money — the spend ceiling and ADR-021's no-raise-to-finish rule. Front-matter and the one-line assessment in `## Goal` are the whole change; no scope, file plan, interface, criterion or test-plan row is touched.
- 2026-09-04 failed — validator — TST-036, verdict findings: all 19 test-plan rows pass and the money seam (ADR-021) is cleanly mutation-killed (3/3), but `risk: high` covers the whole order under doctrine 0.13.2, and probing further found `stages.ts`'s live-listener halt-on-ending (`progress()`'s `while(true)` branch) can be deleted without failing any of this WO's 46 tests — every existing test publishes synchronously ahead of its own pulls, so only the replay for-loop's independent halt is ever exercised — next: implementer, add one test to `tests/scan/free/stages.test.ts` that lets a subscriber go genuinely idle before a live `emitEnding`, per this report's "What clears this." All 5 `rests-on` rows dispositioned on this WO's own front matter (2 undischargeable, 3 confirmed); none left open.
- 2026-09-04 started — implementer — dispatched to close TST-036's coverage gap; test-only change, no production file touched.
- 2026-09-04 finished — implementer — added `stages/ending · a live-published ending reaches a genuinely idle subscriber, and closes the stream (TST-036)` to `tests/scan/free/stages.test.ts`: a subscriber that waits five real macrotask ticks with nothing queued (asserted still unsettled) before `emitEnding` is called, exercising the live `while(true)` listener path rather than the replay for-loop. Watched fail first (§8): with `stages.ts` line 217's `if ("ending" in event) return;` commented out, the new case timed out at its own 3 s guard while the other 15 in the file stayed green; restored, `git diff --stat src/lib/scan/stages.ts` empty, all 37 tests across the WO's three suites pass. `npm run typecheck` and targeted `eslint` on the WO's 6 files: clean. No production file touched; ready for `/regress` / re-`/validate`.
- 2026-09-04 finished — validator — TST-038, re-validation of TST-036's one finding, verdict pass: the live-listener halt-on-ending gap is closed, independently re-mutated (M5 re-applied to `stages.ts` directly, not taken on the implementer's word) and confirmed the new test is the only one of 16 in the file to fail under it. `/regress` run in the same pass: `Regression:` line appended to TST-038 — 49 files, REQ-001/002/003/004/093 re-checked, pass.

## Validation report (appended by validator)

## TST-038 — WO-281 re-validation report

### Scope
Re-validation of TST-036's one finding, per dispatch: confirm the
live-listener halt-on-ending coverage gap (`stages.ts`'s `progress()`
`while(true)` branch, distinct from the replay for-loop) is genuinely
closed by the two identical commits on `wave/W3` — `3e04ba9` / `0eb44ff`
"test(WO-281): discriminate the live-listener halt on ending (TST-036)" —
that the new test discriminates, and that TST-036's other 19 test-plan
rows and its money-seam mutation testing (M1–M3) still hold on `wave/W3`
tip `86dc87c`. `git diff 5135f34 wave/W3 -- src/lib/scan/ src/app/api/scan/`
is empty — no production file in this WO's own file plan moved; only
`tests/scan/free/stages.test.ts` gained the one new case TST-036 named.

### The gap — closed, and the new test discriminates
Read the commit directly: one new `it` block in `stages.test.ts`,
`stages/ending · a live-published ending reaches a genuinely idle
subscriber, and closes the stream (TST-036)`. It subscribes to
`progress(scanId)` with nothing yet published, awaits five real
macrotask ticks (`setTimeout(resolve, 0)`) with the pull still asserted
unsettled — forcing the generator past `scanExists`, through the empty
replay for-loop, and into its live listener's idle `await
Promise.race(...)` — then calls `emitEnding` from outside and asserts
the following `.next()` resolves `{ ending }` and the one after that
`done: true`, each guarded by its own timeout so a regression hangs
loudly rather than silently.

Independently reproduced, not merely read (substrate quiet, `ps aux`
confirmed empty of other `vitest`):
- Baseline: `npx vitest run --project node tests/scan/free/stages.test.ts`
  — **16/16 pass** (15 from TST-036 plus the one new case).
- **M5 re-applied** — the identical mutation TST-036 itself defined: the
  live while-loop's own halt, `if ("ending" in event) return;` right
  after `yield event;` inside the `queue.length > 0` branch of
  `progress()`'s `while (true)` loop, replaced with `if (false) return;`
  — patched `src/lib/scan/stages.ts` directly, ran the file: exactly
  **1 test fails**, the new one, with the exact predicted signature
  (`Error: timed out: the stream did not close after the live ending
  (TST-036)`, its own 3 s guard). The other 15 stay green — no
  collateral. Reverted (`cp` from a pre-mutation backup), `git diff
  --stat src/lib/scan/stages.ts` confirmed empty, baseline 16/16
  restored.

The gap TST-036 named is closed and the closing test genuinely
discriminates the live-listener halt it claims to cover — not merely the
replay for-loop's independent, already-covered one.

### TST-036's other 19 rows and M1–M3 — spot-re-confirmed
`npx vitest run --project node tests/scan/free/ceilings.test.ts
tests/scan/free/stages.test.ts tests/app/scan-address/
progress-route.test.ts`: **3 files, 37 passed** (36 + the one new case).
`npm run typecheck`: clean. `src/lib/scan/ceilings.ts` and
`src/app/api/scan/[scanId]/progress/route.ts` are byte-identical to what
TST-036 already validated (`git diff` against `5135f34` empty on both),
so nothing that discriminated ADR-021's money seam (M1–M3, all killed
per TST-036) has reason to discriminate it differently now; not
independently re-mutated here. `stages.ts` carries the one addition
already covered above.

### `rests-on` disposition (rule 2.3b)
Unchanged from TST-036: all 5 of this WO's own rows are already
dispositioned on the front matter (2 `undischargeable`, 3 `confirmed`)
and this closure touches only `tests/scan/free/stages.test.ts` — no row's
grounds move. Nothing `open` remains on this WO.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict

pass

TST-036's one finding — the live-listener halt-on-ending unfailing under
deletion — is closed: the new test fails under the exact mutation that
exposed the gap (`timed out: the stream did not close after the live
ending`) and passes on the real implementation, independently
reproduced rather than taken on the implementer's or TST-036's own word.
Every other row TST-036 passed still passes on an unchanged
`ceilings.ts`/`route.ts`, and the money seam this `risk: high` order
names (ADR-021) is untouched by this change. `risk: high` — mutation
tested per doctrine 0.13.2 (see TST-036's M1–M3 and this report's M5).

Regression: 49 files · REQ-001, REQ-002, REQ-003, REQ-004, REQ-093
re-checked (union of this wave's 8-order impact sets) — pass. Full
detail in the wave-close regression note under `## Log`; no order-
specific defect found for WO-281 beyond the now-closed TST-036 gap. `npm
test` reproduced clean across 4 independent runs (49 files, 1252
passed/1 skipped each run, no flake observed); `npm run typecheck`
clean; `npm run lint` 8115 problems, matching the established baseline,
none in this WO's 6 files; `npm run build` clean via a throwaway,
deleted, never-committed `.env.local`. `factory-console impact WO-281`:
76 files → BP-001, BP-012, BP-022, BP-023 → 0 importers → REQ-001,
REQ-002, REQ-003 — re-checked against current code (full suite pass,
`ceilings.ts`/`stages.ts`/`route.ts` byte-identical since TST-036 except
the one covered addition). `factory-console --check`: 0 errors.

No `ui: yes` — no `Placement:` line owed.

## TST-036 — WO-281 validation report

### Scope
WO-281, merged to `wave/W3`. `git diff 88e951d 5135f34 --stat` (this WO's
own commit against its stacked base, WO-276): exactly 6 files, all NEW —
`src/lib/scan/ceilings.ts`, `src/lib/scan/stages.ts`, `src/app/api/scan/
[scanId]/progress/route.ts`, and their three suites — matching the file
plan exactly, 0 files outside it. Scoped to `--project node` runs only
(no `db` project, no `npm test`), per this dispatch's substrate-discipline
instruction. `risk: high — seam: money` (WO's own `## Goal`); mutation
testing owed and performed, below.

### Suite (targeted)
`npx vitest run --project node tests/scan/free/ceilings.test.ts
tests/scan/free/stages.test.ts tests/app/scan-address/
progress-route.test.ts`: **3 files, 36 passed.** `npm run typecheck`:
clean. `npx eslint src/lib/scan/ceilings.ts src/lib/scan/stages.ts
"src/app/api/scan/[scanId]/progress/route.ts" tests/scan/free/
ceilings.test.ts tests/scan/free/stages.test.ts tests/app/scan-address/
progress-route.test.ts`: clean, zero output. Working tree left clean
after every mutation (`git diff --stat` empty after each revert,
confirmed).

### Criterion-by-criterion (WO's own `## Test plan`, 19 rows)

| Criterion (source) | Test | Verdict |
|---|---|---|
| REQ-003 c5 — deadline fires at 90s whatever is blocking | `ceilings.test.ts` "the deadline fires at 90 s…" | PASS |
| REQ-003 c5 — `stopNow()` re-checked between calls | `ceilings.test.ts` "re-checked between calls…" | PASS |
| REQ-003 c11 — no call after cap fires [ADR-021] | `ceilings.test.ts` "no call is made after the cap fires" | PASS — **mutation-killed**, see M1 |
| REQ-003 c11 — a score input cut off like any other work [ADR-021] | `ceilings.test.ts` "a score input is cut off like any other work" | PASS — **mutation-killed**, see M3 |
| REQ-003 c11 — never an error/hang/refusal [ADR-021] | `ceilings.test.ts` "never an error, a hang, or a refusal" | PASS — **mutation-killed**, see M1 |
| REQ-003 c4 — failed means nothing was determined | `ceilings.test.ts` "failed means nothing was determined" | PASS |
| REQ-004 c9 — cut-off work never zero, never absent | `ceilings.test.ts` (`not_attempted` recorded, this WO's own scope is the signal only) | PASS |
| REQ-003 non-goal — no cost figure to a visitor | `ceilings.test.ts` "no cost figure leaves this module…" | PASS |
| REQ-003 c2 — hard stop absolute over a batch | `ceilings.test.ts` "the hard stop is absolute over a batch" | PASS |
| REQ-003 c1 — six named stages, in order | `stages.test.ts` "six named stages, in order…" + "advance on completed work" | PASS |
| REQ-003 c1 — never an unlabelled spinner (heartbeat) | `stages.test.ts` "a slow third party never reads as stuck…" | PASS |
| REQ-003 c5 — cut-off stage emits no completion | `stages.test.ts` "a cut-off stage emits no completion" | PASS |
| REQ-003 c11 — spend ceiling ends the stream the same way | `stages.test.ts` "the spend ceiling ends the stream the same way" | PASS |
| REQ-003 c4 — exactly one ending, then closes | `stages.test.ts` "exactly one ending, then the stream closes" | PASS as tested — **coverage gap found**, see below |
| REQ-003 non-goal — no estimate on the stream | `stages.test.ts` "the stream carries no estimate…" | PASS |
| REQ-003 non-goal — no cost figure on the stream | `stages.test.ts` "no cost figure on the stream…" | PASS |
| REQ-003 c1 — every stage event reaches the client, in order | `progress-route.test.ts` "every stage event reaches the client…" | PASS |
| REQ-003 c1 — heartbeats pass through unaltered | `progress-route.test.ts` "heartbeats pass through unaltered" | PASS |
| REQ-003 c4 — ending is last, stream closes | `progress-route.test.ts` "the ending is the last event and the stream closes" | PASS |
| REQ-003 c5 — response ends inside the ceiling | `progress-route.test.ts` "the response ends inside the ceiling" | PASS |
| REQ-003 non-goal — disconnect cancels the iterator | `progress-route.test.ts` "a disconnect cancels the iterator" | PASS |
| REQ-003 non-goal — nothing added on the wire | `progress-route.test.ts` "nothing is added on the wire" | PASS |

All 19 test-plan rows PASS as written. One coverage gap found by mutation
that no row in the table catches (below); it does not flip any row's own
verdict because the row's own claim is genuinely discriminated — the gap
is in a code path none of the rows happens to exercise.

### Mutation testing — required, `risk: high` (doctrine 0.13.2)
Every mutation applied to a tracked file, run, then reverted (`git
diff --stat` confirmed empty after each). Money-seam mutations first
(ADR-021, the WO's own stated reason for `risk: high`), then a broader
sweep into `stages.ts`'s stream-closing guarantee.

- **M1 — delete the spend-ceiling check** (`if (bounds.capHit()) return
  "spend_ceiling"` → `if (false) return "spend_ceiling"`, in `makeBounds`'s
  `stopNow()`): **4/10 `ceilings.test.ts` cases fail** — "no call is made
  after the cap fires," "a score input is cut off," and both spend-ceiling
  scenarios in "never an error, a hang, or a refusal" (which regress to
  `no_report` rather than `report`, since nothing stops the throwing body
  and no ceiling ever fires to force the `report` ending). **Killed.**
- **M2 — invert the ceiling tie-break** (time-then-spend →
  spend-then-time, in the same `stopNow()`): breaks exactly "when both
  ceilings have fired, time wins," the row that names ADR-021/REQ-003 c5's
  own absolute bound. **Killed.**
- **M3 — add an optional third parameter** to `withFreeBounds`
  (`override?: boolean`, an unused param): breaks exactly "a score input
  is cut off like any other work," via `withFreeBounds.length === 2`, the
  arity assertion ADR-021's ceiling-test-plan row names by design.
  **Killed.** Confirms the arity check is not decorative — a real,
  reachable escape hatch is caught the moment it is added, even one that
  does nothing.

**Coverage gap found (not one of the three named M1–M3, found by probing
the "exactly one ending, then the stream closes" row further, since that
row is REQ-003 c4's own criterion and this WO is `risk: high` end to end,
not only on its money half):**

- **M4 — remove `publish()`'s `if (stream.done) return` guard** in
  `stages.ts` (the producer-side "no event of any kind after the ending"
  check): **survived** — 15/15 `stages.test.ts` still pass. Traced why:
  every test in this file that calls `emitEnding` does so synchronously,
  in the same tick as the pull that observes it, so the generator is
  always still draining its own *replay* for-loop (`for (const event of
  stream.events) { yield event; if ("ending" in event) return; }`, which
  M4 does not touch) rather than the live while-loop section M4 mutates.
  That for-loop's own, separate `return` independently guarantees the
  same observable property, so the customer-facing contract holds and no
  test in the suite is wrong to pass — but `publish()`'s own guard is,
  as far as this suite can see, dead code.
- **M5 — remove the live while-loop's own halt-on-ending** (`if ("ending"
  in event) return;` right after `yield event;`, inside the
  `queue.length > 0` branch of `progress()`'s `while (true)` loop):
  **survived against every existing test in `stages.test.ts` and
  `progress-route.test.ts`** — same root cause as M4: no existing test
  ever lets the generator reach a genuinely idle `await Promise.race(…)`
  state *before* the ending is published; every test either bursts
  producer calls synchronously ahead of its pulls (replay path) or mocks
  `progress()` wholesale (`progress-route.test.ts`, which never touches
  `stages.ts`'s own internals). Built an independent probe (not part of
  this WO's files, discarded after use) that subscribes first, awaits
  three real macrotask ticks with nothing queued (forcing the generator to
  drain the empty replay loop, find `stream.done` false, and register a
  live listener), *then* publishes — confirmed by a companion probe with
  an unconditional `throw` in the same branch that the while-loop's
  `queue.length > 0` branch is genuinely unreached by any of this WO's own
  46 tests. Against that genuinely-live probe, **M5 hangs to a 10s
  timeout** — confirmed live delivery does depend on this line; it is
  real, load-bearing code, not dead.

**Judgement:** this is a real coverage gap, not a vacuous-test finding —
the property REQ-003 c4 states ("exactly one ending, then the stream
closes") is correctly guaranteed by the code today (verified by the
targeted probe above), but the *test suite* only ever exercises it through
one of two independent enforcement paths (the replay for-loop), leaving
the second (the live listener path, which is the one that matters for a
real visitor: a genuinely-connected SSE client watching a scan run over
real wall-clock time, not a same-tick unit test) unfailing under a
deletion of its own code. That is exactly the shape constitution §8's
mutation-testing bar exists to catch. It is scoped to `stages.ts` alone —
`ceilings.ts` (the money seam this WO's own `risk: high` framing names) is
cleanly mutation-tested (M1–M3, all killed) — but the WO as merged
carries no test that fails if a driver's live-published ending event, on
a genuinely-idle subscriber, silently resumes emitting instead of closing
the stream.

### `rests-on` disposition (rule 2.3b)
All 5 rows were `open` on this WO's own front matter; none were
self-dispositioned by the implementer (correct — contrast WO-278's
process note, above). Dispositioned here, in the front matter directly,
against direct observation of the current tree:

1. "Free battery completes within 60s at p95 vendor latency; `BUILD.md`
   §16 milestone 3 is the first measurement" — **undischargeable**. A
   claim about real vendor response-time distribution; no test run in
   this sandbox (or any sandbox) observes real vendor latency. The WO's
   own test plan says the same for its batch test (fixture latencies
   only). No build discharges this; it is milestone 3's alone.
2. "`withCostContext`/`capHit()` exists, bound to `CAPS.FREE_C` (12¢)" —
   **confirmed**. Read directly: `src/lib/costs/index.ts` exports
   `withCostContext`; `CAPS.FREE_C: 12` in `src/lib/config/constants.ts`;
   `ceilings.ts` line 194 calls `withCostContext({ …, cap: "FREE", … })`,
   and `costs/index.ts` line 36 maps `FREE: CAPS.FREE_C`.
3. "BP-005 pins `TIMING.reportTargetS: 60`, `reportCeilingS: 90`,
   `CAPS.FREE_C: 12`" — **confirmed**. Read directly:
   `src/lib/config/constants.ts` line 68 has all three; `ceilings.ts`
   imports `TIMING` and declares no numeric literal of its own for these.
4. "BP-005 pins `TIMING.progressHeartbeatS: 30`" — **confirmed**. Same
   file, same line; `stages.ts` imports it, declares none.
5. "The deployment platform does not buffer/terminate SSE inside the
   heartbeat interval; `BUILD.md` §16 milestone 3 is the first run that
   tests it" — **undischargeable**. A platform/infrastructure property;
   this sandbox has no deployed platform to observe it against. Explicitly
   named as milestone 3's in the row's own text.

None left `open`. Gate condition met — 2 `undischargeable` (both
explicitly named as future-milestone claims no build can discharge, per
2.3a), 3 `confirmed` by direct read of the merged code.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict

findings

All 19 test-plan rows independently reproduced and pass; scope is exactly
the file plan, 6/6 files, no drift. The money seam this WO declares
`risk: high` for is cleanly mutation-tested: three independent mutations
against ADR-021's guarantees (deleting the spend check, inverting the
ceiling tie-break, adding an unused override parameter) are each killed by
name, by a specific row. That much is a clean pass on its own.

What keeps this at `findings` rather than `pass`: `risk: high` on this WO
is not scoped to the money seam alone in the doctrine's own terms (0.13.2
requires mutation testing across a high-risk order's criteria, not only
the seam named in its `## Goal`), and probing REQ-003 c4's own "exactly
one ending, then the stream closes" row further found a genuine,
non-vacuous gap: `stages.ts`'s live-listener halt-on-ending
(`progress()`'s `while(true)` branch, not its replay for-loop) can be
deleted without failing any of this WO's 46 tests, because every existing
test publishes synchronously ahead of its own pulls and never lets a
subscriber go genuinely idle before an ending arrives live. Verified with
an independent, non-committed probe that this is real production
behaviour (a real visitor watching a real scan run over wall-clock time
would hit exactly this path) and not merely an artifact of my probe's own
construction (confirmed by a second probe with an unconditional `throw` in
the same branch, proving it unreached by the shipped suite).

**What clears this:** one test in `tests/scan/free/stages.test.ts` that
subscribes to `progress(scanId)` with nothing yet published, awaits until
the generator has genuinely entered its idle wait (not merely called a
producer function synchronously beforehand), *then* calls `emitEnding`
from outside, and asserts the following `.next()` resolves `done: true`
rather than hanging or yielding a further event. This is an
implementer-scoped addition to an existing file in this WO's own file
plan, not a re-plan; `ceilings.ts` and `route.ts` need no change.

Regression not run — held for `/regress`, per this dispatch's
substrate-discipline instruction (no `npm test`, no `db` project this
session).

No `ui: yes` — no `Placement:` line owed.

- 2026-09-04 finished — librarian — done audit: gate list all clauses met — TST-038 pass verdict with Regression line, 5 rests-on rows (2 undischargeable, 3 confirmed) non-open, ui: no (no placement owed), blocked-by: [], commits name WO-281.
