---
id: WO-080
type: work-order
title: "checkCoherence and coherenceThreshold — a three-valued verdict against a rescaling threshold"
status: approved
ui: no
implements: [BP-028, BP-011, REQ-094]
depends-on: [WO-006, WO-077]
blocked-by: []
rests-on:
  - claim: "`COHERENCE: { minMeasuredSearches: 3; shareDivisor: 4; minAppearances: 2 }` is declared in BP-005's `## Public interface` — verbatim, `export const COHERENCE: { minMeasuredSearches: 3; shareDivisor: 4; minAppearances: 2 }`, under the header 'Added 2026-08-31 (decision 3)' and traced there to 'BP-028 · REQ-094 c2 · BUILD §6.7 step 5'. Confirmed 2026-08-31: BP-028's `## NFR budget` note that it was 'not yet listed in BP-005's interface, reported to the parent' is spent and BP-028 now records the pin as declared. `src/lib/config/constants.ts` is BP-005's file and is not touched here; if the block is absent from that file when this WO starts, that is a `blocked-by` on BP-005's work order, added at that moment."
    disposition: confirmed
supersedes: []
---

# WO-080 — checkCoherence and coherenceThreshold — a three-valued verdict against a rescaling threshold

- Branch: wo/WO-080-coherence-check
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/coherence/check.ts` so a report's market is judged coherent,
incoherent, or unjudgeable — against a threshold that is a share of what was measured, never
an absolute count, and never the whole number measured.

## Context an implementer needs
No source code exists on disk; both paths below are NEW. The arithmetic is
`max(2, ceil(n / 4))` with both operands pinned in BP-005 (`minAppearances: 2`,
`shareDivisor: 4`) — BP-028 decision 1: "The threshold is a pinned expression, not a pinned
number." BUILD §6.7 step 5's literal "no domain appears in ≥3 of the 12 top-10s" is the
value of that expression at n = 12 only; REQ-006 criterion 1 makes a market that yields
fewer than twelve a legal *complete* result, so the denominator is variable.

The parent/leaf divergence this WO recorded is **resolved**. When it was cut, BP-011's
`## Public interface` declared the two failing arms bare (`{ verdict: 'incoherent' }`,
`{ verdict: 'unjudgeable' }`) while BP-028's declared them carrying `threshold`/`best` and
`measuredCount`, and this WO implemented BP-028's. BP-011 was corrected in-file on 2026-08-31
and now carries the same three-arm shape, with the reason written beside it: "This section
declared a bare three-arm union at approval; BP-028, the leaf cut from REQ-094, ships the
shape below, and one symbol in `src/lib/market/coherence/check.ts` cannot carry two." Parent
and leaf now read alike; the shape implemented is unchanged.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/coherence/check.ts` | **NEW** | `CoherenceVerdict`, `checkCoherence` and `coherenceThreshold`, exactly as BP-028's `## Public interface` states them. Pure: no I/O, no clock, no `CostContext`. |
| `tests/market/coherence/check.test.ts` | **NEW** | The tests named in the test plan, including the property test over denominators. |

## Interfaces
**Consumes**: `SerpResult` (BP-008), `registrableDomain` (WO-077 — one domain
normalisation in the product, not two), `COHERENCE` from `src/lib/config/constants.ts` (BP-005).

**Exposes** — copied exactly from BP-028's `## Public interface`:

```ts
// src/lib/market/coherence/check.ts
export type CoherenceVerdict =
  | { verdict: 'coherent' }
  | { verdict: 'incoherent'; threshold: number; best: number }
  | { verdict: 'unjudgeable'; measuredCount: number }
export function checkCoherence(a: { serps: SerpResult[]; measuredCount: number }): CoherenceVerdict
/** max(2, ceil(n / 4)) — a share of what was measured, never an absolute count. */
export function coherenceThreshold(measuredCount: number): number
```

## Steps
1. `coherenceThreshold(n)` = `Math.max(COHERENCE.minAppearances, Math.ceil(n / COHERENCE.shareDivisor))`.
   Neither `2` nor `4` appears as a literal in this file (`structure.md` rule 5).
2. `checkCoherence`: where `measuredCount < COHERENCE.minMeasuredSearches`, return
   `{ verdict: 'unjudgeable', measuredCount }` **before** counting anything — with fewer than
   three measured top-tens no domain can reach the threshold whatever they contain, so a
   coherent and an incoherent market would produce the identical result.
3. Otherwise count, per registrable domain, in how many of the supplied SERPs' top tens it
   appears (once per SERP). `best` = the highest such count, `0` where no domain appears.
4. `best >= coherenceThreshold(measuredCount)` ⇒ `{ verdict: 'coherent' }`. Otherwise
   `{ verdict: 'incoherent', threshold, best }`.
5. Emit the observability fields BP-028 names: verdict, threshold, best appearance count.
6. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-094) | Test |
|---|---|
| 2. "Given a report that measured three or more searches, when it renders, then one written line states that the market may have been read wrongly and points at criterion 1's control, exactly when no single domain appears in the top ten of as many of those measured searches as the threshold — a quarter of the searches the report measured, rounded up, or two, whichever is the greater. That threshold is three where twelve searches were measured, and is never the whole number measured, so the same share of the market is asked for at every denominator (REQ-006 criterion 1). Given a report that measured fewer than three searches — which a market that yielded that few (REQ-006 criterion 1) or a scan that stopped early (REQ-001 criterion 14) can produce — when it renders, then no line says the market may have been read wrongly, and one written line instead says that coherence could not be judged and points at that same control." — the verdict limb; both written lines are BP-019's and the report surface's. | `tests/market/coherence/check.test.ts › threshold/is-a-share-not-a-count` — `coherenceThreshold(12) === 3`, `coherenceThreshold(4) === 2`, `coherenceThreshold(3) === 2`, `coherenceThreshold(20) === 5`; plus a property test over n = 3…200 asserting `coherenceThreshold(n) >= 2` and `coherenceThreshold(n) < n` — "never the whole number measured". `check/incoherent-exactly-when-best-is-below-threshold` — at n = 12, a best of 2 returns `{ verdict: 'incoherent', threshold: 3, best: 2 }` and a best of 3 returns `{ verdict: 'coherent' }`. `check/fewer-than-three-is-unjudgeable` — at `measuredCount` 0, 1 and 2 the result is `{ verdict: 'unjudgeable', measuredCount }` whatever the SERPs contain, including SERPs in which one domain appears in every top ten. |

Discrimination: replacing the expression with the literal 3 must fail
`threshold/is-a-share-not-a-count` at n = 4; removing the `unjudgeable` guard must fail
`check/fewer-than-three-is-unjudgeable`; letting the threshold reach n must fail the property test.

Purity, from BP-028's `## NFR budget` ("**0¢ and no I/O**"):
`check/is-pure` — `check.ts` resolves no import into `src/lib/costs/`, `src/lib/vendors/`,
`src/lib/llm/`, `src/lib/db/` or `src/lib/scan/`, takes no `CostContext` and reads no clock.

## Out of scope
- The two written lines criterion 2 requires — BP-019's copy registry, rendered by the
  report surface (BP-022).
- The correction control, its offer and its state — WO-081, WO-082.
- Running the re-measurement — BP-012's `runScan({ correctionOf })`.
- REQ-094 criteria 1 and 3–7.

## Rollback
Delete both files; nothing imports them.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-028 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 1 row of `## Test plan` quote REQ-094 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — BP-005 verified to declare `COHERENCE` in its own interface, so the pin row is `confirmed`; BP-011 now carries BP-028's three-arm `CoherenceVerdict` and the divergence note becomes a resolution note
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-006, WO-077); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 finished — planner — implements gains BP-011 (/workorder BP-011): this order's file plan creates the file that exports `CoherenceVerdict and checkCoherence`, which BP-011's ## Public interface declares and the leaf ships identically — capabilities.md: "the component's declaration is the contract and the leaf is the implementation"; no other field changes.
