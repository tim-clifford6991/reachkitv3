---
id: WO-137
type: work-order
title: "The account card and the email change — the new address proves itself before the old one stops working"
status: approved
ui: no
implements: [BP-061, REQ-077]
depends-on: [WO-006, WO-267, WO-106, WO-135, WO-136]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-137 — The account card and the email change — the new address proves itself before the old one stops working

- Branch: wo/WO-137-email-change
- Estimate: ≤1 day

## Goal
Move an account's sign-in address safely: hold the change pending until the new
address redeems its link, then switch, end the other sessions and write once to
the old address — and let the customer see, cancel or replace the pending change
until it lapses.

## File plan
> **Every path below is NEW except `links.ts`.** No `src/` exists; `links.ts` is
> created by WO-136 and modified here, which is why these two work orders are
> sequential and not parallel. Anchored to `registry/structure.md` and BP-061's
> `code:` glob `src/lib/account/session/**`. Provisional against a repo with no
> code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/account/session/email-change.ts` | NEW | Exports `accountCard`, `beginEmailChange` and `cancelEmailChange` with BP-061's signatures verbatim. `beginEmailChange` writes **only** the three pending columns and issues an `email_change` link to the new address; `users.email` is not touched. The in-use check reads `users.email` and `users.pending_email` together **through `dbAdmin()`**, so a soft-deleted account inside its 30-day window still refuses (BP-061's error behavior; ADR-051 point 5 — "the row is present but hidden"). Validity is a syntactic check plus a deliverability attempt: no MX probe, no disposable-domain list. `cancelEmailChange` clears all three columns atomically and spends any live `email_change` token. `accountCard` returns `noteKeys` verbatim and **never** returns an invoice address (REQ-076 c2). |
| `src/lib/account/session/links.ts` | MODIFY | Add the `email_change` completion branch to `redeemLink`'s existing transaction: set `users.email` to `pending_email`, clear the three pending columns, mark every unspent token for that user spent, end all other sessions for that user, create a session at the new address, and send one `account` mail to the **old** address. The change stands even if that mail fails, and the failure is raised (BP-061: "reverting an address the customer has already proved would lock them out of the account they just moved"). No other line of `links.ts` changes. |
| `tests/account/session/email-change.test.ts` | NEW | Begin writes only the pending columns and leaves `users.email` intact; the old address still signs in while a change is pending; `in_use` for an existing account, for a pending address held by another customer, and for a soft-deleted account's address; `invalid` for a syntactically bad address; both refusals send no link and change nothing; cancel clears the pending state and leaves the account unchanged; a change not redeemed within `EMAIL_CHANGE_TTL_H` lapses and the account is unchanged; `accountCard` returns the pending address and its expiry, and returns no invoice address on any path. |
| `tests/account/session/email-change-complete.test.ts` | NEW | Redemption switches the address; **only** the new address can sign in afterwards and the old one cannot; every other session for that user is ended; exactly one `account` mail goes to the **old** address; a failing mail leaves the change standing and raises. The mutation case: deleting the other-sessions termination fails `› every other session is ended`. |

## Interfaces
Exposes (verbatim from BP-061's `## Public interface`):
```ts
export function accountCard(userId: string): Promise<{
  name: string | null
  email: string
  pending: { email: string; expiresAt: Date } | null
  noteKeys: readonly ['account.magic_link_note', 'account.invoices_go_to_billing']
}>

export function beginEmailChange(userId: string, next: string): Promise<
  | { ok: true; expiresAt: Date }
  | { ok: false; reason: 'in_use';  lineKey: 'account.email_in_use' }
  | { ok: false; reason: 'invalid'; lineKey: 'account.email_invalid' }
>

export function cancelEmailChange(userId: string): Promise<void>
```
Consumes: `issueLink` and `redeemLink` (WO-136, BP-061's signatures);
`sendEmail` (BP-016) with `'account': { occasionsFrom: 'REQ-024, REQ-074, REQ-076, REQ-077, REQ-079', stoppable: false }`;
`dbAdmin()` (BP-002); BP-005's `EMAIL_CHANGE_TTL_H: 24`.

## Steps
1. Write `tests/account/session/email-change.test.ts` and watch every case fail.
2. Write `email-change.ts`.
3. Modify `redeemLink`'s transaction to add the completion branch.
4. Write `email-change-complete.test.ts`, including the old-address-cannot-sign-in
   case, which is the one that discriminates c3.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-077 c1 — "Given a customer on Settings, when they open the account card, then their name and the address they sign in with are shown, alongside one written line saying ReachKit sends a sign-in link rather than using a password, and one saying invoices and receipts go to the address held in the billing portal and are changed there (REQ-076 criterion 2), not here." | `tests/account/session/email-change.test.ts › accountCard returns name, email and the two note keys`, `› accountCard returns no invoice address on any path` |
| REQ-077 c2 — "Given the customer submits a new email address, when that address is not already a ReachKit account and is a valid address, then a sign-in link is sent to it and the old address keeps working until that link is used; when it is already an account or is not a valid address, then no link is sent, nothing changes, and one written line says why." | `tests/account/session/email-change.test.ts › a valid new address is sent a link and the old address still signs in`, `› an address already in use refuses with account.email_in_use and sends no link`, `› an invalid address refuses with account.email_invalid and changes nothing` |
| REQ-077 c3 — "Given the link sent to the new address is used, when it succeeds, then only the new address can sign in, every mail ReachKit itself sends goes to it, and one `account` mail (REQ-064) goes to the old address saying the account now signs in at a different address and this one no longer can." | `tests/account/session/email-change-complete.test.ts › after redemption only the new address signs in`, `› exactly one account mail goes to the old address`, `› every other session for that user is ended` |
| REQ-077 c4 — "Given a change is pending, when the customer opens the account card, then it shows the address awaiting confirmation and lets them cancel the pending change or submit a different address; and the link expires 24 hours after it is sent, after which the pending change lapses and the account is unchanged." | `tests/account/session/email-change.test.ts › accountCard shows the pending address and its expiry`, `› cancel clears the pending state and leaves the account unchanged`, `› a second submission replaces the pending address`, `› a change unredeemed at 24 hours lapses and the account is unchanged` |

## Out of scope
- The Settings account card surface and every sentence behind
  `account.magic_link_note`, `account.invoices_go_to_billing`,
  `account.email_in_use` and `account.email_invalid` — BP-001's surface and
  BP-019's registry; the owner's words. This work order names keys and writes no
  copy.
- Changing the address Stripe invoices go to — the portal, WO-132.
- Changing the site's domain — BP-056, a different key entirely (REQ-077
  non-goal).
- Account recovery when both addresses are inaccessible (REQ-077 non-goal).

## Rollback
Delete `email-change.ts` and both test files, and revert the one branch added to
`links.ts`. Addresses already changed stay changed — the customer proved the new
one, and reverting it would be the lock-out this requirement exists to prevent.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-061 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan` quote REQ-077 as it stands on disk.
- **Bounded.** 4 files in the file plan (3 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-006, WO-011, WO-106); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267

## Validation report (appended by validator)
