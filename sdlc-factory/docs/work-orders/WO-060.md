---
id: WO-060
type: work-order
title: "stages.ts — the six named stages, the heartbeat, and exactly one ending on the stream"
status: approved
ui: no
implements: [BP-023, REQ-003]
depends-on: [WO-006, WO-267, WO-056, WO-059]
blocked-by: []
rests-on:
  - claim: "BP-005 pins `TIMING.progressHeartbeatS: 30`. `structure.md` rule 5 puts every number in `constants.ts`; this WO imports it and declares none"
    disposition: open
supersedes: []
---

# WO-060 — `stages.ts` — the six named stages, the heartbeat, and exactly one ending on the stream

- Branch: wo/WO-060-stages
- Estimate: ≤1 day

## Goal
Name every stage a running free scan advances through, emit them as work
completes rather than on a timer, keep the stream alive while a third party is
slow, and close it after exactly one ending.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md`; `src/lib/scan/stages.ts` is BP-023's file inside
> BP-012's container. Provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/scan/stages.ts` | NEW | `StageName`, `STAGES`, `StageEvent`, `progress`. Six handles, one per dataset boundary of `BUILD.md` §6.3's free-scan list (BP-023 decision 7), ordered. The stream emits `{stage, done:false}` on entry and `{stage, done:true}` on exit, a `heartbeat` at least every `TIMING.progressHeartbeatS`, and exactly one `ending` before closing. A stage the ceilings cut off emits no `done:true`. No word a visitor reads appears in this file — every handle is internal. |
| `tests/scan/free/stages.test.ts` | NEW | The ordering, heartbeat, cut-off and single-ending suites named in the test plan. |

## Interfaces

Consumes: WO-059's `Ending`; WO-056's `scans.status` including `failed`;
BP-002's `dbAdmin()`; BP-005's `TIMING.progressHeartbeatS`.

Exposes (verbatim from BP-023 `## Public interface`, `src/lib/scan/stages.ts`):

```ts
/** Internal handles. Every word a visitor reads is BP-019's (REQ-093 c1). */
export type StageName =
  | 'reading_your_site'        // own fetches: home + detected pricing page
  | 'reading_access_rules'     // robots.txt and the home document's reader rules
  | 'reading_your_market'      // profile + keyword_suggestions → market set + the 12 questions
  | 'checking_your_presence'   // ranked_keywords@50
  | 'asking_the_twelve'        // 12 live organic SERPs, with their AI overviews
  | 'scoring'                  // drivers, score, band, problems, first page
export const STAGES: readonly StageName[]      // exactly the six above, in order

export type StageEvent =
  | { stage: StageName; done: boolean }
  | { heartbeat: true }                                   // ≥ every 30 s
  | { ending: Ending }                                    // terminal; the stream then closes
export function progress(scanId: string): AsyncIterable<StageEvent>
```

`progress` is the narrowing of BP-012's declared
`progress(scanId: string): AsyncIterable<{ stage: StageName; done: boolean }>` —
BP-023's union adds the heartbeat and the ending arms. That refinement is
BP-023's contract and is not re-decided here.

## Steps
1. Write the failing suites first.
2. Declare `StageName` and freeze `STAGES` in order. Assert at module load that
   `STAGES.length === 6` and that every `StageName` member appears exactly once.
3. Implement `progress(scanId)` as an async iterable over the scan's recorded
   stage transitions, emitting entry and exit events per stage.
4. Add the heartbeat: if no event has been emitted for `TIMING.progressHeartbeatS`,
   emit `{ heartbeat: true }`. The heartbeat never advances a stage and never
   carries a stage name.
5. Emit exactly one `{ ending }` and then close the iterable. After the ending,
   the iterable is done — no further event of any kind.
6. On a stage the ceilings cut off, emit no `done: true` for it; the outputs are
   recorded `unmeasured / not_attempted` by the pipeline (WO-052's arm), never 0
   and never simply absent.
7. Emit the one structured log line per stage transition BP-023's NFR budget
   specifies.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-003.md`; line breaks are
> normalised to fit the table and no word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-003 c1 — "Given a scan is running — however it started: a submission on the landing page, a first visit to a shared report address, a re-scan a visitor started from a stored report (REQ-001 criteria 14 and 15), or a re-measurement after a market correction — when the visitor watches the page, then it shows named stages that advance as work completes, never an unlabelled spinner or an indeterminate bar alone." | `stages/order · six named stages, in order, one per dataset boundary` — `STAGES` equals the six handles above in that order; a scan driven through the pipeline emits `{stage, done:false}` then `{stage, done:true}` for each, in order, with no stage repeated and none skipped. And `stages/advance · a stage advances on completed work, not on a clock` — with the clock advanced arbitrarily and no work completing, no `done:true` is emitted; only a completed unit of work produces one. |
| REQ-003 c1, "never an unlabelled spinner or an indeterminate bar alone" | `stages/heartbeat · a slow third party never reads as stuck, and never as progress` — with a stage stalled 95 s, at least three `{heartbeat:true}` events are emitted at ≥30 s intervals, and every one of them is asserted to carry no `stage` field, so a heartbeat cannot be rendered as an advancing stage. |
| REQ-003 c5 — "Given a scan still running past 60 seconds — including when the delay is the scanned domain's own server answering slowly — when it reaches 90 seconds, then measuring stops and the visitor is shown the report of everything measured by that point, with the rest reported as unmeasured (REQ-004). No visitor waits on a running scan beyond 90 seconds." | `stages/cutoff · a cut-off stage emits no completion` — with the time ceiling firing inside `asking_the_twelve`, the stream carries that stage's `done:false` and never its `done:true`, then one `ending` with `stoppedReason: 'time_ceiling'`, and the iterable closes inside the ceiling. |
| REQ-003 c11 — "… all remaining work is skipped, and the visitor is shown the report of everything measured up to that point, the skipped parts reading as not measured and the score reading as REQ-004 criterion 9 requires …" | `stages/cutoff · the spend ceiling ends the stream the same way` — the same assertions with `stoppedReason: 'spend_ceiling'`, and an assertion that no stage after the cut-off emits any event at all. |
| REQ-003 c4 — "Given a scan that cannot complete at all, when it ends, then the visitor is told in one written line that the measurement failed and is offered a manual retry (the retry window is REQ-001's)." | `stages/ending · exactly one ending, then the stream closes` — over all four `Ending` shapes: exactly one `{ending}` event is emitted, it is the last event, and pulling the iterable again yields done. A stream that emits two endings, or an ending followed by a heartbeat, fails. |
| REQ-003's non-goal — "No queue position, percentage estimate, or countdown timer." | `stages/shape · the stream carries no estimate` — a type-level and runtime assertion that `StageEvent` has exactly three arms and that no event carries a percentage, an ETA, a remaining-count or a position field. |
| REQ-003's non-goal — "No spend, cost or cap figure shown to a visitor." | `stages/shape · no cost figure on the stream` — every event emitted across the full suite is asserted to carry no cents, cost, cap or spend field, including the `ending` arm. |

## Out of scope
- The SSE transport — WO-063, the route handler that serves this iterable.
- The client behaviour that replaces the progress view with the report without a
  reload (REQ-003 c3) — BP-022's, WO-069.
- The words for each stage — BP-019's `CopyKey`s, another node's.
- Driving the stages: the pipeline that does the work is BP-012's `runScan`.

## Rollback
Delete both files. WO-063 is the only consumer and does not exist yet.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-023 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 7 rows of `## Test plan` quote REQ-003 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-006, WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267

## Validation report (appended by validator)
