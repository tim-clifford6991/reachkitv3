---
id: WO-062
type: work-order
title: "POST /api/scan — the canonicaliser and starter, with a no-JavaScript path"
status: approved
ui: no
implements: [BP-022, REQ-001, REQ-003]
depends-on: [WO-051, WO-058]
blocked-by: []
rests-on:
  - claim: "`src/middleware.ts` lists every path in this node on its public allow-list, so no session is read on this route. The middleware is BP-001's; this repo carries no `src/` today"
    disposition: confirmed
    reason: "TST-024 (validator): independently confirmed no `src/middleware.ts` exists anywhere in this repo (`find` returns nothing). WO-003 (`status: approved`, depends-on WO-001/WO-002 only) is the sole owner of that path and its own step 1 derives `PUBLIC_PATHS` from BP-001's public route list, which already names `POST /api/scan` verbatim (BP-001 `## Public interface`, line 73). The claim is therefore genuinely dischargeable, just not by this build — there is nothing on disk yet to observe it against. Left `open` deliberately (constitution §validator step 4a): this is the stage that can first observe it (WO-003's own validator, or a later `/regress` on WO-062 once WO-003 lands), not this one. Blocks WO-062's own `done` until then — expected and correct, not an oversight. — TST-025 (WO-003's validator, 2026-09-03): dispositioned `confirmed`, not merely because `src/middleware.ts` now exists but because its built content discharges the claim's actual words. `PUBLIC_PATHS` in `src/middleware.ts` lists `\"/api/scan\"` verbatim (an exact-segment match, not a prefix), and `middleware()`'s `isPublic()` check runs before `hasSession()` and short-circuits it — a request never reaches the session check when the path is on the allow-list, so 'no session is read on this route' holds for the method Next's matcher actually sees (path only, not verb). Reproduced directly: `middleware(new NextRequest(new URL('/api/scan', 'http://localhost'), { method: 'POST' }))` returns a non-redirect (`NextResponse.next()`) with no session cookie supplied — independently run, not taken from the implementer's report — and `grep -n 'session\\|cookie\\|auth' src/app/api/scan/route.ts` (WO-062's own route) returns nothing, confirming the route itself reads no session either. Mutation-tested: deleting `\"/api/scan\"` from `PUBLIC_PATHS` turns this exact request into a 307 redirect, breaking two independent tests in `tests/app/middleware.test.ts` (the allow-list enumeration bijection and the route's own dedicated test) — so the claim is discriminated, not merely plausible."
supersedes: []
wave: W2
---

# WO-062 — `POST /api/scan` — the canonicaliser and starter, with a no-JavaScript path

- Branch: wo/WO-062-api-scan
- Estimate: ≤1 day

## Goal
Turn one submitted value into the canonical report address and, where admission
allows, one claimed scan — responding with a location the client navigates to,
and never with a rendered report.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md` (`src/app/api/**` is BP-001's container and every
> file in it is transport-only, rule 1; BP-022 anchors this route). Provisional
> against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/app/api/scan/route.ts` | NEW | The `POST` handler and `StartScanResponse`. A thin adapter: parse the body, call WO-051's `parseDomain`, call WO-057's `networkKeyOf` over `x-forwarded-for`, call WO-058's `claimFreeScanSlot`, and return a location. It contains no admission logic, no SQL and no rendering. Content negotiation: a JSON request gets `StartScanResponse` (422 on a malformed value); a form submission with no JavaScript gets 303 to the canonical address on success and 303 back to the landing page carrying the problem handle and the submitted value on failure. |
| `tests/app/scan-address/api-scan.test.ts` | NEW | The response, idempotence, no-JS and no-session suites named in the test plan. |

## Interfaces

Consumes: WO-051's `parseDomain` and `DomainProblem`; WO-057's `networkKeyOf`;
WO-058's `claimFreeScanSlot`.

Exposes (verbatim from BP-022 `## Public interface`):

```
Routes
  POST /api/scan                            { value: string } — start, or name what is wrong
```

```ts
// src/app/api/scan/route.ts
export type StartScanResponse =
  | { ok: true;  location: string; scanId: string }
  | { ok: false; problem: DomainProblem }                                // REQ-001 c3, HTTP 422
```

## Steps
1. Write the failing suite first.
2. Parse and validate the request body to `{ value: string }`; anything else is a
   400 with no problem handle, since it is not a visitor's typed value.
3. `parseDomain(value)`. On failure respond 422 with `{ ok: false, problem }`
   (JSON) or 303 back to the landing page with the handle and the value
   preserved (form).
4. Derive the network key from `x-forwarded-for`'s first entry and claim the
   slot. On a claim, respond `{ ok: true, location, scanId }` (JSON) or 303 to
   `location` (form).
5. On a refusal, respond `{ ok: true, location, scanId: '' }`? **No** — respond
   with the canonical location and no scan id: the refusal is shown at the report
   address on load, by WO-061's resolver and WO-069's view (REQ-003 c12). Model
   this as `{ ok: true, location }` with `scanId` present only when a scan was
   claimed, and keep the union shape BP-022 declares by returning the running
   scan's id where admission returned `in_flight` with `sameDomain: true`.
6. Make the post idempotent on the canonical domain: a double mount, a refresh
   mid-scan and two tabs all join the one running scan through
   `claimFreeScanSlot`'s in-flight arm rather than starting a second.
7. Read no session and set no cookie anywhere in the handler.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-001.md` and
> `requirements/REQ-003.md`; line breaks are normalised to fit the table and no
> word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-001 c2 — "Given a visitor who enters a domain in any of its written forms — with or without a scheme, with or without `www`, with a trailing path, in any letter case — when they submit, then they arrive at the single public report address for that domain; no two of those forms produce different addresses or different stored reports." | `api/scan · every written form posts to one location` — posting each written form of one domain returns the identical `location`, and the location is the canonical `/scan/{domain}` address. |
| REQ-001 c3 — "Given a visitor who submits a value that is not a well-formed public domain name — an empty value, an IP literal, or a hostname with no public suffix — when they submit, then one written line names what is wrong with the value and the visitor stays on the landing page with their input intact. A well-formed domain is accepted here whether or not it resolves; a domain that cannot be reached is a failed scan (REQ-003), not a rejected input." | `api/scan · a malformed value is 422 with its handle` — each of the three named cases returns HTTP 422 and `{ ok: false, problem }` with the matching `DomainProblem`. And `api/scan · without JavaScript the value survives` — the form-encoded equivalent returns 303 to the landing page with the problem handle and the submitted value carried, so the landing page can re-render in place with the input intact (the render is WO-070's). And `api/scan · a domain that does not resolve is accepted` — a well-formed unresolvable domain claims a scan and returns a location; asserted by spy that no DNS call was made in this handler. |
| REQ-001 c9 — "Given a well-formed domain that has no stored report, is not in a failure cooldown, and whose report was not removed at its owner's request, when its report address is first reached — by a submission on the landing page, or by opening that address directly … then a scan for that domain is already underway with no further action from the visitor …" | `api/scan · a clean domain is underway on return` — the response carries a `scanId` and a `running` `scans` row exists for it before the response is sent. |
| REQ-001 c10 — "Given a visitor for whom a scan started under criterion 9, when they wait through it and read the report, then nothing is asked of them at any point between the domain they gave and the report they read — no account, sign-in, email address, or payment, and no further field. The finished page they may trade an address for afterwards is REQ-010's." | `api/scan · nothing is asked, nothing is set` — the handler is asserted to read no session and no cookie and to set no `Set-Cookie` header on any response, success or failure; a source assertion that `route.ts` imports no session, auth or account module. |
| REQ-003 c7 — "Given a scan is already running from the visitor's network, when another scan would start for them, then no second scan starts; if the running scan is of the domain they asked for they are returned to it, and if it is not they are refused in writing, told how long until they may scan, and are never shown a scan or a report of a domain they did not ask for." | `api/scan · a double post joins the one running scan` — two posts for the same domain from one network key in flight produce one `running` row and both responses carry the same scan id. And `api/scan · a different domain is not returned` — a post for domain B while A runs returns the canonical location of B with no scan id and no reference to A; the refusal is read at B's address. |
| REQ-003 c12 — "Given a visitor refused under criterion 6 or 7 at the report address of a domain with no stored report, when the address loads, then it shows that refusal in writing and starts no scan — never a blank page, a 404, or an error (REQ-001 criterion 5) …" | `api/scan · a refusal still yields a location, never an error status` — for each refusal reason, the response is a 2xx (JSON) or a 303 (form) to the canonical address and never a 4xx or 5xx; the refusal itself is not rendered by this handler. |
| BP-022 decision 3 — "the API is a canonicaliser and a starter … the report is only ever served by `GET /scan/{domain}`" | `api/scan · this route never returns a report` — every response body across the suite is asserted to contain no report, no score and no band; a source assertion that `route.ts` does not import `readCurrentReport`, `resolveAddress` or any component. |

## Out of scope
- Rendering the landing page and its written line — WO-070.
- Rendering the refusal at the report address — WO-069.
- The admission order itself — WO-057, WO-058.
- Any `GET` on this path: this file exports `POST` only.

## Rollback
Delete both files. Only WO-070's landing form posts here.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 7 rows of `## Test plan` quote REQ-001, REQ-003 as they stand on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — `src/app/api/scan/route.ts` and `tests/app/scan-address/api-scan.test.ts` built and passing (28/28); full suite green (536 passed, 1 pre-existing skip); ready for validation.
- 2026-09-03 finished — validator — TST-024, verdict pass: all 7 test-plan criteria independently reproduced and confirmed to discriminate by mutation; both rule-4.2 deviations (`scanId?` optionality, the `problem`/`value` redirect params) confirmed legitimate parameter authority (rule 1.1), correctly flagged once and adequately documented; full suite (536/1 skip), typecheck, lint (byte-identical pre-existing 8118-problem count, isolated files clean) and build (clean once a fixture `.env.local` supplies the bindings `env.ts` requires — a local-environment gap, not a WO-062 defect) all clean; this WO's own `rests-on` row left `open` deliberately (WO-003 not yet built) and BP-022's matching upstream row persisted to `undischargeable` (same claim WO-057/TST-021 already settled).
- 2026-09-03 finished — validator — `/regress` on TST-024's passing verdict: `npm test` 536/537 (1 pre-existing skip), `npm run typecheck` clean, `npm run test:layout` 16/16, `npm run build` clean via the same throwaway-`.env.local` workaround TST-024 found (deleted after, never committed) all independently re-run and matching TST-024's own counts exactly; `factory-console impact WO-062` named REQ-001 and REQ-002 (via BP-001, BP-022) — REQ-001's 4 WO-062-covered criteria (2, 3, 9, 10) re-checked and still pass against `route.ts`; its other 14 and all 4 of REQ-002's have no implementing code anywhere in this repo yet (WO-061/069/070 still `approved`, not built), consistent with WO-062's own `## Out of scope`; all 13 named earlier-`done` WOs re-checked, still `status: done`, untouched by this diff; branch current with `doctrine-0.11.0` (shared tip `4532ad6`), no drift. `Regression:` line appended to `## TST-024`, no findings.
- 2026-09-03 refused — librarian — constitution §2.3b / §3.1: `rests-on` row 1 is `open` (WO-003, `src/middleware.ts`'s `PUBLIC_PATHS` allow-list not yet built). Clear with: WO-003 complete and its validator disposition the row.
- 2026-09-03 finished — librarian — form repair: `## TST-024` heading was bare; restored to standard validation-report form. Validation ran under Sonnet 5 stand-in during 2026-09-03 model incident; heading form not carried to report, substance stands.
- 2026-09-03 finished — validator (WO-003's, TST-025) — dispositioned `rests-on` row 1 `confirmed`: `src/middleware.ts`'s `PUBLIC_PATHS` lists `/api/scan` verbatim, `isPublic()` short-circuits `hasSession()` on that path, and `route.ts` itself reads no session — independently reproduced and mutation-confirmed (removing the entry breaks two tests). Full reasoning in WO-003's own `## TST-025`.

## Validation report (appended by validator)

## TST-024 — WO-062 validation report

### Scope
WO-062 (`wo/WO-062-api-scan`, commit `f8a1ced`, worktree
`/root/projects/reachkitv3-wt/WO-062`), validated against its own `## Test
plan`, `## Interfaces`, `## Steps` and `## Out of scope`, against constitution
rule 4.2 (the implementer's two flagged deviations), and against rule 2.3b
(`rests-on` disposition, this WO's own row and the upstream rows on BP-022 and
REQ-001/REQ-003). `git diff 4532ad6 f8a1ced --stat`: exactly 3 files —
`src/app/api/scan/route.ts` (NEW, 141 lines), `tests/app/scan-address/
api-scan.test.ts` (NEW, 321 lines), and this WO's own `## Log` (+2 lines) —
matching the file plan exactly. Working tree was clean at commit; nothing
outside WO-062's own scope touched.

### Suite / build
- `vitest run --project node --project ui --project db`: 25 files, 536
  passed, 1 skipped (537) — full corpus suite, matching the implementer's
  claimed count exactly.
- `vitest run --project node tests/app/scan-address/api-scan.test.ts` in
  isolation: 28/28 passed, matching the implementer's claimed count.
- `tsc --noEmit`: clean.
- `eslint .`: 8118 pre-existing problems (8093 errors, 25 warnings) —
  byte-identical to WO-057's and WO-058's own validation reports (TST-021,
  TST-022/023), all in vendored/generated files; `eslint src/app/api/scan/
  route.ts tests/app/scan-address/api-scan.test.ts` in isolation: clean,
  zero output.
- `next build`: **fails as first attempted** — `Failed to collect page data
  for /api/scan`, `env.ts: invalid or missing environment binding(s)`. Root
  cause traced, not assumed: `src/app/api/scan/route.ts` is the **first**
  route in this repo's `src/app/**` that a real Next.js build's page-data
  collection actually executes and that transitively imports `@/lib/config/
  env` (via `@/lib/scan/admission` → `@/lib/db`) unmocked — every earlier
  `done` WO's build either shipped no consuming route (WO-005/WO-006/WO-011)
  or shipped a route that never imports `env.ts` (WO-001/WO-002/WO-029/
  WO-030/WO-051). `env.ts` is deliberately fail-fast by BP-005's own design
  ("a missing binding is a boot failure, not a runtime undefined... there is
  no default and no fallback, so a deployment cannot start half-configured")
  — a real Vercel deployment supplies these bindings; this sandbox's repo
  carries no `.env.local` (correctly gitignored, `.env*.local`, and no
  fixture ever committed). Reproduced the fix independently: wrote a
  throwaway `.env.local` with schema-valid placeholder values for all 17
  bindings, re-ran `npx next build` — compiles, typechecks, collects page
  data and prerenders cleanly (`ƒ /api/scan` dynamic route registered
  correctly, `○ /_not-found` static). Deleted the file immediately after
  (`git status` clean, confirms it was never tracked). **Verdict: a local
  build-environment gap, not a WO-062 code defect** — the code is correct
  and matches this repo's fail-fast design; this is the first WO to make
  that design's local-build cost visible, worth a note for whoever next
  needs `next build` green in this sandbox (a fixture `.env.local`, not a
  code change), but not this WO's to fix and not a criterion any test-plan
  row here claims.

### Criterion-by-criterion

| Criterion (verbatim, per WO's own table) | Test | Verdict | Mutation probe |
|---|---|---|---|
| REQ-001 c2 — every written form, one location | `api/scan · every written form posts to one location` (6 cases) | PASS | Independently reproduced: `isForm` inverted → 20/28 fail across the file; deleting the whole claim block → 18/28 fail (compile-time break confirms the type system also refuses the shape) |
| REQ-001 c3 — malformed value is 422 with handle; no-JS survives; unresolvable accepted | 3 tests (`.each` ×3 cases, no-JS survival, unresolvable-accepted) | PASS | Independently confirmed: `isForm` inversion (above) breaks the no-JS test directly; the "no DNS call" source-scan is a documented, reasoned substitute for a runtime spy (Vitest ESM limitation, explained in-file) — checked both `route.ts` and `domain.ts` for `node:dns`, neither imports it |
| REQ-001 c9 — clean domain underway on return | `api/scan · a clean domain is underway on return` | PASS | Confirmed `await claimFreeScanSlot(` source assertion is real (not decorative): mutating the awaited-before-response ordering is unreachable without also breaking the type system, since `scanId` is derived from `claim` |
| REQ-001 c10 — nothing asked, no session, no cookie | 2 tests (cookie-header assertion across 4 request shapes; source scan for session/auth/account imports) | PASS | Injected `Set-Cookie: sid=1` into the JSON success response — the cookie assertion fails immediately (1/1), confirmed live |
| REQ-003 c7 — double post joins running scan; different domain not returned | 2 tests | PASS | Inverted `claim.refusal.sameDomain` → `!claim.refusal.sameDomain`: "a clean domain is underway" test fails (1/1). Forced `scanId` to always be present (`scanId ?? "leaked-id"`) even on a different-domain refusal: "a different domain is not returned" fails (1/1), confirmed live both times |
| REQ-003 c12 — refusal yields a location, never an error status | `.each` over 6 refusal reasons (JSON 2xx + form 303) | PASS | Same `scanId`-leak mutation above and the `isForm` inversion both independently break this suite; the 6-way `.each` covers every `Admission` refusal arm the type declares, none omitted |
| BP-022 decision 3 — never returns a report | 2 tests (body-text scan for report/score/band; source scan for `readCurrentReport`/`resolveAddress`/`@/ui/` imports) | PASS | Confirmed by reading `route.ts` directly: no such import exists; the body-text scan is exercised against all 3 request shapes in the suite |

Two additional in-scope suites beyond the WO's own table, both exercised and
passing, both independently reproduced: `## Steps` step 2 (malformed-body
400, 3 tests: bad JSON, wrong shape, unrecognised content-type — all assert
`400` and the **absence** of a `problem` key, correctly distinguishing "not a
visitor's typed value" from a parseable-but-wrong domain) and the
out-of-scope guard (`route.ts` declares no `GET`, both `export function GET`
and `export const GET` forms checked).

### Rule 4.2 finding 1 — `StartScanResponse.scanId` optionality — independently confirmed, correct call

Reproduced from first principles: BP-022 `## Public interface` (current text
on disk, not the implementer's paraphrase) reads verbatim
`{ ok: true; location: string; scanId: string }` — `scanId` required, no `?`.
WO-062's own `## Interfaces` section quotes this exact text as "verbatim from
BP-022." WO-062's own `## Steps` step 5 then explicitly overrides it: "Model
this as `{ ok: true, location }` with `scanId` present only when a scan was
claimed" — only representable with `scanId?: string`. This is a real,
pre-existing internal contradiction in the WO's own text, not one the
implementer introduced.

The contradiction is also **substantively decidable**, not just a style
choice between two authors: a required `scanId` is unsatisfiable against
BP-022's own `## Error & edge behavior` table (row 7, `refused`, carries no
scan) and against REQ-003 c7's own text, which this WO's test plan quotes —
"a different domain is not returned" requires a response with no `scanId` at
all on a cross-domain refusal (confirmed live above: forcing a `scanId` onto
that response breaks the test the WO itself specifies). A required field
cannot express "absent." The implementer's choice (`scanId?: string`,
following step 5) is the only one of the two readings that lets this WO's own
test-plan pass at all — the required-`scanId` reading is not merely
undesirable, it is impossible to implement against this WO's own criteria.

Correctly flagged once, in one place (rule 4.2): the `route.ts` header
comment states the contradiction, quotes both sides, and states the
resolution and its reasoning — no re-argument elsewhere in the diff. Correctly
scoped as a parameter, not an escalation (rule 1.1): this is an internal
TypeScript union shape, never a customer-visible string or promise, so no
owner ruling is due.

**One thing this WO could not fix and did not attempt to: BP-022's own
`## Public interface` text is now stale relative to its own downstream WO's
necessary reading, and stays stale after this merge** (rule 2.4, one claim
one home — there are now two statements of this interface, the blueprint's
and the shipped code's, and they disagree). This is not a WO-062 defect
— rule 2.5 puts the type declaration itself in the code, correctly, and this
WO's job was to build against BP-022, not rewrite it. Flagged here for the
librarian/architect's backward pass (`/sync`): BP-022's `## Public interface`
`StartScanResponse` should be corrected to `scanId?: string` to match what
every consuming WO (WO-062, and downstream WO-070) actually needs and got.

### Rule 4.2 finding 2 — the `problem`/`value` redirect query params — independently confirmed, adequately documented

WO-070 (the landing page) is `status: approved`, not yet built, and does not
exist in this repo. The choice of `problem` and `value` as the two query-param
names on the 303 redirect back to `/` is an internal wire format between this
adapter and a not-yet-built consumer — never a string a visitor reads (the
written line itself is WO-070's, resolved through BP-019's copy registry) —
so it is squarely inside rule 1.1's parameter authority, not a customer-visible
promise requiring owner escalation. Correctly flagged once, in the `route.ts`
header, with the resolution instruction for whoever builds WO-070 next
("renamed together with WO-070's own reader, never redefined independently").

Checked whether WO-070's own file plan already commits to a conflicting
convention: it does not — WO-070 `## File plan` and `## Steps` describe the
redirect only generically ("its 303 back here carrying the problem"), naming
no query-param keys of its own anywhere in its text. No collision exists to
find. WO-070 `## Interfaces` already states "Consumes: WO-062's `POST
/api/scan` and `StartScanResponse`," so its own implementer is already
pointed at the file that carries the convention — this is the corpus's normal
channel (rule 5.6, the graph reaches the code) and matches how every other
downstream-consumer note in this corpus works, not a special case invented
here.

**Not restated in this WO's own `## Log`** — only in the `route.ts` header.
This satisfies rule 4.2's "once, in one place" bar and is not a defect, but
is worth naming as a minor discoverability note (not a finding): a
one-line pointer in WO-070's own file, added at its own expansion or build
time, would make the convention easier to find without re-deriving it — left
for WO-070's own implementer, not required of this WO.

### rests-on disposition (rule 2.3b)

**WO-062's own row**, left **`open`**, deliberately: `src/middleware.ts` does
not exist anywhere in this repo (`find / -iname middleware.ts` under
`/root/projects`, excluding `node_modules`, confirmed empty — matching the
implementer's own claim, independently reproduced, not taken on their word).
This is not `undischargeable` — the claim is not unobservable in principle,
it is simply not yet built: **WO-003** ("Default-deny middleware and the one
public allow-list," `status: approved`, `depends-on: [WO-001, WO-002]` only —
no dependency on WO-062 or BP-022) is the corpus's sole owner of
`src/middleware.ts`, and its own step 1 derives `PUBLIC_PATHS` "from BP-001's
public route list, one entry per route" — BP-001 `## Public interface`
already names `POST /api/scan` verbatim on that list (line 73), so the claim
is expected to be confirmed, not refuted, once WO-003 ships. Per rule 2.3b
("an assumption is discharged by the first stage that can observe it"), that
stage is WO-003's own validator or a later `/regress` on WO-062, not this
build — there is nothing on disk for this build to observe the claim
against. This blocks WO-062's own `done` until then (librarian's
`open-assumption-on-done` gate) — a deliberate, expected consequence of
build order in this corpus, not an oversight of this report. The identical
pattern already exists on WO-069's own `rests-on` row (same claim, same
reason, also left `open`), so this is consistent with, not a departure from,
how the rest of the corpus has handled the same not-yet-built dependency.

**Upstream — BP-022's three rows, walked, one persisted:**
- Row 3 ("Vercel presents the visitor's address in `x-forwarded-for`...")
  — **persisted to `undischargeable`.** This is the identical claim WO-057's
  own `rests-on` row already states verbatim ("BP-022 and BP-023 both carry
  this row; it is spent here") and TST-021 already dispositioned
  `undischargeable` — by the same reasoning REQ-003's own row 2 shows the
  corpus already uses (TST-021 confirmed by first observation but did not
  persist to that row; TST-022/WO-058 re-observed the same unchanged
  mechanism and persisted it there). WO-062's own `route.ts` calls the same
  `networkKeyOf(request.headers.get("x-forwarded-for"))` path (mocked in
  this WO's own suite, exercised for real in WO-057's), so this build
  re-observes, not re-derives, the same settled claim — persisted here since
  no earlier validator had written it onto BP-022's own front matter.
  Genuinely `undischargeable`, not merely unconfirmed: no test in this or any
  future WO can observe that Vercel's edge actually populates
  `x-forwarded-for` correctly in production; only a live deployment could,
  and re-litigating it on every WO that touches the header is the exact
  waste rule 2.3a warns against.
- Row 1 (the removal address's copy-registry value) and Row 2 (a person, not
  the product, writes `domain_blocks`) — **left as they stand, untouched.**
  WO-062 renders no removal line and writes nothing to `domain_blocks`; this
  build observed neither claim. Not this WO's to disposition (rule 2.3b: "the
  validator of the first work order that implements it **and observed the
  claim**") — left for whichever WO actually builds the removal render path
  or the `domain_blocks` writer-absence guard (WO-012, per WO-057's own
  finding 1).

**Upstream — REQ-003's one open row** (the 60 s p95 battery-completion
claim) — **left untouched.** WO-062 starts a scan but runs no part of the
battery itself (`## Out of scope`: "the admission order itself"); this build
observed nothing about vendor response time. REQ-001 carries `rests-on: []`,
nothing to walk.

### Coverage gaps
None found against this WO's own test-plan rows — all 7 are covered by tests
independently confirmed to discriminate by targeted mutation, and the two
supplementary in-scope suites (step 2's malformed-body handling, the
out-of-scope `GET` guard) are exercised too. The build-environment gap (no
`.env.local` in this sandbox) is noted above under Suite/build and is not a
test-coverage gap — it is orthogonal to whether the code is correct.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass /
findings / reject."

### Verdict

pass

WO-062 shipped exactly what its own `## Interfaces`, `## Steps` and `## Out
of scope` specify, in exactly the 2 files its `## File plan` names. All 7
test-plan rows are covered by tests independently reproduced and confirmed to
discriminate by mutation — inverting the same-domain check, leaking a
`scanId` onto a refusal, inverting the form/JSON transport branch, injecting
a `Set-Cookie` header, and deleting the claim step outright each broke the
test(s) claiming to cover that behaviour, several of them broadly (18-20 of
28), and none of the probes left the suite green. Both rule-4.2 deviations
are independently confirmed to be correctly reasoned exercises of parameter
authority (rule 1.1), correctly flagged once each, and — for the `scanId`
case — the only reading that makes this WO's own test plan satisfiable at
all. Full suite (536/537, 1 pre-existing skip), typecheck and lint (isolated
files clean, whole-repo count byte-identical to precedent) all pass; `next
build` is clean once schema-valid bindings are supplied, confirmed
independently, and its as-found failure is a local sandbox gap traced to its
root cause, not a WO-062 defect. No files outside the file plan touched. This
WO's own `rests-on` row is left `open` deliberately, named and reasoned above,
blocking `done` until WO-003 ships; BP-022's matching upstream row is
persisted to `undischargeable`, a settled claim carried over from WO-057/
TST-021 rather than re-litigated. One documentation follow-up is flagged for
`/sync`, not this validation: BP-022's own `## Public interface` should be
corrected to `scanId?: string` to match what its own downstream WOs actually
need and got.

Regression: 11 files (`factory-console impact WO-062`, run from this
worktree: `sdlc-factory/docs/blueprints/BP-022.md`,
`sdlc-factory/docs/registry/waves.md`, `sdlc-factory/docs/work-orders/
WO-041.md`, `WO-051.md`, `WO-056.md`, `WO-057.md`, `WO-058.md`, `WO-062.md`,
`WO-070.md`, `src/app/api/scan/route.ts`,
`tests/app/scan-address/api-scan.test.ts` → BP-001 (2 files), BP-022 (2
files) → 0 importers → REQ-001, REQ-002) · REQ-001, REQ-002 re-checked —
pass. Independent full re-run, off the same tip (`aa3bcbd`): `npm test`
(`vitest run --project node --project ui --project db`) 25 files / 536
passed / 1 skipped (537), matching TST-024's own count exactly (the visible
`ERROR`/`NOTICE` lines in the run's stderr are the `db` project's own
constraint-violation and cascade-drop test fixtures, not failures — checked
against the corresponding assertions, which expect the rejection); `npm run
typecheck` clean; `npm run test:layout` 3 files / 16 passed; `npx eslint .`
8118 problems (8093 errors, 25 warnings), byte-identical to TST-024's own
count, all in vendored/generated files, isolated `route.ts`/
`api-scan.test.ts` clean; `npm run build` reconfirmed as the same
local-sandbox gap TST-024 already found and traced to root cause (`env.ts`
is the first unmocked import this route's page-data collection reaches) —
reproduced the same workaround independently (a throwaway `.env.local` with
17 schema-valid fixture bindings), `next build` compiled, typechecked and
prerendered clean (`ƒ /api/scan` dynamic, `○ /_not-found` static), and the
fixture file was deleted immediately after (`git status` clean both before
and after, confirming it was never tracked or left behind). `tests/app/
scan-address/api-scan.test.ts` re-run in isolation: 28/28, matching
TST-024's own count.

`REQ-001`'s 18 criteria re-checked against the code on disk, not restated
from TST-024's table: criteria 2, 3, 9 and 10 are the ones WO-062's own file
plan covers and remain implemented exactly as TST-024 found them in
`src/app/api/scan/route.ts` (canonical `/scan/{domain}` location for every
written form; 422/303 with the `DomainProblem` handle on a malformed value
and no DNS call for an unresolvable-but-well-formed one; a claimed
`scanId` and a `running` row before the response; no session, cookie or
`Set-Cookie` header, confirmed by the same source-scan and header
assertions, re-run and still green). The remaining 14 (1, 4–8, 11–18) have
no implementing code anywhere in this repo to re-check against: the landing
page (WO-070), the report-address resolver and its view (WO-061, WO-069)
are all still `status: approved`, not built — `find src/app -type f` shows
only layout shells and this WO's own route under `src/app/**`, no landing
form and no `/scan/[domain]` page. `REQ-002`'s 4 criteria (the removal path)
are likewise entirely unimplemented — no removal address, no removal
handler, no `domain_blocks` reader/writer exists in `src/` — which is
consistent with, not a regression against, WO-062's own `## Out of scope`
("Rendering the refusal at the report address — WO-069," "The admission
order itself — WO-057, WO-058") and BP-022's still-`open` rows 1 and 2 on
the removal address and the `domain_blocks` writer, both untouched by this
build per TST-024's own disposition.

The 13 named `done` work orders (WO-001, WO-002, WO-005, WO-006, WO-029,
WO-030, WO-041, WO-051, WO-056, WO-057, WO-058, WO-267, WO-268) were
re-checked for status and gate integrity, not just re-listed: all 13 still
carry `status: done`, unchanged by this branch, and every one of their own
test suites is part of the 536/537 re-run above (`api-scan.test.ts` is the
only new file on this diff — `git diff 4532ad6 aa3bcbd --stat` still shows
exactly the 2 file-plan files plus this WO's own `## Log`/validation
section, nothing under any of those 13 WOs' own paths touched). Branch
currency: `wo/WO-062-api-scan`'s own merge-base with `doctrine-0.11.0` is
`4532ad6` — the exact tip of both the local and `origin/doctrine-0.11.0`
branches as fetched for this check — so the branch is current with the
integration branch with nothing to merge and no drift to reconcile.

No findings. `Regression:` line recorded on `## TST-024`.
