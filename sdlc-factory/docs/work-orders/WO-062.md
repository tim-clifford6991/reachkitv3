---
id: WO-062
type: work-order
title: "POST /api/scan — the canonicaliser and starter, with a no-JavaScript path"
status: approved
ui: no
implements: [BP-022, REQ-001, REQ-003]
depends-on: [WO-051, WO-058]
blocked-by: []
rests-on:
  - claim: "`src/middleware.ts` lists every path in this node on its public allow-list, so no session is read on this route. The middleware is BP-001's; this repo carries no `src/` today"
    disposition: open
supersedes: []
wave: W2
---

# WO-062 — `POST /api/scan` — the canonicaliser and starter, with a no-JavaScript path

- Branch: wo/WO-062-api-scan
- Estimate: ≤1 day

## Goal
Turn one submitted value into the canonical report address and, where admission
allows, one claimed scan — responding with a location the client navigates to,
and never with a rendered report.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md` (`src/app/api/**` is BP-001's container and every
> file in it is transport-only, rule 1; BP-022 anchors this route). Provisional
> against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/app/api/scan/route.ts` | NEW | The `POST` handler and `StartScanResponse`. A thin adapter: parse the body, call WO-051's `parseDomain`, call WO-057's `networkKeyOf` over `x-forwarded-for`, call WO-058's `claimFreeScanSlot`, and return a location. It contains no admission logic, no SQL and no rendering. Content negotiation: a JSON request gets `StartScanResponse` (422 on a malformed value); a form submission with no JavaScript gets 303 to the canonical address on success and 303 back to the landing page carrying the problem handle and the submitted value on failure. |
| `tests/app/scan-address/api-scan.test.ts` | NEW | The response, idempotence, no-JS and no-session suites named in the test plan. |

## Interfaces

Consumes: WO-051's `parseDomain` and `DomainProblem`; WO-057's `networkKeyOf`;
WO-058's `claimFreeScanSlot`.

Exposes (verbatim from BP-022 `## Public interface`):

```
Routes
  POST /api/scan                            { value: string } — start, or name what is wrong
```

```ts
// src/app/api/scan/route.ts
export type StartScanResponse =
  | { ok: true;  location: string; scanId: string }
  | { ok: false; problem: DomainProblem }                                // REQ-001 c3, HTTP 422
```

## Steps
1. Write the failing suite first.
2. Parse and validate the request body to `{ value: string }`; anything else is a
   400 with no problem handle, since it is not a visitor's typed value.
3. `parseDomain(value)`. On failure respond 422 with `{ ok: false, problem }`
   (JSON) or 303 back to the landing page with the handle and the value
   preserved (form).
4. Derive the network key from `x-forwarded-for`'s first entry and claim the
   slot. On a claim, respond `{ ok: true, location, scanId }` (JSON) or 303 to
   `location` (form).
5. On a refusal, respond `{ ok: true, location, scanId: '' }`? **No** — respond
   with the canonical location and no scan id: the refusal is shown at the report
   address on load, by WO-061's resolver and WO-069's view (REQ-003 c12). Model
   this as `{ ok: true, location }` with `scanId` present only when a scan was
   claimed, and keep the union shape BP-022 declares by returning the running
   scan's id where admission returned `in_flight` with `sameDomain: true`.
6. Make the post idempotent on the canonical domain: a double mount, a refresh
   mid-scan and two tabs all join the one running scan through
   `claimFreeScanSlot`'s in-flight arm rather than starting a second.
7. Read no session and set no cookie anywhere in the handler.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-001.md` and
> `requirements/REQ-003.md`; line breaks are normalised to fit the table and no
> word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-001 c2 — "Given a visitor who enters a domain in any of its written forms — with or without a scheme, with or without `www`, with a trailing path, in any letter case — when they submit, then they arrive at the single public report address for that domain; no two of those forms produce different addresses or different stored reports." | `api/scan · every written form posts to one location` — posting each written form of one domain returns the identical `location`, and the location is the canonical `/scan/{domain}` address. |
| REQ-001 c3 — "Given a visitor who submits a value that is not a well-formed public domain name — an empty value, an IP literal, or a hostname with no public suffix — when they submit, then one written line names what is wrong with the value and the visitor stays on the landing page with their input intact. A well-formed domain is accepted here whether or not it resolves; a domain that cannot be reached is a failed scan (REQ-003), not a rejected input." | `api/scan · a malformed value is 422 with its handle` — each of the three named cases returns HTTP 422 and `{ ok: false, problem }` with the matching `DomainProblem`. And `api/scan · without JavaScript the value survives` — the form-encoded equivalent returns 303 to the landing page with the problem handle and the submitted value carried, so the landing page can re-render in place with the input intact (the render is WO-070's). And `api/scan · a domain that does not resolve is accepted` — a well-formed unresolvable domain claims a scan and returns a location; asserted by spy that no DNS call was made in this handler. |
| REQ-001 c9 — "Given a well-formed domain that has no stored report, is not in a failure cooldown, and whose report was not removed at its owner's request, when its report address is first reached — by a submission on the landing page, or by opening that address directly … then a scan for that domain is already underway with no further action from the visitor …" | `api/scan · a clean domain is underway on return` — the response carries a `scanId` and a `running` `scans` row exists for it before the response is sent. |
| REQ-001 c10 — "Given a visitor for whom a scan started under criterion 9, when they wait through it and read the report, then nothing is asked of them at any point between the domain they gave and the report they read — no account, sign-in, email address, or payment, and no further field. The finished page they may trade an address for afterwards is REQ-010's." | `api/scan · nothing is asked, nothing is set` — the handler is asserted to read no session and no cookie and to set no `Set-Cookie` header on any response, success or failure; a source assertion that `route.ts` imports no session, auth or account module. |
| REQ-003 c7 — "Given a scan is already running from the visitor's network, when another scan would start for them, then no second scan starts; if the running scan is of the domain they asked for they are returned to it, and if it is not they are refused in writing, told how long until they may scan, and are never shown a scan or a report of a domain they did not ask for." | `api/scan · a double post joins the one running scan` — two posts for the same domain from one network key in flight produce one `running` row and both responses carry the same scan id. And `api/scan · a different domain is not returned` — a post for domain B while A runs returns the canonical location of B with no scan id and no reference to A; the refusal is read at B's address. |
| REQ-003 c12 — "Given a visitor refused under criterion 6 or 7 at the report address of a domain with no stored report, when the address loads, then it shows that refusal in writing and starts no scan — never a blank page, a 404, or an error (REQ-001 criterion 5) …" | `api/scan · a refusal still yields a location, never an error status` — for each refusal reason, the response is a 2xx (JSON) or a 303 (form) to the canonical address and never a 4xx or 5xx; the refusal itself is not rendered by this handler. |
| BP-022 decision 3 — "the API is a canonicaliser and a starter … the report is only ever served by `GET /scan/{domain}`" | `api/scan · this route never returns a report` — every response body across the suite is asserted to contain no report, no score and no band; a source assertion that `route.ts` does not import `readCurrentReport`, `resolveAddress` or any component. |

## Out of scope
- Rendering the landing page and its written line — WO-070.
- Rendering the refusal at the report address — WO-069.
- The admission order itself — WO-057, WO-058.
- Any `GET` on this path: this file exports `POST` only.

## Rollback
Delete both files. Only WO-070's landing form posts here.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 7 rows of `## Test plan` quote REQ-001, REQ-003 as they stand on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
