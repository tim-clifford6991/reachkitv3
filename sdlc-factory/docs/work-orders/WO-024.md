---
id: WO-024
type: work-order
title: "DataForSEO Labs — rankedKeywords, keywordSuggestions, competitorsDomain"
status: approved
wave: W4
ui: no
risk: high
implements: [BP-008]
depends-on: [WO-006, WO-023]
blocked-by: []
rests-on:
  - claim: "BP-008's own open assumption stands and is not discharged here: whether Labs `ranked_keywords` on a root domain returns rows for pages served on the `content.` subdomain. `BUILD.md` §16 milestone 2 is the first measurement, so this work order neither confirms nor refutes it and adds no subdomain handling on speculation."
    disposition: open
supersedes: []
---

# WO-024 — DataForSEO Labs — rankedKeywords, keywordSuggestions, competitorsDomain

- Branch: wo/WO-024-labs-endpoints
- Estimate: ≤1 day

## Goal
Implement the three Labs endpoints `BUILD.md` §6.3 admits, each priced, cached
and cost-contexted, returning product types rather than vendor payloads.

**Risk: high — seam: money.** Three billable Labs endpoints, each priced against the price book, each cost-contexted and each cached. A price test or a cache-key test that survives deletion bills the product on every call and shows nothing on any screen — the exact failure the charter's cost envelope ("Enforced in the seam, not assumed. Caps are hard") is written against. Mutation-tested (doctrine 0.13.2, rule 2b).

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW under BP-008's `code:`
glob `src/lib/vendors/**`. It shares no file with WO-025.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/vendors/dataforseo/labs.ts` | NEW | `rankedKeywords`, `keywordSuggestions`, `competitorsDomain`. Each wraps its call in `c.recordFetch({ source, cacheKey, freshnessDays, costCents, run })` with the cost taken from `PRICE_BOOK` and the freshness from `CACHE_WINDOWS_D` (own 7d, rival 30d, suggestions 30d) — never a literal. Each parses the vendor payload into the product type and maps a vendor error, a timeout or an unparseable payload to `unmeasured` with its reason. |
| `src/lib/vendors/dataforseo/index.ts` | MODIFY | Re-exports the three. Sequenced before WO-025 so the two never edit this file at once. |
| `tests/vendors/labs.test.ts` | NEW | The tests below. |

## Interfaces
Exposes (verbatim from BP-008 `## Public interface`):
```ts
export function rankedKeywords(c: CostContext, a: { domain: string; rows: 50|100|300 }): Promise<Measured<RankedRow[]>>
export function keywordSuggestions(c: CostContext, a: { seed: string; rows: 50 }): Promise<Measured<SuggestionRow[]>>
export function competitorsDomain(c: CostContext, a: { domain: string }): Promise<Measured<CompetitorRow[]>>
```
Consumes: `transport.ts` (WO-023), `CostContext` (BP-007), `PRICE_BOOK` and
`CACHE_WINDOWS_D` (BP-005).

## Steps
1. Implement the three against `transport.ts`.
2. Wire each price from `PRICE_BOOK` and each window from `CACHE_WINDOWS_D`.
3. Write the tests; watch the "zero rows is measured, not unmeasured" test fail
   first by mapping an empty array to `unmeasured`.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-008 carries
> `satisfies: []`.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-008 error behaviour: "Zero rows is a legal, billed result and returns a measured empty array — never `unmeasured`." | `tests/vendors/labs.test.ts` — an empty vendor response yields a measured `[]` and a ledgered cost. Mapping it to `unmeasured` fails. |
| BP-008 error behaviour: "A vendor error, a timeout or an unparseable payload returns `unmeasured` with its reason, so REQ-004's trichotomy survives the vendor boundary intact." | Same file — three cases, three reasons, none thrown. |
| BP-008 public interface: "Every call takes the CostContext; none can be made outside one." | Same file — a type-level assertion that none of the three can be called without a context, plus a runtime assertion that each goes through `recordFetch`. |
| `BUILD.md` §6.1 price book rows for `RANKED_FREE`, `RANKED_PAID`, `RANKED_RIVAL`, `COMPETITORS_DOMAIN_COST`, `SUGGESTIONS_COST` | Same file — asserts each call's `costCents` equals the `PRICE_BOOK` entry, read from `constants.ts` and never written as a literal in `labs.ts`. |
| `BUILD.md` §6.4: "Cache windows: own domain 7d · rivals 30d · SERPs 30d … suggestions 30d" | Same file — asserts each call's `freshnessDays` comes from `CACHE_WINDOWS_D`. |

## Out of scope
- `serpOrganic`, `aiMode`, `llmScraper` — WO-025.
- Per-rival `ranked_keywords` on the free path: on the never-list; enforcement is
  the caller's tier logic (BP-012), and this module offers no free-path variant.
- Anything about the `content.` subdomain (see `rests-on`).

## Rollback
Delete `labs.ts`, its test, and the three re-exports.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-008 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote BP-008's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 3 files in the file plan (2 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-006); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-04 finished — planner — /wave propose: **W4**, MoSCoW **Should** (it declares no member of `StoredReport`; if it slips the wave still lands four of them, but WO-072 and WO-087 slip with it); `risk: high` set with its seam in `## Goal` (money, doctrine 0.13.2 rule 2b).

## Validation report (appended by validator)
