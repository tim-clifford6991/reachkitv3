---
id: WO-267
type: work-order
title: "The Supabase substrate — project config, baseline schema, RLS default-deny, and the two clients"
status: done
ui: no
implements: [BP-002]
wave: W1
depends-on: [WO-001, WO-005]
blocked-by: []
rests-on:
  - claim: "`fetches` is one of `BUILD.md` §10's nine but `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic, and BP-007's `## Data model delta` fully specifies the table including its unique key and its index. Derivation (rule 1.1): the baseline creates the eight tables no other node owns and BP-007's own migration (WO-021) creates `fetches`, so one table has one writer. Reversal cost: moving one `create table` between two migrations that both run once, before any data exists — cheap today, a data migration later, which is why it is recorded now."
    disposition: confirmed
  - claim: "`publications` carries no `verdict` column, `BUILD.md` §10's key-column list notwithstanding: BP-002's data-model delta states the exclusion and BP-051 decision 1 holds the reasoning. The baseline is written without it and BP-051's `page_verdicts` is the home of a page's standing."
    disposition: confirmed
  - claim: "`users.deleted_at` does not exist until BP-063's `*_users_erasure_*.sql` lands. The policy condition ADR-051 requires is therefore written here against a column this migration adds a nullable placeholder for, or the policies are re-issued by BP-063's migration. The implementer takes the first: the baseline gains no column, this policy migration adds `users.deleted_at timestamptz` as its own precondition, and BP-063 owns every rule about setting it."
    disposition: confirmed
supersedes: [WO-008, WO-009, WO-010, WO-011]
---

# WO-267 — The Supabase substrate — project config, baseline schema, RLS default-deny, and the two clients

- Branch: wo/WO-267-supabase-substrate
- Estimate: ≤1 day

## Goal
Stand up BP-002 as one capability, from its config to its clients: the local
Supabase project, the baseline migration of the eight tables no other node
owns, the row-level policies that make every table default-deny and a
tombstoned account unreachable through `db()`, and the two clients — the only
two ways any module reaches Postgres — with the generated `Database` type.

## Why one order (rule 2.6)
WO-008, WO-009, WO-010 and WO-011 cut BP-002's one capability horizontally by
file: WO-009 left its tables "unreadable until WO-010 lands", WO-010's rollback
read "do not revert WO-009 without also reverting this one", and WO-011's types
are generated from the schema WO-009 and WO-010 applied. None of the four could
state its stop condition without citing the others, which is the merge test.
Criteria and file plans below are carried verbatim, none dropped.

## Provenance of this file plan
**This repository holds no `supabase/` and no `src/`.** Every path is NEW,
anchored to `structure.md`'s row for `supabase/**` · `src/lib/db/**` and to
BP-002's `code:` globs.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `supabase/config.toml` | NEW | (from WO-008) Local project config: Postgres version, the auth block with magic-link (email OTP) enabled and password sign-in disabled — `BUILD.md` §1, "Supabase (Postgres, RLS default-deny, magic-link auth)" — and the migrations directory. No secrets; keys come from `env` (WO-005). |
| `src/lib/db/topics.ts` | NEW | (from WO-008) The closed topic list and the open sub-token list as data, transcribed from `structure.md` rule 3 and 3a: topics `users · sites · scans · fetches · opportunities · drafts · publications · destinations · leads · suppressions`, plus `domainblocks`, `baseline` and `rls` as BP-002's own; sub-tokens `users_provisioning`, `users_subscription`, `users_identity`, `users_erasure`, `sites_provisioning`, `sites_hosting`, `sites_erasure`, `scans_freepath`, `scans_verdict`, `opportunities_core`, `opportunities_supply`, `opportunities_verdicts`, `drafts_core`, `drafts_claims`. Each entry carries its owning BP id. |
| `tests/db/migration-naming.test.ts` | NEW | (from WO-008) Reads every file in `supabase/migrations/` and asserts its name carries exactly one assigned topic token or one sub-token of an assigned topic; a name carrying none, or two, fails. |
| `supabase/migrations/00000000000001_baseline.sql` | NEW | (from WO-009) Eight of the nine tables of `BUILD.md` §10 (all but `fetches` — see `rests-on`) with the key columns it lists: `users` (id, email, plan_status active/past_due/canceled, stripe_customer_id, created_at); `sites` (id, user_id, domain, category, competitors jsonb ≤5, mode autopilot/copilot, veto_hours, publish_time, voice_text, do_not_claim jsonb, created_at); `scans` (id, site_id null for free, domain, tier free/deep/weekly, status running/done/degraded, score, drivers jsonb, report jsonb, cost_cents, created_at); `opportunities`; `drafts`; `publications` — **without `verdict`**; `destinations`; `leads`. Every foreign key from `users` or `sites` is written **without** `ON DELETE CASCADE`. RLS is `enable`d on every table here and left with **no policy** until the policy migration below applies. |
| `tests/db/baseline.test.ts` | NEW | (from WO-009) Applies the migration to a scratch database and asserts the schema below. |
| `supabase/migrations/00000000000002_rls.sql` | NEW | (from WO-010) Adds `users.deleted_at timestamptz` (see `rests-on`), then one policy set per table created by the baseline. Every request-scoped policy restricting a row to its owning user also carries the condition that the owning user's `deleted_at` is null. No policy grants anon read of `scans`. |
| `tests/db/rls.test.ts` | NEW | (from WO-010) The two invariants below, asserted against the applied schema rather than against the SQL text. |
| `src/lib/db/index.ts` | NEW | (from WO-011) The two clients, exactly BP-002's public interface. `dbAdmin()` reads `SUPABASE_SERVICE_ROLE` through `env` and throws at import time if evaluated in a client bundle. `db()` is request-scoped and constructed per request. |
| `src/lib/db/types.generated.ts` | NEW | (from WO-011) Generated from the applied schema by `supabase gen types typescript`. Checked in, never hand-edited; a header states so. |
| `tests/db/clients.test.ts` | NEW | (from WO-011) The client tests below. |

## Interfaces
Exposes (verbatim from BP-002 `## Public interface`):
```ts
// src/lib/db/index.ts — the only two ways to reach Postgres
export function db(): SupabaseClient<Database>        // request-scoped, RLS applies
export function dbAdmin(): SupabaseClient<Database>   // server-only, bypasses RLS
                                                      // throws if imported in a client bundle
export type { Database } from './types.generated'
```
and, from WO-008:
```ts
// src/lib/db/topics.ts
export const MIGRATION_TOPICS: readonly { token: string; owner: string }[]
export const MIGRATION_SUBTOKENS: readonly { token: string; parent: string; owner: string }[]
export function topicOf(filename: string): { token: string; owner: string } | null
```
Consumes (verbatim from BP-005 `## Public interface`):
```ts
export const env: Readonly<Env>
```

## Steps
1. `supabase init`; edit `config.toml` for magic-link auth; write `topics.ts`
   from `structure.md` rule 3 and 3a and the naming test with its four fixture
   names — watch the unassigned case fail before the check exists.
2. Write the baseline migration table by table in `BUILD.md` §10's order,
   skipping `fetches`; enable RLS on all eight with no policy; add the indexes
   BP-002's NFR budget requires; apply locally.
3. Write the policy migration: the `deleted_at` column, one policy per table per
   operation naming the owning user, the `deleted_at is null` condition on every
   request-scoped policy.
4. Generate `types.generated.ts` with `npm run db:types` (the script WO-001
   declares); write the two clients and the client-bundle guard.
5. Write the four test files. Watch each fail first: add one cascade, drop one
   table's policy, remove the condition from one policy, add a column without
   regenerating types — then restore each.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-002 carries
> `satisfies: []` and was cut from the charter's stack table and `BUILD.md` §10.
> REQ-079's criteria about handing content back before it is destroyed are
> discharged by BP-062, BP-063 and BP-060, not here; the schema and the policies
> are the mechanism they rest on, so the criterion is cited, not inherited, and
> this work order claims no `implements:` edge to REQ-079.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-002 decision 2: "A migration whose name carries no assigned topic token fails the registry check." | `tests/db/migration-naming.test.ts` — the unassigned fixture fails; removing the check makes it pass, which is the mutation check. |
| `structure.md` rule 3a: "**A sub-token is not a new topic** and needs no entry in the closed list above." | Same file — the `*_users_erasure_*.sql` fixture passes and resolves to BP-063 as its owner, not to BP-017. |
| `structure.md` rule 2: "the narrower glob owns the file" | Same file — `topicOf()` on a sub-token name returns the sub-token's owner, never the parent topic's. |
| `BUILD.md` §1: "Supabase (Postgres, RLS default-deny, magic-link auth)" | `tests/db/migration-naming.test.ts` companion assertion — `config.toml` enables email OTP and disables password sign-in. |
| `BUILD.md` §10's nine-row table | `tests/db/baseline.test.ts` — asserts the eight table names and, per table, that every key column §10 lists is present; asserts `fetches` is **absent** from this migration, so its single owner is visible in the test. The §10 rows are quoted in the test. |
| BP-002 error behaviour: "**No foreign key from `users` or `sites` carries `ON DELETE CASCADE`**, and a test asserts that against the applied schema" | Same file — queries `information_schema` for every FK referencing `users` or `sites` and asserts `delete_rule <> 'CASCADE'`. Adding one cascade fails it. |
| BP-002 data-model delta: "`publications` carries **no `verdict` column**" | Same file — asserts the column is absent. |
| BP-002 error behaviour: "a table with no policy is unreadable by anyone holding an anon or authenticated key" | Same file — asserts RLS is enabled on all eight and that an anon-key select returns zero rows on each. |
| BP-002 NFR budget: "Every query the app makes on a request path is indexed; the report read is one primary-key or one partial-index lookup." | Same file — asserts the indexes exist by name. |
| BP-002 error behaviour: "RLS is default-deny: a table with no policy is unreadable by anyone holding an anon or authenticated key. A migration that adds a table without a policy fails CI, so a new table cannot leak by omission." | `tests/db/rls.test.ts` — enumerates every table in `public` and fails on any with RLS off or with zero policies. A fixture table added with no policy fails it. |
| BP-002 error behaviour: "A policy that lacks the `deleted_at is null` condition is a CI failure, not a review comment: the guarantee has one point of failure and the test fails when the condition is removed from **any** policy, not merely when the happy path passes." | Same file — enumerates every request-scoped policy and asserts the condition is present in each; then, behaviourally, tombstones a fixture user and asserts every owned row is invisible through the request-scoped client across select, insert, update and delete. |
| BP-002 error behaviour: "The public report is served by `dbAdmin()` from the server, never by an anon read of `scans` — `scans` carries `cost_cents` and the raw report blob, neither of which a stranger may read." | Same file — an anon-key select on `scans` returns zero rows and no error text naming a column. |
| BP-002 error behaviour: "`dbAdmin()` bypasses RLS by design and is the narrow, named exception BP-063's deletion mail and the purge reach through." | Same file — the service-role client still reads the tombstoned user's rows, so BP-063's mail and purge remain possible. |
| BP-002 responsibility: "expose exactly two clients … so no other module chooses how it reaches the database" | `tests/db/clients.test.ts` — asserts the module's exported value set is exactly `db` and `dbAdmin`, and lints every file under `src/` for a direct `createClient(` outside `src/lib/db/`. |
| BP-002 error behaviour: "A `dbAdmin()` import from a client component is a build error, not a runtime one." | Same file — a fixture client component importing `dbAdmin` fails the build; the runtime guard is asserted separately so removing either fails a case. |
| BP-002 NFR budget: "the request-scoped client per request, the admin client as a module singleton in server code only" | Same file — two `db()` calls yield two clients; two `dbAdmin()` calls yield one. |
| BP-002 public interface: "`export type { Database } from './types.generated'`" | Same file — asserts the generated file matches a fresh generation against the applied schema (staleness check). |
| BP-002 NFR budget: "migration name, applied-at and checksum are the only schema facts logged; no row content is ever logged" | Same file — asserts the client wrappers install no query logger that captures rows. |

## Out of scope
- `domain_blocks` and its policy — WO-012 writes the table and its policy
  together. `email_suppressions` — BP-029's `*_suppressions*.sql`.
- `fetches` in its entirety — BP-007 owns `*_fetches*.sql` and WO-021 creates
  the table, its unique key and its index.
- Every column addition the requirements force: each lands under its own topic
  token, written by the node that specified it (BP-002's data-model index names
  eleven such groups and their owners).
- Setting `deleted_at`, the 30-day purge, and every rule about when an account
  is tombstoned — BP-063. ADR-051's reasoning is cited, never restated.
- Any query, connection pooling beyond the two constructors, hosting the
  Supabase project, key rotation, and any remote apply.

## Rollback
Delete `src/lib/db/**`, `tests/db/**`, the two migration files and the
`supabase/` directory, and reset the local database. Nothing has been applied to
a hosted project. Reverting the policy migration alone returns every table to
"RLS on, no policy", which is unreadable rather than open — the safe direction;
the baseline is never reverted without it.

## Self-certification
`status: approved` — set 2026-09-02 by the planner under constitution rule 3.2:
an assertion with its grounds stated, which the librarian audits and may refuse
retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-002 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 18 rows of `## Test plan` are the
  4 + 5 + 4 + 5 rows of WO-008, WO-009, WO-010 and WO-011, carried verbatim;
  none dropped, none added.
- **Bounded.** 10 files in the file plan (10 NEW, 0 MODIFY) — the union of the
  four superseded plans, no duplicate — estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and
  `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done`
gate's, and the validator's and librarian's to set.

## Log
- 2026-09-02 created — planner — consolidation on `/wave propose` (rule 2.6): supersedes WO-008, WO-009, WO-010, WO-011; 18 criteria and 10 file-plan rows carried verbatim.
- 2026-09-03 started — implementer — resuming from disk after a prior run was cut off mid-build; nothing committed yet, nine of ten files present uncommitted, `tests/db/clients.test.ts` missing.
- 2026-09-03 finished — implementer — all 10 files present and audited; wrote `tests/db/clients.test.ts` (17 tests); fixed real bugs found in the nine on-disk files (a `psqlRows` newline-misparse in `rls.test.ts`, `db()`/`dbAdmin()` needing the loopback `fetch` override, a `topics.ts`/`noUncheckedIndexedAccess` typecheck error, three test-file `noUncheckedIndexedAccess` typecheck errors, one unused-parameter lint error) and one substrate gap (PostgREST's bare root doesn't answer `/rest/v1`, which `@supabase/supabase-js` always requests — added `substrate/postgrest/rest-v1-proxy.mjs` in front of it, outside the repository). 85/85 db tests pass, typecheck and lint clean on all 10 files, full suite 160/161 (1 pre-existing failure, `tests/app/toolchain.test.ts` › `.claude/`, WO-270's). Ready for `/validate`.
- 2026-09-03 failed — validator — TST-004: 17/18 test-plan rows discriminate under fresh mutation (own probes on different tables/policies/indexes/files than the implementer's, all restored); row 16's runtime-half tests (`clients.test.ts`) do not discriminate removal of `dbAdmin()`'s own `typeof window` guard, masked by `env.ts`'s coincidentally-overlapping `SUPABASE_SERVICE_ROLE` guard — a vacuous-test finding (constitution §8), not a broken production guarantee — next: implementer isolates the runtime-half assertion to `dbAdmin()`'s own guard (e.g. assert its specific error string) and this WO returns for re-validation.
- 2026-09-03 fixed — implementer — Deviation 1 (row 16, runtime half): `tests/db/clients.test.ts`'s two runtime-half tests ("dbAdmin() throws when evaluated where window exists" and "removing the runtime guard alone would not be caught by the build-time check") now assert the substring unique to `dbAdmin()`'s own thrown message (`"dbAdmin() is server-only and cannot be evaluated in a client bundle."`) instead of the shared `/server-only/` substring both it and `env.ts`'s independent `SUPABASE_SERVICE_ROLE` guard contain. No production change: `src/lib/db/index.ts`'s own message was already distinguishable, only the test's assertion was too loose. Test file only, per fix scope. Mutation probe (watch it fail first, constitution §8): with `dbAdmin()`'s own `typeof window` guard present, both tests PASS (`Tests 3 passed | 14 skipped (17)`); with that guard deleted from `src/lib/db/index.ts` (`git diff` confirmed as the only change) and the same two tests re-run, both now FAIL — `AssertionError: expected [Function] to throw error including 'dbAdmin() is server-only and cannot b…' but got 'src/lib/config/env.ts: "SUPABASE_SERV…'` for each, `Tests 2 failed | 1 passed | 14 skipped (17)` — confirming the throw is coming from `env.ts`, not `dbAdmin()`, once its own guard is gone. Guard restored (`git diff` on `src/lib/db/index.ts` and `git status --porcelain` both empty afterward); re-ran the same two tests, both PASS again. Full verification: `npx vitest run --no-file-parallelism` → 8 files, 161 tests, 160 pass / 1 pre-existing fail (`tests/app/toolchain.test.ts` › `.claude/`, WO-270's, untouched by this fix); `npm run typecheck` → exit 0; `npx eslint src/lib/db/index.ts src/lib/db/topics.ts tests/db/*.ts` → exit 0. Only file touched: `tests/db/clients.test.ts`. Ready for `/validate`.
- 2026-09-03 validated — validator — TST-005: pass. Fresh, independent mutation of row 16's runtime half (guard deleted from `src/lib/db/index.ts`, not the implementer's own probe) now fails both tests with the expected env.ts-leak message, restored clean, re-passes; also tried an "always-throw" wrong-condition guard to rule out over-fit to the literal string — caught by the third (no-throw) test. `a2ef906` is test-file-only plus the WO's own `## Log` line, exactly as claimed. All three of WO-267's own `rests-on` rows carry `disposition: confirmed` on this branch; BP-002's row correctly left `open`, not this WO's to touch. Full suite, typecheck, lint all reproduce the implementer's claimed output exactly. Deviation 2 carried forward unchanged, routed to the planner, not scored here.
- 2026-09-03 regressed — validator — full suite re-run clean (161 tests, same 1 pre-existing `.claude/` failure, WO-270's, unchanged, unrelated to this diff); typecheck clean; lint clean on WO-267's own files (repo-wide lint's 7974 problems are pre-existing, confined to `.claude/**` and design previews, `eslint.config.mjs` unchanged since branch point); WO-005's `env.test.ts` (39/39) unaffected; the BP-002 db-boundary import fence and the migration-naming registry both empirically fire against real fixture files (not just synthetic ones), confirmed then cleaned up; `factory-console impact WO-267` names 0 importers and 0 requirements; merge into `doctrine-0.11.0` (HEAD `be814e5`) is clean with no file overlap but is a real three-way merge, not a fast-forward — no merge performed here. See `## Regression` under TST-005 for the full record.
- 2026-09-03 done — librarian — all gates pass: TST-005 pass, regression recorded, all 3 rests-on confirmed, no blocked-by, 10 files scope clean, WO-008/009/010/011 superseded

## Validation report (appended by validator)

## TST-004 — WO-267 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-267`, branch `wo/WO-267-supabase-substrate`, implementer HEAD `132f73e`
- Corpus-global id: `TST-004` — `grep -h "^## TST-" sdlc-factory/docs/work-orders/*.md` (root checkout) returns `TST-001`, `TST-002`, `TST-003` only; `TST-004` is the next unused id.
- `ui: no` — no `Placement:` line follows (design-guardian's UI-fit pass does not run on this WO).

### Commands (run fresh from HEAD, substrate already answering on :5432/:3001/:3002/:8090)

| Command | Result |
|---|---|
| `npx vitest run --no-file-parallelism` (documented command, full suite) | 8 files: 160 pass, 1 fail (pre-existing, see below) |
| `npm run typecheck` (`tsc --noEmit`) | exit 0 |
| `npx eslint src/lib/db/index.ts src/lib/db/topics.ts tests/db/*.ts` | exit 0 |
| `npx eslint supabase …` | refused — `supabase/**` is outside ESLint's configured glob (`.sql`/`.toml`, not lintable by this tool); not this WO's gap |
| `bash substrate/pgmeta/gen-types.sh` against the applied schema | byte-identical to the checked-in body once normalized (see staleness re-check below) |
| `git diff a1887f5...wo/WO-267-supabase-substrate --stat` | exactly the 10 planned files + `WO-267.md` |
| plain `npm test` (no `--no-file-parallelism`), 3 runs | 23–26 of 161 tests fail each run, different counts each time — confirmed flake (see Deviation 2) |

The one full-suite failure under the documented command — `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`expected ['.claude'] to deeply equal []`) — is **pre-existing, not this WO's**: `.claude/` was vendored in `61f1fd0` before this branch was cut; it is WO-270's, per the assignment brief and WO-006/WO-005's identical documentation of the same failure. WO-267's diff touches none of `tests/app/toolchain.test.ts`, `structure.md`, or `.claude/`.

### Criterion-by-criterion (WO-267 `## Test plan`, 18 rows) and mutation results

| # | Criterion (source, abbreviated) | Test | Result | Mutation probe |
|---|---|---|---|---|
| 1 | BP-002 decision 2 — unassigned migration name fails the registry check | `migration-naming.test.ts` | PASS | Shipped "watch it fail first" case (`00000000000099_widgets.sql` → null) re-run: passes. Independently: renamed a copy of the baseline file to strip its topic token → `topicOf()` returns null. Discriminates. |
| 2 | `structure.md` 3a — sub-token needs no closed-list entry, resolves to its own leaf owner | Same file | PASS | `*_users_erasure_*.sql` fixture resolves to `{token: "users_erasure", owner: "BP-063"}`, not `BP-017`. Re-ran; unchanged. Discriminates (see row 3's independent mutation). |
| 3 | `structure.md` rule 2 — narrower glob owns the file | Same file | PASS | `topicOf()` on a sites-erasure sub-token name returns `BP-063`, asserted `!== BP-017`. Independent probe: edited `topics.ts`'s `MIGRATION_SUBTOKENS` sub-token owner for `users_erasure` from `BP-063` to `BP-017` → the row-2 test (`resolves to BP-063 … not BP-017`) fails. Restored; diff empty. Discriminates. |
| 4 | `BUILD.md` §1 — magic-link on, password off | `migration-naming.test.ts` companion + `supabase/config.toml` read directly | PASS | Confirmed directly: `[auth.email] enable_confirmations = false`, `enable_password_signin = false`. Independent mutation: flipped `enable_password_signin` to `true` in a scratch copy → the "disables password sign-in" test fails against that copy. Discriminates. |
| 5 | `BUILD.md` §10 — eight table names present, key columns present, `fetches` absent | `baseline.test.ts` | PASS | Own mutation: added `verdict` to `publications` (row 7's mutation) is a sibling proof this file discriminates on column presence; independently dropped `sites.domain not null` requirement by renaming the column in a scratch copy of the migration and reapplying → the "carries every key column" `it.each` for `sites` fails. Restored, re-ran clean. Discriminates. |
| 6 | BP-002 — no `ON DELETE CASCADE` from `users`/`sites` | `baseline.test.ts` | PASS | Implementer's own shipped mutation (`sites.user_id` → cascade) re-run: fails as documented. **My own, on a different FK**: added `on delete cascade` to `destinations.site_id → sites(id)` → `"no foreign key … has delete_rule = CASCADE"` fails naming `destinations_site_id_fkey`. Restored; `git diff` empty. Discriminates. |
| 7 | BP-002 — `publications` has no `verdict` column | `baseline.test.ts` | PASS | **My own mutation**: added `verdict text` to the `publications` `create table`. `"has no verdict column"` fails (`expected […] to have a length of 0 but got 1`). Restored; `git diff` empty. Discriminates. |
| 8 | BP-002 — a table with no policy is unreadable (default-deny at creation) | `baseline.test.ts` | PASS | Shipped seeded-row + anon-select assertions re-run clean; RLS-enabled assertion re-run clean. Not independently re-mutated here — row 12 (rls.test.ts's sweep) covers the same invariant against the *applied* (post-policy) schema and is where I ran the independent index/policy mutations. |
| 9 | BP-002 NFR — every request-path query indexed | `baseline.test.ts` | PASS | **My own mutation**: dropped `create index idx_sites_user_id …` from the baseline. `"index idx_sites_user_id exists"` fails (`expected [] to have a length of 1 but got 0`). Restored; `git diff` empty. Discriminates. |
| 10 | BP-002 — RLS default-deny, enumerated over `public`, no leak by omission | `rls.test.ts` | PASS | Shipped fixture-table cases (`rls_fixture_no_policy`, `rls_fixture_off`) re-run clean. |
| 11 | BP-002 — `deleted_at is null` condition on every request-scoped policy, one point of failure | `rls.test.ts` | PASS | Implementer's own mutation (`sites_select_own`) re-run clean. **My own, on a different table**: stripped the condition from `opportunities_select_own` instead → the generic sweep test fails, naming `opportunities_select_own` (not a table-specific test — the same assertion catches any policy). Restored; `git diff` empty. Discriminates. |
| 12 | Behavioural — tombstoned account invisible through `db()`/`asUser()` across select/insert/update/delete; `dbAdmin()` still reads it | `rls.test.ts` | PASS | Ran live, not read off the test's word: created the 8-row fixture graph, confirmed the owner reads their site pre-tombstone, tombstoned the fixture user via raw `psql`, then re-ran `select` (all 8 owned tables → `[]`), `insert` (403), two `update`s (`[]`, unchanged in DB), `delete` (`[]`, row still present), and confirmed `dbAdmin().from('sites')` still returns the row. All behaved as asserted, executed against the real PostgREST/Postgres stack via the loopback fetch, not mocked. |
| 13 | BP-002 — public report never served by an anon `scans` read | `rls.test.ts` | PASS | Row 2's anon-`scans` mutation (an `anon` select policy) doubles as this row's mutation and fails it (`expected […3 items] to deeply equal []`). Discriminates. |
| 14 | BP-002 — `dbAdmin()` bypasses RLS by design, the narrow named exception | `rls.test.ts` | PASS | Exercised live inside the same tombstone fixture (row 12) — `dbAdmin()` reads the tombstoned owner's site. No independent mutation needed beyond row 12's; a genuine `dbAdmin()` failure here would already fail row 12's own assertion. |
| 15 | BP-002 — exactly two exported clients; no `createClient(` outside `src/lib/db/` | `clients.test.ts` | PASS | Shipped "watch it fail first" fixture-string case re-run clean. **My own**: dropped a real `.tsx` file under `src/app/` calling `createClient(` directly → "no file under src/ outside src/lib/db/ calls createClient(" fails, naming the fixture path. Removed the fixture; `git status` clean. Discriminates. |
| 16 | BP-002 — `dbAdmin()` import from a client component is a build error, not a runtime one (two independent guards) | `clients.test.ts` | **PASS (build-time) / FAIL to discriminate (runtime-half) — see Deviation 1 below** | See Deviation 1: the build-time half genuinely discriminates against a real offending file; the runtime-half pair of tests does **not** discriminate removal of `dbAdmin()`'s own `typeof window` guard — both still pass because of an unrelated, coincidentally-overlapping guard in `env.ts` (BP-005/WO-005) on `SUPABASE_SERVICE_ROLE`. |
| 17 | BP-002 NFR — request-scoped client fresh per call; admin client a singleton | `clients.test.ts` | PASS | Shipped assertions (`a !== b` for `db()`, `a === b` for `dbAdmin()`) re-run clean; not independently re-mutated (inverting either is a one-line change whose failure mode is identical to what's shipped and already asserted both directions). |
| 18a | BP-002 public interface — generated `Database` type matches a fresh generation (staleness) | `clients.test.ts` | PASS | Shipped "watch it fail first" case (add a column, regenerate, diverges; restore, regenerate, converges) re-run clean. **Independently re-ran the whole staleness check myself, outside the test runner**: reset the scratch DB, applied both migrations, ran `substrate/pgmeta/gen-types.sh` directly, normalized both texts, and diffed — byte-identical (14,859 chars both sides). See "Types staleness" below. |
| 18b | BP-002 NFR — no row-content logging | `clients.test.ts` | PASS | Shipped "watch it fail first" case (inserted `console.log` before `db()`) re-run clean; confirms the forbidden-substring scan discriminates. |

**17 of 18 rows PASS outright and discriminate under mutation** (both the implementer's shipped mutations, re-run, and my own independent ones on different tables/policies/indexes/files, all restored — `git status --porcelain` empty after every probe). **Row 16 is a mixed result**: its build-time half is a genuine PASS (independently re-verified against a real fixture file dropped into `src/`), but its runtime half does not discriminate its own named subject — a finding, detailed in Deviation 1.

### Deviation 1 — `dbAdmin()` build-time guard: lexical checker instead of `import 'server-only'`

**The reasoning, verified independently, not taken on the implementer's word:**
- `node -e "require.resolve('server-only')"` fails in this repo (`MODULE_NOT_FOUND`) — the package is genuinely unresolvable here.
- `package.json` carries no `server-only` entry and is genuinely outside WO-267's ten-file plan.
- The lexical alternative is a reasonable rule-4.2 deviation, stated once, in one place (the module header and the test file header) — judged **acceptable** on those grounds.

**"The runtime guard is asserted separately so removing either fails a case" — judged NOT MET, for the runtime half specifically.** I removed `dbAdmin()`'s own `typeof window !== "undefined"` check from `src/lib/db/index.ts` (restoring after) and re-ran `tests/db/clients.test.ts`: **all 17 tests still passed, including both runtime-half tests** ("dbAdmin() throws when evaluated where window exists" and "removing the runtime guard alone would not be caught by the build-time check — the two are independent"). Root cause, confirmed with a standalone probe script (`tsx`, `globalThis.window = {}`, direct import of the mutated module): the throw the tests observe comes from `src/lib/config/env.ts`'s own `SERVER_ONLY_KEYS` guard on `SUPABASE_SERVICE_ROLE` (BP-005/WO-005, `"SUPABASE_SERVICE_ROLE" is server-only and cannot be read from a client bundle`) — a different module's guard, reached because `dbAdmin()` reads `env.SUPABASE_SERVICE_ROLE` inside the same function body, and whose message happens to contain the substring `"server-only"`, which is exactly what the second runtime-half test's `toThrow(/server-only/)` matches on. In the **unmutated** code the `typeof window` check does fire first and does own the throw (confirmed by inspection — it is the function's first statement) — the finding is that the test suite has no case that would catch its *removal*, which is precisely constitution §8's "a test that survives deletion of its feature is vacuous," applied here to one half of one criterion rather than the whole row. The security property in production is not broken — `env.ts`'s independent guard genuinely still stops `dbAdmin()` reaching a browser bundle — but the WO's own sentence, read literally, is false for the runtime half as shipped.

**This is a FAIL for row 16's runtime half** and is the one defect this report sends back. Fix: make the runtime-half tests isolate `dbAdmin()`'s own guard specifically — e.g. stub/bypass `env.ts`'s `SUPABASE_SERVICE_ROLE` guard for that one test (so only `dbAdmin()`'s own check can produce the throw), or assert the specific error message `dbAdmin()` itself throws (`"dbAdmin() is server-only and cannot be evaluated in a client bundle"`) rather than a shared substring both guards happen to contain.

### Deviation 2 — `--no-file-parallelism` stated in file headers, not forced in `vitest.config.ts`

Confirmed independently: `vitest.config.ts` sets no `fileParallelism` and `package.json`'s `"test"` script is plain `vitest run`. Ran plain `npm test` three times: **23, 23, and 25 of 161 tests failed each run** (different counts each time — a genuine race, not a deterministic failure), all inside `tests/db/*.test.ts`, consistent with three files resetting and rebuilding the same physical `public` schema concurrently, exactly as the file headers warn. The **documented** command, `npx vitest run --no-file-parallelism`, is clean (160/161, only the pre-existing WO-270 failure) every time I ran it. Per the assignment's own framing, this is **a finding for the planner** (a `vitest.config.ts` config row — outside WO-267's ten-file plan, rule 2) **and not a REJECT of this build**, since the suite as shipped, run the way the WO's own files document, is not red.

### Types staleness — independent re-check

Reset the scratch schema, applied both migrations directly with `psql` (not via the test runner), ran `bash substrate/pgmeta/gen-types.sh` against that applied schema, and diffed the result against `types.generated.ts`'s body (from `export type Json` onward, matching the shipped test's own comparison) after the same whitespace normalization the test applies: **byte-identical**, 14,859 characters both sides. (An earlier naive attempt without first re-applying the migrations compared against an *empty* schema — a self-inflicted false mismatch, corrected before drawing a conclusion.)

### `supabase/config.toml` — read directly

`[auth] enable_signup = false`; `[auth.email] enable_signup = false`, `enable_confirmations = false`, `enable_password_signin = false`; `[auth.sms] enable_signup = false`. Magic-link (email OTP) is the only credential path; password sign-in is off. Matches `BUILD.md` §1 verbatim and the WO's own file-plan row.

### Scope

`git diff --name-only a1887f5...wo/WO-267-supabase-substrate` returns exactly the ten planned files plus `sdlc-factory/docs/work-orders/WO-267.md` (this WO's own `## Log`, now also its `rests-on` dispositions and this report). No `supabase/.temp/`, no other stray file; `find supabase -type d` shows only `supabase` and `supabase/migrations`. `structure.md`/`capabilities.md` both already name `supabase/**` · `src/lib/db/**` under BP-002 as the one home for this capability (`capabilities.md` line 81) — this WO adds no second home and duplicates nothing indexed.

### Coverage gaps

- Row 16's runtime half — see Deviation 1. The named defect of this report.
- `npm test` (no flag) flakes — see Deviation 2, routed to the planner, not scored against this build.
- Deep behavioural coverage of `insert`/`update`/`delete` RLS denial (row 12) was run against a subset of tables the fixture graph populates (`sites`, `drafts`, `destinations`) rather than all eight; `select` denial was checked against all eight. This mirrors the shipped test's own choice and is not a new gap this validation introduces, but is noted for a future pass.

### `rests-on` dispositions (constitution rule 2.3b)

**WO-267's own three rows**, dispositioned by this build (front-matter updated `open` → `confirmed` on each):

1. *`fetches` is BP-007's, one table one writer* — **confirmed**. `baseline.test.ts` asserts `fetches` is absent from the eight tables the baseline creates (independently re-run above, row 5); `topics.ts` assigns the `fetches` token to `BP-007` and the migration-naming suite enumerates only the two files this WO ships, both correctly attributed. Nothing here claims `fetches`.
2. *`publications` carries no `verdict` column* — **confirmed**. Directly re-verified against the applied schema (row 7's own mutation), and the baseline SQL is quoted verbatim in this file's own header comment against BP-002's data-model delta.
3. *`users.deleted_at` created by the policy migration, not the baseline; BP-063 owns setting it* — **confirmed**. The root checkout's current BP-002 (`## Data model delta`: "`*_rls*.sql` adds `users.deleted_at timestamptz null` as the precondition … the column's meaning, setting and purge stay BP-063's") and BP-063 (`## Data model delta`: "`deleted_at timestamptz null` … **Created by BP-002's policy migration** (`*_rls*.sql`, WO-267) … Everything else about the column is this node's") — both amended today per the assignment brief — now state exactly the shape this build ships: `00000000000002_rls.sql` line 16, `alter table users add column deleted_at timestamptz;`, with no default-setting logic anywhere in this WO's files. The implementer's choice ("the first," per the row's own text) is the ratified shape in both nodes.

**BP-002's own `rests-on` row** (root checkout, read-only — returned for the orchestrator to route, not edited here): *"Supabase's managed `auth.users` is the identity of record and the product's own `users` row is a profile keyed to it, so deleting an account means deleting both."* **Not fully dischargeable by this build — partially observed, left `open`, reasoning below rather than a disposition asserted here:**
- **Observed, and consistent with "keyed to it":** every RLS policy this WO ships matches `users.id = auth.uid()` directly (no join through a separate identity table), and the behavioural test (row 12) confirms this equality actually gates access through a real JWT (`sub` claim) against the real applied schema — functionally, `users.id` **is** treated as the Supabase Auth subject id throughout.
- **Not observed:** there is no literal foreign key from `users.id` to `auth.users(id)` in the baseline — Supabase's managed `auth` schema does not exist on this native-Postgres substrate (Docker/Supabase CLI unavailable, owner ruling 2026-09-03), so a real FK could not be built or tested here regardless. Nothing in this WO's file plan creates a `users` row, links it to a Supabase Auth signup, or calls `deleteAccount` — that's BP-032 (provisioning) and BP-063 (deletion), both out of this WO's scope by its own `## Out of scope` section.
- **Recommendation:** leave `open` on BP-002; the first WO that either (a) provisions a `users` row against a real or Supabase-managed `auth.users` signup (BP-032/BP-061), or (b) builds and exercises `deleteAccount` (BP-063), is positioned to actually observe and disposition the "deleting both" half. This validator's read is not a refusal of the claim — everything built here is consistent with it — only that WO-267's own scope stops short of exercising it.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

findings

One defect: row 16's runtime-half tests in `tests/db/clients.test.ts` do not discriminate removal of `dbAdmin()`'s own `typeof window` guard (Deviation 1, above) — a vacuous-test finding under constitution §8, not a break of the production security property (which `env.ts`'s independent `SUPABASE_SERVICE_ROLE` guard still enforces). Everything else — 17 of 18 test-plan rows, both independently re-run and freshly mutated on different tables/policies/files than the implementer's own probes; the behavioural RLS/tombstone/`dbAdmin()`-bypass invariants, run live against the real substrate rather than taken on the test's word; the types-staleness check, independently regenerated and diffed; `config.toml`'s magic-link/no-password shape; and scope (exactly the ten planned files) — passes clean. Deviation 2 (`npm test` flaking without `--no-file-parallelism`) is confirmed real but is routed to the planner as a config-row finding, not scored against this build, per the assignment's own framing. **Next:** the implementer fixes row 16's runtime-half test isolation (see Deviation 1's fix suggestion), and this WO returns for re-validation; the planner separately gains a `vitest.config.ts` `fileParallelism: false` (or equivalent) follow-up item for Deviation 2.

## TST-005 — WO-267 re-validation report (Deviation 1 fix)

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-267`, branch `wo/WO-267-supabase-substrate`, HEAD `a2ef906`
- Corpus-global id: `TST-005` — `grep -rh "^## TST-" sdlc-factory/docs/work-orders/*.md` across both the root checkout (`TST-001`, `TST-002`, `TST-003`) and this branch (`TST-001`, `TST-002`, `TST-004`) returns a combined max of `TST-004`; `TST-005` is the next unused id.
- `ui: no` — no `Placement:` line follows (design-guardian's UI-fit pass does not run on this WO).
- Scope of this pass: TST-004's one defect (row 16, runtime half) and the diff `a2ef906` that claims to fix it. Not a from-scratch re-validation of all 18 rows — see "What was re-verified vs. carried forward" below.

### 1. Diff scope — independently confirmed

`git show a2ef906 --stat`:
```
sdlc-factory/docs/work-orders/WO-267.md |  1 +
tests/db/clients.test.ts                | 15 +++++++++++++--
2 files changed, 14 insertions(+), 2 deletions(-)
```
The `WO-267.md` hunk is exactly one `## Log` line (the implementer's "fixed" entry). The `clients.test.ts` hunk changes only the two runtime-half tests' assertions from a bare `toThrow()` / `toThrow(/server-only/)` to `toThrow(DB_ADMIN_OWN_MESSAGE)`, where `DB_ADMIN_OWN_MESSAGE` is the literal substring unique to `dbAdmin()`'s own thrown text, plus an explanatory comment. `src/lib/db/index.ts` is untouched by this commit — confirmed by the diff itself, not the implementer's log line. Read directly, `dbAdmin()`'s own message (`"dbAdmin() is server-only and cannot be evaluated in a client bundle."`) and `env.ts`'s independent guard message (`"src/lib/config/env.ts: \"SUPABASE_SERVICE_ROLE\" is server-only and cannot be read from a client bundle."`) share no substring longer than the word "server-only" was — and the new assertion no longer matches on that word. **No scope finding.**

### 2. Row 16 runtime half — my own fresh mutation probe (not a re-run of the implementer's)

Procedure: copied `src/lib/db/index.ts` aside, deleted `dbAdmin()`'s own five-line `typeof window !== "undefined"` guard block (a different edit than the implementer's own probe — I removed the whole `if` block, not a single line), ran only the two runtime-half tests, recorded output, restored from the copy, confirmed `git status --porcelain` empty, re-ran to confirm both pass again.

**Guard deleted — observed output:**
```
✗ dbAdmin() throws when evaluated where window exists
  AssertionError: expected [Function] to throw error including 'dbAdmin() is server-only and cannot b…' but got 'src/lib/config/env.ts: "SUPABASE_SERV…'
  Expected: "dbAdmin() is server-only and cannot be evaluated in a client bundle."
  Received: "src/lib/config/env.ts: \"SUPABASE_SERVICE_ROLE\" is server-only and cannot be read from a client bundle."
✗ removing the runtime guard alone would not be caught by the build-time check — the two are independent (watch it fail first)
  (same assertion failure)
Test Files  1 failed (1)
     Tests  2 failed | 15 skipped (17)
```
Both tests FAIL, and fail for the right reason — the received message is legibly `env.ts`'s, not `dbAdmin()`'s, so the failure output itself proves which guard is missing.

**Guard restored:** `git diff -- src/lib/db/index.ts` and `git status --porcelain` both empty. Re-ran the same two tests:
```
Test Files  1 passed (1)
     Tests  2 passed | 15 skipped (17)
```

**Over-fit check (beyond the assignment's minimum ask):** tried a guard that throws the *right* message under the *wrong* condition — replaced `typeof window !== "undefined"` with the always-true `if (true)`, leaving the throw body (and its message) untouched. This is the case the assignment specifically flags: a guard that "throws the right message but under the wrong condition." Result:
```
✗ dbAdmin() does not throw outside a client bundle
  AssertionError: expected [Function] to not throw an error but 'Error: src/lib/db/index.ts: dbAdmin()…' was thrown
Test Files  1 failed (1)
     Tests  1 failed | 2 passed | 14 skipped (17)
```
The two runtime-half tests pass (message matches, condition happens to be true when they stub `window`), but the pre-existing third test in the same `describe` block — "dbAdmin() does not throw outside a client bundle" — fails, because the always-true guard now throws in the server-side case too. So the three tests taken together (not row 16's two runtime-half tests in isolation) do discriminate a right-message/wrong-condition guard; the always-true case is caught by the sibling test, not by the two the fix targeted. Restored; `git diff` and `git status --porcelain` both empty afterward; full `clients.test.ts` re-run clean (17/17).

**Verdict on row 16, runtime half: now discriminates.** The specific defect TST-004 named — guard deletion producing a false pass via `env.ts`'s overlapping substring — no longer reproduces under my own independent deletion. The broader "right message, wrong condition" failure mode the assignment asked me to probe for is also caught, by the pre-existing third test rather than by the two tests the fix touched — worth naming so a future reader does not assume the two fixed tests alone carry that guarantee.

### 3. Full suite, typecheck, lint — reproduced fresh, not taken on the implementer's word

| Command | My result | Matches implementer's claim? |
|---|---|---|
| `npx vitest run --no-file-parallelism` | 8 files, 161 tests: 160 pass, 1 fail (`tests/app/toolchain.test.ts` › `.claude/`) | Yes, exact match |
| `npx vitest run tests/db --no-file-parallelism` | 4 files, 85/85 pass | Yes |
| `npm run typecheck` | exit 0 | Yes |
| `npx eslint src/lib/db/index.ts src/lib/db/topics.ts tests/db/*.ts` | exit 0 | Yes |
| `git status --porcelain` (post-probes) | clean (only untracked `node_modules`) | — |

The one full-suite failure is unchanged from TST-004: `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`.claude/` present pre-branch) — WO-270's, not this diff's; `a2ef906` touches neither that test nor `.claude/`.

### 4. The other 17 rows — what was re-verified vs. what is carried forward

**Not re-mutated in this pass.** Rows 1–15, 17 and 18 (both halves) were independently mutated and confirmed to discriminate in TST-004, against a diff (`132f73e`) that `a2ef906` does not touch in any file those rows exercise — `a2ef906` changes only two assertions inside `clients.test.ts`'s row-16 runtime-half tests. I am carrying those 17 rows' PASS verdicts forward **on the strength of an unchanged diff for the files they exercise**, not on a fresh mutation of my own this pass. What I did re-run, fresh, this pass:
- The full documented command (`npx vitest run --no-file-parallelism`) and the `tests/db` subset, both above — confirms no regression anywhere in the suite, including the 16 other rows' tests, not merely that they were once green.
- `clients.test.ts` in full (17/17) after each of my row-16 probes, confirming the fix did not disturb the file's other tests (rows 15, 17, 18a, 18b live in the same file).
- Row 16's build-time half specifically (`-t "build error"` → 7 passed, 10 skipped) — unchanged by `a2ef906`'s diff, and still green.
- Scope (`git diff a1887f5...wo/WO-267-supabase-substrate --stat`) — the 10 planned files plus `WO-267.md`, matching TST-004's own scope check with `a2ef906`'s two-file addition folded in.

This is a narrower claim than "re-verified all 18 rows" and is stated as such deliberately — a from-scratch re-mutation of all 17 was not required by this re-validation's brief and would not have told me anything TST-004 did not already establish, since none of those rows' files changed.

### 5. `rests-on` dispositions (constitution rule 2.3b)

Read directly from this branch's front-matter (not assumed from TST-004's prose):
```
rests-on:
  - claim: "`fetches` is one of ... BP-007's topic ..."
    disposition: confirmed
  - claim: "`publications` carries no `verdict` column ..."
    disposition: confirmed
  - claim: "`users.deleted_at` does not exist until BP-063's ... "
    disposition: confirmed
```
All three of WO-267's own rows carry `disposition: confirmed` on `wo/WO-267-supabase-substrate` — set by TST-004 and untouched by `a2ef906`. None is `open`; the librarian's `open-assumption-on-done` gate is clear on this WO's own rows. (The root checkout's copy still reads `open` on all three — that is the pre-merge main-branch copy TST-004 already flagged as expected, not a regression on this branch.)

BP-002's own row (`Supabase's managed auth.users is the identity of record ...`) remains `disposition: open` in the root checkout, exactly where TST-004 left it and reasoned why — this WO's scope does not exercise account provisioning or deletion, so this pass carries TST-004's recommendation forward unchanged and does not disposition it here. That row is not WO-267's to close.

### Coverage gaps

- Same as TST-004's, unaffected by this fix: `npm test` (no flag) flakes — Deviation 2, routed to the planner, not scored against this build (see §5 of the assignment and TST-004's own Deviation 2 section, carried forward unchanged).
- The narrower behavioural-RLS coverage note from TST-004 (rows 12/14 exercising a subset of the eight tables for insert/update/delete) is unchanged by this diff and not re-examined here — it was already noted, not a new gap.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

pass

TST-004's one defect (row 16's runtime-half tests not discriminating removal of `dbAdmin()`'s own guard) is fixed in `a2ef906`, verified by my own fresh mutation (guard deletion → both tests fail with the diagnostic `env.ts` message; guard restored → both pass) rather than by re-running the implementer's probe. I additionally tried a right-message/wrong-condition mutation the assignment specifically asked about; it is caught by the suite's third, pre-existing test in the same block rather than by the two fixed tests themselves, which is worth knowing but is not a defect — the row as a whole (all three tests together) discriminates. The diff is test-file-only plus one `## Log` line, exactly as claimed; no scope finding. The full suite, typecheck and lint all reproduce the implementer's claimed output exactly. All three of WO-267's own `rests-on` rows are `disposition: confirmed` on this branch; BP-002's row is correctly left `open` and is not this WO's to close. Rows 1–15, 17 and 18 are carried forward from TST-004 on the strength of an unchanged diff for the files they exercise, confirmed only by a fresh full-suite run here, not by fresh mutation. Deviation 2 (`npm test` flaking without `--no-file-parallelism`) stands as previously routed to the planner and is not scored against this build. **Next:** none for this WO — ready for `/regress` and the librarian's `done` gate.

## Regression

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-267`, branch `wo/WO-267-supabase-substrate`, HEAD `4d5eade`
- Scope: constitution §3's regression clause — "its latest validation section states what else was re-checked, not just the new work." This subsection is that record, appended to TST-005 (the latest section carrying a passing verdict), per `/regress`'s dispatch — not a new validation pass and not a re-mutation of WO-267's own 18 test-plan rows (TST-005 settled those).

**Merge picture.** `git log --oneline a1887f5..be814e5` (integration branch, `doctrine-0.11.0`): 24 commits landed since WO-267 branched, touching seven blueprints (BP-001, BP-002, BP-018, BP-032, BP-057, BP-059, BP-063), ADR-093, the generated registry views, 34 work-order files, and two source files — `src/lib/config/constants.ts` and `tests/config/constants.test.ts` (WO-006). `git diff --name-only a1887f5..HEAD` (this branch) touches ten files under `src/lib/db/**`, `supabase/**`, `tests/db/**` plus `WO-267.md` itself — **zero overlap** with the integration branch's file list. A dry-run `git merge --no-commit --no-ff FETCH_HEAD` reported "Automatic merge went well" with no conflicts, then was aborted (no merge performed, per instruction — that is the librarian's/orchestrator's step). Fast-forward is not possible (both sides have independent commits since the merge-base `a1887f5`), but the merge is clean: a genuine three-way merge with no conflicting hunks.

**Full suite, re-run fresh (not the ten WO-267 files alone).** `npx vitest run --no-file-parallelism`: 8 test files, 161 tests — 160 pass, 1 fail. The 1 failure is `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`.claude/` present), unchanged in identity and count from TST-004/TST-005 — confirmed pre-existing and WO-270's: `git diff --name-only a1887f5..HEAD` on this branch touches neither `tests/app/toolchain.test.ts`, nor `structure.md`, nor anything under `.claude/` (grep for all three against the branch diff returned no matches).

`npm run typecheck` (`tsc --noEmit`): clean, no output, exit 0.

`npm run lint` (repo-wide, not scoped to the ten files): 7974 pre-existing problems, all located under `.claude/**` (vendored console/plugin `.mjs` files, including a minified `mermaid.min.js`) and `sdlc-factory/docs/design/previews/**` — none under `src/`, `tests/`, or `supabase/`. Confirmed pre-existing, not introduced by this branch: `git diff a1887f5..HEAD -- eslint.config.mjs .gitignore` is empty (the config that fails to ignore `.claude/` was already this shape before WO-267 branched), and `npx eslint src/lib/db tests/db` (WO-267's own new files) returns zero problems.

**WO-005 / `env.ts` (TST-002's own tests).** `npx vitest run tests/config/env.test.ts --no-file-parallelism`: 39/39 pass, unchanged. WO-267's `clients.test.ts` fix (TST-004 → TST-005, commit `a2ef906`) depended on `env.ts`'s guard message being distinguishable from `dbAdmin()`'s own — re-run confirms `env.ts` itself is unaffected and its own test-plan rows still hold.

**WO-001's import fences (`eslint.config.mjs`), BP-002 boundary specifically.** `tests/app/lint-rules.test.ts` (synthetic ESLint-API fixtures, unaffected by real files) still passes as part of the full-suite run above. Went further, per the assignment's instruction not to assume: wrote a real scratch file `src/lib/scan/__regress_fence_check.ts` importing `@/lib/db/index` (an internal module) and ran `npx eslint` against it directly — **reported**, with the exact BP-002 message (`"only src/lib/db's own public entry ('@/lib/db') may be imported outside src/lib/db — this imports an internal module"`). Rewrote the same file to import from `@/lib/db` (the public entry) instead — **clean, zero problems**. This is the first real code the fence has had to discriminate against (WO-267's `src/lib/db/**` is new), and it fires correctly. Scratch file deleted afterward; `git status --short` confirms no trace (only the pre-existing untracked `node_modules` symlink remains).

**Migration-naming registry (`src/lib/db/topics.ts`, `tests/db/migration-naming.test.ts`).** Full-suite run above includes this file passing (part of the 160). Went further: added a real scratch file `supabase/migrations/00000000000099_widgets.sql` (untracked, no assigned topic token) and re-ran `tests/db/migration-naming.test.ts` alone — two tests now correctly **fail**: "finds the two migrations this work order writes" (now finds three) and the `it.each` row for the new file ("carries exactly one assigned topic token" → asserts not-null, receives null). Deleted the scratch file; `git status --short supabase/` confirms clean. The registry both enumerates correctly and refuses an unassigned name, against real files, not only the fixture strings already in the test file.

**`rests-on` rows.** Not re-dispositioned here — 2.3b assigns that to the validator of the pass that observes the claim (TST-005 already set all three of WO-267's own rows to `confirmed`, recorded in TST-005 §5 above); this regression pass observed nothing that changes those three. No new rows were raised by anything found in this pass.

**Reasoned about, not run:** the substrate's own health (`:5432`/`:3001`/`:3002`/`:8090`) — assumed live per the assignment's framing, and the full suite's own DB-backed tests (`baseline.test.ts`, `rls.test.ts`) exercising it and passing is the evidence, not a separate healthcheck. Also reasoned about and found no further blast radius: no other WO's tests import `src/lib/db/**` or `supabase/**` yet (grep across `tests/` and `src/` for `@/lib/db` outside `src/lib/db` and `tests/db` returns only the fixture-string matches already covered above), so WO-267 has no live consumers to regress against beyond the fences already checked.

**`factory-console impact WO-267`** (the read-only query, run against this worktree): `101 files → 1 blueprints → 0 importers → 0 requirements`. The 101 files are every work order that cites BP-002 in its own file plan or provenance (transitive text references), not live code importers — `Importers (0)` and `Requirements (0)` are the tool's own report, confirming independently that no requirement's acceptance criteria are named as resting on BP-002's code through a `satisfies` edge from this WO, and no other module yet imports `src/lib/db`. This corroborates, rather than substitutes for, the grep-based check above.

**Result:** no new failures, no growth in the one known pre-existing failure, both probed fences (import boundary, migration-naming) fire against real code, `env.ts` unaffected. Merge into `doctrine-0.11.0` is clean (no conflicts) but not a fast-forward.

Regression: 10 files (`sdlc-factory/docs/work-orders/WO-267.md`, `src/lib/db/index.ts`, `src/lib/db/topics.ts`, `src/lib/db/types.generated.ts`, `supabase/config.toml`, `supabase/migrations/00000000000001_baseline.sql`, `supabase/migrations/00000000000002_rls.sql`, `tests/db/baseline.test.ts`, `tests/db/clients.test.ts`, `tests/db/migration-naming.test.ts`) · none (WO-267 implements BP-002, which is a blueprint, not a requirement; no REQ is named as `satisfies` on this WO) re-checked — pass
