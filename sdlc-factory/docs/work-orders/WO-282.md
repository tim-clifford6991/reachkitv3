---
id: WO-282
type: work-order
title: "The report screen — the route, the total view switch, the verdict strip, the one notice, the one control, and the removal address"
status: approved
wave: W3
ui: yes
risk: normal
implements: [BP-022, BP-024, REQ-001, REQ-002, REQ-003, REQ-004]
depends-on: [WO-041, WO-268, WO-277, WO-278, WO-280, WO-281]
blocked-by: []
rests-on:
  - claim: "The address a report names for a removal request is an owner-written string. BP-019's copy registry holds it under one key and this node renders it. **No value for it exists anywhere in the corpus** — it is owner-owed and outstanding. This WO creates the reference and the test; it writes no address, and `env.OWNER_EMAILS` (`BUILD.md` §15) is a deployment binding for owner accounts, not a published removal address, so substituting it would invent a customer fact (rule 1.2)"
    disposition: open
  - claim: "A person, not the product, writes the `domain_blocks` row that removes a report, and does so inside REQ-002 criterion 2's five working days. BP-005 pins `RETENTION_D.removalSlaWorkingDays: 5`; no code in this WO enforces the window, because no code can — the operation is manual, and ADR-002 carries the same claim for the operation as a whole"
    disposition: open
  - claim: "No blueprint's `code:` glob covers the report's header-strip markup. **Discharged 2026-08-31**: BP-024's `## Module / boundary` now states it — 'The header strip itself is a registered component (BP-018) composed by BP-022's shell and written through BP-019's `renderMeasured`; no sentence and no band word is written here' — which is exactly what this WO does, in BP-022's `_address/` glob. BP-022's own boundary states the other half: 'this node owns the shell that decides whether a body renders at all, and the order the modules sit in (`BUILD.md` §4.1)'. No file here sits outside a declared glob"
    disposition: confirmed
  - claim: "The four band words reach a screen only through BP-019's `SCORE_BANDS`, and the one line naming the limiting factor through BP-019's `LIMITING_LINES` (WO-278). Both are BP-019/BP-020's; this WO renders keys and writes no word. **Confirmed 2026-08-31**: BP-019 declares `SCORE_BANDS` over the four handles with REQ-004 criterion 1's own words — 'transcribed, never chosen' — and WO-041 carries the four strings in `keys/bands.ts`, so the keys resolve"
    disposition: confirmed
  - claim: "The AI-answers card, the Google-presence card, the free-page card and the pricing card are sibling nodes' modules. This WO renders them as named slots and supplies none of them, so the report is composable before those nodes ship"
    disposition: open
  - claim: "`src/middleware.ts` puts `/scan/{domain}` and `/api/scan/**` on BP-001's public allow-list, so no session is read on any path in this node. The middleware is BP-001's; this repo carries no `src/` today"
    disposition: open
  - claim: "No sitemap the product publishes names a report address. The sitemaps are BP-004/BP-047's (`src/app/(hosted)/**`); this WO asserts the absence from this side and cannot enforce it from inside BP-022"
    disposition: open
supersedes: [WO-067, WO-068, WO-069]
---

# WO-282 — The report screen — the route, the total view switch, the verdict strip, the one notice, the one control, and the removal address

- Branch: wo/WO-282-report-screen
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-282.html
- Signed-off: 2026-09-04 — v1

## Goal
Serve the one public report address end to end: resolve it, 308 a non-canonical form, render exactly one arm of the closed union — including the removed arm and the removal address every report names — mark every response `noindex`, and never answer with a blank page, a 404 or an unhandled error.

**Risk: normal.** It moves no money, reads no session (its middleware entry is on BP-001's public allow-list), calls back to no third party and publishes to no customer domain. Its criteria are rendering and response-header assertions a plain test discriminates directly — a deleted `noindex` header fails a presence assertion outright rather than surviving it — and the silent half of the removal promise, the state precedence, is WO-280's, which is `risk: high`. `ui: yes` already routes this order through rule 7.3's preview gate and the UI-fit pass (doctrine 0.13.2, rule 2b).

## Consolidation (rule 2.6)
WO-069 is a total switch over `AddressState` whose `report` arm is WO-068 and
whose `removed` arm is WO-067; WO-068's `## Rollback` reads "WO-069's switch is
the only consumer and does not exist yet"; WO-067 exists so "WO-068 and WO-069
can consume them without re-deriving anything", in its own words. None of the
three can state its stop condition — every arm renders, exactly one control, one
removal address — without citing the other two. One order, one journey step:
JN-001 steps 3 and 4, the screen a stranger lands on.

**A `draft` order brought forward.** WO-067 carried `status: draft` because the
removal address is an owner-owed string with no value in the corpus. That is the
owner's standing posture for this wave — an order that needs customer-visible
strings renders keys and the keys stay owner-owed (WO-070's pattern) — not an
unmet gate, so the row is carried as an open `rests-on` and the replacement is
self-certified below.

**The problem-card and DIY-method modules are slots here (rule 1.1).** WO-068
carried `depends-on: WO-066` and filled `BUILD.md` §4.1 modules 3 and 4 directly.
WO-066 is REQ-009's and JN-001 step 8's, outside this wave (rule 7.4), so those
two modules become named, absent-safe slots exactly as modules 2, 5 and 6 already
were in WO-068's own plan. Cost of reversing: two slot props and the `depends-on`
edge.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `src/app/(public)/scan/[domain]/_address/removal.tsx` | NEW | WO-067 | Two exports. `RemovedView` renders the `{ kind: 'removed' }` arm: one written line saying the report was removed at the domain owner's request, naming the removal address, served with HTTP 200 (the status is WO-069's to set; this file renders the body). `RemovalAddressLine` renders the same address for the foot of every report, so criterion 1 and criterion 3 name one address from one key. Both resolve `copy()` keys; neither composes an address from parts, and neither contains a string literal a person reads. |
| `tests/app/scan-address/removal.test.tsx` | NEW | WO-067 | The one-key, one-address, no-writer and no-control suites named in the test plan. |
| `src/app/(public)/scan/[domain]/_address/report-view.tsx` | NEW | WO-068 | `ReportView`, over WO-280's `{ kind: 'report' }` arm. Renders (1) the verdict strip: domain, measurement date, score and band through `renderMeasured` and `SCORE_BANDS`, and **one written line** resolved from `LIMITING_LINES[verdict.limiting.factor]` where `limiting.kind === 'factor'` and **no line at all** where it is `none` — the three driver mini-bars are removed (BP-024 decision 6, the owner's ruling of 2026-09-03) and no factor value renders anywhere; (2) at most one `AddressNotice`, as a total switch; (3) exactly one `AddressControl`, as a total switch, `none` rendering nothing; (4) the module slots in `BUILD.md` §4.1's order, **all of modules 2 to 6 as named, absent-safe slots** — module 3's problem cards and module 4's DIY sections are REQ-009's and WO-066's, outside this wave (rule 7.4), and this WO supplies none of them; (5) the copy-link control; (6) `RemovalAddressLine` at the foot. Contains no string literal a person reads and no branch on payment, session or tier. |
| `tests/app/scan-address/report-view.test.tsx` | NEW | WO-068 | The verdict, notice, control, order and no-paywall suites named in the test plan. |
| `src/app/(public)/scan/[domain]/page.tsx` | NEW | WO-069 | The route: derive the network key from `x-forwarded-for`, call `canonicalRedirect` and issue a 308 where it returns a target, otherwise call `resolveAddress` and hand the state to the view switch. Sets `noindex` as both a meta tag and an `X-Robots-Tag` header on every response, and sets no CDN cache. Serves the `removed` arm with HTTP 200. Renders no module itself. |
| `src/app/(public)/scan/[domain]/_address/view.tsx` | NEW | WO-069 | The total switch over `AddressState`: `malformed`, `scanning`, `starting`, `refused` and `cooldown` are rendered here; `report` delegates to WO-068's `ReportView` and `removed` to WO-067's `RemovedView`. The `starting` arm renders the progress shell and posts `/api/scan` on first frame — never during server render. The `scanning` and `starting` arms subscribe to WO-281's SSE stream and swap in the report without a reload. Every line is a `CopyKey`. |
| `tests/app/scan-address/route.test.tsx` | NEW | WO-069 | The status, header, redirect and no-session suites. |
| `tests/app/scan-address/view.test.tsx` | NEW | WO-069 | The totality, per-arm rendering and progress-swap suites. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-067

Consumes: WO-280's `AddressState` (`{ kind: 'removed' }` arm) and its `report`
arm; BP-019's `copy()` and `CopyKey`; BP-018's registered components.

Exposes:

```tsx
// src/app/(public)/scan/[domain]/_address/removal.tsx
export function RemovedView(): JSX.Element          // the { kind: 'removed' } arm's body
export function RemovalAddressLine(): JSX.Element   // REQ-002 c1, at the foot of every report
```

Both are BP-022's own composition of BP-018 primitives over BP-019 keys; BP-022's
`## Public interface` declares the state union, not the views, so these two
signatures are this WO's and are named here so WO-068 and WO-069 can consume
them without re-deriving anything.

Copy keys referenced (created in BP-019's registry by that node, **not here**):
one key for the removal address itself, one for the removed-report line, one for
the line that names the address on a live report. This WO references them; it
supplies no value for any of the three.

### From WO-068

Consumes: WO-280's `AddressState`, `AddressControl`, `AddressNotice`; WO-277's
`Verdict`, reached through BP-012's declared `StoredReport.verdict` (decision 3,
2026-08-31 — the blob has a shape now, and this file reads it by field rather
than through a reader interface of its own); WO-277's `sectionOutcome`; WO-066's
cards and `MethodSections`; WO-067's `RemovalAddressLine`; BP-019's `copy()`,
`renderMeasured()`, `SCORE_BANDS`; BP-018's registered components. Per
`design/components.md`, the strip is a composition over registered rows and
mints nothing: the band is `Badge` (required text child), the limiting-factor
line is body text and no chart at all — the three `Progress` mini-bars the
earlier plan named are removed with the bars themselves (BP-024 decision 6) —
the copy-link control is `Btn`,
and `ReportView` is named there as a screen composition that "must not become" a
registry row.

Exposes:

```tsx
// src/app/(public)/scan/[domain]/_address/report-view.tsx
export function ReportView(p: {
  state: Extract<AddressState, { kind: 'report' }>
  slots: {                                  // sibling nodes' modules, absent-safe
    aiAnswers?: JSX.Element
    googlePresence?: JSX.Element
    problems?: JSX.Element
    methods?: JSX.Element
    freePage?: JSX.Element
    pricing?: JSX.Element
  }
}): JSX.Element
```

The module order, verbatim from `BUILD.md` §4.1: "1. **Header strip** (card):
domain · date · category · score (big mono, band badge) · three driver mini-bars ·
Copy link. 2. **Two equal cards, side by side** … 3. **Three problem cards**
(grid) … 4. **DIY collapses** (3) … 5. **Free page card** … 6. **Pricing card**".

`BUILD.md` §4.1 is quoted above as it stands; its "three driver mini-bars" is
**superseded** by REQ-004 criterion 2 and BP-024 decision 6 (the owner's ruling
of 2026-09-03). The strip renders the score, the band badge and one written line,
and no driver bar. The order of the six modules is unaffected.

The `slots` prop above gains `problems?: JSX.Element` and `methods?: JSX.Element`
for modules 3 and 4, on the same absent-safe terms as the other four: WO-066
supplies them in the wave that builds REQ-009, and nothing in this WO does.

### From WO-069

Consumes: WO-280's `resolveAddress`, `canonicalRedirect`, `AddressState`;
WO-057's `networkKeyOf`; WO-281's SSE route; this WO's own `RemovedView` and
`ReportView`; BP-019's `copy()`; BP-018's registered components.

Exposes (verbatim from BP-022 `## Public interface`):

```
Routes
  GET  /scan/{domain}                       the one public report address
```

## Steps

**WO-067's steps** (renumbered; order within the block unchanged):

1. Produce the preview artifact at the path in the display bullets above and hold
   in draft until it is signed off (rule 7.3).
2. Write the failing suites first.
3. Implement `RemovalAddressLine` over one address key and one framing key. The
   address is resolved by `copy()` and is never concatenated, templated from a
   domain, or read from `env`.
4. Implement `RemovedView` reusing `RemovalAddressLine`, so one key serves both
   surfaces and the address on the removal line and the address on a report can
   never differ.
5. Render no control on `RemovedView`: no re-scan, no retry, no landing field,
   no form.
6. Add the product-wide no-writer assertion: `domain_blocks` is written by no
   file under `src/`.

**WO-068's steps** (renumbered; order within the block unchanged):

7. Produce the preview artifact at the path in the display bullets above and hold
   in draft until it is signed off (rule 7.3).
8. Write the failing suites first.
9. Render the verdict strip from the stored `Verdict`: `scoreAndBand` through
   `renderMeasured`, the band word from `SCORE_BANDS[handle]`, and the one line
   from `LIMITING_LINES` where `limiting.kind === 'factor'`. The three factors
   each named and each rendered through `renderMeasured`. On the `unmeasured`
   arm render the dash, render no band element at all, and render the one line
   naming every missing factor with its reason's key.
10. Render `notice` as a total switch over `AddressNotice | null` — at most one
   line, ever.
11. Render `control` as a total switch over `AddressControl` — `none` renders
   nothing; every other arm renders exactly one control.
12. Render the modules in order, omitting a slot that is absent and rendering a
   section that `sectionOutcome` reports `present: false` as absent with one
   written line naming it — never an empty card and never a spinner.
13. Render the copy-link control, whose value is the canonical address and which
   carries no token and no expiry.
14. Render `RemovalAddressLine` at the foot of every report.

**WO-069's steps** (renumbered; order within the block unchanged):

15. Produce the preview artifact at the path in the display bullets above and hold
   in draft until it is signed off (rule 7.3).
16. Write the failing suites first.
17. Implement the route: network key, `canonicalRedirect` → 308, else
   `resolveAddress` → view switch.
18. Set `noindex` as a meta tag and as an `X-Robots-Tag` header on every response
   of this route, including the redirect, the removal line, the malformed line,
   the refusal, the cooldown and the progress shell. Read `decisions/ADR-002.md`
   before touching this: the `noindex` policy and the absent sitemap are that
   decision's, and this node implements them.
19. Implement the switch as a total `switch` over `AddressState.kind` with a
   `never` default, so a new arm fails the build.
20. Implement the `starting` arm: render the progress shell server-side, and post
   `/api/scan` from the client on first frame. Nothing is posted during server
   render.
21. Implement the progress swap: subscribe to `/api/scan/{scanId}/progress`, and
   on the `ending` event replace the progress view with the report without a
   reload.
22. Set no CDN cache header on this route: the `refused` and `scanning` arms
   depend on the visitor, so the render is not shared.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| REQ-002 c1 — "Given any report, when it renders, then it names in writing the address to which a request to remove the report about that domain is sent, readable by a visitor with no account, session, or payment." | WO-067 | `removal/address · every report names the address, from one key` — `RemovalAddressLine` renders on every `report` arm fixture; the rendered address is asserted equal to `copy()` of the single removal-address key, and a source assertion that key is referenced in exactly two places in `src/` — `RemovedView` and `RemovalAddressLine` — and that no other file in `src/` contains an address literal. And `removal/address · no session is read` — the component is asserted to read no cookie, no session and no header. **This test fails until the owner writes the address**: the key has no value in the corpus (this WO's first `rests-on` row), and that failure is the intended signal, not a defect in this WO. |
| REQ-002 c3 — "Given a domain whose report has been removed, when its report address is opened, then it shows the removed report to nobody and starts no scan for anyone, on that visit or any later one, and in its place one written line says the report was removed at the domain owner's request and names the address criterion 1 names — until a request to that same address asks for that domain to be scannable again." | WO-067 | `removal/view · one line, naming the same address, and nothing else` — `RemovedView` renders exactly one written line plus the address line; its subtree is asserted to contain no report content, no score, no band, no control and no form. And `removal/view · the address is the same address` — the address rendered by `RemovedView` is asserted identical to the one `RemovalAddressLine` renders on a live report, by both resolving the same key. |
| REQ-001 c18 — "Given a domain whose report was removed at its owner's request (REQ-002 criterion 3), when its report address is opened, then no stored report is shown, no re-scan or retry control is offered, and no scan starts — criteria 12 to 17 offer no route back to a removed report." | WO-067 | `removal/view · no control, no route back` — the rendered tree holds no button, link or form that starts a measurement; asserted by enumerating every interactive element in the subtree and finding none. The state-level half of this criterion is WO-280's. |
| REQ-002 c4 — "Given a domain whose owner has made no removal request, when its report address is opened, then no report is withheld this way: a report is taken down only by a removal request received at the address criterion 1 names, never at ReachKit's own initiative." | WO-067 | `removal/no-writer · nothing in the product writes domain_blocks` — a repository-wide source assertion that no file under `src/` contains an insert, update, upsert or delete against `domain_blocks`, and that no migration outside BP-002's `*_domainblocks*.sql` mentions the table. This is the structural discharge BP-022 names: the promise is kept by the absence of a writer, not by a permission check. |
| REQ-002 c2 — "Given a removal request for a domain received at the address criterion 1 names, when it is granted, then within 5 working days of its receipt the report is no longer readable at its address." | WO-067 | Not testable in code, and named here rather than left silent: the grant is a person's act (this WO's second `rests-on` row). What *is* tested is the effect — `removal/view · a domain_blocks row removes the report on the next read` — inserting a row by hand makes the address resolve `removed` on the very next request, with no cache to invalidate and no deploy. The five-working-day window is an operational promise recorded in ADR-002, not a code path. |
| REQ-004 c1 — "Given a completed report whose score could be computed, when it opens, then the first module shows a single score on a 0–100 scale, its band as one of the four written words — Invisible, Hard to find, Findable, Dominant — the domain measured, and the date it was measured." | WO-068 | `report-view/verdict · the first module carries score, band, domain and date` — the first rendered module holds exactly one score numeral, one band word resolved from `SCORE_BANDS`, the domain and the measurement date; asserted that no second score appears anywhere in the tree. |
| REQ-004 c2 — "Given a score that could be computed, when it renders, then one written line beside it names the one driver, of the three the score is composed of, whose measurement holds the score down the most — and names it without stating that driver's value, its weight, or how the score is put together. No driver's own value is shown anywhere on the report — the counts REQ-009's three problem cards carry are measurements of their own, not driver values, and this criterion does not touch them. Where the score could not be computed, criterion 3 governs the line instead." | WO-068 | `report-view/verdict · one line, no value, no bar` — with `limiting: { kind: 'factor', factor }`, exactly one line renders, resolved from `LIMITING_LINES[factor]`, and it is asserted to contain no numeral, no percentage and no bar element; with `limiting: { kind: 'none' }`, no line renders at all. A source assertion that `report-view.tsx` contains no `Progress` import and reads no `factors` field — the field does not exist on `Verdict` (BP-024 decision 6), so a bar is unrepresentable rather than tested for. And a rendering assertion that the strip holds exactly one score numeral and no second number anywhere. |
| REQ-004 c3 — "Given a report whose score could not be computed (criteria 6 and 9), when it opens, then the first module shows "—" in place of the score, shows no band word at all, still names the domain and the date, and carries one written line saying the verdict could not be reached, naming every driver that has no value, and stating for each which of the two reasons applies — that the input behind it could not be determined (criterion 6) or that the scan stopped before reaching it (criterion 9) — in the words that criterion gives the case, and never calling a driver the scan never attempted a missing one. No band is stated, implied, or guessed from the drivers that were measured." | WO-068 | `report-view/verdict · an unmeasured score shows a dash and no band` — with `scoreAndBand` unmeasured: a dash renders in the score position, no element resolving any `SCORE_BANDS` key appears anywhere in the tree, domain and date still render, and one line renders naming each entry of `missing` with the key for *its own* reason. A mutation test swapping the two reason keys fails. |
| REQ-004 c4 — "Given the band, when it renders, then it is conveyed in words and not by colour alone." | WO-068 | `report-view/verdict · the band survives colour removal` — with all colour stripped from the rendered tree, the band word is still present and readable; a badge carrying a tone and no word fails. |
| REQ-004 c5 — "Given a free report, when it renders, then no part of the score, the band or the line criterion 2 requires is hidden, blurred, rounded down, locked, or marked as available on payment; the free report shows the whole verdict." | WO-068 | `report-view/verdict · nothing is hidden and nothing can be` — a source assertion that `report-view.tsx` contains no `tier`, `paid`, `locked`, `blur`, `upgrade` or `entitle` identifier and reads no session; and a rendering assertion that the strip's markup is identical for two fixtures that differ only in the presence of a paid site record. |
| REQ-004 c10 — "Given a section whose data could not be retrieved, when the report renders, then that section is absent and one written line names the missing section and states that it could not be measured — never an empty card, a persistent spinner, or placeholder content." | WO-068 | `report-view/sections · an unretrievable section is absent and named` — for each `SectionName` reported `present: false`, that module is absent from the tree and one written line naming it renders in its place; asserted that no empty card, skeleton or spinner element renders anywhere. |
| REQ-004 c11 — "Given a partially failed scan, when the report renders, then every part that was measured is shown in full and the report is otherwise usable." | WO-068 | `report-view/sections · a partial report still renders everything measured` — a fixture with one section absent and the score unmeasured still renders the problem cards, the method sections, the remaining slots and the copy-link control. |
| REQ-001 c16 — "… and exactly one control that starts a new measurement is offered beside it: the retry, which is the offer criterion 14 or criterion 15 leaves standing — or, where the measurement that failed was a market correction, the one retry of that correction (REQ-094 criterion 7) — never a second control alongside it." | WO-068 | `report-view/control · exactly one, or none` — over all four `AddressControl` arms, the count of measurement-starting controls in the rendered tree is exactly 1 for `rescan`, `retry` and `correction_retry`, and exactly 0 for `none`; asserted by enumerating controls, not by looking for a known label. |
| REQ-001 c14 — "… then one written line says it is incomplete and names what was not measured, and a re-scan control is offered beside that line however new the report is …" | WO-068 | `report-view/notice · at most one line, and the incomplete line names what was missing` — over all three `AddressNotice` arms plus `null`, the count of notice lines is exactly 1 or 0; the `incomplete` arm renders one line naming each `UnmeasuredPart`, beside the one control. |
| REQ-001 c7 — "Given a visitor on a completed report, when they use the copy-link control, then the copied address renders the same report, with the same measurements and the same measurement date, for a different person on a different device." | WO-068 | `report-view/copy-link · the copied value is the canonical address, bare` — the control's value equals the canonical `/scan/{domain}` address, carries no query string, no token and no expiry, and is identical across two renders under different network keys. |
| REQ-002 c1 — "Given any report, when it renders, then it names in writing the address to which a request to remove the report about that domain is sent, readable by a visitor with no account, session, or payment." | WO-068 | `report-view/removal · every report foot names the address` — `RemovalAddressLine` renders on every report fixture, including the incomplete, refused-notice and failed-notice ones. The address's value is WO-067's concern and is owner-owed. |
| REQ-001 c5 — "Given a report address for a well-formed domain, when a visitor opens it directly, then what loads is always one of three readable things — a stored report, a scan in progress, or one written line saying why there is no report for them to read — and never a blank page, a 404, or an unhandled error." | WO-069 | `route/status · every arm answers 200 with a body` — over every `AddressState` arm, the route responds 200 (or 308 on the redirect path) with a non-empty rendered body; no fixture produces a 404, a 500 or an empty document. And `view/switch · the switch is total` — a `never` default makes an unmapped arm a build failure, asserted by a compile-error fixture. |
| REQ-001 c4 — "Given a report address whose domain segment is not a well-formed public domain name, when it is opened, then one written line names what is wrong with it and the visitor is offered the landing field — never a blank page, an unhandled error, or a scan." | WO-069 | `view/malformed · one line and the landing field` — for each `DomainProblem`, the page renders one written line resolved from that problem's key and a landing field; asserted no scan was claimed, by spy. |
| REQ-001 c2 — "… then they arrive at the single public report address for that domain; no two of those forms produce different addresses or different stored reports." | WO-069 | `route/redirect · a non-canonical segment 308s once` — each non-canonical written form responds 308 to the canonical address; following the redirect once reaches a 200 and no second redirect. |
| REQ-001 c8 — "Given any report address, when a search engine or other crawler requests it, then the page tells it not to index the page, and no sitemap the product publishes lists any report address; the report stays reachable by its link to anyone who has it." | WO-069 | `route/noindex · every response carries noindex twice` — every arm's response carries the `X-Robots-Tag: noindex` header *and* the meta tag, including the 308; and `route/noindex · no sitemap names a report address` — a repository-wide assertion that no sitemap source under `src/` emits a `/scan/` path. And `route/noindex · the link still works` — a request carrying no referrer and no cookie still receives the report. |
| REQ-001 c6 — "Given the report address for a domain, when a visitor with no session, no cookie, and no prior visit opens it, then the full report is readable without signing in or paying." | WO-069 | `route/no-gate · no session, no cookie, no gate` — the route sets no `Set-Cookie`, reads no session, and renders the full report for a bare request; a source assertion that neither file imports an auth or account module. |
| REQ-001 c9 — "… then a scan for that domain is already underway with no further action from the visitor …" | WO-069 | `view/starting · the post happens on first frame, never on the server` — server-rendering the `starting` arm makes no call to `/api/scan`, asserted by spy; mounting the client component issues exactly one post; mounting it twice issues one further post that joins the running scan rather than starting a second. |
| REQ-003 c1 — "Given a scan is running — however it started … when the visitor watches the page, then it shows named stages that advance as work completes, never an unlabelled spinner or an indeterminate bar alone." | WO-069 | `view/scanning · named stages, never a bare spinner` — the `scanning` arm renders one labelled element per `StageName` from copy keys, advancing on stage events; a heartbeat event changes no stage label; asserted that no indeterminate progress element renders without a stage label beside it. |
| REQ-003 c3 — "Given a scan that completes while the visitor is watching, when it finishes, then the report replaces the progress view without the visitor reloading." | WO-069 | `view/scanning · the ending swaps in the report in place` — feeding the SSE stream a `report` ending replaces the progress view with `ReportView` in the same document, with no navigation and no reload, asserted by watching for a location change and finding none. |
| REQ-003 c12 — "Given a visitor refused under criterion 6 or 7 at the report address of a domain with no stored report, when the address loads, then it shows that refusal in writing and starts no scan — never a blank page, a 404, or an error (REQ-001 criterion 5) …" | WO-069 | `view/refused · the refusal is written at the address, and nothing starts` — for each refusal reason, the `refused` arm renders one line from that reason's key, with an elapsed duration where the refusal carries one and none where it does not; asserted 200, and asserted by spy that no scan was claimed. |
| REQ-001 c16 — "Given a domain whose scan failed without producing a report less than 24 hours ago, when its report address is opened, then one written line says what happened and a manual retry control is offered, and no scan starts by itself." | WO-069 | `view/cooldown · one line, one retry, nothing automatic` — the `cooldown` arm renders one line and exactly one retry control; asserted that no scan is claimed on load and that no timer re-attempts. |
| REQ-002 c3 — "Given a domain whose report has been removed, when its report address is opened, then it shows the removed report to nobody and starts no scan for anyone …" | WO-069 | `route/removed · 200, the line, and no scan` — the `removed` arm responds HTTP 200 (never 404 and never 410), renders WO-067's `RemovedView`, and claims no scan; asserted also that the response carries `noindex`. |

## Out of scope
- The removal address's **value** — the owner's, and outstanding. This WO
  creates the reference and the test and writes no string.
- The `domain_blocks` table and its migration — BP-002's.
- Deciding that a domain is removed — WO-057's admission step and WO-280's row 3.
- The address resolution and the control/notice derivation — WO-280.
- The problem cards and the DIY sections — WO-066.
- The AI-answers, Google-presence, free-page and pricing modules — sibling
  nodes'; this WO renders their slots and supplies none of them.
- The resolution order and the control derivation — WO-280.
- The SSE transport — WO-281; the start endpoint — WO-062.
- The landing page — WO-070.
- The AI-answers, Google-presence, free-page, problem-card, DIY-method and
  pricing modules — sibling nodes'; this WO renders their slots and supplies none
  of them.

## Rollback
Delete `src/app/(public)/scan/[domain]/page.tsx`, `…/_address/view.tsx`,
`…/_address/report-view.tsx`, `…/_address/removal.tsx` and their four suites. The
address stops being served; nothing else depends on this WO.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022, BP-024 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-067, WO-068, WO-069.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-067, WO-068, WO-069 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.
- 2026-09-04 preview — design-guardian — v1 — https://claude.ai/code/artifact/ffc95f9a-3b37-4dd0-b246-d152c428276a — drawn ahead of implementation, before any of this WO's own files exist (`src/lib/presentation/**` and `src/lib/measure/**` checked absent), against BP-022, BP-024, BP-019/BP-020 and REQ-001/002/003/004/093 as they stand on disk; dispatched in parallel with W3's Must builds (`src/lib/egress/`, `src/lib/costs/`, `src/lib/presentation/generated/`, three other implementers, disjoint directories). All seven `AddressState` arms drawn; five `components.md` §1 rows used (`Card`, `Badge`, `Alert`, `Btn`, `Steps`), all already `approved` — no new component or token proposed, no row flipped. Duty-2 self-review: PASS, one non-blocking gap reported (§7's copy keys for the notice/control/copy-link text have no fixed `CopyKey` name anywhere in the corpus yet — distinct from the `verdict.limiting.*`, `unmeasured.*` and `removal.*` keys, which are already named). URL returned to the dispatcher for the owner's word; no `Signed-off:` line written here — that is the librarian's, from the owner's ruling.
- 2026-09-04 re-planned — planner — `risk: normal` set (doctrine 0.13.2, rule 2b): no charter seam — rendering and header assertions; the silent half is WO-280's. Front-matter and the one-line assessment in `## Goal` are the whole change; no scope, file plan, interface, criterion or test-plan row is touched.
- 2026-09-04 signed-off — librarian — sign-off read back from https://claude.ai/code/artifact/ffc95f9a-3b37-4dd0-b246-d152c428276a v1

## Validation report (appended by validator)
