---
id: WO-252
type: work-order
title: "The four measured quantities — BUILD.md §5's driver arithmetic as four pure functions"
status: draft
ui: no
implements: [BP-010]
depends-on: [WO-001, WO-006, WO-052, WO-251]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31 — this repository carries no `src/`). Every path in this file plan is NEW and its placement is provisional against a repo with no code."
    disposition: open
  - claim: "BP-005's `constants.ts` carries the answerability floor. BP-010 decision 2 assigns it there by name and `BUILD.md` §5 fixes its value: \"Answerability = shape of the home + measured pages, 0–100, floored at 1\". BP-005's `## Public interface` as it stands on disk declares no pin carrying it. Same stop condition as WO-251's, restated under `## Interfaces` because this file is where the floor is applied."
    disposition: open
  - claim: "The remaining coefficients of `BUILD.md` §5 — 25, 0.55, 0.45 and 4 in SearchPresence, 0.4 and 0.6 in AIPresence, and the cube root — are **formula shape, not pins**, and belong in this file. BP-010 decision 2 names exactly three things as BP-005's (\"band thresholds, the answerability floor, the direct-answer character window\") and puts the rest here: \"the formula's shape is code here, covered by a fixture suite per driver\". Chosen on that sentence (rule 1.1); reversal is moving six numbers into `constants.ts` and one line into `tests/pins.test.ts`."
    disposition: open
supersedes: []
---

# WO-252 — The four measured quantities — `BUILD.md` §5's driver arithmetic as four pure functions

- Branch: wo/WO-252-drivers
- Estimate: ≤1 day

## Goal
Compute BP-010's four measured quantities — Foundations, Answerability,
SearchPresence and AIPresence — from counted facts and vendor rows, each as a
`Measured<number>`, with `BUILD.md` §5's formulas as the whole of the
arithmetic and no I/O of any kind.

## Provenance of this file plan
**This repository holds no `src/`.** Both paths are NEW. `src/lib/measure/` is
BP-010's row in `structure.md` and its `code:` globs are `src/lib/measure/**`
and `tests/measure/**`. `drivers.ts` is named by no leaf: BP-024's `code:`
enumerates five other files and `tests/measure/verdict/**`, so this file and
`tests/measure/domain/**` are the component's under `structure.md` rule 2.

**Reuse check (rule 7.2).** `capabilities.md`'s row "The three shown score
factors, the score and its band" is BP-024's `factorsOf` · `presenceOf` ·
`computeScore` · `bandOf`, shipped by WO-054 — those take `Drivers` and
`ScoreFactors` as *given* and are downstream of this file. There is no index row
and no work order for the four measured quantities themselves; BP-010's decision
3 states the separation ("keep four fields — they are what the parsers produce")
and WO-054's own `## Interfaces` consumes `Drivers` as "another node's file".
This work order produces what WO-054 consumes; it duplicates nothing.

**Status.** Left `status: draft` — written and not self-certified in the same
pass. A later audit sets the status against §3's gate list.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/measure/drivers.ts` | **NEW** | The whole public interface below: `foundationsOf`, `answerabilityOf`, `searchPresenceOf`, `aiPresenceOf`. `BUILD.md` §5's formulas transcribed, and nothing else — no fetch, no vendor call, no clock read (`at` is a parameter), no persistence. Every function returns `Measured<number>` through WO-052's constructors and combinators; none returns a bare `number` and none has a branch that reaches a raw number out of an `unmeasured` arm. `foundationsOf` returns a **measured 0** — never `unmeasured` — for a document that was read and carries a generic `noindex` (BP-010 `## Error & edge behavior`; REQ-004 c7). `answerabilityOf` applies BP-005's floor, and applies it only to a `measured` or `zero` arm. `searchPresenceOf` and `aiPresenceOf` return `unmeasured` with the reason their input carried, folded by WO-052's `worseReason`, and never estimate from the part that was measured. The four numbers of §5's presence formulas (25, 0.55/0.45/4, 0.4/0.6) live here with `BUILD.md` §5 quoted in a comment beside each; the answerability floor does not (third and second `rests-on` rows). |
| `tests/measure/domain/drivers.test.ts` | **NEW** | A fixture suite per driver, as BP-010 decision 2 requires ("covered by a fixture suite per driver"), plus the boundary and mutation cases named in the test plan. |

## Interfaces

**Consumes** (verbatim from the nodes named):
```ts
// BP-010 · src/lib/measure/measured.ts (WO-052)
export type Measured<T> =
  | { kind: 'measured';   value: T; at: Date }
  | { kind: 'zero';       value: T; at: Date }
  | { kind: 'unmeasured'; reason: 'undeterminable' | 'not_attempted'; at: Date }
// and WO-052's constructors and total combinators `combine`, `mapMeasured`, `worseReason`.

// BP-010 · src/lib/measure/parse.ts (WO-251)
export interface OnPageFacts { /* … the counted facts; see WO-251 */ }

// BP-006 · src/lib/egress/robots.ts (WO-020)
export interface RobotsPolicy {
  ok: true; origin: string; readAt: Date
  disallowsAll: boolean
  disallowedAgents: Readonly<Record<string, boolean>>
  sitemaps: readonly string[]
  absent: boolean
}

// BP-008 · src/lib/vendors/dataforseo/types.ts (WO-023)
export interface RankedRow { /* … a ranked keyword row */ }
export interface SerpResult { /* … organic top-10 AND the ai_overview item with its reference domains */ }

// BP-005 · src/lib/config/constants.ts — the pin BP-010 decision 2 assigns there
export const SCORING: { directAnswerCharsMin: 40; directAnswerCharsMax: 320; answerabilityFloor: 1 }
```

> **Stop condition — the answerability floor.** `BUILD.md` §5 fixes it verbatim:
> "Answerability = shape of the home + measured pages, 0–100, **floored at 1**",
> and BP-010 decision 2 puts it in BP-005. If `SCORING.answerabilityFloor` (or a
> differently-named pin carrying the same number) is absent from
> `src/lib/config/constants.ts` when this work order starts, the implementer
> **stops and logs `failed — next: BP-005 carries no pin for BUILD.md §5's
> answerability floor, which BP-010 decision 2 assigns to it — reported by the
> planner 2026-08-31`**. They do **not** write `1` into `src/lib/measure/`
> (`structure.md` rule 5) and do **not** add the pin to `constants.ts`, which is
> BP-005's file (rule 7.1). The presence coefficients are a different case and
> are settled by the third `rests-on` row: they belong here.

**Exposes** — the four measured quantities. Their composed record type `Drivers`
is BP-010's and is declared in `src/lib/measure/index.ts` by WO-253, whose
`measureDomain` composes these four; this file imports nothing from there, so
the two do not form a cycle.

```ts
/** `BUILD.md` §5: "Foundations = access gates + clarity signals, 0–100
 *  (generic noindex on the home document ⇒ 0, and score ⇒ 0)". A document we
 *  read that tells every reader not to index it is a **measured** 0, never an
 *  `unmeasured` — REQ-004 c7's case, and the reason both booleans exist on
 *  `OnPageFacts`. A document we could not read is `unmeasured`. */
export function foundationsOf(a: {
  onPage: OnPageFacts | null            // null = the home document could not be read
  robots: Measured<RobotsPolicy>
  at: Date
}): Measured<number>

/** §5: "Answerability = shape of the home + measured pages, 0–100, floored at 1
 *  · shape = (questionShaped + directAnswers + evidenceDensity) / 3", where
 *  `questionShaped` = question-shaped headings ÷ all headings × 100,
 *  `directAnswers` = direct-answer headings ÷ all headings × 100, and
 *  `evidenceDensity` is the saturating log curve over
 *  (numerals + dates + outboundCitations) per 1,000 visible characters.
 *  Empty denominators read 0, never null. */
export function answerabilityOf(a: {
  pages: readonly OnPageFacts[]         // empty = nothing was read
  at: Date
}): Measured<number>

/** §5: "SearchPresence = min(100, 25 × log10(ranked + 1)) ×
 *  (0.55 + 0.45 × min(1, top10share × 4))". Zero ranked rows is a legal result
 *  and a measured 0 (`BUILD.md` §6.3: "`ranked_keywords`@50 → the customer's own
 *  presence (0 rows is a legal result)"), not an `unmeasured`. */
export function searchPresenceOf(a: {
  ranked: Measured<readonly RankedRow[]>
  at: Date
}): Measured<number>

/** §5: "AIPresence = max(1, (0.4 × mentionRate + 0.6 × citationRate) × 100)".
 *  `BUILD.md` §6.3: "Brand mentions/citations = string match over references."
 *  The rates are over the SERPs whose AI-overview item was measured — never over
 *  the twelve asked for, because a SERP we could not read is not a SERP that
 *  named nobody. Every SERP `unmeasured` yields `unmeasured`, never 0. */
export function aiPresenceOf(a: {
  serps: readonly Measured<SerpResult>[]
  ownDomain: string
  at: Date
}): Measured<number>
```

## Steps
1. Read `src/lib/config/constants.ts` for the answerability floor. If the pin is
   absent, take the stop condition above.
2. Write `tests/measure/domain/drivers.test.ts` first and run it; every case must
   fail on a missing module (constitution §8, "Watch it fail first").
3. Implement `foundationsOf`. Order the branches so the read/not-read question is
   answered before the gate question: `onPage === null` is `unmeasured` with the
   reason the caller carried; `onPage.noindex && onPage.noindexAppliesToEveryReader`
   is `zero` with value 0; everything else is the access-gate and clarity-signal
   composition over `OnPageFacts` and `RobotsPolicy`.
4. Implement `answerabilityOf` over the three sub-measures, dividing by 3, then
   applying the pin's floor — and applying it only to a `measured` or `zero`
   arm, so an `unmeasured` answerability can never surface as 1.
5. Implement `searchPresenceOf` and `aiPresenceOf` with §5's formulas quoted
   beside each coefficient.
6. Route every arithmetic step through WO-052's `combine`, so no branch can
   reach a raw number from an `unmeasured` arm.
7. Mutate to prove the suite discriminates: (a) make the generic-noindex case
   return `unmeasured` instead of `zero` and confirm the REQ-004 c7 row fails;
   (b) apply the answerability floor to the `unmeasured` arm and confirm the
   floor row fails; (c) drop the `min(1, …)` from the top-10 share term and
   confirm the saturation row fails.

## Test plan

> **Criterion source: the blueprint and `BUILD.md`, not a requirement.** BP-010
> carries `satisfies: []` and its status grounds say it was cut from `BUILD.md`
> §5, not from one requirement; `covers:` is not an authorising ancestor
> (rule 5.4). **No REQ criterion is inherited here**, and none is paraphrased
> into one. Where a row cites REQ-004 it is quoting BP-010's own sentence about
> it, not the requirement.

| Acceptance criterion (source quoted) | Test |
|---|---|
| `BUILD.md` §5: "Foundations = access gates + clarity signals, 0–100 (generic noindex on the home document ⇒ 0, and score ⇒ 0)" | `tests/measure/domain/drivers.test.ts › a read document with a generic noindex is a measured 0, not an unmeasured` — the fixture returns `{ kind: 'zero', value: 0 }`; the targeted-noindex fixture does not. Discriminates: returning `unmeasured` for the generic case makes the report show "—" where BP-010 requires a 0 with a band word. |
| BP-010 `## Error & edge behavior`: "Empty denominators read 0, never null (`BUILD.md` §5): a page with no headings scores `zero`, not `unmeasured`." | `… › no headings is a zero, not an unmeasured` — `answerabilityOf` over an `OnPageFacts` with `headings: 0` returns the `zero` arm; a type-level case that no arm of any of the four functions returns a bare number. |
| `BUILD.md` §5: "Answerability = shape of the home + measured pages, 0–100, floored at 1 · shape = (questionShaped + directAnswers + evidenceDensity) / 3" | `… › the shape is the mean of exactly three sub-measures` — three fixtures each isolating one sub-measure at 100 with the others at 0 return 100/3 to the same precision; a fourth term added to the mean changes all three and fails. And `… › the floor applies to a measured zero and never to an unmeasured` — a `zero` answerability floors to 1; an `unmeasured` one stays `unmeasured` and carries no value. |
| `BUILD.md` §5: "SearchPresence = min(100, 25 × log10(ranked + 1)) × (0.55 + 0.45 × min(1, top10share × 4))" | `… › SearchPresence saturates rather than clipping` — a top-10 share of 0.25 and of 0.9 both give the multiplier 1.0; a share of 0 gives 0.55; ranked counts of 0, 1, 10, 10_000 and 1_000_000 are asserted against the formula, and the last is capped at the `min(100, …)`. Discriminates: dropping either `min` fails one of the two ends. |
| `BUILD.md` §6.3: "`ranked_keywords`@50 → the customer's own presence (**0 rows is a legal result**)" | `… › zero ranked rows is a measured presence of zero` — a `Measured` `zero` of an empty row array returns the `zero` arm, not `unmeasured`; an `unmeasured` row set returns `unmeasured` carrying the same reason. Discriminates: treating an empty array as unmeasured makes a cold-start site's score withhold rather than read 0, which is REQ-091's whole case. |
| `BUILD.md` §5: "AIPresence = max(1, (0.4 × mentionRate + 0.6 × citationRate) × 100)" | `… › AIPresence weights citations above mentions and floors at 1` — a fixture with mentions only and one with citations only give 40 and 60 for equal rates; an all-zero fixture returns 1, not 0. And `… › the rates are over the SERPs measured, not the SERPs asked for` — twelve SERPs of which four are `unmeasured` and one names the customer give a citation rate of 1/8, never 1/12. Discriminates: dividing by 12 silently understates presence for every partial scan. |
| BP-010 `## Error & edge behavior`: "`computeScore` returns `unmeasured` if any driver it composes is `unmeasured`, and never estimates from the drivers that were measured (REQ-004 criteria 3 and 9)." — **the driver half of it** | `… › an unmeasured input never yields a number` — for each of the four functions, an `unmeasured` input returns `unmeasured` carrying the fold of its inputs' reasons under `worseReason`, and never a value. A source assertion that no `??`, `\|\|` or `?:` in this file supplies a default number for an `unmeasured` arm. |
| BP-010 decision 2: "the formula's shape is code here, covered by a fixture suite per driver" | `… › each driver has its own fixture suite` — the file declares four named `describe` blocks, one per driver, and a meta-assertion that each holds at least one boundary case. |

## Out of scope
- Parsing HTML — WO-251. This file takes `OnPageFacts` as given.
- Fetching anything, buying any vendor row, and reading any clock — WO-253.
- `Drivers`, the composed record — WO-253's `src/lib/measure/index.ts`.
- `factorsOf`, `presenceOf`, `computeScore`, `bandOf` and `verdictOf` — BP-024's
  (WO-054, WO-055). This file produces the four inputs those take.
- Adding either pin to `constants.ts` — BP-005's file; see the stop condition.
- Any migration, schema or write.

## Rollback
Delete both files. WO-253 imports them, so roll that back first if it has landed.

## Log
- 2026-08-31 created — planner

## Validation report (appended by validator)
