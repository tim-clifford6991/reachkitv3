---
id: WO-172
type: work-order
title: "The draft read view: the whole page, its grounding, its claim badge, and what happens if you do nothing"
status: approved
ui: yes
implements: [BP-044, REQ-045]
depends-on: [WO-041, WO-043, WO-169, WO-206, WO-207]
blocked-by: []
rests-on:
  - claim: "The two authorship labels, the four claim-state lines and the do-nothing line are owner-owed strings in BP-019's registry; this WO renders keys and tests them, never the strings"
    disposition: open
supersedes: []
---

# WO-172 — The draft read view: the whole page, its grounding, its claim badge, and what happens if you do nothing

- Branch: wo/WO-172-draft-read-view
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-172.html
- Signed-off: <date>

## Goal
Render every word that would publish, the grounded fact marked in the text with
its source line, the claim outcome in every case, the generated-content label,
and the do-nothing box with approve, edit and veto available without leaving the
view.

## File plan
> Every path below is **NEW** (no `src/` on disk, checked 2026-08-31). Placement
> follows `structure.md` and BP-044's `code:` globs; provisional.

| File | New/Modify | Change |
|---|---|---|
| `src/app/(account)/app/draft/[draftId]/page.tsx` | NEW | Server component: reads `readDraft`, renders the full body, the authorship label through BP-019's `generatedLabel`, the grounded-fact mark with its URL and read date, the claim badge for all four states, and the do-nothing box. |
| `src/app/(account)/app/draft/[draftId]/DraftRead.tsx` | NEW | The read surface: the marked grounded passage, the claim badge, and the action row — approve and veto post to the publishing node's routes and this view re-reads; which are offered follows the same projection from BP-015's `TRANSITIONS` that BP-039 decision 2 describes, so the two surfaces cannot offer different actions for one state. |
| `tests/app/draft/read-view.test.tsx` | NEW | Full body, the label in both states, the grounding line, the four claim states, the do-nothing box, the offered actions. |

## Interfaces
Consumes (verbatim from BP-044 `## Public interface`):
```ts
export interface DraftView {
  draftId: string
  title: string
  bodyMd: string                                             // every word that would publish
  authorship: Authorship
  grounded: { fact: string; url: string; readAt: Date; present: boolean }  // criteria 2 and 8
  claim: ClaimState
  doNothing: { key: CopyKey; publishesAt: Date | null }      // criterion 4, from becomesPublishable
  lastSavedAt: Date | null
}
export function readDraft(draftId: string): Promise<DraftView>
```
and `generatedLabel()` / `copy()` (BP-019), the registered component set
(BP-018), `TRANSITIONS` (BP-015/BP-045) for the action projection.

Exposes: the `/app/draft/{id}` route. No new engine interface.

## Steps
1. **Precondition (rule 7.3):** signed preview. As of 2026-08-31
   `sdlc-factory/docs/design/tokens.md` is `Status: ready` and `components.md`
   registers 24 components, every row `proposed` and reaching `approved` only
   when a preview carrying it is signed off. The claim badge, the generated-
   content label and the source chip are all `Badge`, each with its required
   text child (`components.md` §4, "not gaps"); this WO mints no component.
2. Write the failing tests first (§8).
3. Render `bodyMd` in full: no truncation, no "show more" on the body.
4. Mark the grounded passage inside the text and render its URL and read date
   beside it; where `present` is false, the mark is absent — the fact is not
   rewritten to fit.
5. Render the claim badge for `passed`, `failed` (naming the matched entry),
   `outstanding` (no outcome shown) and `nothing_to_check`.
6. Render the authorship label from `Authorship` — two states, page-level; it
   never presents the customer's own words as text ReachKit generated.
7. Render the do-nothing box from `doNothing`, and the approve / edit / veto
   controls from the transition projection, all without leaving the view.
8. Mutation check: truncate the body at a character budget and confirm the
   full-body test fails; render `passed` while `claim` is `outstanding` and
   confirm the badge test fails.

## Test plan
| Acceptance criterion (verbatim from REQ-045) | Test |
|---|---|
| 1. Given a draft awaiting review, when the customer opens it, then every word that would publish is rendered for them to read, with nothing withheld or summarised, and what ReachKit wrote is labelled here as that page's generated content, in the form REQ-093 criterion 2 requires of it wherever it appears. Where the customer has edited the draft, that label never presents their own words as text ReachKit generated (REQ-093's non-goal on text the customer writes themselves). | `tests/app/draft/read-view.test.tsx › the rendered body contains every word of bodyMd, with no truncation and no show-more`, `(same file) › the generated-content label renders for an unedited draft and renders its edited form once the customer has edited, never claiming their words` |
| 2. Given a draft is rendered for review, when the customer reads it, then the fact it is grounded in is marked within the text and shown with the URL it was read from and the date it was read. | `tests/app/draft/read-view.test.tsx › the grounded passage is marked within the text and its URL and read date render beside it`, `(same file) › with present false no mark renders and the fact is not rewritten` |
| 3. Given a draft is rendered for review, when the customer reads it, then the outcome of the check against their do-not-claim list is stated — including where they have recorded no entries, in which case it states that there was nothing to check against. | `tests/app/draft/read-view.test.tsx › each of passed, failed, outstanding and nothing_to_check renders its own badge, and outstanding shows no outcome` |
| 4. Given a draft awaiting review, when the customer reads it, then they are told what will happen if they do nothing, and can approve, edit or veto without leaving the view. | `tests/app/draft/read-view.test.tsx › the do-nothing box renders its line and its publish time`, `(same file) › approve, edit and veto are all reachable without navigating away, and the offered set matches the transition projection the calendar's day panel uses for the same state` |
| 11. Given the check fails on an edited draft, whoever wrote the matching text, when the check completes, then a claim re-check stands outstanding on that draft until one passes against the text as it then stands, so the draft does not become publishable (REQ-057 criterion 2); it is never regenerated, and it is returned to the customer naming the entry it matched, with their own text intact, so no draft is ever held without the customer being told which entry held it. | `tests/app/draft/read-view.test.tsx › a failed claim state renders the matched entry, so the customer is told which entry held the draft` |

## Out of scope
- The editor, the live preview, the unsaved indicator and the copy-out controls
  (WO-173).
- The read model (WO-169), the autosave (WO-170), the export route (WO-171).
- Approve, veto and skip themselves — the publishing node's routes.
- Reproducing a customer's WordPress theme (REQ-060), images or media,
  comments, suggestion mode, version history — REQ-045's non-goals.
- Typography, highlight styling, badge tone, two-column versus tabbed layout —
  design system.
- Any customer-visible string; keys only.

## Rollback
Revert the branch; the view is a read-only render.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-044 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-045 as it stands on disk.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — step 1 now states the real design-corpus status (tokens.md ready, 24 components registered `proposed`) replaces the stale draft/empty claim in step 1, and names Badge as the registered home of the claim badge, authorship label and source chip.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 4 cross-block depends-on edges added (WO-041, WO-043, WO-206, WO-207); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
