---
id: WO-023
type: work-order
title: "DataForSEO transport, credentials and the never-list enforced by absence"
status: approved
wave: W4
ui: no
risk: high
implements: [BP-008]
depends-on: [WO-006, WO-276]
blocked-by: []
rests-on:
  - claim: "`Measured<T>` is BP-010's (`src/lib/measure/`, per `registry/capabilities.md`) and does not exist in this repo. BP-008's whole public interface returns it. This work order declares a local type-only import from `@/lib/measure` and cannot type-check until BP-010's work order lands; the type is consumed, never re-declared here (rule 7.1). The type fan-out check the planner would normally run — make the type edit alone, run the project's type-check, take the erroring files as the plan — could not be run: the repo holds no TypeScript to check."
    disposition: confirmed
    reason: "Implementer, 2026-09-04 (rule 2.3b). `Measured<T>` exists now — `src/lib/measure/measured.ts`, shipped by WO-277 (consolidating WO-052) under BP-024, not BP-010 as this row assumed; the discriminated union and its constructors are exactly what BP-008's interface returns. The type-fan-out check the row said could not be run, ran: `index.ts` imports `Measured` from `@/lib/measure/measured` (type-only) and `npm run typecheck` passes clean over the whole repo including this WO's three new files and its test. The row's blocking condition — no `Measured<T>` to type-check against — no longer holds; consumed, never re-declared, as the row itself required. Re-confirmed independently, validator, 2026-09-04 (TST-039, rule 2.3b): read `src/lib/measure/measured.ts` directly — exports `Measured<T>`, `measured()`, `measuredZero()`, `unmeasured()` — and `src/lib/vendors/dataforseo/index.ts` line 41, `import type { Measured } from \"@/lib/measure/measured\"`; `npm run typecheck` re-run clean over the whole repo on this session's own pass."
supersedes: []
---

# WO-023 — DataForSEO transport, credentials and the never-list enforced by absence

- Branch: wo/WO-023-dataforseo-transport
- Estimate: ≤1 day

## Goal
Build the one typed boundary to DataForSEO — transport, credentials, error
mapping and the shape every endpoint function is written to — so that a call the
product may not make has no function to call.

**Risk: high — seams: money, and data leaving the system.** This file is the only place the DataForSEO credential is read and the only place an outbound call to a paid third party is made, and `00-project.md`'s cost envelope is enforced in exactly this kind of seam. Its central promise is negative — the never-list is enforced by a function's *absence*, so a call the product may not make has nothing to call — and a test that survives adding an endpoint function leaves that promise vacuous while every added call bills. Mutation-tested (doctrine 0.13.2, rule 2b).

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW, anchored to
`structure.md`'s row for `src/lib/vendors/**` and BP-008's `code:` globs.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/vendors/dataforseo/transport.ts` | NEW | The one request builder. Credentials from `env` (`DATAFORSEO_LOGIN`, `DATAFORSEO_PASSWORD`), never appearing in a log, a payload or an error message. Every call is fixed to `SERP_LOCATION` (Google US / en) and depth 10 — both read from `constants.ts`. `mode` is a required argument on every call it builds; there is no default. Not exported outside the module. |
| `src/lib/vendors/dataforseo/types.ts` | NEW | `RankedRow`, `SuggestionRow`, `CompetitorRow`, `SerpResult` (organic top-10 **and** the `ai_overview` item with its reference domains), `AiAnswer`. Product types, not vendor payload shapes. |
| `src/lib/vendors/dataforseo/index.ts` | NEW | The module's exported surface: exactly BP-008's six functions, re-exported from WO-024 and WO-025. Created here empty of implementations so the closed list is one readable file from the first commit. |
| `tests/vendors/never-list.test.ts` | NEW | The tests below. |

## Interfaces
Exposes (verbatim from BP-008 `## Public interface`, the surface this work order
fixes and the next two fill):
```ts
// Every call takes the CostContext; none can be made outside one.
export function rankedKeywords(c: CostContext, a: { domain: string; rows: 50|100|300 }): Promise<Measured<RankedRow[]>>
export function keywordSuggestions(c: CostContext, a: { seed: string; rows: 50 }): Promise<Measured<SuggestionRow[]>>
export function competitorsDomain(c: CostContext, a: { domain: string }): Promise<Measured<CompetitorRow[]>>
export function serpOrganic(c: CostContext, a: { query: string; mode: 'live'|'std' }): Promise<Measured<SerpResult>>
  // SerpResult carries organic top-10 AND the ai_overview item with its
  // reference domains — the free AI matrix rides here at 0c extra
export function aiMode(c: CostContext, a: { query: string; mode: 'live'|'std' }): Promise<Measured<AiAnswer>>       // paid battery only
export function llmScraper(c: CostContext, a: { query: string; mode: 'std' }): Promise<Measured<AiAnswer>>          // paid battery only

// Every call is fixed to SERP_LOCATION (Google US / en) and depth 10.
```
Consumes: `CostContext` (BP-007), `Measured<T>` (BP-010, type-only), `env` and
`PRICE_BOOK`/`SERP_LOCATION` (BP-005).

## Steps
1. Write `types.ts` as product types.
2. Write `transport.ts` with credentials, location and depth fixed.
3. Write `index.ts` declaring the six exports.
4. Write the never-list test; watch it fail first by adding a generic
   `dataforseo(endpoint, params)` export.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-008 carries
> `satisfies: []` and was cut from `BUILD.md` §6.1 to §6.4.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-008 error behaviour: "**The never-list is enforced by absence**: SERP depth > 10, search operators, clickstream flags, `load_async_ai_overview`, Labs historical endpoints, per-rival `ranked_keywords` on the free path, a fourth engine, per-draft re-probing and AI Keyword Data have no function to call." | `tests/vendors/never-list.test.ts` — asserts the module's exported function set is exactly the six named, and that no exported signature admits a depth argument, an operator string, a clickstream flag or `load_async_ai_overview`. Adding a generic caller fails it. |
| BP-008 decision 1: "a generic `dataforseo(endpoint, params)` with a runtime allow-list — rejected" | Same file — asserts no export takes an endpoint name. |
| BP-008 public interface: "Every call is fixed to SERP_LOCATION (Google US / en) and depth 10." | Same file — asserts every request the transport builds carries `SERP_LOCATION` from `constants.ts` and depth 10, with no way to override either. |
| BP-008 error behaviour: "Credentials come from BP-005's `env` and never appear in a log, a payload or an error message." | Same file — forces a transport error and asserts neither credential appears in the thrown value, the outcome, or the log record. |
| BP-008 decision 2: "no default; every call site states whether a human is waiting" | Same file — a type-level assertion that `serpOrganic` and `aiMode` cannot be called without `mode`. |

## Out of scope
- The three Labs endpoint implementations — WO-024.
- The three SERP and AI endpoint implementations — WO-025.
- Any parsing of a real vendor payload against a real account.

## Rollback
Delete `src/lib/vendors/dataforseo/**` and the test.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-008 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote BP-008's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 4 files in the file plan (4 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-006); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-04 finished — planner — /wave propose: **W4** (first in the row, the owner's ruling of 2026-09-04), MoSCoW **Must**; `risk: high` set with its seam in `## Goal` (money · data leaving the system, doctrine 0.13.2 rule 2b). Ordering ground for W4: declaration-first — this order declares `SerpResult`, one of the ten types `StoredReport` composes, and WO-286 is `blocked-by` it for exactly that.
- 2026-09-04 started — implementer
- 2026-09-04 finished — implementer — `transport.ts` (request builder, credential read, `sendRequest`), `types.ts` (five product types) and `index.ts` (six-function stub surface, signatures matched to *current* BP-008 post-ADR-094 rather than this WO's own stale `## Interfaces` text — flagged in `index.ts`'s header, rule 4.2) built; `tests/vendors/never-list.test.ts` (9 cases + type witnesses) watched to fail first (module-not-found, then unused-`@ts-expect-error`), then to pass; mutation probes on all 5 test-plan rows confirmed discrimination, including the never-list absence probe (added a `dataforseo(endpoint, params)` export, watched 2 tests die, reverted) and a credential-leak probe that the first version of the test missed (base64 `Authorization` header leaked into a log line passed a plaintext-only check; test strengthened to check the encoded token too, then caught it) — full mutation detail in the implementer's return. `npm run typecheck` and scoped `eslint` clean; full suite 51 files / 1292 passed / 1 skipped (baseline 50/1283/1 plus this WO's 1 file / 9 tests, no regressions); `npm run lint` unchanged at 8115, none in this WO's files. Ready for validation.

## Validation report (appended by validator)

## TST-039 — WO-023 validation report

### Scope
Independent verification of WO-023 on `wave/W3` tip `3caa2ae` (merge commits `7404bc0`/`ef34567`). Read: this WO, BP-008, ADR-094, `tests/vendors/never-list.test.ts`, `src/lib/vendors/dataforseo/{transport,index,types}.ts`. Risk: high — mutation-tested per doctrine 0.13.2.

### Criteria (WO-023 `## Test plan`, 5 rows)
| # | Criterion (source) | Test | Verdict |
|---|---|---|---|
| 1 | BP-008: never-list enforced by absence | `never-list.test.ts` exact-six-exports + no-forbidden-argument type witnesses | PASS — discriminates (see mutation below) |
| 2 | BP-008 decision 1: no generic `dataforseo(endpoint, params)` | forbidden-name + no-endpoint-argument witness | PASS — discriminates |
| 3 | BP-008: every call fixed to `SERP_LOCATION`/depth 10 | `buildRequest` fixed-fields + override-is-ignored | PASS — discriminates |
| 4 | BP-008: credentials never in a log/payload/error | plaintext + base64 `SECRETS` scan across reject/4xx/success | PASS — discriminates (see mutation below; this is the test the implementer's own mutation pass caught vacuous and strengthened) |
| 5 | BP-008 decision 2: `mode` required, never defaulted | `@ts-expect-error` witnesses, `serpOrganic`/`aiMode`/`llmScraper` | PASS — discriminates (typecheck-enforced) |

### Mutation testing (risk: high, doctrine 0.13.2 rule 2b) — re-run independently, not taken on report
1. **Added a generic `dataforseo(endpoint, params)` export** to `index.ts` — killed exactly 2 tests (exact-six-exports, forbidden-name), as claimed. Reverted; tree confirmed clean.
2. **Logged `request.headers` on the failure path** in `transport.ts` (the exact regression the implementer's own build found vacuous against the original, plaintext-only credential check) — killed 2 tests in the *current*, strengthened suite (both the reject-path and 4xx-path credential checks), confirming the base64-aware `SECRETS` array genuinely catches what the pre-strengthening version missed. Reverted.
3. **Widened `serpOrganic`'s `mode` to optional** — `npm run typecheck` fails on an unused `@ts-expect-error` directive (`tests/vendors/never-list.test.ts:157`). Reverted.
4. **Let caller-supplied `location_name`/`language_code`/`depth` win over the pins** (`??=` instead of strip-then-set) — killed the override test (`location_name` observed `"Nowhere"` instead of the pin). Reverted.
All four probes killed exactly the test(s) their own row names; nothing survived deletion. No vacuous test found on this pass beyond the one already caught and fixed during the build itself (row 4's history).

### Structure / scope
File plan followed exactly: `transport.ts`, `types.ts`, `index.ts`, `tests/vendors/never-list.test.ts` — no file outside the plan touched (`git show ef34567 --stat` confirmed). `eslint.config.mjs:151-154`'s `no-fetch-outside-egress` carve-out for `src/lib/vendors/**` and `BP-006.md`'s own line excluding BP-008's vendor client both verified directly — the second egress path is sanctioned, not smuggled.

### Rule 4.2 judgement — building against current BP-008, not the WO's own stale `## Interfaces`
Confirmed legitimate. `ADR-094` (`status: accepted`, read directly) amended `serpOrganic` on 2026-09-03 — after this WO's own text was cut (2026-08-31) — to add the required `loadAsyncAiOverview: boolean` as the never-list's one admitted exception. Rule 2.4 makes the blueprint the one home for this fact; a WO's inlined transcription of it is not a second authority. The implementer flagged the deviation once, in `index.ts`'s own header, per rule 4.2's "once, in one place" — not re-argued in the test file, the commit message, or here. Judged correct, not a defect.

### `rests-on` disposition (rule 2.3b)
One row, already `confirmed` by the implementer with grounds stated; independently re-verified in this pass (see front-matter edit above): `Measured<T>` exists at `src/lib/measure/measured.ts`, imported type-only at `index.ts:41`, `npm run typecheck` clean. No row left `open` on WO-023 itself.

### Gates (independently re-run this session, not taken on report)
- `npm run typecheck`: clean.
- `npm test` (full suite, `--project node --project ui --project db`): **57 files / 1406 passed / 1 skipped** — unchanged from this session's own measured baseline on this tip.
- `npm run lint`: **8115** problems, unchanged from baseline, none in this WO's files.
- `factory-console --check`: 0 errors / 59 warn / exit 0, unchanged from baseline.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict
pass

Regression: 24 files · none re-checked — pass

`git diff --stat 3caa2ae..bb9373e -- src/ tests/` is empty: no `src/`/`tests/` file has moved since TST-039's own tip; everything after it is docs (validation reports, `rests-on` dispositions, blueprint amendments). Full suite independently re-run this session, 7 runs on tip `bb9373e`: 6/7 clean at 57 files / 1406 passed / 1 skipped; 1/7 showed a single failure consistent with the documented intermittent `tests/egress/safe-fetch.test.ts` timer-tolerance flake (load-induced, not in this WO's files). `npm run typecheck` clean; `npm run lint` 8115, none in `src/` or `tests/` (`grep` over the full lint output, zero hits under either path); `npm run build` clean against a throwaway, gitignored `.env.local` of schema-valid placeholders (deleted immediately after, tree confirmed byte-clean), `NANO_API_KEY` omitted to exercise its fallback to `ANTHROPIC_API_KEY`. `factory-console impact WO-023`: 24 files → 1 blueprint (BP-008) → 0 importers → 0 requirements — BP-008 carries `satisfies: []`, matching this WO's own `## Test plan` header note, so no requirement's acceptance criteria have implementing code here to regress. No `done` work order's governed files appear in the impact set outside WO-023's own three; none disturbed.

## Log
- 2026-09-04 finished — validator — TST-039: every test-plan row discriminates under live mutation (re-run, not taken on report), rule 4.2 deviation against ADR-094 judged correct, one `rests-on` row confirmed and closed. No open row remains on WO-023 itself.
- 2026-09-04 finished — validator — /regress (W4 wave-close pass): full suite (7 independent runs), typecheck, lint and build re-run clean on tip `bb9373e`; `factory-console impact WO-023` re-checked; no code has moved since TST-039's own tip. Regression: pass.
