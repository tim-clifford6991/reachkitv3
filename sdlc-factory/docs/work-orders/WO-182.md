---
id: WO-182
type: work-order
title: "Winnability bars, banding, and the counts that may satisfy them"
status: approved
ui: no
implements: [BP-040, REQ-047]
depends-on: [WO-006, WO-052, WO-181]
blocked-by: []
rests-on:
  - claim: "BP-052 writes `report.rivalSizes = RivalSize[]` into the scan blob, each entry carrying `rankedCount` for a rival domain; BP-052's own open row records that a count at the 100-row fetch cap may be a floor rather than a count, which is the condition `rankedCountFrom` coerces on."
    disposition: open
  - claim: "BP-005's `## Public interface` declares `export const WINNABILITY: { qualifyFloor: 500; qualifyMultiple: 5; nearFloor: 100; nearMultiple: 2 }` and `tests/pins.test.ts` asserts it, so the four numbers this work order needs already have exactly one home and are imported, never re-declared."
    disposition: confirmed
supersedes: []
---

# WO-182 — Winnability bars, banding, and the counts that may satisfy them

- Branch: wo/WO-182-winnability
- Estimate: ≤1 day

> **Every path in the file plan is NEW.** There is no source code on disk — the
> repo holds `BUILD.md`, `DATA-COSTS.md` and this factory (`00-project.md`:
> "Greenfield. No source code on disk yet") — so no path below was verified
> against an existing file. Placement follows `sdlc-factory/docs/registry/structure.md`'s
> module map and this node's `code:` globs, and is provisional against a repo
> with no code.

> **Runner and type-check.** The test runner is unchosen (`00-project.md`:
> "Test runner is unnamed in the spec — `tests/pins.test.ts` implies one.
> System to choose at M1 and record as an ADR"). This WO adds test files at the
> paths named and chooses no runner. `factory.config.json` sets no `typeCheck`
> key, so the type-check command is derived from the project's own tooling —
> greenfield Next.js + TypeScript (`structure.md` header) — as `npx tsc
> --noEmit`; every compile-time assertion below is a failure of that command.

## Goal
Implement the two bars, the three-band assignment, and the coercion that stops an unmeasured or capped ranked count from ever satisfying a bar — with `qualifies` and `bandWinnability` shaped so that a per-customer threshold has no argument to arrive through.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/opportunities/winnability/bars.ts` | NEW | `qualifyingBar` and `winnableBar`, computed from BP-005's `WINNABILITY` — `qualifyingBar(r) = Math.max(WINNABILITY.qualifyFloor, WINNABILITY.qualifyMultiple * r)` and `winnableBar(r) = Math.max(WINNABILITY.nearFloor, WINNABILITY.nearMultiple * r)`. **The four numbers are imported from `src/lib/config/constants.ts` and declared nowhere in this file** (`structure.md` rule 5: a number that appears in two files is wrong). No local `QUALIFYING_FLOOR`, `WINNABLE_FLOOR` or any other module-local copy of a pin; no numeric literal for a bar anywhere under `src/lib/opportunities/winnability/`. No I/O, no clock, no configuration object. |
| `src/lib/opportunities/winnability/band.ts` | NEW | `qualifies` and `bandWinnability`, verbatim from the blueprint's signatures. A bar is satisfied only by a count whose `Measured` kind is `measured` or `zero`; `unmeasured` never satisfies one (BP-040 decision 2). `bandWinnability` returns `winnable` when some count satisfies `winnableBar`, `reach` when some count satisfies `qualifyingBar` and none satisfies `winnableBar`, and `not-yet` otherwise. |
| `src/lib/opportunities/winnability/counts.ts` | NEW | Two planner-named pure helpers the derive layer uses to build `qualifies`' argument without giving `qualifies` an option: `rankedCountFrom(a: { count: Measured<number>; rowCap: 50 | 100 | 300 }): Measured<number>` — coerces a `measured` count equal to `rowCap` to `unmeasured` with reason `undeterminable`, per BP-040 decision 2 ("a count that arrived at the row cap is coerced to `unmeasured` before comparison"); and `ownRankedFloor(m: Measured<number>): number` — `measured`/`zero` give the value, `unmeasured` gives 0. |
| `tests/opportunities/winnability/bars.test.ts` | NEW | Table tests over the two bars at `ownRanked` 0, 1, 20, 99, 100, 250, 251, 10_000; plus the property `winnableBar(r) < qualifyingBar(r)` over the whole non-negative range (a generated sweep, not three examples). One further assertion: each bar equals the arithmetic on `WINNABILITY`'s own members read from `constants.ts` at test time, so re-pinning `WINNABILITY` moves the bars and a re-declared local copy would leave this test green while the pin moved — the mutation the second-copy rule exists to catch. |
| `tests/opportunities/winnability/band.test.ts` | NEW | Band assignment across the boundary of each bar; an all-`unmeasured` top ten yields `not-yet` and satisfies no bar; a `zero` count satisfies both bars; a count at the row cap, after `rankedCountFrom`, satisfies neither; cold start (`ownRanked = 0`) still yields `winnable` and `reach` for the right inputs; the same target assessed with two different site fixtures returns the same band. |
| `tests/opportunities/winnability/shape.test.ts` | NEW | Compile-time assertions under `tsc --noEmit`: the parameter object of `qualifies` and of `bandWinnability` is exactly `{ top10RankedCounts: Measured<number>[]; ownRanked: number }` (a hand-rolled `Equals<A, B>` conditional type, so adding an optional `siteId` or `options` member fails the type-check), and each takes exactly one argument. |

## Interfaces
**Consumes** (verbatim from the blueprints named):
```ts
// BP-010 · src/lib/measure/
export type Measured<T> =
  | { kind: 'measured';   value: T; at: Date }
  | { kind: 'zero';       value: T; at: Date }
  | { kind: 'unmeasured'; reason: 'undeterminable' | 'not_attempted'; at: Date }

// WO-181 · src/lib/opportunities/types.ts
export type Winnability = 'winnable' | 'reach' | 'not-yet'   // internal handles

// BP-005 · src/lib/config/constants.ts — the only home of these four numbers
export const WINNABILITY: { qualifyFloor: 500; qualifyMultiple: 5; nearFloor: 100; nearMultiple: 2 }
```

**Exposes** (verbatim from the blueprint):
```ts
/** Pure. Takes no site id, no options and no configuration, so criterion 13
 *  ("the threshold applied is identical for both") holds by construction:
 *  there is no argument through which a per-customer threshold could arrive. */
export function qualifyingBar(ownRanked: number): number      // max(500, 5 * ownRanked)
export function winnableBar(ownRanked: number): number        // max(100, 2 * ownRanked)
export function qualifies(a: { top10RankedCounts: Measured<number>[]; ownRanked: number }): boolean
export function bandWinnability(a: { top10RankedCounts: Measured<number>[]; ownRanked: number }): Winnability
```

BP-040 states the source of the four numbers in the same block, verbatim:

```
/** The four numbers are BP-005's `WINNABILITY`
 *  ({ qualifyFloor: 500, qualifyMultiple: 5, nearFloor: 100, nearMultiple: 2 }),
 *  imported, never re-declared here — `structure.md` rule 5. */
```

## Steps
1. Write all three test files first and run them: every one must fail on a missing module.
2. Create `bars.ts`. **Import `WINNABILITY` from `src/lib/config/constants.ts` and declare no number in this file.** The four are REQ-047 c9's and c10's own (`the greater of 500 searches and five times`, `the greater of 100 searches and twice`) and BP-005 has held them since it was cut, as `WINNABILITY`; BP-040's public interface says so in the same block that declares the two bars. Declaring them again here — the shape a previous cut of this work order carried, as `QUALIFYING_FLOOR`/`WINNABLE_FLOOR` — is the second copy of a pin `structure.md` rule 5 forbids, and it is invisible: both copies read 500 and 100 on the day they are written. Cost of reversing a retune: one edit to `constants.ts` and one to `tests/pins.test.ts`, both BP-005's — nothing here, and nothing stored. `bars.test.ts`'s property sweep fails immediately if `WINNABILITY.nearFloor` is raised to 500, which is the guard BP-040's error-and-edge section names.
3. Create `band.ts`. The `Measured` kind check comes first, so an `unmeasured` count cannot reach a numeric comparison at all.
4. Create `counts.ts`. `qualifies` takes no cap argument by design (that is what makes c13 structural), so the cap coercion happens here, before the argument is built.
5. Run the tests. Then mutate: make `unmeasured` satisfy a bar and confirm the c9 test fails; drop the `rowCap` coercion and confirm the capped-count test fails; add `siteId?: string` to `qualifies`' parameter and confirm `tsc --noEmit` fails.

## Test plan
> Watch it fail first (constitution §8): every test below is written and
> run against the absent implementation before the implementation exists.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-047 c9** Given a candidate target for a new page, when it is assessed, then it qualifies only if at least one domain currently in its top ten ranks for no more than the greater of 500 searches and five times the customer's own measured count. | `band.test.ts › qualifies passes only where a measured count is at or under qualifyingBar` and `bars.test.ts › qualifyingBar is max(500, 5 x ownRanked)` |
| **REQ-047 c10** Given an assessed target, when its winnability is recorded, then it carries exactly one of three bands: Winnable, where a domain in its top ten ranks for no more than the greater of 100 searches and twice the customer's own measured count — a site roughly their size already ranks there; Reach, where it passes criterion 9 but no domain that small is present; and Not-yet, where it fails criterion 9. A Not-yet target is never queued for a page. | `band.test.ts › the three bands at each bar boundary` (Winnable / Reach / Not-yet); the `not-yet` half of "never queued" is WO-183's |
| **REQ-047 c11** Given a customer who ranks for nothing, when targets are assessed, then qualifying targets are still produced. | `band.test.ts › at ownRanked 0 the bars are 500 and 100 and both winnable and reach remain assignable` |
| **REQ-047 c13** Given any two customers, when the same target is assessed for both, then the threshold applied is identical for both and follows only from measured counts. | `shape.test.ts › qualifies takes exactly { top10RankedCounts, ownRanked }` (compile-time) and `band.test.ts › two site fixtures, one target, one band` |
| **REQ-047 c14** Given the thresholds criteria 9 and 10 state, when targets are assessed for a customer at any measured ranked count, zero included, then each of the three bands remains reachable: no customer's count makes Winnable, Reach or Not-yet impossible to assign. | `bars.test.ts › winnableBar(r) < qualifyingBar(r) for every non-negative r` (property sweep) and `band.test.ts › each band is reachable at ownRanked 0, 50, 250, 10000` |

**Discriminates (mutation check, constitution §8).**
- Deleting the `Measured`-kind guard in `band.ts` makes the all-unmeasured test pass a bar and fails the c9 test — the test asserts the rejection, not merely the acceptance.
- Raising `WINNABILITY.nearFloor` to 500 in `constants.ts` makes the strict-inequality property fail for every `ownRanked` under 250, which is exactly the change REQ-047's second `rests-on` says would make the Reach band unreachable. It fails **only** while the bars read the pin: re-declaring the four numbers locally makes this mutation pass silently, which is why the file plan bans a local copy and `bars.test.ts` asserts the bars against `WINNABILITY`'s own members.
- Adding an optional member to either parameter object breaks `shape.test.ts`'s `Equals` assertion under `tsc --noEmit`; a test that only called the functions would survive that mutation, which is why the compile-time assertion is the test of record for c13.

## Out of scope
- `src/lib/config/constants.ts` and `tests/pins.test.ts` — BP-005's alone
  (`structure.md` rules 4 and 5). This WO imports `WINNABILITY` and hardcodes
  none of its members; if the pin is absent when work starts, the implementer
  adds nothing to that file and declares nothing locally — it logs `failed`
  naming the missing pin and stops (constitution rule 4.2).
- Every customer-visible string. This WO emits handles only; the words are the
  owner's and live in BP-019's copy registry (ADR-001), which this WO does not
  touch.
- The three words a customer reads for `winnable | reach | not-yet`. Ruled by the owner on 2026-08-31 and now in BP-019's `BAND_LABELS.winnability` (`winnable → band.winnability.winnable → "Winnable"`, `reach → band.winnability.reach → "Reach"`, `not-yet → band.winnability.notYet → "Not yet"`); BP-040's third `rests-on` is `confirmed`. This WO still emits handles only and creates no copy key — the rendering is BP-019's and BP-001's.
- Ranking and queueing a banded target — WO-183.
- Assembling `top10RankedCounts` from a report — WO-184.

## Rollback
Pure modules with no schema and no writes. Reverting the branch removes them; nothing consumes them until WO-184.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-040 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-047 as it stands on disk.
- **Bounded.** 6 files in the file plan (6 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — re-cut: the four winnability numbers are imported from BP-005's `WINNABILITY`, the module-local `QUALIFYING_FLOOR`/`WINNABLE_FLOOR` copies are deleted (`structure.md` rule 5), and the band-words out-of-scope bullet is discharged against BP-019's `BAND_LABELS`.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-006, WO-052); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
