---
id: WO-188
type: work-order
title: "The supply migration and unused-supply depth"
status: approved
ui: no
implements: [BP-041, REQ-095]
depends-on: [WO-011, WO-181]
blocked-by: []
rests-on:
  - claim: "BP-002's baseline has created `sites` and `scans` and installed the default-deny RLS policy; `db()` is exported from `src/lib/db/index.ts`."
    disposition: open
  - claim: "BP-012 records a completion time per scan that `exhaustedSince` falls back to for a site that has never held an open opportunity."
    disposition: open
supersedes: []
---

# WO-188 — The supply migration and unused-supply depth

- Branch: wo/WO-188-supply-depth
- Estimate: ≤1 day

> **Every path in the file plan is NEW.** There is no source code on disk — the
> repo holds `BUILD.md`, `DATA-COSTS.md` and this factory (`00-project.md`:
> "Greenfield. No source code on disk yet") — so no path below was verified
> against an existing file. Placement follows `sdlc-factory/docs/registry/structure.md`'s
> module map and this node's `code:` globs, and is provisional against a repo
> with no code.

> **Runner and type-check.** The test runner is unchosen (`00-project.md`:
> "Test runner is unnamed in the spec — `tests/pins.test.ts` implies one.
> System to choose at M1 and record as an ADR"). This WO adds test files at the
> paths named and chooses no runner. `factory.config.json` sets no `typeCheck`
> key, so the type-check command is derived from the project's own tooling —
> greenfield Next.js + TypeScript (`structure.md` header) — as `npx tsc
> --noEmit`; every compile-time assertion below is a failure of that command.

## Goal
Add the one column supply needs and implement `supplyDepth` — days of pages, which is open non-`fix` rows, and the date supply hit zero.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `supabase/migrations/20260901091000_opportunities_supply.sql` | NEW | Adds `status_changed_at timestamptz not null default now()` to `opportunities` and the `before update` trigger that maintains it when `status` changes. That is the only new column: no counter, no `supply_depth` column, no materialised view (BP-013 decision 2), and no `sites.supply_arrival_told_scan_id` (BP-041's data-model delta says why). |
| `src/lib/opportunities/supply/depth.ts` | NEW | `supplyDepth(siteId)` — `unused` is `count(*) where status = 'open' and family <> 'fix'`, served off WO-181's partial index; `exhaustedSince` is `max(status_changed_at)` over that site's opportunities when `unused` is 0, falling back to the completion time of the site's most recent completed scan where the site has never held one, and `null` whenever `unused > 0`. One indexed count, plus one `max()` in the exhausted case only. |
| `tests/opportunities/supply/depth.test.ts` | NEW | Against a migrated database: four open `unblock` rows and no others give `unused: 0`; a mix gives the non-`fix` count; `exhaustedSince` is null while `unused > 0`; it is the moment the last non-`fix` row left `open` when `unused` is 0; a site that never held one falls back to its latest completed scan's time; the count issues one query. |

## Interfaces
**Consumes** (verbatim from the blueprints named):
```ts
// BP-002 · src/lib/db/index.ts
export function db(): SupabaseClient<Database>        // request-scoped, RLS applies

// WO-181 · supabase/migrations/20260901090000_opportunities_core.sql
//   partial index `(site_id) where status = 'open'`; `family` in ('write','improve','fix')
```

**Exposes** (verbatim from the blueprint):
```ts
/** Unused supply = open, non-fix. `unblock` consumes no supply (REQ-047 c5),
 *  so it is never counted as a day of pages. */
export function supplyDepth(siteId: string): Promise<{
  unused: number
  exhaustedSince: Date | null              // set only while unused === 0
}>
```

## Steps
1. Write `depth.test.ts` first and run it: it must fail on a missing column and a missing module.
2. Write the migration. The trigger sets `status_changed_at = now()` only when `new.status is distinct from old.status`, so an unrelated update does not move the date `exhaustedSince` reads.
3. Implement `depth.ts`. The predicate `status = 'open' and family <> 'fix'` appears once in this file and is imported by everything else that reads depth (BP-041 decision 1; BP-013 decision 2's amendment).
4. Run the tests. Then mutate: drop `and family <> 'fix'` and confirm the all-`unblock` fixture reports four days of pages instead of zero.

## Test plan
> Watch it fail first (constitution §8): every test below is written and
> run against the absent implementation before the implementation exists.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-095 c5** Given a customer's unused supply falls below 7, when they open the app, then the screen they land on carries one written line saying supply is running short and how many days of pages remain; that line stands until unused supply is 7 or more and stops when it is; and where criterion 3's telling would also apply, the customer reads one statement of supply, not two. | `depth.test.ts › unused counts open non-fix rows only` — the count the short line states; the line itself is WO-190's |
| **REQ-095 c6** Given a customer's unused supply reaches zero, when they open the app, then the screen they land on states that the product holds nothing worth writing for them and since which date that has been true; the statement stands while it remains true, is restated after each weekly re-measurement that adds no supply, and stops when supply returns. A date rendering empty is never the only place a customer is told their supply is exhausted. | `depth.test.ts › exhaustedSince is the moment the last non-fix row left open` and `… falls back to the latest completed scan for a site that never held one` — the statement is WO-190's |

**Discriminates (mutation check, constitution §8).**
- Counting every open row makes the all-`unblock` fixture report four days of pages — the precise failure REQ-095 was written against, and the test asserts zero.
- Setting `status_changed_at` on every update rather than on a status change makes the `exhaustedSince` date move under an unrelated edit; the test updates a non-status column and asserts the date did not move.
- Adding a cached counter column would leave every test green until it drifted, which is why BP-013 decision 2 forbids one and the file plan states its absence.

## Out of scope
- `src/lib/config/constants.ts` and `tests/pins.test.ts` — BP-005's alone
  (`structure.md` rules 4 and 5). This WO imports the pins it needs and
  hardcodes none of them; if a pin named in `rests-on` is absent when work
  starts, the implementer adds nothing to that file — it logs `failed` naming
  the missing pin and stops (constitution rule 4.2).
- Every customer-visible string. This WO emits handles only; the words are the
  owner's and live in BP-019's copy registry (ADR-001), which this WO does not
  touch.
- `SUPPLY_TARGET_DEPTH` and `SUPPLY_SHORT_BELOW` — BP-005 pins consumed by WO-189 and WO-190, not by this WO.
- The three supply lines and any surface that renders them — BP-019's and BP-001's.

## Rollback
Revert the branch and apply a down migration dropping the trigger, its function and the column. `opportunities` rows are untouched by the rollback; nothing outside this node reads `status_changed_at`.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-041 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 2 rows of `## Test plan` quote REQ-095 as it stands on disk.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
