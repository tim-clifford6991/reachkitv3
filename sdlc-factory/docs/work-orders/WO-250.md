---
id: WO-250
type: work-order
title: "The presentation barrel: src/lib/presentation/index.ts, and the boundary test that keeps it the only door"
status: approved
ui: no
implements: [BP-019]
depends-on: [WO-001, WO-041, WO-042, WO-043, WO-045, WO-049, WO-248, WO-249]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code. This WO creates no repo-root file and no lint configuration."
    disposition: open
  - claim: "All four leaf modules expose their surface through an `index.ts` that re-exports 'the symbols listed under Exposes and nothing else': `copy/index.ts` (WO-041), `generated/index.ts` (WO-043), `place/index.ts` (WO-045), `stopped/index.ts` (WO-049). This barrel re-exports those four entry points rather than reaching into a leaf's internal files, so a leaf can move a symbol between its own files without touching this one. If any of the four ships without an `index.ts`, the implementer stops and says so rather than importing the leaf's internals (constitution rule 4.2)."
    disposition: open
  - claim: "No import path alias (`@/lib/presentation`) is assumed. The barrel is a relative-path module; whether consumers reach it by alias is BP-001's tsconfig decision and is not this WO's to make."
    disposition: open
supersedes: []
---

# WO-250 — The presentation barrel: `src/lib/presentation/index.ts`, and the boundary test that keeps it the only door

- Branch: wo/WO-250-presentation-barrel
- Estimate: ≤1 day

## Goal
Create `src/lib/presentation/index.ts` so BP-019's declared public interface is
one importable surface, and add the boundary test that fails when a consumer
reaches past it into a leaf or when two leaves claim one name.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`
> and no `tests/`. Placement follows `registry/structure.md`, whose
> `src/lib/presentation/**` row names this module's public entry points as
> `copy() · renderMeasured() · emptyStateLine() · stoppedWorkStatement() ·
> BAND_LABELS`. This file is the thing that makes that row true.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/presentation/index.ts` | **NEW** | The component's one public entry point. Re-exports the four leaf entry points — `./copy`, `./generated`, `./place`, `./stopped` — and this component's own two files, `./bands` and `./measured`. **Import and export statements only**: no declaration, no constant, no function, no type alias, no side effect. A file that contains logic is a fifth module hiding inside the door. |
| `tests/presentation/index.test.ts` | **NEW** | The reachability, no-collision, interface-completeness, direction and no-logic suites named in the test plan. |

**Not modified:** none of the four leaves' `index.ts` files, none of their
internals, `bands.ts`, `measured.ts`, `registry.ts`, any partition file, or any
test another work order owns. This WO writes two files and edits nothing.

## Interfaces

**Consumes:** the four leaf entry points and this component's own two modules —
`src/lib/presentation/copy/index.ts` (WO-041, WO-042),
`src/lib/presentation/generated/index.ts` (WO-043),
`src/lib/presentation/place/index.ts` (WO-045),
`src/lib/presentation/stopped/index.ts` (WO-048, WO-049),
`src/lib/presentation/bands.ts` (WO-248),
`src/lib/presentation/measured.ts` (WO-249). Nothing outside
`src/lib/presentation/`.

**Exposes:** BP-019's `## Public interface` in full, as one module surface. The
declarations themselves live in the six files above and are not restated here;
what this WO adds is that all of the following resolve from
`src/lib/presentation`:

```ts
export type CopyKey
export const COPY: Readonly<Record<string, string>>
export function copy(key: CopyKey, vars?: Record<string, string | number>): string

export function renderMeasured<T>(m: Measured<T>, o: {
  format: (v: T) => string
  unmeasuredLine: CopyKey
}): { text: string; isDash: boolean; line?: string }

export function emptyStateLine(place: PlaceKey): { line: string }
export function generatedLabel(a: { pageTitle: string; written: boolean }): { label: string }

export type { WorkStop } from './stopped/stop'
export { stoppedWorkStatement, nextPublishStatement, dayAccount } from './stopped/statement'

export const BAND_LABELS: {
  winnability: Record<'winnable' | 'reach' | 'not-yet', CopyKey>
  rivalSize:   Record<'near' | 'middle' | 'far', CopyKey>
}
export const SCORE_BANDS: Record<'invisible'|'hard-to-find'|'findable'|'dominant', CopyKey>
export const SEVERITY: readonly [CopyKey, CopyKey, CopyKey]
```

## Steps
1. Write `tests/presentation/index.test.ts` first and watch every case fail —
   there is no barrel to import (constitution §8).
2. Create `src/lib/presentation/index.ts` as six star re-exports and nothing
   else:

   ```ts
   export * from './copy'
   export * from './generated'
   export * from './place'
   export * from './stopped'
   export * from './bands'
   export * from './measured'
   ```

3. **Decision taken under rule 1.1 — star re-exports, not an enumerated list.**
   Two options. An enumerated list (`export { copy, COPY, COPY_META } from './copy'`, and so on for all six)
   states the surface here, but then every leaf that adds an export must also
   edit this file, which makes the barrel a shared file across six work orders
   and re-creates the collision BP-020 decision 5 was written to remove. Star
   re-exports keep exactly one author per file — the barrel names modules, the
   leaves name symbols — and turn a name claimed by two leaves into a signal
   rather than a silent win: under ES semantics an ambiguous star export is
   *dropped*, so the symbol simply stops resolving from the barrel, which the
   reachability test in step 5 catches by name. That dropped-name behaviour is
   also rule 7.1's "two implementations of one capability" arriving as a test
   failure with both leaves named. **Chosen: star re-exports plus a reachability
   test.** Cost of reversing: rewrite six lines as an enumerated list; no
   consumer changes, because the import specifier is unchanged either way.
4. **The two path notes BP-019 writes, reconciled.** BP-019's interface block
   names `'./stopped/stop'` and `'./stopped/statement'` directly. This barrel
   goes through `./stopped` instead — the leaf's own declared entry point
   (WO-049's file plan). The symbols reaching a consumer are identical; the
   difference is an internal module path, which is the system's under
   constitution §1's rights table, and it means WO-049 can move a symbol between
   `stop.ts` and `statement.ts` without editing a file this WO owns. Cost of
   reversing: two lines. Reported to the architect for BP-019's interface
   comment.
5. Write the reachability test: for each of the six modules, read its own
   `export` list and assert every exported name resolves from the barrel with the
   same binding. This is the assertion that catches an ambiguous star export
   being silently dropped — the one failure mode step 3's choice introduces.
6. Write the no-collision test: assert the six modules' export-name sets are
   pairwise disjoint, and that the union's size equals the barrel's export count.
   A name in two sets fails with both module paths in the message.
7. Write the interface-completeness test: assert each of the fourteen symbols in
   the `## Interfaces` block above resolves from `src/lib/presentation`, by name
   and by kind (value or type). This is the test that fails the day a leaf
   renames an export BP-019 declares.
8. Write the direction and no-logic tests per the test plan.

## Test plan

**No acceptance criterion is inherited here, and that is a statement rather than
an omission.** BP-019 carries `satisfies: []`; the three law requirements it
`covers` — REQ-091, REQ-092, REQ-093 — are satisfied by its feature children and
their criteria are discharged in WO-041..WO-050, and REQ-004's, REQ-009's,
REQ-047's and REQ-096's by WO-248 and WO-249. A barrel promises a customer
nothing. **The criterion source for every test below is the blueprint** —
BP-019's `## Module / boundary` and `## Public interface`, and
`registry/structure.md` rules 1, 6 and 7.1 — quoted in each row.

| Criterion (source: BP-019 / structure.md, quoted) | Test |
|---|---|
| BP-019 `## Public interface` — the fourteen declared symbols. | `presentation/index · BP-019's declared interface resolves from one module` — each of `CopyKey`, `COPY`, `copy`, `renderMeasured`, `emptyStateLine`, `generatedLabel`, `WorkStop`, `stoppedWorkStatement`, `nextPublishStatement`, `dayAccount`, `BAND_LABELS`, `SCORE_BANDS`, `SEVERITY` is importable from `src/lib/presentation` and is of the declared kind. Count assertion: the test states how many symbols it checked (rule 5.5). Discriminates: a leaf renaming any one of them fails with the name. |
| BP-019 `## Module / boundary` — "Three leaves own subtrees inside this boundary and this component owns none of their files". | `presentation/index · the barrel names modules, never a leaf's internals` — the barrel's import specifiers are exactly the six in Steps 2; no specifier reaches below a leaf's directory (`./copy/keys/bands`, `./stopped/stop`, `./place/render`). Discriminates: importing `./copy/registry` directly fails it — that is the reach-past that would let this file survive a leaf's internal move and then break silently. |
| `structure.md` rule 7.1 (constitution §7.1) — "One capability, one BP node, one module. Two implementations of one capability is a `blocked-by`, never a judgement call." | `presentation/index · no name is claimed by two leaves` — the six modules' export-name sets are pairwise disjoint; the barrel's export count equals the union's size. A collision fails naming both modules, and is raised as a `blocked-by` rather than resolved by import order. Discriminates: this is the test that makes the dropped-name behaviour of an ambiguous star export loud instead of silent. |
| `structure.md` rule 6 — "Dependency direction is one-way, `src/app/` and `src/jobs/` → `src/lib/` → `src/lib/{config,egress,costs,db}`. No `src/lib/` module imports from `src/app/`. A cycle between `src/lib/` modules is a build failure, not a review comment." | `presentation/index · the barrel imports nothing outward and closes no cycle` — read the transitive import graph of `src/lib/presentation/**` from source and assert no path under `src/app/`, `src/jobs/`, `src/lib/db/`, `src/lib/egress/` or `src/lib/llm/`, and no cycle among the six modules. The one outward edge the component is allowed — `src/lib/measure/measured.ts`, type-only, WO-249's — is asserted present and asserted type-only. Discriminates: a leaf pulling in a surface, or `copy/` importing `bands.ts` back, fails it. |
| BP-019 `## NFR budget` — "Zero runtime cost beyond a string lookup; no interpolation engine" and "This layer logs nothing; it is pure." | `presentation/index · the door holds no logic` — the barrel's source parses to import and export statements only: no function, no class, no constant, no type alias, no top-level call, no side effect. And importing the barrel performs no network, database, clock or console call, asserted by spies over the whole module load. Discriminates: any helper added "just here" fails it, which is how a barrel becomes a seventh module nobody owns. |

## Out of scope
- Every symbol the barrel re-exports. All six modules and their tests are other
  work orders' — WO-041, WO-042, WO-043, WO-045, WO-048, WO-049, WO-248,
  WO-249 — and this WO changes none of them.
- The string-literal sweep and the generated-text flow gate (WO-044), the
  cold-start route sweep (WO-047) and the stopped-state sweep (WO-050). Those
  enumerate surfaces; this test enumerates one module's exports.
- Any lint rule, `tsconfig` path alias or `package.json` `exports` field that
  would make importing a leaf directly an error at build time. That is BP-001's
  toolchain row in `structure.md` and needs a decision this WO does not own; the
  test above is **recorded, not enforced** (constitution rule 3.3 — no CI
  configuration, no branch protection and no pre-commit hook exists in this
  repository at cut time).
- Migrating any consumer onto the barrel. The sixteen named consumers of
  `renderMeasured` and the ten of `BAND_LABELS` each import it in their own work
  order; none is edited here.

## Rollback
Delete the two files. Nothing imports the barrel at the time this WO lands — the
six modules stand on their own and every consumer is a later work order — so
removing it breaks no build. No leaf file, no migration, no schema and no
repo-root file is touched.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-019 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 6 rows of `## Test plan` quote BP-019's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-001, WO-042); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
