---
id: WO-269
type: work-order
title: "The layout law, declared and checked — Surface with its required arms, the conformance suite at five widths, its canary, and the browser runner"
status: approved
ui: no
implements: [BP-018, BP-001]
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "A real layout engine is what makes this suite measure anything (ADR-093 rests-on row 1, in this order's words): `vitest.config.ts` today routes every file under `tests/ui/**` to jsdom, where every box metric is 0 and every one of checks 1–4 holds on every element of every page. A layout suite dropped into `tests/ui/layout/` without the `layout` project split in this file plan passes vacuously. The canary fixture is what discharges this: the run in which the canary's checks 1–4 do not fail is the run that measured nothing."
    disposition: confirmed
  - claim: "Nothing in this repository enforces a failing suite (ADR-093 rests-on row 2; ADR-010 point 6). No `.github/workflows`, no hook, no branch protection, and `npm run test:layout` is not part of `npm test`. A layout failure is recorded in the run's output and nowhere else until a platform check exists (rule 3.3)."
    disposition: confirmed
  - claim: "ADR-093 decision 1 fixes exactly two boundaries; `design/tokens.md` §2b (closed 2026-09-02 under rule 1.1) mints four — `--breakpoint-sm` 640, `-md` 768, `-lg` 1024, `-xl` 1280. This order maps the two boundaries from decision 1's own structural facts: `compact|medium` is where the sidebar returns (`--breakpoint-lg`) and `medium|wide` is where the day panel sits beside the grid (`--breakpoint-xl`), because those are the two facts decision 1's table uses to define the bands. `--breakpoint-sm` and `--breakpoint-md` are widths the suite does not probe; whether they are content-dimension steps in decision 1's sense or a fourth and fifth band is the architect's to rule, and reversing this mapping costs two entries in `BAND_MIN` and nothing else."
    disposition: undischargeable
  - claim: "`sdlc-factory/docs/design/components.md` carries no `Surface` row (grepped 2026-09-02: none). BP-018 decision 3 says `design/` owes it. WO-037's two-way registry test will name the gap until it lands; this order does not write under `design/`."
    disposition: confirmed
  - claim: "Today `src/app/` holds only `.gitkeep`, so the route sweep enumerates zero routes and only the canary exercises the browser. Three things are observed the first time a route exists (WO-002 and the first leaf): that the app boots for the suite under `tests/setup.ts`'s network refusal and WO-005's boot-time env validation; that Playwright's own transport to Chromium (a child-process pipe, not `http.request`) does not trip that refusal; and what an `(account)` route renders to the anonymous visitor the suite is — REQ-020 criterion 5's address prompt, which is the document measured until a session fixture exists."
    disposition: undischargeable
supersedes: []
---

# WO-269 — The layout law, declared and checked — Surface with its required arms, the conformance suite at five widths, its canary, and the browser runner

- Branch: wo/WO-269-layout-law
- Estimate: ≤1 day

## Goal
Create BP-018's `src/ui/layout/**` and `tests/ui/layout/**` so that a screen
root cannot compile without declaring its three band arms, every App Router
route is rendered in a real browser at five widths against ADR-093 decision 6's
four checks, and the one fixture that overflows on purpose is seen to fail —
with the browser driver and the script that run it landing in BP-001's root
toolchain (BP-001 decision 2).

## File plan

> Verified 2026-09-02: `src/ui/` and `tests/ui/` hold no file (`src/ui/.gitkeep`
> only). Every `src/`/`tests/` path below is NEW. The three root-file rows are
> MODIFY on files WO-001 shipped; no other open work order names them in its
> plan (WO-018 modifies `eslint.config.mjs` only), so this order shares a file
> with nothing running beside it.

| File | New/Modify | Change |
|---|---|---|
| `src/ui/layout/bands.ts` | NEW | `BANDS`, `Band`, `Arm` verbatim from BP-018 `## Public interface`, plus `BAND_MIN: Record<Band, number>` — the lower bound of each band: `compact` 320 (ADR-093 decision 2), `medium` and `wide` transcribed from `design/tokens.md` §2b's `--breakpoint-lg` and `--breakpoint-xl` by name (rests-on row 3). Three numbers, each commented with the token or decision it transcribes; no other value. |
| `src/ui/layout/Surface.tsx` | NEW | The primitive, signature verbatim from BP-018 with `React.JSX.Element` as the return type (see Parameters). Renders its children inside one element carrying `data-surface` and one `data-arm-<band>` attribute per band with the arm's `kind` (and `count`/`note`); no class, no style, no string of its own — it is a declaration, not a widget. |
| `src/ui/layout/index.ts` | NEW | Barrel exporting exactly `BANDS`, `Band`, `Arm`, `BAND_MIN`, `Surface`. |
| `tests/ui/surface.test.tsx` | NEW | Under the jsdom `ui` project (no layout needed): the two `@ts-expect-error` fixtures and the no-chrome assertions in the test plan. |
| `tests/ui/layout/browser.ts` | NEW | The `layout` project's global setup/teardown: launches Chromium through `playwright`, starts the built app on a free port, exposes the base URL, and opens each of `widths()`'s five widths at `{ width, height: 480 }` — the height is this file's one named constant with its provenance comment (ADR-093 decision 6, amended 2026-09-03; REQ-099 criterion 1), the suite parameter the ADR names, so `bands.ts` stays at its three numbers; when no Chromium binary is installed it fails the run with a written line naming `npx playwright install chromium` — never a skip, because a skipped sweep reads exactly like a clean one. |
| `tests/ui/layout/routes.ts` | NEW | The enumerator (ADR-010's idiom): walks `src/app/**/page.tsx`, strips route groups, fills dynamic segments and `(hosted)` Host headers from one fixture map in this file; a segment or host with no fixture row fails naming the route rather than skipping it. Reports the count it enumerated (rule 5.5). |
| `tests/ui/layout/widths.ts` | NEW | The five widths derived from `BAND_MIN`: `[320, medium−1, medium, wide−1, wide]` — one function, no literal but the floor's provenance in a comment. |
| `tests/ui/layout/checks.ts` | NEW | Checks 1–4 as four functions over a Playwright page, each returning the offending elements with the check's name; the scroll-container and declared-truncation allow-lists as two exported arrays, both empty at cut (a surface that declares a truncation adds its own row); check 3 reads computed `font-family` and refuses an allow-list entry on a value (ADR-093 decision 6 point 3); check 4 reads `--t-floor` from the document's `:root` and fails, never defaults, when it is undeclared. |
| `tests/ui/layout/fixtures/canary.html` | NEW | The fixture that overflows on purpose: a document declaring `--t-floor` on its own `:root`, with one element wider than the viewport (check 1), one child escaping its parent's padding box (check 2), one clipped text-bearing box and one allow-listed **value** in the mono family that is truncated (check 3), and one text below the floor (check 4). |
| `tests/ui/layout/routes.test.ts` | NEW | The enumerator and width derivation against a fixture tree (route group, static, dynamic with and without a fixture row, hosted). |
| `tests/ui/layout/canary.test.ts` | NEW | Navigates to the canary fixture and asserts each of checks 1–4 fails on it, naming the element; a canary that passes any check fails this test. |
| `tests/ui/layout/layout.test.ts` | NEW | The sweep: every enumerated route × five widths × checks 1–4, the one-`[data-surface]`-root assertion, and the token pin (`:root` declares `--breakpoint-lg`, `--breakpoint-xl`, `--t-floor`; the first two equal `BAND_MIN.medium`/`.wide`). Prints `n routes × 5 widths` and passes with `n = 0` only by saying so. |
| `tests/app/layout-runner.test.ts` | NEW | BP-001's half, in BP-001's glob: `package.json` declares `playwright` as an exact-pinned devDependency and a `test:layout` script; `npm test`'s script runs the `node` and `ui` projects and not `layout`; `vitest.config.ts` routes `tests/ui/layout/**` to a node-environment project and excludes it from the jsdom one. |
| `vitest.config.ts` | MODIFY | The `ui` project gains `exclude: ["tests/ui/layout/**"]`; a fourth project `layout` (node environment, `include: ["tests/ui/layout/**/*.test.ts"]`, `globalSetup: ["tests/ui/layout/browser.ts"]`). Sequencer and `setupFiles` untouched. |
| `package.json` | MODIFY | `playwright` in `devDependencies`, exact pin (WO-001 step 1's no-`^` rule); `"test"` becomes `vitest run --project node --project ui`; new `"test:layout": "vitest run --project layout"`. |
| `package-lock.json` | MODIFY | Regenerated by the install — the generated companion TST-001 recorded as BP-001's. |

## Interfaces

**Consumes:** `design/tokens.md` §2b by token name — `--breakpoint-lg`,
`--breakpoint-xl` (transcribed once into `BAND_MIN`, pinned against `:root` by
the sweep) and `--t-floor` (read from `:root` at run time, never transcribed);
`react` types; `playwright` (Chromium only) as the driver; WO-001's `npm run
typecheck`, `npm test`, `npm run build`. Nothing from `src/lib/**`.

**Exposes** (verbatim from BP-018 `## Public interface`, "Layout (ADR-093)"):
```ts
// ── Layout (ADR-093). Three bands, closed; a fourth is an ADR, not a query. ──
export const BANDS = ['compact', 'medium', 'wide'] as const
export type Band = (typeof BANDS)[number]

/** What this surface does in one band. No optional member: a surface that has
 *  nothing to say at a band says so with `same-as-below`, which is a decision
 *  someone made rather than a field someone forgot. */
export type Arm =
  | { kind: 'same-as-below' }
  | { kind: 'columns'; count: 1 | 2 | 3 }
  | { kind: 'declared'; note: string }

/** Every screen root is a `Surface`. `arms` is required and has no default, so
 *  a screen that declares no band behaviour fails to compile — not a test.
 *  `Surface` renders no chrome and is not a widget: BUILD.md §2.2's closed set
 *  of five custom components is untouched by it. */
export function Surface(p: {
  arms: Record<Band, Arm>
  children: React.ReactNode
}): JSX.Element
```
Plus `BAND_MIN` (this order's, see Parameters) and the script `npm run
test:layout` on BP-001's root toolchain.

## Parameters chosen (rule 1.1)
- **Driver: `playwright`, Chromium only.** Alternatives: `puppeteer` (same
  engine, no viewport-emulation advantage); Vitest browser mode
  (`@vitest/browser-playwright`) — rejected because it renders components, and
  the enumerator is the route tree, which needs the Next server. Reversal: one
  devDependency and `browser.ts`.
- **A fourth vitest project, not a second config file.** A root
  `vitest.layout.config.ts` would be a file `structure.md`'s root-config row
  does not name. Reversal: one project block and one script line.
- **`npm test` excludes `layout`.** A browser binary is not installed by `npm
  install`, and every sibling work order's stop condition is `npm test`
  green. Reversal: one script line.
- **`BAND_MIN` lives in `src/ui/layout/`, not `constants.ts`.** `structure.md`
  rule 5 pins `BUILD.md`'s numbers into BP-005's file; a design token's code
  home is BP-018's (`structure.md` top-level row), and WO-006 is in flight on
  `constants.ts`. The sweep's token pin is what keeps the transcription and
  the CSS declaration from diverging (the `tests/pins.test.ts` shape).
- **Viewport height 480 at every width.** `browser.ts` opens each of the five widths at `{ width, height: 480 }`; the height is one named constant beside `BAND_MIN` with its provenance in a comment (ADR-093 decision 6, amended 2026-09-03; the promise is REQ-099 criterion 1) and no other literal. Reversal: one suite parameter.
- **The canary is a static fixture under `tests/ui/layout/fixtures/`, navigated
  by `file://`.** A page under `src/app/` would be enumerated by the sweep it
  is meant to fail, would ship unless gated, and would sit in BP-001's glob
  rather than BP-018's. Same browser, same four functions. Reversal: one file
  moved and one enumerator exclusion.
- **`Surface` applies no style.** An `Arm` of kind `columns` is a declaration
  the screen's own CSS honours; a primitive that laid out a grid would carry
  spacing of its own and reopen `BUILD.md` §2.2's closed set. Reversal: one
  component body.
- **Type fan-out check (1b), run 2026-09-02** on a scratch copy of `bands.ts`
  and `Surface.tsx`: `npm run typecheck` reports one error, in the new file
  itself — `TS2503: Cannot find namespace 'JSX'` under `@types/react` 19.2 —
  and zero with `React.JSX.Element`. No existing file consumes the new type,
  so the plan has no type-change rows; the return type is spelled
  `React.JSX.Element` (an internal name, rule 1.1).

## Steps
1. Write `fixtures/canary.html` and `canary.test.ts`. Run the file once under
   the existing jsdom `ui` project and record that checks 1–4 **pass** on the
   canary there — the vacuous run rests-on row 1 describes, watched before the
   cure exists (constitution §8).
2. Add `playwright`, the `layout` project and the two scripts; write
   `browser.ts`. Run `npm run test:layout` with no Chromium installed and
   record the written refusal; install Chromium; watch `canary.test.ts` go
   green because the fixture now fails all four checks.
3. Write `surface.test.tsx` and watch it fail (no module); create `bands.ts`,
   `Surface.tsx`, `index.ts`; `npm run typecheck` and `npm test` green.
4. Write `routes.test.ts`, then `routes.ts` and `widths.ts`.
5. Write `layout.test.ts` over `checks.ts`; run the sweep and record its
   coverage line (`0 routes × 5 widths` today).
6. Write `tests/app/layout-runner.test.ts`; `npm test`, `npm run test:layout`,
   `npm run typecheck`, `npm run lint`, `npm run build` all green.

## Test plan

> **Criterion source: the blueprint and the decision, not a requirement.**
> BP-018 carries `satisfies: []`, and ADR-093 rests-on row 3 records as
> `undischargeable` that no approved requirement promises the product works
> at every screen size. Rows below quote BP-018 and ADR-093; none is inherited
> from a requirement, and none is paraphrased.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-018 `## Public interface`: "`arms` is required and has no default, so a screen that declares no band behaviour fails to compile — not a test." | `tests/ui/surface.test.tsx` — two `@ts-expect-error` fixtures: `<Surface>` with no `arms`, and `arms` missing one band. Widening `arms` to optional, or adding a default, makes a fixture compile and fails the test. |
| BP-018 `## Public interface`: "`Surface` renders no chrome and is not a widget: BUILD.md §2.2's closed set of five custom components is untouched by it." | `tests/ui/surface.test.tsx` — renders with three arms and a child; asserts the child is the only text, the root carries no `class` and no `style`, and carries `data-surface` plus `data-arm-compact`/`-medium`/`-wide` with the declared kinds. Adding a class or a default string fails it. |
| BP-018 `## NFR budget`: "`tests/ui/layout/**` enumerates routes from the App Router tree and renders each at five widths — 320, the three bands, and each boundary minus one pixel" and ADR-093 decision 2: "**320 CSS px and above**" | `tests/ui/layout/routes.test.ts` — on a fixture tree: a route group is stripped, a dynamic segment with a fixture row resolves, one without fails naming the route, a `(hosted)` page carries its Host; `widths()` returns exactly `[320, BAND_MIN.medium − 1, BAND_MIN.medium, BAND_MIN.wide − 1, BAND_MIN.wide]` and `BAND_MIN.compact === 320`. Dropping a width or a boundary-minus-one fails it. |
| BP-018 `## NFR budget`: "asserting: no horizontal document scroll; every border box contained in its containing block's padding box; `scrollWidth <= clientWidth` and `scrollHeight <= clientHeight` on every text-bearing element outside the declared truncation allow-list, with a truncated **value** failing whether allow-listed or not; and computed `font-size` at or above the floor." | `tests/ui/layout/canary.test.ts` — each of the four checks reports at least one offender on the fixture, and the allow-listed mono value is among check 3's. Deleting any one check's body, or letting the allow-list save the value, fails it. `tests/ui/layout/layout.test.ts` — the same four functions run on every enumerated route at all five widths and report no offender. |
| BP-018 `## NFR budget`: "It ships **one fixture route that overflows on purpose and must fail** … A green run in which the canary also passes is a failed run." | `tests/ui/layout/canary.test.ts` — the test is red when any check passes on the canary; step 1's jsdom run is the recorded proof that without a layout engine all four pass and this test fails. |
| BP-018 `## Public interface`: "Every screen root is a `Surface`." | `tests/ui/layout/layout.test.ts` — each enumerated route's document has exactly one `[data-surface]` element at its root; states the route count it checked. |
| BP-018 `## Error & edge behavior`: "a token missing from `:root` fails a test" | `tests/ui/layout/layout.test.ts` — on each route, `:root` declares `--breakpoint-lg`, `--breakpoint-xl` and `--t-floor`, and the first two equal `BAND_MIN.medium` and `.wide`; `tests/ui/layout/canary.test.ts` — check 4 fails, never defaults, on a fixture with `--t-floor` removed. |
| BP-018 `## Module / boundary`: "Its *runner* (a real browser driver as a dev dependency and a script) is BP-001's root toolchain, not this module's." | `tests/app/layout-runner.test.ts` — `playwright` is an exact-pinned devDependency; `test:layout` exists; `test` names `node` and `ui` and not `layout`; `vitest.config.ts` routes `tests/ui/layout/**` to a node-environment project and excludes it from the jsdom one. Removing the exclusion fails it. |

## Out of scope
- Each surface's arms and its `compact` list/tabbed forms — WO-033, WO-034,
  WO-155 and every `ui: yes` order; the `TabBar` composition (ADR-093
  decision 7, BP-037).
- Declaring `--breakpoint-*`, `--t-floor` and the rest of `tokens.md` §2b in
  `src/ui/theme.css` and Tailwind `screens` — WO-029's file, whose plan
  predates §2b; the sweep's pin is what will report the gap.
- The `components.md` row for `Surface`, the two allow-list rows the ADR
  names from the previews, and anything under `design/`.
- CI, hooks, branch protection (rests-on row 2); a session fixture for
  `(account)` routes (rests-on row 5).
- `src/ui/index.ts` — no order creates a root barrel; `src/ui/layout/index.ts`
  is this module's own.

## Rollback
Delete `src/ui/layout/`, `tests/ui/layout/`, `tests/ui/surface.test.tsx` and
`tests/app/layout-runner.test.ts`; revert `package.json`, `package-lock.json`
and `vitest.config.ts` to WO-001's. Nothing imports `Surface` yet and no
schema, migration or `design/` file is touched.

## Self-certification
`status: approved` — set 2026-09-02 by the planner under constitution rule 3.2:
an assertion with its grounds stated, which the librarian audits and may refuse
retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-018 — `status: approved`; BP-001 —
  `status: approved` (the runner rows, decision 2). ADR-093 reads
  `status: proposed` on disk; the gate binds the blueprint, and BP-018 as
  approved already carries the suite, `Surface` and the bands.
- **Criteria inherited, not paraphrased.** The 8 rows of `## Test plan` quote
  BP-018 and ADR-093 as they stand on disk; no requirement is inherited, and
  the header says why.
- **Bounded.** 16 files (13 NEW, 3 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and
  `## Rollback` states how to undo the change.
- **Merge test (rule 2.6).** `Surface` and the suite were tried as two
  orders: the suite's stop condition names the `[data-surface]` root, the
  primitive's names the attributes the suite reads, and both consume
  `bands.ts` — neither states its goal without the other, and the primitive
  alone is three files under an hour, which is a per-file order. One slice.
- **`ui: no`.** Nothing customer-visible changes: `Surface` renders no chrome
  and no string, the fixture is a test asset, and the rest is tests and
  toolchain. Rule 7.3's preview holds the orders that draw the arms.

Re-checked 2026-09-03 after ADR-093 decision 6's amendment (19d8997): the
grounds hold — BP-018 is `approved` and its NFR budget cites the height
(35ff6bd); ADR-093 now reads `status: accepted`; the 8 test-plan rows still
quote BP-018 and ADR-093 as they stand on disk (both quoted spans are
unchanged substrings); 16 files, ≤1 day; every stop condition unchanged. Status
stays `approved`.

Not asserted here: validation, regression and merge. Those are the `done`
gate's, and the validator's and librarian's to set.

## Log
- 2026-09-02 created — planner
- 2026-09-03 finished — planner — ADR-093 decision 6 amended (19d8997): the suite's viewport is 480 CSS px tall at every width, serving REQ-099 criterion 1; height parameter and `browser.ts` row added, grounds re-checked, status unchanged
- 2026-09-03 finished — validator — regression on TST-006: merged `doctrine-0.11.0` (`83daed1`, 19 commits, no conflicts, none touching the file plan); full suite re-run fresh — `node`+`ui` 275/276 pass (1 pre-existing failure, WO-270's `.claude/` fix, unmerged into `doctrine-0.11.0` — confirmed not WO-269's), `layout` 16/16 pass; typecheck and build clean; blast radius (`factory-console impact WO-269`: 18 files → 2 blueprints → 1 importer → 0 requirements) re-checked, nothing to re-verify against acceptance criteria (0 requirements named); criteria 6–7 stay vacuous at `n = 0` routes, stated per rule 5.5. `Regression: 18 files · none re-checked — pass` appended to TST-006.
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — all 16 file-plan files landed: `src/ui/layout/{bands,Surface,index}.ts` (BANDS/Band/Arm/BAND_MIN/Surface), `tests/ui/surface.test.tsx`, `tests/ui/layout/{browser,routes,widths,checks}.ts`, `tests/ui/layout/fixtures/canary.html`, `tests/ui/layout/{routes,canary,layout}.test.ts`, `tests/app/layout-runner.test.ts`, `vitest.config.ts` (ui excludes `tests/ui/layout/**`; new `layout` project, node env, `globalSetup: browser.ts`), `package.json`/`package-lock.json` (playwright 1.62.1 exact-pinned; `test` → `--project node --project ui`; new `test:layout`). Step 1 (§8, watch it fail first): loaded `fixtures/canary.html` directly into jsdom's own `document` (no Playwright) and ran checks 1-4 in-process — checks 1-3 (box-metric-dependent: no-horizontal-scroll, containment, clipping/truncation) each returned 0 offenders, the vacuous pass ADR-093 rests-on row 1 describes; check 4 (the type floor) returned 1 offender even under jsdom, because computed `font-size` is a style-cascade value jsdom resolves without a layout engine — a real, honest asymmetry worth recording, not smoothed over: jsdom's own gap is box geometry (`scrollWidth`/`clientWidth`/`getBoundingClientRect`), not computed style, and checks 1-3 are the ones that go vacuous. Step 2: `npm run test:layout` with no Chromium installed failed the whole run before any test executed, naming `npx playwright install chromium` verbatim (observed: "no Chromium binary is installed for Playwright. Run `npx playwright install chromium`…"); a first `chromium.launch()` attempt after installing the browser itself failed on a missing system library (`libnspr4.so`), resolved with `npx playwright install-deps chromium` (`--with-deps` per the WO's own fallback clause) — recorded because it is a real, if minor, deviation from "one command" and not a change to any committed file. A second finding worth recording: the design initially shared one Chromium across worker processes via `chromium.launchServer()` + `chromium.connect(wsEndpoint)`; the connect step tripped `tests/setup.ts`'s network refusal (`browserType.connect: tests/setup.ts: a test attempted a real network call via http.request()`), which rests-on row 1's own wording only clears for the *launch* transport. Fixed by having `withPage()` call `chromium.launch()` directly per use (the pipe transport, confirmed clean against the refusal by the full test run) rather than connecting to a shared server; `globalSetup`'s one `launch()`-and-`close()` is now a preflight check only. Separately confirmed by probe: `fetch()` inside `globalSetup` is a real network call unaffected by `tests/setup.ts` (globalSetup runs outside the setupFiles context), which is what `browser.ts`'s `waitForServer` relies on for the not-yet-exercised build+start path. After the fix: `npm run test:layout` — 3 files, 16 tests, all passed, canary's checks 1-4 all fail on the fixture as required. Route sweep: `tests/ui/layout/routes.ts` enumerated 0 routes under `src/app/` (holds only `.gitkeep` — rests-on row 5), so `layout.test.ts` prints "0 route(s) × 5 widths — src/app/ holds no route yet" and its per-route/per-width sweep, `[data-surface]` and token-pin assertions are all vacuously satisfied and say so (rule 5.5); their code paths (navigation via `browser.ts`'s `getBaseURL()`, `Host`-header injection for `(hosted)` pages) are written but unexercised until the first route lands, per rests-on row 5. `npm test` (node+ui): 191 tests, 190 passed, 1 failed — the pre-existing `tests/app/toolchain.test.ts` "no top-level directory outside structure.md rule 7's set" failure on `.claude/` (WO-270's, not touched). `npm run typecheck`: clean, including the two `@ts-expect-error` fixtures in `surface.test.tsx` (verified the mutation: widening either fixture reports "Unused '@ts-expect-error' directive"). `npx eslint` over every touched file: clean. `npm run build`: clean (the worktree's Turbopack/symlinked-`node_modules` build failure observed before `npm install` added `playwright` was resolved as a side effect of that install replacing the symlink with a real local `node_modules` — not a code change, noted for the record). No deviation from the file plan; the two implementation findings above (the transport fix and the `install-deps` step) are recorded here per rule 4.2 as residual detail, not as objections — nothing in the WO was wrong, both were resolved inside this order's own new files. Ready for `/validate`.
- 2026-09-03 finished — validator — TST-006: pass. All 8 test-plan rows discriminate under fresh, independent mutation (own probes, distinct from the implementer's); §8's canary vacuity independently reproduced under jsdom and its non-vacuity under real Chromium; scope exactly the 16 planned files; all five of this WO's own `rests-on` rows dispositioned (2 confirmed, 2 undischargeable, 1 confirmed — none left `open`); ADR-093's two matching `open` rows also dispositioned `confirmed`. Ready for `/regress`.

## Validation report (appended by validator)

## TST-006 — WO-269 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-269`, branch `wo/WO-269-layout-law`, implementer HEAD `588298c`
- Corpus-global id: `TST-006` — `grep -rh "^## TST-" sdlc-factory/docs/work-orders/*.md` across the root checkout (`doctrine-0.11.0`: `TST-001`…`TST-005`, `TST-005` from WO-267's merged report) and this branch (`TST-001`…`TST-003` only, branched before WO-267 merged) returns a combined max of `TST-005`; `TST-006` is the next unused id.
- `ui: no` — no `Placement:` line follows (design-guardian's UI-fit pass does not run on this WO).

### Commands (run fresh from HEAD)

| Command | Result |
|---|---|
| `npm run typecheck` (`tsc --noEmit`) | exit 0 |
| `npx eslint src/ui/layout tests/ui/layout tests/ui/surface.test.tsx tests/app/layout-runner.test.ts` | exit 0 |
| `npx vitest run --project node --project ui --no-file-parallelism` | 7 files, 191 tests: 190 pass, 1 fail (pre-existing, see below) |
| `npx vitest run --project layout --no-file-parallelism` | 3 files, 16 tests: 16 pass — Chromium genuinely installed (`~/.cache/ms-playwright/chromium-1234`), not stubbed |
| `npm run build` (`next build`) | clean, exit 0 |
| `npm run lint` (repo-wide) | 7974 pre-existing problems, all under `.claude/**` and `sdlc-factory/docs/design/previews/**`; zero under `src/`, `tests/`, matching WO-267's TST-005 regression record of the same pre-existing count |
| `git diff --name-only be814e5..HEAD` | exactly the 13 NEW + 3 MODIFY files the file plan names, plus `WO-269.md` itself |

The one full-suite failure — `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`.claude/` present) — is pre-existing and WO-270's, identically documented in WO-267's TST-004/005/regression record; this branch's diff touches neither `tests/app/toolchain.test.ts`, `structure.md`, nor `.claude/`.

### §8 discipline — watch it fail first, independently reproduced (not taken on the implementer's word)

The WO's own rests-on row 1 and its `## Log` claim that a layout suite under jsdom is vacuous and that the canary is what discharges the risk. I did not read this off the implementer's log; I reproduced it myself with a throwaway probe (`tests/ui/__scratch_jsdom_vacuity.test.ts`, deleted after use, `git status --porcelain` clean before and after):

- Parsed `fixtures/canary.html` into the `ui` project's real jsdom `document` and ran all four `checks.ts` functions in-process, unmodified. Result: `checks 1–3` (`checkNoHorizontalScroll`, `checkContainment`, `checkNoClippingOrTruncation` — all box-metric-dependent) each returned **zero offenders** on a fixture built to overflow on all four checks — exactly the vacuous pass the rests-on row describes. `check 4` (`checkTypeFloor`, a computed-style read, not a box metric) correctly returned one offender even in jsdom.
- Ran `tests/ui/layout/canary.test.ts` under the real `layout` project (real Chromium, confirmed by `~/.cache/ms-playwright/chromium-1234` on disk and a live `chromium.launch()`/`page.goto('file://…')` round trip): all 6 tests pass, i.e. checks 1–4 **do** fail on the canary there.

This is the run in which the canary's checks 1–4 do not fail (jsdom) *and* the run in which they do (real Chromium), both independently observed — which is what discharges rests-on row 1, not merely what the WO asserts.

### Criterion-by-criterion (WO-269 `## Test plan`, 8 rows) — PASS/FAIL and independent mutation

| # | Criterion (source, abbreviated) | Test | Result | Mutation probe (mine, not the implementer's) |
|---|---|---|---|---|
| 1 | BP-018 — `arms` required, no default, fails to compile | `tests/ui/surface.test.tsx` (`@ts-expect-error` × 2) | PASS | Widened `Surface`'s `arms` to `arms?:` → `npm run typecheck` reports `TS2578: Unused '@ts-expect-error' directive` on the first fixture, exactly the failure mode the criterion names. Restored; `git diff` empty. Discriminates. |
| 2 | BP-018 — `Surface` renders no chrome, not a widget | `tests/ui/surface.test.tsx` (root assertions) | PASS | Added `className="surface-root"` to `Surface`'s root element → `"renders one root … no class, no style"` fails (`expected 'surface-root' to be null`). Restored; `git diff` empty. Discriminates. |
| 3 | BP-018 NFR + ADR-093 decision 2 — enumerator, 5 widths, 320 floor | `tests/ui/layout/routes.test.ts` | PASS | (a) Dropped `BAND_MIN.medium - 1` from `widths()`'s return array → `"returns exactly […]"` fails (`4` items instead of `5`). (b) Made `(hosted)` fixture-less pages default to a placeholder Host instead of throwing → `"fails naming the route when a (hosted) page has no Host fixture row"` fails (`"expected function to throw … but it didn't"`). Both restored; `git diff` empty each time. Discriminates. |
| 4 | BP-018 NFR — checks 1–4 report on the canary; layout.test.ts runs them on every route | `tests/ui/layout/canary.test.ts` + `tests/ui/layout/layout.test.ts` | PASS | (a) Deleted `checkContainment`'s body, replaced with `return []` → "check 2 … fails, naming the escaping element" fails (`expected 0 to be greater than 0`). (b) Changed check 3's allow-list branch from `if (!allowListed \|\| isValue)` to `if (!allowListed)` (letting a mono *value* be saved by an allow-list entry) → "an allow-list entry does not save an element whose text is a value" fails. (c) Made `checkTypeFloor` default to `11` instead of reporting an offender when `--t-floor` is unset → "check 4 fails, never defaults, when --t-floor is undeclared" fails (offender no longer names `:root`). All three restored independently; `git diff` empty after each. Discriminates on three independent axes of the same row. |
| 5 | BP-018 NFR — one fixture that overflows on purpose and must fail; a green canary is a failed run | `tests/ui/layout/canary.test.ts` | PASS | Same three mutations above each turn a canary check from failing-as-required to passing, which is exactly the criterion's inverse condition; §8's own "watch it fail first" was independently reproduced above (jsdom vacuity) rather than only cited. |
| 6 | BP-018 — every screen root is a `Surface` | `tests/ui/layout/layout.test.ts` (`[data-surface]` count) | PASS (vacuously, stated explicitly) | Not independently mutable today: `src/app/` holds no route (rests-on row 5), so the `for (const route of routes)` loop that would assert `count === 1` iterates zero times; the file's own `it` explicitly asserts `routes.length === 0` and states this in a `console.log` line (rule 5.5), which I observed directly (`tests/ui/layout/routes.ts: enumerated 0 route(s) under …/src/app`). The assertion's code path is written and typechecks but is unexercised until the first route lands — same status the WO's own `## Log` records, independently confirmed rather than taken on it. |
| 7 | BP-018 error behaviour — a token missing from `:root` fails, never defaults | `tests/ui/layout/layout.test.ts` (route sweep, vacuous today) + `tests/ui/layout/canary.test.ts` (check 4 undeclared case) | PASS | The canary half is independently mutated above (row 4c) and discriminates. The route-sweep half is vacuous for the same reason as row 6 (zero routes) — stated, not silent. |
| 8 | BP-018 — the runner (driver + script) is BP-001's root toolchain | `tests/app/layout-runner.test.ts` | PASS | (a) Removed `vitest.config.ts`'s `exclude: ["tests/ui/layout/**"]` from the `ui` project → `"vitest.config.ts excludes tests/ui/layout/** from the jsdom ui project"` fails. (b) Added `--project layout` to `package.json`'s `"test"` script → `"package.json's test script runs the node and ui projects, and not layout"` fails. Both restored; `git diff` empty each time. Discriminates. |

**8 of 8 rows PASS and discriminate** under fresh, independent mutation on axes different from the implementer's own probes (a different fixture, a different guard clause, a different config field, or — for rows 6–7 — an honest, explicit "unexercised" status rather than a false pass). No vacuous test found among the 8 test-plan rows themselves.

### Scope

`git diff --name-only be814e5..HEAD` (the merge base with the integration branch, `doctrine-0.11.0`) returns exactly the file plan's 13 NEW files, 3 MODIFY files (`package.json`, `package-lock.json`, `vitest.config.ts`), plus `WO-269.md` itself (this branch's own `## Log` and, now, this report and the `rests-on` disposition edits). No stray file. `structure.md`'s row for `src/ui/**` · `tests/ui/**` already names `src/ui/layout/**` (`BANDS`, `Surface`) and `tests/ui/layout/**` under BP-018 — this order adds no second home. Grepped `src/` and `tests/` outside `src/ui/layout/**` and `tests/ui/layout/**`/`tests/ui/surface.test.tsx`/`tests/app/layout-runner.test.ts` for `export function Surface`, `export const BAND_MIN` and `data-surface`: no matches — no duplicate capability.

### Coverage gaps

- Rows 6 and 7's route-sweep halves are vacuously satisfied at `n = 0` routes (rests-on row 5, undischargeable below) — written, typechecked, and stated explicitly per rule 5.5, but not exercised against a real route by this build. The first work order that adds a route under `src/app/` is where this becomes a real, non-vacuous assertion.
- ADR-093 rests-on row 3 (whether `--breakpoint-sm`/`-md` constitute a fourth/fifth band) remains the architect's open call, mirrored by this WO's own row 3 — not a defect, an explicit deferral both artifacts already name.

### `rests-on` dispositions (constitution rule 2.3b)

**WO-269's own five rows** (front-matter updated on this branch):

1. *A real layout engine, discharged by the canary* — **confirmed**. Independently reproduced above: jsdom vacuity (checks 1–3 report zero offenders on the overflowing canary) and real-Chromium non-vacuity (all 6 canary tests fail-as-required) both observed directly, not read off the implementer's log.
2. *Nothing enforces a failing suite; `test:layout` not part of `npm test`* — **confirmed**. `package.json`'s `"test"` script is `vitest run --project node --project ui` (no `layout`); `ls .github/workflows`, `ls .husky`, `git config core.hooksPath`, and `.git/hooks/` (non-sample) all confirmed absent, independently.
3. *`BAND_MIN`'s two-boundary mapping; whether `-sm`/`-md` are a fourth/fifth band is the architect's* — **undischargeable**. The dischargeable half is confirmed by inspection: `BAND_MIN.medium = 1024` and `.wide = 1280` transcribe `design/tokens.md` §2b's `--breakpoint-lg`/`-xl` exactly (grepped directly), and `widths()` probes only those two boundaries, never `-sm`/`-md` — both as claimed. Whether `-sm`/`-md` should become a fourth and fifth band is an architectural parameter choice (rule 1.1), not a fact any test run can settle; the row's own text says as much ("is the architect's to rule"). No build discharges this.
4. *`design/components.md` carries no `Surface` row* — **confirmed**. Grepped `sdlc-factory/docs/design/components.md` directly: no `Surface` primitive row exists (the one "Surface compositions" mention is a *screen-composition* list explicitly marked "not registry rows," a different thing). This WO's own diff writes nothing under `design/`, so the gap the row names is unchanged and accurately described.
5. *`src/app/` holds only `.gitkeep`; three things unobservable until the first route lands* — **undischargeable by this build**. Confirmed directly (`find src/app` → only `.gitkeep`). None of the three named observations — app-boot-under-network-refusal, Chromium's launch transport not tripping it, an `(account)` route's anonymous-visitor content — can be exercised with zero routes on disk; this is the honest disposition the row's own text anticipates ("observed the first time a route exists"), not a claim this build could have discharged and didn't.

**ADR-093's own rows** (root checkout / this branch's copy, dispositioned as the first work order that implements decision 6 and observed the claims — rule 2.3b):

- Row 1 (layout engine required, canary discharges) — `open` → **confirmed**, on the same evidence as WO-269 row 1 above; this WO's own text is a near-verbatim restatement of ADR-093's row 1 and this build is the first to create `tests/ui/layout/`.
- Row 2 (nothing enforces a failing suite) — `open` → **confirmed**, on the same evidence as WO-269 row 2 above.
- Row 3 (no approved requirement promises every-screen-size) — already `undischargeable` on disk; left unchanged, not this build's to touch (it concerns requirement coverage, not this WO's observation).

BP-018's `rests-on: []` (nothing to disposition) and BP-001's one row (Vercel/Inngest shared process image) are unrelated to anything this build observed — left untouched, not this WO's to close.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

pass

All 8 test-plan rows PASS and discriminate under fresh, independent mutation (a different fixture defect, guard clause, or config field than any the implementer's own log describes) — rows 6 and 7's route-sweep halves are the sole exception, honestly vacuous at `n = 0` routes and stated as such per rule 5.5, not a false pass. §8's "watch it fail first" was independently reproduced, not taken on the implementer's word: jsdom genuinely makes checks 1–3 vacuous on the canary, and the real-Chromium `layout` project genuinely makes all four checks fail on it (Chromium confirmed actually installed and invoked, not stubbed). `npm run typecheck`, targeted `eslint`, `npm run build`, and the full `node`+`ui`+`layout` suite all reproduce clean except the one pre-existing `.claude/`-directory failure already attributed to WO-270 in WO-267's own regression record. Scope is exactly the file plan's 16 files; no duplicate capability. All five of WO-269's own `rests-on` rows are dispositioned above — two `confirmed`, two `undischargeable` (both honestly so: one an architect's parameter call, one unobservable before the first route exists), one `confirmed` (the `design/components.md` gap) — none left `open`, clearing the librarian's `open-assumption-on-done` gate. ADR-093's two `open` rows this build observed are also dispositioned to `confirmed`, its third left untouched. **Next:** none for this WO — ready for `/regress` and the librarian's `done` gate.

### Regression (2026-09-03)

Branch brought current: merged `doctrine-0.11.0` (`83daed1`, no conflicts) — 19 commits since this branch's merge base (`be814e5`), none touching the file plan's 18 files: WO-267's Supabase substrate (`src/lib/db/**`, `supabase/**`), the four `research/seo-automation` documentation commits, `docs(evidence)` relink (`5aff98c`, 19 artifacts), and the `docs(design)` §5 preview-state correction (`045ace9`, `components.md`). Re-ran fresh from the merged HEAD, not read off the prior TST-006 run.

**Blast radius** (`factory-console impact WO-269`): `18 files → 2 blueprints (BP-001, BP-018) → 1 importer (tests/app/toolchain.test.ts) → 0 requirements`. The one importer is BP-001's toolchain check (asserts `playwright`'s exact pin, the `test`/`test:layout` script split and the `vitest.config.ts` project routing) — already exercised by the full-suite run below, unchanged since TST-006. Zero requirements named: BP-018 carries `satisfies: []` and ADR-093 rests-on row 3 records as `undischargeable` that no approved requirement promises every-screen-size behaviour (unchanged since TST-006) — nothing to re-check against acceptance criteria this pass.

**Full suite, fresh from merged HEAD:**

| Command | Result |
|---|---|
| `npm run typecheck` | exit 0 |
| `npx vitest run --project node --project ui --no-file-parallelism` | 11 files, 276 tests: 275 pass, 1 fail |
| `npx vitest run --project layout --no-file-parallelism` | 3 files, 16 tests: 16 pass — `0 route(s) × 5 widths`, unchanged (WO-002 and the first leaf still not landed; rests-on row 5 stands `undischargeable by this build`) |
| `npm run build` | clean, exit 0 |
| `npm run lint` (repo-wide) | 8117 problems, all under `.claude/**` and `sdlc-factory/docs/design/previews/**` (grepped: zero under `src/` or `tests/`) — grew from TST-006's 7974 because the merge landed more `.claude/factory/console/**` files; same two directories, same zero under this WO's own paths |

**The one failure — status, precisely.** `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`.claude/`) still fails on this merged HEAD. This is WO-270's fix (`test(WO-270): admit .claude/ into structure.md rule 7's top-level set`, TST-006 pass on `wo/WO-270-top-level-set`, `51b35b3`), and WO-270 has **not** merged into `doctrine-0.11.0` — `doctrine-0.11.0`'s own `tests/app/toolchain.test.ts` (`b172edf`) still reads the `allowed` set without `.claude`, confirmed directly (`git log -- tests/app/toolchain.test.ts` on `doctrine-0.11.0` shows only WO-001's commit). This branch's diff touches neither `tests/app/toolchain.test.ts`, `structure.md`, nor `.claude/`, so the failure is not WO-269's to fix and not new: identically the single failure recorded in TST-006 (191→276 tests reflects WO-267's db suite joining via the merge, not a new failure). It will clear when WO-270 merges ahead of or alongside this WO — not reported here as WO-269's own red suite.

**Criteria 6 and 7 (route-sweep halves), stated per rule 5.5.** Both stay vacuously satisfied at `n = 0` routes, unchanged since TST-006: `tests/ui/layout/routes.ts` enumerates zero routes under `src/app/` (still only `.gitkeep`), so `layout.test.ts`'s per-route `[data-surface]`-root and token-pin assertions (criteria 6 and 7) iterate zero times and say so in their own output (`0 route(s) × 5 widths`) rather than passing silently. This is not a regression finding — it is the same honest non-coverage TST-006 already recorded — and it stays vacuous until WO-002 and the first leaf route land, at which point it becomes the first work order to observe rests-on row 5.

**Result:** no new failures, no growth in the one known pre-existing failure (WO-270's, unmerged), blast radius's one importer already exercised, zero requirements to re-check. Merge into `doctrine-0.11.0` is clean (no conflicts) but not a fast-forward.

Regression: 18 files · none re-checked — pass
