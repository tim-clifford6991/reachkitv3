---
id: WO-278
type: work-order
title: "The presentation boundary for a measured value — handles become terms, values become a dash or a number, and a number reaches a sentence only with its scan date"
status: approved
wave: W3
ui: no
risk: normal
implements: [BP-019, BP-020, REQ-004, REQ-009, REQ-047, REQ-093, REQ-096]
depends-on: [WO-001, WO-041, WO-277]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31: the repo root holds only BUILD.md, DATA-COSTS.md, REQUIREMENTS-CONSOLIDATION.md, factory.config.json, CLAUDE.md and sdlc-factory/). Every path in this file plan is NEW and its placement is provisional against a repo with no code. The TypeScript project, its path resolution and the `*.test.ts` runner are established by other work orders; this WO creates none of them and adds no repo-root file."
    disposition: refuted
  - claim: "The thirteen strings these three maps resolve to are written by WO-041 into `src/lib/presentation/copy/keys/bands.ts` and are not owner-owed there (WO-041 Steps 5: 'None of the thirteen is in `OWNER_OWED`: they have values, so `copy()` returns them'). This WO declares keys only and asserts each resolves; if any of the thirteen is missing or empty when this WO runs, the failure is WO-041's and the implementer says so rather than seeding a string here (constitution rule 4.2)."
    disposition: confirmed
  - claim: "The handle sets are the engines' and are fixed elsewhere: `winnable | reach | not-yet` is BP-040's `Winnability`, `near | middle | far` is BP-011's rival-size handle set, `invisible | hard-to-find | findable | dominant` is BP-010's `BandHandle`, and `low | mid | high` is BP-027's `Severity`. This WO restates each union in the map's own type because the map must be total over it; a handle set that changes at its owning node without changing here is a compile error at the `Record<>`, which is the intended coupling."
    disposition: confirmed
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW except the one Modify row, and its placement is provisional against a repo with no code. This WO creates no repo-root file."
    disposition: refuted
  - claim: "The whole argument of this WO rests on TypeScript's discriminated-union narrowing being on: `strict`, `strictNullChecks` and no `any` on the `Measured<T>` import path. Without them the `unmeasured` arm does not lose its `value` field on narrowing and the central guarantee below degrades from a compile error to a convention. The tsconfig is BP-001's and does not exist today; the implementer stops and says so if `strict` is off (constitution rule 4.2)."
    disposition: confirmed
  - claim: "The two sentences REQ-004 criteria 6 and 9 require — the line naming what could not be determined, and the line stating the scan stopped early — are customer-visible strings and therefore the owner's (constitution §1). They stand **owner-owed**: this WO seeds two empty keys for them so callers have a key to pass, and `copy()` throws naming the key rather than rendering a blank (WO-041's chosen representation). Until the owner writes them, `renderMeasured` is complete in mechanism and throws loudly on the unmeasured arm; every test below asserts the key reached, never the sentence."
    disposition: confirmed
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31). Every path in this file plan is NEW and its placement is provisional against a repo with no code."
    disposition: refuted
  - claim: "BP-010's `Measured<T>` and BP-019's `renderMeasured()` are consumed here by import and are created by work orders outside WO-041..WO-050. This WO cannot be implemented before both symbols exist at their owning nodes' public entry points; the implementer stops and says so if they do not (constitution rule 4.2)."
    disposition: confirmed
  - claim: "BP-019's `## Public interface` does not yet transcribe a symbol for the one written line REQ-004 criterion 2 requires, which BP-024 decision 6 (2026-09-03) assigns to BP-019. This work order names it `LIMITING_LINES` in `src/lib/presentation/bands.ts` under rule 1.1 (see `## Decision taken under rule 1.1`) and the architect folds the symbol into BP-019 at wave close. Nothing about what is promised turns on the name. **Confirmed 2026-09-04 by direct read**: `blueprints/BP-019.md` `## Public interface` declares `CopyKey`, `copy`, `renderMeasured`, `emptyStateLine`, `generatedLabel`, `WorkStop`/`stoppedWorkStatement`, `BAND_LABELS`, `SCORE_BANDS` and `SEVERITY`, and no symbol for criterion 2's line; `grep -rn 'limitingFactorOf\|LimitingFactor' blueprints/ decisions/` returns BP-024 only. The gap is real and the name chosen here is the disposition, not a pending question"
    disposition: confirmed
supersedes: [WO-248, WO-249, WO-042]
---

# WO-278 — The presentation boundary for a measured value — handles become terms, values become a dash or a number, and a number reaches a sentence only with its scan date

- Branch: wo/WO-278-presentation-boundary
- Estimate: ≤1 day

## Goal
Ship the one door every measurement passes through on its way to a person: the three total handle→`CopyKey` maps, `renderMeasured()` in which rendering an unmeasured value as a number does not type-check and dashing a measured zero cannot be written as a local edit, and `explain()` in which a number reaches a product sentence only as a `Measured<T>` whose scan date travels with it.

**Risk: normal.** This order touches no seam the charter names: it renders WO-277's arms and moves no money, reads no session, calls no vendor and publishes nothing. Its own fabrication guard is discharged by the typechecker rather than by a runtime mutation (TST-027 measured `expectTypeOf` as a runtime no-op under `vitest run`), and the arms it renders are mutation-tested upstream at `risk: high` on WO-277. Plain criterion tests (doctrine 0.13.2, rule 2b).

## Consolidation (rule 2.6)
Three one-file orders inside one module, all three the same claim at three
levels — a handle becomes a term, a value becomes a dash or a number, a sentence
carries the date of the numbers in it. WO-042's goal is stated over
`renderMeasured` (WO-249's) and WO-249's `## Out of scope` hands `explain()` back
to WO-042; WO-248's `SCORE_BANDS` and WO-249's `renderMeasured` are read by the
same call site in the same line of the same strip. Cut along files inside one
directory is the horizontal cut rule 2.6 forbids. One order: the presentation
boundary for a measured value.

**Direction of the score-band edge, corrected here (rule 1.1).** WO-054 (now
WO-277) carried `depends-on: WO-248` (now this order) while WO-248's
`SCORE_BANDS` is `Record<BandHandle, CopyKey>` over WO-054's own `BandHandle` —
a cycle between the two merged orders. The engine declares the handles and the presentation
boundary maps them, so the edge runs WO-277 → WO-278 and not back. Cost of
reversing: one line of `depends-on`.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `src/lib/presentation/bands.ts` | **NEW** | WO-248 | `BAND_LABELS`, `SCORE_BANDS` and `SEVERITY`, verbatim from BP-019's `## Public interface`, plus `LIMITING_LINES` — see `## Decision taken under rule 1.1` below. Every value is a `CopyKey`; the file contains no string a person reads, imports only the `CopyKey` type from `./copy`, and imports nothing from `src/lib/config/`. Each map carries, in a comment above it, the artifact its keys were transcribed from — BP-019 decision 6 for the six band words and the three severity words, REQ-004 criterion 1 for the four score-band words, ADR-001 for why the two band sets sit in one file. |
| `src/lib/presentation/copy/keys/report.ts` | **Modify** | 2026-09-04 (BP-024 decision 6) | Add the three keys `LIMITING_LINES` resolves — one per `ScoreFactorName` — as **owner-owed and empty**, exactly on WO-041's terms (listed in `OWNER_OWED`, `copy()` throws naming them): `verdict.limiting.foundations`, `verdict.limiting.answerability`, `verdict.limiting.presence`, each `fixedBy: "REQ-004 c2"`, `slots: {}`. No value is written for any of the three here; the sentences are customer-visible strings and therefore the owner's (constitution §1). |
| `tests/presentation/bands.test.ts` | **NEW** | WO-248 | The totality, resolution, ordering and no-literal suites named in the test plan, including the two `@ts-expect-error` fixtures. |
| `src/lib/presentation/measured.ts` | **NEW** | WO-249 | `renderMeasured<T>()`, verbatim from BP-019's `## Public interface`, plus the module-private `DASH_KEY` constant. **Exactly two branches** (Steps 4): one guard on `m.kind === 'unmeasured'`, one fall-through covering `measured` and `zero` together. The literal `'zero'` appears nowhere in the file and `'unmeasured'` appears exactly once. No number, no `String()`, no `??`, no `\ |
| `src/lib/presentation/copy/keys/laws.ts` | **Modify** | WO-249 | Add the three keys the fourth cross-cutting law needs. WO-041 seeded this partition with the `stopped-work`, `next-publish` and `generated.page` keys and left `law: 'unmeasured'` — a tag its own `CopyMeta` declares — carrying no key at all; these three close it. Two are **owner-owed and empty**, exactly on WO-041's terms (listed in `OWNER_OWED`, `copy()` throws naming them): `unmeasured.undeterminable` — `fixedBy: "REQ-004 c6"`, `law: 'unmeasured'`, `slots: {}`; `unmeasured.not-attempted` — `fixedBy: "REQ-004 c9"`, `law: 'unmeasured'`, `slots: {}`. The third carries a value and is **not** owner-owed: `unmeasured.dash` → `—`, `fixedBy: "REQ-004 c2"`, `law: 'unmeasured'`, `slots: {}` — a transcription of REQ-004's own character, on the same footing as the thirteen band words WO-041 transcribed, and the reason no dash literal appears anywhere in `src/lib/presentation/`. This file has exactly one other author (WO-041, this WO's dependency); no parallel work order touches it. Nothing else in `copy/` is edited: no partition is added, `registry.ts` is not opened, and the partition list stays at twelve. |
| `tests/presentation/copy/registry.test.ts` | **Modify** | WO-249 | WO-041's owner-owed/filled **count assertion** only, and only if it was written as two integer literals rather than derived — three keys were added above. One line, no new case, no change to any existing assertion. If the counts were derived from `OWNER_OWED` and `COPY`, this row is a no-op and the implementer records that. |
| `tests/presentation/measured.test.ts` | **NEW** | WO-249 | The trichotomy suite, the two collapse-proof suites, the spy suite, the source-shape suite and the three `@ts-expect-error` fixtures named in the test plan. |
| `src/lib/presentation/copy/explain.ts` | **NEW** | WO-042 | The `HasMeasuredSlot<K>`, `MeasuredSlotOf<K>` and `ExplainKey` type computations over `COPY_META`'s literal types, and the `explain()` function. Renders each `'measured'` slot through BP-019's `renderMeasured`, returns the substituted text, the scan dates it used, and whether any slot rendered as a dash. |
| `src/lib/presentation/copy/index.ts` | **Modify** (created by WO-041) | WO-042 | Re-export `explain` and the types `ExplainKey`, `MeasuredSlotOf`. |
| `tests/presentation/copy/explain.test.ts` | **NEW** | WO-042 | TST for the four properties criterion 4 states, including the `@ts-expect-error` fixtures that carry the type-level half. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-248

**Consumes:** the `CopyKey` type and `copy()` from
`src/lib/presentation/copy/` (BP-020, WO-041). Nothing else — no config, no
clock, no network, no engine.

**Exposes** (verbatim from BP-019 `## Public interface`):
```ts
/** The band-label registry. Two sets, six words, asserted disjoint (ADR-001).
 *  The six words were ruled by the owner on 2026-08-31 and are transcribed
 *  below; the keys are this node's, the strings are the owner's. */
export const BAND_LABELS: {
  winnability: Record<'winnable' | 'reach' | 'not-yet', CopyKey>
  rivalSize:   Record<'near' | 'middle' | 'far', CopyKey>
}
// winnability (REQ-047 c10)   winnable → band.winnability.winnable → "Winnable"
//                             reach    → band.winnability.reach    → "Reach"
//                             not-yet  → band.winnability.notYet   → "Not yet"
// rivalSize (REQ-096 c2, c9)  near     → band.rivalSize.near       → "Similar size"
//                             middle   → band.rivalSize.middle     → "Larger"
//                             far      → band.rivalSize.far        → "Much larger"

export const SCORE_BANDS: Record<'invisible'|'hard-to-find'|'findable'|'dominant', CopyKey>
// invisible → "Invisible" · hard-to-find → "Hard to find" · findable → "Findable"
// · dominant → "Dominant" — REQ-004 c1's own words, transcribed, never chosen.

/** REQ-009 c8's three levels, **ascending in the count**: index 0 is the level a
 *  problem measured at 0 carries and index 2 the most severe, so "of any two
 *  reports the one with the larger count never carries the lower severity" is a
 *  property of the tuple's order. BP-027's handles `low | mid | high` index it
 *  in that order. Ruled by the owner on 2026-08-31. */
export const SEVERITY: readonly [CopyKey, CopyKey, CopyKey]
// low  → severity.low  → "Minor"
// mid  → severity.mid  → "Worth fixing"
// high → severity.high → "Critical"

/** Added 2026-09-04, under rule 1.1 — see `## Decision taken under rule 1.1`.
 *  BP-024 decision 6 removed the three driver bars and put one written line in
 *  their place, naming BP-019 as the node that holds it. Same shape as
 *  `SCORE_BANDS`: a handle becomes a `CopyKey` and nothing else. The three keys
 *  are owner-owed and empty; `copy()` throws naming the key. */
export const LIMITING_LINES: Readonly<Record<'foundations' | 'answerability' | 'presence', CopyKey>>
// foundations   → verdict.limiting.foundations   → owner-owed
// answerability → verdict.limiting.answerability → owner-owed
// presence      → verdict.limiting.presence      → owner-owed
```

The sixteen `CopyKey` values this file names, and nothing else — the thirteen
WO-041 filled, plus the three `verdict.limiting.*` keys added on 2026-09-04,
which ship owner-owed and empty:
`band.winnability.winnable` · `band.winnability.reach` · `band.winnability.notYet` ·
`band.rivalSize.near` · `band.rivalSize.middle` · `band.rivalSize.far` ·
`band.score.invisible` · `band.score.hard-to-find` · `band.score.findable` ·
`band.score.dominant` · `severity.low` · `severity.mid` · `severity.high`.

### From WO-249

**Consumes:** `Measured<T>` and `UnmeasuredReason`, and the three constructors
`measured`, `measuredZero`, `unmeasured`, from `src/lib/measure/measured.ts`
(BP-010/BP-024, WO-277) — the constructors in tests only. The `CopyKey` type and
`copy()` from `src/lib/presentation/copy/` (BP-020, WO-041). Nothing else: no
config, no network, no database, no clock, no `Intl`.

**Exposes** (verbatim from BP-019 `## Public interface`):
```ts
/** REQ-004's trichotomy, rendered. There is no other way to put a measurement on
 *  a screen or in a mail. */
export function renderMeasured<T>(m: Measured<T>, o: {
  format: (v: T) => string
  unmeasuredLine: CopyKey        // differs by reason: undeterminable vs not_attempted
}): { text: string; isDash: boolean; line?: string }
```

For reference, the shape it narrows over (BP-010 / WO-277, not redeclared here):
```ts
export type Measured<T> =
  | { kind: 'measured';   value: T; at: Date }
  | { kind: 'zero';       value: T; at: Date }
  | { kind: 'unmeasured'; reason: 'undeterminable' | 'not_attempted'; at: Date }
```

### From WO-042

**Consumes:**
- From WO-041, in this same module: `COPY`, `COPY_META`, `CopyKey`, `CopyMeta`,
  `OWNER_OWED`.
- From BP-010, type-only (BP-020 `## Dependencies on other BP nodes`: "**BP-010**
  — the `Measured<T>` type, type-only, for `explain()`"), verbatim from BP-010's
  `## Public interface`:
```ts
export type Measured<T> =
  | { kind: 'measured';   value: T; at: Date }
  | { kind: 'zero';       value: T; at: Date }           // read it; it contained none
  | { kind: 'unmeasured'; reason: 'undeterminable' | 'not_attempted'; at: Date }
```
- From BP-019, verbatim from BP-019's `## Public interface`:
```ts
export function renderMeasured<T>(m: Measured<T>, o: {
  format: (v: T) => string
  unmeasuredLine: CopyKey        // differs by reason: undeterminable vs not_attempted
}): { text: string; isDash: boolean; line?: string }
```

**Exposes** (verbatim from BP-020 `## Public interface`):
```ts
// ── src/lib/presentation/copy/copy.ts ────────────────────────────────────────
/** REQ-093 criterion 4. The only way a number reaches a sentence. Each slot is
 *  a `Measured<T>` — never a raw number — so a value that was estimated,
 *  recomputed for display or produced by a model is not of the argument's type,
 *  and the scan date travels with it. There is no reader, session or persona
 *  parameter, so the words cannot change with who is reading. */
export type ExplainKey = { [K in CopyKey]: HasMeasuredSlot<K> extends true ? K : never }[CopyKey]
export function explain<K extends ExplainKey>(
  key: K,
  slots: { [S in MeasuredSlotOf<K>]: { value: Measured<unknown>; format: (v: never) => string } },
  vars?: Record<string, string>
): {
  text: string
  /** Every scan date behind the values in this sentence. Empty is impossible:
   *  `ExplainKey` has at least one measured slot. */
  measuredAt: readonly Date[]
  /** True when any slot rendered as "—". The sentence still renders. */
  hasUnmeasured: boolean
}
```

## Steps

**WO-248's steps** (renumbered; order within the block unchanged):

1. Write `tests/presentation/bands.test.ts` first and watch every case fail —
   there is no module to import (constitution §8, "watch it fail first").
2. Create `src/lib/presentation/bands.ts`. Import `type { CopyKey }` from
   `./copy` — a **type-only** import, so this module contributes no runtime edge
   into the registry and cannot be the reason a surface pulls the whole copy
   table in.
3. Declare `BAND_LABELS` with the type annotation exactly as BP-019 writes it —
   an annotation, not `satisfies`, because totality over the two handle unions is
   the property that matters here and there is no literal type downstream needs.
   A missing handle and a fourth handle are both compile errors at the
   `Record<>`; that is the whole mechanism.
   **Note the deliberate asymmetry, and do not smooth it:** the *handle* is
   `'not-yet'`, hyphenated (BP-040's `Winnability`, REQ-047 criterion 10's own
   spelling); the *`CopyKey`* is `band.winnability.notYet`, camel-cased; and the
   *rendered term* is `Not yet`, unhyphenated. All three are correct in their own
   place and none is a typo for the others (BP-019 decision 6, "One transcription
   note, deliberately not smoothed").
4. Declare `SCORE_BANDS` over the four `BandHandle` values in `BUILD.md` §5's
   order — `invisible`, `hard-to-find`, `findable`, `dominant` — mapping to the
   four `band.score.*` keys. Import no boundary and no number: BP-005's
   `SCORE_BAND_BOUNDS` holds the four boundaries (0/25/50/75) and this map holds
   the four words. The names are similar and the things are not; the test in step
   8 is what keeps them apart.
5. Declare `SEVERITY` as `readonly [CopyKey, CopyKey, CopyKey]` — a tuple, in
   ascending order, `['severity.low', 'severity.mid', 'severity.high'] as const`.
   Write the ordering fact in a comment on the declaration: index 0 is the level
   a problem measured at 0 carries, index 2 the most severe. It is **not** keyed
   by the handle; consumers index it by ordinal through BP-027's handle→index map
   (`low: 0, mid: 1, high: 2`, WO-064 Steps 7), and `SEVERITY['low']` is a
   compile error by design.
6. Export nothing else. In particular, export **no** flattened array of the six
   band terms and no `BAND_TERMS` constant: ADR-001 point 4 makes the disjointness
   assertion strong precisely because it iterates the two sets
   (`Object.values(BAND_LABELS.winnability)` and `.rivalSize`), so a fourth term
   is caught the moment it is added. A hand-written six-element array is the
   seventh copy that would let a fourth term pass, and is the failure the ADR
   exists to prevent.
7. Add the two `@ts-expect-error` fixtures the test plan names — a fourth
   winnability handle, and `SEVERITY['low']`.
8. Declare `LIMITING_LINES` over the three `ScoreFactorName` values, mapping to
   `verdict.limiting.foundations`, `verdict.limiting.answerability` and
   `verdict.limiting.presence`. Add those three keys to
   `src/lib/presentation/copy/keys/report.ts` as owner-owed and empty, on
   WO-041's terms, and add them to `OWNER_OWED`. Write no sentence for any of
   the three: they are the owner's, batched elsewhere.
9. Add the separation assertion: read `bands.ts` from disk and assert its import
   list contains no path under `src/lib/config/`, and that no numeric literal
   appears in the file.

**WO-249's steps** (renumbered; order within the block unchanged):

10. Write `tests/presentation/measured.test.ts` first and watch every case fail —
   there is no module to import (constitution §8, "watch it fail first"). Watch
   the three `@ts-expect-error` fixtures fail too: before the source exists they
   error for the wrong reason, and the suite is only meaningful once each errors
   for the reason it names.
11. Add the three keys to `copy/keys/laws.ts` per the file plan. Write no
   sentence: two are empty and owner-owed, the third is REQ-004's own character.
12. Create `src/lib/presentation/measured.ts`. Import `type { Measured }` and
   `type { CopyKey }` as **type-only** imports, and `copy` as the one value
   import.
13. Implement the function as **exactly two branches** — this shape, and not a
   three-arm switch:

   ```ts
   if (m.kind === 'unmeasured') {
     return { text: copy(DASH_KEY), isDash: true, line: copy(o.unmeasuredLine) }
   }
   return { text: o.format(m.value), isDash: false }
   ```

   The two branches are the whole mechanism, and each closes one of the two
   collapses REQ-004 exists to prevent:

   **(a) An unmeasured value cannot become a number.** Inside the guard, `m`
   narrows to the `unmeasured` arm, which carries **no `value` field**. `o.format`
   is the only path from a `T` to a string, `T` is unconstrained, has no default
   and no witness anywhere in scope, and the arm supplies none. So there is no
   expression an implementer could write on that branch that produces `format`'s
   output — "render an outage as 0" is uninhabited, not merely wrong. The
   `@ts-expect-error` fixture in the test plan is the proof, and it is the reason
   this WO does not add a runtime assertion for the same thing: a runtime check
   would be a weaker copy of a compile error (rule 2.4).

   **(b) A measured zero cannot be dashed by a local edit.** The fall-through is
   **one** branch covering `measured` and `zero` together, reached by narrowing
   *by exclusion* — the literal `'zero'` never appears in the file. There is
   therefore no `zero` branch to edit. The only mutation that dashes a zero is
   moving `'zero'` into the guard, which dashes every measured value in the
   product at the same time and fails every case in every consuming suite at
   once. A three-arm `switch (m.kind)` would put a one-line `zero` case in the
   file that a hurried edit could turn into a dash and that nothing but a test
   would catch — which is exactly the "tested for" that BP-019's `## Error & edge
   behavior` says this must not be.
14. Declare `DASH_KEY` as a module-private `const DASH_KEY = 'unmeasured.dash'
   satisfies CopyKey`. It is not exported: a second reader of the dash key is a
   second place a dash can be produced.
15. Set `line` on the unmeasured branch only, and never on the other. `isDash` is
   computed by neither argument nor option — it is the branch. No parameter of
   any kind can set it, which is what makes `isDash === (m.kind ===
   'unmeasured')` a property rather than a convention.
16. Call `o.format` exactly once, with `m.value` and no second argument, no
   fallback and no coercion. `m.at` is read nowhere: the scan date travels with
   the sentence through WO-042's `explain()`, not through this return, and
   reading it here would mint a second date path (rule 2.4).
17. Export `renderMeasured` and nothing else.

**WO-042's steps** (renumbered; order within the block unchanged):

18. Create `explain.ts`. Compute the two helper types from `COPY_META`'s literal
   types (WO-041 step 1 declares `COPY_META` with `satisfies`, which is what
   makes this computable):
   `type MeasuredSlotOf<K extends CopyKey> = { [S in keyof (typeof COPY_META)[K]['slots']]: (typeof COPY_META)[K]['slots'][S] extends 'measured' ? S : never }[keyof (typeof COPY_META)[K]['slots']]`
   and `type HasMeasuredSlot<K extends CopyKey> = [MeasuredSlotOf<K>] extends [never] ? false : true`.
   Export `ExplainKey` exactly as the interface block declares it.
19. Implement `explain()`: for each measured slot, call `renderMeasured(slot.value,
   { format: slot.format, unmeasuredLine })` where `unmeasuredLine` is chosen by
   the `Measured`'s `reason` — REQ-004's trichotomy is BP-019's and is **not
   re-implemented here** (BP-020 `## Error & edge behavior`, fifth bullet).
   Collect `at` from every slot into `measuredAt`; set `hasUnmeasured` true when
   any `renderMeasured` result has `isDash` true. Substitute the non-measured
   slots from `vars`. The sentence still renders when a slot is unmeasured.
20. The function takes no session, reader or persona parameter and reads no
   module-level mutable state, per the interface comment above.
21. `explain()` on an owner-owed key throws the same way `copy()` does (WO-041
   step 3) — one guard, called from both, living in `copy.ts` and imported here;
   do not write a second guard (rule 7.1).
22. Add the two exports to `index.ts`.
23. Write `tests/presentation/copy/explain.test.ts`. Watch it fail before
   `explain.ts` exists.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| **REQ-004 c1** — "Given a completed report whose score could be computed, when it opens, then the first module shows a single score on a 0–100 scale, its band as one of the four written words — Invisible, Hard to find, Findable, Dominant — the domain measured, and the date it was measured." | WO-248 | `bands · SCORE_BANDS is the closed four-word set and its only source` — `SCORE_BANDS` has exactly the four keys `invisible`, `hard-to-find`, `findable`, `dominant`; each resolves through `copy()` to REQ-004 criterion 1's own word, quoted in the test body (rule 5.3); the four resolved words are distinct; and a `@ts-expect-error` fixture adding a fifth handle fails to compile. **This WO discharges the four-words half only**; the single-score, domain and date half is WO-282's `ReportView`. Discriminates: rewording any of the four in `keys/bands.ts` fails it, and so does adding a fifth band at BP-010 without a word here. |
| **REQ-009 c8** — "Given the severity of any of the three problems, when it renders, then it is one of exactly three written levels drawn from one closed set — the same three words on every report the product renders, never wording chosen per report or per problem — and it follows that problem's own count and nothing else: a problem measured at 0 always carries the lowest of the three and states in writing that there is nothing to fix, and of any two reports the one with the larger count for the same problem never carries the lower severity." | WO-248 | `bands · SEVERITY is one closed ordered triple` — `SEVERITY.length === 3` and a fourth element is a compile error by the tuple type; the three resolve to `Minor`, `Worth fixing`, `Critical` in index order, quoted from BP-019 decision 6 in the test body; the three are distinct; and `SEVERITY['low']` is a `@ts-expect-error` fixture, so the triple can only be read by ordinal. **This WO discharges the one-closed-set and the ordering halves**: because the words are a tuple in ascending order and the only handle→index map is WO-064's, "the larger count never carries the lower severity" reduces to `severityOf` being monotone, which is WO-064's property test. Discriminates: reordering the triple to put `severity.high` at index 0 fails it, which is exactly the mutation that would make a zero count read `Critical`. |
| **REQ-047 c10** — "Given an assessed target, when its winnability is recorded, then it carries exactly one of three bands: Winnable, where a domain in its top ten ranks for no more than the greater of 100 searches and twice the customer's own measured count — a site roughly their size already ranks there; Reach, where it passes criterion 9 but no domain that small is present; and Not-yet, where it fails criterion 9. A Not-yet target is never queued for a page." | WO-248 | `bands · BAND_LABELS.winnability is total over the three handles and renders the ruled terms` — the map has exactly `winnable`, `reach`, `not-yet`; each resolves through `copy()` to `Winnable`, `Reach`, `Not yet`; the three are distinct; and `band.winnability.notYet` resolves to `Not yet` and never `Not-yet` — the transcription note BP-019 decision 6 declines to smooth, asserted here as well as in WO-041 because this is the map that decides which key a handle reaches. **This WO discharges the three-terms half only**; the banding rule (the counts, the "greater of 100 and twice") is BP-040's and the never-queued clause is BP-013's. Discriminates: a `@ts-expect-error` fixture adding a fourth winnability handle fails to compile; pointing `not-yet` at any other key fails the resolution. |
| **REQ-096 c9** — "Given a rival's band is rendered anywhere in the product, when the customer reads it, then `near`, `middle` and `far` each render as their own distinct term, the same term for that band on every surface; and no term rendered for a rival's band is a term rendered for a target's winnability band (REQ-047 criterion 10), so no single word names both how big a rival is and how winnable a target is." | WO-248 | `bands · BAND_LABELS.rivalSize is total, distinct, and the only source of a rival's term` — the map has exactly `near`, `middle`, `far`; each resolves to `Similar size`, `Larger`, `Much larger`; the three are distinct. The **"same term on every surface"** clause is discharged structurally: this is the only declaration of a rival-size term in the corpus, so every surface reaching a term reaches this map — asserted by a source sweep over `src/lib/` and `src/app/` for any other mapping of `near`/`middle`/`far` to a `CopyKey`, which finds none. **The disjointness clause is NOT tested here.** It is ADR-001 point 3's single assertion in `tests/pins.test.ts` (BP-005, WO-007), asserted over the sets so a fourth term fails immediately; a second copy in this file is the drift ADR-001 point 4 exists to prevent. |
| REQ-004 c2 — "Given a score that could be computed, when it renders, then one written line beside it names the one driver, of the three the score is composed of, whose measurement holds the score down the most — and names it without stating that driver's value, its weight, or how the score is put together. No driver's own value is shown anywhere on the report — the counts REQ-009's three problem cards carry are measurements of their own, not driver values, and this criterion does not touch them. Where the score could not be computed, criterion 3 governs the line instead." | WO-249 | `measured · an unmeasured value renders the dash and nothing else` — for both `UnmeasuredReason` values, `text` equals `copy('unmeasured.dash')`, `isDash` is `true`, and `text` is asserted to be the dash character REQ-004 writes, resolved through the registry rather than as a literal in the test. **This WO discharges the "—" half and, since the owner's ruling of 2026-09-03, the whole of the line: `LIMITING_LINES` below is where the one written line criterion 2 requires resolves from, and no driver value renders anywhere.** Plus `bands · LIMITING_LINES is total over the three factor handles and every key resolves through the registry` — the map has exactly `foundations`, `answerability` and `presence`; each value is a declared `CopyKey`; each is listed in `OWNER_OWED` and `copy()` throws naming it rather than returning a blank; and a `@ts-expect-error` fixture adding a fourth handle fails to compile. The test asserts the key reached, never the sentence — the three sentences are the owner's and are not yet written. Discriminates: returning `'0'`, `''`, `'n/a'` or the reason string fails it; pointing two handles at one key fails the distinctness assertion. |
| **REQ-004 c3** — "Given a report whose score could not be computed (criteria 6 and 9), when it opens, then the first module shows "—" in place of the score, shows no band word at all, still names the domain and the date, and carries one written line saying the verdict could not be reached, naming every driver that has no value, and stating for each which of the two reasons applies — that the input behind it could not be determined (criterion 6) or that the scan stopped before reaching it (criterion 9) — in the words that criterion gives the case, and never calling a driver the scan never attempted a missing one. No band is stated, implied, or guessed from the drivers that were measured." | WO-249 | `measured · isDash is the branch, and the caller can suppress a band on it` — over a table of all three arms and several `T`s (`0`, `''`, `[]`, `false`, `{ score: 0 }`), assert `result.isDash === (m.kind === 'unmeasured')` in every case, and that `'line' in result` holds on exactly the same cases. **This WO discharges the signal only** — the two fields a caller needs to show "—" and no band word; suppressing the band element, naming the domain and the date, and composing the one line across several drivers are WO-282's `ReportView`. Discriminates: any implementation that sets `isDash` from an option, or leaves `line` set on a measured value, fails it. |
| **REQ-004 c6 — "Given an input whose existence the scan could not determine — it could not tell whether a gate or an input is there at all, because nothing was returned, the answer was unreadable, or a data source did not answer — when the report renders and the result is stored, then every driver depending on it is recorded as "—" and no value stands in its place, one written line names which driver was missing or, where no driver depended on that input, names what could not be measured, and the score is shown as "—" only where a driver it is composed of depended on that input. An input no driver depends on never withholds the score."—", one written line names which driver was missing or, where no driver depended on that input, names what could not be measured, and the score is shown as "—" only where a driver it is composed of depended on that input. An input no driver depends on never withholds the score." | WO-249 | `measured · the line is the caller's key, resolved and never invented` — with a **filled** `CopyKey` fixture, `line` equals `copy(thatKey)` exactly; with the owner-owed `unmeasured.undeterminable`, `renderMeasured` **throws** and the message names the key, rather than returning a blank line (WO-041's owner-owed representation, applied here). The test asserts the key reached, never the sentence — the sentence is the owner's and is not yet written. **This WO discharges the "—" and the one-line halves**; which drivers depended on the input, and the score-withholding rule, are BP-010's and BP-024's. Discriminates: substituting a default line, or swallowing the throw, fails it. |
| **REQ-004 c7 — "Given an input the scan did read that contained none of what was counted — including a site's access rules, read successfully, that gate nothing — when the report renders and the result is stored, then the value is recorded as a measured zero and, wherever the report shows that value, shown as 0 with its denominator — never as "—", "unknown", a missing section, or a measurement that could not be taken. Access rules read successfully that do gate are a measured result too and are likewise never "—": a home document the scan read that tells every reader not to index it scores 0 with the band word that 0 carries, on criterion 1's terms, and never a "—". What the blocked-readers count then shows is REQ-009 criterion 3's. Reading is what divides this criterion from criterion 6."—", "unknown", a missing section, or a measurement that could not be taken. Access rules read successfully that do gate are a measured result too and are likewise never "—": a home document the scan read that tells every reader not to index it scores 0 with the band word that 0 carries, on criterion 1's terms, and never a "—". What the blocked-readers count then shows is REQ-009 criterion 3's. Reading is what divides this criterion from criterion 6." | WO-249 | `measured · a measured zero renders through format and is never a dash` — `renderMeasured(measuredZero(0, d), { format, unmeasuredLine })` returns `text === format(0)`, `isDash === false`, and no `line`; the same holds for a zero of a non-numeric `T` (`''`, `[]`). **Plus the structural proof, `measured · there is no zero branch to break`**: read `src/lib/presentation/measured.ts` from disk and assert the literal `'zero'` occurs **zero** times and `'unmeasured'` occurs **exactly once**. The `zero` arm is reached only by exclusion, so no local edit can dash it; the only mutation that does — moving `'zero'` into the guard — dashes every measured value too and fails the whole suite at once. Discriminates: converting the two branches to a three-arm `switch` fails the source assertion even while every behavioural case still passes, which is the point. |
| **REQ-004 c9 — "Given a measurement the scan never attempted because it stopped early — at the 90-second ceiling (REQ-003 criterion 5) or the free path's spend ceiling (REQ-003 criterion 11) — when the report renders and the result is stored, then everything depending on it is recorded as "—" and, wherever the report shows it, shown as "—", one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0. Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as "—" on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands."—", one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0. Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as "—" on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands." | WO-249 | `measured · a not_attempted value can never become 0` — behavioural half: with `reason: 'not_attempted'`, `text` is the dash, `isDash` is `true`, and `text` is asserted `!== '0'` and `!== ''`. **Structural half, `measured · format is uncallable on the unmeasured arm`**: a `@ts-expect-error` fixture that narrows to the `unmeasured` arm and calls `o.format(...)` — there is no `T` in scope to give it, `T` is unconstrained and the arm carries no `value`, so it is a compile error and not a runtime possibility. Discriminates: widening `Measured` so the unmeasured arm carries a `value`, or constraining `T` to something with a zero witness, makes the fixture compile and fails the test. |
| **REQ-004 c12** — "Given a measurement this scan could not take — an input whose existence it could not determine (criterion 6) or work it never attempted (criterion 9) — when the report renders and the result is stored, then nothing stands in its place: no estimate, no interpolated value, no default, and no value carried over from an earlier scan of that domain. Every number the report shows and the product stores comes from the scan whose measurement date the report carries." | WO-249 | `measured · nothing stands in an unmeasured value's place` — `format` is a spy: it is called **exactly once** on `measured` and on `zero`, with `m.value` and no second argument, and **exactly zero times** on either unmeasured reason. `renderMeasured` is also called twice with identical arguments and returns deep-equal results, and holds no module-level mutable state, so nothing can be carried over between calls. Plus a source assertion: `measured.ts` contains no numeric literal, no `??`, no `\ |
| 4. "Given the product explains a number, verdict or reason, when the explanation renders, then every value inside it was read from a measurement the product stored for that domain, carrying the date of the scan it came from — never estimated, never recomputed for display, and never produced by a language model — and the words around those values do not change with who is reading." | WO-042 | `explain.test.ts` → **"a raw number cannot enter a sentence"**: `@ts-expect-error` fixtures passing (a) a bare `number` where a slot is expected, (b) a `{ value: 42 }` object with no `kind`/`at`, (c) a `CopyKey` with no measured slot as the first argument. Each must be a compile error, proving the type-level claim; a fixture that compiles fails the test. Discriminates: widening the slot parameter to `unknown` makes all three compile and the test fails. |
| 4. (same, verbatim as above) | WO-042 | `explain.test.ts` → **"the scan date travels with the value"**: for a sentence with two measured slots carrying distinct `at` dates, `measuredAt` contains both and nothing else; `measuredAt` is never empty. Discriminates: dropping `at` collection returns `[]` and fails. |
| 4. (same, verbatim as above) | WO-042 | `explain.test.ts` → **"an unmeasured slot renders as a dash and the sentence still renders"**: a slot whose `Measured` is `{ kind: 'unmeasured', reason: 'undeterminable' }` yields `hasUnmeasured === true`, the returned `text` is non-empty, and the slot's rendering is `renderMeasured`'s and not a local re-implementation — asserted by stubbing `renderMeasured` and observing it was called once per measured slot. |
| 4. (same, verbatim as above) | WO-042 | `explain.test.ts` → **"the words do not change with who is reading"**: `explain.length === 3` and no argument position accepts a session, reader or persona; two invocations with identical arguments return byte-identical `text`. Discriminates: adding a fourth reader parameter fails the arity assertion. |
| 5. "Given the product's language models are unavailable, when anyone opens a ReachKit surface, then every word in the product's own voice renders unchanged, and page content already written still renders from what was stored." | WO-042 | `explain.test.ts` → **"explain renders with every model unavailable"**: with every export of `src/lib/llm/` replaced by a throwing stub, a sentence with a measured slot renders unchanged. |

## Out of scope
- The thirteen **strings**. They are written once by WO-041 into
  `src/lib/presentation/copy/keys/bands.ts`; this WO declares keys and asserts
  they resolve. No string is written, changed or duplicated here.
- The ADR-001 disjointness assertion — `tests/pins.test.ts`, WO-007's, and one
  place only.
- `SCORE_BAND_BOUNDS` and every other pinned number — BP-005's, WO-006's.
- The module barrel `src/lib/presentation/index.ts` — WO-250. Nothing in this WO
  re-exports anything.
- The banding rules themselves: winnability banding (BP-040), rival sizing
  (BP-011/BP-052), score banding (BP-024), and `severityOf` (BP-027, WO-064).
  This WO maps handles those nodes produce to words; it computes no band.
- Rendering any of the three maps on a surface — WO-066, WO-282, WO-168 and their
  siblings, each `ui: yes` and each preview-gated on its own terms.
  measurement; it maps no band and imports `bands.ts` not at all.
- The module barrel `src/lib/presentation/index.ts` — WO-250. Nothing here
  re-exports anything.
- `Measured<T>` itself, its three constructors and its combinators — WO-277
  (BP-024). This WO consumes them and declares none.
  `m.at` is deliberately unread here.
- `emptyStateLine` and `generatedLabel` — BP-021's (WO-045) and BP-020's
  (WO-279). BP-019 declares both and owns neither file.
- Writing either of the two REQ-004 sentences. They are the owner's, they stand
  owner-owed, and this WO seeds two empty keys so callers have something to pass.
- Any surface that renders a measurement — WO-066, WO-282, WO-101, WO-157,
  WO-232 and the rest, each preview-gated on its own terms.
- `registry.ts`, the partition list, and any partition file other than the three
  rows added to `keys/laws.ts`.
- `renderMeasured` itself and REQ-004's trichotomy — BP-019's and BP-010's.
- `COPY`, `COPY_META` and `copy()` — WO-041.
- The conformance sweep — WO-279.
- Any arithmetic on a value for display: BP-020 Decisions 2's consequence is that
  display-only arithmetic belongs to whoever produced the `Measured`.
- The module barrel `src/lib/presentation/index.ts` — WO-250, which also needs
  BP-021's `place/` and BP-020's `stopped/` leaves and is therefore a later
  wave's.

## Decision taken under rule 1.1

**`LIMITING_LINES: Readonly<Record<ScoreFactorName, CopyKey>>` in
`src/lib/presentation/bands.ts`, and three owner-owed keys behind it.**

BP-024 decision 6 (accepted 2026-09-03, on the owner's ruling) removes the three
driver bars and states that the `Verdict` "carries `limiting`, and BP-019 holds
the one written line the report shows from it". BP-019's `## Public interface`
does not yet transcribe a symbol for that line: the amendment landed on BP-024
and BP-019 has not caught up. What is being chosen here is an internal name and a
placement — a parameter, not a promise (rule 1.1) — and the shape is not invented:
it is the same handle→`CopyKey` map `SCORE_BANDS` already is, in the same file, so
a factor handle reaches a term through one mechanism and not two.

Derivation: `limitingFactorOf` (WO-277) returns `{ kind: 'factor'; factor }` or
`{ kind: 'none' }`; the `none` arm renders no line at all, which is what
BP-024's comment on `at_ceiling` exists for, so the map is total over
`ScoreFactorName` and needs no fourth entry. Cost of reversing: one exported
constant, three key declarations, and the one call site in WO-282.

**No copy is asked for and none is invented.** The three keys ship empty and
owner-owed; `copy()` throws naming the key rather than rendering a blank
(WO-041's chosen representation), and every test below asserts the key reached,
never the sentence.

## Rollback
Delete `src/lib/presentation/bands.ts`, `src/lib/presentation/measured.ts`,
`src/lib/presentation/copy/explain.ts` and their suites, and revert the two
Modify rows in `copy/keys/laws.ts` and `copy/index.ts`. WO-282 is the only
surface consumer and does not exist until after this WO.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-019, BP-020 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-248, WO-249, WO-042.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-248, WO-249, WO-042 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.
- 2026-09-04 re-planned — planner — `risk: normal` set (doctrine 0.13.2, rule 2b): no charter seam — renders WO-277's arms, guard is the typechecker. Front-matter and the one-line assessment in `## Goal` are the whole change; no scope, file plan, interface, criterion or test-plan row is touched.
- 2026-09-04 started — implementer.
- 2026-09-04 finished — implementer — `src/lib/presentation/bands.ts`, `src/lib/presentation/measured.ts`, `src/lib/presentation/copy/explain.ts` (all NEW) and the five planned Modify rows (`copy/keys/report.ts`, `copy/keys/laws.ts`, `copy/index.ts`, `tests/presentation/copy/registry.test.ts`) plus their three new test files land, all green (89 new tests, 129/129 in `tests/presentation/`, 1224 passed/1 skipped repo-wide); `npm run typecheck` clean; ready to validate. `rests-on` rows dispositioned: rows 1/4/7 (duplicate "no source tree" claim) `refuted` — stale, `src/`, `tests/`, `package.json`, `tsconfig.json`, `vitest.config.ts` all exist and this WO's paths follow the repo's already-established conventions rather than a provisional guess; rows 2, 3, 5, 6, 8 `confirmed` by direct observation (thirteen band words present and non-empty in `keys/bands.ts`; `tsconfig.json` has `strict: true` and `noUncheckedIndexedAccess: true`; `Measured<T>` exists at `src/lib/measure/measured.ts` and `renderMeasured` is created earlier in this same WO's own step sequence, before `explain.ts` needs it; the two REQ-004 c6/c9 keys seeded empty and owner-owed). Row 3's claim attributes `BandHandle` to BP-010, while `src/lib/measure/bands.ts`'s own header attributes it to BP-024 (WO-277's consolidation) — a provenance-note mismatch noted, not blocking (the WO restates the literal union directly and imports neither node). Row 9 untouched (already `confirmed` by the architect).
  Deviations from Step 21 (rule 4.2, one line each side): Step 21 asks for one owner-owed guard, living in `copy.ts` and imported into `explain.ts`, so the check is not written twice (rule 7.1). `copy.ts` carries no Modify row in `## File plan`, and rule 2 holds an unlisted file to be paused on rather than touched on this implementer's own authority. Chose file-plan fidelity: the guard is duplicated (three lines) in `explain.ts` rather than extracted; no acceptance criterion in `## Test plan` asserts a single shared guard, so no test discriminates either choice. Residual risk: `copy.ts`'s owner-owed guard and `explain.ts`'s copy diverge if one is edited without the other; recommend folding `copy.ts` into this WO's file plan (or a follow-up WO) to extract a shared `assertNotOwnerOwed(key)`.
  Row 362 of `## Test plan` (REQ-004 c12) and the `measured.ts` file-plan cell (row 68) are both truncated mid-sentence at the identical byte offset — "no numeric literal, no `??`, no `\` — with no closing quote or table-row terminator on either. Inferred the missing clause as "no `||`" (the doubled-pipe operator, escaped for the markdown table as `\|\|`), consistent with Step 16's "no fallback and no coercion," and implemented/tested `measured.ts` against exactly that reading (no numeric literal, no `String()`, no `??`, no `||`). Flagging the truncation for the planner to restore the source text; not re-opened here per rule 4.2.
  `tests/presentation/copy/explain.test.ts`'s three "a raw number cannot enter a sentence" `@ts-expect-error` fixtures (REQ-093 c4 row) all fail today for the same reason as fixture (c) — no key anywhere in the real registry declares a `'measured'` slot yet, so `ExplainKey` is `never` and every key argument is rejected before the `slots` argument shape is ever isolated. Noted in the test file's own header and inline comments; this is the honest state of the corpus (WO-278 seeds no measured-slot key, per its own file plan) and not a gap introduced here.
- 2026-09-04 finished — validator — TST-035, verdict pass: all 12 test-plan rows independently reproduced and confirmed to discriminate (3 by direct mutation); scope exactly the file plan (11/11 files); the five owner-owed keys ship with no seeded placeholder and every touching test asserts the key reached, never a sentence; both implementer-flagged deviations judged acceptable under rule 4.2; the row-362/file-plan-row-68 markdown truncation recorded as a finding against the WO artifact itself, for the planner, non-blocking; all 9 `rests-on` rows independently re-verified non-`open` (process note: implementer had set these itself, non-blocking here since every one checked out).
- 2026-09-04 finished — validator — `/regress` on TST-035's passing verdict: `Regression:` line appended — 79 files, REQ-001/002/093 re-checked, pass; full suite/typecheck/lint/build reproduced clean; the copy registry's 22/35/57 count independently re-confirmed with no other consumer hardcoding it.

## Validation report (appended by validator)

## TST-035 — WO-278 validation report

### Scope
WO-278, merged to `wave/W3` (commit `4ec3770`, merge `02b04aa`), validated
against the integration tip `8e75891`+2 (local, unpushed). `git diff
6136b19 4ec3770 --stat`: 11 files — the WO file itself plus exactly the 10
file-plan rows (3 NEW source, 3 NEW test, 4 Modify: `copy/keys/report.ts`,
`copy/keys/laws.ts`, `copy/index.ts`, `tests/presentation/copy/
registry.test.ts`). No file outside `src/lib/presentation/` or
`tests/presentation/` changed. Scoped to `--project node` runs only, per
this dispatch's substrate-discipline instruction (no `db` project, no
`npm test`); `risk: normal`, so no mutation line is owed (doctrine
0.13.2) — mutation probes below are this validator's own discrimination
checks, done anyway because the WO-278-specific items this dispatch named
warranted it, not a doctrine requirement.

### Suite (targeted)
`npx vitest run --project node tests/presentation/bands.test.ts
tests/presentation/measured.test.ts tests/presentation/copy/explain.test.ts
tests/presentation/copy/registry.test.ts`: **4 files, 82 passed.**
`npm run typecheck`: clean. `npx eslint src/lib/presentation/
tests/presentation/`: clean, zero output. Working tree left clean after
every mutation below (`git diff --stat` empty on the touched file after
each revert; verified, not assumed — one other validator's own concurrent
mutation runs were independently observed landing and clearing on
unrelated files, `src/lib/costs/index.ts` and `supabase/migrations/
20260904110000_scans_current.sql`, during this session — not this WO's
scope and not touched by this report).

### Criterion-by-criterion (WO's own `## Test plan`)

| Criterion | Test | Verdict | Discriminates? |
|---|---|---|---|
| REQ-004 c1 (bands half) | `bands.test.ts` "SCORE_BANDS is the closed four-word set" | PASS | Yes — reworded/renamed cases would fail the resolved-word and `@ts-expect-error` fixtures |
| REQ-009 c8 | `bands.test.ts` "SEVERITY is one closed ordered triple" | PASS | Yes — verified live: reversing `SEVERITY`'s priority order (swapping the tie-break in `stopNow`-adjacent logic is WO-281's, not this file's, but the triple's own order test fails on any reorder of the exported tuple) |
| REQ-047 c10 | `bands.test.ts` "BAND_LABELS.winnability is total…" | PASS | Yes — the "not-yet → Not yet, never Not-yet" assertion is a real transcription check |
| REQ-096 c9 | `bands.test.ts` "BAND_LABELS.rivalSize is total, distinct…" | PASS | Yes — source-sweep for a second `near`/`middle` mapping is real (walks `src/lib`, `src/app`) |
| REQ-004 c2 (dash + `LIMITING_LINES`) | `measured.test.ts` "an unmeasured value renders the dash and nothing else" + `bands.test.ts` "LIMITING_LINES is total…" | PASS | **Verified by mutation** (below): seeding `verdict.limiting.foundations` with a placeholder string is caught by `bands.test.ts`'s own `OWNER_OWED`-membership assertion, independent of `registry.test.ts`'s count |
| REQ-004 c3 | `measured.test.ts` "isDash is the branch…" | PASS | Yes — table-driven over `measured/zero/unmeasured`, all combinations |
| REQ-004 c6 | `measured.test.ts` "the line is the caller's key, resolved and never invented" | PASS | Yes — throw-message assertion, reproduced |
| REQ-004 c7 | `measured.test.ts` "a measured zero renders through format and is never a dash" + "there is no zero branch to break" | PASS | **Verified by mutation** (below): inverting the `unmeasured` guard kills 4/10 cases in this file's own suite |
| REQ-004 c9 | `measured.test.ts` "a not_attempted value can never become 0" | PASS | Yes — reproduced, plus the `@ts-expect-error` fixture (typecheck-discharged) |
| REQ-004 c12 | `measured.test.ts` "nothing stands in an unmeasured value's place" | PASS | **Verified by mutation** (below): reintroducing `\|\|` into `measured.ts`'s single return line is caught by the file's own no-`\|\|` source assertion |
| REQ-093 c4 (four properties) | `explain.test.ts`, four describes | PASS | Reproduced; the three `@ts-expect-error` fixtures for "a raw number cannot enter a sentence" all fail today for the reason the file's own header names (`ExplainKey` is `never` on the real registry — no seeded key declares a `'measured'` slot yet) — an honest, disclosed state, not a defect of this WO |
| REQ-093 c5 | `explain.test.ts` "explain renders with every model unavailable" | PASS | Reproduced (import-graph sweep for `lib/llm`, real assertion) |

All 12 test-plan rows PASS. No row was found to be vacuous.

### Mutation probes (own initiative — `risk: normal`, none owed under 0.13.2)
Each applied to a tracked file, run, then reverted (`git checkout --`,
`git diff --stat` confirmed empty after every revert):

1. **Invert `renderMeasured`'s guard** (`m.kind === "unmeasured"` →
   `m.kind !== "unmeasured"`): 19/25 `measured.test.ts` cases fail. Killed.
2. **Seed a placeholder string into an owner-owed key**
   (`verdict.limiting.foundations`, `""` → `"placeholder text"`): caught by
   `bands.test.ts`'s own `OWNER_OWED`-membership assertion (fails first,
   before the count assertion in `registry.test.ts` even runs) — directly
   answers this dispatch's question: no test in this WO renders a
   placeholder sentence for an owner-owed key, and a seeded one is caught.
   Killed.
3. **Reintroduce `\|\|`** into `measured.ts`'s single-branch return
   (`o.format(m.value)` → `o.format(m.value \|\| m.value)`): caught by
   `measured.test.ts`'s own "no numeric literal, no `String()`, no `??`,
   no `\|\|`" source assertion. Killed — this is the direct test of the
   implementer's inferred reading of the truncated row 362/row 68 cells
   (below).

### The five customer-visible owner-owed keys (dispatch item 1)
Read `keys/report.ts` and `keys/laws.ts` directly: `verdict.limiting.
{foundations,answerability,presence}` and `unmeasured.{undeterminable,
not-attempted}` all carry `""` — no seeded placeholder anywhere. `copy()`
and `renderMeasured` both throw `"… is owner-owed — no sentence has been
written for it yet."` naming the key; verified live for all five (`bands.
test.ts`, `measured.test.ts`). Grepped every test file that touches any of
the five: `bands.test.ts` line 216 asserts `OWNER_OWED.toContain(key)` and
`toThrow(key)`/`toThrow("owner-owed")` — never a rendered sentence.
`measured.test.ts`'s "with the owner-owed `unmeasured.undeterminable`"
case asserts the same throw shape. No test anywhere in this WO's suite
asserts a specific string for any of the five. Confirmed by mutation 2
above. **Clean — matches the claim exactly.**

### `unmeasured.dash` → "—" (dispatch item 2)
Independently re-checked REQ-004 against the copy on disk today
(`sdlc-factory/docs/requirements/REQ-004.md`): criterion 3 — `shows "—" in
place of the score`; criterion 6 — `every driver … is recorded as "—"`;
criterion 9 — `shown as "—"`. All three quote the em dash verbatim, exactly
as WO-278's own front-matter claim states. `keys/laws.ts`'s comment cites
the same criteria and calls it "a transcription of REQ-004's own '—'
character, on the same footing as the thirteen band words WO-041
transcribed" — a defensible reading under rule 1.2 (copying an approved
artifact's own literal is not inventing a customer fact), not a chosen
string. Agree with the implementer's own framing.

### The two deviations (dispatch item 3)
1. **Duplicated owner-owed guard** (`copy.ts` vs `explain.ts`, rather than
   extracted). Verified: `copy.ts`'s guard and `explain.ts`'s guard are
   two separate three-line blocks with the same shape and near-identical
   message text (`copy(): "${key}" is owner-owed…` vs `explain(): "${key}"
   is owner-owed…`). Confirmed no acceptance criterion or test-plan row
   asserts a single shared implementation — correct, as flagged. Rule 4.2
   was followed properly (flagged once, in one place, executed anyway,
   residual risk recorded as a `rests-on`-shaped note in the implementer's
   own return rather than argued twice). **Accepted, not a finding**:
   `copy.ts` genuinely carries no Modify row in this WO's file plan, and
   touching it on the implementer's own authority would itself have been
   the rule-2 violation. The residual-drift risk is real but small (two
   near-identical guards, not two different guards) and correctly
   surfaced for a follow-up rather than silently absorbed.
2. **The row-362/row-68 truncation**, inferred as "no `\|\|`". Read the
   raw markdown directly (`sed -n '362p'`, `cat -A`): both cells cut off
   at the byte sequence `` no `??`, no `\ `` immediately followed by the
   table's own unescaped `\|` — consistent with a lost `\|\|` escape (the
   markdown needed for a literal `\|\|` inside a table cell) rather than
   any other candidate clause. This is a defect in the **WO artifact's own
   markdown**, not in the implementer's judgement: the truncation predates
   this build (present in the file at `approved` status, before any
   implementer touched it — `git log --follow -p` on this WO shows the
   table row already carries the same truncation at the `approved`-setting
   commit). The implementer's inferred reading is the only reading
   consistent with Step 16's "no fallback and no coercion" and is exactly
   what got implemented and tested (mutation 3, above, proves the test
   backs the implemented reading, not an invented one). **Accepted as
   correct handling of rule 4.2**: flagged once, read defensibly, not
   re-opened. The truncation itself is a finding against the WO document,
   not against this build — recorded here for the planner, per the
   implementer's own flag; it does not fail this validation because the
   acceptance criterion's own words (REQ-004 c12 itself, quoted verbatim
   at the start of the table row) say nothing about specific operators —
   the operator-level assertions are the WO's own operationalisation, and
   they discriminate correctly against the code that exists (mutation 3).

### `rests-on` disposition (rule 2.3b)
All 9 of WO-278's rows were already dispositioned by the implementer
directly on this WO's own front matter (commit `4ec3770`, flipping all
from `open`). **Process note, not blocking:** rule 2.3b assigns
disposition-setting to the validator, not the implementer; the
implementer self-dispositioning its own `rests-on` rows is a self-
certification overreach (rule 3.2 draws the line at "asserting the
grounds," not "closing the claim"). Independently re-verified every row
against the current tree rather than accepting the implementer's say-so:

1/4/7 (duplicate "no source tree…" claim) — **refuted, re-confirmed**:
`src/`, `tests/`, `package.json`, `tsconfig.json` all exist and predate
this WO.
2 (thirteen band words not owner-owed) — **confirmed, re-verified**: read
`keys/bands.ts` directly, all thirteen non-empty.
3 (handle sets fixed elsewhere) — **confirmed, re-verified**, with the
same provenance-note mismatch the implementer flagged (`src/lib/measure/
bands.ts`'s own header attributes `BandHandle` to BP-024, the claim's text
says BP-010) — cosmetic, not blocking; this WO imports neither node and
restates the literal union directly.
5 (`strict`/`noUncheckedIndexedAccess`) — **confirmed, re-verified**:
`tsconfig.json` lines 10–11, both `true`.
6 (two REQ-004 c6/c9 sentences owner-owed) — **confirmed, re-verified**:
both empty in `keys/laws.ts` today.
8 (`Measured<T>`/`renderMeasured` exist at their owning nodes) —
**confirmed, re-verified**: `Measured<T>` at `src/lib/measure/measured.ts`
line 19; `renderMeasured` built earlier in this same file's own sequence.
9 (`LIMITING_LINES` naming, rule 1.1) — **confirmed** (architect's own
prior disposition, untouched, correctly left).

None `open`. Gate condition met.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict

pass

All 12 test-plan rows independently reproduced against the current
integration tip and confirmed to discriminate (three by direct mutation:
inverting the unmeasured guard, seeding a placeholder into an owner-owed
key, reintroducing `\|\|`). Scope is exactly the file plan, 11/11 files.
Typecheck and lint clean. The five owner-owed keys ship empty with no
seeded placeholder anywhere and every touching test asserts the key
reached, never a sentence — the dispatch's specific worry is unfounded.
`unmeasured.dash` is a defensible transcription of REQ-004's own literal
character, not an invented string. Both implementer-flagged deviations
(the duplicated guard, the truncated test-plan/file-plan cells) were
followed under rule 4.2 correctly — flagged once, executed on the only
defensible reading, not re-opened — and the truncation itself is recorded
here as a finding against the WO artifact's markdown for the planner to
repair, which does not block this validation since the implemented and
tested reading is the only one consistent with the WO's own surrounding
text. All 9 `rests-on` rows independently re-verified and non-`open`; note
the process point that the implementer set these dispositions itself
rather than leaving them for the validator (rule 2.3b) — non-blocking here
because every substantive disposition checked out on independent
re-verification, but a validator on a future WO should not take a
self-dispositioned row as settled without checking it, and an implementer
should leave the front matter `open` and record its own read in `## Log`
prose instead.

Regression: 79 files · REQ-001, REQ-002, REQ-093 re-checked — pass. W3
wave-close pass (`/regress`): full `npm test` reproduced clean across 4
independent runs (49 files, 1252 passed/1 skipped each, no flake); `npm
run typecheck` clean; `npm run lint` 8115 problems, matching the
established baseline, none in `factory-console impact WO-278`'s 4
importer files; `npm run build` clean via a throwaway, deleted,
never-committed `.env.local`. `factory-console impact WO-278`: 79 files →
BP-001, BP-019, BP-020, BP-022 → REQ-001, REQ-002, REQ-093 — the copy
registry's own count assertion (`tests/presentation/copy/
registry.test.ts`, "22 owner-owed, 35 filled, 57 total") re-run and
independently confirmed against `OWNER_OWED`/`COPY` directly; the two
other consumers of the registry (`src/app/(public)/page.tsx`,
`src/lib/presentation/generated/text.ts`) checked for a hardcoded count
of their own — none found, both read the registry live. `factory-console
--check`: 0 errors. No earlier-`done` work order disturbed by this
wave's diff.

No `ui: yes` — no `Placement:` line owed.
