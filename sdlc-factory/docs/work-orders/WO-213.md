---
id: WO-213
type: work-order
title: "publish() — the delivery call after the commit, and the outcome written exactly as it happened"
status: approved
ui: no
implements: [BP-045, REQ-056, REQ-062, ADR-084]
depends-on: [WO-206, WO-207, WO-209, WO-212, WO-225]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-213 — publish() — the delivery call after the commit, and the outcome written exactly as it happened

- Branch: wo/WO-213-publish-attempt
- Estimate: ≤1 day

## Goal
Ship `publish()`: claim, call the destination adapter after the transaction has committed, and write the outcome — including `verify_due_at` on a delivery that has a live address.

## File plan

> This repository carries no `src/`, no `tests/`, no `supabase/` and no
> `package.json` (verified 2026-08-31). **Every path below is NEW** — none was
> verified to exist, because none can. Placement is anchored to
> `sdlc-factory/docs/registry/structure.md` and to this work order's blueprint
> `code:` globs, and is provisional against a repo with no code: if the
> implementer finds a file already present at one of these paths, they stop and
> log `failed` naming the drift rather than merging into it.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/publish/attempt/index.ts` | NEW | `publish()` in BP-045's declared shape, the `FailureReason` re-export and `RETRYABLE`. Order: `claim()` (WO-212); on `alreadyPublished` return without calling the adapter; otherwise commit, then call `adapterFor(kind).deliver(page, cfg, draftId)` with a 20 s hard timeout; then one outcome write — `delivery_state`, `published_at`, `live_url`, `remote_id`, `failure_reason`, **`made_live_by_us = result.madeLive`, copied straight off the `DeliveryResult` the adapter returned and derived from nothing else** (ADR-082 Decision 2, carried forward by ADR-084 Decision 4), and `verify_due_at = published_at + 24h` where `live_url` is non-null and null where it is null (BP-049's column, BP-049's rule, **which reads the address and never `destination.kind`**). Then `transition()` to `published` or `failed`. A timeout is the retryable `timeout` reason and the delivery is reconciled by the idempotency key on the next attempt. |
| `tests/publish/attempt/publish.test.ts` | NEW | The adapter is never called before the claim transaction commits (asserted by an adapter that inspects transaction state); an attempt already under way at the moment the switch is recorded runs to a known outcome and that outcome is written exactly as it happened; **`verify_due_at` is set on a delivery that returned an address and left null on one that did not — asserted from the address and against three adapter kinds, `hosted`, `wordpress` and a hypothetical third, so the rule cannot be re-derived from the kind**; a 20 s timeout produces `timeout` and no second delivery on the following attempt. **Four `made_live_by_us` rows:** a stub adapter returning `madeLive: true` with a `liveUrl` stores `true`; a stub returning `madeLive: false` with no `liveUrl` stores `false`; and — the discriminating pair — **a stub returning `madeLive: true` with `liveUrl: undefined` still stores `true`, and one returning `madeLive: false` with a `liveUrl` set still stores `false`.** The last pair fails against any implementation that derives the column from `live_url != null` or from `servesPublicly`, and passes against every other implementation including the wrong ones — which is why the fixtures are contradictory on purpose, and why the pair matters more now than when it was written: `live_url` used to be null for every WordPress row and is now non-null for every one of them, so the wrong predicate has stopped under-classifying and started over-classifying. |

## Interfaces

**Consumes**

- `Actor`, `DestinationAdapter`, `DeliveryResult`, `FailureReason`, `RenderedPage` — WO-206.
- `claim()` — `src/lib/publish/attempt/claim.ts` (WO-212).
- `transition()` — WO-209.
- `adapterFor(kind: DestinationKind): DestinationAdapter` and `withConfig` — BP-058, `src/lib/publish/destinations/index.ts` (WO-225).
- `publications.verify_due_at` — WO-208; the rule for setting it is BP-049's data-model delta.

**Exposes** — copied exactly from BP-045's `## Public interface`:

```ts
// src/lib/publish/attempt/index.ts
export type { FailureReason } from '../types'      // declared in the leaf; re-exported here
export const RETRYABLE: readonly FailureReason[]

export function publish(a: { draftId: string; destinationId: string; by: Actor }): Promise<
  | { ok: true; publicationId: string; liveUrl?: string; alreadyPublished: boolean }
  | { ok: false; reason: FailureReason; retryable: boolean; attemptNo: number }
  | { ok: false; reason: 'held'; heldBy: 'switch_off' | 'ceiling_day' | 'ceiling_week'
                                        | 'destination_not_working' | 'not_publishable' | 'telling_owed' }
>
```

`FailureReason` is *declared* in `src/lib/publish/types.ts` (WO-206) and
**re-exported from this file**, which is BP-045's own text as of 2026-08-31:
"Declared **here**, in the leaf, not in `attempt/`. `DeliveryResult.reason` names
it and `types.ts` imports nothing from `src/lib/publish/`, so declaring it under
`attempt/index.ts` made this file uncompilable as specified. `attempt/index.ts`
re-exports it, which is why every caller's spelling is unchanged. `RETRYABLE`
stays in `attempt/` — it is policy, not shape." So no importer's spelling
changes: `import { FailureReason } from '.../attempt'` still resolves. `RETRYABLE`
is defined here and is the first four members.

## Steps
1. Create `attempt/index.ts` with the block above. `FailureReason` is re-exported from `../types`, never re-declared here — a second declaration would compile and would be the copy rule 2.4 forbids.
2. Call the adapter strictly after the claim transaction has committed. Assert it in test, not in a comment.
3. Write the outcome in one statement. `liveUrl` is `result.liveUrl` — the address the adapter returned, and **since ADR-084 both adapters return one on every successful delivery**, so a WordPress row carries a `live_url` exactly as a hosted one does. `made_live_by_us` is `result.madeLive` and **nothing else** — not `servesPublicly`, not `hostedByUs`, not `liveUrl != null`, not `kind === 'hosted'`. ADR-084 Decision 4: `madeLive` is a fact about what this call did to this page; the others are facts about the destination, and `liveUrl != null` in particular is the substitution ADR-081 alternative 4 rejected and ADR-084 records as **still wrong with the sign reversed** — it is now non-null for every WordPress row and would classify every WordPress page as made-live-by-us.
4. Set `verify_due_at = published_at + 24h` on a delivered publication with a non-null `live_url`, and null otherwise — BP-049's stated rule, landing on this write because this is where `delivery_state` becomes `delivered`. **Read the address, never the kind.** Since ADR-084 that means every delivery at both destinations is due for the 24-hour check, and WordPress joined the verified population with no second destination rule written anywhere — which is the seam BP-049 decision 2 built and this step must not undo.
5. Map a hard timeout at 20 s to `timeout`, retryable, and take no edge other than `publishing → failed`.
6. Write the "adapter is not called inside the transaction" test first and watch it fail against an implementation that calls it inside (constitution §8).

## Test plan

Each criterion below is inherited verbatim from the requirement named in the
row, reflowed onto one line and otherwise unaltered.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-056 c6** — Given a page has reached `published`, when the customer opens that page, then they are shown the opportunity it came from, the search it targets, the date of the measurement behind it, whether it was approved or published automatically, and the address ReachKit published it at, whichever destination that is. That address is offered as the address the page is publicly readable at only while the page still stands `published`; from the moment it moves to `unpublished` (criterion 7) the record presents the same address as the one the page was published at and never as one it is readable at now, and carries beside it the outcome criterion 15 or criterion 16 names for that page and no other account of what is at that address — ReachKit never goes back to look, so it never claims a page is live at an address it has itself stopped serving or asked the customer's site to stop serving. Where ReachKit never made the page live at its destination, no address is offered at all and the record says so in place of one. | `tests/publish/attempt/publish.test.ts` › "a delivered publication carries the mode and the address the adapter returned, whichever destination it was" — `publications.mode` is written from `drafts.approved_by`, and `live_url` is `result.liveUrl` at both destinations. This test pins **only the row those facts are read from**; the criterion's address *label*, its `unpublished` arm and its never-made-live arm are the page record's and are WO-263's, and this row does not claim to discharge them. |
| **REQ-056 c12** — Given a publish attempt was already under way at the moment the switch was recorded, when its outcome is known, then the customer is shown that outcome: a page that did reach its destination is shown as published, with the record criterion 6 requires, and a page that did not is shown as held — so what the customer reads after switching off never says nothing was published while a page went out, and never says a page went out that did not. | `tests/publish/attempt/publish.test.ts` › "an attempt under way at the moment the switch is recorded is written exactly as it happened" — the switch is recorded mid-delivery; a page that reached the destination is `delivered`, one that did not is not, and neither is inferred from the switch. |
| **REQ-062 c1** — Given a page published to a destination that serves it publicly, when 24 hours have passed since it published, then its live address is fetched and the product records whether the page is reachable, is not marked against indexing, is present in the sitemap, and returns its whole page content to a crawler that runs no scripts and identifies as one the robots policy REQ-059 serves permits. | `tests/publish/attempt/publish.test.ts` › "verify_due_at is published_at + 24h where a live address exists, and null where it does not" — the scheduling half of the 24-hour check; the check itself is WO-234. › "the rule reads the address, not the kind" runs the same assertion over a `hosted`, a `wordpress` and a hypothetical third adapter kind, so an implementation that special-cases WordPress fails here rather than silently excluding every WordPress page from verification. |

## Out of scope
- The retry policy and `unpublish()` — WO-214; the WordPress arms of `unpublish` are WO-239's.
- *Reading* `made_live_by_us` to choose an unpublish arm — WO-214 and WO-239. This work order only stores what the adapter said.
- Anything inside an adapter. `deliver` is BP-058's hosted (WO-228) and BP-048's WordPress (WO-237).
- Running the check at +24h — BP-049, WO-232 and WO-234.

## Rollback
Delete both files. `claim.ts` has no other caller, so the revert leaves a claimed-but-never-delivered path that no job invokes.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-045 — `status: approved`; ADR-084 — `status: accepted`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-056 and REQ-062 as they stand on disk on 2026-09-02; REQ-056 c6 was re-extracted in this pass.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

**The gate this amendment does not meet.** The 2026-09-02 edits derive from
**REQ-056 criterion 6 as rewritten on 2026-09-01** and from **REQ-062**, both
`status: in-review`. Both carry zero open `REVIEW(...)` lines after two full
review rounds (rule 3.4), so the text is **settled but unapproved**. This work
order does not claim §3's gate is met for these edits; the status is asserted on
the design under rule 3.2, and the librarian may revert it retroactively and
name the clause. No `blocked-by` is added (rule 2.3).

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended for ADR-081: the outcome write stores `made_live_by_us = result.madeLive` copied off the `DeliveryResult`, with three tests including a deliberately contradictory `madeLive`/`liveUrl` pair that fails any implementation deriving the column from `live_url` or `servesPublicly`.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — re-extracted against ADR-084: the refuted assertion "`verify_due_at` is set on a hosted delivery and left null on a WordPress one" is gone and replaced by a three-kind test that pins the rule to the address rather than the kind; `liveUrl` is now written at both destinations; the `made_live_by_us` contradictory-fixture pair is kept and its reason restated with the sign reversed; REQ-056 c6 re-extracted verbatim and its address-label half explicitly deferred to WO-263; `implements` gains REQ-062 and ADR-084.

## Validation report (appended by validator)

