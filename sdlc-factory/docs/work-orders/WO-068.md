---
id: WO-068
type: work-order
title: "The report arm — the verdict strip, the one notice, the one control, and the module order"
status: approved
ui: yes
implements: [BP-022, BP-024, REQ-001, REQ-004]
depends-on: [WO-031, WO-041, WO-055, WO-061, WO-066, WO-067, WO-248, WO-249]
blocked-by: []
rests-on:
  - claim: "No blueprint's `code:` glob covers the report's header-strip markup. **Discharged 2026-08-31**: BP-024's `## Module / boundary` now states it — 'The header strip itself is a registered component (BP-018) composed by BP-022's shell and written through BP-019's `renderMeasured`; no sentence and no band word is written here' — which is exactly what this WO does, in BP-022's `_address/` glob. BP-022's own boundary states the other half: 'this node owns the shell that decides whether a body renders at all, and the order the modules sit in (`BUILD.md` §4.1)'. No file here sits outside a declared glob"
    disposition: confirmed
  - claim: "The four band words reach a screen only through BP-019's `SCORE_BANDS`, and the three driver names through the copy registry. Both are BP-019/BP-020's; this WO renders keys and writes no word. **Confirmed 2026-08-31**: BP-019 declares `SCORE_BANDS` over the four handles with REQ-004 criterion 1's own words — 'transcribed, never chosen' — and WO-041 carries the four strings in `keys/bands.ts`, so the keys resolve"
    disposition: confirmed
  - claim: "The AI-answers card, the Google-presence card, the free-page card and the pricing card are sibling nodes' modules. This WO renders them as named slots and supplies none of them, so the report is composable before those nodes ship"
    disposition: open
supersedes: []
---

# WO-068 — The report arm — the verdict strip, the one notice, the one control, and the module order

- Branch: wo/WO-068-report-view
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-068.html
- Signed-off: <date>

## Goal
Render the `report` arm of the address union: one verdict strip at the top, at
most one written notice, exactly one control or none, the report's modules in
`BUILD.md` §4.1's order, and the removal address at the foot.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md`; `_address/` is BP-022's private folder inside
> BP-001's `src/app/(public)/**`. Provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/app/(public)/scan/[domain]/_address/report-view.tsx` | NEW | `ReportView`, over WO-061's `{ kind: 'report' }` arm. Renders (1) the verdict strip: domain, measurement date, score and band through `renderMeasured` and `SCORE_BANDS`, and the three factor mini-bars, each named and each with its own value or a dash; (2) at most one `AddressNotice`, as a total switch; (3) exactly one `AddressControl`, as a total switch, `none` rendering nothing; (4) the module slots in `BUILD.md` §4.1's order, with modules 3 and 4 filled by WO-066 and modules 2, 5 and 6 as named slots; (5) the copy-link control; (6) `RemovalAddressLine` at the foot. Contains no string literal a person reads and no branch on payment, session or tier. |
| `tests/app/scan-address/report-view.test.tsx` | NEW | The verdict, notice, control, order and no-paywall suites named in the test plan. |

## Interfaces

Consumes: WO-061's `AddressState`, `AddressControl`, `AddressNotice`; WO-055's
`Verdict`, reached through BP-012's declared `StoredReport.verdict` (decision 3,
2026-08-31 — the blob has a shape now, and this file reads it by field rather
than through a reader interface of its own); WO-053's `sectionOutcome`; WO-066's
cards and `MethodSections`; WO-067's `RemovalAddressLine`; BP-019's `copy()`,
`renderMeasured()`, `SCORE_BANDS`; BP-018's registered components. Per
`design/components.md`, the strip is a composition over registered rows and
mints nothing: the band is `Badge` (required text child), the three driver
mini-bars are `Progress` ("not a sixth chart"), the copy-link control is `Btn`,
and `ReportView` is named there as a screen composition that "must not become" a
registry row.

Exposes:

```tsx
// src/app/(public)/scan/[domain]/_address/report-view.tsx
export function ReportView(p: {
  state: Extract<AddressState, { kind: 'report' }>
  slots: {                                  // sibling nodes' modules, absent-safe
    aiAnswers?: JSX.Element
    googlePresence?: JSX.Element
    freePage?: JSX.Element
    pricing?: JSX.Element
  }
}): JSX.Element
```

The module order, verbatim from `BUILD.md` §4.1: "1. **Header strip** (card):
domain · date · category · score (big mono, band badge) · three driver mini-bars ·
Copy link. 2. **Two equal cards, side by side** … 3. **Three problem cards**
(grid) … 4. **DIY collapses** (3) … 5. **Free page card** … 6. **Pricing card**".

## Steps
1. Produce the preview artifact at the path in the display bullets above and hold
   in draft until it is signed off (rule 7.3).
2. Write the failing suites first.
3. Render the verdict strip from the stored `Verdict`: `scoreAndBand` through
   `renderMeasured`, the band word from `SCORE_BANDS[handle]`, the three factors
   each named and each rendered through `renderMeasured`. On the `unmeasured`
   arm render the dash, render no band element at all, and render the one line
   naming every missing factor with its reason's key.
4. Render `notice` as a total switch over `AddressNotice | null` — at most one
   line, ever.
5. Render `control` as a total switch over `AddressControl` — `none` renders
   nothing; every other arm renders exactly one control.
6. Render the modules in order, omitting a slot that is absent and rendering a
   section that `sectionOutcome` reports `present: false` as absent with one
   written line naming it — never an empty card and never a spinner.
7. Render the copy-link control, whose value is the canonical address and which
   carries no token and no expiry.
8. Render `RemovalAddressLine` at the foot of every report.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-004.md` and
> `requirements/REQ-001.md`; line breaks are normalised to fit the table and no
> word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-004 c1 — "Given a completed report whose score could be computed, when it opens, then the first module shows a single score on a 0–100 scale, its band as one of the four written words — Invisible, Hard to find, Findable, Dominant — the domain measured, and the date it was measured." | `report-view/verdict · the first module carries score, band, domain and date` — the first rendered module holds exactly one score numeral, one band word resolved from `SCORE_BANDS`, the domain and the measurement date; asserted that no second score appears anywhere in the tree. |
| REQ-004 c2 — "Given the score, when it renders, then the three drivers it is composed of are shown beside it, each named and each with its own value, or with '—' in place of a value the scan could not measure (criterion 6)." | `report-view/verdict · three factors, each named, each with its own value` — exactly three factor elements render, named from copy keys, each carrying its own rendered value or a dash; a fourth bar fails, and `searchPresence`/`aiPresence` are asserted absent from the tree. |
| REQ-004 c3 — "Given a report whose score could not be computed (criteria 6 and 9), when it opens, then the first module shows '—' in place of the score, shows no band word at all, still names the domain and the date, and carries one written line saying the verdict could not be reached, naming every driver that has no value, and stating for each which of the two reasons applies — that the input behind it could not be determined (criterion 6) or that the scan stopped before reaching it (criterion 9) — in the words that criterion gives the case, and never calling a driver the scan never attempted a missing one. No band is stated, implied, or guessed from the drivers that were measured." | `report-view/verdict · an unmeasured score shows a dash and no band` — with `scoreAndBand` unmeasured: a dash renders in the score position, no element resolving any `SCORE_BANDS` key appears anywhere in the tree, domain and date still render, and one line renders naming each entry of `missing` with the key for *its own* reason. A mutation test swapping the two reason keys fails. |
| REQ-004 c4 — "Given the band, when it renders, then it is conveyed in words and not by colour alone." | `report-view/verdict · the band survives colour removal` — with all colour stripped from the rendered tree, the band word is still present and readable; a badge carrying a tone and no word fails. |
| REQ-004 c5 — "Given a free report, when it renders, then no part of the score, the band or the three drivers is hidden, blurred, rounded down, locked, or marked as available on payment; the free report shows the whole verdict." | `report-view/verdict · nothing is hidden and nothing can be` — a source assertion that `report-view.tsx` contains no `tier`, `paid`, `locked`, `blur`, `upgrade` or `entitle` identifier and reads no session; and a rendering assertion that the strip's markup is identical for two fixtures that differ only in the presence of a paid site record. |
| REQ-004 c10 — "Given a section whose data could not be retrieved, when the report renders, then that section is absent and one written line names the missing section and states that it could not be measured — never an empty card, a persistent spinner, or placeholder content." | `report-view/sections · an unretrievable section is absent and named` — for each `SectionName` reported `present: false`, that module is absent from the tree and one written line naming it renders in its place; asserted that no empty card, skeleton or spinner element renders anywhere. |
| REQ-004 c11 — "Given a partially failed scan, when the report renders, then every part that was measured is shown in full and the report is otherwise usable." | `report-view/sections · a partial report still renders everything measured` — a fixture with one section absent and the score unmeasured still renders the problem cards, the method sections, the remaining slots and the copy-link control. |
| REQ-001 c16 — "… and exactly one control that starts a new measurement is offered beside it: the retry, which is the offer criterion 14 or criterion 15 leaves standing — or, where the measurement that failed was a market correction, the one retry of that correction (REQ-094 criterion 7) — never a second control alongside it." | `report-view/control · exactly one, or none` — over all four `AddressControl` arms, the count of measurement-starting controls in the rendered tree is exactly 1 for `rescan`, `retry` and `correction_retry`, and exactly 0 for `none`; asserted by enumerating controls, not by looking for a known label. |
| REQ-001 c14 — "… then one written line says it is incomplete and names what was not measured, and a re-scan control is offered beside that line however new the report is …" | `report-view/notice · at most one line, and the incomplete line names what was missing` — over all three `AddressNotice` arms plus `null`, the count of notice lines is exactly 1 or 0; the `incomplete` arm renders one line naming each `UnmeasuredPart`, beside the one control. |
| REQ-001 c7 — "Given a visitor on a completed report, when they use the copy-link control, then the copied address renders the same report, with the same measurements and the same measurement date, for a different person on a different device." | `report-view/copy-link · the copied value is the canonical address, bare` — the control's value equals the canonical `/scan/{domain}` address, carries no query string, no token and no expiry, and is identical across two renders under different network keys. |
| REQ-002 c1 — "Given any report, when it renders, then it names in writing the address to which a request to remove the report about that domain is sent, readable by a visitor with no account, session, or payment." | `report-view/removal · every report foot names the address` — `RemovalAddressLine` renders on every report fixture, including the incomplete, refused-notice and failed-notice ones. The address's value is WO-067's concern and is owner-owed. |

## Out of scope
- The address resolution and the control/notice derivation — WO-061.
- The problem cards and the DIY sections — WO-066.
- The removal address's value and the removed arm — WO-067.
- The AI-answers, Google-presence, free-page and pricing modules — sibling
  nodes'; this WO renders their slots and supplies none of them.
- The route, its headers and the 308 — WO-069.

## Rollback
Delete both files. WO-069's switch is the only consumer and does not exist yet.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022, BP-024 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 11 rows of `## Test plan` quote REQ-001, REQ-004 as they stand on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — verified against the amended BP-022 and BP-024: the plan still matches both, so the only changes are two discharged `rests-on` rows (BP-024 now places the header strip; BP-019's `SCORE_BANDS` now holds the four words) and the `StoredReport`/registered-component citations behind them. The third row, the sibling nodes' module slots, stands open.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 4 cross-block depends-on edges added (WO-031, WO-041, WO-248, WO-249); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
