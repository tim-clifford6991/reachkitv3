---
id: WO-005
type: work-order
title: "Environment bindings — env.ts, parsed and validated at boot"
status: approved
ui: no
implements: [BP-005]
wave: W1
depends-on: [WO-001]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-005 — Environment bindings — env.ts, parsed and validated at boot

- Branch: wo/WO-005-env
- Estimate: ≤1 day

## Goal
Parse and validate every environment binding the product reads, once, at module
load, so a deployment cannot start half-configured.

## Provenance of this file plan
**This repository holds no `src/`.** Both paths are NEW, anchored to
`structure.md`'s row for `src/lib/config/**` and to BP-005's `code:` globs.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/config/env.ts` | NEW | A Zod schema over `process.env` covering exactly `BUILD.md` §15's list — `DATABASE_URL SUPABASE_URL SUPABASE_ANON_KEY SUPABASE_SERVICE_ROLE STRIPE_SECRET_KEY STRIPE_WEBHOOK_SECRET STRIPE_PRICE_ID RESEND_API_KEY DATAFORSEO_LOGIN DATAFORSEO_PASSWORD ANTHROPIC_API_KEY NANO_API_KEY IP_HASH_SALT KILL_SWITCH OWNER_EMAILS NEXT_PUBLIC_APP_URL` — plus `HOSTED_EDGE_CNAME_TARGET`, the one binding BP-005 adds and states the derivation for. Parsed at module load; a missing or malformed binding throws. No default, no fallback. `SUPABASE_SERVICE_ROLE`, `STRIPE_SECRET_KEY`, `RESEND_API_KEY`, `DATAFORSEO_PASSWORD`, `ANTHROPIC_API_KEY`, `NANO_API_KEY` and `IP_HASH_SALT` are marked server-only and their access throws if the module is evaluated in a client bundle. |
| `tests/config/env.test.ts` | NEW | The tests below. `structure.md` rule 4 puts a module's tests at `tests/<module>/**`; `tests/pins.test.ts` is the one named exception and is WO-007's. |

## Interfaces
Exposes (verbatim from BP-005 `## Public interface`):
```ts
// src/lib/config/env.ts
export const env: Readonly<Env>   // parsed and validated at module load; a
                                  // missing binding is a boot failure, not a
                                  // runtime undefined
```
Consumes: nothing. BP-005 "imports nothing."

## Steps
1. Write the `Env` type as the union of `BUILD.md` §15's names plus
   `HOSTED_EDGE_CNAME_TARGET`, each with its parse rule (URL, non-empty string,
   boolean-ish for `KILL_SWITCH`, comma-separated list for `OWNER_EMAILS`).
2. Parse at module load and freeze.
3. Add the client-bundle guard on the secret bindings.
4. Write the tests; watch the "missing binding throws at boot" test fail first by
   supplying a default, then remove the default.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-005 carries
> `satisfies: []` and was cut from the charter's repo table and `BUILD.md` §6.1,
> not from a requirement. Acceptance is quoted from BP-005 and `BUILD.md` §15.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-005 error behaviour: "`env` throws at boot on a missing or malformed binding; there is no default and no fallback, so a deployment cannot start half-configured." | `tests/config/env.test.ts` — for each binding, delete it and assert module evaluation throws. Adding a default to any one binding fails the case for that binding. |
| `BUILD.md` §15's binding list | Same file — asserts the schema's key set equals §15's list plus `HOSTED_EDGE_CNAME_TARGET`, quoted in the test as the source. An extra binding fails; a missing one fails. |
| BP-005 `## Public interface`: "`HOSTED_EDGE_CNAME_TARGET` — the hostname a customer points `content.{their-domain}` at … It is one deployment-scoped hostname, so it is a binding rather than a constant" | Same file — asserts it is read from `env`, not from `constants.ts`. |
| BP-002 error behaviour: "A `dbAdmin()` import from a client component is a build error, not a runtime one." | Same file — asserts a client-bundle evaluation of the secret accessors throws, so the binding cannot leak ahead of BP-002's own guard (WO-011). |

## Out of scope
- The pinned numbers — WO-006.
- `tests/pins.test.ts` — WO-007. This work order writes no assertion into it.
- Secret rotation, secret storage, and any deployment platform configuration.

## Rollback
Delete both files. No other module imports `env` until WO-011 lands.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-005 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan` quote BP-005's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 started — implementer
- 2026-09-02 finished — implementer — `src/lib/config/env.ts` (Zod schema over `process.env`, frozen, server-only getters) and `tests/config/env.test.ts` (39 tests, all 4 test-plan rows covered) land; full suite green except the pre-existing, out-of-scope `.claude` toolchain failure; typecheck and targeted lint clean. Ready for validation.
- 2026-09-02 finished — validator — TST-002: pass, all 4 test-plan rows discriminate under mutation, no new failure, file scope clean.

## Validation report (appended by validator)

## TST-002 — WO-005 validation report

- Date: 2026-09-02
- Worktree: `/root/projects/reachkitv3-wt/WO-005`, branch `wo/WO-005-env`, HEAD `0513163`
- Corpus-global id: `TST-002` — `grep -n "^## TST-\|^id: TST-" sdlc-factory/docs/work-orders/*.md` (run in both the main repo and this worktree) finds only `TST-001` (WO-001). `TST-002` is the next unused id. If WO-006's validator is running in a sibling worktree concurrently, its report should take `TST-003`.

### Commands (run fresh from HEAD)

| Command | Result |
|---|---|
| `npm run typecheck` (`tsc --noEmit`) | exit 0 |
| `npx eslint src/lib/config/env.ts tests/config/env.test.ts` | exit 0 |
| `npx vitest run` | 4 test files, 76 tests: 75 pass, 1 fails |
| `npx vitest run tests/config/env.test.ts` | 1 test file, 39 tests, all pass |

The one full-suite failure — `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`expected ['.claude'] to deeply equal []`) — is **pre-existing on `doctrine-0.11.0`**, not introduced by this WO: `.claude/` was committed in `61f1fd0` before this WO branched, `/sync` has since amended `structure.md` rule 7 to admit `.claude/`, and the test edit is being cut as its own work order under BP-001. WO-005's diff touches none of `tests/app/toolchain.test.ts`, `structure.md`, or `.claude/`. Judged on its own criteria, WO-005 introduces zero new failures.

### Criterion-by-criterion (4 rows, quoted from `## Test plan`)

| # | Criterion (source) | Test | My own mutation | Result |
|---|---|---|---|---|
| 1 | BP-005 error behaviour: "`env` throws at boot on a missing or malformed binding; there is no default and no fallback, so a deployment cannot start half-configured." | `tests/config/env.test.ts` — `it.each(BINDING_NAMES)("throws at module load when %s is missing", …)` plus the boolean/email/URL/empty-string malformed-value cases | Added `.default('price_default')` to `STRIPE_PRICE_ID` in `env.ts` → "throws at module load when STRIPE_PRICE_ID is missing" failed (`rejects.toThrow()` got a resolved module instead). Restored, clean. | **PASS** — discriminates |
| 2 | `BUILD.md` §15's binding list | `tests/config/env.test.ts` — "env's key set equals §15's list plus HOSTED_EDGE_CNAME_TARGET" | Added an 18th key, `EXTRA_TEST_KEY: z.string().default('extra')`, to the schema → the key-set test failed (unexpected `"EXTRA_TEST_KEY"` in the sorted array). Restored, clean. | **PASS** — discriminates |
| 3 | BP-005 `## Public interface`: "`HOSTED_EDGE_CNAME_TARGET` — … a binding rather than a constant" | `tests/config/env.test.ts` — "is read from env, tracking process.env rather than a fixed constant" | Rewrote the `env` export to override `HOSTED_EDGE_CNAME_TARGET` with a hardcoded literal after parsing → test failed (got the hardcoded value instead of the value set via `process.env`). Restored, clean. | **PASS** — discriminates |
| 4 | BP-002 error behaviour, applied to `env`'s own secrets: "A `dbAdmin()` import from a client component is a build error, not a runtime one." | `tests/config/env.test.ts` — `it.each(SERVER_ONLY_NAMES)("%s throws when read from a client bundle", …)` | Removed `"SUPABASE_SERVICE_ROLE"` from `SERVER_ONLY_KEYS` in `env.ts` → "SUPABASE_SERVICE_ROLE throws when read from a client bundle" failed (`expected [Function] to throw an error`). Restored, clean. | **PASS** — discriminates |

`git status --porcelain` was clean after every mutation-restore cycle; confirmed clean before writing this report (only the pre-existing symlinked `node_modules` shows as untracked, unrelated to any file this WO touches).

### Coverage beyond the 4 quoted rows

The test file also asserts, without contradicting the 4 rows: per-binding malformed-value throws (`KILL_SWITCH` non-boolean, `OWNER_EMAILS` malformed address, a malformed URL, an empty plain string), a "does not throw when every binding is valid" happy path, and a negative control (`NEXT_PUBLIC_APP_URL`, not server-only, does **not** throw when read from a simulated client bundle) — this last one is the discriminator that proves row 4's guard is scoped to the 7-key closed set, not a blanket throw. None of this is a gap; it strengthens the 4 rows.

### Edge probes (run against a scratch test file, then removed — never fixed)

- `KILL_SWITCH` accepted spellings: `"1"`, `"yes"`, `"TRUE"`, `"on"` all parse to boolean `true` via `z.stringbool()` (case-insensitive, several truthy tokens) — consistent with WO-005 step 1's "boolean-ish"; no defect.
- `KILL_SWITCH=""` (empty string): throws — correctly rejected rather than coerced to a default falsy value.
- `OWNER_EMAILS=""` (empty string): throws (`.min(1)` on the raw string before the split) — an empty owner list is rejected, not silently allowed.
- `OWNER_EMAILS` with exactly one address: accepted, one-element array — the floor is one, not two.
- `OWNER_EMAILS` with whitespace around entries (`" a@x.com , b@x.com "`): both entries trimmed correctly by the transform's `.trim()`, both parse — no defect.
- `OWNER_EMAILS` with a trailing comma (`"a@x.com,b@x.com,"`): throws — the trailing empty segment fails `z.email()`, so a trailing separator is rejected rather than silently dropped.
- `NEXT_PUBLIC_APP_URL` with a trailing slash (`"https://app.example.com/"`): accepted, preserved verbatim — not a defect against any of the 4 rows (none asks for normalization), but worth naming for later WOs: a module concatenating a path onto `env.NEXT_PUBLIC_APP_URL` must not assume the absence of a trailing slash.
- `DATABASE_URL` / `SUPABASE_URL` / `NEXT_PUBLIC_APP_URL` padded with leading/trailing whitespace: accepted, with the whitespace silently trimmed. Isolated the cause with a direct probe of the library, not `env.ts`: `z.url().safeParse('  https://example.com  ')` → `{success:true, data:'https://example.com'}` (zod 4.5.4, confirmed via `node_modules/zod/package.json`). This is inherited `z.url()` behaviour (its underlying `new URL()` parse trims per the WHATWG URL spec), not something `env.ts` added, and no test-plan row addresses it — not a defect of this WO, but a corpus fact worth recording since later modules may assume `env.*` URL strings are exactly what the process env set.

### File scope

`git diff doctrine-0.11.0...wo/WO-005-env --stat` touches exactly three files: `sdlc-factory/docs/work-orders/WO-005.md` (`## Log` only, 2 lines added — the implementer's, now a third for this validation), `src/lib/config/env.ts` (NEW), `tests/config/env.test.ts` (NEW). This matches the file plan's 2-NEW-file table exactly; no file outside WO-005's plan changed.

### Structure & capability compliance

- `src/lib/config/env.ts` matches `structure.md`'s row `src/lib/config/** · tests/pins.test.ts | … BP-005 | constants.ts · env.ts · tests/pins.test.ts` and `capabilities.md`'s existing row ("Every pinned number and env binding | `src/lib/config/` | BP-005 | … `env` … | every module") — this WO fills a capability slot the registry already declared for BP-005, it does not mint a new one.
- `tests/config/env.test.ts` matches `structure.md` rule 4, `tests/<module>/**`.
- `grep -rn "process.env" src/` outside `src/lib/config/env.ts` returns nothing — single owner per capability (constitution rule 7.1) holds; no second env-reading site exists to duplicate this one.

### Assumption disposition (constitution rule 2.3b)

- **WO-005's own `rests-on`**: `[]` — no rows on this work order to disposition.
- **BP-005's `rests-on`** (the blueprint this WO `implements`): also `[]` — walked per instruction, found no row. Nothing was left `open` by omission; there is simply no row for this build to observe. Nothing else upstream of BP-005 is claimed as implemented by this WO, so no other blueprint's `rests-on` rows were walked.

Since both are empty, no row of any kind blocks this WO's `rests-on` gate (constitution §3, "no work order reaches `done` with an `open` `rests-on` row" — vacuously satisfied, there being no row).

### Verdict vocabulary note

`.claude/factory/plugins/sdlc-factory/skills/work-order-writing/SKILL.md` (present in this worktree) states the TST-id and `## Log` grammar but does not itself enumerate the validator's verdict words. The verdict word below is cited from `sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

pass

All 4 quoted acceptance rows carry a test that fails under my own targeted mutation of the exact code it covers (not the implementer's claims); the full suite introduces zero new failures against `doctrine-0.11.0` (the one failure present is `tests/app/toolchain.test.ts`'s pre-existing `.claude`-directory case, out of this WO's scope and already dispatched to its own work order); `typecheck` and `eslint` are clean on both changed files; the diff touches exactly the file plan's 2 NEW files plus this WO's own `## Log`; placement matches `structure.md` and `capabilities.md` with no duplicate capability; both `rests-on` fields relevant to this build (WO-005's own, and BP-005's) are empty, so no row is left `open`. No UI placement line applies (`ui: no`).

Regression: not run as part of this pass — `/regress` is a separate dispatch (validator.md step 7) and was not invoked here; no `Regression:` line is added to this section.
