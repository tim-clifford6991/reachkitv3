---
id: WO-268
type: work-order
title: "The fifteen registered components — Btn, Card, Badge, Alert, Stat, Tabs, Table, Progress, Toggle, Steps, Join, Collapse, Input, Divider, Kbd — and the closed barrel"
status: approved
ui: yes
implements: [BP-018]
wave: W1
depends-on: [WO-029, WO-030]
blocked-by: []
rests-on:
  - claim: "**No approved requirement states the design system.** BP-018 carries `satisfies: []` and says so in its own status grounds. Acceptance below is quoted from BP-018 and `BUILD.md` §2.2 and §2.5; none is inherited from a requirement."
    disposition: confirmed
  - claim: "`sdlc-factory/docs/design/components.md` is an empty registry with no rows. BP-018 decision 1 makes entry in that file the precondition of export: \"a new component enters `sdlc-factory/docs/design/components.md` as `proposed` with its data contract and states before it can be exported.\" The eight components below must be registered there by the design-guardian — with the data contract and states this work order's preview shows — before this work order can be signed off. The planner may not write that file."
    disposition: refuted
  - claim: "The seven components below must be registered in `sdlc-factory/docs/design/components.md` — an empty table today — by the design-guardian before this work order can be signed off (BP-018 decision 1). The planner may not write that file."
    disposition: refuted
  - claim: "**Stale as of implementation (flagged by the implementer, not dispositioned): the three rows above describe `components.md` as an empty registry with no rows.** It is not — the fifteen §1 rows are `status: approved` (owner signature on `design/previews/WO-268.html`, 2026-09-03). The validator should re-read all three against the current registry file, not this stale description."
    disposition: confirmed
  - claim: "**The `-content` gap flagged forward from WO-029 (its `## Log` TST-012 entry, 2026-09-03) is real, confirmed empirically, and not fixed by this work order.** `tailwind.config.ts` maps six BUILD.md §2.1 slots (`base-100/200/300/content`, `primary`, `success/warning/error`) but no paired daisyUI `-content` slot (`--color-primary-content`, `--color-success-content`, `--color-warning-content`, `--color-error-content`). Compiled real CSS through the repo's own `@tailwindcss/postcss` + `daisyui`/`daisyui/theme` (the WO-029 TST-012 method) and rendered it in a real Chromium page (Playwright, already a devDependency): `Btn`'s `primary` variant and `Badge`'s `primary`/`success`/`warning`/`error` variants all compute `color: rgb(25, 25, 37)` (`--ink`/`--color-base-content`) on their tinted backgrounds — the var() chain falls back through `--btn-fg`/`--badge-fg` to `--color-base-content` because `--color-primary-content` etc. are never declared anywhere in the compiled output, not `--on-accent` (`#ffffff`), which `theme.css` defines and the idiom's tokens.md reading implies is the intended foreground. This is dark ink on an accent/state-coloured background, not the designed on-accent pairing, on every one of the five `Btn`/`Badge` tone variants that use those two components' `primary`/`success`/`warning`/`error` classes. No token value is invented here (rule 7.1 — `design/` is the design-guardian's); the components are built per their registered daisyUI class contracts as specified. The fix is a `tailwind.config.ts` mapping addition (four `--color-*-content` slots), architect/design-guardian territory, out of this WO's file plan."
    disposition: confirmed
supersedes: [WO-031, WO-032]
---

# WO-268 — The fifteen registered components — Btn, Card, Badge, Alert, Stat, Tabs, Table, Progress, Toggle, Steps, Join, Collapse, Input, Divider, Kbd — and the closed barrel

- Branch: wo/WO-268-registered-components
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-268.html
- Signed-off: 2026-09-03

> `design/previews/WO-031.html` is the sheet the design-guardian drew for the
> first eight of these components. It stands as the preview of record for
> this order until the guardian re-points or extends it; the planner does not
> write `design/`.

## Goal
Ship BP-018's registered daisyUI component set, all fifteen, each with no
default string and every label a required prop, exported from one closed
barrel so an unregistered widget has nowhere to be exported from.

## Why one order (rule 2.6)
WO-031 ("part 1") and WO-032 ("part 2") cut one capability — the registered
set — along a file: both wrote `src/ui/components/index.ts`, WO-032's stop
condition ("the set is closed at fifteen") could only be stated by naming
WO-031's eight, and WO-031's barrel existed for WO-032 to append to. That is
the merge test. Criteria and file plans below are carried verbatim, none
dropped.

## Provenance of this file plan
**This repository holds no `src/`.** Every path is NEW under BP-018's `code:`
glob `src/ui/**`.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/ui/types.ts` | NEW | (from WO-031) `export type Tone = 'ok' \| 'warn' \| 'bad' \| 'neutral' \| 'accent'`, verbatim from BP-018. |
| `src/ui/components/Btn.tsx` | NEW | (from WO-031) daisyUI `btn` (+primary/ghost/sm/block). Label is a required prop. |
| `src/ui/components/Card.tsx` | NEW | (from WO-031) `card`/`card-body`/`card-title`. `BUILD.md` §2.5: the card leads with the answer, so the title slot takes a verdict node, not a metric label with a default. |
| `src/ui/components/Badge.tsx` | NEW | (from WO-031) `badge` (+primary/success/warning/error/ghost), keyed by `Tone`. |
| `src/ui/components/Alert.tsx` | NEW | (from WO-031) `alert`, four tones. Message is a required prop; there is no default empty-state sentence. |
| `src/ui/components/Stat.tsx` | NEW | (from WO-031) `stats`/`stat`. The big number renders through `.num`, so a numeral cannot ship in the UI font. |
| `src/ui/components/Tabs.tsx` | NEW | (from WO-031) `tabs` (boxed + bordered). |
| `src/ui/components/Table.tsx` | NEW | (from WO-031) `table` (+zebra), **always inside an `overflow-x-auto` wrap** — the wrap is part of the component, not the caller's job. |
| `src/ui/components/Progress.tsx` | NEW | (from WO-031) `progress`. Determinate only: a caller cannot request an indeterminate bar. |
| `src/ui/components/Toggle.tsx` | NEW | (from WO-032) daisyUI `toggle`. Label required; no default on/off wording. |
| `src/ui/components/Steps.tsx` | NEW | (from WO-032) `steps`. Each step's label is required — this is the component the scan's named stages render through, so it can never show an unlabelled step. |
| `src/ui/components/Join.tsx` | NEW | (from WO-032) `join`. |
| `src/ui/components/Collapse.tsx` | NEW | (from WO-032) `collapse`. Summary text required. |
| `src/ui/components/Input.tsx` | NEW | (from WO-032) `input`. Placeholder and label are required props, never defaulted. |
| `src/ui/components/Divider.tsx` | NEW | (from WO-032) `divider`. |
| `src/ui/components/Kbd.tsx` | NEW | (from WO-032) `kbd`. Renders through `.num` — it is a code-like string under `BUILD.md` §2.3. |
| `src/ui/components/index.ts` | NEW | (from WO-031 NEW + WO-032 MODIFY, one row) Barrel exporting exactly the fifteen BP-018 lists; written once, closed at fifteen. |
| `tests/ui/components-1.test.tsx` | NEW | (from WO-031) The tests on the first eight below. |
| `tests/ui/components-2.test.tsx` | NEW | (from WO-032) The tests on the seven and on the closed set below. |

## Interfaces
Exposes (verbatim from BP-018 `## Public interface`, the members this work order
ships):
```ts
// Registered components only — daisyUI primitives plus the five allowed customs.
export { Btn, Card, Badge, Alert, Stat, Tabs, Table, Progress, Toggle, Steps,
         Join, Collapse, Input, Divider, Kbd } from './components'
export type Tone = 'ok' | 'warn' | 'bad' | 'neutral' | 'accent'
// Series colours are `chart-you` and `chart-rival` only; Tone is for state and
// is never used for a series.
```
Consumes: `src/ui/theme.css` (WO-029), `src/ui/type.css` and `.num` (WO-030).

## Steps
1. Write `types.ts` with `Tone`.
2. Write the fifteen components, each with required label props and no default
   string.
3. Write the barrel with exactly the fifteen exports.
4. Write the two test files; watch the "no component has a default string" test
   fail first by giving `Alert` a fallback message, and the "closed at fifteen"
   test fail first by exporting a sixteenth.

## Test plan
> **Criterion source: the blueprint, not a requirement — and there is no
> requirement to draw on.** BP-018 has no requirement ancestor.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-018 decision 2: "no component has a default string. Every label, empty state and tooltip is a required prop supplied from BP-019's copy registry." | `tests/ui/components-1.test.tsx` — for each of the eight, renders with no props and asserts a type error and no text node; and asserts no string literal that reads as prose appears in any component file. Adding a fallback fails it. |
| BP-018 decision 1: "daisyUI components only — no bespoke widgets" | Same file — asserts each component's root element carries its daisyUI class and that none defines bespoke layout CSS. |
| `BUILD.md` §2.2: "`table` (+zebra, always inside an `overflow-x-auto` wrap)" | Same file — asserts `Table` always renders the wrap, with no prop to omit it. |
| BP-018 error behaviour: "Every numeral, date, URL, search query and code-like string renders in JetBrains Mono with `tabular-nums`; a numeral in the UI font is a defect." | Same file — asserts `Stat`'s value slot carries `.num`. |
| `BUILD.md` §2.5: "a verdict chip (`You: 0/12`), a big mono number with its delta badge" and REQ-004 criterion 4's words-not-colour rule as BP-018 states it: "colour is never the only carrier of meaning" | Same file — asserts `Badge` and `Alert` each require a text child, so a tone alone cannot carry the meaning. |
| BP-018 public interface: "Registered components only — daisyUI primitives plus the five allowed customs." | `tests/ui/components-2.test.tsx` — asserts the barrel's export set is exactly the fifteen names BP-018 lists. A sixteenth fails. |
| BP-018 decision 1: "An open components directory — rejected, the first unregistered widget is the one that re-implements a card with different spacing and the design system stops being one." | Same file — asserts every `.tsx` under `src/ui/components/` (excluding `custom/`) is exported from the barrel, so a file cannot exist unregistered. |
| BP-018 decision 2: "no component has a default string" | Same file — for each of the seven, the same no-default assertion the first file applies to its eight. |
| `BUILD.md` §2.3: "Every numeral, date, URL, search query and code-like string is JetBrains Mono with `tabular-nums`." | Same file — asserts `Kbd` renders through `.num`. |
| BP-003 / `BUILD.md` §2.5 empty-and-degraded rule as BP-018 states it: "Every label, empty state and tooltip is a required prop" | Same file — asserts `Steps` requires a label per step, so an unlabelled stage cannot render. |

## Out of scope
- The four custom components — WO-033, WO-034. The five charts — WO-035, WO-036.
- The layout bands and the `Surface` root (`src/ui/layout/**`, ADR-093) — no
  work order carries them yet; reported with this wave's return.
- Any product copy. Every string is passed in from BP-019.
- Registering the components in `design/components.md`; that is the
  design-guardian's file.

## Rollback
Delete the nineteen files. No surface consumes them yet.

## Self-certification
`status: approved` — set 2026-09-02 by the planner under constitution rule 3.2:
an assertion with its grounds stated, which the librarian audits and may refuse
retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-018 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 10 rows of `## Test plan` are the
  5 + 5 rows of WO-031 and WO-032, carried verbatim; none dropped, none added.
- **Bounded.** 19 files in the file plan (19 NEW, 0 MODIFY) — the union of the
  two superseded plans with the one shared barrel counted once — estimate
  ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and
  `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's
  *implementation*, not its approval (§3), and the `Signed-off:` bullet is
  unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done`
gate's, and the validator's and librarian's to set.

## Log
- 2026-09-02 created — planner — consolidation on `/wave propose` (rule 2.6): supersedes WO-031, WO-032; 10 criteria and 19 file-plan rows (20 before, one shared barrel) carried verbatim.
- 2026-09-03 signed-off — owner — preview sheet `design/previews/WO-268.html`, https://claude.ai/code/artifact/84aee332-745d-47d0-b69c-1cf06874b76f
- 2026-09-03 started — implementer — depends-on (WO-029, WO-030) both `done`; preview signed; rule 1 refusal check passed.
- 2026-09-03 finished — implementer — all 19 file-plan files written (15 components, `types.ts`, barrel, 2 test files); 383/383 `npm test`, 16/16 `npm run test:layout`, typecheck and build clean; the `-content` daisyUI gap flagged forward from WO-029 empirically confirmed by compiling real CSS and rendering it in Chromium (falls back to `--color-base-content`/`--ink`, not the designed `--on-accent`) — reported as a `rests-on` row for the validator, no token value invented; all three of this WO's existing `rests-on` rows are stale ("empty registry" — the fifteen §1 rows are now `approved`) and flagged for the validator to redispose. Ready for `/validate`.
- 2026-09-03 finished — validator — TST-014 pass; independently reproduced 383/383 `npm test`, 16/16 `npm run test:layout`, clean typecheck/build; scope confirmed exactly the 19-file plan; 3 independent mutation probes (fallback-string, type-widening, sixteenth-export) all caught by the shipped suite; all 10 acceptance criteria PASS; UI-fit spot checks (named tokens only, registered components only, no generated prose, caller-supplied empty/loading/error states) hold; Placement: clean (no `## Open questions` on this WO). All 5 `rests-on` rows dispositioned — 3 confirmed, 2 refuted (the "empty registry" precondition no longer holds); none left `open`.
- 2026-09-03 finished — validator — `/regress`: full suite re-run clean (383/383 `npm test`, 16/16 `npm run test:layout`, typecheck/build clean); `factory-console impact WO-268` names 0 requirements (BP-018 `satisfies: []`) and BP-018-anchored file set is exactly the 19 file-plan files; `Regression:` line appended to TST-014.

## Validation report (appended by validator)

## TST-014 — WO-268 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-268`, branch `wo/WO-268-registered-components`, implementer HEAD `a56a6a1`.
- `git show --stat a56a6a1` / `git diff a56a6a1^ a56a6a1 --stat`: exactly 20 files touched — 19 new source/test files (the file plan's full union of WO-031+WO-032, one shared barrel) plus this WO's own `.md` (`## Log`/`rests-on` additions only). No file outside the plan changed.
- `ui: yes`. **Placement: clean** — no `## Open questions` heading exists on this WO at all, so zero `REVIEW(placement)` lines stand under it.
- Corpus-global id: `TST-014` — next unused id after `TST-013` (grepped every `TST-[0-9]+` heading under `sdlc-factory/docs/`).

### 1. Full suite and build — independently re-run

`npm test` (`vitest run --project node --project ui --project db`): **383/383 passed**, matching the implementer's count exactly. `npm run test:layout`: **16/16 passed**. `npm run typecheck`: clean. `npm run build` (Next.js 16.3.4, Turbopack): compiles, typechecks and prerenders clean. `npx eslint src/ui/components/ src/ui/types.ts tests/ui/components-1.test.tsx tests/ui/components-2.test.tsx`: zero output — clean. (The repo-wide `npm run lint` reports ~8000 pre-existing errors, all in files this WO does not touch — bundled/generated vendor files under `.claude/factory/**` and a `previews/app/` fixture bundle — confirmed unrelated by scoping eslint to this WO's own files above.)

### 2. Mutation probes — my own, not the implementer's, each reverted after

| Mutation | Result |
|---|---|
| `Alert.tsx`: `message` widened to optional + fallback string `"No data available"` substituted in JSX | **FAILS** — 3 of 39 tests in `components-1.test.tsx` fail: the two "required, not optional" source-pattern assertions and the `@ts-expect-error`-adjacent runtime no-text assertion catches the substituted string reaching the DOM |
| `Btn.tsx`: `label` widened to optional (no JSX change) | **FAILS** typecheck — `tsc --noEmit` reports `error TS2578: Unused '@ts-expect-error' directive` at the exact line asserting "Btn without label is a type error" |
| `index.ts`: sixteenth export added (`Rogue.tsx` dropped into the directory and re-exported) | **FAILS** — 4 of 32 tests in `components-2.test.tsx` fail: the "exactly fifteen" barrel-shape assertion, the fifteen-name equality assertion, and both directory-vs-barrel closure assertions all catch it |

All three mutations reverted; `git status --short` clean after each and at the end of this pass.

### 3. Criterion-by-criterion (WO-268 `## Test plan`, 10 rows, carried verbatim from WO-031/WO-032 per rule 2.6)

| # | Criterion (source quoted) | Test | Result |
|---|---|---|---|
| 1 | BP-018 decision 2: "no component has a default string… every label, empty state and tooltip is a required prop" (first eight) | `components-1.test.tsx` — compile-time `@ts-expect-error` block + runtime no-text-on-omission block + prose-literal scan | PASS — independently mutated (§2, rows 1–2) |
| 2 | BP-018 decision 1: "daisyUI components only — no bespoke widgets" | `components-1.test.tsx` — root-class + no-inline-style assertions | PASS — every one of the eight asserted directly against its rendered root; `Btn`→`.btn`, `Card`→`.card`/`.card-body`/`.card-title`, `Badge`→`.badge`, `Alert`→`.alert`, `Stat`→`.stats`/`.stat`, `Tabs`→`.tabs`, `Table`→`table.table`, `Progress`→`.progress`, all confirmed by reading source |
| 3 | `BUILD.md` §2.2: `table` +zebra, always inside `overflow-x-auto` wrap | `components-1.test.tsx` — wrap-is-root assertion, no-suppressing-prop assertion, no-skeleton assertion, caller-line assertion | PASS — read `Table.tsx`: wrap is the unconditional root element, no `wrap`/`loading`/`skeleton` prop exists in its type |
| 4 | BP-018 error behaviour: numeral in JetBrains Mono/`tabular-nums`, never UI font | `components-1.test.tsx` — `.stat-value` carries `.num` in both `measured` and `unmeasured` states | PASS — read `Stat.tsx`: the single `.stat-value num` node is shared by both branches, cannot regress independently |
| 5 | `BUILD.md` §2.5 / REQ-004 c4: colour never the only carrier of meaning | `components-1.test.tsx` — `Badge`/`Alert` required-text-child assertions plus a real-text render | PASS — `Badge`'s `children: React.ReactNode` and `Alert`'s `message: React.ReactNode` are both non-optional in source |
| 6 | BP-018 public interface: registered set only | `components-2.test.tsx` — barrel export-set equality to the fifteen names, plus an explicit "sixteenth is caught" mutation test already shipped in the suite itself | PASS — independently reproduced with my own sixteenth file (§2, row 3), distinct from the implementer's shipped mutation test |
| 7 | BP-018 decision 1: no unregistered widget can exist unexported | `components-2.test.tsx` — every `.tsx` under `src/ui/components/` (excl. `custom/`) is in the barrel's export set | PASS — independently reproduced with a rogue file dropped into the real directory (§2, row 3), not just the suite's own simulated-array version |
| 8 | BP-018 decision 2: no default string (remaining seven) | `components-2.test.tsx` — compile-time + runtime blocks, same shape as row 1, applied to Toggle/Steps/Join/Collapse/Input/Divider/Kbd | PASS — spot-read `Toggle.tsx`/`Input.tsx`/`Steps.tsx`: `label` required, no fallback string in any |
| 9 | `BUILD.md` §2.3: code-like string in JetBrains Mono/`tabular-nums` | `components-2.test.tsx` — `Kbd` renders `.num`, plus a source-string mutation test already shipped | PASS — read `Kbd.tsx`: `className="kbd num"` fixed, unconditional |
| 10 | BP-003/`BUILD.md` §2.5 empty-and-degraded rule applied to `Steps` | `components-2.test.tsx` — compile-time per-step label requirement + full-render every-stage-labelled assertion | PASS — read `Steps.tsx`: `label: string` required on every array element's type, no default |

All 10 rows PASS. No criterion maps to zero tests; no test is vacuous (§2's three independent mutations, chosen to hit a representative case from each half of the suite — a runtime-fallback mutation, a type-widening mutation, and a barrel-closure mutation — were each caught, and reading every one of the fifteen component source files found no default string, no bespoke CSS, and no daisyUI class omitted).

### 4. Structure compliance

`src/ui/components/**`, `src/ui/types.ts` sit under `src/ui/**`; `tests/ui/components-{1,2}.test.tsx` sit under `tests/ui/**` — both are `structure.md` line 40's governed globs for BP-018, matching `BP-018.md`'s own `code:` front matter exactly. `grep -rln "src/ui/components/Btn.tsx\|src/ui/components/index.ts" sdlc-factory/docs/work-orders/*.md` returns WO-031 and WO-032 alongside WO-268 — both confirmed `status: superseded` in their own front matter, exactly the merge rule 2.6 requires (carried, not duplicated). No other WO's file plan claims any of these nineteen files. No duplicate capability, no structure violation. `tailwind.config.ts` — BP-018's own `code:` glob but NOT in WO-268's file plan — is untouched by this WO's commit (confirmed via `git diff a56a6a1^ a56a6a1 --stat`).

### 5. UI-fit spot checks (rule 7.3, since `ui: yes`)

- Named tokens only: `grep -nE "#[0-9a-fA-F]{3,6}\b|[0-9]+px" src/ui/components/*.tsx src/ui/types.ts` → zero hits. No raw hex or px anywhere in the fifteen components.
- Registered components only: all fifteen source files render through a single daisyUI root class each (confirmed by source read and the class-assertion tests in §3 rows 2/7); no bespoke widget.
- No long generated text: every string-bearing prop is a required pass-through (label/message/summary/etc.); no component source file contains a hardcoded sentence — confirmed by the prose-literal denylist scan (`components-1.test.tsx`, independently re-run) and by my own read of all fifteen files.
- Loading/empty/error states: `Table` takes a caller-supplied `emptyMessage` (never a built-in skeleton — asserted and confirmed); `Stat` has an explicit `unmeasured` state printing only a fixed glyph plus a caller-supplied `reason`; `Alert`/`Badge` carry `Tone`/`AlertTone` for state colour, always paired with required text. No component invents its own loading/empty/error copy.
- Preview: `design/previews/WO-268.html` exists, is signed (`Signed-off: 2026-09-03`, front matter and `## Log` both record it), and its own DUTY-2 header records the design-guardian drew all fifteen at their registered contracts with the card-idiom widenings noted separately — matches the file plan's registered-contract framing, not the widened idiom.

### 6. `rests-on` disposition (constitution rule 2.3b)

1. **"No approved requirement states the design system… BP-018 carries `satisfies: []`…"** — **confirmed**. Re-read `BP-018.md`: front matter `satisfies: []`, and the blueprint's own `## Status grounds` states the same reason verbatim ("no approved requirement states the design system, which is why `satisfies` is empty"). This build's own criterion-by-criterion pass (§3) independently confirms every one of the ten test-plan rows is quoted from BP-018/`BUILD.md`, none from a requirement edge.
2. **"`components.md` is an empty registry with no rows… the eight components below must be registered there… before this work order can be signed off"** — **refuted**. Read `sdlc-factory/docs/design/components.md` directly: §1's fifteen rows are all `Status: approved`, not empty/`proposed`, and the file's own header prose records the owner's 2026-09-03 signature on `design/previews/WO-268.html` as the act that flipped them. The precondition this row names no longer obtains — it was met before this validation ran.
3. **"The seven components below must be registered… an empty table today…"** — **refuted**, same grounds as row 2: the same fifteen-row table (including the seven) reads `approved`, not empty.
4. **"Stale as of implementation… the three rows above describe `components.md` as an empty registry… The validator should re-read all three against the current registry file"** — **confirmed**. This row's own instruction is exactly what rows 2–3's disposition above did, and its description of current reality (fifteen `approved` rows, not an empty table) matches what I independently read in `components.md`.
5. **"The `-content` gap flagged forward from WO-029… `Btn`'s `primary` variant and `Badge`'s `primary`/`success`/`warning`/`error` variants all compute `color` … from `--color-base-content`, not `--on-accent`…"** — **confirmed**, by independent evidence distinct from the implementer's Chromium run: read `tailwind.config.ts` directly — it maps six `--color-*` slots (`base-100/200/300/content`, `primary`, `success/warning/error`) and declares no `--color-primary-content`/`--color-success-content`/`--color-warning-content`/`--color-error-content` slot at all; separately, `grep -rn "on-accent" src/` shows `--on-accent` defined three times in `theme.css` (light/dark-media/explicit-dark) and consumed nowhere in `tailwind.config.ts` or any component file. Both facts are consistent with and support the claim's mechanism (the four content slots have no source, so daisyUI's own fallback chain is what would resolve them). Confirmed as a real, currently-live gap; correctly out of this WO's file plan (`tailwind.config.ts` is not one of the 19 files WO-268 touches — confirmed in §4) and correctly forwarded, not silently absorbed.

All five rows dispositioned; none left `open`.

### Coverage gaps

None found against this WO's own ten-row test plan. One pre-existing, out-of-scope gap is carried forward by `rests-on` row 5 above (the `-content` daisyUI slot mapping) — it affects the *rendered colour* of `Btn`/`Badge` tone variants once composed with `tailwind.config.ts`, not any assertion this WO's own test plan makes (the plan tests class presence and required-prop discipline, not computed color), so it does not fail any of the 10 criteria in §3, but it is a real visual defect for `tailwind.config.ts`/`theme.css` (BP-018, out of this WO's file plan) to fix.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

pass

383/383 `npm test`, 16/16 `npm run test:layout`, clean `typecheck`/`build`, all independently re-run and matching the implementer's counts. Three independent mutation probes (§2) each caught by the shipped suite; every one of the ten acceptance criteria maps to at least one test that discriminates, verified by direct source read plus mutation, not by trusting the test's own claims. Scope is exactly the 19 file-plan files (confirmed by diffing the implementer's commit against its parent) plus this WO's own log — no file outside the plan touched, and `tailwind.config.ts` (BP-018's territory but not this WO's) is correctly untouched. Structure compliance holds: both superseded predecessor WOs (WO-031, WO-032) are `status: superseded`, carrying no duplicate live capability claim. UI-fit spot checks (§5) hold: no raw hex/px, registered components only, no generated prose, and empty/loading/error states are caller-supplied everywhere. Placement is clean (no `## Open questions`/`REVIEW(placement)` lines exist on this WO). All five `rests-on` rows dispositioned above — one confirmed as a sourcing fact, two refuted (the "empty registry" precondition no longer holds — `components.md` §1 is now fully `approved`), one confirmed as an accurate staleness flag, and one confirmed as a real, correctly out-of-scope `-content` token-mapping gap for a later BP-018 change to close. None left `open`.

Regression: 19 files (`factory-console impact WO-268`'s BP-018-anchored subset — `src/ui/components/{Alert,Badge,Btn,Card,Collapse,Divider,Input,Join,Kbd,Progress,Stat,Steps,Table,Tabs,Toggle}.tsx`, `src/ui/components/index.ts`, `src/ui/types.ts`, `tests/ui/components-{1,2}.test.tsx` — exactly TST-014 §1's 19-file scope, independently re-derived; the command's raw 113-file list is noise from `chore(wave)` commit `5c114d4`, which names WO-268 in its body prose per rule 5.6's whole-message `WO-\d{3}` scan while actually being the bulk wave-propose commit that wrote/updated every WO-###.md in W1 — none of those 94 extra files are BP-018-anchored or touched by WO-268's own feature commit `a56a6a1`) · none (`factory-console impact WO-268` names 0 requirements; BP-018 `satisfies: []`, independently reconfirmed by reading `BP-018.md` front matter) re-checked — pass. Full suite independently re-run on the current branch tip (`178b518`): `npm test` 383/383, `npm run test:layout` 16/16, `npm run typecheck` clean, `npm run build` clean (Next.js 16.3.4, Turbopack, compiles/typechecks/prerenders clean). 0 importers named, so no adjacent module's own behaviour is at risk from this change. Spot-reconfirmed two of TST-014's own criteria against current code as a sanity check (not required by rule 2.3b, since no requirement is named, but cheap given the file set is unchanged since TST-014): `src/ui/components/index.ts`'s barrel still exports exactly the fifteen BP-018 names, and `Table.tsx`'s `overflow-x-auto` wrap is still unconditional. No finding.
