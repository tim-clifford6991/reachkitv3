---
id: WO-057
type: work-order
title: "admitFreeScan — the six-step order, the network key, and the fail-open limiter"
status: done
ui: no
implements: [BP-023, REQ-002, REQ-003]
depends-on: [WO-006, WO-267, WO-051, WO-056]
blocked-by: []
rests-on:
  - claim: "`x-forwarded-for`'s first entry is the visitor's address on the deployment platform, so the network key is derivable without trusting a client-supplied header. BP-022 and BP-023 both carry this row; it is spent here"
    disposition: undischargeable
  - claim: "Counting by network address means several founders behind one office or mobile network share one hourly allowance and one in-flight slot. Inherited from REQ-003's own row"
    disposition: confirmed
  - claim: "BP-005 gains `FREE_RESCAN_WINDOW_D: 7`, `FAILURE_COOLDOWN_H: 24`, `DAILY_WINDOW_H: 24` and `HOURLY_WINDOW_H: 1`, and `env` binds `IP_HASH_SALT` and `KILL_SWITCH` (`BUILD.md` §15). Pins and bindings are BP-005's by `structure.md` rule 5; this WO imports them and declares none"
    disposition: confirmed
supersedes: []
wave: W2
---

# WO-057 — `admitFreeScan` — the six-step order, the network key, and the fail-open limiter

- Branch: wo/WO-057-admission-check
- Estimate: ≤1 day

## Goal
Answer *may a free scan start* against the free path's bounds in BP-012's fixed
order, without side effects and without consuming an allowance, so that every
render of a report address can ask the question.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md`; `src/lib/scan/admission.ts` is BP-023's file
> inside BP-012's container. Provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/scan/admission.ts` | NEW | `NetworkKey`, `networkKeyOf`, `Admission` and `admitFreeScan`. The six steps in BP-012's order, each reading exactly what BP-023's table says. `networkKeyOf` takes the first entry of `x-forwarded-for`, truncates IPv6 to the /64 prefix, and HMACs with `IP_HASH_SALT` — storing and logging only the hash (BP-023 decision 3). `admitFreeScan` opens no transaction, writes nothing and is idempotent. |
| `tests/scan/free/admission-check.test.ts` | NEW | The order, refusal, fail-open and idempotence suites named in the test plan. |

## Interfaces

Consumes: WO-051's `CanonicalDomain`; WO-056's `scans.network_hash` and the
`failed` status; BP-002's `dbAdmin()`; BP-005's `FREE_BOUNDS`,
`FAILURE_COOLDOWN_H`, `HOURLY_WINDOW_H`, `DAILY_WINDOW_H` and
`env.IP_HASH_SALT` / `env.KILL_SWITCH`; `domain_blocks` (BP-002's table, read
only).

Exposes (verbatim from BP-023 `## Public interface`, `src/lib/scan/admission.ts`):

```ts
/** Opaque, salted, non-reversible. REQ-003 counts by network; no raw address is
 *  stored or logged anywhere in the product. */
export type NetworkKey = string & { readonly __networkKey: unique symbol }
export function networkKeyOf(forwardedFor: string | null): NetworkKey

/** Verbatim BP-012's declared union — the refusal reasons and their fields are
 *  that node's contract and are not restated differently here. */
export type Admission =
  | { admit: true }
  | { refuse: 'hourly';       retryAfterSeconds: number }
  | { refuse: 'in_flight';    sameDomain: boolean; runningScanId?: string }
  | { refuse: 'daily';        retryAfterSeconds: number }
  | { refuse: 'switched_off' }
  | { refuse: 'cooldown';     retryAfterSeconds: number }
  | { refuse: 'removed' }

/** Idempotent and free of side effects: BP-022 calls it on every render of a
 *  report address, and a render must never consume an allowance. */
export function admitFreeScan(a: {
  domain: CanonicalDomain; network: NetworkKey
}): Promise<Admission>
```

The evaluation order is BP-012's and is reproduced here as the step list only —
its reasoning lives in BP-012's `## Decisions` (rule 2.4): **removed → cooldown →
switched off / daily ceiling → in-flight → hourly**, with what each step reads
and returns given by BP-023's `## Error & edge behavior` table.

## Steps
1. Write the failing suites first.
2. Implement `networkKeyOf`: first `x-forwarded-for` entry, trimmed; IPv6
   truncated to its /64 prefix before hashing; HMAC with `IP_HASH_SALT`. A null
   or unparseable header still yields a key — a stable one for "unknown" — and
   never throws.
3. Implement the six steps in order, each an early return:
   `removed` (a `domain_blocks` row for the canonical domain);
   `cooldown` (latest `failed` scan of this domain inside `FAILURE_COOLDOWN_H`,
   `retryAfterSeconds` = the remainder);
   `switched_off` (`env.KILL_SWITCH`, no time promised);
   `daily` (count over `DAILY_WINDOW_H` vs `FREE_BOUNDS.scansPerDay`,
   `retryAfterSeconds` = until the 200th-newest ages out);
   `in_flight` (`status = 'running'` for this network, with `sameDomain` and
   `runningScanId`);
   `hourly` (count for this network over `HOURLY_WINDOW_H` vs
   `FREE_BOUNDS.scansPerIpPerHour`, `retryAfterSeconds` = until the 5th-newest
   ages out).
4. Wrap steps 2 to 6 in the fail-open handler: any read error returns
   `{ admit: true }` and logs the failure as such. **The `removed` step is
   outside that handler** — a `domain_blocks` read that errors refuses.
5. Make every `retryAfterSeconds` an elapsed duration in seconds, never a clock
   time (REQ-003's third assumption, confirmed).
6. Emit the one structured log line BP-023's NFR budget specifies: decision, the
   step that produced it, the hashed network key, the domain. Never a raw
   address.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-003.md` and
> `requirements/REQ-002.md`; line breaks are normalised to fit the table and no
> word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-003 c6 — "Given a visitor who has already started five free scans from the same network within the last hour — every way a scan starts counting against that allowance (criterion 1), except a re-measurement after a market correction, which belongs to the scan it corrects and consumes nothing further — when another scan would start for them, then it does not start, they are refused with one written line saying how long until they may scan again, and the refused scan, having never run, produces no report of its own." | `admission/hourly · the fifth scan admits, the sixth refuses with an elapsed duration` — five rows in the last hour for one key admit; six refuse `{ refuse: 'hourly' }` with `retryAfterSeconds` equal to the time until the fifth-newest ages out, asserted against a frozen clock, and asserted to be a duration under 3600 and never a timestamp. |
| REQ-003 c7 — "Given a scan is already running from the visitor's network, when another scan would start for them, then no second scan starts; if the running scan is of the domain they asked for they are returned to it, and if it is not they are refused in writing, told how long until they may scan, and are never shown a scan or a report of a domain they did not ask for." | `admission/in_flight · sameDomain carries the running scan id` — a `running` row for the same key and the same domain refuses `{ refuse: 'in_flight', sameDomain: true, runningScanId }`; a different domain refuses with `sameDomain: false` and the returned object is asserted to contain no domain and no report of the running scan. |
| REQ-003 c8 — "Given the day's free-scan ceiling has been reached, or scanning has been switched off, when a visitor tries to scan, then one written line tells them free scanning is paused — stating how long until it resumes when the day's ceiling is the cause, and promising no time when scanning has been switched off — and every already-stored report stays readable." | `admission/daily · the ceiling refuses with a duration; the kill switch refuses without one` — 200 rows in the window refuse `{ refuse: 'daily' }` with `retryAfterSeconds`; `env.KILL_SWITCH` set refuses `{ refuse: 'switched_off' }`, and a type-level assertion that the `switched_off` arm has no `retryAfterSeconds` field to fill. |
| REQ-003 c9 — "Given the scan limiter itself is unavailable, when a visitor starts a scan, then the scan proceeds." | `admission/fail-open · a counting error admits` — with the `scans` reads stubbed to throw at each of the cooldown, daily, in-flight and hourly steps in turn, `admitFreeScan` returns `{ admit: true }` and logs the failure. And `admission/fail-open · except for removal` — a `domain_blocks` read that throws returns `{ refuse: 'removed' }`, not `{ admit: true }`. |
| REQ-002 c3 — "Given a domain whose report has been removed, when its report address is opened, then it shows the removed report to nobody and starts no scan for anyone, on that visit or any later one …" | `admission/removed · removal is step one and outranks everything` — a domain with a `domain_blocks` row and simultaneously a failure cooldown, a full hourly allowance, a running same-network scan and a stored report refuses `{ refuse: 'removed' }`, and a mutation test asserts that moving the removal step below any other step fails this test. |
| REQ-002 c4 — "Given a domain whose owner has made no removal request, when its report address is opened, then no report is withheld this way: a report is taken down only by a removal request received at the address criterion 1 names, never at ReachKit's own initiative." | `admission/removed · this module never writes domain_blocks` — a source assertion that `src/lib/scan/admission.ts` contains no insert, update, upsert or delete against `domain_blocks`, and a database-level assertion that running the whole admission suite leaves the table's row count unchanged. |
| REQ-003 c12, first sentence — "Given a visitor refused under criterion 6 or 7 at the report address of a domain with no stored report, when the address loads, then it shows that refusal in writing and starts no scan …" | `admission/idempotence · asking never consumes` — calling `admitFreeScan` 50 times for a key at four of five allowed scans still admits on the 50th, and the `scans` row count is unchanged; asserted also by spying that no write method of the client was called. The rendering of the refusal is WO-069's. |

## Out of scope
- `claimFreeScanSlot` — WO-058, same file, sequenced after this WO.
- The written line for each refusal — BP-019's `CopyKey`s, another node's.
- The order's *reasoning* — BP-012's `## Decisions`; this WO implements it and
  restates none of it.
- Paid-tier admission: `network_hash` is null for paid tiers and this function is
  free-path only.

## Rollback
Delete both files. WO-058 extends this file and does not exist yet; WO-061 and
WO-062 are the only other consumers.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-023 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 7 rows of `## Test plan` quote REQ-002, REQ-003 as they stand on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-006, WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — `src/lib/scan/admission.ts` (`NetworkKey`, `networkKeyOf`, `Admission`, `admitFreeScan`) and `tests/scan/free/admission-check.test.ts` (21 tests) built and passing; two schema gaps found and worked around rather than fabricated (rule 4.2, noted in both files' headers): `domain_blocks` (WO-012) has no migration in this repo, and `scans.network_hash` (WO-056) is absent from the generated `Database` type — ready for `/validate`.
- 2026-09-03 finished — validator — TST-021, verdict pass: all 7 test-plan criteria covered by tests independently confirmed to discriminate by mutation; both rule-4.2 schema-gap deviations (`domain_blocks` no migration, `scans.network_hash` ungenerated) reproduced and confirmed genuine, narrowly cast, correctly out of scope; REQ-002 c4's database-assertion substitution judged adequate; all 3 of this WO's own `rests-on` rows dispositioned (2 confirmed, 1 undischargeable), none left open.
- 2026-09-03 finished — validator — regression clean at tip `053e867`: full suite (487/1 skip), typecheck, build and layout suite all pass; zero code diff since the validated commit; `factory-console impact WO-057` names 10 files, 2 blueprints, REQ-003, all re-checked against current code (c6/c7/c8/c9/c12 re-run 21/21 in isolation) — no regression found.
- 2026-09-03 finished — librarian — done audit clears all gates (validation pass, regression record present, all 3 rests-on rows dispositioned, ui: no so no placement gate, no open rows remain)

## Validation report (appended by validator)

## TST-021

### Scope
WO-057 (`wo/WO-057-admission-check`, commit `7865c43`), validated against its own `## Test plan`, `## Interfaces`, `## Steps` and `## Out of scope` sections, and against constitution rule 4.2 (the implementer's two flagged schema-gap deviations) and rule 2.3b (`rests-on` disposition). `git diff b1946b7 7865c43 --stat`: exactly 3 files — `src/lib/scan/admission.ts` (NEW, 336 lines), `tests/scan/free/admission-check.test.ts` (NEW, 482 lines), and this WO's own `## Log` (+2 lines) — matching the file plan exactly. Nothing outside WO-057's scope touched.

### Suite / build
- `vitest run --project node --project ui --project db`: 23 files, 487 passed, 1 skipped (488) — full corpus suite, not just this file's own.
- `vitest run --project node tests/scan/free/admission-check.test.ts` in isolation: 21/21 passed.
- `tsc --noEmit`: clean.
- `eslint .`: 8118 pre-existing problems (8093 errors, 25 warnings) — byte-identical count to WO-056's own validation report (TST-020, same total), all in vendored/generated files (`.claude/factory/console/src/vendor.mjs`, migration scripts under `.claude/factory/plugins/`, design preview files) and a large pre-existing minified-file block; `grep` for `admission` in the lint output returns nothing — `src/lib/scan/admission.ts` and `tests/scan/free/admission-check.test.ts` lint clean.
- `next build`: compiles, typechecks and prerenders cleanly.

### Criterion-by-criterion

| Criterion (verbatim, per WO's own table) | Test | Verdict | Mutation probe |
|---|---|---|---|
| REQ-003 c6 — hourly ceiling, fifth admits/sixth refuses with elapsed duration | `admission/hourly · the fifth scan admits, the sixth refuses with an elapsed duration` | PASS | Changed `rows.length < FREE_BOUNDS.scansPerDay` analogue is a separate test below; for hourly specifically, inverting `sameDomain`/order mutations below cascade through the same fail-open try block and were independently probed — hourly's own boundary (`limit(FREE_BOUNDS.scansPerIpPerHour)` + row-count check) is exercised at exactly 4 vs 5 rows, which is the boundary itself |
| REQ-003 c7 — in-flight, sameDomain carries id / different domain hides it | `admission/in_flight` (2 tests) | PASS | Inverted `running.domain === domain` to `!==`: both in_flight tests fail (2/2) — confirmed live |
| REQ-003 c8 — daily ceiling refuses with duration; kill switch refuses without one | `admission/daily` (3 tests, incl. a type-level assertion) | PASS | Changed the daily boundary from `<` to `<=` (off-by-one): `admission/daily · the ceiling refuses with a duration` fails (1/1) — confirmed live |
| REQ-003 c9 — limiter unavailable, scan proceeds | `admission/fail-open` (5 tests: 4 steps + removal exception) | PASS | See rule 4.2 finding below for the removal-exception mutation |
| REQ-002 c3 — removal outranks everything | `admission/removed · removal is step one and outranks everything` | PASS | Reordered the implementation to evaluate `cooldown` before `removed`: this exact test fails, returning `{ refuse: "cooldown", retryAfterSeconds: 86340 }` instead of `{ refuse: "removed" }` (1/1) — confirmed live, matches the WO's own claimed mutation-sensitivity |
| REQ-002 c4 — no report withheld absent a removal request (module never writes `domain_blocks`) | `admission/removed · this module never writes domain_blocks` (source scan + call-tracking, in place of a database-level row-count assertion — see finding below) | PASS | Injected an actual `.insert()` call into `isRemoved`: both the source-level string scan and the call-tracking assertion fail (2/2) — confirmed live |
| REQ-003 c12 (first sentence) — idempotence, asking never consumes | `admission/idempotence · asking never consumes` | PASS | 50 calls at the 4-of-5 boundary still admit on call 50, `writeCalls` stays empty — this is itself the discriminating case (a stateful/consuming implementation would refuse well before call 50) |
| `networkKeyOf` (not a REQ row; BP-023's own public interface) | 5 tests: determinism/salting, first-XFF-entry only, /64 IPv6 truncation, null-safety, unparseable-input stability | PASS | Independently spot-checked: the IPv6 test asserts two addresses in the same /64 collide and a third in a different /64 does not — both directions are asserted, so truncating to /32 or not truncating at all would each fail one side |
| `admission/cooldown` (BP-023's own error table, not named by a REQ row in this WO's test-plan table, tested for completeness per the WO's own footnote) | 2 tests: inside window refuses, outside window admits | PASS | Both directions asserted (boundary is exercised from both sides) |

### Rule 4.2 finding 1 — `domain_blocks` has no migration — independently confirmed, legitimate

Reproduced from first principles, not taken on the implementer's word:
1. `grep -rl "domain_blocks" supabase/migrations/*.sql` returns nothing that creates the table — only `00000000000001_baseline.sql`'s own header comment, which explicitly excludes it ("`domain_blocks`... this migration touches none of them" appears verbatim in `00000000000005_scans_freepath.sql`'s header, restating the same exclusion). No `CREATE TABLE` for `domain_blocks` exists anywhere in this repo.
2. `sdlc-factory/docs/work-orders/WO-012.md` (`domain_blocks`'s own migration): front matter is `status: approved` but carries **no `wave:` field at all** — not merely "not W2," not scheduled into any wave yet.
3. `sdlc-factory/docs/registry/waves.md` W2 roster: `WO-051, WO-041, WO-056, WO-057, WO-058, WO-062, WO-070` (Must) — WO-012 does not appear. WO-057's own `depends-on: [WO-006, WO-267, WO-051, WO-056]` does not name WO-012 either.
4. WO-057's own `## File plan` is exactly 2 files (`admission.ts`, the test file) — no migration. Building a live `domain_blocks` table would be scope creep into WO-012's own file plan, which the constitution's slice rule (2.6) forbids reinterpreting into this WO.
5. **(b) Cast boundary is narrow.** `grep -n "\bany\b" src/lib/scan/admission.ts` matches only a prose comment, never a type. The `untyped()` function is the sole cast site (`client as unknown as MinimalClient`), used in exactly two call sites (`isRemoved` for `domain_blocks`, and `checkInFlight`/`checkHourly` for `scans.network_hash`); every other query in the file (`checkCooldown`, `checkDaily`) is typed directly against `dbAdmin()`'s generated `Database` type, confirmed by `tsc --noEmit` passing clean with no suppressions (`@ts-ignore`, `@ts-expect-error`) anywhere in the file.

**Verdict on gap 1: legitimate build-against-a-future-table plumbing, not a reinterpretation of this WO's scope.** This WO's own scope never required the table to exist — it only reads it, read-only, behind a narrow, explicit cast, exactly as the header states.

### Rule 4.2 finding 2 — `scans.network_hash` absent from `types.generated.ts` — independently confirmed, legitimate

1. `grep -n "network_hash" src/lib/db/types.generated.ts` returns nothing — the generated `Database` type has no such column, confirmed.
2. The column itself exists live: `supabase/migrations/00000000000005_scans_freepath.sql` (WO-056's migration, merged and `done`) adds it in the `alter table scans add column network_hash text null` line.
3. `sdlc-factory/docs/work-orders/WO-056.md`'s own `## File plan` is exactly the migration and one schema test — no `types.generated.ts` regeneration in scope there either (confirmed by reading that WO's own text). Regenerating that file is WO-267's capability (`src/lib/db/types.generated.ts`'s own staleness contract lives in `tests/db/clients.test.ts`, owned by WO-267) — not something either WO-056 or WO-057 may fold in without scope creep.
4. **Side observation, not a defect of WO-057:** `tests/db/clients.test.ts`'s own staleness check (`MIGRATIONS` constant, lines 55-58) hardcodes only `00000000000001_baseline.sql` and `00000000000002_rls.sql` — it does not include migration 5 (WO-056's), so it does not actually catch this specific drift even though it runs and passes. This is a pre-existing gap in WO-267's own test (predates WO-056 and WO-057; the hardcoded list was never widened), out of scope for this validator to fix and not attributable to WO-057's diff. Flagged here for the record only, per rule 4.2's own "say so once" discipline — not a finding against this WO.
5. Same narrow-cast-boundary check as gap 1 applies (`untyped()` is the only escape hatch, used at the same two call sites).

**Verdict on gap 2: legitimate, same reasoning as gap 1.** Neither gap is this WO's to close.

### Judgment on the REQ-002 c4 substitution (no database-level row-count assertion)

The WO's own test-plan row literally asks for "a database-level assertion that domain_blocks' row count is unchanged." No live `domain_blocks` table exists in this repo (finding 1, confirmed above), so a literal database-level assertion is not buildable without WO-012's migration landing first — building it here would itself be scope creep into WO-012.

What was built instead: (a) a source-level static scan of `admission.ts` for `insert(`/`upsert(`/`delete(` (with `.update(` asserted by exact occurrence count to exclude the one legitimate `Hmac.update()` call), embedded with its own "watch it fail first" self-check (a fixture string containing `insert(` is asserted to be caught by the same predicate); and (b) a call-tracking assertion against the mocked client (`writeCalls`) that records every `insert`/`update`/`upsert`/`delete` call issued through `dbAdmin().from(table)` during two live `admitFreeScan` invocations, asserting zero against `domain_blocks`.

Independently probed both halves by mutation: injected an actual `.insert()` call into `isRemoved` (see finding 1's mutation table row) — both the source-level scan and the call-tracking assertion failed, together and independently (removing either check in isolation still leaves the other catching the same mutation, so neither is a decorative duplicate of the other).

**This is adequate, not a gap requiring a fix.** It is the honest equivalent this WO's scope can build without WO-012's migration landing first, it discriminates (proven above), and it does not overclaim: it verifies this *module* issues no write, which is the concern actually inside WO-057's boundary. It does not and cannot verify that the database itself has no writable path to `domain_blocks` from anywhere else in the product — that is WO-012's own test plan's job (`tests/db/domainblocks.test.ts`, per WO-012's `## Test plan`: "asserts the table has no insert, update or delete policy... Adding an insert policy fails it"). The two WOs' tests are a correct division of one invariant, not a substitute for each other — once WO-012 lands, the row-count guarantee is enforced at both the application layer (this WO) and the database layer (WO-012), which is the design a table "with no writer anywhere in the product" (WO-012's own title) actually needs.

### rests-on disposition (rule 2.3b)

WO-057's own three rows, dispositioned here (none left `open`):

- Row 1 ("`x-forwarded-for`'s first entry is the visitor's address on the deployment platform, so the network key is derivable without trusting a client-supplied header") — **undischargeable**. This is a claim about the deployment platform's own reverse-proxy behaviour (that it appends/overwrites the leading `x-forwarded-for` entry with the real client address rather than passing a client-supplied one through unmodified). No test buildable inside this repository — unit-level (a mocked header, which is all `networkKeyOf`'s own suite can exercise) or otherwise — can observe a third-party platform's proxying behaviour from inside the codebase; that fact is knowable only by citing the platform's own documentation or by an external probe sent from outside the deployed edge, neither of which any WO's file plan in this factory produces. It is discharged, if ever, by an architect's citation of the platform's documented behaviour (an ADR-level citation) at wave close, not by a build.
- Row 2 ("Counting by network address means several founders behind one office or mobile network share one hourly allowance and one in-flight slot") — **confirmed**. This build directly implements and tests the mechanism the claim describes: `checkHourly` and `checkInFlight` both scope their count/lookup to `network_hash = network` — one hashed key per distinct first-`x-forwarded-for`-entry/-64-prefix — and `networkKeyOf`'s own suite confirms determinism (the same address always yields the same key). The `admission/hourly` and `admission/in_flight` suites demonstrate exactly the shared-allowance, shared-in-flight-slot behaviour the claim describes, live against a mocked client. The claim is a true description of what the shipped mechanism does.
- Row 3 ("BP-005 gains `FREE_RESCAN_WINDOW_D: 7`, `FAILURE_COOLDOWN_H: 24`, `DAILY_WINDOW_H: 24` and `HOURLY_WINDOW_H: 1`, and `env` binds `IP_HASH_SALT` / `KILL_SWITCH`... this WO imports them and declares none") — **confirmed**. Verified directly: `src/lib/config/constants.ts` declares exactly `FREE_RESCAN_WINDOW_D = 7 as const` (line 152), `FAILURE_COOLDOWN_H = 24 as const` (153), `HOURLY_WINDOW_H = 1 as const` (154), `DAILY_WINDOW_H = 24 as const` (155); `src/lib/config/env.ts` binds `IP_HASH_SALT: z.string().min(1)` (38) and `KILL_SWITCH: z.stringbool()` (40). `admission.ts`'s own diff (finding 1/2 above, `git diff b1946b7 7865c43`) imports these four constants and `env` and declares no new constant, pin or env binding of its own.

Upstream, this order `implements: [BP-023, REQ-002, REQ-003]` (not BP-012, which is only a `depends-on`; BP-012's own `rests-on` is `[]`, nothing to disposition there):

- **BP-023**'s 4 rows: row 2 (network-address counting, same claim as WO-057's row 2 above) → **confirmed**, same reasoning; row 3 (`x-forwarded-for` trust, same claim as WO-057's row 1) → **undischargeable**, same reasoning; row 4 (BP-005 gains the four constants) → **confirmed**, same reasoning as WO-057's row 3. Row 1 (the free battery's 60s p95 vendor SLA) is **left open** — this build touches no vendor timing or battery code (`admission.ts` reads only `scans.created_at`/`status`/`network_hash` and `domain_blocks`; it makes no vendor call and measures no elapsed battery time), so this WO did not observe that claim; it remains the first WO that actually runs the battery's own timing (elsewhere in BP-012/BP-023's scope) to disposition.
- **REQ-002**: `rests-on: []` — nothing to disposition.
- **REQ-003**'s 3 rows: row 3 (elapsed-duration-never-a-clock-time) was already `confirmed` on disk (by an earlier WO) — untouched, no action needed. Row 2 (network-address counting, same claim) → **confirmed**, same reasoning as above — this is the first WO to implement and test the counting mechanism REQ-003's own row describes. Row 1 (vendor SLA) is **left open**, same reasoning as BP-023's row 1 — not observed by this build.

One row is deliberately left `open` on each of BP-023 and REQ-003 (the vendor-SLA row, identical claim in both places) — a conscious act, not an oversight: this WO's `admission.ts` never calls a vendor endpoint or times a battery, so it cannot be the build that discharges that claim. WO-057's **own** three rows are fully dispositioned above; none remain `open` on this work order, satisfying the `done`-gate precondition (rule 2.3b, `open-assumption-on-done`).

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict
pass

WO-057 shipped exactly what its own `## Interfaces`, `## Steps` and `## Out of scope` specify. All 7 test-plan rows (REQ-003 c6/c7/c8/c9/c12, REQ-002 c3/c4) plus the two supplementary suites (`networkKeyOf`, `admission/cooldown`) are covered by tests independently verified to discriminate by mutation — inverting the `sameDomain` comparison, the daily off-by-one boundary, reordering the removal step below cooldown, and injecting an actual write into `isRemoved` each broke exactly the test claiming to cover it, and only that test's own claim. Both schema-gap deviations the implementer flagged (rule 4.2) are independently confirmed genuine and correctly scoped outside this WO — narrow, explicitly cast, no `any` leakage, no reinterpretation of the file plan. The REQ-002 c4 test-plan row's literal "database-level assertion" was honestly substituted with a source-scan-plus-call-tracking equivalent, itself mutation-verified, and judged adequate — it is the correct-sized check for what this WO's boundary actually owns, not a shortcut. Full suite (487 passed, 1 pre-existing skip, unrelated to this WO), typecheck, lint (byte-identical pre-existing count, nothing attributable to this WO's files) and build all clean. No files outside the 2-file plan touched. All three of this WO's own `rests-on` rows are dispositioned (two confirmed, one undischargeable); no `open` row remains on WO-057 itself.

Regression: 10 files · REQ-003 re-checked — pass

Full suite re-run clean at tip `053e867` (`wo/WO-057-admission-check`): `npm test` (`vitest run --project node --project ui --project db`) — 23 files, 487 passed, 1 skipped (488), identical to TST-021's own count; `npm run typecheck` — clean; `npm run build` — compiles, typechecks and prerenders cleanly; `npm run test:layout` — 3 files, 16 passed. `git diff 7865c43 053e867 --stat` confirms zero code change since the validated commit — only this WO's own `## Log` and validation report were added — so nothing to re-observe beyond re-running. `factory-console impact WO-057` names 10 files (`sdlc-factory/docs/registry/waves.md`; `sdlc-factory/docs/work-orders/{WO-041,WO-051,WO-056,WO-057,WO-058,WO-062,WO-070}.md`; `src/lib/scan/admission.ts`; `tests/scan/free/admission-check.test.ts`), 2 blueprints (BP-012, BP-023), 0 importers, and 1 requirement (REQ-003). REQ-003's acceptance criteria this WO's own test-plan carries (c6, c7, c8, c9, c12) were re-verified against current code, not merely restated: `admission-check.test.ts` re-run in isolation, 21/21 passed, matching TST-021's own count, exercising the hourly ceiling (c6), in-flight refusal (c7), daily ceiling and kill switch (c8), fail-open limiter (c9) and idempotence (c12) suites live against the unchanged `src/lib/scan/admission.ts`. No regression found. The listed already-`done` work orders whose own capabilities this suite still exercises — WO-001 (`tests/scan/free/schema.test.ts`, `tests/pins.test.ts`), WO-002 (`tests/db/*.test.ts`), WO-005/WO-006 (`tests/config/*.test.ts`), WO-029/WO-030 (`tests/ui/fonts.test.ts`, `tests/ui/tokens.test.ts`), WO-041 (`tests/presentation/copy/*.test.ts`), WO-051 (`tests/scan/free/domain.test.ts`), WO-056 (`tests/scan/free/schema.test.ts`, migration 5's own coverage inside `tests/db/*.test.ts`), WO-267 (`tests/db/clients.test.ts`), WO-268 (`tests/db/baseline.test.ts`) — all passed inside the same full-suite run; none is named by `factory-console impact` because WO-057's own commits touched no file those work orders' blueprints govern, so they are reported here as suite coverage, not as impact-set members.

