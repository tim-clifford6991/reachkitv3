---
id: WO-143
type: work-order
title: "deleteAccount — signed out, subscription ended, tombstone stamped, and one mail about what is still live"
status: approved
ui: no
implements: [BP-063, REQ-079]
depends-on: [WO-006, WO-011, WO-106, WO-133, WO-136, WO-140, WO-141, WO-142]
blocked-by: []
rests-on:
  - claim: "A connected WordPress site permits one place that brings every post ReachKit created there up together, so criterion 6's mail has a place to name — REQ-079's own `rests-on` row and BP-048's, unverified in this corpus, and answered per destination by BP-058's `stamp_capable` (ADR-083). Where a site permits none, `place` is `null`, the sentence says so and still carries its count, and the customer is left with pages live on their own domain that no product surface can point them to. That sentence is owner-owed and is BP-063's first Open question; this work order renders whatever the registry holds and mints none."
    disposition: open
  - claim: "`publications.unpublish_outcome` records, per publication, which of `UnpublishResult`'s five arms the run produced (BP-045's column, ADR-082 Decision 6 carried forward by ADR-084), so the four counts this mail carries are read from the run's own outcomes and no population is re-derived here (rule 2.4). The count for an arm with no rows is not zero — the key is absent from the map."
    disposition: open
supersedes: []
---

# WO-143 — deleteAccount — signed out, subscription ended, tombstone stamped, and one mail about what is still live

- Branch: wo/WO-143-delete-account
- Estimate: ≤1 day

## Goal
Run the deletion arm in the fixed order — take the pages down, end the
subscription at once, stamp the tombstone and the promised purge date, sign the
customer out — and write once to the address being deleted about any pages a
destination would not let go of.

## File plan
> **Every path below is NEW except `confirm.ts`.** No `src/` exists; `confirm.ts`
> is created by WO-142 and modified here, which is why the two are sequential.
> Anchored to `registry/structure.md` and BP-063's `code:` glob
> `src/lib/account/lifecycle/**`. Provisional against a repo with no code.
>
> **Read ADR-051 before writing this file.** `deleteAccount` "sets
> `users.deleted_at = now()` and `users.purge_due_at = now() + ERASURE_DAYS`. It
> issues no `DELETE` of any kind." Finishing the delete path is the mistake the
> ADR exists to prevent.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/account/lifecycle/delete-account.ts` | NEW | Exports `DeleteAccountResult` verbatim — **including `leftInWordPress`, added 2026-09-02** — and the deletion sequence, in BP-063's fixed order, each step a precondition of the next: (1) run WO-142's take-down loop and collect `stillLive` **and the four `leftInWordPress` counts, tallied from the `UnpublishResult` arms that run returned and from nothing else**; (2) end the subscription **at once** through BP-060 — cancel immediately and set `paid_through = now()`, so `hasActiveAccess` is false at once, the hosted 30-day retention window never begins and **neither hosting notice is sent**; (3) set `sites.hosted_serving_ends_at = now()` so BP-060's `hostedServingState` answers `serve: false` — there is no second switch (decision 5); (4) stamp `users.deleted_at = now()` and `users.purge_due_at = now() + ERASURE_DAYS`, the date **stored**, not computed (decision 4); (5) end every session for the user; (6) **where `stillLive` is non-empty *or* `leftInWordPress` holds any entry**, send one `account` mail to the address being deleted — composed through `dbAdmin()`, because the tombstone has already hidden the rows from `db()` (ADR-051 point 3). A step that fails stops the ones after it. **Criterion 6's last sentence is the whole send condition: "Where nothing of either kind is left behind, no such mail is sent" — *either* kind, so a run that reached every destination but left posts in a customer's WordPress still sends.** |
| `src/lib/account/lifecycle/left-in-wordpress.ts` | NEW | Builds `DeleteAccountResult['leftInWordPress']` — **four sentences' worth of data, one per REQ-056 criterion 16 outcome, each with its own count and, where there is anywhere to look, its own place**. This is the third shape this half has taken in two days (a list, then one count and one place, now four counts); it is re-extracted here from the requirement and from no earlier work order. Four rules, each of which the type or this module enforces rather than a reviewer: (a) **the map is keyed by `UnpublishResult`'s four WordPress arms** — `returned_to_draft`, `named_for_removal`, `already_gone`, `unreachable` — because that is where the outcome set lives (rule 2.4), so a single count spanning two arms is unrepresentable rather than forbidden; (b) **an outcome holding no posts is absent from the map, not present with `count: 0`** — the criterion says it "is not named at all rather than named with a count of none", and an absent key is that sentence's representation, so the mail cannot render a zero; (c) **`already_gone`'s `place` is `null` by construction, not by capability** — the criterion says that sentence names no place "because there is nothing there to find", and it is `null` even at a site whose stamp was applied, because sending a customer to a list to look for posts that cannot be in it is the failure the 2026-09-01 fold corrected; (d) for the other three, `place` is `WordPressListPlace` where BP-058's `stamp_capable` is true for that destination and `null` where it is not — and where it is `null` the sentence still carries its count. |
| `src/lib/account/lifecycle/confirm.ts` | MODIFY | Add the `delete_account` arm to `confirmDangerAction`'s existing switch, behind the same ticket guard. No other line changes. |
| `tests/account/lifecycle/delete-account.test.ts` | NEW | The order, asserted by call sequence; `hasActiveAccess` is false immediately afterwards; `hostedServingState` answers `account_deleted`; `deleted_at` and `purge_due_at` are stamped and `purge_due_at` is exactly 30 days later and does **not** move when `ERASURE_DAYS` is changed afterwards; every session is ended; **no row is deleted by this path** (a whole-database row-count assertion before and after); a failing take-down stops the sequence before the tombstone. |
| `tests/account/lifecycle/left-in-wordpress.test.ts` | NEW | One fixture per rule. A run producing all four outcomes yields four entries, each with its own count, and **no entry's count is the sum of two arms** — asserted by a fixture whose four counts are mutually distinct primes, so any pairwise merge is arithmetically visible; an outcome with no rows is **absent from the map** and a compile-time assertion that the map is a `Partial<Record<>>` over the four arms rather than a total record, so `count: 0` has no place to be written; `already_gone`'s `place` is `null` **even when that destination's `stamp_capable` is true** — the discriminating case, and the one a capability-driven implementation gets wrong; a stamp-incapable destination gives `place: null` on the other three arms while their counts stand; and the map is keyed by `UnpublishResult`'s arms, asserted at compile time, so a fifth key of this module's own does not typecheck. |
| `tests/account/lifecycle/deletion-mail.test.ts` | NEW | One `account` mail when `stillLive` is non-empty, naming those pages, sent to the address being deleted; **one mail when `stillLive` is empty but `leftInWordPress` holds an entry** — the case criterion 6's "either kind" turns on, and the one the previous cut of this work order got wrong; **no mail when both are empty**; **neither hosting notice is sent and the retention window never begins** (asserted against WO-134's two queries, which must return this site in neither); the mail composes successfully **after** the tombstone is stamped, which only holds because the row is present and hidden rather than gone — the discriminating case for ADR-051's first consequence. |

## Interfaces
Exposes (verbatim from BP-063's `## Public interface`):
```ts
export interface DeleteAccountResult {
  signedOut: true
  subscriptionEndedAt: Date
  stillLive: UnpublishAllResult['stillLive']   // c6 — named in one `account` mail to the deleted address
  /** c6's WordPress half. **Four sentences, one per REQ-056 c16 outcome, each
   *  with its own count** (the 2026-09-01 round-two fold; earlier it was one
   *  count and one place, and before that a list). The outcome set has one home
   *  and it is REQ-056 c16's (rule 2.4), so this is keyed by
   *  `UnpublishResult`'s four WordPress arms and never by a set of its own.
   *
   *  - `returned_to_draft` — made live by ReachKit and now back to draft. Has a
   *    place. **This is the populous arm since ADR-084**, and it was the empty
   *    one when this interface was written.
   *  - `named_for_removal` — created and never made live, "which nobody
   *    published, which were never public, and into which nothing has been
   *    written". Has a place. **Empty in production since ADR-084**, kept
   *    renderable for the reason ADR-084 Decision 4 keeps the arm.
   *  - `already_gone` — no longer in that site when ReachKit went to return
   *    them. **`place: null` by construction, not by stamp capability**: c6 says
   *    this sentence names no place "because there is nothing there to find". A
   *    non-null place here would send the customer to look for posts that cannot
   *    be in the list.
   *  - `unreachable` — standing in a site ReachKit could not reach, may still be
   *    live, nothing written. Has a place.
   *
   *  "An outcome holding no posts is not named at all rather than named with a
   *  count of none", so an entry with `count === 0` is absent from the map and
   *  the mail cannot render a zero. `place` is REQ-060 c6's list, which exists
   *  only where the stamp was applied (ADR-083): `null` where the site would not
   *  take it, and that sentence then says so and still carries its count — an
   *  owner-owed sentence, not one this node mints. */
  leftInWordPress: Partial<Record<
    'returned_to_draft' | 'named_for_removal' | 'already_gone' | 'unreachable',
    { count: number; place: WordPressListPlace | null }
  >>
  purgeDueAt: Date                              // c7 — deleted_at + 30 days
}
/** The one place, from BP-058's `stamp_capable` row and BP-005's pin. This node
 *  renders no sentence and builds no URL text — it hands the values to the
 *  `account` mail's blocks (BP-053) and the copy key is BP-019/BP-020's. */
export interface WordPressListPlace { siteBaseUrl: string; stampSlug: string }
```
Consumes: `UnpublishAllResult`, the take-down loop and the per-page
`UnpublishResult` outcomes it collected (WO-142); `stampCapability()` (BP-058)
for whether a destination's site took ADR-083's stamp, and `WORDPRESS_STAMP_SLUG`
(BP-005's pin, whose **value is owner-owed and outstanding** — ADR-084's second
Open question — so this work order reads the pin and never a literal);
`cancelSubscription` and `hostedServingState` (BP-060, WO-133 and WO-130);
`signOut` (BP-061, WO-136); `sendEmail` (BP-016) with
`'account': { occasionsFrom: 'REQ-024, REQ-074, REQ-076, REQ-077, REQ-079', stoppable: false }`;
`dbAdmin()` (BP-002); BP-005's `ERASURE_DAYS = 30`.

## Steps
1. Write `tests/account/lifecycle/delete-account.test.ts` and watch the ordering
   and no-row-deleted cases fail.
2. Write `delete-account.ts` in the fixed order.
3. Add the `delete_account` arm to `confirm.ts`.
3a. Write `left-in-wordpress.ts`. Build the map from the run's own outcomes;
   **omit** any arm with no rows rather than writing `count: 0`; set
   `already_gone`'s `place` to `null` before looking at `stamp_capable` at all,
   so the capability check cannot reach that arm. Read the three
   `## File plan` rules again before the first `if`: each of them is a sentence
   of criterion 6, and each has a plausible implementation that violates it.
4. Write `left-in-wordpress.test.ts` and `deletion-mail.test.ts`, including the
   compose-after-tombstone case and the mail-sent-with-`stillLive`-empty case.
5. Run the mutation check: replacing the tombstone with a `DELETE` must fail both
   the no-row-deleted case and the compose-after-tombstone case; writing
   `count: 0` for an empty arm must fail the absent-key case; giving
   `already_gone` a place at a stamp-capable site must fail its case; and
   suppressing the mail when `stillLive` is empty must fail the either-kind
   case.

## Test plan
> Watch it fail first (constitution §8). REQ-079's criteria are quoted from
> `requirements/REQ-079.md` as it stands on 2026-09-02. **Criterion 6's WordPress
> half has now moved three times** — a list of posts, then one count and one
> place, now four sentences with four counts — so it is re-extracted here from
> the requirement file itself and from no earlier work order, no blueprint
> restatement and no changelog. The quote this work order carried until
> 2026-09-02 was a summary in quotation marks and matched no version of the text.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-079 c6** Given "delete account" completes, when it does, then the customer is signed out, a sign-in request at that address finds no account, the subscription ends at once with no further charge and no remaining paid access, and pages are taken down at their destinations as criterion 4 describes. Because criterion 7 leaves the customer no ReachKit surface to read afterwards, the naming criterion 4 puts on such a surface is carried instead by one `account` mail (REQ-064) sent to the address being deleted. Where ReachKit created posts in the customer's own WordPress, that mail gives each of REQ-056 criterion 16's four outcomes one sentence of its own, carrying that outcome's own count and, where there is anywhere to look, its own place — and no sentence carries two outcomes or one count for both: - how many posts ReachKit made live in that site and has now returned to draft, and where those can be found; - how many posts ReachKit created there and never made live, which nobody published, which were never public, and into which nothing has been written, and where those can be found; - how many were no longer in that site when ReachKit went to return them — this sentence naming no place, because there is nothing there to find; - how many stand in a site ReachKit could not reach, which may still be live there and into which nothing was written, and where those can be found. An outcome holding no posts is not named at all rather than named with a count of none. The mail says of every post still in that site that it is theirs to keep or remove, and never lists any post, whatever the number. The place a sentence names is the one place in their own WordPress that brings them up together (REQ-060 criterion 6); where the site permits no such place, that sentence says instead that the posts are in that site and that ReachKit cannot point to them there, and it still carries its count. Where a page is still live at a destination that could not be reached, the mail names it and says what they must do about it. Where nothing of either kind is left behind, no such mail is sent. | `tests/account/lifecycle/delete-account.test.ts › every session is ended`, `› hasActiveAccess is false immediately`, `› no further charge is created`, `› pages are taken down at their destinations through WO-142's loop and this path takes no unpublish branch of its own`; `tests/account/lifecycle/left-in-wordpress.test.ts › four outcomes give four entries, each with its own count, and no count spans two arms`, `› an outcome holding no posts is absent from the map and no zero can be written`, `› already_gone carries no place even at a stamp-capable site`, `› a stamp-incapable site gives place null on the other three while their counts stand`; `tests/account/lifecycle/deletion-mail.test.ts › one account mail names the pages left live`, `› one mail is sent when stillLive is empty but posts are left in the customer's WordPress`, `› no mail when nothing of either kind is left behind`, `› the mail lists no post, whatever the number` |
| **REQ-079 c7** Given an account was deleted, when the customer or anyone else reaches for its data through any product surface — sign-in, export, a published page's record — then none of it is reachable from the moment of deletion; and when 30 days have passed, then the account and the sign-in address it was reached at, the site, its pages, drafts, measurements, destination credentials and every copy ReachKit holds of the customer's own page content — including the passages copied from their live pages and kept as a draft's grounding evidence (REQ-050 criterion 1, "the passage is kept unchanged from then on"), whose retention ends here — are no longer present in ReachKit's stored data at all. (the tombstone half: the stamp is written, the promised date is stored, and nothing is deleted now; the unreachability is enforced in BP-002's row policy and the purge is WO-144) | `tests/account/lifecycle/delete-account.test.ts › deleted_at and purge_due_at are stamped`, `› purge_due_at is exactly 30 days later and does not move when the pin changes`, `› no row is deleted by this path` |
| **REQ-076 c10** Given a customer whose access has ended, when 30 days have passed since their paid-through date, then ReachKit stops serving their hosted pages and every address that served one returns 410 Gone rather than a page, a redirect or a not-found; until that day the pages are served unchanged — unless the customer deletes their account, when serving stops at the moment of deletion and this window never begins (REQ-079 criterion 6). (the deletion half only; the retention half is WO-134) | `tests/account/lifecycle/delete-account.test.ts › hostedServingState answers account_deleted at once` |
| **REQ-076 c11** Given a customer with pages served at the hosted destination, when their access ends, however it ends (criterion 8), and again 7 days before the day serving stops, then they are told in writing which day it stops and that their pages remain exportable afterwards (the `account` mail, REQ-064), so no customer's live pages go dark without both notices having been sent — except where the customer deletes their own account, which takes those pages down at once and carries no notice of a day the customer chose (REQ-079 criterion 6). (the exception half only) | `tests/account/lifecycle/deletion-mail.test.ts › a deleted account appears in neither hosting-notice queue and neither notice is sent` |

**Discriminates (mutation check, constitution §8).**
- **Writing `count: 0` for an outcome with no posts** — the total-record
  implementation, which is the natural one — fails the absent-key case and the
  compile-time `Partial` assertion. In production it puts a sentence in front of
  a customer saying nothing happened to nothing, on the last mail they will ever
  get from ReachKit; the criterion says the outcome "is not named at all".
- **Giving `already_gone` a place because the site is stamp-capable** passes
  every capability-driven reading and fails its own case. It sends the customer
  to a list to look for posts that cannot be in it — the failure the 2026-09-01
  fold corrected, and the one a reviewer reading only the code will call a
  missing `if`.
- **Summing two arms into one count** — most plausibly `already_gone` with
  `unreachable`, which both mean "not returned to draft" — fails the distinct-primes
  fixture. The criterion says "no sentence carries two outcomes or one count for
  both", and one number for both tells the customer a different thing about their
  own site than two numbers do.
- **Suppressing the mail when `stillLive` is empty** fails the either-kind case.
  It is the previous cut of this work order's behaviour, and its effect is that a
  customer whose destinations all answered gets no mail at all while posts stand
  live under their own name on their own domain, with no product left to tell
  them where.
- Replacing the tombstone with a `DELETE` fails both the no-row-deleted case and
  the compose-after-tombstone case — the discriminating case for ADR-051's first
  consequence.

## Out of scope
- The RLS condition that makes a tombstoned account unreachable through `db()` —
  **BP-002's**, per ADR-051 point 3. This work order stamps; it does not enforce.
- The 30-day purge itself — WO-144.
- Every unpublish outcome and its routing — WO-142's loop, whose **five-arm**
  `UnpublishResult` handling and empty-arm pin this WO consumes and does not
  restate (ADR-084, superseding ADR-082). This work order counts the outcomes
  that loop returned; it decides none of them, re-reads no customer site, and
  derives no population of its own (rule 2.4).
- **Deleting `named_for_removal` from `leftInWordPress`'s key set** because it
  has no members in production since ADR-084 Decision 4. The absent key is how
  the arm correctly disappears from this mail — that is the arm working as
  specified, not evidence it can be removed.
- **REQ-079 criterion 6's unreachable-destination arm for a *hosted* destination**
  — "names it and says what they must do about it", where after deletion the
  customer can do nothing. BP-063's second Open question, owner-owed and not
  settled in the round-two fold; this work order carries `stillLive` and renders
  whatever sentence the owner rules.
- Answering 410 — BP-004's edge, reading WO-130's state.
- Whether the unused remainder of a paid period is refunded — refunds are deferred
  by owner ruling (REQ-076, REQ-079 non-goals).
- Every sentence in the deletion mail — BP-019's registry; the owner's words.
  Four of them are outstanding by name: the four outcome sentences, and the one
  said where a site permits no place to point to (BP-063's first Open question).
  This work order supplies counts, places and keys and mints no sentence.
- **`WORDPRESS_STAMP_SLUG`'s value** — owner-owed and outstanding (ADR-084's
  second Open question); this work order reads BP-005's pin and never a literal.

## Rollback
Delete `delete-account.ts`, `left-in-wordpress.ts` and all three test files, and
revert the one arm added to `confirm.ts`. Accounts already tombstoned stay tombstoned and their stored
`purge_due_at` stands — the customer was promised that date (decision 4), and a
rollback does not move it.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-063 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan` quote REQ-079 and REQ-076 as they stand on disk, checked character-for-character on 2026-09-02. The criterion 6 row this work order carried until then was a summary inside quotation marks and matched no version of the text — a paraphrase presented as a quotation, which constitution §8 calls a defect.
- **Bounded.** 6 files in the file plan (5 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

**Status grounds addendum, 2026-09-02 (rule 3.2) — the gate this amendment does
not meet.** `leftInWordPress`, its four counts, the absent-key representation and
`already_gone`'s null place all derive from **REQ-079 criterion 6, `status:
in-review`** — its WordPress half rewritten for the third time in two days in the
2026-09-01 round-two fold — and read **REQ-056 criterion 16** and **REQ-060
criterion 6** (`in-review`, both). All three carry **zero open `REVIEW(...)`
lines** after two full review rounds (rule 3.4), so the text is settled — but
§3's gate stands on BP-063, whose own status-grounds addendum says the blueprint
gate is not met for the same edit and states in terms that "the work order
implementing this mail is owed a third re-extract". **This is that re-extract, and
it does not claim §3's gate is met.** `status: approved` is left as it stood and
is asserted under rule 3.2 on the design, not on the gate; if the owner's
signature changes criterion 4 or 6 rather than ratifying it, this status reverts
by the librarian's retroactive refusal. No `blocked-by` (rule 2.3): a requirement
mid-gate is not a conflict with another artifact.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — ADR-081 folded in: the c6 test-plan row no longer defers to WO-142's deleted `## Held, not planned` section, and unpublish routing is named out of scope as WO-142's.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-006, WO-011, WO-106); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — **third re-extract of criterion 6**, taken from `requirements/REQ-079.md` itself: the c6 row was a summary in quotation marks matching no version of the text and is now quoted whole, four bullets included; `DeleteAccountResult` gains `leftInWordPress` as a **partial map keyed by REQ-056 c16's four arms**, with `left-in-wordpress.ts` and its test added to hold the four rules the criterion states — one count per arm and never one for two, an absent key rather than a zero, `already_gone`'s place `null` by construction and not by stamp capability, and `place: null` with the count still carried where a site would not take the stamp; the send condition corrected to "either kind", so a run that reached every destination still mails when posts are left in the customer's WordPress; `stampCapability()` and `WORDPRESS_STAMP_SLUG` added to the consumed list.

## Validation report (appended by validator)
