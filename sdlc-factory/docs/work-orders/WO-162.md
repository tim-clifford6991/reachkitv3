---
id: WO-162
type: work-order
title: "Alerts, the overflow count, and the one supply statement Overview may make"
status: approved
ui: no
implements: [BP-038, REQ-041]
depends-on: [WO-268, WO-049, WO-156, WO-165, WO-188, WO-190]
blocked-by: []
rests-on:
  - claim: "Every alert line, the overflow line and the supply lines are owner-owed strings in BP-019's registry; this WO creates keys and their tests, never the strings"
    disposition: open
supersedes: []
---

# WO-162 — Alerts, the overflow count, and the one supply statement Overview may make

- Branch: wo/WO-162-overview-alerts
- Estimate: ≤1 day

## Goal
Select at most two alerts with the count of any remainder, and resolve the
supply statement and the stopped-work statement into exactly one key each.

## File plan
> Every path below is **NEW** (no `src/` on disk, checked 2026-08-31). Placement
> follows `structure.md` and BP-038's `code:` globs; provisional.

| File | New/Modify | Change |
|---|---|---|
| `src/app/(account)/app/overview/alerts.ts` | NEW | `Alert` selection: the pages awaiting review and the items that cannot proceed without the customer, ranked, capped at two, each with the `href` that navigates straight to the item; plus `overflow` carrying how many remain and where to see them. An empty list is a success state carrying BP-019's line, never a blank. |
| `src/app/(account)/app/overview/supply.ts` | NEW | The supply statement resolver — one key, resolved in the order exhausted → short → first-arrival-shortfall — and the stopped-work statement for a day ReachKit stopped its own work, which stops being stated when the work resumes. Overview **shares BP-039's empty-account resolver** and defines no second one (ADR-061). |
| `tests/app/overview/alerts.test.ts` | NEW | Cap, ordering, the overflow count, the empty-state line. |
| `tests/app/overview/supply.test.ts` | NEW | One statement not two, the resolution order, the shared resolver, the stopped-work line. |

## Interfaces
Consumes (verbatim from BP-038 `## Public interface`):
```ts
  supply?: { key: CopyKey; vars: Record<string, string> } // REQ-095 criteria 3, 5, 6 — one statement, never two
  alerts: readonly Alert[]                                // at most two (criterion 5)
  overflow?: { remaining: number; whereKey: CopyKey }
export interface Alert { key: CopyKey; vars: Record<string, string>; href: string }
```
and `supplyDepth()` / `supplyNotice()` (BP-013/BP-041), `accountFor()`
(BP-039, WO-165 — the shared empty-account resolver), `stoppedWorkStatement`
(BP-019/BP-054), the draft counts (BP-015).

Exposes: `readAlerts()` and `readSupplyStatement()` inside the overview module;
no new cross-node interface.

## Steps
1. Write the failing tests first (§8).
2. Select alerts from pages awaiting review and items blocked on the customer;
   cap at two; emit `overflow` with the remaining count and the where-to-see-it
   key when more exist.
3. Give every alert exactly one control — the `href` that navigates straight to
   the item.
4. Resolve the supply statement in the order exhausted → short →
   first-arrival-shortfall and return **one** key even where all three
   conditions hold at once.
5. Call BP-039's `accountFor` for the stopped-work fact; do **not** write a
   second resolver here (ADR-061 alternative 5).
6. Mutation check: return two supply keys and confirm the one-statement test
   fails; raise the cap to three and confirm the at-most-two test fails.

## Test plan
| Acceptance criterion (verbatim from REQ-041) | Test |
|---|---|
| 5. Given pages awaiting the customer's review, or items that cannot proceed without them, when they open Overview, then at most two are named, each with a single control that takes them straight to it, and where more exist the screen states how many remain and where to see them. | `tests/app/overview/alerts.test.ts › with five waiting items, exactly two alerts are returned, each with one href, and overflow carries remaining = 3 with its where key`, `(same file) › with none waiting the list is empty and the success line is carried, never a blank region` |

Additional discriminating tests: `tests/app/overview/supply.test.ts › with
exhausted, short and first-arrival-shortfall all true, exactly one key is
returned and it is the exhausted key`; `(same file) › the stopped-work statement is read
from BP-039's resolver, not from a second derivation, and stops being stated
when the work resumes`.

## Out of scope
- Rendering (WO-163).
- `supplyDepth`, `supplyNotice` and REQ-095's own thresholds (BP-041).
- The empty-date resolver itself (WO-165) — consumed, never duplicated.
- Any customer-visible string; keys only.

## Rollback
Revert the branch; both resolvers are pure reads.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-038 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 1 row of `## Test plan` quote REQ-041 as it stands on disk.
- **Bounded.** 4 files in the file plan (4 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 4 cross-block depends-on edges added (WO-031, WO-049, WO-188, WO-190); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-031 → WO-268

## Validation report (appended by validator)
