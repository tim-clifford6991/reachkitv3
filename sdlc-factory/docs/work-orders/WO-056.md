---
id: WO-056
type: work-order
title: "The free-path migration — network_hash, finished_at, from_incomplete_rescan, and the failed status"
status: approved
ui: no
implements: [BP-023, REQ-003]
depends-on: [WO-267]
blocked-by: []
rests-on:
  - claim: "`scans` exists with `domain`, `tier`, `status(running/done/degraded)`, `created_at`, `is_current`, `supersedes_scan_id`, `correction_state` and `stopped_reason` before this migration runs. That table and those columns are BP-002's baseline and BP-012's; this repo carries no `supabase/` directory today"
    disposition: refuted
  - claim: "The partial unique index `unique (domain) where is_current` is BP-012's and already exists, so a `failed` scan never becoming current holds by the index rather than by application logic"
    disposition: refuted
supersedes: []
wave: W2
---

# WO-056 — The free-path migration — `network_hash`, `finished_at`, `from_incomplete_rescan`, and the `failed` status

- Branch: wo/WO-056-scans-freepath
- Estimate: ≤1 day

## Goal
Add to `scans` the three columns and the one status value the free path's four
bounds are counted from, so that no rate-limit table is needed and REQ-003
criterion 2's p95 is checkable from stored data.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no
> `supabase/` directory and no `tests/`. Placement per `structure.md` rule 3 and
> 3a: all migrations live in `supabase/migrations/`, and `*_scans_freepath*` is
> BP-023's sub-token of BP-012's `scans` topic. Provisional against a repo with
> no code.

| File | New/Modify | Change |
|---|---|---|
| `supabase/migrations/<timestamp>_scans_freepath.sql` | NEW | Adds exactly three columns to `scans` and widens one enum, per BP-023's data-model delta and nothing else: `network_hash text null` with an index on `(network_hash, created_at desc)`; `finished_at timestamptz null`; `from_incomplete_rescan boolean not null default false`. Adds `failed` to `scans.status` alongside `running / done / degraded`. Creates no table. |
| `tests/scan/free/schema.test.ts` | NEW | Asserts the migrated shape and the index against a live migrated database, and asserts that no new table was created. |

## Interfaces

Consumes: BP-002's `dbAdmin()` for the test harness only. Nothing at runtime.

Exposes: no TypeScript surface. The columns are read by WO-057 (`admitFreeScan`),
written by WO-058 (`claimFreeScanSlot`) and read by BP-022's control derivation
(WO-061). Their contract, verbatim from BP-023 `## Data model delta`:

| Column | Type | Why |
|---|---|---|
| `network_hash` | `text null` | the counter REQ-003 criteria 6 and 7 are evaluated over; null for paid tiers. Indexed `(network_hash, created_at desc)` |
| `finished_at` | `timestamptz null` | REQ-003 criterion 2's p95 is a claim about elapsed time; without an end stamp it cannot be checked from data |
| `from_incomplete_rescan` | `boolean not null default false` | REQ-001 criterion 14's "made once and does not chain", set at claim time and read by BP-022 |

## Steps
1. Write the failing schema test first.
2. Write the migration: the three `alter table` statements, the enum widening,
   and the one index.
3. Confirm the index is the composite `(network_hash, created_at desc)` BP-023's
   NFR budget costs its two aggregates against — not two single-column indexes.
4. Run the migration on a scratch database and the test against it.
5. Confirm no `rate_limits` table, no counter table and no new topic token appear
   anywhere in the diff (BP-023 decision 1, `BUILD.md` §10's bar).

## Test plan

> Criteria are copied verbatim from `requirements/REQ-003.md`; line breaks are
> normalised to fit the table and no word is changed. This WO makes the criteria
> below *checkable*; the behaviour they describe is WO-057's and WO-058's.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-003 c2 — "Given any 100 consecutive free scans of reachable domains, counted in the order they started and whichever of criterion 1's ways started each of them, when the time from each scan's start to a readable report is measured, then no more than 5 of those 100 take longer than 60 seconds, and none takes longer than the 90 seconds of criterion 5." | `scan/free/schema · elapsed time is derivable from stored rows alone` — asserts `finished_at` exists and is nullable, and that a query over `scans(created_at, finished_at, tier)` alone computes the p95 for the last 100 free rows without joining another table. This is the criterion's *measurability*; the ceiling behaviour is WO-059's. |
| REQ-003 c6 — "Given a visitor who has already started five free scans from the same network within the last hour … when another scan would start for them, then it does not start, they are refused with one written line saying how long until they may scan again, and the refused scan, having never run, produces no report of its own." | `scan/free/schema · the hourly counter is an indexed aggregate over scans` — asserts `network_hash` exists, is nullable, and that the composite index `(network_hash, created_at desc)` is present and is used by the planner for a count over `created_at > now() - interval '1 hour'` (an `explain` assertion). |
| REQ-003 c7 — "Given a scan is already running from the visitor's network, when another scan would start for them, then no second scan starts …" | `scan/free/schema · in-flight is `status = 'running'`, not a second record` — asserts `scans.status` accepts `running`, `done`, `degraded` and `failed` and rejects any other value, and asserts by catalogue query that the migration created no table. |
| REQ-001 c14 — "… That offer is made once and does not chain: a report that is itself the product of a re-scan started from it carries the same written line and no re-scan control …" | `scan/free/schema · from_incomplete_rescan defaults false and is not null` — asserts the column's default and not-null constraint, so a row inserted by a writer that never heard of this column reads `false` rather than `null`. |
| REQ-001 c17 — "… A measurement that produces no report — it failed, or it was refused — leaves the previous report and its date unchanged at that address …" | `scan/free/schema · a failed scan cannot become current` — inserts a `failed` row for a domain that already has an `is_current` row and asserts the existing partial unique index still holds exactly one current row, unchanged. |

## Out of scope
- Any writer of these columns — WO-058.
- Any reader of them — WO-057, WO-061.
- The `stopped_reason`, `is_current`, `supersedes_scan_id` and `correction_state`
  columns — BP-012's, already declared there.
- `domain_blocks` — BP-002's table; this WO adds no migration touching it.

## Rollback
A down migration dropping the three columns and the index, and narrowing the
status enum back. No production data exists; the columns are additive and
nullable or defaulted, so the forward migration is safe to re-run.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-023 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-003 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267
- 2026-09-03 started — implementer — worktree `/root/projects/reachkitv3-wt/WO-056`, branch `wo/WO-056-scans-freepath`, off `doctrine-0.11.0` tip `da68a4b`.
- 2026-09-03 finished — implementer — migration `supabase/migrations/00000000000005_scans_freepath.sql` and `tests/scan/free/schema.test.ts` (14 cases, 13 pass + 1 `it.skip`) built and watched-fail-first; full suite 22 files / 466 pass / 1 skipped, typecheck and lint clean. **Rule 4.2 finding, stated once:** the WO's own file-plan header ("this repo carries no `supabase/` directory today") and `rests-on` row 1 ("`is_current` … before this migration runs … already declared") are both stale — `supabase/` now exists (WO-267, merged since this WO was authored) with 4 migrations, and `scans.is_current`/`supersedes_scan_id`/`correction_state`/`stopped_reason` do **not** exist anywhere in the repo; the migration that adds them (WO-254) is `status: draft`, not in wave W2, and not named in this WO's `depends-on`. BP-023 `## Data model delta` carries the same stale premise. I proceeded per instruction: built exactly the 3 columns + `failed` status widening this WO's own `## Interfaces` table specifies (nothing from `is_current`'s group, matching `## Out of scope`), and wrote REQ-001 c17's test row as far as this migration's own scope reaches (a `failed` insert touches no sibling row) with the literal "existing partial unique index still holds exactly one current row" half left `it.skip`'d, cited to WO-254, rather than fabricated. Ready for `/validate`.
- 2026-09-03 finished — validator — TST-020: pass, with the implementer's rule 4.2 stale-premise finding confirmed independently and both `rests-on` rows set `refuted`; full suite (22 files / 466 pass / 1 skipped), typecheck, lint and build all clean; mutation probes on all 5 criteria discriminate.

## Validation report (appended by validator)

## TST-020

### Scope
WO-056 (`wo/WO-056-scans-freepath`, commit `c6c1788`), validated against its own `## Test plan`, `## Interfaces` and `## Out of scope` sections, and against constitution rule 4.2 (the implementer's flagged deviation) and rule 2.3b (`rests-on` disposition). Diff against `da68a4b` (WO-056's `depends-on` base) is exactly 3 files: `supabase/migrations/00000000000005_scans_freepath.sql` (NEW), `tests/scan/free/schema.test.ts` (NEW), and this WO's own `## Log` — matching the file plan exactly, nothing outside WO-056's scope touched.

### Suite / build
- `vitest run --project node --project ui --project db`: 22 files, 466 passed, 1 skipped — matches the implementer's logged count exactly.
- `tsc --noEmit`: clean.
- `eslint .`: 8118 pre-existing problems, verified byte-identical in count and file list on the pre-WO-056 commit `da68a4b` (checked out and re-run) — none attributable to this WO's two new files; `tests/scan/free/schema.test.ts` lints clean standalone.
- `next build`: compiles and prerenders cleanly.

### Criterion-by-criterion

| Criterion | Test | Verdict | Mutation probe |
|---|---|---|---|
| REQ-003 c2 (p95 measurability) | `finished_at` nullable timestamptz + no-join query assertion | PASS | Reverting `finished_at` to `not null default now()` fails the nullability assertion (1/1 targeted test fails) |
| REQ-003 c6 (hourly counter, indexed) | `network_hash` column + composite index existence + planner-usage (`explain`) assertion | PASS | Splitting the composite into two single-column indexes fails both the "no single-column index" and the "planner uses composite" assertions (2/2); dropping `network_hash` entirely fails the whole file (migration itself fails to apply) |
| REQ-003 c7 (in-flight / no second table) | `status` enum accepts `running/done/degraded/failed`, rejects other values; catalogue query shows no new table | PASS | Removing the `failed` widening fails the enum-acceptance assertion and cascades into every later `insertScan({status:'failed'})` call (2/2); adding a `rate_limits` table fails the table-list assertion by name |
| REQ-001 c14 (`from_incomplete_rescan` not-null default false) | column shape, default-read, explicit-null-rejected | PASS | Making the column nullable with `default true` fails all 3 assertions in the block |
| REQ-001 c17 (failed scan leaves other rows unchanged) | in-scope half: a `failed` insert for a domain with an existing row leaves that row and its `report`/`status` untouched, row count is 2 | PASS (partial, by design — see below) | Depends on the `failed` status existing (covered by the c7 mutation above); this half does not independently discriminate beyond that, since no application logic in this WO's scope could corrupt a sibling row — it is an honest assertion of what is checkable now, not a stronger claim |
| REQ-001 c17, literal test-plan wording ("the existing partial unique index still holds exactly one current row, unchanged") | `it.skip`, cited to WO-254 | **NOT COVERED — by design, see finding below** | n/a — no fabricated pass exists in its place |

### Rule 4.2 finding — independently confirmed
Reproduced the implementer's claim from first principles, not taken on their word:
1. `grep -rn "is_current|supersedes_scan_id|correction_state|stopped_reason"` across `.sql`/`.ts` in the repo: every hit is a comment or a doc reference to the *future* migration (BP-023 `## Data model delta`, BP-012, WO-254, the baseline migration's own comment at line 96, WO-056's own `## Out of scope`). None is a `create table`/`alter table` that actually adds the column. `supabase/migrations/00000000000001_baseline.sql`'s `create table scans (...)` (the only DDL for the table) has no such columns and no partial unique index — only `idx_scans_site_id` and a non-unique `idx_scans_domain`.
2. `sdlc-factory/docs/work-orders/WO-254.md` front matter: `status: draft`, no `wave:` field at all. It is not named in WO-056's `depends-on: [WO-267]`, and it does not appear in `sdlc-factory/docs/registry/waves.md`'s W2 row (`WO-051, WO-041, WO-056, WO-057, WO-058, WO-062, WO-070`).
3. WO-056's own `## Interfaces` table (the actual contract) lists exactly the 3 columns built; its `## Out of scope` explicitly excludes `stopped_reason`/`is_current`/`supersedes_scan_id`/`correction_state`. The migration file and the diff build exactly that and nothing else — confirmed by `git diff da68a4b c6c1788 --stat` (3 files) and by reading the migration SQL directly (3 `add column` statements + 1 index + the status-check widening, no more).
4. Checked whether this gap propagates: grepped WO-057, WO-058 and WO-062 (the rest of wave W2) for the same four column names — zero hits in either file. WO-057's own `## Interfaces` names exactly `WO-056's scans.network_hash and the failed status` as what it consumes, not `is_current`. **The gap is self-contained to WO-056/WO-254 and does not block WO-057, WO-058 or WO-062.**

### Judgment on the `it.skip`
This is the right call, not a validation failure. The stale premise lives in the WO's own `rests-on` row 1, its `## Test plan` row 5's literal wording, and BP-023's `## Data model delta` prose — all upstream of the implementer, all pre-dating WO-267's merge (which created `supabase/` after this WO was cut and self-certified). The WO's own binding contract — `## Interfaces` and `## Out of scope` — is internally consistent and was built exactly as specified. Faking the partial-unique-index assertion against a column that does not exist would itself be the constitution §1.2/§8 violation (a test that appears to pass while asserting nothing real, worse than a vacuous test because it actively misrepresents a database property that isn't there). The `it.skip` is named, dated in intent (cited to WO-254), documented once in the test file's own header and once in the WO's `## Log` — not silently dropped, not repeated. This is rule 4.2's flag-once-and-proceed pattern executed correctly.

The one thing this does **not** excuse: REQ-001 c17's full claim is not yet checkable end-to-end, and won't be until WO-254 lands and either WO-056 or WO-254 re-opens this test. That gap is real and is correctly WO-254's to close (WO-254's own `## File plan` already names the `is_current` migration and its partial unique index as in scope), not a defect to send back to this WO's implementer.

### rests-on disposition (rule 2.3b)
- Row 1 (`scans` exists with `is_current`/`supersedes_scan_id`/`correction_state`/`stopped_reason` before this migration runs) — **refuted**. None of the four columns exist anywhere in the repo (confirmed above); the claim that they are "already declared" and present "before this migration runs" does not hold as of `c6c1788`.
- Row 2 (the partial unique index `unique (domain) where is_current` already exists) — **refuted**. No such index exists; `idx_scans_domain` (non-unique, non-partial) is the only index on that column. WO-254's file plan is the migration that would create it.

No `open` row remains on this WO. Upstream: BP-023's own `rests-on` list (4 rows — vendor SLA, network-address counting, `x-forwarded-for` trust, BP-005 pins) contains no claim about `is_current`'s existence, so none of its rows change disposition on this observation; the equivalent claim there lives in `## Data model delta` prose ("already declared" — true only as *documented in BP-012*, not as *implemented*), which is a wording risk for the architect to consider at wave close, not a `rests-on` row this validator can disposition. REQ-003's `rests-on` rows (vendor battery SLA, network counting, timezone) are likewise unaffected by this build.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict
pass

WO-056 shipped exactly what its own `## Interfaces` and `## Out of scope` specify; every in-scope criterion is covered by a test verified to discriminate by mutation; the one criterion row the WO's own text over-promised (REQ-001 c17's literal index assertion) is honestly deferred rather than fabricated, cited to the work order that actually owns it (WO-254), and does not block any other W2 work order. Full suite, typecheck, lint (no new problems) and build all clean.
