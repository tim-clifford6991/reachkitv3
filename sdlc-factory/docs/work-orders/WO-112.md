---
id: WO-112
type: work-order
title: "Address-wide suppression and the opt-out token"
status: approved
ui: no
implements: [BP-029, REQ-010]
depends-on: [WO-267, WO-109]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-112 — Address-wide suppression and the opt-out token

- Branch: wo/WO-112-suppress-optout

- Estimate: ≤1 day

## Goal
Stop every running and waiting sequence for an address, block every future one,
and give a stranger exactly one capability — a signed, unexpiring, idempotent
token that suppresses the address it was signed for.

## Provisional against an empty repo
**No `src/` exists.** All files NEW, inside BP-029's `src/lib/mail/leads/**`
glob.

Reuse check (rule 7.2): `registry/capabilities.md` row "Address-wide
suppression — `src/lib/mail/leads/` — BP-029 — `suppressAddress()` ·
`applyOptOutToken()` — consumers BP-001 (opt-out), BP-017". Nothing exists. The
`unsubscribeTokenFor()` family (WO-108, BP-059) is **not** reused: ADR-042 fixes
two token formats over two subjects, permanently.

## Read ADR-042 before touching this file
`suppressAddress()` never writes `users.notify`. This store is keyed by an email
address and reaches `stoppable: 'opt-out'` kinds only. Merging it with the
per-kind toggles removes a paying customer's magic-link sign-in mail, silently,
discovered only when they cannot get back in.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/mail/leads/suppress.ts` | NEW | `suppressAddress(email, cause)` — one idempotent insert into `email_suppressions` on the lowercased address (a second opt-out click writes nothing and still succeeds), and in the same transaction sets every `running` and `waiting` sequence for that address to `stopped`. Releases nothing (REQ-010 c12's last sentence). Plus `isSuppressed(email): Promise<boolean>`, the read WO-106's seam performs immediately before the vendor call for a `'opt-out'` kind, and the read `scheduleSequence()` performs before starting anything. |
| `src/lib/mail/leads/optout.ts` | NEW | `optOutTokenFor(email)` — signed with BP-005's secret, no expiry field encoded. `applyOptOutToken(token)` — verifies, calls `suppressAddress(email, 'opt_out')`, returns `{ email }`; idempotent, so applying it twice succeeds both times. An unverifiable token returns `{ error: 'invalid' }` and writes nothing. |
| `tests/mail/leads/suppress.test.ts` | NEW | REQ-010 c10 and c11's store half; idempotency; the ADR-042 separation. |
| `tests/mail/leads/optout.test.ts` | NEW | The token's three properties: unexpiring, idempotent, single-capability. |

## Interfaces
**Exposes** (verbatim from BP-029 `## Public interface`):
```ts
/** BP-016's declared entry point, realised here. Ends every running and every
 *  waiting sequence for the address and blocks every future one. Never touches
 *  users.notify — that is BP-059's, and ADR-042 states why the two must not
 *  become one. */
export function suppressAddress(email: string, cause: 'opt_out' | 'subscribed'): Promise<void>

// src/lib/mail/leads/optout.ts
export function optOutTokenFor(email: string): string                 // signed, no expiry
export function applyOptOutToken(token: string): Promise<
  | { email: string }
  | { error: 'invalid' }>                                             // idempotent
```
Plus, chosen under rule 1.1: `isSuppressed(email: string): Promise<boolean>` —
the read side of the same store. Derivation: BP-053's seam "consults BP-029's
suppressions … immediately before the vendor call" and BP-029's error section has
`scheduleSequence` "refuse an address present in `email_suppressions`"; both need
a read, and neither node names one. One exported read beats two ad-hoc queries in
two modules. Cost of reversing: inline the query at both call sites.

**Consumes**: `dbAdmin()` (BP-002); the `email_suppressions` and `leads` schema
(WO-109); BP-005's opt-out signing secret.

## Steps
1. Write both test files first and watch them fail.
2. `suppress.ts`: the idempotent insert and the sequence-stop, in one
   transaction — a suppression that commits without stopping the running
   sequence is the failure c11 names.
3. `optout.ts`: sign, verify, apply.
4. Assert this module imports nothing from `src/lib/mail/notifications/`
   (ADR-042), as a dependency assertion.
5. Mutation check: make the sequence-stop a separate non-transactional step and
   confirm the c11 test fails under an induced failure between the two.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-010 c11 — "Given any of these emails, when it is opened, then it carries a working opt-out, and using it stops all future follow-up email to that address — every remaining touch of the running sequence, every sequence for every other domain, and any sequence a future delivery would otherwise start." | `tests/mail/leads/optout.test.ts` → `applying an opt-out token stops the running sequence, every waiting sequence for every other domain, and makes isSuppressed true for a domain never scanned` — three cases, one per limb, the third asserting the read a future `scheduleSequence` performs. **The "carries a working opt-out" limb is WO-113's** (the link is a block in every lead template). |
| REQ-010 c10 — "Given a lead who subscribes at any point, when they subscribe, then no further follow-up is sent to that address for any domain, and no later delivery starts one." | `tests/mail/leads/suppress.test.ts` → `suppressAddress(email, 'subscribed') stops every sequence for the address and blocks a later one` — the same mechanism under the second cause. The BP-017 webhook that stamps `leads.converted_at` and calls this is BP-017's; BP-029 states the call is made "from the same transition, so criterion 10 does not depend on the webhook remembering to", and this WO exposes the entry point that transition calls. |
| REQ-010 c12 — "Given a domain's page has been delivered to an address, when that domain's sequence begins, then it begins no earlier than the delivery of that page and never while another sequence to that address is running or still waiting to run: at most one sequence to an address is ever running, and sequences to one address run in the order their pages were delivered, however many domains that address has. A sequence that has not begun within 7 days of its own page's delivery is dropped and never sent, so no follow-up about a domain ever reaches an address more than 14 days after that domain's page was delivered to it. A sequence ended by a subscribe (criterion 10) or an opt-out (criterion 11) releases nothing — those end the waiting sequences too, and no waiting sequence ever begins after either." | `tests/mail/leads/suppress.test.ts` → `suppression sets waiting sequences to stopped and releases none` — **the last sentence only**. The rest of c12 is WO-114's. |

## Out of scope
- The `/opt-out/{token}` route and the confirmation page — BP-001's surfaces.
- Releasing, dropping or sending sequences (WO-114).
- The per-kind toggles and their token (WO-107, WO-108) — ADR-042.
- Any customer-visible string (REQ-093 c1).

## Rollback
Delete both source files and both tests. WO-109's tables stand; no row has been
written by this block yet.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-029 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-010 as it stands on disk.
- **Bounded.** 4 files in the file plan (4 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267

## Validation report (appended by validator)
