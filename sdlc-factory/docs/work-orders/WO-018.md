---
id: WO-018
type: work-order
title: "safeFetch — the SSRF-guarded, DNS-pinned, size-capped outbound fetcher"
status: approved
wave: W3
ui: no
implements: [BP-006]
depends-on: [WO-006]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-018 — safeFetch — the SSRF-guarded, DNS-pinned, size-capped outbound fetcher

- Branch: wo/WO-018-safe-fetch
- Estimate: ≤1 day

## Goal
Ship the one path from this process to a URL the product did not author, refusing
by policy before any connection and returning a typed outcome rather than
throwing.

## Provenance of this file plan
**`src/` exists (since W1), and none of the five paths below is in it.** Each
was checked directly on 2026-09-04 and is genuinely absent, so every one is
NEW; `eslint.config.mjs` exists and is the plan's one MODIFY. Paths are
anchored to `structure.md`'s row for `src/lib/egress/**` and BP-006's `code:`
globs. (Corrected 2026-09-04 by the planner: the sentence here read "This
repository holds no `src/`", true when this order was cut on 2026-08-31 and
stale since W1. WO-018's implementer flagged it under rule 4.2 and correctly
did not edit the plan. Only the sentence moved — no path, no file-plan row and
no criterion changed.)

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/egress/types.ts` | NEW | `FetchOutcome` and `RobotsPolicy`, both exactly BP-006's declarations, transcribed with their comments (BP-006 declares `RobotsPolicy` as of 2026-08-31: six fields, no optional arm). The reader that produces one is WO-020's; the type is shipped here because `safeFetch`'s robots port takes it and because BP-010, BP-024 and BP-047 all consume it and none of them declares it. |
| `src/lib/egress/policy.ts` | NEW | The refusal policy, evaluated **before any connection**: private, loopback, link-local, multicast and reserved address space; non-http(s) schemes; ports other than 80/443; and a redirect re-check so each hop is evaluated again. |
| `src/lib/egress/safe-fetch.ts` | NEW | `safeFetch()`. Resolves the name, checks the resolved address against `policy.ts`, then **connects to that resolved address** — the pinning — so a name cannot be re-resolved between check and connect. Timeout default 8000 ms, hard max 15000; size cap 2 MB, a larger response returning `too_large` rather than a truncated parse. Never throws. Robots consultation is delegated to WO-020's reader and is on by default. |
| `eslint.config.mjs` | MODIFY | Adds BP-006's audit rule: a `fetch(` call anywhere under `src/lib/` that is not inside `src/lib/egress/` or `src/lib/vendors/` is an error. This is the only work order in the block after WO-001 that edits this file. |
| `tests/egress/policy.test.ts` | NEW | Refusal cases. |
| `tests/egress/safe-fetch.test.ts` | NEW | Outcome, pinning, cap and timeout cases. |

## Interfaces
Exposes (verbatim from BP-006 `## Public interface`):
```ts
export type FetchOutcome =
  | { ok: true; status: number; url: string; html: string; bytes: number; readAt: Date }
  | { ok: false; reason: 'dns' | 'refused' | 'timeout' | 'too_large' | 'blocked_by_policy'
              | 'robots_disallowed' | 'status'; status?: number; url: string; readAt: Date }

export function safeFetch(url: string, opts?: {
  timeoutMs?: number            // default 8000, hard max 15000
  maxBytes?: number             // default 2_000_000
  respectRobots?: boolean       // default true
  userAgent?: 'reachkit-measure' | 'reachkit-verify'
}): Promise<FetchOutcome>

/** Declared here because `readRobots` produces it and this is the one module
 *  that reads a `robots.txt`. BP-010 returns `Measured<RobotsPolicy>` from
 *  `measureDomain`, BP-024's `verdictOf` takes one and BP-047 serves one; none
 *  of them declared it. It is a parse of a fetched document, never a judgement
 *  about it: what each rule means for the blocked-readers count is BP-024's and
 *  what ReachKit serves on a hosted domain is BP-047's. */
export interface RobotsPolicy {
  ok: true
  origin: string
  readAt: Date
  /** True where the document tells *every* reader not to crawl — REQ-004 c7's
   *  "a home document the scan read that tells every reader not to index it". */
  disallowsAll: boolean
  /** Per user-agent token, whether this document disallows it at the origin
   *  root. Keyed by the token as written in the document, lowercased; the
   *  closed set the product counts over is BP-005's `AI_READER_AGENTS`
   *  (ADR-022, ADR-090) and is applied by the caller, not here. */
  disallowedAgents: Readonly<Record<string, boolean>>
  /** Sitemap declarations found in the document, in the order they appeared. */
  sitemaps: readonly string[]
  /** True where the origin answered 404 or an equivalent "no robots.txt".
   *  That is a *read* with nothing in it — REQ-004 criterion 7's `zero`, not
   *  criterion 6's `undeterminable` — and the distinction is the reason this
   *  field exists rather than a null policy. */
  absent: boolean
}
```

**Why `absent` is a field and not a null policy.** A missing `robots.txt` is a
document ReachKit *read* and found nothing in — REQ-004 criterion 7's measured
zero — not a document it could not determine, which is criterion 6's `—`.
Modelling it as `RobotsPolicy | null` or as the `{ ok: false }` arm would make
"this site blocks no AI reader" indistinguishable from "we could not tell", and
the blocked-readers count would then read `—` on exactly the sites whose true
answer is 0. The field is the shape that keeps the two apart at every call site.
Consumes (BP-005 `constants.ts`): `VERIFY.userAgent`, `TIMING`.

## Steps
1. Write `types.ts` transcribing the union and the `RobotsPolicy` interface,
   comments included. Every field is required: there is no optional arm and no
   `null` policy, so `absent` is the only way to say "no robots.txt".
2. Write `policy.ts` against the address classes above.
3. Write `safe-fetch.ts` with resolve-check-connect in that order.
4. Add the lint rule.
5. Write both test files. Watch the DNS-rebinding case fail first by connecting
   to the hostname instead of the pinned address (constitution §8).

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-006 carries
> `satisfies: []` and was cut from `BUILD.md` §6.4. BP-006's `## Error & edge
> behavior` cites REQ-004 criteria 6 and 7 as the reason its return type is a
> union, but **REQ-004 is not in BP-006's `covers:` list** — reported with this
> block's return. No REQ criterion is inherited here.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-006 error behaviour: "Refuses by policy, before any connection: private, loopback, link-local, multicast and reserved address space; non-http(s) schemes; redirects that leave the policy (each hop re-checked); ports other than 80/443." | `tests/egress/policy.test.ts` — one case per class, plus a redirect chain whose second hop is private. Each returns `blocked_by_policy` and opens no socket. |
| BP-006 error behaviour: "DNS-pinned: the address resolved during the policy check is the address connected to, so a name cannot be re-resolved to a private address between check and connect." | `tests/egress/safe-fetch.test.ts` — a resolver that returns a public address on the first call and a private one on the second still connects to the first. Removing the pin fails it. |
| BP-006 error behaviour: "**Never throws.** Every failure is a typed `FetchOutcome` with `ok: false`" | Same file — every failure class asserted to resolve, not reject. |
| BP-006 NFR budget: "Size cap 2 MB; a larger response is `too_large`, not a truncated parse." | Same file — a 3 MB response yields `too_large` and no `html`. |
| BP-006 NFR budget: "p95 ≤ 3 s, hard timeout 8 s per fetch" | Same file — a hanging server yields `timeout` at the configured bound, and `timeoutMs` above 15000 is clamped. |
| BP-006 NFR budget: "A `fetch(` call anywhere else under `src/lib/` that is not BP-008's vendor client is a lint error." | `tests/egress/policy.test.ts` — lints a fixture under `src/lib/measure/` calling `fetch(` and asserts the rule reports. Removing the rule fails it. |
| BP-006 NFR budget: "host, outcome reason, status, bytes and duration; never a body" | `tests/egress/safe-fetch.test.ts` — asserts the log record's field set exactly. |
| BP-006 public interface, `RobotsPolicy.absent`: "True where the origin answered 404 or an equivalent \"no robots.txt\". That is a *read* with nothing in it — REQ-004 criterion 7's `zero`, not criterion 6's `undeterminable` — and the distinction is the reason this field exists rather than a null policy." | `tests/egress/policy.test.ts` — a type-level suite over `RobotsPolicy`: its fields are exactly `ok`, `origin`, `readAt`, `disallowsAll`, `disallowedAgents`, `sitemaps`, `absent`; none is optional; the type admits no `null` and no second arm. Deleting `absent`, or widening any field to `\| null`, fails to compile — which is the whole discrimination, because a nullable policy makes "read it, nothing there" and "could not read it" the same value. The *behaviour* of a 404 is WO-020's. |

## Out of scope
- `resolvesInDns()` — WO-019.
- `readRobots()` and the per-agent verdicts — WO-020. `safeFetch` calls the
  reader through a narrow port so the two work orders share no file. This WO
  ships `RobotsPolicy`'s **declaration**; WO-020 ships the parse that produces
  one, including what `absent` is set to.
- Crawling, link following and sitemap reading: `BUILD.md` §17 excludes them and
  BP-006 states "the caller supplies the URL set".

## Rollback
Delete the egress files and revert the one lint rule. Nothing consumes the seam
until BP-010 and BP-015 land.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-006 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 8 rows of `## Test plan` quote BP-006's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 6 files in the file plan (5 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended: BP-006 now declares `RobotsPolicy`, so `types.ts` ships the six-field interface verbatim instead of a bare mention, with a type-level test that `absent` cannot be collapsed into a null policy.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-04 finished — planner — `wave: W3` set by `/wave propose`. Carried into W3 unmerged: it passes rule 2.6's merge test against WO-019 and WO-020 (each states its goal and stop condition without citing the others, and its own file plan says so), and W3's verdict engine imports `RobotsPolicy` from the `src/lib/egress/types.ts` this order ships.
- 2026-09-04 started — implementer — worktree `/root/projects/reachkitv3-wt/WO-018`, branch `wo/WO-018-safe-fetch` cut from `doctrine-0.11.0` (tip `9786ef7`).
- 2026-09-04 finished — implementer — shipped `types.ts` (`FetchOutcome`/`RobotsPolicy`, verbatim), `policy.ts` (address/scheme/port refusal, `node:net`'s `BlockList`) and `safe-fetch.ts` (`safeFetch`: resolve once → check the resolved address → connect to that address, never the hostname; redirects re-checked per hop; never throws; timeout 8000ms default/15000ms hard max; 2MB size cap; robots delegated through a port WO-020 wires). Added `eslint.config.mjs`'s fifth local rule (`no-fetch-outside-egress`) confining `fetch(` to `src/lib/egress/**`/`src/lib/vendors/**`. 59 new tests across `tests/egress/policy.test.ts` and `tests/egress/safe-fetch.test.ts`, all watched to fail first (module absent, and the pinning case specifically fails against a deliberately unpinned connect); full suite 629 passed/1 skipped/29 files (baseline 570/1/27, additive only); typecheck clean; lint 8118 problems, byte-identical to baseline, new rule fires nowhere in the existing tree; build clean via a throwaway, deleted, never-committed `.env.local`. Rule 4.2 note: the file plan's provenance sentence ("This repository holds no `src/`") is stale — `src/` has existed since W1 — but every file-plan path and the file plan itself were independently confirmed correct; flagged for the planner to correct the sentence, not acted on unilaterally. Two rule-1.1 parameters chosen and recorded in `safe-fetch.ts`'s own header comments: `MAX_REDIRECTS = 5` (BP-006 names no hop count) and `MEASURE_USER_AGENT`/the default user-agent token (BP-005's `constants.ts` declares `VERIFY.userAgent` for `'reachkit-verify'` but no `MEASURE` group; `constants.ts` itself is out of this WO's file plan). No `rests-on` rows carried by this WO. Ready for `/validate`.
- 2026-09-04 finished — implementer — TST-027 finding fixed: `classifyAddress`'s `IPV4_MAPPED_RE` unwrap-and-reclassify block in `policy.ts` was dead code (`node:net`'s `BlockList` already resolves an IPv4-mapped IPv6 literal against the IPv4 subnets held in the same per-class list, since each class's `BlockList` mixes both families — confirmed directly against `node:net`, and by removing the block and re-running all 42 `policy.test.ts` cases unchanged) credited in comment with closing an SSRF bypass it did not close. Deleted the block and the false comment; kept the `::ffff:127.0.0.1`/`::ffff:10.0.0.5` behavioural assertion, rewritten to credit `BlockList`'s mixed-family check rather than the removed mechanism, and confirmed by mutation (splitting a class's IPv4/IPv6 subnets into two separate `BlockList`s) that it now fails exactly the way a regression of that kind would break it. Also removed `isBlockedAddress`: exported, unused anywhere in the repo, untested, not part of BP-006's public interface — same untested-security-surface pattern, lower stakes. Checked the rest of `policy.ts` for the same failure mode: found nothing else vacuous or over-credited — `checkSchemeAndPort`'s default-port branches, the `family === 0` garbage-input branch, and every named address class are each independently exercised and each fails under its own targeted mutation. `FetchOutcome`, `RobotsPolicy` and `safeFetch()`'s signature unchanged; `safe-fetch.ts` carries zero diff. `npm test` 629 passed/1 skipped/29 files (unchanged from the prior run), typecheck clean, lint 8118 (unchanged). Commit `8452d07` on `wo/WO-018-safe-fetch`. Ready for re-validation.
- 2026-09-04 failed — validator — TST-027, verdict findings: `tests/egress/policy.test.ts`'s IPv4-mapped-IPv6 "known bypass" test does not discriminate the code it names — deleting `policy.ts`'s explicit unwrap-and-reclassify block leaves all 42 tests in the file passing (`node:net`'s `BlockList` already resolves an IPv4-mapped IPv6 literal against any IPv4-family subnet in the same `BlockList`, which is how the five class lists are built, so the block is dead code and its comment misattributes the protection). No exploit exists — `::ffff:127.0.0.1` and `::ffff:10.0.0.1` are still correctly refused, by the pre-existing mechanism — so this is a record/test-suite defect against BP-006's refusal-policy criterion (row 1), not a security hole in the shipped behaviour. Every other criterion, both mutation probes this validation was specifically asked to run (DNS-pinning TOCTOU, per-hop redirect re-check), and every count (629/1/29, typecheck, lint 8118 byte-identical, build) independently reproduced clean — next: implementer, `tests/egress/policy.test.ts`'s "IPv4-mapped IPv6 literal is unwrapped and reclassified" test and `src/lib/egress/policy.ts`'s `IPV4_MAPPED_RE` block/comment, either delete the redundant code and correct the comment to credit `BlockList`'s native cross-family matching, or rewrite the test so it actually discriminates that code path. Regression not run (gated on a passing verdict); full detail in `## TST-027` below.
- 2026-09-04 finished — validator — TST-027 re-validation, commit `8452d07`, verdict pass: the finding is resolved — the dead IPv4-mapped-IPv6 unwrap block and its misattributing comment are removed, `isBlockedAddress` (confirmed unused repo-wide, not in BP-006's interface) removed with it, and the renamed test now confirmed to discriminate — reproduced the exact per-family-`BlockList`-split mutation the test's own comment names and got `expected 'public' to be 'loopback'`, not taken on the implementer's word. Swept the rest of `policy.ts` independently: every `CLASS_ORDER` range reachable against one address per distinct CIDR entry, none masked. Exported interface (`FetchOutcome`, `RobotsPolicy`, `safeFetch`, `SafeFetchOpts`, `RobotsPort`, `__setRobotsPortForTesting`) confirmed byte-for-byte unchanged since `68a0219` — `safe-fetch.ts`/`types.ts` diff empty — the promise WO-277 is branched on holds. Counts (629/1/29, typecheck clean, lint 8118 byte-identical) reproduced in a substrate-quiet window; several earlier same-session runs showed transient unrelated failures from three concurrent implementers sharing this sandbox's Postgres instance and CPU, detailed in `## TST-027`'s `### Regression` section, none inside `tests/egress/**` in any uncontended run. `rests-on` — this WO's own and BP-006's upstream row — both `[]`, nothing to disposition.
- 2026-09-04 finished — validator — `/regress` on TST-027's re-validated passing verdict: full suite/typecheck/lint/build re-run clean in a quiet window (629/1/29, byte-identical lint, clean build), matching this validation's own figures; `no-fetch-outside-egress` re-confirmed firing on a real injected `fetch(` after the fix, repo-wide lint and full suite both clean with the rule live; `factory-console impact WO-018` (run from the worktree) named BP-001 (1 file) and BP-006 (5 files), both `satisfies: []`, correctly 0 requirements to re-check (rule 5.4), stated explicitly per rule 5.5 rather than left silent; branch current with `doctrine-0.11.0` (`9786ef7`, unmoved, 2 ahead/0 behind, no rebase needed); WO-003's middleware default-deny re-confirmed live (16/16); all 21 earlier `done` WOs re-checked, untouched by this diff, still `done`. Collision check: WO-276 and WO-279 are file-disjoint from this WO's plan; WO-277's worktree is stacked on this branch's pre-fix commit `68a0219` (zero commits of its own yet) and has not picked up the fix commit `8452d07` — named as a heads-up for its own implementer to rebase before building further, not a conflict today and not this validator's to act on. `Regression:` line appended to `## TST-027`.

- 2026-09-04 finished — planner — provenance sentence corrected in place per the implementer's rule-4.2 flag: `## Provenance of this file plan` opened "This repository holds no `src/`", stale since W1. All five NEW paths re-checked and genuinely absent, `eslint.config.mjs` genuinely present, so the file plan itself stands unchanged — one sentence moved, no path, no row, no criterion.
## Validation report (appended by validator)

## TST-027 — WO-018 validation report

### Scope
WO-018 (`wo/WO-018-safe-fetch`, commit `68a0219`, worktree
`/root/projects/reachkitv3-wt/WO-018`, cut from `doctrine-0.11.0` tip `9786ef7`,
not merged), validated against its own `## Test plan`, `## Interfaces`,
`## Steps` and `## Out of scope`, against BP-006's `## Public interface` and
`## Error & edge behavior` verbatim, against constitution rule 4.2 (the
implementer's flagged deviations — the stale provenance sentence, the two
rule-1.1 parameters), against rule 2.3b (`rests-on` disposition), and — per
the dispatching instruction — with particular weight on whether the SSRF
guard's tests discriminate rather than merely pass. `git diff doctrine-0.11.0
68a0219 --stat`: exactly 6 files — `eslint.config.mjs` (MODIFY, +47/-4),
`src/lib/egress/{types,policy,safe-fetch}.ts` (NEW) and
`tests/egress/{policy,safe-fetch}.test.ts` (NEW) — matching the file plan
exactly (5 NEW, 1 MODIFY). Working tree was clean at commit and after every
mutation trial below (each mutation was applied, run, then reverted and
`git status --porcelain` confirmed empty before the next). No file outside
the file plan touched.

### Suite / build
- `npm test` (`vitest run --project node --project ui --project db`): 29
  files, 629 passed, 1 skipped (630) — matching the implementer's claimed
  count exactly, and matching the additive arithmetic against the 570/1/27
  baseline (59 new tests, all in the two new files).
- `npm run typecheck` (`tsc --noEmit`): clean.
- `npm run lint` (`eslint .`, full repo): 8118 problems (8093 errors, 25
  warnings) — byte-identical to the implementer's claimed count and to
  TST-024/025/026's own baseline figure; `grep -c no-fetch-outside-egress`
  on the full lint output: 0 — the new rule fires nowhere in the existing
  tree, confirmed independently, not assumed from the total being
  unchanged.
- `npm run build` (`next build`): fails as first attempted with the same
  root cause TST-024/TST-025 already recorded — `env.ts` throws on missing
  bindings, this sandbox carries no `.env.local`. Reproduced the same fix
  independently: a throwaway `.env.local` with 17 schema-valid placeholder
  bindings, `npm run build` — compiles, typechecks, collects page data and
  prerenders cleanly (`ƒ /api/scan`, `ƒ Proxy (Middleware)`, `○
  /_not-found`, matching TST-025's own route summary — this diff adds no
  route, so the summary is unchanged). Deleted the file immediately after;
  `git status` clean before and after, never tracked. Re-run a second time
  for the record (see Suite/build under `### Regression` — not reached,
  verdict below is not `pass`; the two runs above are this validation's own
  reproduction, not a regression pass).

### Criterion-by-criterion (WO's own `## Test plan`, 8 rows)

| # | Acceptance criterion (source quoted) | Verdict | Reproduction / mutation |
|---|---|---|---|
| 1 | "Refuses by policy, before any connection: private, loopback, link-local, multicast and reserved address space; non-http(s) schemes; redirects that leave the policy (each hop re-checked); ports other than 80/443." | **PASS, with a finding** | All named classes independently reproduced refused, both at `checkAddress`/`classifyAddress` level and end-to-end through `safeFetch` (zero sockets opened, `blocked_by_policy`). Scheme/port cases reproduced. Redirect-hop mutation (below): wrapping the address check in `if (hop === 0)` fails the "second hop is private" test — each hop genuinely re-checked. **Finding**: the IPv4-mapped-IPv6 sub-case's dedicated test is vacuous — see `### Mutation & bypass probes` and `### Coverage gaps` below. |
| 2 | "DNS-pinned: the address resolved during the policy check is the address connected to, so a name cannot be re-resolved to a private address between check and connect." | PASS | Reproduced (public-then-private lookup mock, `lookupSpy` called once, `calls[0].host === PUBLIC_IP`). Mutation: passing `currentUrl.hostname` instead of `resolved.address` to `performRequest` fails the test on `expect(calls[0]?.host).toBe(PUBLIC_IP)` — `expected 'example.com' to be '93.184.216.34'` — a logic assertion, not an import error. |
| 3 | "**Never throws.** Every failure is a typed `FetchOutcome` with `ok: false`" | PASS | Reproduced for DNS failure, connection refusal, policy refusal, malformed URL. Mutation: removing the `try/catch` around `new URL(url)` turns the malformed-URL case into an unhandled `TypeError: Invalid URL` that the test catches as a rejected promise — `AssertionError: promise rejected ... instead of resolving`. |
| 4 | "Size cap 2 MB; a larger response is `too_large`, not a truncated parse." | PASS | Reproduced (3 MB streamed response → `too_large`, no `html`; caller `maxBytes: 50` also enforced). Mutation: neutering the `bytes > cap` branch (`if (false) { tooLarge = true }`) fails both size-cap tests, each with a clear `ok: true` vs `ok: false` mismatch. Read the code directly to confirm the truncated-parse path is structurally impossible, not merely untested: on `tooLarge`, `res.destroy()` and `settle({ kind: "too_large" })` return immediately, before `chunks.push(chunk)` or `Buffer.concat` ever run for that response — there is no code path that reaches `{ ok: true, html }` after the cap trips. |
| 5 | "p95 ≤ 3 s, hard timeout 8 s per fetch" (default 8000/hard max 15000) | PASS | Reproduced: a hung server with `timeoutMs: 25` yields `timeout`; default (no `timeoutMs`) schedules an 8000 ms timer; `timeoutMs: 999_999` schedules a 15000 ms timer, not 999_999. Mutation: `clampTimeout` returning `v` unclamped fails the clamp test — `expected [999999, 999994] to include 15000`. |
| 6 | "A `fetch(` call anywhere else under `src/lib/` that is not BP-008's vendor client is a lint error." | PASS | Reproduced via the suite's own `ESLint.lintText` cases (measure/ reports, egress/ and vendors/ don't) and independently: wrote a real file `src/lib/probe-fetch-lint-test.ts` calling `fetch(`, ran `npx eslint` on it — one error, `local/no-fetch-outside-egress`; deleted the file after (`git status` clean). |
| 7 | "host, outcome reason, status, bytes and duration; never a body" | PASS | Reproduced: successful and policy-refused fetches both log exactly `{host, reason, status, bytes, duration}`, `status: null` on refusal, response body text never present in the log line. Not separately mutated beyond reading `logFetch`'s five-argument signature — a field-set assertion (`Object.keys(record).sort()`) already fails on any added or removed key by construction; no probe is owed beyond the reproduction. |
| 8 | `RobotsPolicy.absent`, exact seven-field shape, no optional arm, no null policy | PASS, mechanism clarified | Field-for-field identical to BP-006's declaration on disk (byte-compared: `ok`, `origin`, `readAt`, `disallowsAll`, `disallowedAgents`, `sitemaps`, `absent` — seven fields; the WO's own 2026-08-31 log line calling this "six-field" is stale, the test-plan row itself correctly lists all seven). Mutation: widening `absent` to `boolean \| null` is caught by `npm run typecheck` (`tsc TS2344` on the `expectTypeOf` line) but **not** by `npx vitest run` — `expectTypeOf` from `vitest` is a runtime no-op outside `vitest --typecheck` mode, and the second ("real value's key set") test only checks a hand-written literal against a hand-written array, never the live type, so it also can't catch a shape change at runtime. This matches what the row's own wording promises ("fails to compile", not "fails a test run") and `npm run typecheck` is part of every gate this validation and every prior TST report in this corpus runs — not a gap against what was claimed, but worth stating plainly: `npm test` alone would not catch a regression here, only `npm run typecheck` would. |

### Mutation & bypass probes

**DNS pinning / TOCTOU (dispatch's top priority).** Reproduced the
implementer's report exactly: `dns.promises.lookup` mocked to return
`93.184.216.34` first and `10.1.2.3` second, `lookupSpy` called exactly
once, `calls[0].host === '93.184.216.34'`. Mutated `performRequest`'s
call site in `safe-fetch.ts` to pass `currentUrl.hostname` instead of
`resolved.address` — the test fails on `expect(calls[0]?.host).toBe(PUBLIC_IP)`
(`expected 'example.com' to be '93.184.216.34'`), a logic assertion, not an
import or type error. Reverted; `git diff --stat` empty. This is the single
most load-bearing assertion in the module and it discriminates correctly.

**Redirects re-check every hop.** Wrapped `checkAddress(resolved.address)`
in `if (hop === 0) { ... }` so only the first hop is policy-checked. The
"redirect chain whose second hop resolves to a private address" test fails
(times out reaching a scenario the mocked transport was never scripted for,
since the address check that should have refused it before a second
connection no longer runs) — confirms the per-hop re-check is real and
tested, not merely re-run silently. Reverted.

**Bypass probes beyond the suite:**
- `::ffff:127.0.0.1`, `::ffff:10.0.0.5` — refused (loopback/private).
  Confirmed via the suite and independently. See the finding below on how
  they're actually refused.
- Decimal (`2130706433`), octal (`017700000001`), hex (`0x7f000001`) and
  shorthand (`127.1`) IPv4 forms, and a leading-zero octet form
  (`0177.0.0.1`): none is recognised by `node:net.isIP` (returns `0`), so
  `resolveAddress` treats each as a hostname and calls
  `dns.promises.lookup`. Verified directly against this sandbox's real
  `dns.promises.lookup` (no mock): every one of these resolves to the
  canonical `127.0.0.1`, which then correctly fails `checkAddress` as
  `loopback`. Not a bypass — confirmed live via a throwaway test file
  (`tests/egress/probe-decimal.test.ts`, written, run, deleted; `git
  status` clean before and after) asserting `safeFetch` refuses all five
  forms with `blocked_by_policy` and never calls `http.request`.
- A trailing dot (`127.0.0.1.`): `dns.promises.lookup` returns `ENOTFOUND`
  for this in the sandbox's resolver — refused as `dns`, not a bypass
  (confirmed in the same throwaway file).
- IPv6 unique-local `fc00::/7`: covered directly in the suite
  (`fc00::1` → `private`), reproduced.
- A DNS name resolving to a private address — the whole reason the module
  resolves before checking: covered directly (`internal.example.com` →
  `10.0.0.5` → `blocked_by_policy`, no socket), reproduced.

**Finding — the IPv4-mapped-IPv6 "known bypass" test is vacuous.**
`policy.ts` builds each of its five address-class `BlockList`s by adding
both IPv4 and IPv6 subnets into the *same* `BlockList` instance. Verified
directly against `node:net.BlockList` (`npx tsx`, no application code):
`BlockList.check('::ffff:7f00:1', 'ipv6')` and
`BlockList.check('::ffff:127.0.0.1', 'ipv6')` both return `true` against a
list holding only `127.0.0.0/8` (ipv4) — `BlockList` already resolves an
IPv4-mapped IPv6 literal, in either compact-hex or dotted-decimal spelling,
against IPv4-family subnets in the same list, with no help from
`classifyAddress`'s own `IPV4_MAPPED_RE`-based unwrap-and-reclassify block.
Mutation: deleted that entire block (the `mapped`/`embeddedIpv4`/
`embeddedClass` lines) from `classifyAddress` — all 42 tests in
`tests/egress/policy.test.ts` still pass, including the test named
specifically for this ("an IPv4-mapped IPv6 literal is unwrapped and
reclassified — a known SSRF bypass") and the two `::ffff:*` cases named in
the class table. Reverted; `git diff --stat` empty. Constitution §8: "A
test that survives deletion of its feature is vacuous." **No exploit
exists** — the literals the dispatch named are correctly refused, by
`BlockList`'s own cross-family matching — but the code's comment
("IPv4-mapped IPv6 literals... are unwrapped and reclassified... a known
SSRF bypass... that a naive per-family check misses") misattributes the
protection to code that is, on every input this suite or this validation
constructed, dead. See `### Coverage gaps`.

### Robots-port seam — judgement
`robotsPort` defaults to `notWiredYet`, which returns `{ ok: false, reason:
"robots reader not wired" }`; `safeFetch`'s `if (robots.ok && isDisallowed(...))`
never fires on that arm, so the unwired default fails open — every fetch
proceeds as if robots.txt allowed everything, despite `respectRobots:
true` being the default. Judged correct for an unbuilt dependency: BP-006's
own text (quoted in `types.ts`'s header) is explicit that "could not
determine" (REQ-004 criterion 6) must never become a fabricated disallow —
fabricating a block on a document the module hasn't actually read would be
the wrong failure mode, not a safer one, and out-of-scope-by-design (WO-020
ships the reader). `__setRobotsPortForTesting` is exported (so any future
importer *could* call it) but is, today, called from nowhere except
`tests/egress/safe-fetch.test.ts` — confirmed by
`grep -rn "__setRobotsPortForTesting|safeFetch|resolvesInDns|readRobots|RobotsPort" src/ | grep -v egress/`
returning nothing: no production code imports any of this module's exports
yet. Not a defect; worth a note for whoever wires WO-020 that the default
degrades to "robots ignored," not "robots blocks," until that WO lands.

### ESLint rule real scope — judgement
`no-fetch-outside-egress` is a bare-identifier `CallExpression` rule scoped
by `files: ["src/lib/**/*.{ts,tsx}"], ignores: ["src/lib/egress/**",
"src/lib/vendors/**"]`. Confirmed it fires on a real, on-disk file
(`src/lib/probe-fetch-lint-test.ts`, `src/lib` but outside both exclusions)
and confirmed it stays silent on the same source under `src/lib/egress/`
and `src/lib/vendors/` (via the suite's own fixture-based cases). The
repo-wide byte-identical 8118-count and the independent
`grep -c no-fetch-outside-egress` = 0 together confirm it is not merely
narrow-by-construction-and-untested — it is scoped correctly and produces
zero false positives against the real tree.

### Environment-deviation audit
`node_modules` is a real `npm ci` install (not this worktree convention's
symlink) — confirmed `git check-ignore -v node_modules` still reports it
gitignored and `git status --porcelain` shows nothing for it. Every count
in this report (629/1/29 tests, clean typecheck, 8118/8118 lint,
byte-identical to baseline, clean build) was reproduced independently from
this same real install and matches the implementer's own reported figures
exactly — the deviation changed no result. Nothing from the `npm ci` or
from the throwaway `.env.local` used for `npm run build` is committed or
tracked (`git status --porcelain` empty before, during and after every
step of this validation).

### `rests-on` disposition (rule 2.3b)
**WO-018's own row** — front-matter reads `rests-on: []` at commit
`68a0219`, confirmed directly against the file on disk (not inferred):
nothing to disposition.

**Upstream — BP-006, the blueprint this order implements** — front-matter
also reads `rests-on: []`, confirmed directly against
`sdlc-factory/docs/blueprints/BP-006.md` on disk: nothing to disposition
there either. No other blueprint or decision was observed as newly
implemented by this build.

### Coverage gaps
One: the IPv4-mapped-IPv6 sub-case of criterion row 1 is exercised by a
test that does not discriminate the code it names (above) — the class of
defect constitution §8 calls a vacuous test, found in exactly the file
this validation was dispatched to scrutinize hardest. No other criterion
row, mutation probe or bypass probe produced a gap; every other named
class, encoding and failure mode reproduced and discriminated correctly.

### Finding answered — commit `8452d07`, re-validated 2026-09-04

New commit on `wo/WO-018-safe-fetch`: `8452d07`
(`fix(WO-018): remove dead IPv4-mapped-IPv6 unwrap in policy.ts (TST-027)`).
`git diff 68a0219 8452d07 --stat`: exactly 2 files — `src/lib/egress/policy.ts`
(26 lines changed) and `tests/egress/policy.test.ts` (10 lines changed).
`src/lib/egress/safe-fetch.ts` and `src/lib/egress/types.ts` are byte-identical
to `68a0219` (`git diff 68a0219 8452d07 -- src/lib/egress/safe-fetch.ts
src/lib/egress/types.ts` — empty). Every claim in the implementer's answer
audited directly, not accepted:

- **The dead block is gone.** `IPV4_MAPPED_RE`, the `mapped`/`embeddedIpv4`/
  `embeddedClass` unwrap-and-reclassify lines, and the header comment
  crediting them with closing "a known SSRF bypass" are deleted. Confirmed
  by reading the diff, not the commit message.
- **Both assertions kept.** `tests/egress/policy.test.ts`'s renamed test
  ("an IPv4-mapped IPv6 literal is still refused when its embedded IPv4
  address is blocked (TST-027)") still asserts `classifyAddress('::ffff:127.0.0.1')
  === 'loopback'` and `classifyAddress('::ffff:10.0.0.5') === 'private'`,
  verbatim the same two literals.
- **The rewritten test now discriminates — reproduced myself, not taken on
  the implementer's word.** Mutated `policy.ts` to split each class's mixed
  `BlockList` into two separate per-family `BlockList`s
  (`BLOCK_LISTS_V4`/`BLOCK_LISTS_V6`, `classifyAddress` selecting by
  `family`) — exactly the refactor the test's own new comment names as the
  thing it exists to catch. Result: `AssertionError: expected 'public' to
  be 'loopback'` on `classifyAddress('::ffff:127.0.0.1')` — a clean logic
  failure, not an import or type error. Reverted; `git diff --stat` empty
  after. This is the fix for TST-027's exact finding: the test now fails
  when the mechanism it names is actually removed.
- **`isBlockedAddress` removal confirmed safe.** `grep -rn
  "isBlockedAddress" .` across the whole repo (`src/`, `tests/`, config)
  outside `node_modules`: zero hits before counting the deleted
  declaration itself — it was never imported anywhere. Not in BP-006's
  `## Public interface` either. Full suite, typecheck, lint and build (all
  below) confirm removing the export breaks nothing.
- **Swept the rest of `policy.ts` myself, not just read the implementer's
  sweep.** Constructed one address from every distinct CIDR entry in
  `RANGES` (24 entries across 5 classes plus 2 public controls, one v4 and
  one v6 per class where present) and ran `classifyAddress` directly: every
  one returns its own class, none is masked by an earlier class in
  `CLASS_ORDER`'s check order — no overlapping range swallows another
  class's reachability. `checkSchemeAndPort`'s three branches
  (scheme/default-port/explicit-bad-port) and `classifyAddress`'s
  `family === 0` garbage-input branch were already covered by tests
  untouched by this commit, reproduced clean in the full run below.
- **Exported interface genuinely did not move — confirmed plainly, since
  WO-277 is already branched on this promise.** `git diff 68a0219 8452d07
  --stat` names only `policy.ts` and `policy.test.ts`; `safe-fetch.ts` and
  `types.ts` are byte-for-byte unchanged (empty diff, checked directly).
  `FetchOutcome`, `RobotsPolicy`, `safeFetch`, `SafeFetchOpts`, `RobotsPort`
  and `__setRobotsPortForTesting` are all untouched. **Confirmed: the
  interface WO-277 depends on has not moved.**
- **Counts.** `npm test`: 629 passed / 1 skipped / 29 files, matching the
  implementer's claim and the pre-fix baseline exactly (the fix renamed one
  test, added no test, removed none). `npm run typecheck`: clean. `npm run
  lint`: 8118 problems (8093 errors, 25 warnings), byte-identical.
  Reproduced independently in a quiet window — see the contention note
  under `### Regression` below for why several earlier runs in this same
  session showed spurious failures unrelated to this commit.

No further finding. TST-027's one defect is resolved: the record now
matches the code (the comment credits `BlockList`'s real, native mechanism)
and the test that names the mechanism fails when that mechanism is
actually removed, confirmed by reproducing the exact mutation named in the
test's own comment.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass
/ findings / reject."

### Verdict

pass

WO-018 ships exactly the 6 files its own `## File plan` names (5 NEW, 1
MODIFY across `68a0219` and `8452d07` combined; `git diff doctrine-0.11.0
8452d07 --stat` confirms, nothing outside the plan touched). `FetchOutcome`
and `RobotsPolicy` are transcribed byte-for-byte from BP-006 and, as of
`8452d07`, still untouched. The two mutation probes this validation was
specifically dispatched to run — DNS pinning/TOCTOU and per-hop redirect
re-check — both independently reproduced and both confirmed to fail on a
logic assertion (not an import or type error) when the guarding code is
broken. "Never throws," the size cap's `too_large`-not-truncated-parse
guarantee, and the timeout default/clamp all independently reproduced and
mutation-confirmed. The lint rule fires on a real injected `fetch(` call
and stays silent on the existing tree (byte-identical 8118-problem count).
The robots-port seam's fail-open default for an unbuilt dependency is
judged correct against BP-006's own stated semantics, and its test-only
setter is unreachable from any production code today. The `npm ci`-vs-symlink
environment deviation changed no result. Full suite (629/1 skip), typecheck,
lint and build all reproduced clean.

TST-027's finding is resolved: the dead IPv4-mapped-IPv6 unwrap block and
its misattributing comment are removed, `isBlockedAddress` (unused,
untested, not in BP-006's interface) is removed with it, and the test that
names the real mechanism (`BlockList`'s native cross-family matching) now
fails when that mechanism is removed — reproduced myself via the exact
per-family-`BlockList`-split mutation the test's own comment names,
`expected 'public' to be 'loopback'`. No new finding from the sweep of the
rest of the file (every `CLASS_ORDER` range reachable and distinguishing,
confirmed against one address per distinct CIDR entry, not sampled). The
exported interface (`FetchOutcome`, `RobotsPolicy`, `safeFetch`,
`SafeFetchOpts`, `RobotsPort`, `__setRobotsPortForTesting`) is confirmed
unchanged since `68a0219` — `safe-fetch.ts` and `types.ts` carry an empty
diff — which is the promise WO-277 is already branched on.

`rests-on` — this WO's own and BP-006's upstream row — both read `[]`,
confirmed directly, nothing to disposition. Not committed, not merged, no
status changed. Continuing to `/regress` in this same run, per dispatch.

### Regression

Match `## TST-024`, `## TST-025` and `## TST-026`'s shape and rigour.

**Branch currency.** `doctrine-0.11.0` was at `9786ef7` when this branch
was cut and is still at `9786ef7` — confirmed via `git fetch origin
doctrine-0.11.0` in the worktree, sha unchanged. `wo/WO-018-safe-fetch` is
2 commits ahead of `doctrine-0.11.0`, 0 behind
(`git rev-list --left-right --count HEAD...doctrine-0.11.0` → `2 0`). No
rebase needed. `tests/app/middleware.test.ts` (WO-003's default-deny edge,
live in front of every request): 16/16 passed, unaffected — this diff
touches no file under `src/app/**` or `src/middleware.ts`.

**Substrate contention — a real, observed condition this session, not a
WO-018 defect.** Three other implementers are active on this shared
sandbox right now (WO-276 in `src/lib/costs/` + `supabase/migrations/`,
WO-279 in `src/lib/presentation/generated/` + `tests/presentation/copy/`,
WO-277 stacked on this branch) and share one live Postgres instance
(`reachkit_scratch`) and one machine's CPU with this validation. During
this regression window, `npm test` runs while WO-276's or WO-279's own
`vitest run --project ... --project db` was concurrently resetting and
reapplying the same schema (`ps aux` caught it live: WO-276's `psql -f
.../00000000000001_baseline.sql` and WO-279's own full-suite run, each
from their own worktree, hitting the same `127.0.0.1:5432` database)
produced transient failures — up to 17 of 630 — entirely inside
`tests/db/**`, `tests/scan/free/**` and one CPU-timeout in
`tests/app/lint-rules.test.ts` (a real `ESLint.lintText` call exceeding a
5000ms test timeout under load). None of these ever touched
`tests/egress/**`; isolating `tests/egress/` alone during the same
contention window intermittently reproduced two purely load-sensitive
flakes of its own (the same 5000ms `ESLint.lintText` timeout in
`policy.test.ts`'s lint-rule test, and a `setTimeout`-argument assertion
off by single-digit milliseconds under real CPU pressure) — both
resource-contention artefacts of a real `ESLint` instance and real
wall-clock scheduling, not logic defects; deterministic and clean (59/59)
in every uncontended run. Polled for a quiet window (`ps aux` showing no
`vitest`/`psql -h` process from any worktree), then captured the counts
below in that window — the same discipline TST-025 used for its own
db-project flake, not waved through.

**Suite / build (quiet-window run, reproduced against this validation's
own TST-027 figures above).**
- `npm test`: 629 passed / 1 skipped (630), 29 files — matches this
  report's own re-validation figure exactly.
- `npm run typecheck`: clean.
- `npm run lint`: 8118 problems (8093 errors, 25 warnings), byte-identical.
  `no-fetch-outside-egress` re-confirmed firing: injected a real `fetch(`
  call into a throwaway `src/lib/probe-fetch-lint-test2.ts`, ran `npx
  eslint` on it — one error, `local/no-fetch-outside-egress`; deleted
  after (`git status` clean). Repo-wide `npm run lint` and `npm test` both
  ran clean with this rule live — the eslint.config.mjs edit breaks no
  existing package script and no other suite.
- `npm run build`: clean via the same throwaway, deleted, never-tracked
  `.env.local` precedent TST-024/025/026 set (`ƒ /api/scan`, `ƒ Proxy
  (Middleware)`, `○ /_not-found` — unchanged route summary, this diff adds
  no route).

**`factory-console impact WO-018`, run from the worktree** (not the repo
root, per WO-070's regression finding): 72 files → 2 blueprints (BP-001, 1
file — `eslint.config.mjs`; BP-006, 5 files — the egress module and its
tests) → 0 importers → 0 requirements. Confirmed directly, not inferred
from the tool's silence: both `BP-001` and `BP-006` carry `satisfies: []`
on their own front-matter (checked on disk), so 0 requirements is correct
per rule 5.4 (`covers` is not gate-bearing) — **stated explicitly, per
rule 5.5: no requirement's acceptance criteria are in this regression's
re-check scope, because no requirement is this diff's gate-bearing
ancestor.** The 72 files include work-order docs across the corpus that
cite BP-006 (e.g. WO-019, WO-020, and the newly-cut WO-276/277/278/279/280/281/282)
and the registry's own generated projections — none of them is a `src/`
or `tests/` file this diff touches; `git diff 9786ef7 8452d07 --name-only`
confirms exactly the same 6 files as the original commit's file plan (2 of
them, `policy.ts`/`policy.test.ts`, carrying the fix).

**Earlier `done` work orders.** All of W1 and W2 — 21 work orders
(`WO-001, WO-002, WO-003, WO-005, WO-006, WO-029, WO-030, WO-041, WO-051,
WO-056, WO-057, WO-058, WO-062, WO-070, WO-267, WO-268, WO-269, WO-270,
WO-272, WO-274, WO-275`) — re-checked directly: all still `status: done`,
none named in `git diff 9786ef7 8452d07 --name-only`, so none is touched by
this diff. Only `eslint.config.mjs` is shared with any of them (repo-wide
config); the full suite and full lint both ran clean against every one of
their own files in the quiet-window run above.

**Parallel-branch collision check — worth flagging.** WO-276
(`src/lib/costs/**`, `supabase/migrations/20260903080000_fetches.sql`) and
WO-279 (`src/lib/presentation/generated/**`, `tests/presentation/copy/**`)
are file-disjoint from WO-018's own plan — no merge collision. **WO-277's
worktree (`/root/projects/reachkitv3-wt/WO-277`) is stacked on
`68a0219`, WO-018's *pre-fix* commit, not `8452d07`** — `git log --oneline
-1` in that worktree shows `HEAD` still at `68a0219`, with zero commits of
its own yet (`git diff 68a0219 HEAD --stat` there is empty). Not a
conflict today — WO-277 hasn't added any file yet, and the interface it
depends on is confirmed unchanged above — but if WO-277's implementer
starts building on that worktree as it stands, they'll be building against
the version of `policy.ts` that still carries the now-fixed dead code and
the still-exported (but now-removed) `isBlockedAddress`. Worth a rebase of
WO-277's worktree onto `8452d07` before or during its own work — a
fast-forward with zero conflict potential today, since WO-277 has touched
nothing yet — flagged here for the planner/implementer, not acted on
unilaterally (outside this validator's scope to touch another WO's
worktree).

Regression: 6 files (`factory-console impact WO-018`, run from this
worktree: `eslint.config.mjs`, `src/lib/egress/policy.ts`,
`src/lib/egress/safe-fetch.ts`, `src/lib/egress/types.ts`,
`tests/egress/policy.test.ts`, `tests/egress/safe-fetch.test.ts` → BP-001
(1 file), BP-006 (5 files) → 0 importers → 0 requirements — none) · none
re-checked — pass. Both named blueprints carry `satisfies: []` (checked
directly), so no requirement's acceptance criteria are gate-bearing here;
`covers` lists on both are non-empty but not authorising ancestry (rule
5.4). Full suite/typecheck/lint/build all reproduced clean in a
substrate-quiet window; the session's own contention (three concurrent
implementers sharing one Postgres instance and one machine) produced
transient, non-reproducing failures strictly inside `tests/db/**`,
`tests/scan/free/**` and one CPU-bound lint timeout — none inside
`tests/egress/**` in any uncontended run, and none attributable to this
diff. Branch current with `doctrine-0.11.0` (`9786ef7`, unmoved, 2 ahead/0
behind, no rebase needed). WO-003's middleware default-deny re-confirmed
live (16/16). All 21 earlier `done` WOs re-checked, untouched, still
`done`. One collision risk named, not this diff's to fix: WO-277's
worktree has not yet picked up this branch's fix commit `8452d07` and
should rebase before building further.
