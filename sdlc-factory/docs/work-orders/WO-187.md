---
id: WO-187
type: work-order
title: "explainChoice: why this page exists, in handles and stored values"
status: approved
ui: no
implements: [BP-040, REQ-047]
depends-on: [WO-181, WO-186]
blocked-by: []
rests-on:
  - claim: "BP-019 renders the day-panel line from the handles this function returns; no word of it is written here (ADR-001). Discharged 2026-08-31: the owner ruled the three winnability words and BP-019's `BAND_LABELS.winnability` now maps `winnable -> band.winnability.winnable -> \"Winnable\"`, `reach -> band.winnability.reach -> \"Reach\"`, `not-yet -> band.winnability.notYet -> \"Not yet\"`. BP-040's third `rests-on` is `confirmed`. Nothing in this work order changes: it still returns the handle and no word."
    disposition: confirmed
supersedes: []
---

# WO-187 — explainChoice: why this page exists, in handles and stored values

- Branch: wo/WO-187-explain-choice
- Estimate: ≤1 day

> **Every path in the file plan is NEW.** There is no source code on disk — the
> repo holds `BUILD.md`, `DATA-COSTS.md` and this factory (`00-project.md`:
> "Greenfield. No source code on disk yet") — so no path below was verified
> against an existing file. Placement follows `sdlc-factory/docs/registry/structure.md`'s
> module map and this node's `code:` globs, and is provisional against a repo
> with no code.

> **Runner and type-check.** The test runner is unchosen (`00-project.md`:
> "Test runner is unnamed in the spec — `tests/pins.test.ts` implies one.
> System to choose at M1 and record as an ADR"). This WO adds test files at the
> paths named and chooses no runner. `factory.config.json` sets no `typeCheck`
> key, so the type-check command is derived from the project's own tooling —
> greenfield Next.js + TypeScript (`structure.md` header) — as `npx tsc
> --noEmit`; every compile-time assertion below is a failure of that command.

## Goal
Expose the stored reason one planned page exists — its type, family, band, acceptance test and evidence — as handles and stored values, with no sentence and no recomputation.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/opportunities/derive/explain.ts` | NEW | `explainChoice(opportunityId)` — one indexed read returning `{ type, family, fitBand, acceptance, evidence }` exactly as stored at creation. No `Measured` value is recomputed, no volume is refreshed, no band is re-derived, and no string is composed (REQ-093 c1, c4). |
| `tests/opportunities/derive/explain.test.ts` | NEW | Reads back a row created by WO-186's fixtures and asserts field-by-field equality with what was stored, including dates; a second scan that moves the numbers does not change what `explainChoice` returns for the earlier opportunity; the return carries no string that is not a stored value or a handle. |

## Interfaces
**Consumes** (verbatim from the blueprints named):
```ts
// WO-181 · src/lib/opportunities/types.ts
export type Acceptance =
  | { form: 'top20';       query: string }
  | { form: 'named_on';    question: string }
  | { form: 'gate_cleared'; gate: Barrier }
export type Winnability = 'winnable' | 'reach' | 'not-yet'   // internal handles
```

**Exposes** (verbatim from the blueprint):
```ts
/** What the day panel needs to say why a page exists (REQ-047 c7, c12).
 *  Handles and measured values only — no sentence, no word (REQ-093 c1, c4). */
export function explainChoice(opportunityId: string): Promise<{
  type: OpportunityType; family: Family
  fitBand: Winnability | null
  acceptance: Acceptance
  evidence: Evidence
}>
```

## Steps
1. Write `explain.test.ts` first and run it: it must fail on a missing module.
2. Implement `explain.ts` as a single read through `db()` under RLS.
3. Assert in the test that the returned band is the stored `fit_band` string, not a value computed from `bandWinnability` at read time — the two agree today and would diverge the first time a bar is retuned, which is the whole point of storing it.
4. Run the test, then mutate: re-derive the band on read and confirm the second-scan test fails.

## Test plan
> Watch it fail first (constitution §8): every test below is written and
> run against the absent implementation before the implementation exists.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-047 c7** Given a page in the calendar, when the customer inspects it, then that test is shown as part of why the page exists. | `explain.test.ts › the recorded acceptance test is returned with the reason the page exists` |
| **REQ-047 c12** Given the customer inspects a planned page in the day detail (REQ-043 criterion 8), when the reason it was chosen is shown, then it names that page's winnability band. | `explain.test.ts › the returned reason carries the stored winnability band handle` — one of exactly `winnable`, `reach`, `not-yet`, and asserted to be a member of `BAND_LABELS.winnability`'s key set so a handle this function invents has no word to render; the rendering that names it is BP-019's and BP-001's |

**Discriminates (mutation check, constitution §8).**
- Recomputing `fitBand` on read leaves every same-day test green and fails `… a second scan that moves the numbers does not change the earlier opportunity's explanation`.
- Returning a composed sentence instead of handles fails `… the return carries no string that is not a stored value or a handle`.

## Out of scope
- `src/lib/config/constants.ts` and `tests/pins.test.ts` — BP-005's alone
  (`structure.md` rules 4 and 5). This WO imports the pins it needs and
  hardcodes none of them; if a pin named in `rests-on` is absent when work
  starts, the implementer adds nothing to that file — it logs `failed` naming
  the missing pin and stops (constitution rule 4.2).
- Every customer-visible string. This WO emits handles only; the three
  winnability words were ruled by the owner on 2026-08-31 and live in BP-019's
  `BAND_LABELS.winnability` (ADR-001) — "Winnable", "Reach", "Not yet" — which
  this WO does not touch and does not import a string from.
- The day panel, the day's line and every word in it — BP-001's surface and BP-019's copy registry.
- Any read path that recomputes a measurement for display (REQ-093 c4 forbids it).

## Rollback
One read-only module. Revert the branch; no schema and no writes are involved.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-040 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 2 rows of `## Test plan` quote REQ-047 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended: the band-words `rests-on` is discharged against BP-019's ruled `BAND_LABELS.winnability` ("Winnable" / "Reach" / "Not yet"); this WO still emits handles only, and c12's test now pins the handle against that key set.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
