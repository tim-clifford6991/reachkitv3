---
id: WO-116
type: work-order
title: "users billing columns — the migration checkout writes into"
status: approved
ui: no
implements: [BP-030, REQ-022]
depends-on: [WO-011]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-116 — users billing columns — the migration checkout writes into

- Branch: wo/WO-116-users-billing-migration
- Estimate: ≤1 day

## Goal
Add the four additive `users` columns BP-030's data-model delta names, under the
`users_billing` migration sub-token, with the two uniqueness constraints the
provisioning replay key depends on.

## File plan
> **Every path below is NEW.** This repository carries no `src/` and no
> `supabase/` — nothing here was verified against an existing file. Placement is
> anchored to `registry/structure.md` (`supabase/**` · `src/lib/db/**` row, and
> rule 3a's sub-token list, which names `*_users_billing_*` inside BP-017's
> `*_users*`) and to BP-030's own `code:` globs. It is provisional against a repo
> with no code.

| File | New/Modify | Change |
|---|---|---|
| `supabase/migrations/<ts>_users_billing_columns.sql` | NEW | `alter table users add column stripe_customer_id text`, `billing_country text null`, `vat_number text null`, `checkout_session_id text null`. Add `unique (stripe_customer_id)` and `unique (checkout_session_id)` as named constraints (`users_stripe_customer_id_key`, `users_checkout_session_id_key`). No `not null` on any of the four: a `users` row exists before `recordCheckoutFacts` fills them. No default. No `on delete cascade` anywhere in this file (ADR-051 point 2). `<ts>` is the implementer's `YYYYMMDDHHMMSS` at creation; the filename carries exactly one sub-token (`users_billing`) — two is a lint failure under `structure.md` rule 3a. |
| `tests/account/checkout/schema.test.ts` | NEW | Asserts the applied schema: the four columns exist with the stated types and nullability; both unique constraints exist; `vat_number` carries no check constraint, no trigger and no normalisation default (REQ-022 c7's "whatever any registry would say" must not be defeated in the schema); no foreign key introduced by this file carries `on delete cascade`. |

## Interfaces
Consumes: `db()` · `dbAdmin()` (BP-002, verbatim: `export function db(): SupabaseClient<Database>` / `export function dbAdmin(): SupabaseClient<Database>`).

Exposes: no TypeScript surface. The columns are the substrate for BP-030's
`recordCheckoutFacts` (WO-119) and the replay key BP-032's provisioning conflicts
on (BP-030 data-model delta: "`checkout_session_id text unique` — the replay key
BP-032's provisioning conflicts on (BP-017 decision 2)").

## Steps
1. Write the migration file with the four `add column` statements and the two
   unique constraints, each named.
2. Apply it locally and regenerate BP-002's `Database` types (the generated types
   file is BP-002's; regenerating it is a side effect of applying a migration, not
   an edit this WO authors).
3. Write `tests/account/checkout/schema.test.ts` and watch it fail before the
   migration is applied (constitution §8, "watch it fail first").
4. Apply and re-run.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-022 c5 — "Given a founder at checkout, when they pay, then the country of the billing address they gave at checkout is recorded against their billing record, and no country they give refuses them the purchase." (storage half only; the write is WO-119 and the no-refusal half is WO-118) | `tests/account/checkout/schema.test.ts › users.billing_country exists, is text and is nullable` |
| REQ-022 c6 — "Given a business buyer at checkout, when they have a VAT number, then a field exists to enter it, and what they enter appears on their billing record and their invoices." (storage half only) | `tests/account/checkout/schema.test.ts › users.vat_number exists, is text, is nullable and carries no check, trigger or default` |
| REQ-022 c7 — "Given a buyer with no VAT number, when they leave that field empty, then the purchase completes unimpeded; and given a buyer who enters one, when they submit it, then it is recorded and the purchase completes whatever any registry would say about that number — no check against VIES or any other registry stands between them and their purchase." (schema half only: nothing in the schema can reject a value) | `tests/account/checkout/schema.test.ts › a null and an arbitrary non-null vat_number both insert` |

## Out of scope
- Writing any of the four columns — WO-119.
- `users` columns owned by other sub-tokens: `*_users_provisioning_*` (WO-124),
  `*_users_subscription_*` (WO-129), `*_users_identity_*` (WO-135),
  `*_users_erasure_*` (WO-140).
- The baseline `users` table, RLS policies and the `Database` type generator —
  BP-002's.

## Rollback
Drop the two constraints and the four columns in a follow-up migration. No data is
written by this WO, so a rollback before WO-119 lands loses nothing. After WO-119
has run in an environment, a rollback discards recorded billing countries and VAT
numbers — REQ-022's rationale ("a record that was never captured cannot be
reconstructed later") makes that a data-loss event, not a clean revert.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-030 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-022 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
