---
id: WO-078
type: work-order
title: "deriveRivals — the §6.6 partition, the score, and a total tie-break"
status: approved
ui: no
implements: [BP-026, REQ-008]
depends-on: [WO-006, WO-077]
blocked-by: []
rests-on:
  - claim: "`BATTERY.COMPETITORS_MAX` (5) is pinned in `src/lib/config/constants.ts` (BP-005), which BP-026's `## NFR budget` records as 'already declared there'. That file is BP-005's and is not touched here."
    disposition: open
supersedes: []
---

# WO-078 — deriveRivals — the §6.6 partition, the score, and a total tie-break

- Branch: wo/WO-078-derive-rivals
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/rivals/derive.ts` so the rival set is counted over the twelve
SERPs the scan already bought — at zero extra cost, identical at 0 and at 10,000 ranked
searches for the customer, and in a byte-identical order on every run.

## Context an implementer needs
No source code exists on disk; both paths below are NEW. **This module buys nothing.**
BUILD §6.6: "Zero extra cost". It must never call BP-008, and the test plan asserts the
absence of that import path. Cold start is the path, not a branch: the derivation counts
over the market's SERPs and never reads the customer's own rankings.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/rivals/derive.ts` | **NEW** | `RivalCandidate` and `deriveRivals`, exactly as BP-026's `## Public interface` states them. Counts top-ten appearances and AI citations per registrable domain across the supplied SERPs, strips the customer's own domain, partitions platform domains into `sources`, scores, sorts totally, and returns the top `BATTERY.COMPETITORS_MAX`. |
| `tests/market/rivals/derive.test.ts` | **NEW** | The tests named in the test plan. Pure; no network. |

## Interfaces
**Consumes**: `SerpResult` (BP-008), `registrableDomain` / `isPlatformDomain` /
`isOwnDomain` (WO-077, quoted there verbatim), `BATTERY.COMPETITORS_MAX` (BP-005).

**Exposes** — copied exactly from BP-026's `## Public interface`:

```ts
// src/lib/market/rivals/derive.ts — BUILD §6.6, zero extra cost
export interface RivalCandidate {
  domain: string
  top10Appearances: number
  aiCitations: number
  score: number                    // top10Appearances + 2 × aiCitations
}
export function deriveRivals(a: { serps: SerpResult[]; ownDomain: string }):
  { rivals: RivalCandidate[]; sources: string[] }   // rivals: top 5 by score; sources: platform hits
```

## Steps
1. For each SERP, walk the organic top ten and the `ai_overview` references, mapping every
   host through `registrableDomain`; drop nulls.
2. Drop every domain for which `isOwnDomain(domain, ownDomain)` — before scoring, so a
   customer's own hosted page never becomes their own rival.
3. Partition: `isPlatformDomain` ⇒ the domain accumulates into `sources` and **never** into
   `rivals`. `sources` is the de-duplicated list of platform domains hit, in first-seen order.
4. Count `top10Appearances` (once per SERP per domain, never twice for two results on one
   SERP) and `aiCitations` (once per SERP per domain).
5. `score = top10Appearances + 2 * aiCitations`.
6. Sort by the four-key total order BP-026 decision 3 fixes: `score` desc, then `aiCitations`
   desc, then `top10Appearances` desc, then `domain` ascending. Take the first
   `BATTERY.COMPETITORS_MAX`.
7. Emit the observability fields BP-026 names: candidate count before and after the platform
   partition, and rival count kept.
8. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-008) | Test |
|---|---|
| 5. "Given a market whose 12 searches yield fewer than five rival domains, when the card renders, then every rival found is shown beside the customer, and where none was found one written line says so — never a card holding the customer's bar alone with nothing to compare it against." — the derivation limb: fewer than five, and zero, are legal results. The card's framing state is WO-079's; the written line is BP-019's. | `tests/market/rivals/derive.test.ts › deriveRivals/fewer-than-five-is-legal` — SERPs yielding 2 non-platform, non-own domains return exactly 2 rivals; SERPs whose every result is the customer's own domain or a platform domain return `rivals: []` and a populated `sources`, with no throw. |
| 2. "Given a customer appearing in none of the 12, when the card renders, then 0 is shown against the same denominator as every rival — never an error, a blank card, or a ratio." — *contributing arm only*: the rival set is identical whether or not the customer appears anywhere. | `tests/market/rivals/derive.test.ts › deriveRivals/cold-start-is-the-path` — the same SERP fixture run twice, once with the customer present in every top ten and once with the customer absent from all of them, returns an identical `rivals` array. |
| 1. "Given a completed report, when the Google card renders, then it shows, for the customer and for each rival, in how many of the market's 12 biggest searches they appear in the top ten, each labelled with its name and its value rather than identified by colour alone." — *contributing arm only*: which domains are rivals at all. | `tests/market/rivals/derive.test.ts › deriveRivals/counts-once-per-serp` — a SERP listing one domain at positions 3 and 7 raises that domain's `top10Appearances` by 1, not 2; `score` equals `top10Appearances + 2 * aiCitations` for every returned candidate. |

Determinism, required by BP-026's `## NFR budget` ("identical `SerpResult[]` in,
byte-identical card and rival order out"):
`deriveRivals/total-order-under-ties` — six domains with an identical `score` return in the
order the four-key comparator dictates, and a shuffled input produces the identical output.

Zero cost, required by BUILD §6.6 and BP-026's `## NFR budget` ("**0¢.** Nothing here buys
anything"): `deriveRivals/buys-nothing` — `derive.ts` resolves no import into
`src/lib/vendors/`, `src/lib/costs/` or `src/lib/llm/`, and the signature takes no
`CostContext`.

Discrimination: dropping the `domain` tie-break, counting a domain twice on one SERP, or
letting a platform domain into `rivals`, must each fail a named test above.

## Out of scope
- The presence card, its denominator, its volumes and its framing — WO-079.
- Rival *sizing*: BP-052's, unreachable from this module by construction (WO-086, WO-087).
- Rendering `sources`: BP-026's `## Data model delta` says it is stored and not rendered in MVP.
- Writing `report.rivals` into `scans.report jsonb` — BP-012's.
- REQ-008 criteria 3, 4, 6, 7, 8, 9, 10.

## Rollback
Delete both files; `presence.ts` has not landed, so nothing consumes `RivalCandidate`.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-026 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-008 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-006); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
