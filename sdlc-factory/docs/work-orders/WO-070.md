---
id: WO-070
type: work-order
title: "The landing page — one text input, one submit, and nothing else"
status: approved
ui: yes
implements: [BP-022, REQ-001]
depends-on: [WO-002, WO-268, WO-041, WO-062]
blocked-by: []
rests-on:
  - claim: "BP-019's `copy()` holds the landing headline, the field label, the submit label and one written line per `DomainProblem`. All are the owner's strings and this WO renders keys; `BUILD.md` §3 quotes a headline ('See what AI tells buyers about your market — and write your way in.') but a quotation in the build specification is not a copy-registry value, and minting one here would be writing customer-visible copy this node has no right to (constitution §1)"
    disposition: confirmed
supersedes: []
wave: W2
---

# WO-070 — The landing page — one text input, one submit, and nothing else

- Branch: wo/WO-070-landing
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-070.html
- Signed-off: 2026-09-03 — v1 — https://claude.ai/code/artifact/99a116b7-5305-4aad-84dd-3cff31bb3704

## Goal
Serve `/` as one text input and one submit control, with no other input of any
kind, no gate before submitting, and a written line in place when the value is
not a well-formed public domain name.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md`; `src/app/(public)/page.tsx` is BP-022's inside
> BP-001's `src/app/(public)/**`. Provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/app/(public)/page.tsx` | NEW | The landing page: one `Input`, one `Btn`, and the copy keys around them. The form posts to `/api/scan`. With JavaScript it posts JSON and navigates to `location`; without it, it is a plain form post that follows WO-062's 303. On a 422 or a returned problem handle it re-renders in place with the value intact and one written line from that handle's key. Reads no session, sets no cookie, renders no gate. |
| `tests/app/scan-address/landing.test.tsx` | NEW | The one-control, no-gate, error-in-place and no-JavaScript suites named in the test plan. |

## Interfaces

Consumes: WO-062's `POST /api/scan` and `StartScanResponse`; WO-051's
`DomainProblem` (type only, to key the written line); BP-019's `copy()`;
BP-018's `Input` and `Btn`.

Exposes (verbatim from BP-022 `## Public interface`):

```
Routes
  GET  /                                    landing: one text input, one submit, nothing else
```

## Steps
1. Produce the preview artifact at the path in the display bullets above and hold
   in draft until it is signed off (rule 7.3).
2. Write the failing suites first.
3. Render exactly one text input and one submit control. Render no select, no
   toggle, no checkbox, no radio, no second text field, optional or required.
4. Wire the JavaScript path: post JSON, then navigate to `location` on `ok: true`;
   on `ok: false` re-render in place with the value intact and the problem's
   written line.
5. Wire the no-JavaScript path: the same form as a plain post; WO-062's 303 to
   the canonical address on success, and its 303 back here carrying the problem
   handle and the value on failure, which this page re-renders in place.
6. Read no session and set no cookie; render no sign-in, no payment and no email
   field anywhere on the page.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-001.md`; line breaks are
> normalised to fit the table and no word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-001 c1 — "Given a visitor on the landing page, when the page renders, then it presents exactly one text input and one submit control and no other input control of any kind — no market, competitor, keyword or country field, selector or toggle, optional or required — and no sign-in or payment is required before submitting." | `landing/controls · exactly one input and one submit, and nothing else` — enumerate every form control in the rendered tree: exactly one text input, exactly one submit, and zero of every other control type; asserted structurally over the element list rather than by looking for known names, so a new field cannot slip past. And `landing/controls · no gate before submitting` — the page reads no session and sets no cookie, and the rendered tree contains no sign-in link, no payment control and no email field; a source assertion that `page.tsx` imports no auth or account module. |
| REQ-001 c3 — "Given a visitor who submits a value that is not a well-formed public domain name — an empty value, an IP literal, or a hostname with no public suffix — when they submit, then one written line names what is wrong with the value and the visitor stays on the landing page with their input intact. A well-formed domain is accepted here whether or not it resolves; a domain that cannot be reached is a failed scan (REQ-003), not a rejected input." | `landing/error · the line names the problem and the value survives` — for each of the three named cases, the page re-renders at `/` with exactly one written line resolved from that `DomainProblem`'s key and with the submitted value still in the input; the three lines are asserted distinct. And `landing/error · a well-formed unresolvable domain is not rejected here` — submitting one navigates to its report address rather than re-rendering with a line. |
| REQ-001 c2 — "Given a visitor who enters a domain in any of its written forms — with or without a scheme, with or without `www`, with a trailing path, in any letter case — when they submit, then they arrive at the single public report address for that domain; no two of those forms produce different addresses or different stored reports." | `landing/submit · every written form lands on the one address` — submitting each written form of one domain navigates to the identical canonical address; asserted end to end through WO-062's handler. |
| REQ-001 c3, the no-JavaScript half — "… the visitor stays on the landing page with their input intact." | `landing/no-js · the promise holds without a client runtime` — with scripting disabled, a valid submission follows the 303 to the canonical address and an invalid one returns to `/` with the line and the value intact; asserted against the server-rendered markup only. |
| REQ-001 c10 — "Given a visitor for whom a scan started under criterion 9, when they wait through it and read the report, then nothing is asked of them at any point between the domain they gave and the report they read — no account, sign-in, email address, or payment, and no further field. The finished page they may trade an address for afterwards is REQ-010's." | `landing/no-ask · nothing is asked on the way in` — the landing render and the navigation that follows a valid submission are asserted to present no further field of any kind before the report address is reached. The rest of this criterion is discharged at the report address by WO-069. |
| REQ-093 c1 (BP-022: "This node contains no string literal a person reads") | `landing/copy · every sentence is a key` — a source assertion that `page.tsx` contains no string literal rendered as text, and that every visible string resolves through `copy()`. |

## Out of scope
- The parse and the start — WO-051 and WO-062.
- The report address and everything at it — WO-069.
- The landing headline's wording — the owner's; this WO renders a key.
- Any second surface at `/`: this WO adds one route file and no other.

## Open questions

- [ ] REVIEW(placement): REQ-099 (`status: approved`, exercised by JN-001's
      first step — "reads what it is and finds the one field ready to type
      in ... watches the demo ... reads down it why they should care, what
      it does for them, and what they do to start today") names this exact
      address, `GET /`, as carrying the whole pitch: a first screen with a
      tagline, a subline and a live product specimen, then three sections —
      why to care, what it does, how to start — all standing above the
      field. WO-070 renders none of that, and correctly so under its own
      scope (`## Out of scope` above: "The landing headline's wording — the
      owner's; this WO renders a key. Any second surface at `/`: this WO
      adds one route file and no other") — REQ-001 c1 is silent on it and
      this build is not wrong to have left it out. But no work order today
      plans to add it to this same file: a corpus-wide search finds REQ-099
      named by exactly one other work order, WO-269 (`status: done`,
      `ui: no`), which ships only the layout-law conformance suite REQ-099
      criterion 1 will be measured against — no content of its own.
      `registry/generated/orphans.md` confirms independently: "REQ-099
      (requirement, approved) — no blueprint's `satisfies` names it." Until
      a sibling WO is cut against REQ-099 — which itself still carries three
      open owner questions: the REQ-001 c1 control-count reading, the demo
      video's asset ownership, and the twelve owed copy strings — the page
      this WO ships to `done` is a bare form with no persuasion content
      above it: reachable exactly where JN-001 puts a stranger, but not yet
      doing what that step or REQ-099 says it should do there.

## Rollback
Delete both files. `/api/scan` keeps working and the report address is unaffected.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-022 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 6 rows of `## Test plan` quote REQ-001 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-031, WO-032, WO-041); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-031 → WO-268, WO-032 → WO-268
- 2026-09-02 finished — planner — depends-on edge added: WO-002 (the root layout `src/app/layout.tsx` every `src/app/**` page renders inside; the edge was missing)
- 2026-09-03 audited — librarian — rule 7.3 process deviation: preview drawn retroactively against code already built (§3 gate met out of order, not a precondition); owner has signed, deviation stands
- 2026-09-03 ruled — owner — preview signed off
- 2026-09-03 ruled — owner — landing copy approved: headline "See what AI tells buyers about your market — and write your way in.", field label "Your website", submit label "Scan my site"; implementer enters in copy registry per BUILD.md §3
- 2026-09-03 ruled — owner — design values stand at spec defaults: mono numerals, --r-box 14px, labelled specimen, bare score with band; no change
- 2026-09-03 audited — librarian — five owner-owed copy keys remain empty (landing.problem.empty, .not-a-hostname, .ip-literal, .no-public-suffix, .too-long per REQ-001 c3); WO-070 cannot reach done until filled
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — entered the owner's three ruled landing-copy strings (2026-09-03 ruling above, "landing copy approved") verbatim into `src/lib/presentation/copy/keys/report.ts` (`landing.headline`, `landing.field.label`, `landing.submit.label`); `OWNER_OWED` confirmed still derived (no hand-maintenance needed). Amended `tests/app/scan-address/landing.test.tsx`'s closing describe block to assert the five remaining `landing.problem.*` keys still throw naming themselves, plus the converse — the three filled keys return the owner's exact strings — and updated its header comment and describe name off the now-false "all eight" claim; `tests/presentation/copy/registry.test.ts`'s two count assertions updated (38→35 owner-owed, 13→16 filled; 51 total unchanged). Full suite: 555 passed, 1 skipped, 26 files (baseline 554/1/26 plus the one new test); typecheck clean; lint clean on every touched file (pre-existing, unrelated errors remain in vendor/migration/preview files outside this WO's plan). Finding (rule 4.2): the happy-path render now resolves all three real strings and is correct for a visitor; the error path — a visitor who submits a value REQ-001 c3 rejects — still calls `copy()` on one of the five still-empty `landing.problem.*` keys, which throws during render rather than rendering a blank line or the intended written line; a real visitor hitting that path today gets an unhandled render error, not REQ-001 c3's promise. This is the librarian's 2026-09-03 audit line above, confirmed against the real registry, not a new gap — WO-070 remains correctly not-`done` until the owner supplies the five `DomainProblem` lines. Committed `wo/WO-070-landing`, not merged.
- 2026-09-04 started — implementer
- 2026-09-04 finished — implementer — entered the owner's 2026-09-04 ruling on the five `landing.problem.*` strings (REQ-001 c3's per-`DomainProblem` written line) verbatim, byte for byte, into `src/lib/presentation/copy/keys/report.ts` (`landing.problem.empty`, `.not-a-hostname`, `.ip-literal`, `.no-public-suffix`, `.too-long`); source was a canonical JSON file, compared programmatically against the written TypeScript (not by eye) both immediately after writing and again against the test file's own transcription — all five matched byte-for-byte on every non-ASCII character (U+2019, U+2014). All eight landing keys are now supplied; none is owner-owed. `OWNER_OWED` confirmed still self-corrects: 30 owner-owed / 21 filled / 51 total, verified against the real registry, not assumed. Rewrote `tests/app/scan-address/landing.test.tsx`'s closing describe block (renamed, header comment updated) to assert all eight landing keys resolve through the real (unmocked) registry, return exactly the owner's strings (five embedded byte-exact and independently verified against the JSON source), that none throws, and that the five problem lines are non-empty and pairwise distinct (still discriminates against a drift, an emptied key, or a collision). Updated `tests/presentation/copy/registry.test.ts`'s two count assertions and comments (35→30 owner-owed, 16→21 filled; 51 total unchanged). Full suite: 554 passed, 1 skipped, 26 files (net -1 test vs. the 2026-09-03 baseline of 555/1/26: two `it`s that separately proved the three-filled/five-throwing split were merged into one `it` proving all eight resolve, since the split no longer exists — assertions carried forward, not dropped); typecheck clean; lint clean on all three touched files (`npx eslint` targeted), unrelated pre-existing errors remain in vendor/migration/preview files outside this WO's plan (unchanged from 2026-09-03's finding). Exercised the real `copy()` end to end against all five `DomainProblem` handles (`empty`, `not_a_hostname`, `ip_literal`, `no_public_suffix`, `too_long`) via `parseDomain()` outside the mocked test harness: all five now resolve to their sentence with no throw — the 2026-09-03 finding (unhandled render-time exception on the error path) is confirmed fixed for every handle. Committed `wo/WO-070-landing`, not merged.
- 2026-09-04 finished — validator — TST-026, verdict pass: all 6 test-plan rows independently reproduced and confirmed discriminating by mutation (second control, broken canonicalisation, emptied key, colliding keys — distinctness assertion isolated and confirmed to catch it on its own — and a raw string literal); all eight copy strings byte-exact against their canonical sources with correct punctuation throughout; real, unmocked error path confirmed fixed for all five `DomainProblem` handles; 555→554 count audited and is a faithful merge, nothing dropped; full suite/typecheck/isolated lint/build all reproduced clean; the three out-of-plan files are legitimate, correctly-flagged (rule 4.2); WO's own `rests-on` row dispositioned `confirmed`.
- 2026-09-04 finished — design-guardian — UI-fit pass appended to TST-026 (`### UI-fit (design-guardian)`): pass. No raw hex/px in `page.tsx`/`Input.tsx`/`Btn.tsx`, no unregistered component (`Input`, `Btn` only, both `components.md` §1 `approved`), one primary action per screen, no long/generated text (all eight strings are the owner's ruled copy). Checked the owner's real, longer strings against the components directly (not the sheet's shorter placeholders): no truncation/fixed-height/overflow-hidden anywhere in `Input.tsx`, plain block flow in `page.tsx`, so the longer headline and error line wrap in place and the submit control stays below the field at every width. Judged the owner's 2026-09-03 design ruling against this screen specifically: mono numerals, `--r-box` 14px, a labelled specimen and the bare score with band are each **not applicable** to this screen (no numeral, no card/panel, no specimen, no score render here) — said so rather than a blanket pass. `Input.tsx`'s new optional `name` prop: legitimate rule-1.1 widening, no `components.md` edit needed, concur with the sheet's and TST-026's own judgement. Filed one `REVIEW(placement)` line under a new `## Open questions` heading (none existed): REQ-099 (approved, exercised by JN-001's own first step) names this address as carrying "the whole pitch" above the field, and no work order yet plans to add that content to this file — corroborated independently by `registry/generated/orphans.md` ("REQ-099 … no blueprint's `satisfies` names it"). Did not write a `Placement:` line: per `commands/validate.md`/`agents/validator.md`/this agent's own duty 6, that line is the validator's, in the fixed form `Placement: clean` / `Placement: reviewed — <n> open questions`, counted from the `REVIEW(placement)` line just filed — flagged once (rule 4.2) rather than authored here in the wrong hand.
- 2026-09-04 finished — validator — wrote the `Placement:` line on `## TST-026` (`Placement: reviewed — 1 open questions`), counted directly from the one `REVIEW(placement)` line the design-guardian filed; independently re-verified its REQ-099/orphans.md finding rather than taking the count or the judgement on trust — confirmed accurate, not a WO-070 defect, entangled with an ambiguity already sitting with the owner, not resolved here.
- 2026-09-04 finished — validator — `/regress` on TST-026's passing verdict: branch was 21 commits behind `doctrine-0.11.0`, missing WO-003's `src/middleware.ts` merge — rebased onto `doctrine-0.11.0` (new tip `8082012`), one conflict per doc-touching commit, always in `WO-070.md`, resolved by taking `doctrine-0.11.0`'s own copy each time (already carrying this validator's own report); zero conflicts under `src/`/`tests/`. `npm test` 570/1/27 (reconciled exactly against TST-026's 554/1/26 plus WO-003's 16-test `middleware.test.ts`), typecheck clean, lint byte-identical (8118), build clean via the same throwaway-`.env.local` precedent; `factory-console impact WO-070` (run from the worktree) named REQ-001, REQ-002, REQ-093 — re-checked against code on disk: REQ-001 c1/2/3/9/10 and REQ-093 c1/c5 implemented and still pass, REQ-001 c4–8/11–18, all of REQ-002, and REQ-093 c2/c3/c4 have no implementing code anywhere in this repo (named, not left silent); all 20 named `done` WOs still `status: done`, untouched by this diff; middleware regression surface exercised live — built, started a real server, curl'd `GET /` (200, real owner copy, still 200 with an arbitrary cookie) and `POST /api/scan` (200 valid, correct problem handle invalid), control `GET /app` correctly 307; both `rests-on` dispositions (this WO's own, WO-062's row 1) re-confirmed unaffected. No findings. `Regression:` line appended to `## TST-026`.

## Validation report (appended by validator)

## TST-026

Independent reproduction against worktree `/root/projects/reachkitv3-wt/WO-070`,
branch `wo/WO-070-landing`, tip `0033ee4` (not merged). Nothing on `main`
touched; nothing committed by this validation.

### Criterion-by-criterion

| Test-plan row (REQ) | Test | Verdict | Mutation probe |
|---|---|---|---|
| REQ-001 c1 | `landing/controls · exactly one input and one submit, and nothing else`; `landing/controls · no gate before submitting` | PASS | Added a second control (`<input type="checkbox">`) to the rendered tree: 5 of 18 tests in this file failed, including both `## no-ask` and the error-render's own "still exactly one input" assertion — the structural enumeration is not a named-field lookup and cannot be dodged by a differently-named field. |
| REQ-001 c3 (error line + value survives) | `landing/error · the line names the problem and the value survives` (×3), `landing/error · a well-formed unresolvable domain is not rejected here` | PASS | Covered by the empty-key and collision mutations below (the key each case resolves through is the same `PROBLEM_COPY_KEY` map). |
| REQ-001 c2 | `landing/submit · every written form lands on the one address` (×6) | PASS | Broke canonicalisation in `src/lib/scan/domain.ts` (dropped the `www.`-stripping branch): 2 of 6 forms (`www.example.com`, `https://www.EXAMPLE.com/…`) now resolve to `/scan/www.example.com` instead of `/scan/example.com` — test fails immediately, reproducing exactly the collision REQ-001 c2 forbids. |
| REQ-001 c3, no-JS half | `landing/no-js · the promise holds without a client runtime`; `landing/no-js · the <form> itself is a plain post…` | PASS | Reproduced directly: `POST` with `application/x-www-form-urlencoded` gets a 303 to `/scan/example.com` on a valid value and back to `/?problem=ip_literal&value=203.0.113.5` on an invalid one; the page's own SSR render of those exact params reproduces the line and the intact value with no client runtime invoked anywhere in the test. |
| REQ-001 c10 | `landing/no-ask · nothing is asked on the way in` | PASS | Same second-control mutation above also fails this row (it asserts the same zero-control invariant); the source-scan half (`ok` branch statements) was read directly — `window.location.assign(body.location); return;` with no `setProblem`/`setValue` in between, confirmed by eye against `page.tsx`. |
| REQ-093 c1 | `landing/copy · every sentence is a key` | PASS | Inserted a raw `<p>Beta</p>` into the returned JSX: the JSX-text-node scan fails immediately (`expected ['Beta'] to deeply equal []`) — the assertion is scoped to the actual return block, not a whole-file heuristic, and still catches an addition anywhere in it. |

All six test-plan rows independently reproduced, not merely confirmed present. Every mutation above was applied to the actual source file (not the test), run, observed to fail the intended test(s), then reverted; `git diff --stat` confirmed a clean tree before moving to the next probe.

### The owner's copy, byte for byte

Compared `src/lib/presentation/copy/keys/report.ts` programmatically (not by
eye) against two sources: the five `landing.problem.*` strings against the
canonical JSON at
`/tmp/claude-0/-root-projects-reachkitv3/f061a005-8fdf-4a48-9ee6-3bb4bcd0ea61/scratchpad/wo070-owner-copy.json`,
and the three earlier strings (`landing.headline`, `landing.field.label`,
`landing.submit.label`) against the exact quotes in this WO's `## Log`
("landing copy approved" line, 2026-09-03). All eight matched byte-for-byte.
Scanned every non-ASCII code point across all eight value lines
(36–46 in `report.ts`): only U+2019 and U+2014 appear — no straight
apostrophe, no substituted hyphen, anywhere.

**Mutation** — emptied `landing.problem.empty` to `""`: the WO's own
`landing/copy · all eight landing keys resolve…` test failed immediately
(`realCopy` throws "…is owner-owed — no sentence has been written for it
yet", since an empty string re-enters `OWNER_OWED`'s derived set), and both
of `registry.test.ts`'s count assertions failed alongside it (21→20 filled,
30→31 owner-owed). Reverted; suite back to 554/1/26.

**Mutation** — collapsed `landing.problem.not-a-hostname` onto
`landing.problem.empty`'s exact string (two problem lines identical): the
same WO test failed, but at the per-key `.toBe(OWNER_PROBLEM_LINES[key])`
line before ever reaching the distinctness assertion beneath it — so, to
verify the distinctness assertion specifically (not just the exact-match
check sitting in front of it), I temporarily commented out that one
`.toBe()` line in the test (probe only; reverted immediately after) and
re-ran the same collision: `expect(new Set(resolvedLines).size).toBe(5)`
failed on its own (`expected 4 to be 5`). The pairwise-distinctness
assertion the implementer added does independently discriminate a
collision, not just ride along behind the stronger check. Both files
restored to a clean `git diff --stat` afterward.

### The error path against the real, unmocked `copy()`

The WO's own rendering suites mock `copy()` to `(key) => key`, so they
cannot see a render-time throw against the real registry. I wrote a
throwaway probe test (`tests/app/scan-address/__validator-probe.test.tsx`,
no `vi.mock` on `@/lib/presentation/copy`) that imports the real
`LandingPage` and renders it via `renderToStaticMarkup` for all five
`DomainProblem` handles, asserting no throw and inspecting the resolved
line. Result — no throw for any handle, and every line resolved to the
owner's exact sentence:

| Handle | Resolved line |
|---|---|
| `empty` | "Type your website's address first — for example, example.com." |
| `not_a_hostname` | "That doesn't look like a website address. Try the form example.com." |
| `ip_literal` | "That's a numeric address, not a website name. Type the name people visit, like example.com." |
| `no_public_suffix` | "That address is missing its ending — try example.com rather than example." |
| `too_long` | "That's longer than any website address can be — check for extra text pasted in." |

Confirms the implementer's claim: yesterday's unhandled render-time
exception (2026-09-03 Log entry) is fixed for every handle. The probe file
was deleted after use, never committed (`git status --short` clean).

### The 555→554 test-count audit

Diffed `4d741cb..0033ee4` on `tests/app/scan-address/landing.test.tsx`
directly. Confirmed: the prior commit's closing block held two `it`s —
one asserting the five (then-empty) `landing.problem.*` keys threw naming
themselves, one asserting the three filled keys' exact strings. The tip
commit replaces both with one `it` that (a) keeps the three exact-string
assertions verbatim, (b) replaces the now-inapplicable "throws, naming
itself" check with a stronger one for the five now-filled keys — `.not
.toThrow()` plus an exact `.toBe(OWNER_PROBLEM_LINES[key])` per key — since
the keys no longer throw, and (c) adds a non-empty + pairwise-distinct
check that existed in neither prior test. `grep -c '  it('` on both commits:
`landing.test.tsx` 10→9 (net −1, exactly the merge), `registry.test.ts`
15→15 (unchanged — its two touched `it`s were renamed/renumbered, not
added or removed). Net corpus-wide: −1, matching 555→554 exactly. No
assertion from either retired `it` was dropped; the "throws" check was
retired because the state it tested (owner-owed, empty) no longer exists,
which is correct, not a weakening.

### Counts

- `npm test`: 554 passed, 1 skipped, 26 files — reproduced independently,
  matches the implementer's own count.
- `npm run typecheck`: clean.
- `npm run lint` (full): 8118 problems (8093 errors, 25 warnings), all in
  vendor/migration/preview files outside this WO's plan — byte-identical to
  the count TST-024 (WO-062) recorded as the pre-existing baseline.
- `npx eslint` targeted at the five touched files (`page.tsx`, `Input.tsx`,
  `report.ts`, `landing.test.tsx`, `registry.test.ts`): clean, zero output.
- `npm run build`: fails first attempt with the same `env.ts` boot-fail
  documented at TST-024/TST-025 (no `.env.local` in this sandbox); built a
  throwaway `.env.local` with schema-valid placeholders for all 17 BP-005
  bindings, re-ran — clean (`ƒ /` registered as a dynamic route,
  `ƒ /api/scan`, `○ /_not-found`). Deleted the file immediately after;
  `git status --short` confirmed it was never tracked.

### File plan

Plan named two NEW files (`page.tsx`, `landing.test.tsx`); the branch also
touches `src/lib/presentation/copy/keys/report.ts`,
`src/ui/components/Input.tsx` and `tests/presentation/copy/registry.test.ts`.

- `report.ts` and `Input.tsx`: both explicitly flagged once, in the first
  commit's message and in-file (rule 4.2) — `Input.tsx` needed an optional,
  additive `name` prop for a plain-HTML form submission to carry the field
  at all (the registered component's contract didn't expose one), and
  `report.ts` needed the eight landing keys entered into the existing
  report partition rather than a thirteenth file (`registry.ts`'s own
  closed, twelve-partition list, verified: no new partition was created,
  `report.ts` is one of the twelve already imported by `registry.ts`).
  Both are legitimate parameter authority (rule 1.1), not scope creep, and
  correctly flagged once rather than re-argued across three commits.
- `registry.test.ts`: not separately flagged, but its only changes are the
  two hardcoded derived-count assertions (`OWNER_OWED.length`, filled
  count) that mechanically follow from adding keys to `report.ts` —
  necessitated maintenance of an existing test, not a new capability or an
  unflagged deviation in its own right.

Judgement: all three out-of-plan files are legitimately within WO-070's
scope; the plan's own gap (naming no MODIFY row for the copy registry this
WO's `rests-on` row already implied) was correctly caught and flagged
under rule 4.2, once, in the right place.

### `rests-on` disposition

Row 1 (front-matter): "BP-019's `copy()` holds the landing headline, the
field label, the submit label and one written line per `DomainProblem`.
All are the owner's strings and this WO renders keys… minting one here
would be writing customer-visible copy this node has no right to." —
**disposition: confirmed**. Observed directly: all eight landing keys are
now present in the registry, none owner-owed, each traced to an explicit
owner ruling (2026-09-03 for the three, 2026-09-04 for the five,
both quoted in this WO's `## Log`) and verified byte-exact against the
canonical sources above — this node minted no string of its own. Set in
the front-matter above.

Walked BP-022 (this WO's `implements` blueprint) for `open` rows this
build could observe: it carries three. Two (`domain_blocks` removal
address, `domain_blocks` row authorship) concern REQ-002 removal, which
WO-070's own `## Out of scope` explicitly excludes ("The report address
and everything at it — WO-069"; removal is neither page.tsx nor this
build) — left `open`, not this build's to disposition. The third is
already `undischargeable` (persisted by TST-024/WO-062) — untouched.
REQ-001 and REQ-093 both carry `rests-on: []` — nothing to disposition
there.

### Verdict

pass

Every test-plan criterion independently reproduced and confirmed
discriminating by mutation (structural enumeration, canonicalisation,
copy emptiness, copy collision — isolated specifically — and the
raw-string-literal probe, all five caught their intended failure and were
reverted clean). The owner's eight copy strings are byte-exact against
their canonical sources with correct punctuation throughout. The real,
unmocked error path is confirmed fixed for all five `DomainProblem`
handles, closing the 2026-09-03 finding. The 555→554 count is a faithful
merge with every prior assertion carried forward or correctly superseded,
not a silent drop. Full suite (554/1/26), typecheck, isolated lint and
build all reproduced clean. The three out-of-plan files are legitimate,
correctly-flagged parameter authority under rule 4.2. The WO's one
`rests-on` row is dispositioned `confirmed` above, clearing §3's
`open-assumption-on-done` gate for this work order (BP-022's own two still-
`open` rows are out of this build's scope and correctly left).

### UI-fit (design-guardian)

Dispatched per `commands/validate.md`'s UI-fit step, ahead of the verdict
above. Independent pass against `design/tokens.md` and `design/components.md`
on the same worktree/tip TST-026 reproduced
(`/root/projects/reachkitv3-wt/WO-070`, `wo/WO-070-landing`, `0033ee4`) —
reading `src/app/(public)/page.tsx`, `src/ui/components/Input.tsx` and
`src/ui/components/Btn.tsx` directly, not the retroactive preview sheet's own
(concurring) self-review.

**Tokens and components.** `page.tsx` declares no raw hex or px of its own —
no `style` or `className` on any element it returns; every visual choice is
delegated to `Input` and `Btn`, both `components.md` §1 rows, both
`status: approved` (signed at `WO-268.html`, that file's §5). `Input.tsx` and
`Btn.tsx` carry no raw hex or px either — only daisyUI class names (`input`,
`input-error`, `label`, `label-text`, `text-error`, `btn`, `btn-primary`) and
no bespoke CSS. No unregistered component renders anywhere in the tree. One
primary action per screen holds on every state (`Btn variant="primary"` is
the only button rendered, ever). No long generated text: every visible
string is a `copy()` call, confirmed directly in `page.tsx` and by that
file's own `landing/copy · every sentence is a key` test (independently
reproduced above) — the eight strings are the owner's own ruled copy, not
generated filler, so rule 7.3's "long generated text" clause is not in play.

**The owner's real strings, in place of the sheet's placeholders.** The
signed sheet (v1,
https://claude.ai/code/artifact/99a116b7-5305-4aad-84dd-3cff31bb3704) draws
every string as a bracketed key — shorter than the real text now ruled, the
longest being `landing.problem.ip-literal`: "That's a numeric address, not a
website name. Type the name people visit, like example.com." Judged against
the components rather than the sheet: `Input.tsx`'s error line is a plain
`<p className="text-error">{p.invalidMessage}</p>` with no
`white-space: nowrap`, no fixed height and no `overflow: hidden` anywhere in
the component — `tokens.md` §7's buffer rule ("a box never hides its own
overflow", "a required prop of unknown length is never given a fixed
height") holds by construction, not by luck. `page.tsx` sets no width, no
flex-row and no grid around `Input`/`Btn`; the two sit in plain block flow
inside one `<form>`, so the longer real headline and error line wrap in
normal document flow and the submit control stays below the field at every
width, exactly where the sheet drew it — nothing in the built page depends
on the sheet's shorter placeholder lengths.

**The owner's 2026-09-03 design ruling, against this screen specifically —
not a blanket pass.** Four values stand at spec defaults: mono numerals,
`--r-box` 14px, a labelled specimen, the bare score with band.
- Mono numerals — **not applicable.** This screen renders no numeral, date,
  URL or code-like string anywhere: one field, one button, one headline, one
  error sentence.
- `--r-box` 14px — **not applicable.** `--r-box` is named for "cards,
  panels, the day panel" (`tokens.md` §2); this screen renders no `Card` and
  no panel. `Input`/`Btn` take `--r-field` (9px), a token the ruling did not
  name.
- A labelled specimen — **not applicable.** The specimen question
  (`tokens.md` §9.4) belongs to the idiom's sign-in/overview screens; this
  page renders no product illustration and no specimen of any kind — that
  content is REQ-099's, not this WO's (see `## Open questions` below).
- The bare score with band — **not applicable**, same reason: no score
  renders on this screen.

**`Input.tsx`'s `name` prop, judged independently.** Optional, no default
existed before it, changes no visual state, adds no state to the registered
`default · invalid · disabled` set, and exists only so a native,
no-JavaScript form post can carry the field. A legitimate, backward-
compatible widening under rule 1.1 (internal, reversible, changes nothing
the product promises) — not a second, unregistered input, and not a change
the `components.md` row needs to state, since its data contract ("Placeholder
and label required, never defaulted") and its three named states are both
unchanged by an attribute every existing caller keeps omitting. Concur with
the sheet's own self-review and TST-026's file-plan judgement above.

**Verdict: UI-fit passes.** No raw value where a token exists, no
unregistered component, no long generated or invented text, one primary
action per screen, and the registered `Input`/`Btn` states exhibited
correctly with the owner's real, longer strings in place. One
`REVIEW(placement)` finding is filed below, under `## Open questions`
(added — none existed) — a placement recommendation, not a defect, and it
does not change this verdict.

Note on this pass's own scope: the dispatch that requested this review asked
this agent to also write a `Placement:` line stating where this surface sits
in the product. That line's format and authorship are fixed elsewhere in
this corpus — `commands/validate.md`, `agents/validator.md` and this agent's
own definition (`agents/design-guardian.md` duty 6) all agree the
`Placement:` line is the validator's own, written as `Placement: clean` or
`Placement: reviewed — <n> open questions`, counted from the
`REVIEW(placement)` lines this pass leaves under `## Open questions` — never
authored by this pass itself, and never a prose sentence naming a route or a
root layout. Flagged once, per rule 4.2, rather than writing a line in the
wrong hand: the validator dispatched next should write it, reading one
`REVIEW(placement)` line below.

### Placement (validator)

Counted directly: `## Open questions` carries exactly one
`- [ ] REVIEW(placement): …` line (the REQ-099 finding above) — no others
exist anywhere in this file.

Placement: reviewed — 1 open questions

Judgement on the finding itself, independently re-checked (not taken on the
design-guardian's count): `requirements/REQ-099.md` is `status: approved`
with `blocked-by: [REQ-001]` (a live block, matching the librarian's own
reading, not stale bookkeeping), and its own `## Open questions` carries
exactly three unresolved rows — the REQ-001 c1 scan-form-vs-whole-page
reading, the demo video's asset ownership, and the twelve owed copy strings
— confirmed by reading the requirement directly, not restated from the
design-guardian's report. `registry/generated/orphans.md` independently
confirms "REQ-099 (requirement, approved) — no blueprint's `satisfies`
names it," and a corpus-wide search finds REQ-099 named by exactly one
other work order, WO-269 (`status: done`, `ui: no`), which ships only the
viewport-height suite parameter for REQ-099 criterion 1 and no pitch
content. This is not a defect in WO-070's own build: `## Out of scope`
above already excludes the pitch content and any second surface at `/`,
REQ-001 c1 says nothing about it, and every one of this WO's own test-plan
criteria still passes as built. It is a real gap in the corpus's coverage
of REQ-099 — no work order is cut against it yet — and it is entangled with
an ambiguity already sitting with the owner (whether REQ-001 c1 bounds the
scan form or the whole page), which this validator does not resolve here:
on the whole-page reading REQ-099 and REQ-001 contradict outright and this
page could not simply grow the pitch onto its current shape; on the
bounded reading a sibling work order adds it once REQ-099's own three
owner questions are ruled. Recorded here so the situation is accurate for
whoever reads WO-070 next and for whoever cuts REQ-099's sibling work
order; it does not change the `pass` verdict above.

**Branch currency and rebase.** `wo/WO-070-landing`'s merge-base with
`doctrine-0.11.0` was `06666c1`, well behind the integration branch's
current tip `46fb909` — 21 commits back, most consequentially missing
WO-003's `src/middleware.ts` merge (`ca43be3`/`70b58f3`, a default-deny
edge in front of every request) and the librarian's own `done` audit of
WO-003 and WO-062 (`fe01a1b`). Reachability through that middleware is
exactly this WO's own regression surface, so a check against the
pre-rebase tree would have been meaningless — verified no middleware
existed there at all (`find src -name middleware.ts`, empty) before
rebasing. Rebased `wo/WO-070-landing` onto `doctrine-0.11.0`
(`git rebase doctrine-0.11.0`): one conflict per doc-touching commit (3 of
4), always and only in `sdlc-factory/docs/work-orders/WO-070.md` — resolved
by taking `doctrine-0.11.0`'s own copy each time (`git checkout --ours`),
since that file's worktree copy is documented stale and the real record is
maintained directly on `doctrine-0.11.0`, which already carried this
validator's own TST-026 report, `rests-on` disposition and `Placement:`
line written above. Zero conflicts anywhere under `src/` or `tests/`.
New tip `8082012`; `git merge-base wo/WO-070-landing doctrine-0.11.0`
now equals `doctrine-0.11.0`'s own tip exactly — branch is current, no
drift remaining.

**Full suite, typecheck, lint, build — re-run against the rebased tip.**
`npm test`: 570 passed, 1 skipped, 27 files — a real, reconciled delta
against TST-026's own 554/1/26, not a discrepancy: the rebase merged in
exactly one new test file, `tests/app/middleware.test.ts` (WO-003, 16
tests, independently confirmed by running it in isolation), and
554 + 16 = 570, 26 + 1 = 27, exactly. `npm run typecheck`: clean.
`npm run lint` (full): 8118 problems (8093 errors, 25 warnings),
byte-identical to TST-026's own count, all pre-existing outside this WO's
plan; `npx eslint` on the same five touched files: clean, zero output,
unchanged. `npm run build`: clean via the same throwaway `.env.local`
workaround documented in TST-024/TST-025/TST-026 (17 schema-valid
placeholder bindings), now additionally emitting `ƒ Proxy (Middleware)`
alongside `ƒ /`, `ƒ /api/scan` and `○ /_not-found` — the one-time
`middleware.ts`→`proxy.ts` deprecation warning is WO-003's own already-
flagged, already-accepted rule-4.2 deviation, not a new finding here.
Fixture file deleted immediately after; `git status --short` clean before
and after.

**`factory-console impact WO-070`**, run from this worktree (the tool
requires it — run from the repo root instead, it silently returns
WO-062's own file set, checked and corrected): 17 files
(`sdlc-factory/docs/blueprints/BP-022.md`, `sdlc-factory/docs/design/
previews/WO-070.html`, `sdlc-factory/docs/registry/waves.md`,
`sdlc-factory/docs/work-orders/WO-041.md`, `WO-051.md`, `WO-056.md`,
`WO-057.md`, `WO-058.md`, `WO-062.md`, `WO-070.md`, `src/app/(public)/
page.tsx`, `src/app/api/scan/route.ts`, `src/lib/presentation/copy/keys/
report.ts`, `src/ui/components/Input.tsx`, `tests/app/scan-address/
api-scan.test.ts`, `tests/app/scan-address/landing.test.tsx`, `tests/
presentation/copy/registry.test.ts`) → 5 blueprints (BP-001 4 files,
BP-018 3, BP-019 3, BP-020 3, BP-022 4 files) → 3 importers (`src/lib/
presentation/copy/registry.ts`, `src/ui/components/index.ts`, `tests/ui/
components-2.test.tsx`) → 3 requirements (REQ-001, REQ-002, REQ-093).

**Re-checked against the code on disk, not restated from TST-026's own
table.** REQ-001's 18 criteria: 1, 2, 3, 9 and 10 are the ones WO-070's
and WO-062's own file plans cover and remain implemented exactly as
TST-026 found them — re-run above, still green. Criteria 4–8 and 11–18
have **no implementing code anywhere in this repo**: `find src/app -type
f` shows only the layout shells, this WO's own landing page and
`api/scan/route.ts` — no `/scan/[domain]` page exists to serve the report
address those criteria govern, consistent with WO-069/WO-061 still
`status: approved`, not a silent gap. REQ-002's 4 criteria (removal): **no
implementing code** — no removal address, no removal handler, and no
`domain_blocks` writer anywhere in `src/app`; `src/lib/scan/admission.ts`
does read the `domain_blocks` table, but only as a downstream *consumer*
gating free-path admission (REQ-001 c9's "not removed" check), not an
implementation of REQ-002's own criteria, which remain entirely unbuilt.
REQ-093's 5 criteria: c1 (this WO, re-confirmed by the raw-string-literal
mutation above) and c5 ("the registry renders with every model
unavailable," `tests/presentation/copy/registry.test.ts`, independently
re-run in this pass's own full-suite run, still passing) are implemented.
c2 (a proposed page's title/slug), c3 (the twelve tracked questions) and
c4 (the product explaining a number/verdict/reason) have **no
implementing code**: no draft/proposed-page renderer, no tracked-question
surface and no `explain()` module exist anywhere under `src/` yet.

**The 20 named `done` work orders** (WO-001, WO-002, WO-003, WO-005,
WO-006, WO-029, WO-030, WO-041, WO-051, WO-056, WO-057, WO-058, WO-062,
WO-267, WO-268, WO-269, WO-270, WO-272, WO-274, WO-275 — WO-003 and
WO-062 both reached `done` yesterday, after this branch was first cut, and
are correctly on this list now) were re-checked for status and gate
integrity, not just re-listed: all 20 still carry `status: done`.
`git diff --stat $(git merge-base doctrine-0.11.0 8082012) 8082012 --
.` (this branch's own 4 commits, post-rebase, against the rebased base)
touches exactly 6 files — the preview HTML plus the 5 code/test files
already named in `## File plan`'s own audit above — nothing under any of
those 20 WOs' own paths.

**Regression surface — middleware in front of this WO's own two routes,
exercised live, not read from the allow-list.** `PUBLIC_PATHS` in
`src/middleware.ts` lists both `"/"` and `"/api/scan"` verbatim
(confirmed by reading the file directly), but the proof is the live
round-trip, per WO-003's own regression precedent: built the app (same
fixture bindings), ran it with `next start` on a scratch port, then
`curl`'d directly. `GET /` → `200`, real HTML, the owner's real copy
present verbatim in the response body ("See what AI tells buyers about
your market — and write your way in.", "Your website", "Scan my site") —
and still `200`, not redirected, when an arbitrary session-shaped cookie
is attached, confirming the page is genuinely public, not merely untested
against a cookie. `POST /api/scan` with a valid domain → `200
{"ok":true,"location":"/scan/example.com"}`; with an IP literal → `200
{"ok":false,"problem":"ip_literal"}` — both the shapes this WO's and
WO-062's own test plans assert, reached through the real middleware in
front of a real server. Control case, `GET /app` (session-gated, matching
no `PUBLIC_PATHS` entry): `307` to the sign-in address, confirming the
middleware still denies where it should and this is not a blanket
pass-through. Server killed and the fixture `.env.local` deleted
immediately after; `git status --short` clean.

**`rests-on` re-confirmation.** WO-070's own row, dispositioned `confirmed`
by this pass above: re-checked against the rebased tree — unaffected;
`report.ts` carries no diff on `doctrine-0.11.0`'s side of the rebase and
still resolves all eight strings byte-exact (re-run as part of the full
suite above). WO-062's row 1, dispositioned `confirmed` by TST-025
(`WO-062.md` line 12, read directly): still `confirmed` on disk, reasoning
unchanged and unaffected by this diff.

No findings.

Regression: 17 files (`factory-console impact WO-070`, run from this
worktree: `sdlc-factory/docs/blueprints/BP-022.md`,
`sdlc-factory/docs/design/previews/WO-070.html`,
`sdlc-factory/docs/registry/waves.md`, `sdlc-factory/docs/work-orders/
WO-041.md`, `WO-051.md`, `WO-056.md`, `WO-057.md`, `WO-058.md`,
`WO-062.md`, `WO-070.md`, `src/app/(public)/page.tsx`,
`src/app/api/scan/route.ts`, `src/lib/presentation/copy/keys/report.ts`,
`src/ui/components/Input.tsx`, `tests/app/scan-address/api-scan.test.ts`,
`tests/app/scan-address/landing.test.tsx`, `tests/presentation/copy/
registry.test.ts` → BP-001 (4 files), BP-018 (3 files), BP-019 (3 files),
BP-020 (3 files), BP-022 (4 files) → 3 importers → REQ-001, REQ-002,
REQ-093) · REQ-001, REQ-002, REQ-093 re-checked — pass.
