---
id: WO-270
type: work-order
title: "The repository's top-level set is asserted against structure.md rule 7 as amended — `.claude/` admitted"
status: done
ui: no
implements: [BP-001]
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "`structure.md` rule 7 opens with \"A new top-level directory requires an ADR\" and, as amended by the librarian's backward pass (commit 1fdab38), admits `.claude/` by a registry edit rather than by an ADR. This order asserts the set exactly as the registry states it and takes that amendment as the authority on the set — the test's job is to agree with `structure.md`, not to rule on how `structure.md` was changed. If an ADR later records the admission, nothing here moves; if the admission is reversed, reversal is one entry removed from `allowed` and the comment above it."
    disposition: confirmed
supersedes: []
---

# WO-270 — The repository's top-level set is asserted against structure.md rule 7 as amended — `.claude/` admitted

- Branch: wo/WO-270-top-level-set
- Estimate: ≤1 day (well under; one test file, one set and its comment)

## Goal
Bring `tests/app/toolchain.test.ts`'s top-level-directory assertion back into
agreement with `structure.md` rule 7 as amended on 2026-09-02, so that the
vendored `.claude/` directory committed under 61f1fd0 is admitted as a member of
the fixed set — not as never-committed tooling — and any *other* new top-level
directory still fails the test.

## Provenance and the fail-first observation
`tests/app/toolchain.test.ts` exists (WO-001, `status: done`) and is the one
place the top-level set is asserted; this order extends it and creates no
second assertion (rule 7.2). Its `allowed` set names the five directories rule
7 fixed before the amendment; its `toolingOnly` set names only directories that
are never committed (`node_modules`, `.next`, `coverage`, `.git`), which is the
wrong set for `.claude/` — that directory is committed and is a member of rule
7's set by the registry's own words.

**The test is already failing, and that is this order's fail-first
observation.** Run on `doctrine-0.11.0` on 2026-09-03:
`› "no top-level directory outside structure.md rule 7's set is present"` —
`expected [] received [".claude"]`, 1 failed / 10 passed. The implementer does
not need to construct a failing state before editing; the repository is in it.
After the edit, the discriminating check is the reverse: remove `.claude` from
`allowed` again and the same case must fail again with the same received value.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `tests/app/toolchain.test.ts` | MODIFY | In the case `"no top-level directory outside structure.md rule 7's set is present"`: add `.claude` to the `allowed` set (not to `toolingOnly` — it is committed, and rule 7 names it as a member of the set); replace the comment's quotation of rule 7 with the amended sentence quoted verbatim in `## Test plan` below, so the comment and the registry say the same thing. Nothing else in the file changes. |

No exported type is touched, so the type fan-out check (planner step 1b) has
nothing to enumerate; `npm run typecheck` (`tsc --noEmit`, derived in WO-001
from the absence of a `typeCheck` key in `factory.config.json`) is run once
after the edit as a matter of course.

## Interfaces
Consumes: `sdlc-factory/docs/registry/structure.md` rule 7 (the set), read by a
person and transcribed into the test — the test reads the filesystem, not the
registry. Exposes: nothing; this is a test.

## Steps
1. Observe the failing case as it stands (the fail-first observation above).
2. Add `.claude` to `allowed` and update the comment; the case passes and the
   file's other ten cases are unchanged.
3. Remove `.claude` from `allowed`, watch the case fail with
   `received [".claude"]`, restore it.
4. `npm test` and `npm run typecheck`.

## Test plan
> **Criterion source: the registry the blueprint governs, not a requirement.**
> BP-001 carries `satisfies: []` (WO-001's header note applies unchanged), so the
> acceptance below is quoted from `structure.md` rule 7 as amended by commit
> 1fdab38, and no requirement criterion is inherited or paraphrased.

| Acceptance criterion (source quoted) | Test |
|---|---|
| `structure.md` rule 7: "**A new top-level directory requires an ADR.** The top-level set is fixed at `src/`, `supabase/`, `tests/`, `public/`, `sdlc-factory/`, and `.claude/` (the factory's vendored agents, commands, skills and console — tooling, not product; committed 2026-09-02 under commit 61f1fd0)." | `tests/app/toolchain.test.ts › no top-level directory outside structure.md rule 7's set is present` — `allowed` is exactly the six directories the sentence names; `toolingOnly` is unchanged and still names only never-committed directories. Passes on the tree as committed; fails with `received [".claude"]` when `.claude` is removed from `allowed` (the mutation), and fails naming any other directory added at the root. |

## Out of scope
- Whether `.claude/` should have been admitted by an ADR rather than a registry
  amendment — the `rests-on` row records it; the architect's, not this order's.
- Any change to what `.claude/` contains, or to `.gitignore`.
- `tests/app/layout-runner.test.ts` and the `package.json` / `vitest.config.ts`
  edits WO-269 makes — a different slice under the same node; no file is shared.

## Rollback
Revert the one-file edit; the test returns to its current failing state, which
is the state the registry amendment left it in.

## Self-certification
`status: approved` — set 2026-09-03 by the planner under constitution rule 3.2:
an assertion with its grounds stated, which the librarian audits and may refuse
retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-001 — `status: approved`; `tests/app/**`
  is in its `code:` globs.
- **Criteria inherited, not paraphrased.** The 1 row of `## Test plan` quotes
  `structure.md` rule 7 as it stands on disk after 1fdab38; BP-001 carries no
  requirement of its own and cites, never inherits.
- **Bounded.** 1 file in the file plan (0 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** The row names the test that decides it and the
  mutation that must fail it; `## Rollback` states how to undo the change.
- **Merge test (rule 2.6).** One assertion in one existing test, whose goal and
  stop condition cite no other order. It is not a per-file order by accident of
  size: the slice *is* "the top-level set agrees with the registry", and that
  slice lives in one place.
- **`ui: no`.** A test file; nothing customer-visible.
- **Reuse (rule 7.2).** Extends WO-001's existing assertion; mints no second one.

Not asserted here: validation, regression and merge. Those are the `done`
gate's, and the validator's and librarian's to set.

## Log
> The checkpoint any agent resumes from (constitution rule 6.1).
- 2026-09-03 created — planner
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — `.claude` added to `allowed` in
  `tests/app/toolchain.test.ts`; fail-first observed (`received [".claude"]`,
  1 failed / 10 passed), mutation confirmed (removing `.claude` reproduces
  the same failure), a fabricated top-level directory
  (`zzfabricated-top-level`) confirmed to still fail and name itself; `npm
  test` (183 passed) and `npm run typecheck` pass. Ready to validate.
- 2026-09-03 finished — validator — TST-006: pass. All five implementer
  claims independently reproduced (fail-first, verbatim quotation against
  1fdab38, mutation, a second independently-named fabricated directory,
  full suite 183/183 + typecheck clean); `rests-on` row confirmed; no
  `Regression:` line yet — that is `/regress`'s to write.
- 2026-09-03 finished — validator — regression on TST-006: merged
  `doctrine-0.11.0` (13 commits since branch, clean, no conflicts), full
  suite re-run 268/268 pass (9 files), typecheck clean; blast radius
  (structure.md rule 7 / the top-level set) confirmed to have no other
  consumer (`factory-console impact WO-270`: 0 importers, 0
  requirements); WO-270's own `rests-on` row stands `confirmed`,
  BP-001's unrelated open row left for the architect. `Regression: 2
  files · none re-checked — pass` appended to TST-006.
- 2026-09-03 finished — librarian — audit clears all gates; merged wo/WO-270-top-level-set into doctrine-0.11.0

## Validation report (appended by validator)
> Once this WO reaches `status: done`, its LATEST `## TST-###` section must
> also carry a `Regression:` line — what was re-checked alongside the new
> work, not just the new work itself: `Regression: <n> files ·
> <REQ-… list | none> re-checked — pass` or `Regression: <n> files ·
> <REQ-… list | none> re-checked — findings: <one line>`. Missing it is
> `tst-without-regression` (warn).

## TST-006 — WO-270 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-270`, branch `wo/WO-270-top-level-set`, implementer HEAD `a2c082e`
- Corpus-global id: `TST-006` — `grep -rn "^## TST-" sdlc-factory/docs/work-orders/*.md` (root checkout) returns `TST-001` … `TST-005` (WO-001, WO-005, WO-006, WO-267 ×2); `TST-006` is the next unused id.
- `ui: no` — no `Placement:` line follows (design-guardian's UI-fit pass does not run on this WO).
- Scope of the diff, independently confirmed: `git show a2c082e --stat` touches exactly two files — `tests/app/toolchain.test.ts` (9 lines) and this WO's own `## Log` (7 lines). Matches the one-file file plan exactly; no second assertion minted (rule 7.2).

### 1. Verbatim-quotation check (constitution §8) — independently re-run, not taken on report

`git show 1fdab38 -- sdlc-factory/docs/registry/structure.md` confirms the amendment is genuinely in that commit: rule 7 changes from ending at `` `sdlc-factory/`. `` to naming `.claude/` with the "vendored agents, commands, skills and console — tooling, not product; committed 2026-09-02 under commit 61f1fd0" parenthetical, exactly as `structure.md` reads on disk today (`sed -n '118,121p'`). The test's new comment (`tests/app/toolchain.test.ts` lines 51–55) quotes the sentence beginning "The top-level set is fixed at…" through "…under commit 61f1fd0)." **character-for-character** against `structure.md` lines 118–121 — diffed by hand, no divergence. Not a paraphrase. PASS.

### 2. Fail-first — independently reproduced

Reverted only the `allowed` line to its pre-WO-270 state (`src`, `supabase`, `tests`, `public`, `sdlc-factory` — no `.claude`) and re-ran: `AssertionError: expected [ '.claude' ] to deeply equal []`, 1 failed / 10 passed — the exact failure the WO's "Provenance" section and the implementer's log both cite. Restored; `git diff` on the file empty afterward. Matches.

### 3. Mutation / discrimination check — independently re-run

- **Removing `.claude` from `allowed` again** (the WO's own named mutation): reproduces `received [".claude"]`, same as above. Discriminates.
- **A fabricated top-level directory that is not `.claude`** (my own probe, independent of the implementer's `zzfabricated-top-level`): created `./zzz-fabricated-dir/` at repo root, ran the suite — `AssertionError: expected [ 'zzz-fabricated-dir' ] to deeply equal []`, naming the directory. Removed; `git status --porcelain` clean afterward (only pre-existing untracked `node_modules`). The assertion does not merely fail on "anything new" as a boolean — it names the offending entry, so a passing run cannot be mistaken for one that admits everything. Not vacuous.

### 4. Untouched surface — independently confirmed

`git diff a2c082e^ a2c082e -- tests/app/toolchain.test.ts` shows exactly two hunks: the comment block and the single `allowed` line. `toolingOnly` (`node_modules`, `.next`, `coverage`, `.git`) is byte-identical before and after — `.claude` was not added there, consistent with the WO's own instruction ("not to `toolingOnly` — it is committed"). All 10 other cases in the file (the describe block's remaining assertions) are untouched by the diff — confirmed by the diff itself, not by running them selectively. The set widened by exactly one entry, `.claude`, nothing else.

### Criterion-by-criterion (WO-270 `## Test plan`, 1 row)

| # | Criterion (source quoted) | Test | Result |
|---|---|---|---|
| 1 | `structure.md` rule 7 as amended (1fdab38): top-level set is `src/`, `supabase/`, `tests/`, `public/`, `sdlc-factory/`, `.claude/` | `tests/app/toolchain.test.ts › no top-level directory outside structure.md rule 7's set is present` | PASS — `allowed` is exactly the six named directories; `toolingOnly` unchanged; passes clean on the tree as committed; fails naming `.claude` when it is removed from `allowed`; fails naming any other fabricated top-level directory (independently probed, not just asserted). |

### Full suite and typecheck — independently re-run

`npm test` (full, no flags): 5 files, **183 passed**, 0 failed — matches the implementer's reported count exactly. `npm run typecheck` (`tsc --noEmit`): exit 0, no output. Both re-run fresh in the worktree, not read off the log line.

### Structure compliance

`tests/app/**` is BP-001's per `structure.md`'s own globs; this WO extends WO-001's existing single top-level-set assertion (`structure.md` §"one assertion, one place" reasoning restated in the WO's own `## Provenance` section) rather than minting a duplicate — confirmed no second `readdirSync(ROOT` assertion exists anywhere else in `tests/` (`grep -rn "readdirSync(ROOT" tests/` returns the one site, this file). No structure violation.

### Coverage gaps

None found. The one criterion is covered by a test that discriminates on both directions of mutation (removal of the admitted entry, addition of a fabricated one), and the comment's citation is verbatim.

### `rests-on` disposition (constitution rule 2.3b)

WO-270's one row — *"`structure.md` rule 7 … admits `.claude/` by a registry edit rather than by an ADR. This order asserts the set exactly as the registry states it and takes that amendment as the authority on the set — the test's job is to agree with `structure.md`, not to rule on how `structure.md` was changed."* — is **confirmed**.

The row's own text scopes what this build needs to observe: agreement with the registry as it stands, not a ruling on whether an ADR should have been written for the admission. That agreement is exactly what sections 1–4 above establish independently: the test's `allowed` set and its comment match `structure.md` rule 7 (as amended by 1fdab38) verbatim and by membership, and nothing here re-litigates the amendment's own legitimacy. Separately checked, for the record: `grep -rl "\.claude" sdlc-factory/docs/decisions/` returns no ADR — no ADR records the `.claude/` admission as of this build, consistent with the row's own "if an ADR later records the admission, nothing here moves" framing; that absence does not block confirmation because the row explicitly disclaims ruling on the how. Front-matter row updated `open` → `confirmed`.

No upstream `rests-on` row on BP-001 is newly observed by this build — this WO touches no BP-001 code path, only a test assertion already governed by `structure.md`; BP-001's own rows (if any) are left for the architect's wave-close read per rule 2.3b.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

pass

All five of the implementer's claims held under independent re-run: the fail-first observation (`expected [ '.claude' ] to deeply equal []`) reproduced by reverting the edit; the verbatim re-quotation of `structure.md` rule 7 confirmed character-for-character against the amendment in commit 1fdab38; the mutation (removing `.claude` again) reproduced the same failure; a fabricated top-level directory (independently named `zzz-fabricated-dir`, distinct from the implementer's own probe) still fails and is named in the assertion; the full suite (183/183) and `tsc --noEmit` both re-ran clean. Scope is exactly the one file the plan names plus the WO's own `## Log`. `toolingOnly` and the file's other 10 cases are byte-identical before and after. The one `rests-on` row is confirmed on the terms it states for itself. No coverage gap, no vacuous test, no scope creep.

No `Regression:` line follows: that record is `/regress`'s to write, a separate dispatch from this `/validate` pass (constitution §3's gate list; the validator agent's own step 7). This WO does not carry one yet and should not reach `done` until it does.

## Regression

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-270`, branch `wo/WO-270-top-level-set`, HEAD `967fd47` (merge commit; pre-merge tip `51b35b3`)
- Scope: constitution §3's regression clause — "its latest validation section states what else was re-checked, not just the new work." This subsection is that record, appended to TST-006 (the latest section carrying a passing verdict), per `/regress`'s dispatch — not a new validation pass and not a re-mutation of the one test-plan row TST-006 already settled.

**Blast radius.** WO-270's own diff (`git diff --name-only be814e5..51b35b3`) touches exactly two files: `tests/app/toolchain.test.ts` and this WO's own file. `tests/app/toolchain.test.ts` is the sole assertion of `structure.md` rule 7's top-level set: `grep -rn "readdirSync(ROOT" tests/` returns this one site, and `grep -rln "structure.md" tests/ src/` returns four files total (`tests/config/env.test.ts` citing rule 4, `tests/app/lint-rules.test.ts` citing rule 6, `src/lib/config/constants.ts` citing rule 5, and this file citing rule 7) — no other file asserts or depends on rule 7 or the top-level directory set. `factory-console impact WO-270` (this worktree, post-merge): `5 files → 1 blueprints → 0 importers → 0 requirements` — `sdlc-factory/docs/work-orders/{WO-006,WO-127,WO-267,WO-270}.md` and `tests/app/toolchain.test.ts`; zero live importers and zero requirements downstream of BP-001 through this WO, confirming the assertion has no other consumer to regress. WO-127 cites BP-001 but neither the toolchain test nor the top-level set (`grep -n "toolchain.test.ts\|top-level" sdlc-factory/docs/work-orders/WO-127.md` returns nothing) — unrelated citation, no shared surface.

**Merge picture.** `doctrine-0.11.0` moved substantially since WO-270 branched (from `be814e5`): not only the four `docs(evidence)` commits and the `5aff98c` relink the dispatch named, but also WO-267's own merge (`ad7bbfe`, Supabase substrate), the 0.12.2 doctrine migration (`c4e1cac`, `829cb63`), REQ-097's round-two fold (`d0addff`), and BP-003's drift-assessment disposition (`7f6db13`) — 13 commits, none touching `tests/app/toolchain.test.ts`, `structure.md`, or anything under `.claude/` (confirmed: none of those three paths appear in `git diff --name-only be814e5..origin-local/doctrine-0.11.0`). `git merge --no-ff origin-local/doctrine-0.11.0` completed with **no conflicts** (`git diff --check` empty, `git status --short` shows only the pre-existing untracked `node_modules` symlink) — a genuine three-way merge, not a fast-forward.

**Full suite, re-run fresh on the merged tree.** `npx vitest run --no-file-parallelism`: 9 test files, 268 tests — **268 passed, 0 failed**. This includes `tests/app/toolchain.test.ts`'s top-level-set case passing clean against the merged tree, which now also carries everything the `research/seo-automation` evidence branch and the 0.12.2 migration landed at the root (`OWNER-QUESTIONS.md`, `sdlc-factory/docs/registry/evidence/**`, `sdlc-factory/docs/archive/**`) — none of it a new top-level directory, so the set WO-270 asserts is unchanged and still agrees with `structure.md` rule 7. (TST-006's own pre-merge run reported 5 files / 183 passed; the growth to 9 files / 268 tests is WO-267's `src/lib/db/**` and `tests/db/**` surface arriving via the merge, not anything this WO changed — WO-270's own diff is still the two files named above.)

`npm run typecheck` (`tsc --noEmit`): clean, no output, exit 0.

**`rests-on` rows.** WO-270's own row is already `disposition: confirmed`, set by TST-006 and unaffected by anything in this merge (the merge touches no file under `tests/app/`, `structure.md`, or `.claude/`). BP-001's own open row (Vercel serving the App Router and Inngest endpoint from one build/process image) is not something this build observes — WO-270 exercises a filesystem assertion only, not deployment topology — so it is left `open` here, for the architect's wave-close read, not dispositioned by this pass.

**Result:** no new failures, no change in test outcome for the one case this WO touches, merge into `doctrine-0.11.0` is clean (no conflicts), typecheck clean.

Regression: 2 files (`sdlc-factory/docs/work-orders/WO-270.md`, `tests/app/toolchain.test.ts`) · none (BP-001 carries `satisfies: []`; no REQ is named on this WO) re-checked — pass
