---
id: WO-283
type: work-order
title: "npm test is flaky again — the live-scratch-schema file set outgrew the db project's include list; make the two lists one"
status: approved
wave: W3
ui: no
implements: [BP-001]
depends-on: [WO-275]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-283 — npm test is flaky again — the live-scratch-schema file set outgrew the db project's include list; make the two lists one

- Branch: wo/WO-283-live-db-project-membership
- Estimate: ≤1 day (one file, one list)

## Goal
Make `npm test` repeatably green by enumerating every test file that resets
the one live scratch schema in a single declared list that `vitest.config.ts`'s
`db` project includes and its `node` project excludes, so the membership of
the serialized boundary WO-275 established can no longer drift as new live-DB
test files are added.

## Why this runs first in W3
Three of W3's orders are unvalidated and two more are mid-build. A validation
report, a regression record and a `done` verdict all rest on `npm test`'s
result, and `npm test` is red on a varying set of files today — so every
verdict taken while it stays flaky is a verdict whose evidence cannot be
distinguished from noise. TST-025 has already spent one pass chasing a
transient `db` failure. This order is one file and no product behaviour; it
goes ahead of the orders still to be validated so their evidence means
something.

## Provenance of this file plan
`vitest.config.ts` exists and is BP-001's (`code:` glob `vitest.config.ts`;
`structure.md`'s root-toolchain row, BP-001 decision 2). The file is MODIFY,
not NEW. The defect and its confirmation are WO-276's implementer's, recorded
on that order's `## Log` under constitution rule 4.2 and quoted in
`## Test plan` below — it is not re-derived here. Read directly on
`doctrine-0.11.0` at cut time: `db.include` names four files
(`tests/db/baseline.test.ts`, `tests/db/rls.test.ts`,
`tests/db/clients.test.ts`, `tests/scan/free/admission-claim.test.ts`) and
`node.exclude` names the same four; `tests/scan/free/schema.test.ts` and
`tests/account/columns.test.ts` each run
`"drop schema public cascade; create schema public; …"` and are in neither
list, so they race each other and the four under `node`'s default
concurrency. `tests/scan/free/admission-check.test.ts` was checked and stays
in `node`: it mocks `@/lib/db` (`vi.mock("@/lib/db", …)`) and opens no
connection.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `vitest.config.ts` | MODIFY | One declared list of the test files that reset the one live scratch schema — the four already named plus `tests/scan/free/schema.test.ts`, `tests/account/columns.test.ts`, `tests/costs/fetches-schema.test.ts` and `tests/costs/context.test.ts` — referenced by both the `db` project's `include` and the `node` project's `exclude`, so the two cannot diverge again. That divergence is the defect; two hand-maintained copies of one membership set is the second copy rule 2.4 forbids. The `db` project's `fileParallelism: false`, the `ui` and `layout` projects, `PinsFirstSequencer` and the `@` alias are untouched. |

## Interfaces
Consumes: the per-project `fileParallelism: false` boundary WO-275
established on the `db` project.
Exposes: no code interface. `npm test`'s script value and its zero-argument
invocation are unchanged — the `--project node --project ui --project db`
list already names every project this order touches.

## Steps
1. Watch it fail first (constitution §8): run `npm test` ten times against the
   config as it stands and record the pass/fail counts and the differing
   failure sets.
2. Make the change in `vitest.config.ts`.
3. Run `npm test` ten consecutive times; record every run's counts.
4. Attribution probe: show that only the files on the declared list serialize
   and that `ui`, `layout` and the rest of `node` still schedule concurrently
   — a fix whose evidence is only "green now" is indistinguishable from
   blanket serialization.
5. Revert the change alone, reproduce the varying-failure pattern, restore it,
   return to green.

## Test plan
> **Criterion source.** BP-001 carries `satisfies: []` and this order changes
> no product behaviour its `## Public interface` states — only the toolchain's
> test-execution discipline, exactly as WO-275's own test-plan header records.
> Rows quote WO-276's `## Log` evidence, `structure.md`'s root-toolchain row
> and constitution §8/§10 directly.

| Acceptance criterion (source quoted) | Test |
|---|---|
| WO-276 `## Log`: "`npm test`'s full run is flaky because `vitest.config.ts`'s `db` project (WO-275) was never extended past its original three files to cover the live-scratch-database test files added since — `tests/scan/free/schema.test.ts`, `tests/account/columns.test.ts`, and now this WO's own two `tests/costs/*.test.ts` — so all five race under the `node` project's default concurrency" | Step 3 — ten consecutive `npm test` runs, 0 failed each. Ten, not one: a single pass is inside the observed flake rate and proves nothing. |
| WO-276 `## Log`: "confirmed pre-existing (reproduces with this WO's two new files removed, 2 of 3 runs)" | Step 1 — the same ten runs before the change reproduce the varying failure set, so the change is what moved the result. |
| WO-276 `## Log`: "confirmed fixed by isolation (this WO's two files pass 100% across many runs under `--no-file-parallelism`, alone and together)" | Step 5 — reverting only this change reproduces the pattern; restoring it returns to green. |
| §8: "A test that survives deletion of its feature is vacuous." §10: "every acceptance criterion is covered by a test that discriminates." | Step 4 — the attribution probe: the serialized set is exactly the declared list, and nothing else lost its concurrency. Blanket serialization would also be green and would fail this row. |
| `structure.md`'s root-toolchain row names `npm test` as one of the commands the toolchain is verified by (BP-001 decision 2) — the command an external caller invokes must not change. | `git diff` on `package.json` is empty; only `vitest.config.ts` moves. |

## Out of scope
- Any test file's own contents. No test is rewritten, skipped or deleted; the
  coupling being fixed is which project a file runs in, not what it asserts.
- The scratch substrate itself, and any change to how a test acquires it.
- WO-276's two `tests/costs/*.test.ts` files. They are named on the declared
  list so the boundary is right the moment they land, but they arrive with
  WO-276 and this order neither waits for them nor creates them; a listed
  path that matches no file is not an error, and the ten runs of step 3 are
  measured against whatever `doctrine-0.11.0` holds at implementation time.
- Making the list derive itself from the test files (a marker, a glob, a
  naming convention). Considered and not taken: the set is eight explicit
  paths and a convention would be a second thing to get wrong. Reversal cost
  if it grows past that: this one file.

## Rollback
`git revert` the single commit. `vitest.config.ts` returns to its four-file
`db` project and `npm test` returns to being flaky — no product behaviour and
no test assertion moves either way.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule
3.2. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-001 — `status: approved`.
- **Criteria sourced, not paraphrased.** Every row quotes its source as it
  stands on disk on 2026-09-04.
- **Bounded.** One file, one list; ≤1 day.
- **Merge test (rule 2.6) applied.** This order states its goal and its stop
  condition — ten consecutive green `npm test` runs — without citing another
  order's goal. WO-275 is cited as the `done` provenance of the boundary it
  extends and WO-276 as the source of the evidence, neither as a dependency
  of what "done" means here. It is not merged into either: WO-275 is `done`
  and WO-276 is mid-validation on a disjoint file set.
- **Stop condition stated.** Ten consecutive green runs, plus the step-4
  attribution probe and the step-5 revert probe.

Not asserted here: validation, regression and merge.

## Log
- 2026-09-04 created — planner
