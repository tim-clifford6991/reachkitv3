---
id: WO-277
type: work-order
title: "The verdict — the three-armed measurement, the partition, the score and its band, and the one Verdict the report is made of"
status: approved
wave: W3
ui: no
risk: high
implements: [BP-024, REQ-004]
depends-on: [WO-001, WO-006, WO-018, WO-051]
blocked-by: []
rests-on:
  - claim: "The Next.js + TypeScript scaffold (package.json, tsconfig.json) exists before this WO runs, with `strict` and exhaustiveness checking on. It is BP-001's and BP-005's, not this node's; this repo carries none of it today"
    disposition: confirmed
    reason: "TST-031 — observed directly. `package.json` and `tsconfig.json` exist at the repo root; `tsconfig.json`'s `compilerOptions.strict` is `true`; `npm run typecheck` (`tsc --noEmit`) exits 0 against the six `src/lib/measure/**` files this WO ships, which rely on discriminated-union exhaustiveness. The scaffold this row names was on disk and load-bearing throughout this build."
  - claim: "BP-005 gains the four score-band boundaries of `BUILD.md` §5 — 0–24, 25–49, 50–74, 75–100 — as a pin under a name BP-005 chooses. **Discharged 2026-08-31**: BP-005 declares `SCORE_BAND_BOUNDS: { invisible: 0; 'hard-to-find': 25; findable: 50; dominant: 75 }`, named so as not to collide with BP-019's `SCORE_BANDS` copy map, and BP-024's `bandOf` comment now names it. This WO imports that pin and does not create it"
    disposition: confirmed
  - claim: "The closed list of AI reader user-agents the `blockedReaders` count is computed over is pinned once in BP-005 as `AI_READER_AGENTS` (ADR-022, amended by ADR-090, which cites REQ-059 criterion 4 for the membership). This WO reads the pin and populates nothing; a build with the list empty fails BP-005's own pins test"
    disposition: confirmed
    reason: "TST-031 — observed directly. `src/lib/config/constants.ts` declares `AI_READER_AGENTS` frozen with six named members (GPTBot, ClaudeBot, OAI-SearchBot, Claude-SearchBot, PerplexityBot, Google-Extended), non-empty; `verdict.ts`'s `robotsCount` imports and iterates it by name, reading no other list."
  - claim: "`scans` exists with `domain`, `report jsonb`, `score numeric null`, `drivers jsonb` and the `unique (domain) where is_current` partial index before this migration runs. That table is BP-002's baseline and BP-012's; this repo carries no `supabase/` directory today"
    disposition: confirmed
    reason: "TST-031 — observed directly against `supabase/migrations/00000000000001_baseline.sql` (`scans.domain`, `scans.report jsonb`, `scans.drivers jsonb`, `scans.score integer` — nullable, not `numeric`, but present and null-capable, which is the row's operative claim) and `20260904110000_scans_current.sql` (`create unique index … on scans (domain) where is_current`, WO-280). Both migrations apply cleanly in sequence against a freshly dropped scratch schema."
  - claim: "`RobotsPolicy` has a shape. **Discharged 2026-08-31**: BP-006 declares it — `ok`, `origin`, `readAt`, `disallowsAll`, `disallowedAgents`, `sitemaps`, `absent` — and WO-018 ships the declaration in `src/lib/egress/types.ts`. This WO imports that type and reads `disallowedAgents` and `absent`; it declares no local reader interface"
    disposition: confirmed
supersedes: [WO-052, WO-053, WO-054, WO-055]
---

# WO-277 — The verdict — the three-armed measurement, the partition, the score and its band, and the one Verdict the report is made of

- Branch: wo/WO-277-verdict
- Estimate: ≤1 day

## Goal
Ship the value the top of the report is made of, end to end: the three-armed `Measured<T>` no constructor can fabricate, the exhaustive classification of every scan outcome into one of its arms, `BUILD.md` §5's arithmetic over those arms, the four band handles, the limiting factor, and one `Verdict` the stored scalar cannot disagree with.

> **Read `decisions/ADR-021.md` before writing a line of this WO.** It is a
> landmine: raising the cap to finish the last call, and computing a partial
> score from the factors that were measured, both read as kindnesses and both
> break a promise a customer can be shown.

**Risk: high — seam: money.** ADR-021 has two halves and this order carries the second: a scan the spend cap clamped must yield *no* score rather than a partial one computed from the factors that were measured. The trigger is the cost cap (WO-276's clamp, WO-281's ceiling); the harm is a fabricated customer-visible number that reads exactly like a real one. That is silent, so this order's criteria are mutation-tested (doctrine 0.13.2, rule 2b).

## Consolidation (rule 2.6)
The four orders were one capability cut along its five files. WO-053's and
WO-054's `## Rollback` each read "`verdict.ts` (WO-055) is the only consumer and
does not exist until after this WO"; WO-052's reads "Nothing imports them until
WO-053 lands"; WO-055's file plan composes all three. None of the four can state
its stop condition without citing another, which is rule 2.6's merge test
exactly. One order, one capability: a scan outcome becomes a `Verdict`.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `src/lib/measure/measured.ts` | NEW | WO-052 | The whole public interface below. `Measured<T>` is a plain discriminated union — not a class, no methods, no prototype (BP-024's NFR budget: "a per-value allocation and nothing more"). Three constructors and no fourth. `mapMeasured`, `combine` and `worseReason` are total. `combine` returns `unmeasured` when any part is `unmeasured`, folding the reasons with `worseReason`. |
| `tests/measure/verdict/measured.test.ts` | NEW | WO-052 | The suite named in the test plan, including the negative tests that are type-level assertions. |
| `src/lib/measure/partition.ts` | NEW | WO-053 | The whole public interface below. `classify` is one function over one closed input union with one arm each, implementing BP-024's six-row classification table exactly. `FEEDS` is the transitively closed dependency map of BP-024's `FEEDS` table. `sectionOutcome` returns `{ present: false; reason }` only when every input the section is made of is `unmeasured`, and the reason is the fold of those reasons under `worseReason`. |
| `tests/measure/verdict/partition.test.ts` | NEW | WO-053 | The suite named in the test plan, including the exhaustiveness tests over both closed unions. |
| `src/lib/measure/score.ts` | NEW | WO-054 | `ScoreFactorName`, `ScoreFactors`, `presenceOf`, `factorsOf`, `computeScore`, `LimitingFactor` and `limitingFactorOf` (added 2026-09-04 by BP-024 decision 6, the owner's ruling of 2026-09-03: the three driver bars are removed and the verdict says one line instead). All arithmetic runs through WO-052's `combine`, so no branch can reach a raw number from an `unmeasured` arm. `computeScore` takes exactly one parameter and no flag (ADR-021 decision 3). Floors: Answerability and Presence are floored at 1, and a floor applies only to a `measured` or `zero` arm. |
| `src/lib/measure/bands.ts` | NEW | WO-054 | `BandHandle` and `bandOf(score: number): BandHandle`, reading the four boundaries from BP-005's `SCORE_BAND_BOUNDS` — `{ invisible: 0, 'hard-to-find': 25, findable: 50, dominant: 75 }`, imported, never written here (`structure.md` rule 5, and BP-024's own comment on `bandOf`). **No numeric literal 0, 25, 50 or 75 appears in this file.** No band word appears in it either; the words are BP-019's `SCORE_BANDS`. |
| `tests/measure/verdict/score.test.ts` | NEW | WO-054 | The arithmetic and ADR-021 suites named in the test plan. |
| `tests/measure/verdict/bands.test.ts` | NEW | WO-054 | The band-boundary suite, including the boundary values themselves. |
| `src/lib/measure/verdict.ts` | NEW | WO-055 | `Verdict` and `verdictOf`. Composes WO-054's `factorsOf`/`computeScore`/`limitingFactorOf`, carries `limiting` and **no `factors` field** (BP-024 decision 6, 2026-09-03), derives `missing` over factors (never over BP-010's four quantities), derives `unmeasuredElsewhere` from the inputs whose `FEEDS` row is empty of the factors that failed, and computes `blockedReaders` as the count of `AI_READER_AGENTS` members the site's access rules disallow (ADR-022 decision 2). `measuredAt` is a parameter; this file reads no clock. No `tier` field and no `forFree` parameter appears anywhere in it. |
| `supabase/migrations/<timestamp>_scans_verdict.sql` | NEW | WO-055 | The check constraint BP-024 fixes, verbatim: `check ((report->'verdict'->'scoreAndBand'->>'kind' = 'unmeasured') = (score is null))`. Adds no column. The migration file name carries the `scans_verdict` sub-token and nothing else. |
| `tests/measure/verdict/verdict.test.ts` | NEW | WO-055 | The composition, determinism and blocked-readers suites. |
| `tests/measure/verdict/constraint.test.ts` | NEW | WO-055 | The constraint suite: an insert whose scalar and blob disagree is refused by the database, in both directions. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-052

Consumes: nothing.

Exposes (verbatim from BP-024 `## Public interface`, `src/lib/measure/measured.ts`):

```ts
/** BP-010 declares this type; this node produces it and owns its combinators.
 *  Every stored and rendered number in the product is one of the three arms. */
export type Measured<T> =
  | { kind: 'measured';   value: T; at: Date }
  | { kind: 'zero';       value: T; at: Date }
  | { kind: 'unmeasured'; reason: UnmeasuredReason; at: Date }
export type UnmeasuredReason = 'undeterminable' | 'not_attempted'

/** The only constructors. There is no `Measured.of(number | null)`: a nullable
 *  number cannot say why, and the whole point of REQ-004 is that it must. */
export function measured<T>(value: T, at: Date): Measured<T>
export function measuredZero<T>(zero: T, at: Date): Measured<T>
export function unmeasured<T>(reason: UnmeasuredReason, at: Date): Measured<T>

/** Total combinators. Arithmetic never leaves the type, so no call site can
 *  reach a raw number by accident and no reason is lost on the way. */
export function mapMeasured<A, B>(m: Measured<A>, f: (a: A) => B): Measured<B>
export function combine<A extends readonly Measured<unknown>[], B>(
  parts: A, f: (values: Values<A>) => B
): Measured<B>          // unmeasured if any part is unmeasured — see `worseReason`
export function worseReason(a: UnmeasuredReason, b: UnmeasuredReason): UnmeasuredReason
```

`Values<A>` is the tuple of the `value` types of `A`'s arms; it is a local type
helper of this file and is not part of the exported surface BP-024 declares.

### From WO-053

Consumes (from WO-052, `src/lib/measure/measured.ts`): `Measured<T>`,
`UnmeasuredReason`, `measured`, `measuredZero`, `unmeasured`, `worseReason`.

Exposes (verbatim from BP-024 `## Public interface`, `src/lib/measure/partition.ts`):

```ts
/** The exhaustive classification REQ-004's last non-goal hands to the blueprint.
 *  One function, one closed input union, one arm each — a new input cannot ship
 *  without a classification. */
export type ScanInput =
  | 'home_document' | 'pricing_document' | 'access_rules'
  | 'business_profile' | 'market_suggestions'
  | 'own_ranked_rows' | 'question_serps'
export type InputOutcome =
  | { read: true;  empty: boolean }        // we read it; `empty` = it contained none of what we counted
  | { read: false; because: 'undeterminable' }
  | { read: false; because: 'not_attempted' }
export function classify(input: ScanInput, o: InputOutcome, at: Date): Measured<null>

/** REQ-004 criterion 6's second sentence — "an input no driver depends on never
 *  withholds the score" — is this map, not a judgement at each call site. */
export const FEEDS: Readonly<Record<ScanInput, readonly ScoreFactorName[]>>

/** Section-level outcome, for REQ-004 criterion 10. A section whose data could
 *  not be retrieved is absent and named; it is never an empty card. */
export type SectionName = 'verdict' | 'ai_answers' | 'google_presence' | 'problems' | 'first_page'
export function sectionOutcome(s: SectionName, inputs: Readonly<Record<ScanInput, InputOutcome>>):
  { present: true } | { present: false; reason: UnmeasuredReason }
```

`ScoreFactorName` is WO-054's export from `src/lib/measure/score.ts`. To keep the
dependency direction acyclic, declare `ScoreFactorName` in `score.ts` and
`import type` it here; `partition.ts` imports no value from `score.ts`.

### From WO-054

Consumes: WO-052's `Measured<T>`, `combine`, `mapMeasured`, `worseReason`;
BP-010's `Drivers` (`src/lib/measure/index.ts`, another node's file — the four
measured quantities `foundations`, `answerability`, `searchPresence`,
`aiPresence`, each a `Measured<number>`); BP-005's `SCORE_BAND_BOUNDS`.

Exposes (verbatim from BP-024 `## Public interface`, `src/lib/measure/score.ts`
and `bands.ts`):

```ts
/** The three the score is composed of. **The report shows none of them**: since
 *  the owner's ruling of 2026-09-03 (`OWNER-QUESTIONS.md` item 4) the verdict is
 *  the score and its band word plus one written line naming the factor holding
 *  the score down — decision 6. `Presence` is the geometric mean of BP-010's
 *  `searchPresence` and `aiPresence`, floored at 1 (`BUILD.md` §5) — those two
 *  are sub-measures, not factors. */
export type ScoreFactorName = 'foundations' | 'answerability' | 'presence'
export type ScoreFactors = Readonly<Record<ScoreFactorName, Measured<number>>>

export function presenceOf(d: Pick<Drivers, 'searchPresence' | 'aiPresence'>): Measured<number>
export function factorsOf(d: Drivers): ScoreFactors
export function computeScore(f: ScoreFactors): Measured<{ score: number; band: BandHandle }>
export type BandHandle = 'invisible' | 'hard-to-find' | 'findable' | 'dominant'
export function bandOf(score: number): BandHandle   // thresholds are BP-005's SCORE_BAND_BOUNDS

/** Which factor holds the score down — the one thing about the composition the
 *  report says, and it says it in a written line BP-019 holds, never as a value.
 *  `at_ceiling` exists so that a domain with nothing holding it down renders no
 *  line rather than an arbitrary one; `score_unmeasured` is reachable exactly
 *  when `scoreAndBand` is `unmeasured`, which is a test, not a convention. */
export type LimitingFactor =
  | { kind: 'factor'; factor: ScoreFactorName }
  | { kind: 'none'; because: 'score_unmeasured' | 'at_ceiling' }
export function limitingFactorOf(f: ScoreFactors): LimitingFactor
```

The arithmetic, verbatim from `BUILD.md` §5: `Presence = max(1, √(SearchPresence
× AIPresence))`; `Score = round( ∛(Foundations × Answerability × Presence) )`;
`Bands: 0–24 Invisible · 25–49 Hard to find · 50–74 Findable · 75–100 Dominant`.

### From WO-055

Consumes: WO-052's `Measured<T>`/`worseReason`; WO-053's `ScanInput`,
`InputOutcome`, `FEEDS`, `classify`; WO-054's `ScoreFactors`, `factorsOf`,
`computeScore`, `BandHandle`; WO-051's `CanonicalDomain`; BP-005's
`AI_READER_AGENTS`; BP-006's `RobotsPolicy`, imported from WO-018's
`src/lib/egress/types.ts` — the declared six-field interface, read through
`disallowedAgents` and `absent` and through no other field.

Exposes (verbatim from BP-024 `## Public interface`, `src/lib/measure/verdict.ts`):

```ts
/** What the top of the report is made of. There is no tier field and no
 *  `forFree` parameter anywhere in this file: REQ-004 criterion 5's "no part …
 *  hidden, blurred, rounded down, locked, or marked as available on payment" is
 *  discharged by there being nothing to pass that could hide anything. */
export interface Verdict {
  domain: CanonicalDomain
  measuredAt: Date                       // one date; every figure under it is this scan's
  scoreAndBand: Measured<{ score: number; band: BandHandle }>
  /** No `factors` field. The three factor **values** reach no surface (BP-024
   *  decision 6): what the report says about the composition is this handle and
   *  the one written line BP-019 renders from it. `factorsOf` is still computed
   *  inside `verdictOf` — the score is made of it — and is still stored under
   *  `scans.drivers`, which is not a surface. */
  limiting: LimitingFactor
  /** REQ-004 criterion 3: name every factor with no value and, for each, which
   *  of the two reasons applies. Empty when the score could be computed. */
  missing: readonly { factor: ScoreFactorName; reason: UnmeasuredReason }[]
  /** REQ-004 criterion 6, the "no driver depended on it" case, and criterion 10. */
  unmeasuredElsewhere: readonly { input: ScanInput; reason: UnmeasuredReason }[]
  /** Read here because REQ-009 criteria 1, 3 and 4 are a rendering of a
   *  measurement this node performs; BP-027 renders it and computes nothing. */
  blockedReaders: Measured<number>
}
export function verdictOf(a: {
  domain: CanonicalDomain; measuredAt: Date
  drivers: Drivers; inputs: Readonly<Record<ScanInput, InputOutcome>>
  robots: Measured<RobotsPolicy>
}): Verdict
```

## Steps

**WO-052's steps** (renumbered; order within the block unchanged):

1. Write the failing suite first.
2. Declare the union and `UnmeasuredReason`.
3. Implement the three constructors. Export no `of`, no `fromNullable`, no
   `orElse`, and no default parameter that would let `at` be read from a clock.
4. Implement `mapMeasured` — it preserves the arm and the `at`, and never
   touches the `unmeasured` arm's reason.
5. Implement `combine`: if every part is `measured` or `zero`, apply `f` to the
   values and return `measured`; if any part is `unmeasured`, return
   `unmeasured` carrying the fold of every unmeasured part's reason under
   `worseReason`. It never calls `f` when any part is unmeasured.
6. Implement `worseReason` per BP-024 decision 3: `undeterminable` wins whenever
   either side is `undeterminable`.
7. Add the type-level negatives with `@ts-expect-error` so the build fails if
   the guard ever weakens.

**WO-053's steps** (renumbered; order within the block unchanged):

8. Write the failing suite first.
9. Declare `ScanInput`, `InputOutcome` and `SectionName`.
10. Implement `classify` as a total switch over `InputOutcome` per BP-024's
   classification table: `{read:true, empty:false}` → `measured(null, at)`;
   `{read:true, empty:true}` → `measuredZero(null, at)`; `{read:false,
   because:'undeterminable'}` → `unmeasured('undeterminable', at)`;
   `{read:false, because:'not_attempted'}` → `unmeasured('not_attempted', at)`.
   Reading is the whole of the line: no arm inspects the input name to decide.
11. Write `FEEDS` from BP-024's table, transitively closed: `home_document` →
   `['foundations','answerability']`; `pricing_document` → `['answerability']`;
   `access_rules` → `['foundations']`; `business_profile` → `['presence']`;
   `market_suggestions` → `['presence']`; `own_ranked_rows` → `['presence']`;
   `question_serps` → `['presence']`. Freeze it.
12. Implement `sectionOutcome` over a per-section input list held in this file.
13. Add the exhaustiveness guards: a `never` check on both unions so a new member
   fails the build until it has a row.

**WO-054's steps** (renumbered; order within the block unchanged):

14. Write the failing suites first, ADR-021's three prohibitions included.
15. Declare `ScoreFactorName`, `ScoreFactors`, `BandHandle`.
16. Implement `bandOf` over `SCORE_BAND_BOUNDS`, descending through its four
   lower bounds, with a total switch and no default arm that could swallow an
   out-of-range value; assert the input range at the boundary instead. The pin is
   the only source of a threshold: a source assertion in the suite fails on any
   numeric literal in `bands.ts` other than the range guard's own 0 and 100.
17. Implement `presenceOf` with `combine([searchPresence, aiPresence], …)` and the
   floor of 1 applied inside the combining function, so it can only ever apply to
   a value that exists.
18. Implement `factorsOf`: `foundations` and `answerability` pass through from
   `Drivers` (answerability floored at 1), `presence` is `presenceOf`.
19. Implement `computeScore` with `combine([foundations, answerability, presence],
   …)`. Take no second parameter. Do not export a partial variant, an `options`
   object, or a `force` flag — ADR-021 decision 3.
20. Implement `limitingFactorOf` per BP-024 decision 6 (added 2026-09-04): over
   the three factors, return `{ kind: 'factor', factor }` for the one whose
   measured value is lowest, `{ kind: 'none', because: 'score_unmeasured' }`
   when any factor is `unmeasured`, and `{ kind: 'none', because: 'at_ceiling' }`
   when no factor is below the ceiling BP-024 fixes. The tie-break order is
   BP-024's and is read from there, not chosen here. It returns a handle and
   never a value: no factor's number leaves this function.
21. Add each ADR-021 test with a failure message naming `ADR-021` (that ADR's
   consequence: "each names this ADR in its failure message").

**WO-055's steps** (renumbered; order within the block unchanged):

22. Write the failing suites first.
23. Implement `verdictOf`: factors from `factorsOf`, score from `computeScore`, `limiting` from `limitingFactorOf`. The factors are computed and stored under `scans.drivers`; they are not a field of `Verdict` (BP-024 decision 6).
24. Derive `missing`: one entry per factor whose arm is `unmeasured`, carrying that
   arm's reason. Empty when the score is `measured` or `zero`.
25. Derive `unmeasuredElsewhere`: one entry per `ScanInput` whose `InputOutcome`
   is `read: false` and whose `FEEDS` row contains no factor that is itself
   missing — REQ-004 criterion 6's "where no driver depended on that input".
26. Derive `blockedReaders`: carry `robots`' arm through with `mapMeasured`,
   counting the members of `AI_READER_AGENTS` for which
   `policy.disallowedAgents[token] === true` — the closed set is applied here,
   which is what BP-006 means by "applied by the caller, not here", and a token
   absent from the map is not disallowed. `policy.absent === true` is a **read
   with nothing in it**: the count is a measured 0 (REQ-004 criterion 7), never
   an `unmeasured`. `disallowsAll === true` disallows every member of the set.
   An `unmeasured` robots value yields an `unmeasured` count with the same
   reason — never 0.
27. Assert on the way out that every `Measured` value inside the verdict carries
   the same `at` as `measuredAt`; a mismatch throws, per BP-024's "a value whose
   `at` differs from the verdict's is a test failure".
28. Write the migration with the constraint verbatim. Do not add a column.
29. Add the determinism test: same inputs twice, byte-identical JSON.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| REQ-004 c12 — "Given a measurement this scan could not take — an input whose existence it could not determine (criterion 6) or work it never attempted (criterion 9) — when the report renders and the result is stored, then nothing stands in its place: no estimate, no interpolated value, no default, and no value carried over from an earlier scan of that domain. Every number the report shows and the product stores comes from the scan whose measurement date the report carries." | WO-052 | `measured/constructors · there is no way to make a value out of nothing` — a set of `@ts-expect-error` assertions that `measured(undefined as any)`-style escapes do not compile, plus a source assertion that `src/lib/measure/measured.ts` exports exactly the eight symbols above and nothing named `of`, `fromNullable`, `orElse`, `getOrDefault` or `unwrap`. And `measured/map · never fabricates` — `mapMeasured` over an `unmeasured` value returns `unmeasured` with the same reason and the same `at`, and `f` is asserted never to have been called. |
| REQ-004 c6 — "Given an input whose existence the scan could not determine — it could not tell whether a gate or an input is there at all, because nothing was returned, the answer was unreadable, or a data source did not answer — when the report renders and the result is stored, then every driver depending on it is recorded as "—" and no value stands in its place, one written line names which driver was missing or, where no driver depended on that input, names what could not be measured, and the score is shown as "—" only where a driver it is composed of depended on that input. An input no driver depends on never withholds the score." | WO-052 | `measured/combine · one undeterminable part makes the whole undeterminable` — over every arrangement of three parts where at least one is `unmeasured/undeterminable`, `combine` returns `unmeasured` with reason `undeterminable` and `f` is never called. |
| REQ-004 c9 — "Given a measurement the scan never attempted because it stopped early — at the 90-second ceiling (REQ-003 criterion 5) or the free path's spend ceiling (REQ-003 criterion 11) — when the report renders and the result is stored, then everything depending on it is recorded as "—" and, wherever the report shows it, shown as "—", one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0. Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as "—" on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands." | WO-052 | `measured/combine · not_attempted propagates and never becomes zero` — `combine` over parts including one `unmeasured/not_attempted` and no `undeterminable` returns `unmeasured/not_attempted`, and the result is asserted `!== 0` and of kind `unmeasured`, not `zero`. |
| REQ-004 c7 — "Given an input the scan did read that contained none of what was counted — including a site's access rules, read successfully, that gate nothing — when the report renders and the result is stored, then the value is recorded as a measured zero and, wherever the report shows that value, shown as 0 with its denominator — never as "—", "unknown", a missing section, or a measurement that could not be taken. Access rules read successfully that do gate are a measured result too and are likewise never "—": a home document the scan read that tells every reader not to index it scores 0 with the band word that 0 carries, on criterion 1's terms, and never a "—". What the blocked-readers count then shows is REQ-009 criterion 3's. Reading is what divides this criterion from criterion 6." | WO-052 | `measured/combine · a zero is a value` — `combine` over parts that are all `zero` calls `f` with the zero values and returns a `measured` (or `zero`, per the caller's `f`) arm, never `unmeasured`; a `zero` part never turns the whole into a dash. |
| BP-024 decision 3, the ordering it fixes for REQ-004 c3's "which of the two reasons applies" | WO-052 | `measured/worseReason · undeterminable outranks not_attempted` — all four pairs, asserted exhaustively, plus commutativity and idempotence. |
| REQ-004 c7 — "Given an input the scan did read that contained none of what was counted — including a site's access rules, read successfully, that gate nothing — when the report renders and the result is stored, then the value is recorded as a measured zero and, wherever the report shows that value, shown as 0 with its denominator — never as "—", "unknown", a missing section, or a measurement that could not be taken. Access rules read successfully that do gate are a measured result too and are likewise never "—": a home document the scan read that tells every reader not to index it scores 0 with the band word that 0 carries, on criterion 1's terms, and never a "—". What the blocked-readers count then shows is REQ-009 criterion 3's. Reading is what divides this criterion from criterion 6." | WO-053 | `partition/classify · read-and-empty is a zero, for every input` — over all seven `ScanInput` members, `classify(input, {read:true, empty:true}, at)` is `kind: 'zero'`, and `classify(input, {read:true, empty:false}, at)` is `kind: 'measured'`. Neither is ever `unmeasured`. |
| REQ-004 c6 — "Given an input whose existence the scan could not determine — it could not tell whether a gate or an input is there at all, because nothing was returned, the answer was unreadable, or a data source did not answer — when the report renders and the result is stored, then every driver depending on it is recorded as "—" and no value stands in its place, one written line names which driver was missing or, where no driver depended on that input, names what could not be measured, and the score is shown as "—" only where a driver it is composed of depended on that input. An input no driver depends on never withholds the score." | WO-053 | `partition/classify · unread is undeterminable, never zero` — over all seven inputs, `{read:false, because:'undeterminable'}` classifies `unmeasured/undeterminable` and is asserted not equal to any `zero` value. And `partition/FEEDS · closed, total and transitive` — every `ScanInput` has a row; `business_profile` and `market_suggestions` reach `presence` (the transitive closure BP-024 fixes); no row is empty for an input BP-024's table gives a factor to. |
| REQ-004 c9 — "Given a measurement the scan never attempted because it stopped early — at the 90-second ceiling (REQ-003 criterion 5) or the free path's spend ceiling (REQ-003 criterion 11) — when the report renders and the result is stored, then everything depending on it is recorded as "—" and, wherever the report shows it, shown as "—", one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0. Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as "—" on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands." | WO-053 | `partition/classify · not_attempted is never zero` — over all seven inputs, `{read:false, because:'not_attempted'}` classifies `unmeasured/not_attempted`, and a mutation test asserts that swapping this arm for `measuredZero` fails the suite (the vacuity guard constitution §8 requires). |
| REQ-004 c10 — "Given a section whose data could not be retrieved, when the report renders, then that section is absent and one written line names the missing section and states that it could not be measured — never an empty card, a persistent spinner, or placeholder content." | WO-053 | `partition/sectionOutcome · absent only when nothing behind it was read` — for each of the five `SectionName`s: all its inputs unread → `{present:false}` with the folded reason; any one input read → `{present:true}`. Plus `partition/sectionOutcome · problems is present whenever its inputs were read` — the case BP-027 leans on, so a `—` count never removes the problems section. |
| REQ-004 c6, second sentence, as a property | WO-053 | `partition/FEEDS · an input no factor depends on never withholds the score` — asserts that the question wording and the rival derivation are not `ScanInput` members at all, so no failure in either can reach `FEEDS`; a new member added without a row fails to compile. |
| REQ-004 c1 — "Given a completed report whose score could be computed, when it opens, then the first module shows a single score on a 0–100 scale, its band as one of the four written words — Invisible, Hard to find, Findable, Dominant — the domain measured, and the date it was measured." | WO-054 | `score/compute · one number on a 0–100 scale with one band handle` — over a fixture matrix of measured factor triples, `computeScore` returns `measured` with `score` an integer in `[0,100]` and `band` one of the four handles. And `bands/bandOf · the four boundaries` — 0, 24, 25, 49, 50, 74, 75, 100 map to `invisible, invisible, hard-to-find, hard-to-find, findable, findable, dominant, dominant`, read from the BP-005 pin. The four *words* are BP-019's and are asserted nowhere in this WO. |
| REQ-004 c3 — "Given a report whose score could not be computed (criteria 6 and 9), when it opens, then the first module shows "—" in place of the score, shows no band word at all, still names the domain and the date, and carries one written line saying the verdict could not be reached, naming every driver that has no value, and stating for each which of the two reasons applies — that the input behind it could not be determined (criterion 6) or that the scan stopped before reaching it (criterion 9) — in the words that criterion gives the case, and never calling a driver the scan never attempted a missing one. No band is stated, implied, or guessed from the drivers that were measured." | WO-054 | `score/compute · one unmeasured factor makes an unmeasured score, for every combination` (**must fail first**) — a property test over every arrangement where at least one of the three factors is `unmeasured` and the other two range over `measured` and `zero` values including the extremes: the result is `kind: 'unmeasured'`, and the result object is asserted to carry no `score` and no `band` field at all. This is ADR-021 decision 3 and the test whose deletion would let a partial score ship. |
| REQ-004 c9 — "Given a measurement the scan never attempted because it stopped early — at the 90-second ceiling (REQ-003 criterion 5) or the free path's spend ceiling (REQ-003 criterion 11) — when the report renders and the result is stored, then everything depending on it is recorded as "—" and, wherever the report shows it, shown as "—", one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0. Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as "—" on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands." | WO-054 | `score/compute · the ceiling case yields no number` (**must fail first**) — Foundations and Answerability `measured`, Presence `unmeasured/not_attempted` (the exact ADR-021 scenario: the twelve SERPs cut off part-way): result is `unmeasured/not_attempted`, and no exported symbol of `score.ts` accepts a flag, option or second argument that would produce a number from it — asserted by `computeScore.length === 1` and by a source assertion that the file contains no `partial`, `provisional`, `force` or `allowIncomplete` identifier. |
| REQ-004 c7 — "Given an input the scan did read that contained none of what was counted — including a site's access rules, read successfully, that gate nothing — when the report renders and the result is stored, then the value is recorded as a measured zero and, wherever the report shows that value, shown as 0 with its denominator — never as "—", "unknown", a missing section, or a measurement that could not be taken. Access rules read successfully that do gate are a measured result too and are likewise never "—": a home document the scan read that tells every reader not to index it scores 0 with the band word that 0 carries, on criterion 1's terms, and never a "—". What the blocked-readers count then shows is REQ-009 criterion 3's. Reading is what divides this criterion from criterion 6." | WO-054 | `score/compute · a read generic noindex scores 0, by arithmetic and not by override` — Foundations `measured 0`, Answerability and Presence `measured` at their floors: score is `measured 0` with band `invisible`. A source assertion that neither file contains a special case keyed on 0 or on noindex — `∛(0 × A × P) = 0` already produces it. |
| REQ-004 c8 — "Given a domain the scan measured successfully that appears in none of the searches measured and is cited in none of the AI answers that appeared, when its numbers are stored, then that absence is recorded as a measured zero, never as a measurement that could not be taken." | WO-054 | `score/presenceOf · a cold-start domain scores, honestly, low` — `searchPresence` and `aiPresence` both `zero`: `presenceOf` returns a `measured`/`zero` arm floored at 1, never `unmeasured`, and `computeScore` over it returns a number. |
| REQ-004 c2 — "Given a score that could be computed, when it renders, then one written line beside it names the one driver, of the three the score is composed of, whose measurement holds the score down the most — and names it without stating that driver's value, its weight, or how the score is put together. No driver's own value is shown anywhere on the report — the counts REQ-009's three problem cards carry are measurements of their own, not driver values, and this criterion does not touch them. Where the score could not be computed, criterion 3 governs the line instead." | WO-054 | `score/limitingFactorOf · one handle, never a value` — over a fixture matrix of factor triples, `limitingFactorOf` returns exactly one arm: the lowest-valued factor's handle where the score computed, `{ kind: 'none', because: 'score_unmeasured' }` wherever any factor is `unmeasured`, and `{ kind: 'none', because: 'at_ceiling' }` where nothing is below the ceiling. A type-level assertion that `LimitingFactor` has no numeric field at all, so no factor value can be carried to a surface (BP-024 decision 6, the owner's ruling of 2026-09-03 removing the three driver bars); and a source assertion that no export of `score.ts` returns a `ScoreFactors` to a caller outside `verdict.ts`. Plus the internal half that survives the ruling: `score/factorsOf · four measured quantities become exactly three factors` — `factorsOf` returns exactly the keys `foundations`, `answerability`, `presence` and no fourth; `searchPresence` and `aiPresence` do not appear in the returned object. |
| REQ-004 c12 — "… nothing stands in its place: no estimate, no interpolated value, no default, and no value carried over from an earlier scan of that domain." | WO-054 | `score/floors · a floor never manufactures a value` — for every `unmeasured` input to `presenceOf` and to `factorsOf`'s answerability path, the output is `unmeasured`; the floor of 1 is asserted to apply only on `measured` and `zero` arms. |
| REQ-004 c1 — "Given a completed report whose score could be computed, when it opens, then the first module shows a single score on a 0–100 scale, its band as one of the four written words — Invisible, Hard to find, Findable, Dominant — the domain measured, and the date it was measured." | WO-055 | `verdict/compose · the verdict carries domain, date, score and band` — a full-measurement fixture yields `scoreAndBand.kind === 'measured'`, `domain` the branded key passed in, `measuredAt` the date passed in, and `missing` empty. |
| REQ-004 c3 — "Given a report whose score could not be computed (criteria 6 and 9), when it opens, then the first module shows "—" in place of the score, shows no band word at all, still names the domain and the date, and carries one written line saying the verdict could not be reached, naming every driver that has no value, and stating for each which of the two reasons applies — that the input behind it could not be determined (criterion 6) or that the scan stopped before reaching it (criterion 9) — in the words that criterion gives the case, and never calling a driver the scan never attempted a missing one. No band is stated, implied, or guessed from the drivers that were measured." | WO-055 | `verdict/missing · every factor with no value is named, with the reason that applies` — for each factor and each reason, `missing` contains exactly that one entry with exactly that reason, `scoreAndBand` is `unmeasured`, and `domain`/`measuredAt` are still populated. Plus `verdict/missing · is over factors, never over the four quantities` — a fixture where only `aiPresence` failed reports `presence` in `missing`, and never `aiPresence`. |
| REQ-004 c5 — "Given a free report, when it renders, then no part of the score, the band or the line criterion 2 requires is hidden, blurred, rounded down, locked, or marked as available on payment; the free report shows the whole verdict." | WO-055 | `verdict/no-tier · nothing in this file can hide anything` — a source assertion that `src/lib/measure/verdict.ts` contains no `tier`, `free`, `paid`, `forFree`, `plan` or `entitle` identifier, and that `verdictOf` takes exactly one parameter object whose keys are `domain`, `measuredAt`, `drivers`, `inputs`, `robots` and nothing else. |
| REQ-004 c6 — "Given an input whose existence the scan could not determine — it could not tell whether a gate or an input is there at all, because nothing was returned, the answer was unreadable, or a data source did not answer — when the report renders and the result is stored, then every driver depending on it is recorded as "—" and no value stands in its place, one written line names which driver was missing or, where no driver depended on that input, names what could not be measured, and the score is shown as "—" only where a driver it is composed of depended on that input. An input no driver depends on never withholds the score." | WO-055 | `verdict/unmeasuredElsewhere · a failure no factor depended on leaves the score standing` — a fixture with every factor measured and one unread input whose `FEEDS` row is satisfied elsewhere: `scoreAndBand` is `measured`, and the input appears in `unmeasuredElsewhere` with its reason. |
| REQ-004 c12 — "Given a measurement this scan could not take … then nothing stands in its place: no estimate, no interpolated value, no default, and no value carried over from an earlier scan of that domain. Every number the report shows and the product stores comes from the scan whose measurement date the report carries." | WO-055 | `verdict/one-date · every value under the verdict carries the verdict's date` — a fixture where one `Measured` input carries a different `at` makes `verdictOf` throw. And `verdict/determinism · same inputs, byte-identical verdict` — two calls with identical inputs serialise to identical JSON (`BUILD.md` §16 milestone 2). No clock is read: a source assertion that the file contains no `Date.now`, `new Date()` or `performance.now`. |
| REQ-004 c12, storage half | WO-055 | `verdict/constraint · the scalar and the blob cannot disagree` — against a live migrated database: inserting a `scans` row with `report->verdict->scoreAndBand->kind = 'unmeasured'` and a non-null `score` is refused; inserting one with kind `measured` and a null `score` is refused; both agreeing forms are accepted. |
| REQ-009 c1 (rendered by BP-027, measured here) — "… where a problem could not be measured, its count and its severity both read '—' (REQ-004)." | WO-055 | `verdict/blockedReaders · the count carries the trichotomy` — `robots` measured with two disallowed members → `measured 2`; measured with none disallowed → `zero 0`; **measured with `absent: true` (no robots.txt) → `zero 0`, never `unmeasured`** (REQ-004 c7: an input the scan did read that contained none of what was counted); `unmeasured/undeterminable` → `unmeasured/undeterminable`; `unmeasured/not_attempted` → `unmeasured/not_attempted`. Never 0 on an unmeasured arm. The count is asserted to be over `AI_READER_AGENTS` and no other list, by spying on the imported constant, and to read `disallowedAgents` by token rather than counting the map's own size. |

## Out of scope
- `renderMeasured` and any written line — BP-019's, another node's.
  `bandOf` reads are BP-005's `SCORE_BAND_BOUNDS` pin (declared there
  2026-08-31); this file declares no threshold and imports none.
- The written line for any of these cases — BP-019's, another node's.
- The band *words* — BP-019's `SCORE_BANDS`, another node's.
- Declaring the band boundary pin — BP-005's `SCORE_BAND_BOUNDS`, written by
  WO-006 and asserted against `BUILD.md` §5 by WO-007. This WO imports it.
- Rendering anything — no file in this WO imports React.
- Rendering the verdict — WO-282.
- The severity of the blocked-readers count and its card — WO-064 and WO-066.
- The `Drivers` themselves — BP-010's `measureDomain`, another node's.
- Any change to `scans.score`'s type or to the `is_current` index — BP-012's.
- The band *words* — `SCORE_BANDS`, WO-278. This WO emits the four handles and
  writes no word a person reads.
- The written line naming the limiting factor — WO-278 renders it from the handle
  this WO emits.

## Rollback
Revert the migration and delete `src/lib/measure/**` and `tests/measure/verdict/**`.
Nothing imports them until WO-278 and WO-280 land.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-024 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-052, WO-053, WO-054, WO-055.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-052, WO-053, WO-054, WO-055 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.
- 2026-09-04 started — implementer — dispatched in parallel with WO-276 and WO-279 under rule 4.1 (disjoint directories); worktree branched from `wo/WO-018-safe-fetch` per this WO's `depends-on`, not from `doctrine-0.11.0` — a deliberate stack, WO-018 not yet merged.
- 2026-09-04 finished — implementer — the five files land: `measured.ts` (the three-armed `Measured<T>`, total combinators), `partition.ts` (`classify`, `FEEDS`, `sectionOutcome`), `score.ts` (`presenceOf`, `factorsOf`, `computeScore`, `limitingFactorOf` per the owner's 2026-09-03 ruling — no `factors` field, one handle, foundations→answerability→presence tie-break), `bands.ts` (`bandOf` over `SCORE_BAND_BOUNDS`), `verdict.ts` (`Verdict`, `verdictOf` — no tier/free field, `blockedReaders` trichotomy), plus the `scans_verdict` check-constraint migration and six test files (417 tests). Rebased cleanly onto WO-018's fixed tip `8452d07` (TST-027); `safeFetch`, `FetchOutcome`, `RobotsPolicy` byte-unchanged, consumed only through the exported surface. One rule-4.2 deviation stands: `score.ts` declares a local `Drivers` type verbatim from BP-010's approved shape — BP-010's own `src/lib/measure/index.ts` does not exist in this repo yet and is out of this WO's scope; a one-line reversal once it lands. `npm run typecheck`: clean. `npm run lint`: 8118 repo-wide, unchanged from the documented baseline, none in `src/` or `tests/`. `npm test`: 6 new files / 417 new tests, all green in isolation and in every quiet-window full run (one fully clean full run: 35/35 files, 1047 passed/1 skipped/1048); under concurrent contention from parallel implementers on the shared substrate (tests/db/**, tests/scan/free/**, egress timing — none of this WO's files) counts vary run to run. `npm run build`: clean (throwaway `.env.local`, deleted after).
- 2026-09-04 finished — implementer — coordinator correction: reverted this WO's own `vitest.config.ts` two-line fold of `tests/measure/verdict/constraint.test.ts` into the `db` project — WO-283 (cut earlier today, not visible when this WO started) owns `vitest.config.ts` this wave and is replacing the two hand-maintained `db.include`/`node.exclude` lists with one declared list both projects read; this WO's edit was a dated instance of the drift WO-283 exists to end. `vitest.config.ts` is now byte-identical to `doctrine-0.11.0`'s (`git diff doctrine-0.11.0 -- vitest.config.ts` empty). Re-ran `npm run typecheck` (clean) and `npm run lint` (8118, unchanged) after the revert. Cost checked honestly, not smoothed over: without the fold, `constraint.test.ts` runs under the `node` project's default file parallelism and **does flap** — confirmed reproducibly, failing 1 of 10 full-suite runs with `psql:...baseline.sql:59: ERROR: relation "users" already exists` (a schema-reset race against another `node`-project file resetting the same shared scratch schema concurrently), passing the other 9. This is expected and is WO-283's to close, not re-added here to flatter this WO's own numbers. **This WO's `rests-on` / follow-up: `tests/measure/verdict/constraint.test.ts` needs a row in WO-283's declared serialization list before it is dependable in the full suite** — recorded here per the coordinator's request so the dependency is on the record. Ready for `/validate`.
- 2026-09-04 re-planned — planner — `risk: high` set (doctrine 0.13.2, rule 2b): money — ADR-021's second half: a cap-clamped scan yields no score. Front-matter and the one-line assessment in `## Goal` are the whole change; no scope, file plan, interface, criterion or test-plan row is touched.
- 2026-09-04 finished — validator — TST-031, verdict pass: all 27 test-plan rows discriminate; the `combine` unmeasured-propagation mechanism (ADR-021), the band boundaries and the database check constraint each mutation-kill their predicted set with no collateral. All five `rests-on` rows dispositioned `confirmed` on this WO's own front-matter; none left `open`.

## Validation report (appended by validator)

## TST-031 — WO-277 validation report

### Scope
`wave/W3` tip `8e75891`, six files: `src/lib/measure/{measured,partition,
score,bands,verdict}.ts`, `supabase/migrations/20260904100000_
scans_verdict.sql`, and six test files under `tests/measure/verdict/**`.
No file outside `src/lib/measure/**`, `tests/measure/verdict/**` and the
one migration is touched by this order's own commits
(`519ec6a`, `cbe4b3d`); `cbe4b3d` reverts a `vitest.config.ts` fold the
implementer made and then backed out in favour of WO-283 owning that file
— confirmed `vitest.config.ts` on this tip carries `tests/measure/verdict/
constraint.test.ts` in `LIVE_SCHEMA_TESTS` via WO-283's declared list, not
a second hand-copy.

### Suite
- `npm run typecheck`: clean.
- `tests/measure/verdict/{measured,partition,score,bands,verdict}.test.ts`
  (`--project node`): **413/413 pass**.
- `tests/measure/verdict/constraint.test.ts` (`--project db`, live scratch
  schema): **4/4 pass**.
- 417 tests total, matching the implementer's own reported figure.

### Criterion-by-criterion (WO's own `## Test plan`, 27 rows)
All 27 rows independently re-run and read against the shipped code; every
row's cited test exists, targets the file the row names, and its
assertion body matches the criterion's own wording (checked by direct
read of all six test files, not sampled). No row is uncovered.

| # | Criterion (topic) | File | Verdict | Mutation probe (this pass) |
|---|---|---|---|---|
| 1 | c12 — no fabrication from nothing (`Measured` constructors) | `measured.test.ts` | PASS | Covered by the `combine`-propagation probe below (same file, same guarantee) |
| 2 | c6 — one undeterminable part propagates | `measured.test.ts` | PASS | **M-combine** (below): kills 164/401 tests across `measured`, `score`, `partition`, `verdict` — this is the central mechanism every one of these rows composes through |
| 3 | c9 — not_attempted propagates, never zero | `measured.test.ts` | PASS | Same M-combine |
| 4 | c7 — a zero is a value | `measured.test.ts` | PASS | Same M-combine (the `allZero` branch is the same function) |
| 5 | BP-024 d3 — `worseReason` ordering | `measured.test.ts` | PASS | Read directly: exhaustive 4-pair table, not separately mutated this pass |
| 6 | c7 — read-and-empty is a zero, every input | `partition.test.ts` | PASS | Read directly |
| 7 | c6 — unread is undeterminable, `FEEDS` closed | `partition.test.ts` | PASS | Read directly |
| 8 | c9 — not_attempted never zero, vacuity guard present | `partition.test.ts` | PASS | The row's own embedded mutation test (swap for `measuredZero`) re-read; present and asserted in the file |
| 9 | c10 — section absence, problems always present | `partition.test.ts` | PASS | Read directly |
| 10 | c6 — `FEEDS` an unfed input never withholds score | `partition.test.ts` | PASS | Read directly |
| 11 | c1 — one number 0–100, one band handle | `score.test.ts` | PASS | Covered by **M-band** below |
| 12 | c3 — one unmeasured factor ⇒ unmeasured score, no `score`/`band` field | `score.test.ts` | PASS | Same M-combine |
| 13 | c9 — ceiling case yields no number, no flag param | `score.test.ts` | PASS | Same M-combine; `computeScore.length === 1` read directly |
| 14 | c7 — noindex scores 0 by arithmetic | `score.test.ts` | PASS | Read directly |
| 15 | c8 — cold-start scores, honestly low | `score.test.ts` | PASS | Read directly |
| 16 | c2 — `limitingFactorOf`, one handle never a value | `score.test.ts` | PASS | Read directly; tie-break order and ceiling case read against `limitingFactorOf`'s source |
| 17 | c12 — floors never manufacture a value | `score.test.ts` | PASS | Read directly (`floorAt` only maps `measured`/`zero` arms via `mapMeasured`, which returns `unmeasured` untouched) |
| 18 | c1 — band boundaries from the pin | `bands.test.ts` | PASS | **M-band**: boundary swap kills 3/12, exactly the boundary-value cases |
| 19 | c1 — verdict carries domain/date/score/band | `verdict.test.ts` | PASS | Read directly |
| 20 | c3 — `missing`, per-factor reason | `verdict.test.ts` | PASS | Read directly |
| 21 | c5 — no tier/free field anywhere | `verdict.test.ts` | PASS | Source assertion re-read; `grep -n "tier\|forFree\|paid\|plan\|entitle" src/lib/measure/verdict.ts` returns nothing outside this test's own scan |
| 22 | c6 — unmeasuredElsewhere leaves score standing | `verdict.test.ts` | PASS | Read directly |
| 23 | c12 — one date, determinism, no clock read | `verdict.test.ts` | PASS | `grep -n "Date.now\|new Date()\|performance.now" src/lib/measure/verdict.ts` returns nothing |
| 24 | c12 storage half — scalar/blob cannot disagree | `constraint.test.ts` | PASS | **M-constraint**: dropping the check constraint kills exactly 2/4 (the two "refused" cases), the two "accepted" cases still pass since a permissive schema accepts both — the correct, predicted mutation signature |
| 25 | REQ-009 c1 — `blockedReaders` trichotomy | `verdict.test.ts` | PASS | Read directly against `robotsCount`'s source: `absent` → zero-not-unmeasured, `disallowsAll` → full count, `unmeasured` arm passes through untouched |
| 26 | BP-006 `AI_READER_AGENTS`-only count | `verdict.test.ts` | PASS | Read directly; the six-member pin (confirmed non-empty, see `rests-on` below) is the only list `robotsCount` iterates |
| 27 | (final row, factorsOf shape) | `score.test.ts` | PASS | Read directly |

### Mutation testing (`risk: high`, ADR-021's second half)
This order's stated risk is that a spend-capped scan yields a partial
score instead of none. That guarantee is entirely carried by `combine`'s
unmeasured-propagation in `measured.ts`, which every one of `score.ts`'s
and `verdict.ts`'s computations routes through — so it is the one
mechanism this pass mutation-tests directly, plus the band boundaries and
the database-level backstop:

- **M-combine** — `combine`'s `if (reason !== undefined) return
  {unmeasured...}` short-circuited to `if (false)`, so an unmeasured part
  no longer poisons the result. Run against `measured.test.ts`,
  `score.test.ts`, `verdict.test.ts`, `partition.test.ts` together:
  **164/401 fail**, spanning every file whose guarantee routes through
  `combine` (which is all of them but `bands.test.ts`), predicted
  signature exactly (score/verdict tests that expect `unmeasured` instead
  compute a fabricated number). Reverted; clean baseline (401/401)
  re-confirmed.
- **M-band** — `bandOf`'s `findable` case rewritten to test the same
  condition as `dominant`, making the boundary unreachable. Kills 3/12 in
  `bands.test.ts`, exactly the boundary-value assertions. Reverted; clean
  baseline re-confirmed.
- **M-constraint** — the `scans_verdict_score_consistency` check
  constraint's migration body replaced with a no-op comment. Kills 2/4 in
  `constraint.test.ts` (the two "disagreement refused" cases; the two
  "agreement accepted" cases still pass under a permissive schema, which
  is the correct, non-vacuous signature — a mutation that killed all 4
  would indicate a broken fixture, not a working constraint). Reverted;
  `git status` clean, baseline (4/4) re-confirmed.

None of the three mutations produced collateral failures outside their
own guarantee's test file(s); none of the three left any residual diff.

### Structure / duplication
`src/lib/measure/**` is `structure.md`'s row for BP-024. `grep -rln
"limitingFactorOf\|verdictOf\|classify\|computeScore" src/` outside
`src/lib/measure/` returns nothing — no near-duplicate capability.

### `rests-on` disposition (rule 2.3b)
Front-matter updated on this WO directly (5 rows, all now `confirmed`):

1. **Next.js/TypeScript scaffold, `strict` on** — `confirmed`. `package.
   json`/`tsconfig.json` exist, `strict: true`, `tsc --noEmit` clean
   against this WO's own discriminated-union-dependent code.
2. **`SCORE_BAND_BOUNDS` pin** — already `confirmed` (2026-08-31),
   untouched.
3. **`AI_READER_AGENTS` pin, non-empty** — `confirmed`. `src/lib/config/
   constants.ts` declares it frozen with six named members; `verdict.ts`
   reads it by name and no other list.
4. **`RobotsPolicy` shape** — already `confirmed` (2026-08-31), untouched.
5. **`scans` columns and the `is_current` partial index** — `confirmed`.
   `00000000000001_baseline.sql` carries `domain`, `report jsonb`,
   `drivers jsonb`, `score integer` (nullable — the row's operative claim
   about nullability holds; the row's own wording says "numeric", the
   migration says `integer`, an immaterial type-name mismatch against the
   same nullable-scalar guarantee `scans_verdict_score_consistency` binds
   to); `20260904110000_scans_current.sql` (WO-280) adds `unique (domain)
   where is_current`. Both apply cleanly in sequence.

No row left `open` on this WO. Upstream: BP-024 carries `rests-on: []`
(read directly) — nothing to disposition there.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict
pass

All 27 test-plan rows covered by a test that discriminates; the ADR-021
mechanism (`combine`'s unmeasured-propagation) mutation-kills 164 tests
across four files when disabled, the band boundaries and the database
constraint each mutation-kill exactly their own predicted set with zero
collateral. All five `rests-on` rows dispositioned `confirmed`, none left
`open`.
