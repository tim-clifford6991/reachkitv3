---
id: WO-234
type: work-order
title: "verifyLive() — one fetch, three outcomes, and four Measured<boolean> in the one arm that has them"
status: approved
ui: no
implements: [BP-049, REQ-062, ADR-084, ADR-085]
depends-on: [WO-006, WO-018, WO-206, WO-208, WO-231, WO-232, WO-233, WO-261, WO-262]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-234 — verifyLive() — one fetch, three outcomes, and four Measured<boolean> in the one arm that has them

- Branch: wo/WO-234-verify-live
- Estimate: ≤1 day

## Goal
Ship `verifyLive()`: fetch the page's own live address **once, ever**, 24 hours after it published, and record exactly one of REQ-062 criterion 4's three outcomes — the four `Measured<boolean>` checks where the page answered, that no page was found, or that the check could not be confirmed — with the four checks produced in the `found` arm and in no other.

## File plan

> This repository carries no `src/`, no `tests/`, no `supabase/` and no
> `package.json` (verified 2026-08-31). **Every path below is NEW** — none was
> verified to exist, because none can. Placement is anchored to
> `sdlc-factory/docs/registry/structure.md` and to this work order's blueprint
> `code:` globs, and is provisional against a repo with no code: if the
> implementer finds a file already present at one of these paths, they stop and
> log `failed` naming the drift rather than merging into it.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/publish/verify/verify.ts` | NEW | `CheckId`, `verifyLive`, and the **re-export** of `VerifyChecks`, `VerifyOutcome`, `NotConfirmed` and `VerifyDisposition` from `src/lib/publish/types.ts` (BP-045 decision 9). `verifyLive` makes **one** `safeFetch` GET of `live_url` and hands the answer to WO-261's `classify`, which returns one of three arms. **`page_not_found` and `could_not_confirm` produce no checks, no failure of the page and no cause** — `verifyLive` records the arm and returns. **The four checks are produced in the `found` arm and in no other.** `reachable` — **`true` in every `found` result**, because that arm is only ever reached by a 200 carrying our page; it is kept, not deleted, for the reason under Interfaces. `indexable` — true when neither the response's `X-Robots-Tag` nor the document's `<meta name="robots">` marks it against indexing; **both are checked, because either alone can be the one that is set**. `sitemap` — one `safeFetch` of the sitemap the site publishes: BP-047's document for a hosted page, and `/{sitemap_index,sitemap}.xml` plus whatever `robots.txt` names for a site ReachKit does not serve. `aiReadable` — the same document as the page fetch, no script execution of any kind, true when `bodyCoverage(html, drafts.body_md) ≥ VERIFY.coverageFloor`. Records a `SiteCondition` (WO-262) **only from an answer the site gave**; where the sitemap or robots document did not answer or could not be parsed, that one check is `unmeasured` **for this page alone**, no condition is written and no later page's check is suppressed. Inside `found`, a check that could not be read records `unmeasured` with its reason, **never `false`**. Never throws for a failing check and never retries one; a failure of the *job* writes nothing and leaves the row due. Writes only `publications.verify`. |
| `tests/publish/verify/verify.test.ts` | NEW | **The three-arm rows:** a 404 and a 410 each produce `page_not_found` with the status and `checkedAt` and **no `checks` field at all**; a 500, a 302 away from the page, a DNS failure, a reset, a timeout and a 200 that is not our page each produce `could_not_confirm` with its `NotConfirmed` reason, `checkedAt`, and **no `checks` field**; a 200 carrying our page produces `found` with four `Measured<boolean>`. **The two arm-separation rows:** `page_not_found` is never recorded for anything but 404 or 410 — asserted over a table of every other status the fixtures cover — and `could_not_confirm` never sets `reachable: false` or any other check, so ReachKit's own network problem is never turned into an accusation against the customer's page. **Inside `found`:** each check true and false from its own fixture; an `X-Robots-Tag` alone and a meta tag alone each make `indexable` false; a sitemap or robots document that did not answer leaves that one check `unmeasured`, writes no `SiteCondition`, and **suppresses no check for a later page on the same site**; nothing anywhere defaults a `Measured<boolean>` to `false`. A failing check does not throw and is not retried; a job failure before any outcome was written writes nothing and leaves the row due; the request carries `VERIFY.userAgent` and **never one of the six named AI agents**. |

## Interfaces

**Consumes**

- `safeFetch()` — BP-006, `src/lib/egress/`. ≤ 10 s timeout and ≤ 2 MB per fetch.
- `Measured<T>` — BP-010, `src/lib/measure/`. Reused, not re-invented (BP-049 decision 1, rule 7.2).
- `dispositionFor`, `dueNow` — WO-232. `bodyCoverage` — WO-233.
- **`classify` and `isOurPage` — WO-261, `src/lib/publish/verify/answer.ts`.** This module makes the fetch and hands the answer over; it contains **no status mapping of its own**, which is what keeps ADR-085 Decision 2's whitelist in one place.
- **`SiteCondition` and `siteConditionFor` — WO-262, `src/lib/publish/verify/site.ts`.** This module records one on the check that found it and reads back what an earlier check learned.
- `VerifyChecks`, `VerifyOutcome`, `NotConfirmed`, `VerifyDisposition` — `src/lib/publish/types.ts` (WO-206), declared in BP-045's leaf and re-exported here (BP-045 decision 9).
- `VERIFY.coverageFloor` and `VERIFY.userAgent` — BP-005, `src/lib/config/constants.ts`, asserted in `tests/pins.test.ts`.
- The sitemap document it fetches — BP-047, WO-231.
- `publications.verify` — WO-208 and `BUILD.md` §10.

**Exposes** — copied exactly from BP-049's `## Public interface`:

```ts
// src/lib/publish/verify/verify.ts
export type CheckId = 'reachable' | 'indexable' | 'sitemap' | 'aiReadable'

/** `VerifyChecks`, `VerifyOutcome`, `NotConfirmed` and `VerifyDisposition` are
 *  **declared in BP-045's `src/lib/publish/types.ts`** and re-exported here, so
 *  every caller's spelling is what it reads below. They live in the leaf because
 *  BP-045's `record/` must name the recorded outcome for REQ-062 criterion 7 and
 *  `types.ts` imports nothing — declaring them here would make BP-045 → BP-049 →
 *  BP-045 a node cycle and a build failure (BP-045 decisions 6 and 9). The
 *  behaviour behind every arm is this node's. */
export type { VerifyChecks, VerifyOutcome, NotConfirmed, VerifyDisposition } from '../types'

/** REQ-062 criterion 4's three outcomes of the one check, as three arms.
 *  **ADR-085 (landmine).** `checkedAt` is on every arm because criterion 4 and
 *  criterion 7 both require the date on every one of them. There is no fourth
 *  arm and no "pending": a check that has not run yet is a `VerifyDisposition`,
 *  not an outcome. */
export type VerifyOutcome =
  | { outcome: 'found';              checks: VerifyChecks; checkedAt: Date }
  | { outcome: 'page_not_found';     status: 404 | 410;    checkedAt: Date }
  | { outcome: 'could_not_confirm';  why: NotConfirmed;    checkedAt: Date }

export function verifyLive(publicationId: string): Promise<VerifyOutcome>
```

**One fetch, three outcomes, never a fourth — ADR-085 (landmine).** A 404 or a
410 **and no other answer** is `page_not_found`. **Any** other answer — no HTTP
answer at all, a 5xx, a redirect away from the page, or a document that is not
the page ReachKit published — is `could_not_confirm`. The mapping is WO-261's and
is a **whitelist of two statuses, never a blacklist of failures**: a blacklist
admits an unclassified answer into `page_not_found` by default, and the default
arm must be the one that asserts nothing.

**Neither of those two arms produces the four checks, states a failure of the
page, or names a cause.** REQ-062 criterion 4 forbids all three in terms. In
particular, **`could_not_confirm` must never be folded into `reachable: false`**:
that turns ReachKit's own network problem into an accusation against the
customer's page. It reads as tidying up an obviously incomplete union and it
passes every test that would exist without the two arm-separation rows in the
file plan.

**Neither is retried, rescheduled or repeated.** That is `dueNow`'s
(WO-232) and this module adds nothing: `verifyLive` runs once per publication and
returns. A retry here is the single most likely wrong change anyone will make to
this node and ADR-085's warning block leads with it.

**`reachable` is `true` in every `found` result, and is not deleted for it.**
Every way a page could be unreachable now lands in criterion 4's third outcome,
so the arm that produces the four checks is only ever reached by a 200 carrying
our page. The check therefore has one value in production and reads exactly like
a field nobody needs. It is kept because REQ-062 criteria 1, 2 and 3 promise the
customer **four** outcomes with their dates, and because criterion 3 requires all
four to state that they have not been checked yet in the 24 hours before the
check runs — a promise a three-outcome surface cannot keep. Same shape as
ADR-084's `made_live_by_us`: a constant field that is load-bearing for a
rendered promise. **Deleting it is a surface change, not a cleanup.**

**The user agent is our own token.** BP-049 states it plainly: it is permitted
because the robots document BP-047 serves blocks no general crawler (REQ-059
criterion 4), **it is not one of the six named AI agents, and impersonating one
of them would be a false statement to a server we are measuring.** `VERIFY.userAgent`
is a pin in BP-005 and a test asserts it matches none of `AI_CRAWLERS`.

**The four outcomes are `Measured<boolean>`, not booleans** (BP-049 decision 1).
`boolean | null` reads as false in every language the surface will pass through,
and the reason a check did not run would be lost. Nothing anywhere defaults a
`Measured<boolean>` to `false`.

## Steps
1. Create `verify/verify.ts` with the block above verbatim, re-exporting the four shapes from `../types` rather than declaring them.
2. Make **one** `safeFetch` GET of `live_url` and hand the answer straight to WO-261's `classify`. **Write no status mapping in this file** — assert its absence in test, so the whitelist stays in one place.
3. On `page_not_found` and on `could_not_confirm`, record the arm and return. Produce no checks, name no cause, and make neither of the other two fetches: they exist to decide checks those arms do not produce.
4. On `found`, run the remaining three checks. Check both the header and the meta tag for indexability. Fetch the sitemap the site publishes — BP-047's for a hosted page, `/{sitemap_index,sitemap}.xml` plus whatever `robots.txt` names otherwise.
5. Run the `aiReadable` evaluation over the same document, with no script execution of any kind and with `VERIFY.userAgent`. Never send one of the six named AI agent strings.
6. Record a `SiteCondition` only from an answer the site gave. Where the sitemap or robots document did not answer or could not be parsed, leave that one check `unmeasured` for this page alone, write no condition, and suppress nothing for any later page. **Any suppression keyed on "could not be reached" is wrong twice over** — that case moved to criterion 4's third outcome, and criterion 6 may not fire on a non-answer at all.
7. Write `publications.verify` as the serialised `VerifyOutcome` — the **discriminant stored**, so a reader cannot obtain the four outcomes without having gone through the arm that produces them. Write nothing on a job failure.
8. Write the two arm-separation tests first and watch them fail against (a) an implementation that maps any non-2xx to `page_not_found` and (b) one that records `could_not_confirm` as `reachable: false`. Both are the merges ADR-085 exists to stop and both read as tidier code (constitution §8).

## Test plan

Each criterion below is inherited verbatim from the requirement named in the
row, reflowed onto one line and otherwise unaltered.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| **REQ-062 c1** — Given a page published to a destination that serves it publicly, when 24 hours have passed since it published, then its live address is fetched and the product records whether the page is reachable, is not marked against indexing, is present in the sitemap, and returns its whole page content to a crawler that runs no scripts and identifies as one the robots policy REQ-059 serves permits. | `tests/publish/verify/verify.test.ts` › "24 hours after it published, the live address is fetched and the four outcomes are recorded" — reachable, not marked against indexing, present in the sitemap, and returning its whole content to a crawler that runs no scripts and identifies as one the robots policy permits; the last is asserted to use `VERIFY.userAgent` and none of the six named agents. |
| **REQ-062 c3** — Given a check the page itself did not pass, when the outcome is shown, then the failure is stated plainly rather than omitted or shown as pending; and in the 24 hours before the check runs, the four outcomes state that they have not been checked yet and when they will be, never that they passed or failed. | `tests/publish/verify/verify.test.ts` › "a check the page itself did not pass is stated plainly rather than omitted or shown as pending" — a false outcome inside `found` is a recorded `Measured` value, distinct from `unmeasured`; and › "an outcome that was not observed is never stated", the unparseable-sitemap fixture. The criterion is scoped to **the page's own** failures: criterion 4's second and third outcomes are governed by criterion 4 and not by this row. |
| **REQ-062 c4** — Given a page ReachKit's own record shows was unpublished or taken down before its check was due, when the customer inspects it, then no check is run and it states that the page was taken down before it could be verified. Given instead the check runs and the address answers, unambiguously, that there is no such page — a 404 or a 410, and no other answer — then this criterion governs the outcome and not criterion 3: it states that no page was found at that address when the check ran, carrying the date it was looked for; it names no cause and never says who removed the page or that the customer did; it is never stated as a failure of publishing or of any of the four checks; and no further check is run for that page. Given any other answer — the site could not be reached, the address returned a server error, it redirected away from the page, or what came back was not the page ReachKit published — then neither the four outcomes nor the page's absence is asserted: it states that the check could not be confirmed, carrying the date it was tried, the page is not recorded as no longer there, nothing is stated as a failure of this page, and no further check is run for it. | `tests/publish/verify/verify.test.ts` › "a 404 or a 410 and no other answer records that no page was found, with the date" — two fixtures, each producing `page_not_found` with its status and `checkedAt` and no `checks`; › "every other answer records that the check could not be confirmed, with the date" — six fixtures across the four `NotConfirmed` reasons; › **"neither arm produces the four checks, states a failure of the page, or names a cause"** — the returned value has no `checks` field and `NotConfirmed`'s members attribute nothing; › **"page_not_found is never recorded for anything but 404 or 410"** — a table over every other status in the fixtures. The `taken_down_first` limb is WO-232's and the "no further check" limb is WO-232's `dueNow`. |
| **REQ-062 c6** — Given a check that did not pass at a destination ReachKit does not serve, when its outcome is shown, then it says whether what did not pass is this page or the site the page sits in. A site that answered that it publishes no sitemap, or whose own robots policy blocks the crawlers criterion 1 identifies as across that whole site, is recorded as a condition of that site — named, carrying the date it was found, and stated as the customer's own to act on — and no page on that site is marked as having failed a check for that cause, then or on any later page. A condition of the site is recorded only from an answer the site gave: where ReachKit could not reach what it went to read, or could not read what came back, no condition of that site is recorded, nothing about that site is stated as the customer's to act on, that one check is stated as not confirmed for this page alone, and no check is suppressed for any later page. A page absent from a sitemap the site does publish, or blocked where the rest of that site is not, is that page failing and criterion 3 governs it. | `tests/publish/verify/verify.test.ts` › "a site that answered that it publishes no sitemap is recorded as a condition of that site, dated, and no page is marked as having failed for it" — the affected check is `unmeasured` with that reason, never `false`; › "a sitemap that did not answer, or that came back unparseable, records no condition and suppresses nothing for a later page" — **the discriminating row**: an implementation keyed on "could not be reached" passes the first and fails this one; › "a page absent from a sitemap the site does publish is that page failing" — `false`, stated plainly. The condition's storage and read-back are WO-262's. |
| **REQ-062 c2** — Given verification has run, when the customer inspects that page, then each of those four outcomes is shown, with the date and time it was checked in the time zone the customer set. | `tests/publish/verify/verify.test.ts` › "each of the four outcomes is recorded with the moment it was checked" — `checkedAt` on the result; rendering it in the customer's zone is BP-019's and BP-039/BP-001's. |

## Out of scope
- Repairing anything. A failed check produces no retry, no republish and no transition (REQ-062's non-goal). The customer reads it.
- **Classifying the answer.** The two-status whitelist and the identity test are WO-261's `answer.ts`. This module fetches and records; it maps nothing.
- **Storing and reading back a site's condition** — WO-262's `site.ts`.
- Re-verifying — WO-232's `dueNow` selects only `verify is null`. **Adding a retry, a backoff or a "pending" render for `could_not_confirm` is forbidden, not merely out of scope** (ADR-085 Decision 3; REQ-062's non-goal).
- Rendering the four outcomes — BP-019's `renderMeasured` and BP-039/BP-001's surface. Handing the recorded outcome to a surface with the page's state is BP-045's page record, WO-263.
- **What the two not-measured outcomes do downstream.** `page_not_found` stops the page being shown as live (REQ-060 c2) and is a terminal not-judgeable cause; `could_not_confirm` does neither and the page goes on being judged with the outcome shown beside the verdict. Those consequences are BP-051's, BP-039's and BP-045's, and **ADR-085 Decision 4's table is where they are stated**. This module records the arm and nothing more.
- The message — WO-235.
- Traffic, ranking or conversion analytics — REQ-062's non-goal; the weekly verdict is REQ-063's.

## Rollback
Delete both files. `dispositionFor`'s `done` arm then has no producer, so revert together with WO-232 or leave the row perpetually due — the latter is safe, since `dueNow` is idempotent.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-049 — `status: approved`; ADR-084 and ADR-085 — `status: accepted`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-062 as it stands on disk on 2026-09-02; criterion 3 was re-extracted in this pass and criteria 4 and 6 are new rows.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

**The gate this amendment does not meet.** The 2026-09-02 edits — the three-arm
outcome, the site condition, and WordPress joining the verified population —
derive from **REQ-062, `status: in-review`**, and read **REQ-063** and
**REQ-060** (`in-review`, both). All three carry zero open `REVIEW(...)` lines
after two full review rounds (rule 3.4), so the text is **settled but
unapproved**, and BP-049's own status-grounds addendum says the same of the
design. This work order does not claim §3's gate is met for these edits; the
status is asserted on the design under rule 3.2, and the librarian may revert it
retroactively and name the clause. No `blocked-by` is added (rule 2.3).

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-006, WO-018, WO-231); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — re-extracted against ADR-085 and ADR-084: `verifyLive` now returns the three-arm `VerifyOutcome` rather than four bare `Measured` values, the four checks are produced in the `found` arm and in no other, `reachable` is documented as the constant-in-production check that is load-bearing for a rendered promise, the status mapping moves out to WO-261's `answer.ts` and the site condition to WO-262's `site.ts`, and two arm-separation rows are added — `page_not_found` only on 404/410, and `could_not_confirm` never folded into `reachable: false`; criterion 3 re-extracted, criteria 4 and 6 added; `depends-on` gains WO-261 and WO-262, `implements` gains ADR-084 and ADR-085.

## Validation report (appended by validator)

