---
id: WO-111
type: work-order
title: "captureLead() — one insert, confirmed only after it commits"
status: approved
ui: no
implements: [BP-029, REQ-010, REQ-003]
depends-on: [WO-011, WO-109, WO-110]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-111 — captureLead() — one insert, confirmed only after it commits

- Branch: wo/WO-111-capture-lead

- Estimate: ≤1 day

## Goal
Accept an address, write one `leads` row carrying the scan's domain, and fail
closed — refusing in writing and confirming nothing — when the store will not take
it.

## Provisional against an empty repo
**No `src/` exists.** Both files NEW, inside BP-029's `src/lib/mail/leads/**`
glob.

Reuse check (rule 7.2): no lead capture exists in the index or on disk.
`dbAdmin()` (BP-002) is the server-only client this WO consumes — `leads` is
RLS default-deny and a visitor holds no session, so the insert runs server-side
through the admin client from BP-001's thin route adapter, never from a browser.

## Read ADR-041 before adding a suppression check here
Capture does **not** consult `email_suppressions`. A suppressed address that
submits the control for a different domain still gets that domain's page —
REQ-010 c3 against c11 — and the mechanism that makes it so is the register row
`stoppable: false`, not a branch here. Adding one "for safety" is the third of
ADR-041's three honest-looking fixes.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/mail/leads/capture.ts` | NEW | `captureLead()` verbatim from BP-029's signature. Validates the address's **form** only, then one insert into `leads` carrying `scan_id`, lowercased `email`, and `domain` read from the scan (BP-029 decision 1: written at capture, thereafter read-only), with `sequence_state` null and `first_page_state` `pending`. Returns `{ captured: true, leadId }` only after the row commits. Any store failure returns `{ captured: false, reason: 'unavailable' }`; a malformed address returns `{ captured: false, reason: 'invalid-address' }` and writes nothing. Enqueues nothing and calls no model — writing and sending happen in the job (BP-029 decision 3). |
| `tests/mail/leads/capture.test.ts` | NEW | The two refusal reasons, the commit-then-confirm order, the domain write, and the negative property that nothing is enqueued. |

## Interfaces
**Exposes** (verbatim from BP-029 `## Public interface`):
```ts
// src/lib/mail/leads/capture.ts
/** Criterion 1's control accepts an address and nothing else. Criterion 3's
 *  delivery is enqueued, never awaited: the visitor's confirmation states the
 *  address was accepted, which is the only thing true at that moment. */
export function captureLead(a: {
  scanId: string
  email: string
}): Promise<
  | { captured: true; leadId: string }
  | { captured: false; reason: 'invalid-address' | 'unavailable' }>   // REQ-003 c10
```
**Consumes**: `dbAdmin()` (BP-002); the `leads` schema (WO-109);
`firstPageOffer()` (WO-110) only to read the scan's domain and to refuse a
capture for a scan with no offer.

## Steps
1. Write `capture.test.ts` first and watch it fail.
2. Implement form validation, then the insert, then the return. In that order:
   the confirmation is the last statement in the function, so no code path can
   confirm before the commit.
3. Assert the module's dependency set contains no queue, no `llm()`, and no
   `generateDraft` import (BP-029 NFR budget: capture is "one insert; p95 under
   300 ms").
4. Mutation check: move the return above the insert's await and confirm the
   fail-closed test fails; add an `email_suppressions` read and confirm the
   ADR-041 dependency assertion fails.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-003 c10 — "Given lead capture is unavailable, when a visitor submits their email address for the free page, then the submission is refused in writing and no confirmation of delivery is shown." | `tests/mail/leads/capture.test.ts` → `a store failure returns { captured: false, reason: 'unavailable' } and writes nothing` — the refusal is a value the surface renders a written line from; this seam confirms nothing. Explicitly contrasted in the test name with the scan limiter, which fails **open** (`BUILD.md` §11's deliberate asymmetry). |
| REQ-010 c1 — "Given a completed report for which at least one page was found worth writing, when it renders, then it shows the title of the first page ReachKit would write, how many pages in total were found worth writing for that domain, the search that page targets, the rival that page would take ground from, the page's format, and one control to have the full page emailed — a control that asks for an email address and nothing else: no account, no password, no payment, no further field. Where the page was found without a rival to name, that row says so in writing rather than standing blank or naming an invented one (REQ-091 criterion 3)." | `tests/mail/leads/capture.test.ts` → `captureLead's argument type admits a scanId and an email and nothing else` — the "nothing else" clause, asserted at the type level: no account, no password, no payment and no further field are representable in the call. The rendered control is BP-001's; the six facts are WO-110's. |

## Out of scope
- REQ-010 c14, the written line beside the control. It is a sentence on BP-001's
  surface, rendered from a BP-019 copy key; the string is the owner's (REQ-093
  c1, constitution §1) and neither this WO nor any WO in this block authors it.
- Writing or sending the page (WO-115) and starting a sequence (WO-114).
- Any suppression check (ADR-041).
- The route handler — a thin adapter in `src/app/api/**`, BP-001's
  (`structure.md` rule 1).

## Rollback
Delete `src/lib/mail/leads/capture.ts` and its test. WO-109's schema stands and
holds no rows.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-029 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 2 rows of `## Test plan` quote REQ-010, REQ-003 as they stand on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
