---
id: WO-008
type: work-order
title: "Supabase project config and the migration naming convention check"
status: approved
ui: no
implements: [BP-002]
depends-on: [WO-001]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-008 — Supabase project config and the migration naming convention check

- Branch: wo/WO-008-supabase-config
- Estimate: ≤1 day

## Goal
Stand up the local Supabase project and make `structure.md`'s closed topic-token
list executable, so a migration whose name carries no assigned token fails a
check rather than a review.

## Provenance of this file plan
**This repository holds no `supabase/`.** Every path is NEW, anchored to
`structure.md`'s row for `supabase/**` and to BP-002's `code:` globs.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `supabase/config.toml` | NEW | Local project config: Postgres version, the auth block with magic-link (email OTP) enabled and password sign-in disabled — `BUILD.md` §1, "Supabase (Postgres, RLS default-deny, magic-link auth)" — and the migrations directory. No secrets; keys come from `env` (WO-005). |
| `src/lib/db/topics.ts` | NEW | The closed topic list and the open sub-token list as data, transcribed from `structure.md` rule 3 and 3a: topics `users · sites · scans · fetches · opportunities · drafts · publications · destinations · leads · suppressions`, plus `domainblocks`, `baseline` and `rls` as BP-002's own; sub-tokens `users_provisioning`, `users_subscription`, `users_identity`, `users_erasure`, `sites_provisioning`, `sites_hosting`, `sites_erasure`, `scans_freepath`, `scans_verdict`, `opportunities_core`, `opportunities_supply`, `opportunities_verdicts`, `drafts_core`, `drafts_claims`. Each entry carries its owning BP id. |
| `tests/db/migration-naming.test.ts` | NEW | Reads every file in `supabase/migrations/` and asserts its name carries exactly one assigned topic token or one sub-token of an assigned topic; a name carrying none, or two, fails. |

## Interfaces
Exposes:
```ts
// src/lib/db/topics.ts
export const MIGRATION_TOPICS: readonly { token: string; owner: string }[]
export const MIGRATION_SUBTOKENS: readonly { token: string; parent: string; owner: string }[]
export function topicOf(filename: string): { token: string; owner: string } | null
```
Consumes: nothing.

## Steps
1. `supabase init`; edit `config.toml` for magic-link auth.
2. Write `topics.ts` from `structure.md` rule 3 and 3a, one entry per token.
3. Write the naming test with fixture filenames: a valid `*_baseline*.sql`, a
   valid sub-token `*_users_erasure_*.sql`, an unassigned `*_widgets*.sql` and an
   ambiguous two-token name. Watch the unassigned case fail before the check
   exists.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-002 carries
> `satisfies: []` and was cut from the charter's stack table and `BUILD.md` §10.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-002 decision 2: "A migration whose name carries no assigned topic token fails the registry check." | `tests/db/migration-naming.test.ts` — the unassigned fixture fails; removing the check makes it pass, which is the mutation check. |
| `structure.md` rule 3a: "**A sub-token is not a new topic** and needs no entry in the closed list above." | Same file — the `*_users_erasure_*.sql` fixture passes and resolves to BP-063 as its owner, not to BP-017. |
| `structure.md` rule 2: "the narrower glob owns the file" | Same file — `topicOf()` on a sub-token name returns the sub-token's owner, never the parent topic's. |
| `BUILD.md` §1: "Supabase (Postgres, RLS default-deny, magic-link auth)" | `tests/db/migration-naming.test.ts` companion assertion — `config.toml` enables email OTP and disables password sign-in. |

## Out of scope
- The baseline schema — WO-009. The policies — WO-010. The clients — WO-011.
- Hosting the Supabase project, key rotation, and any remote apply.

## Rollback
Delete the three files and the `supabase/` directory. No schema has been applied.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-002 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan` quote BP-002's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
