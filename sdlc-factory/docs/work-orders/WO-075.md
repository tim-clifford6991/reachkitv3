---
id: WO-075
type: work-order
title: "phraseQuestions — the second nano call, and the template fallback that never drops a question"
status: approved
ui: no
implements: [BP-025, BP-011, REQ-006]
depends-on: [WO-022, WO-026, WO-027, WO-074]
blocked-by: []
rests-on:
  - claim: "BP-009's `LlmCallSite` union already carries `'question-phrasing'`, so this WO adds no call site to `src/lib/llm/` (BP-009's file). If the member is absent when this WO starts, that is a `blocked-by` on BP-009's work order, added at that moment."
    disposition: open
supersedes: []
---

# WO-075 — phraseQuestions — the second nano call, and the template fallback that never drops a question

- Branch: wo/WO-075-phrase-questions
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/questions/phrase.ts` so each selected search gains a wording
through the `'question-phrasing'` nano call while carrying its own search unchanged, and a
failed phrasing degrades to a template rather than removing a question from the denominator.

## Context an implementer needs
No source code exists on disk; both paths below are NEW.

**This is the second of the free path's two nano calls.** BUILD §6.7 step 4 reads "Phrase
as questions (nano, **same call as step 1**)", which is arithmetically impossible: step 3 —
deterministic selection — sits between step 1 and step 4, so step 4's input is step 3's
output and cannot exist when step 1 is issued. BP-025 decision 2 settles it: two nano calls
per scan, ≈0.6¢ rather than ≈0.3¢, against `CAPS.FREE_C` 12¢. Do not "optimise" the two
calls back into one; doing so would let the model see the candidate set before selection
runs, which is the coupling §6.7 forbids.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/questions/phrase.ts` | **NEW** | Declares `Question` and `phraseQuestions` exactly as BP-025's `## Public interface` states them. Builds the `'question-phrasing'` nano call with a Zod schema admitting **only** `{ id, text }` per element, so a model that returns a keyword has produced an unparseable response rather than a re-selection. Carries `search` from the input. Holds the template forms (the "best X" / "X vs Y" shapes BUILD §6.7 step 4 names) and applies one per question whenever the model's wording for that id is missing or unparseable, setting `phrasing: 'template'`. |
| `tests/market/questions/phrase.test.ts` | **NEW** | The tests named in the test plan, against a stubbed `llm()`. No network. |

## Interfaces
**Consumes**: `SelectedSearch` (WO-074), `Measured<T>` (BP-010), `CostContext` (BP-007), and

```ts
// BP-009 — the call site, verbatim from its LlmCallSite union
  | 'question-phrasing'  // nano · a selected search -> a question's wording (step 4)
export function llm<T>(c: CostContext, call: {
  site: LlmCallSite
  input: unknown
  schema: ZodType<T>          // output is parsed or the call failed
  tier: 'nano' | 'haiku'
}): Promise<Measured<T>>
```

**Exposes** — copied exactly from BP-025's `## Public interface`:

```ts
// src/lib/market/questions/phrase.ts — step 4
export interface Question {
  id: string                            // stable within a scan; the matrix keys on it
  text: string                          // the wording only
  search: SelectedSearch                // carried, never returned by the model
  phrasing: 'template' | 'model'
}
export function phraseQuestions(c: CostContext, a: { selected: SelectedSearch[] }): Promise<Measured<Question[]>>
```

## Steps
1. Derive a stable `id` per selected search from its `rank` (stable within a scan, which is
   all `Question.id` promises). The id is assigned before the call and is the key the model's
   response is matched back on.
2. Issue **one** `llm()` call with `site: 'question-phrasing'`, `tier: 'nano'`, whose input is
   the ids and keywords of the selected searches and whose schema parses an array of
   `{ id, text }` and nothing else — no keyword, no volume, no intent, no rank.
3. Build one `Question` per input `SelectedSearch`, in input order, `search` carried by
   reference from the input. Where the model supplied a parseable `text` for that id, set
   `phrasing: 'model'`; otherwise apply the template and set `phrasing: 'template'`.
4. A whole-call failure (`unmeasured` from `llm()`) yields every question at
   `phrasing: 'template'` and a `measured` outcome — the questions exist, so the set is
   measured. Nothing is dropped: dropping one would change criterion 1's denominator, which
   every other number on the card is read against (BP-025 decision 3).
5. `phraseQuestions` returns `Measured<Question[]>` whose length always equals
   `selected.length`.
6. Emit the observability field BP-025 names: phrasing fallbacks.
7. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-006) | Test |
|---|---|
| 13. "Given any of the 12 questions, when it renders, then the search shown beside it (criterion 9) is the search that question was selected from, and how the question is worded never changes which search it stands for: a differently worded question is still the same measured search, and no question stands for a search the report did not measure." | `tests/market/questions/phrase.test.ts › phraseQuestions/wording-never-moves-the-search` — for every returned `Question`, `question.search` is reference-equal to the input `SelectedSearch` of the same id; and `phraseQuestions/schema-rejects-a-returned-keyword` — a stub whose response carries a `keyword` field per element does not parse, and the result is the template form, not a re-selected search. |
| 1. "Given a completed report, when the AI-answers card renders, then it states in writing how many of the market's biggest searches the report measured — twelve where the market yielded twelve, and the smaller number it yielded where it yielded fewer — states on how many of those an AI answer appeared, and shows every citation count on the card against those two numbers and never against a count of searches the report did not measure." — the limb this file can break: a phrasing failure must not change the denominator. | `tests/market/questions/phrase.test.ts › phraseQuestions/failure-degrades-never-drops` — three cases: (a) the model omits one id, (b) the model returns unparseable output for every id, (c) `llm()` returns `unmeasured`. In all three the returned array length equals the input length, and the affected questions carry `phrasing: 'template'`. |

Cost shape, from BP-025 decision 2:
`phraseQuestions/issues-exactly-one-nano-call` — one `llm()` invocation per call, `site`
`'question-phrasing'`, `tier` `'nano'`; and the phrasing call's input contains no member of
the raw market set beyond the selected searches handed in.

Discrimination: widening the schema to admit a `keyword`, or dropping an unphrased question
from the array, must each fail a named test above.

## Out of scope
- The AI-answers card, its counts and its cells — WO-076.
- The wording rules of the templates as customer-visible copy: the template shapes are
  BUILD §6.7 step 4's mechanical forms over the keyword, not product sentences. No line the
  product speaks in its own voice is written here (BP-019 owns those).
- Adding `'question-phrasing'` to BP-009's `LlmCallSite` union — BP-009's file.
- REQ-006 criteria 2–12.

## Rollback
Delete both files; `matrix.ts` has not landed, so nothing consumes `Question`.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-025 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 2 rows of `## Test plan` quote REQ-006 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-022, WO-026, WO-027); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 finished — planner — implements gains BP-011 (/workorder BP-011): this order's file plan creates the file that exports `phraseQuestions`, which BP-011's ## Public interface declares and the leaf ships identically — capabilities.md: "the component's declaration is the contract and the leaf is the implementation"; no other field changes.
