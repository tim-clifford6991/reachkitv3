---
id: WO-054
type: work-order
title: "The score and its band — factorsOf, presenceOf, computeScore, bandOf"
status: approved
ui: no
implements: [BP-024, REQ-004]
depends-on: [WO-006, WO-052, WO-248]
blocked-by: []
rests-on:
  - claim: "BP-005 gains the four score-band boundaries of `BUILD.md` §5 — 0–24, 25–49, 50–74, 75–100 — as a pin under a name BP-005 chooses. **Discharged 2026-08-31**: BP-005 declares `SCORE_BAND_BOUNDS: { invisible: 0; 'hard-to-find': 25; findable: 50; dominant: 75 }`, named so as not to collide with BP-019's `SCORE_BANDS` copy map, and BP-024's `bandOf` comment now names it. This WO imports that pin and does not create it"
    disposition: confirmed
supersedes: []
---

# WO-054 — The score and its band — `factorsOf`, `presenceOf`, `computeScore`, `bandOf`

- Branch: wo/WO-054-score-bands
- Estimate: ≤1 day

## Goal
Ship `BUILD.md` §5's arithmetic over `Measured` values and the four band handles,
such that a single unmeasured factor yields an unmeasured score with no band, no
estimate and no interpolation — the property ADR-021 exists to protect.

## File plan

> Every path below is **NEW**. Verified 2026-08-31: this repo carries no `src/`.
> Placement per `structure.md`; provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/measure/score.ts` | NEW | `ScoreFactorName`, `ScoreFactors`, `presenceOf`, `factorsOf`, `computeScore`. All arithmetic runs through WO-052's `combine`, so no branch can reach a raw number from an `unmeasured` arm. `computeScore` takes exactly one parameter and no flag (ADR-021 decision 3). Floors: Answerability and Presence are floored at 1, and a floor applies only to a `measured` or `zero` arm. |
| `src/lib/measure/bands.ts` | NEW | `BandHandle` and `bandOf(score: number): BandHandle`, reading the four boundaries from BP-005's `SCORE_BAND_BOUNDS` — `{ invisible: 0, 'hard-to-find': 25, findable: 50, dominant: 75 }`, imported, never written here (`structure.md` rule 5, and BP-024's own comment on `bandOf`). **No numeric literal 0, 25, 50 or 75 appears in this file.** No band word appears in it either; the words are BP-019's `SCORE_BANDS`. |
| `tests/measure/verdict/score.test.ts` | NEW | The arithmetic and ADR-021 suites named in the test plan. |
| `tests/measure/verdict/bands.test.ts` | NEW | The band-boundary suite, including the boundary values themselves. |

## Interfaces

Consumes: WO-052's `Measured<T>`, `combine`, `mapMeasured`, `worseReason`;
BP-010's `Drivers` (`src/lib/measure/index.ts`, another node's file — the four
measured quantities `foundations`, `answerability`, `searchPresence`,
`aiPresence`, each a `Measured<number>`); BP-005's `SCORE_BAND_BOUNDS`.

Exposes (verbatim from BP-024 `## Public interface`, `src/lib/measure/score.ts`
and `bands.ts`):

```ts
/** The three the score is composed of, and the three the report shows beside it
 *  (REQ-004 c2, `BUILD.md` §4.1's "three driver mini-bars"). `Presence` is the
 *  geometric mean of BP-010's `searchPresence` and `aiPresence`, floored at 1
 *  (`BUILD.md` §5) — those two are sub-measures, not shown factors. */
export type ScoreFactorName = 'foundations' | 'answerability' | 'presence'
export type ScoreFactors = Readonly<Record<ScoreFactorName, Measured<number>>>

export function presenceOf(d: Pick<Drivers, 'searchPresence' | 'aiPresence'>): Measured<number>
export function factorsOf(d: Drivers): ScoreFactors
export function computeScore(f: ScoreFactors): Measured<{ score: number; band: BandHandle }>
export type BandHandle = 'invisible' | 'hard-to-find' | 'findable' | 'dominant'
export function bandOf(score: number): BandHandle   // thresholds are BP-005's SCORE_BAND_BOUNDS
```

The arithmetic, verbatim from `BUILD.md` §5: `Presence = max(1, √(SearchPresence
× AIPresence))`; `Score = round( ∛(Foundations × Answerability × Presence) )`;
`Bands: 0–24 Invisible · 25–49 Hard to find · 50–74 Findable · 75–100 Dominant`.

## Steps
1. Write the failing suites first, ADR-021's three prohibitions included.
2. Declare `ScoreFactorName`, `ScoreFactors`, `BandHandle`.
3. Implement `bandOf` over `SCORE_BAND_BOUNDS`, descending through its four
   lower bounds, with a total switch and no default arm that could swallow an
   out-of-range value; assert the input range at the boundary instead. The pin is
   the only source of a threshold: a source assertion in the suite fails on any
   numeric literal in `bands.ts` other than the range guard's own 0 and 100.
4. Implement `presenceOf` with `combine([searchPresence, aiPresence], …)` and the
   floor of 1 applied inside the combining function, so it can only ever apply to
   a value that exists.
5. Implement `factorsOf`: `foundations` and `answerability` pass through from
   `Drivers` (answerability floored at 1), `presence` is `presenceOf`.
6. Implement `computeScore` with `combine([foundations, answerability, presence],
   …)`. Take no second parameter. Do not export a partial variant, an `options`
   object, or a `force` flag — ADR-021 decision 3.
7. Add each ADR-021 test with a failure message naming `ADR-021` (that ADR's
   consequence: "each names this ADR in its failure message").

## Test plan

> Criteria are copied verbatim from `requirements/REQ-004.md`; line breaks are
> normalised to fit the table and no word is changed. The two tests marked
> **must fail first** are the two "plausible fixes" ADR-021 names.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-004 c1 — "Given a completed report whose score could be computed, when it opens, then the first module shows a single score on a 0–100 scale, its band as one of the four written words — Invisible, Hard to find, Findable, Dominant — the domain measured, and the date it was measured." | `score/compute · one number on a 0–100 scale with one band handle` — over a fixture matrix of measured factor triples, `computeScore` returns `measured` with `score` an integer in `[0,100]` and `band` one of the four handles. And `bands/bandOf · the four boundaries` — 0, 24, 25, 49, 50, 74, 75, 100 map to `invisible, invisible, hard-to-find, hard-to-find, findable, findable, dominant, dominant`, read from the BP-005 pin. The four *words* are BP-019's and are asserted nowhere in this WO. |
| REQ-004 c3 — "Given a report whose score could not be computed (criteria 6 and 9), when it opens, then the first module shows '—' in place of the score, shows no band word at all, still names the domain and the date, and carries one written line saying the verdict could not be reached, naming every driver that has no value, and stating for each which of the two reasons applies … No band is stated, implied, or guessed from the drivers that were measured." | `score/compute · one unmeasured factor makes an unmeasured score, for every combination` (**must fail first**) — a property test over every arrangement where at least one of the three factors is `unmeasured` and the other two range over `measured` and `zero` values including the extremes: the result is `kind: 'unmeasured'`, and the result object is asserted to carry no `score` and no `band` field at all. This is ADR-021 decision 3 and the test whose deletion would let a partial score ship. |
| REQ-004 c9 — "… Where a driver the score is composed of was among the work cut off — including a driver whose measurement was still outstanding when the spend ceiling was reached — the score itself is shown as '—' on the terms criterion 3 states, never computed, banded or estimated from the drivers that were measured; where no driver was cut off, the score still stands." | `score/compute · the ceiling case yields no number` (**must fail first**) — Foundations and Answerability `measured`, Presence `unmeasured/not_attempted` (the exact ADR-021 scenario: the twelve SERPs cut off part-way): result is `unmeasured/not_attempted`, and no exported symbol of `score.ts` accepts a flag, option or second argument that would produce a number from it — asserted by `computeScore.length === 1` and by a source assertion that the file contains no `partial`, `provisional`, `force` or `allowIncomplete` identifier. |
| REQ-004 c7 — "… a home document the scan read that tells every reader not to index it scores 0 with the band word that 0 carries, on criterion 1's terms, and never a '—'." | `score/compute · a read generic noindex scores 0, by arithmetic and not by override` — Foundations `measured 0`, Answerability and Presence `measured` at their floors: score is `measured 0` with band `invisible`. A source assertion that neither file contains a special case keyed on 0 or on noindex — `∛(0 × A × P) = 0` already produces it. |
| REQ-004 c8 — "Given a domain the scan measured successfully that appears in none of the searches measured and is cited in none of the AI answers that appeared, when its numbers are stored, then that absence is recorded as a measured zero, never as a measurement that could not be taken." | `score/presenceOf · a cold-start domain scores, honestly, low` — `searchPresence` and `aiPresence` both `zero`: `presenceOf` returns a `measured`/`zero` arm floored at 1, never `unmeasured`, and `computeScore` over it returns a number. |
| REQ-004 c2 — "Given the score, when it renders, then the three drivers it is composed of are shown beside it, each named and each with its own value, or with '—' in place of a value the scan could not measure (criterion 6)." | `score/factorsOf · four measured quantities become exactly three factors` — `factorsOf` returns exactly the keys `foundations`, `answerability`, `presence` and no fourth; `searchPresence` and `aiPresence` do not appear in the returned object. Each factor carries its own `Measured` arm unchanged where it passed through. |
| REQ-004 c12 — "… nothing stands in its place: no estimate, no interpolated value, no default, and no value carried over from an earlier scan of that domain." | `score/floors · a floor never manufactures a value` — for every `unmeasured` input to `presenceOf` and to `factorsOf`'s answerability path, the output is `unmeasured`; the floor of 1 is asserted to apply only on `measured` and `zero` arms. |

## Out of scope
- `Verdict`, `missing`, `unmeasuredElsewhere`, `blockedReaders` — WO-055.
- The band *words* — BP-019's `SCORE_BANDS`, another node's.
- Declaring the band boundary pin — BP-005's `SCORE_BAND_BOUNDS`, written by
  WO-006 and asserted against `BUILD.md` §5 by WO-007. This WO imports it.
- Rendering anything — no file in this WO imports React.

## Rollback
Delete all four files. `verdict.ts` (WO-055) is the only consumer.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-024 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 7 rows of `## Test plan` quote REQ-004 as it stands on disk.
- **Bounded.** 4 files in the file plan (4 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended: `bandOf` now names and reads BP-005's `SCORE_BAND_BOUNDS` instead of "a pin BP-005 chooses", the boundary `rests-on` row is discharged, and `bands.ts` is barred from holding the four numbers.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-006, WO-248); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
