---
id: WO-087
type: work-order
title: "sizeRivals and dueForResizing — one entry per rival, a date on every band, and two unsized states"
status: approved
ui: no
implements: [BP-052, BP-011, REQ-096]
depends-on: [WO-006, WO-022, WO-024, WO-086]
blocked-by: []
rests-on:
  - claim: "A rival's total count of searches it appears in is readable beside the 100 rows `rankedKeywords` fetches for it, so a band can be cut at 500 from a 100-row call. Origin: REQ-096's own open row. If refuted, `RivalSize.rankedCount` caps at 100, the `far` band becomes unreachable, and REQ-041's how-far-ahead ratio is refuted with it — the fix is shared with that requirement, not owned here."
    disposition: open
  - claim: "`RANKED_RIVAL_ROWS` (100) and `CACHE_WINDOWS_D.rival` (30 days) are pinned in `src/lib/config/constants.ts` (BP-005), which BP-052's `## NFR budget` records as 'both already declared there'. That file is BP-005's and is not touched here."
    disposition: open
supersedes: []
---

# WO-087 — sizeRivals and dueForResizing — one entry per rival, a date on every band, and two unsized states

- Branch: wo/WO-087-size-rivals
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/sizing/size.ts` so every tracked rival gets exactly one entry in
input order — sized with its counts, its date and whether that measurement is current, or
unsized for one of two named reasons — and never disappears from the array.

## Context an implementer needs
No source code exists on disk; both paths below are NEW.

The first `rests-on` row above is inherited verbatim from BP-052's front-matter and is open:
it bounds `rankedCount`'s ceiling, not whether this WO may be built. If refuted, the fix is
shared with REQ-041 and is not owned here.

**No new spend site** (BP-052's `## NFR budget`): sizing reads the rows the paid deep and
monthly passes already buy — `rankedKeywords(rows: 100)` per rival, inside `CAPS.DEEP_C` 150¢
and `CAPS.WEEKLY_C` 40¢. **Paid path only.** BUILD §6.4's never-pull list forbids per-rival
`ranked_keywords` on the free path, and BP-052's `## Error & edge behavior` holds that by the
module graph rather than a runtime check: nothing here is reachable from BP-026's free-report
card, and the test plan asserts the absence of that import edge in both directions.

**No band is ever stored as an independent column** (BP-052 decision 1). `report.rivalSizes`
stores the counts, the date and the derived band together, and re-derivation from the stored
counts must reproduce the stored band.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/sizing/size.ts` | **NEW** | `RivalSize`, `sizeRivals` and `dueForResizing`, exactly as BP-052's `## Public interface` states them. Calls `bandRivalSize` (WO-086) for the band; never re-implements the boundaries. |
| `tests/market/sizing/size.test.ts` | **NEW** | The tests named in the test plan, with `rankedKeywords` stubbed and a fixed injected clock. |

## Interfaces
**Consumes**, copied exactly from BP-008's `## Public interface`:

```ts
export function rankedKeywords(c: CostContext, a: { domain: string; rows: 50|100|300 }): Promise<Measured<RankedRow[]>>
```
plus `Measured<T>` (BP-010), `CostContext` (BP-007), `RivalSizeBand` and `bandRivalSize`
(WO-086), and `RANKED_RIVAL_ROWS` / `CACHE_WINDOWS_D.rival` (BP-005).

**Exposes** — copied exactly from BP-052's `## Public interface`:

```ts
// src/lib/market/sizing/size.ts
export type RivalSize =
  | { domain: string; state: 'sized'
      rankedCount: number; band: RivalSizeBand; at: Date; current: boolean }
  | { domain: string; state: 'unsized'
      because: 'awaiting_deep_pass' | 'added_since_last_sizing' }
export function sizeRivals(c: CostContext, a: { rivals: string[]; ownRanked: number }):
  Promise<Measured<RivalSize[]>>            // one entry per input rival, in input order
export function dueForResizing(a: { lastMeasuredAt?: Date; now: Date }): boolean
```

## Steps
1. `sizeRivals` returns **exactly one entry per input rival, in input order**. There is no code
   path in this module that filters, reorders, excludes, hides or truncates one — that absence
   is how criterion 7 is held, and the test asserts the array's length and order over every
   arm, including failures.
2. Per rival: `rankedKeywords(c, { domain, rows: RANKED_RIVAL_ROWS })`, then
   `rankedCount` from the returned rows' total count, then
   `band = bandRivalSize({ rivalRanked: rankedCount, ownRanked: a.ownRanked })`.
3. `ownRanked` is the customer's own measured count from the **same pass** and is handed in
   with the rivals. Stamp one `at` for the whole call, so a band can never mix a rival's count
   from one pass with a customer count from another.
4. `current` is `true` for a count measured in this call. A rival whose rows could not be
   fetched **keeps its previous `sized` entry with the earlier date and `current: false`** — it
   never falls back to `unsized`, which would read as "we have never measured this" about a
   rival we have. The caller supplies the previous entry; where it does, this function carries
   it forward unchanged but for `current`.
5. The two unsized reasons: `'awaiting_deep_pass'` for a rival settled at setup before the pass
   completed, `'added_since_last_sizing'` for one added afterwards. The `unsized` arm carries
   **no** band and **no** count, so a surface cannot print a band for a rival that has none.
6. `dueForResizing({ lastMeasuredAt, now })` is true when `lastMeasuredAt` is absent or is
   `CACHE_WINDOWS_D.rival` days or more before `now` — one number, not two; the literal 30 does
   not appear in this file.
7. Emit the observability fields BP-052 names: per rival, the two counts, the derived band, the
   measurement date, and whether it was a re-measurement or a first sizing.
8. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-096) | Test |
|---|---|
| 1. "Given a customer whose tracked rivals were settled at setup, when their onboarding deep pass completes, then each tracked rival carries a measured size — the count of searches that rival appears in — taken from that pass and held beside the customer's own measured count from the same pass; and no rival carries a size or a band before that pass has completed." | `tests/market/sizing/size.test.ts › size/one-at-per-call` — every `sized` entry from one call carries the identical `at`, and the `ownRanked` used for every band is the single value handed in; the signature admits no per-rival customer count. `size/before-the-pass-there-is-no-band` — a rival with no measurement returns the `unsized` arm, which a type-level assertion shows carries no `band` and no `rankedCount` key at all. |
| 3. "Given a tracked rival whose size was last measured a month ago or longer, when the product next measures for that customer, then that rival's size is measured again and its band re-derived from the current counts without the customer asking; and a rival the customer added after their deep pass is sized at the first such measurement after they added it." | `tests/market/sizing/size.test.ts › dueForResizing/at-the-cache-window` — with `lastMeasuredAt` exactly `CACHE_WINDOWS_D.rival` days before `now` ⇒ true; one day less ⇒ false; absent ⇒ true; the window is read from `constants.ts`, not restated. `size/added-since-last-sizing-is-sized-next-pass` — a rival carrying `because: 'added_since_last_sizing'` returns a `sized` entry on the next call, with that call's `at`. |
| 4. "Given a rival's band is shown to the customer, when they read it, then it carries the date that rival's size was measured; and where the size could not be measured again when it was due, the last measured band is shown with that earlier date and is never presented as a current measurement." | `tests/market/sizing/size.test.ts › size/stale-carries-current-false` — a stub whose fetch fails for one rival returns that rival's previous `sized` entry with its **earlier** `at` and `current: false`, while the others carry the new `at` and `current: true`. `size/stale-never-becomes-unsized` — the failing rival's arm is still `'sized'`. A type-level assertion that `current` is required on the `sized` arm, so a surface cannot omit the distinction by omitting the date. |
| 5. "Given a customer reading how far ahead a tracked rival is, when that rival has been sized, then its band is shown with that distance; and where it has not been sized yet, one written line says its size has not been measured yet, and no band and no blank stands in its place." — the state limb; the written line is BP-019's and the Overview module's (BP-038). | `tests/market/sizing/size.test.ts › size/unsized-is-a-state-not-a-blank` — both `unsized` reasons are returned as discriminated arms with a `because`, never as `null`, `undefined` or an omitted array element. |
| 7. "Given a tracked rival of any band, when any measurement, comparison or screen in the product uses the rival set, then that rival is measured, compared and shown exactly as every other one, and stays in the set until the customer removes it themselves: no band ever removes a rival, hides it, drops it from a comparison, or stops it being measured." | `tests/market/sizing/size.test.ts › size/one-entry-per-input-in-input-order` — a property test over rival lists of length 0…20 with arbitrary fetch outcomes: the returned array's length always equals the input's, and `map(e => e.domain)` equals the input array exactly. `size/exports-no-filter` — a source-level assertion that this module exports no function taking a `RivalSize[]` and returning a shorter one, and that `far` appears in no conditional that controls inclusion. |
| 2. "Given a tracked rival whose size has been measured, when its band is recorded, then it carries exactly one of three bands, ordered by measured size and named in this requirement by the internal handles `near`, `middle` and `far`: `near`, where the rival appears in no more than the greater of 100 searches and twice the customer's own measured count — a rival roughly the customer's own size; `middle`, where it is not `near` and appears in no more than the greater of 500 searches and five times the customer's own count; and `far`, where it appears in more than that. Of any two rivals measured against the same customer count, the larger never carries a band ordered nearer than the smaller." — the storage limb only: the band travelling with its counts must re-derive identically. The two bars themselves are WO-086's. | `tests/market/sizing/size.test.ts › size/band-re-derives-from-the-stored-counts` — for every `sized` entry, `bandRivalSize({ rivalRanked: e.rankedCount, ownRanked })` equals `e.band` (BP-052 decision 1: "re-derivation from the stored counts must reproduce the stored band, and a test asserts it"). |

Free-path exclusion, from BP-052's `## Error & edge behavior` ("Nothing in this module reaches
BP-026 or the free report… held by the module graph, not by a runtime check") and BUILD §6.4's
never-pull list: `size/not-reachable-from-the-free-report` — `src/lib/market/sizing/**`
resolves no import into `src/lib/market/rivals/**`, and `src/lib/market/rivals/**` resolves no
import into `src/lib/market/sizing/**`.

Spend, from BP-052's `## NFR budget` ("No new spend site"): `size/rows-is-the-pin` — every
`rankedKeywords` call passes `rows: RANKED_RIVAL_ROWS`, and no other vendor endpoint is called.

Discrimination: dropping a rival whose fetch failed, letting a failed re-measurement fall back
to `unsized`, or defaulting `current` to `true` for a carried-forward entry, must each fail a
named test above.

## Out of scope
- The banding boundaries — WO-086.
- The swap offer — WO-088.
- Writing `report.rivalSizes` into `scans.report jsonb`: the blob and its migration glob are
  BP-012's; this module returns the value the pass stores.
- Running the deep or monthly pass — BP-003's and BP-012's.
- The Overview rival module and every written line — BP-038's and BP-019's.
- REQ-096 criteria 6, 8, 9.

## Rollback
Delete both files; WO-088 has not landed, so nothing imports `RivalSize`.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-052 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 6 rows of `## Test plan` quote REQ-096 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-006, WO-022, WO-024); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 finished — planner — implements gains BP-011 (/workorder BP-011): this order's file plan creates the file that exports `sizeRivals`, which BP-011's ## Public interface declares and the leaf ships identically — capabilities.md: "the component's declaration is the contract and the leaf is the implementation"; no other field changes.
