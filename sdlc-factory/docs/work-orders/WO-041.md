---
id: WO-041
type: work-order
title: "The copy registry: COPY, COPY_META and copy() — the closed set of written strings"
status: approved
ui: no
implements: [BP-020, REQ-093]
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31: the repo root holds only BUILD.md, DATA-COSTS.md, REQUIREMENTS-CONSOLIDATION.md, factory.config.json, CLAUDE.md and sdlc-factory/). Every path in this file plan is NEW and its placement is provisional against a repo with no code. The TypeScript project, its path resolution and the `*.test.ts` runner are established by work orders outside WO-041..WO-050; this WO creates none of them and adds no repo-root file."
    disposition: open
  - claim: "Nine values in COPY were owner-owed at cut time and no agent could fill them (BP-020 rests-on row 1: the three winnability band words, the three rival-size band words, REQ-009 criterion 8's three severity words). **Discharged 2026-08-31**: the owner ruled all nine and BP-019 decision 6 records them verbatim, so this WO transcribes them from that approved entry into `keys/bands.ts` rather than seeding them empty. Every other seeded key remains owner-owed and empty, and the machinery below is what makes an unfilled key a loud failure instead of a blank."
    disposition: confirmed
supersedes: []
wave: W2
---

# WO-041 — The copy registry: COPY, COPY_META and copy() — the closed set of written strings

- Branch: wo/WO-041-copy-registry
- Estimate: ≤1 day

## Goal
Create `src/lib/presentation/copy/` holding the twelve partition files, the
composed frozen `COPY` literal, the total `COPY_META` map, `copy()`, and the
owner-owed machinery that makes an unwritten sentence a loud failure rather than
a blank on a screen.

## File plan

> **Re-cut 2026-08-31 against BP-020 decision 5.** As this work order first
> stood, `registry.ts` was created here and appended to by every later block that
> owns a sentence — one file, eleven blocks, every one of them conflicting with
> every other. BP-020 decision 5 repartitions it: one partition file per owning
> module under `copy/keys/`, and `registry.ts` naming the closed partition list,
> **written once, here, with every partition present — empty ones included**. A
> later block fills only its own partition file and touches nothing this work
> order writes twice.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/presentation/copy/keys/report.ts` | **NEW** | `REPORT_COPY: CopyPartition` — the report surface's sentences. Seeded with `place.report.first-page.rival` (see Steps 3). |
| `src/lib/presentation/copy/keys/overview.ts` | **NEW** | `OVERVIEW_COPY` — Overview's. Seeded with the three `place.overview.weekly-presence.*` keys. |
| `src/lib/presentation/copy/keys/calendar.ts` | **NEW** | `CALENDAR_COPY` — the calendar's. Seeded with `place.calendar.date.page`, `cause.unrecognised`, `cause.supply-exhausted`. |
| `src/lib/presentation/copy/keys/draft.ts` | **NEW** | `DRAFT_COPY` — the draft view's. **Empty**, exported frozen, for its own block to fill. |
| `src/lib/presentation/copy/keys/settings.ts` | **NEW** | `SETTINGS_COPY` — Settings'. **Empty.** |
| `src/lib/presentation/copy/keys/setup.ts` | **NEW** | `SETUP_COPY` — setup's. **Empty.** |
| `src/lib/presentation/copy/keys/publish.ts` | **NEW** | `PUBLISH_COPY` — publishing's. **Empty.** |
| `src/lib/presentation/copy/keys/danger.ts` | **NEW** | `DANGER_COPY` — the destructive-action surfaces'. **Empty.** |
| `src/lib/presentation/copy/keys/mail.ts` | **NEW** | `MAIL_COPY` — the mail seam's, BP-016 and BP-029. Seeded with the two opt-out surface lines (Steps 4). |
| `src/lib/presentation/copy/keys/offer.ts` | **NEW** | `OFFER_COPY` — the price and offer surfaces', BP-030 and BP-031. Seeded with the nine keys those two nodes fix (Steps 4). This is the one partition added beyond BP-020's named eleven; see **Decision taken under rule 1.1**, second entry. |
| `src/lib/presentation/copy/keys/bands.ts` | **NEW** | `BANDS_COPY` — the band and severity words. **The one partition seeded with real strings**: BP-019 decision 6's nine ruled words plus REQ-004 criterion 1's four score-band words, transcribed from those approved entries (Steps 5). Nothing here is chosen. |
| `src/lib/presentation/copy/keys/laws.ts` | **NEW** | `LAWS_COPY` — the sentences the four cross-cutting laws are made of, which no single surface owns. Seeded with the five `stopped.work.*`, the five `next-publish.*` and the two `generated.page.*` keys. |
| `src/lib/presentation/copy/registry.ts` | **NEW** | Names the closed partition list — the twelve imports above and no thirteenth — spreads them into `COPY` and `COPY_META`, freezes both, and derives `CopyKey = keyof typeof COPY`. Declares `CopyMeta`, `CopyPartition` and `OWNER_OWED: readonly CopyKey[]`. **Written once**: a later work order that adds a partition adds one import and one spread and is by construction the only one touching it. |
| `src/lib/presentation/copy/copy.ts` | **NEW** | `copy(key, vars?)` — one property read plus named-slot substitution; throws naming the key when the key is in `OWNER_OWED`, and throws naming the key when `COPY_META[key].slots` declares a `'measured'` slot (BP-020 Decisions 2 — the runtime half; the type half is WO-042's). No interpolation engine, no catalogue load, no network, no clock, no session parameter. |
| `src/lib/presentation/copy/index.ts` | **NEW** | The module's public entry point: re-export `COPY`, `COPY_META`, `copy`, and the types `CopyKey`, `CopyMeta`, `CopyPartition`. Nothing else — no partition is re-exported by name, so nothing outside this module can read one partition and miss the rest. |
| `tests/presentation/copy/registry.test.ts` | **NEW** | TST for partition composition, registry totality, freezing, owner-owed consistency and the no-model-import property. |
| `tests/presentation/copy/copy.test.ts` | **NEW** | TST for `copy()` behaviour, including the two throwing paths and the reader-invariance property. |

## Interfaces
**Consumes:** nothing from another node. This module is pure: no network, no
database, no session, no clock (BP-020 `## Module / boundary`).

**Exposes** (verbatim from BP-020 `## Public interface`):
```ts
// ── src/lib/presentation/copy/keys/<partition>.ts ────────────────────────────
/** The sentences and their metadata are written **one partition file per owning
 *  module** — `report.ts`, `overview.ts`, `calendar.ts`, `draft.ts`,
 *  `settings.ts`, `setup.ts`, `publish.ts`, `danger.ts`, `mail.ts`, `bands.ts`,
 *  `laws.ts` — each exporting a frozen `{ key: [string, CopyMeta] }` block for
 *  its own module and nothing else. See decision 5: this is what keeps two
 *  parallel work orders off one file. */
export const REPORT_COPY: CopyPartition    // and one such export per partition
export type CopyPartition = Readonly<Record<string, readonly [string, CopyMeta]>>

// ── src/lib/presentation/copy/registry.ts ────────────────────────────────────
/** Every sentence the product speaks in its own voice, composed from the closed
 *  partition list. A frozen object literal in the bundle — not a fetch, not a
 *  template store, not an i18n catalogue — so REQ-093 criterion 5 holds with
 *  every model unavailable. This file is written **once**, with every partition
 *  named, and is not appended to block by block. */
export const COPY: Readonly<Record<CopyKey, string>>
export type CopyKey = keyof typeof COPY

/** The parallel, exhaustive metadata map, projected from the same partitions.
 *  `Record<CopyKey, CopyMeta>` is total, so a key added without meta is a
 *  compile error — this is the hook every law in this block is driven off, and
 *  the reason no law has to be remembered. Because both maps are projected from
 *  one partition entry, "a key without meta" is now unrepresentable rather than
 *  merely checked. */
export const COPY_META: Readonly<Record<CopyKey, CopyMeta>>

export interface CopyMeta {
  /** Which cross-cutting law, if any, governs this sentence. The conformance
   *  suites enumerate their own scope from this tag rather than from a list a
   *  developer maintains. */
  law?: 'next-publish' | 'stopped-work' | 'no-presence-yet' | 'unmeasured'
  /** Named text slots and what each accepts. A slot of kind 'measured' may be
   *  filled only through `explain()`; `copy()` refuses such a key. */
  slots: Readonly<Record<string, 'text' | 'date' | 'measured'>>
  /** The requirement clause that fixes what this sentence must say. */
  fixedBy: string           // e.g. "REQ-092 c4"
}

// ── src/lib/presentation/copy/copy.ts ────────────────────────────────────────
/** A sentence carrying no measurement. Signature is BP-019's, verbatim. */
export function copy(key: CopyKey, vars?: Record<string, string | number>): string
```

**Exposes additionally** (this WO's, not the blueprint's — see Steps 3):
```ts
export const OWNER_OWED: readonly CopyKey[]
```

## Steps
1. Create the twelve partition files under `src/lib/presentation/copy/keys/`.
   Each exports exactly one frozen `CopyPartition` and imports only the
   `CopyMeta`/`CopyPartition` types. A partition file imports no other partition
   and no surface. Eight of the twelve are created **empty** — an empty frozen
   object is a partition with no sentences yet, which is what lets a later block
   fill its own file and touch nothing else.
2. Create `registry.ts`. Import the twelve partitions by name, spread them in a
   fixed order into one object literal ending
   `as const satisfies Record<string, readonly [string, CopyMeta]>`, then project
   `COPY` (entry `[0]`) and `COPY_META` (entry `[1]`) from it and freeze both
   with `Object.freeze`. Derive `export type CopyKey = keyof typeof COPY`.
   **`satisfies`, not a type annotation**: an annotation widens each entry's
   `slots` to `Record<string, 'text'|'date'|'measured'>` and destroys the per-key
   slot literals WO-042's `ExplainKey` is computed from, while `satisfies` keeps
   the literal types *and* keeps the `Record<CopyKey, …>` totality BP-020
   Decisions 1 relies on. A key declared in two partitions is a duplicate-key
   compile error in the spread — assert it in the test rather than resolving it
   by order.
3. Seed the law keys into `keys/laws.ts`, each with `''` as its value and each
   listed in `OWNER_OWED`. **No string is written.** The seed set, and the clause
   each `fixedBy` cites:
   - `stopped.work.line` — `fixedBy: "REQ-092 c1"`, `law: 'stopped-work'`
   - `stopped.work.needs-nothing` — `fixedBy: "REQ-092 c2"`, `law: 'stopped-work'`
   - `stopped.work.resumes-on` — `fixedBy: "REQ-092 c4"`, `law: 'stopped-work'`,
     `slots: { date: 'date' }`
   - `stopped.work.no-time-promised` — `fixedBy: "REQ-092 c4"`, `law: 'stopped-work'`
   - `stopped.work.partial-pass` — `fixedBy: "REQ-092 c6"`, `law: 'stopped-work'`
   - `next-publish.stopped` — `fixedBy: "REQ-092 c7"`, `law: 'next-publish'`
   - `next-publish.scheduled` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`,
     `slots: { at: 'date' }`
   - `next-publish.paused` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`
   - `next-publish.nothing-approved` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`
   - `next-publish.none-planned` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`
   - `generated.page.written` — `fixedBy: "REQ-093 c2"`, `slots: { pageTitle: 'text' }`
   - `generated.page.proposed` — `fixedBy: "REQ-093 c2"`, `slots: { pageTitle: 'text' }`
   And the place and cause keys into the partition of the surface that renders
   each, on the same terms — empty value, listed in `OWNER_OWED`:
   - `keys/overview.ts`: `place.overview.weekly-presence.chart` —
     `fixedBy: "REQ-041 c3"`, `law: 'no-presence-yet'`;
     `place.overview.weekly-presence.week` — `fixedBy: "REQ-065 c3"`,
     `law: 'no-presence-yet'`;
     `place.overview.weekly-presence.partial-week` — `fixedBy: "REQ-065 c4"`,
     `law: 'no-presence-yet'`
   - `keys/calendar.ts`: `place.calendar.date.page` — `fixedBy: "REQ-043 c5"`,
     `law: 'no-presence-yet'`; `cause.unrecognised` — `fixedBy: "REQ-043 c4"`,
     `law: 'no-presence-yet'`; `cause.supply-exhausted` —
     `fixedBy: "REQ-043 c3"`, `law: 'no-presence-yet'`
   - `keys/report.ts`: `place.report.first-page.rival` —
     `fixedBy: "REQ-010 c1"`, `law: 'no-presence-yet'`
   Every entry above has `slots: {}` unless a `slots` value is given.
4. Seed the two partitions the seven BP-001-owned surfaces need, same terms —
   empty value, listed in `OWNER_OWED`, no string written:
   - `keys/offer.ts`: `price.amount`, `price.vat_included`, `price.interval`
     (BP-030's `PRICE_COPY_KEYS`, spelled there verbatim) — each
     `fixedBy: "REQ-022 c1"`; `offer.cadence.page`, `offer.cadence.measure`,
     `offer.cadence.movement`, `offer.veto.window` — each
     `fixedBy: "REQ-021 c2"`; `offer.cancel_self_service` —
     `fixedBy: "REQ-021 c2"`; `offer.start` — `fixedBy: "REQ-021 c4"`. The four
     `offer.cadence.*`/`offer.veto.*` keys carry `slots: { value: 'text' }`; the
     values are BP-005 pins supplied by BP-031's `offerTerms()`, never written
     into the sentence here.
   - `keys/mail.ts`: `optout.confirmed` — `fixedBy: "REQ-011 c3"`;
     `optout.invalid` — `fixedBy: "REQ-011 c3"`.
5. Fill `keys/bands.ts` with the thirteen band and severity words. These are the
   one exception to "no string is written here", and they are **transcriptions of
   approved artifacts, not choices** (rule 1.2 — never invent a customer fact;
   copying a recorded owner ruling is not inventing one). Each entry carries the
   artifact it was transcribed from in a comment above it:
   - from **BP-019 decision 6** (owner ruling, 2026-08-31):
     `band.winnability.winnable` → `Winnable`;
     `band.winnability.reach` → `Reach`;
     `band.winnability.notYet` → `Not yet` — **unhyphenated**, and the note that
     the hyphenated `'not-yet'` is the internal handle and never the rendered
     term; `band.rivalSize.near` → `Similar size`;
     `band.rivalSize.middle` → `Larger`; `band.rivalSize.far` → `Much larger`;
     `severity.low` → `Minor`; `severity.mid` → `Worth fixing`;
     `severity.high` → `Critical`. Each `fixedBy` is `"REQ-047 c10"`,
     `"REQ-096 c2"` or `"REQ-009 c8"` as BP-019 records.
   - from **REQ-004 criterion 1**, whose own words they are:
     `band.score.invisible` → `Invisible`; `band.score.hard-to-find` →
     `Hard to find`; `band.score.findable` → `Findable`;
     `band.score.dominant` → `Dominant`. `fixedBy: "REQ-004 c1"`.
   None of the thirteen is in `OWNER_OWED`: they have values, so `copy()` returns
   them.
6. Create `copy.ts`. Implementation order inside the function: (a) if
   `OWNER_OWED` contains the key, throw `Error` whose message contains the key
   and the word `owner-owed`; (b) if any value in `COPY_META[key].slots` is
   `'measured'`, throw `Error` whose message contains the key; (c) substitute
   named slots from `vars` and return. Any slot named in `COPY_META[key].slots`
   and absent from `vars` throws naming the slot — a half-substituted sentence is
   the blank REQ-093 exists to prevent. The function takes no session, reader or
   persona argument and reads no module-level mutable state.
7. Create `index.ts` re-exporting exactly the symbols in `## Interfaces`.
8. Write `tests/presentation/copy/registry.test.ts` and
   `tests/presentation/copy/copy.test.ts` per the test plan. Watch each fail
   before the source file it exercises exists (constitution §8).

## Test plan
| Acceptance criterion (verbatim from REQ-093) | Test |
|---|---|
| 1. "Given any text the product renders or sends to a person — whether or not they are a customer: a verdict, an explanation, a label, a heading, an empty state, an alert, or anything else it says in its own voice — when they read it, then it is a written string in the product and no part of it was produced by a language model, unless criterion 2 or criterion 3 admits it. The kinds named here are illustrative, not a closed list: the criterion holds for every text the product renders or sends, on every surface it speaks on — a screen and a mail alike — including surfaces added after this requirement." | `registry.test.ts` → **"COPY is the only source of a product sentence"**: (a) `COPY` is frozen — a write to an existing key and an addition of a new key both throw in strict mode; (b) every value in `COPY` traces to a string literal present in one partition source read from disk, so no value was computed at load; (c) the module's transitive import graph, read from the source files, contains no path under `src/lib/llm/`. Discriminates: adding a getter-backed key, or an import of `src/lib/llm/`, fails it. The surface-wide half of this criterion is WO-044's sweep. |
| 4. "Given the product explains a number, verdict or reason, when the explanation renders, then every value inside it was read from a measurement the product stored for that domain, carrying the date of the scan it came from — never estimated, never recomputed for display, and never produced by a language model — and the words around those values do not change with who is reading." | `copy.test.ts` → **"copy() refuses a measured key and takes no reader"**: (a) `copy()` on a key whose meta declares a `'measured'` slot throws and the message names the key; (b) `copy.length === 2` and the exported signature accepts no third argument, so no session, reader or persona can vary the words; (c) two calls with identical arguments return byte-identical strings. The type-level half is WO-042's. |
| 5. "Given the product's language models are unavailable, when anyone opens a ReachKit surface, then every word in the product's own voice renders unchanged, and page content already written still renders from what was stored." | `registry.test.ts` → **"the registry renders with every model unavailable"**: import the module in a context where every export of `src/lib/llm/` is replaced by a throwing stub, and assert every non-owner-owed key returns its literal through `copy()` unchanged — the thirteen band and severity words included, which is the first time this assertion has anything to render. Discriminates: any lazy fetch or model call on the read path throws. The stored-page half is WO-043's. |

Additional tests this WO owns, not inherited from a criterion:
- `registry.test.ts` → **"the partition list is closed and total"** (BP-020
  decision 5): `registry.ts` imports exactly twelve partitions; every file under
  `keys/` is imported by it; every key in `COPY` traces to exactly one partition;
  and no partition file imports another partition or any surface. A `keys/` file
  nobody imports fails it — the failure mode decision 5's rejected
  "directory the build globs" alternative could not catch.
- `registry.test.ts` → **"COPY_META is total over CopyKey"**: a
  `@ts-expect-error` fixture adding a key to a partition as a bare string rather
  than a `[string, CopyMeta]` pair, asserting the compile error (BP-020 `## Error
  & edge behavior`, second bullet; under decision 5 a key without meta is
  unrepresentable rather than merely checked, and this is the test of that).
- `registry.test.ts` → **"owner-owed and empty agree both ways"**: every key in
  `OWNER_OWED` has `COPY[key] === ''`, and every key with `COPY[key] === ''` is
  in `OWNER_OWED`. Count assertion: the test states how many keys are owner-owed
  and how many are filled (rule 5.5 — a silent index reads as nothing to report).
- `registry.test.ts` → **"the thirteen band words are the ruled words"**: each of
  the nine asserted against BP-019 decision 6's recorded value and each of the
  four against REQ-004 criterion 1's, quoted in the test body (rule 5.3). Also
  asserts `band.winnability.notYet` renders `Not yet` and never `Not-yet` — the
  transcription note BP-019 decision 6 deliberately did not smooth.
- `copy.test.ts` → **"an owner-owed key throws rather than rendering blank"**:
  `copy()` on each `OWNER_OWED` key throws with the key in the message.
- `copy.test.ts` → **"a missing key is a compile error"**: `@ts-expect-error`
  fixture calling `copy('not-a-key')`.

## Out of scope
- `explain()` and the measured-slot type machinery — WO-042.
- `GeneratedText`, `PageIdentity`, `renderGenerated`, `renderQuestion` — WO-043.
- The string-literal sweep and the generated-text flow gate — WO-044.
- `BAND_LABELS`, `SCORE_BANDS`, `SEVERITY` and `renderMeasured` — BP-019's maps,
  not this node's. This WO writes the thirteen **strings** they resolve to and
  none of the three maps; the maps carry `CopyKey`s and are BP-019's to declare.
- Filling any empty partition. Eight of the twelve ship empty on purpose; the
  block that owns each module fills its own file.
- Writing any customer-visible string other than the thirteen transcribed in
  Steps 5. Every other seeded key is owner-owed and empty.
- Any repo-root file (`package.json`, `tsconfig.json`, lint or CI config).

## Decision taken under rule 1.1
**The empty-value-plus-throw representation.** `COPY`'s declared type is
`Readonly<Record<CopyKey, string>>`, so a key whose string the owner has not yet
supplied has no representable "absent" value. Three options: omit the key (then
every downstream module referencing it fails to compile and cannot be built or
tested at all), give it a placeholder sentence (forbidden — the owner owns
customer-visible strings, and a placeholder is the thing REQ-091 criterion 2
forbids on a screen), or give it `''` and make reading it throw. **Chosen: `''`
plus `OWNER_OWED` plus a throw.** Derivation: the key is an internal name (the
system's, per constitution §1), the empty value is not a string a reader can ever
see because `copy()` refuses it, and the failure is loud at the call site rather
than blank on a screen. Cost of reversing: one line per key — delete the
`OWNER_OWED` entry and paste the owner's sentence in; no consumer changes.

**The twelfth partition, `offer.ts`.** BP-020 decision 5 names eleven partitions
by module. The seven BP-001-owned surfaces cut on 2026-08-31 (WO-241..WO-247)
include `/pricing`, whose sentences are BP-030's `PRICE_COPY_KEYS` and BP-031's
`OfferTerms` keys — a module with no partition in that list, because the list was
written before those work orders existed. Options: put them in `settings.ts` or
`setup.ts` (wrong module, and the next reader looking for the price sentence has
two places to look), let WO-241 add a partition (then WO-241 modifies
`registry.ts`, which decision 5 bounds to one line but which is still a second
work order writing this file), or declare the twelfth here. **Chosen: declare it
here**, where the partition list is written once anyway, so `registry.ts` still
has exactly one author. The two opt-out lines WO-247 renders go in `mail.ts` —
BP-029 owns the opt-out and lives in `src/lib/mail/leads/**`, so that partition
is already the right module and no thirteenth is needed. Cost of reversing: one
import and one spread line. Reported to the architect for BP-020's interface
comment, which lists eleven.

## Rollback
Delete the eighteen files. Nothing imports them yet at the time this WO lands,
and no migration, schema or repo-root file is touched.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-020 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-093 as it stands on disk.
- **Bounded.** 17 files in the file plan (17 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — re-cut against BP-020 decision 5: `registry.ts` is now written once over twelve `copy/keys/` partition files instead of being appended to by every block, the nine ruled words are transcribed into `keys/bands.ts` with REQ-004 c1's four, and the owner-owed `rests-on` row is discharged.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-001); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — the twelve partition files, `registry.ts`, `copy.ts` and `index.ts` built per steps 1-7; `registry.test.ts` and `copy.test.ts` written and watched to fail before the source existed; ready to validate.

## Validation report (appended by validator)
