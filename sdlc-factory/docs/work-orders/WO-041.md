---
id: WO-041
type: work-order
title: "The copy registry: COPY, COPY_META and copy() — the closed set of written strings"
status: approved
ui: no
implements: [BP-020, REQ-093]
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "No source tree, package manifest, tsconfig or test runner exists on disk at cut time (verified 2026-08-31: the repo root holds only BUILD.md, DATA-COSTS.md, REQUIREMENTS-CONSOLIDATION.md, factory.config.json, CLAUDE.md and sdlc-factory/). Every path in this file plan is NEW and its placement is provisional against a repo with no code. The TypeScript project, its path resolution and the `*.test.ts` runner are established by work orders outside WO-041..WO-050; this WO creates none of them and adds no repo-root file."
    disposition: confirmed
  - claim: "Nine values in COPY were owner-owed at cut time and no agent could fill them (BP-020 rests-on row 1: the three winnability band words, the three rival-size band words, REQ-009 criterion 8's three severity words). **Discharged 2026-08-31**: the owner ruled all nine and BP-019 decision 6 records them verbatim, so this WO transcribes them from that approved entry into `keys/bands.ts` rather than seeding them empty. Every other seeded key remains owner-owed and empty, and the machinery below is what makes an unfilled key a loud failure instead of a blank."
    disposition: confirmed
supersedes: []
wave: W2
---

# WO-041 — The copy registry: COPY, COPY_META and copy() — the closed set of written strings

- Branch: wo/WO-041-copy-registry
- Estimate: ≤1 day

## Goal
Create `src/lib/presentation/copy/` holding the twelve partition files, the
composed frozen `COPY` literal, the total `COPY_META` map, `copy()`, and the
owner-owed machinery that makes an unwritten sentence a loud failure rather than
a blank on a screen.

## File plan

> **Re-cut 2026-08-31 against BP-020 decision 5.** As this work order first
> stood, `registry.ts` was created here and appended to by every later block that
> owns a sentence — one file, eleven blocks, every one of them conflicting with
> every other. BP-020 decision 5 repartitions it: one partition file per owning
> module under `copy/keys/`, and `registry.ts` naming the closed partition list,
> **written once, here, with every partition present — empty ones included**. A
> later block fills only its own partition file and touches nothing this work
> order writes twice.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/presentation/copy/keys/report.ts` | **NEW** | `REPORT_COPY: CopyPartition` — the report surface's sentences. Seeded with `place.report.first-page.rival` (see Steps 3). |
| `src/lib/presentation/copy/keys/overview.ts` | **NEW** | `OVERVIEW_COPY` — Overview's. Seeded with the three `place.overview.weekly-presence.*` keys. |
| `src/lib/presentation/copy/keys/calendar.ts` | **NEW** | `CALENDAR_COPY` — the calendar's. Seeded with `place.calendar.date.page`, `cause.unrecognised`, `cause.supply-exhausted`. |
| `src/lib/presentation/copy/keys/draft.ts` | **NEW** | `DRAFT_COPY` — the draft view's. **Empty**, exported frozen, for its own block to fill. |
| `src/lib/presentation/copy/keys/settings.ts` | **NEW** | `SETTINGS_COPY` — Settings'. **Empty.** |
| `src/lib/presentation/copy/keys/setup.ts` | **NEW** | `SETUP_COPY` — setup's. **Empty.** |
| `src/lib/presentation/copy/keys/publish.ts` | **NEW** | `PUBLISH_COPY` — publishing's. **Empty.** |
| `src/lib/presentation/copy/keys/danger.ts` | **NEW** | `DANGER_COPY` — the destructive-action surfaces'. **Empty.** |
| `src/lib/presentation/copy/keys/mail.ts` | **NEW** | `MAIL_COPY` — the mail seam's, BP-016 and BP-029. Seeded with the two opt-out surface lines (Steps 4). |
| `src/lib/presentation/copy/keys/offer.ts` | **NEW** | `OFFER_COPY` — the price and offer surfaces', BP-030 and BP-031. Seeded with the nine keys those two nodes fix (Steps 4). This is the one partition added beyond BP-020's named eleven; see **Decision taken under rule 1.1**, second entry. |
| `src/lib/presentation/copy/keys/bands.ts` | **NEW** | `BANDS_COPY` — the band and severity words. **The one partition seeded with real strings**: BP-019 decision 6's nine ruled words plus REQ-004 criterion 1's four score-band words, transcribed from those approved entries (Steps 5). Nothing here is chosen. |
| `src/lib/presentation/copy/keys/laws.ts` | **NEW** | `LAWS_COPY` — the sentences the four cross-cutting laws are made of, which no single surface owns. Seeded with the five `stopped.work.*`, the five `next-publish.*` and the two `generated.page.*` keys. |
| `src/lib/presentation/copy/registry.ts` | **NEW** | Names the closed partition list — the twelve imports above and no thirteenth — spreads them into `COPY` and `COPY_META`, freezes both, and derives `CopyKey = keyof typeof COPY`. Declares `CopyMeta`, `CopyPartition` and `OWNER_OWED: readonly CopyKey[]`. **Written once**: a later work order that adds a partition adds one import and one spread and is by construction the only one touching it. |
| `src/lib/presentation/copy/copy.ts` | **NEW** | `copy(key, vars?)` — one property read plus named-slot substitution; throws naming the key when the key is in `OWNER_OWED`, and throws naming the key when `COPY_META[key].slots` declares a `'measured'` slot (BP-020 Decisions 2 — the runtime half; the type half is WO-042's). No interpolation engine, no catalogue load, no network, no clock, no session parameter. |
| `src/lib/presentation/copy/index.ts` | **NEW** | The module's public entry point: re-export `COPY`, `COPY_META`, `copy`, and the types `CopyKey`, `CopyMeta`, `CopyPartition`. Nothing else — no partition is re-exported by name, so nothing outside this module can read one partition and miss the rest. |
| `tests/presentation/copy/registry.test.ts` | **NEW** | TST for partition composition, registry totality, freezing, owner-owed consistency and the no-model-import property. |
| `tests/presentation/copy/copy.test.ts` | **NEW** | TST for `copy()` behaviour, including the two throwing paths and the reader-invariance property. |

## Interfaces
**Consumes:** nothing from another node. This module is pure: no network, no
database, no session, no clock (BP-020 `## Module / boundary`).

**Exposes** (verbatim from BP-020 `## Public interface`):
```ts
// ── src/lib/presentation/copy/keys/<partition>.ts ────────────────────────────
/** The sentences and their metadata are written **one partition file per owning
 *  module** — `report.ts`, `overview.ts`, `calendar.ts`, `draft.ts`,
 *  `settings.ts`, `setup.ts`, `publish.ts`, `danger.ts`, `mail.ts`, `bands.ts`,
 *  `laws.ts` — each exporting a frozen `{ key: [string, CopyMeta] }` block for
 *  its own module and nothing else. See decision 5: this is what keeps two
 *  parallel work orders off one file. */
export const REPORT_COPY: CopyPartition    // and one such export per partition
export type CopyPartition = Readonly<Record<string, readonly [string, CopyMeta]>>

// ── src/lib/presentation/copy/registry.ts ────────────────────────────────────
/** Every sentence the product speaks in its own voice, composed from the closed
 *  partition list. A frozen object literal in the bundle — not a fetch, not a
 *  template store, not an i18n catalogue — so REQ-093 criterion 5 holds with
 *  every model unavailable. This file is written **once**, with every partition
 *  named, and is not appended to block by block. */
export const COPY: Readonly<Record<CopyKey, string>>
export type CopyKey = keyof typeof COPY

/** The parallel, exhaustive metadata map, projected from the same partitions.
 *  `Record<CopyKey, CopyMeta>` is total, so a key added without meta is a
 *  compile error — this is the hook every law in this block is driven off, and
 *  the reason no law has to be remembered. Because both maps are projected from
 *  one partition entry, "a key without meta" is now unrepresentable rather than
 *  merely checked. */
export const COPY_META: Readonly<Record<CopyKey, CopyMeta>>

export interface CopyMeta {
  /** Which cross-cutting law, if any, governs this sentence. The conformance
   *  suites enumerate their own scope from this tag rather than from a list a
   *  developer maintains. */
  law?: 'next-publish' | 'stopped-work' | 'no-presence-yet' | 'unmeasured'
  /** Named text slots and what each accepts. A slot of kind 'measured' may be
   *  filled only through `explain()`; `copy()` refuses such a key. */
  slots: Readonly<Record<string, 'text' | 'date' | 'measured'>>
  /** The requirement clause that fixes what this sentence must say. */
  fixedBy: string           // e.g. "REQ-092 c4"
}

// ── src/lib/presentation/copy/copy.ts ────────────────────────────────────────
/** A sentence carrying no measurement. Signature is BP-019's, verbatim. */
export function copy(key: CopyKey, vars?: Record<string, string | number>): string
```

**Exposes additionally** (this WO's, not the blueprint's — see Steps 3):
```ts
export const OWNER_OWED: readonly CopyKey[]
```

## Steps
1. Create the twelve partition files under `src/lib/presentation/copy/keys/`.
   Each exports exactly one frozen `CopyPartition` and imports only the
   `CopyMeta`/`CopyPartition` types. A partition file imports no other partition
   and no surface. Eight of the twelve are created **empty** — an empty frozen
   object is a partition with no sentences yet, which is what lets a later block
   fill its own file and touch nothing else.
2. Create `registry.ts`. Import the twelve partitions by name, spread them in a
   fixed order into one object literal ending
   `as const satisfies Record<string, readonly [string, CopyMeta]>`, then project
   `COPY` (entry `[0]`) and `COPY_META` (entry `[1]`) from it and freeze both
   with `Object.freeze`. Derive `export type CopyKey = keyof typeof COPY`.
   **`satisfies`, not a type annotation**: an annotation widens each entry's
   `slots` to `Record<string, 'text'|'date'|'measured'>` and destroys the per-key
   slot literals WO-042's `ExplainKey` is computed from, while `satisfies` keeps
   the literal types *and* keeps the `Record<CopyKey, …>` totality BP-020
   Decisions 1 relies on. A key declared in two partitions is a duplicate-key
   compile error in the spread — assert it in the test rather than resolving it
   by order.
3. Seed the law keys into `keys/laws.ts`, each with `''` as its value and each
   listed in `OWNER_OWED`. **No string is written.** The seed set, and the clause
   each `fixedBy` cites:
   - `stopped.work.line` — `fixedBy: "REQ-092 c1"`, `law: 'stopped-work'`
   - `stopped.work.needs-nothing` — `fixedBy: "REQ-092 c2"`, `law: 'stopped-work'`
   - `stopped.work.resumes-on` — `fixedBy: "REQ-092 c4"`, `law: 'stopped-work'`,
     `slots: { date: 'date' }`
   - `stopped.work.no-time-promised` — `fixedBy: "REQ-092 c4"`, `law: 'stopped-work'`
   - `stopped.work.partial-pass` — `fixedBy: "REQ-092 c6"`, `law: 'stopped-work'`
   - `next-publish.stopped` — `fixedBy: "REQ-092 c7"`, `law: 'next-publish'`
   - `next-publish.scheduled` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`,
     `slots: { at: 'date' }`
   - `next-publish.paused` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`
   - `next-publish.nothing-approved` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`
   - `next-publish.none-planned` — `fixedBy: "REQ-040 c4"`, `law: 'next-publish'`
   - `generated.page.written` — `fixedBy: "REQ-093 c2"`, `slots: { pageTitle: 'text' }`
   - `generated.page.proposed` — `fixedBy: "REQ-093 c2"`, `slots: { pageTitle: 'text' }`
   And the place and cause keys into the partition of the surface that renders
   each, on the same terms — empty value, listed in `OWNER_OWED`:
   - `keys/overview.ts`: `place.overview.weekly-presence.chart` —
     `fixedBy: "REQ-041 c3"`, `law: 'no-presence-yet'`;
     `place.overview.weekly-presence.week` — `fixedBy: "REQ-065 c3"`,
     `law: 'no-presence-yet'`;
     `place.overview.weekly-presence.partial-week` — `fixedBy: "REQ-065 c4"`,
     `law: 'no-presence-yet'`
   - `keys/calendar.ts`: `place.calendar.date.page` — `fixedBy: "REQ-043 c5"`,
     `law: 'no-presence-yet'`; `cause.unrecognised` — `fixedBy: "REQ-043 c4"`,
     `law: 'no-presence-yet'`; `cause.supply-exhausted` —
     `fixedBy: "REQ-043 c3"`, `law: 'no-presence-yet'`
   - `keys/report.ts`: `place.report.first-page.rival` —
     `fixedBy: "REQ-010 c1"`, `law: 'no-presence-yet'`
   Every entry above has `slots: {}` unless a `slots` value is given.
4. Seed the two partitions the seven BP-001-owned surfaces need, same terms —
   empty value, listed in `OWNER_OWED`, no string written:
   - `keys/offer.ts`: `price.amount`, `price.vat_included`, `price.interval`
     (BP-030's `PRICE_COPY_KEYS`, spelled there verbatim) — each
     `fixedBy: "REQ-022 c1"`; `offer.cadence.page`, `offer.cadence.measure`,
     `offer.cadence.movement`, `offer.veto.window` — each
     `fixedBy: "REQ-021 c2"`; `offer.cancel_self_service` —
     `fixedBy: "REQ-021 c2"`; `offer.start` — `fixedBy: "REQ-021 c4"`. The four
     `offer.cadence.*`/`offer.veto.*` keys carry `slots: { value: 'text' }`; the
     values are BP-005 pins supplied by BP-031's `offerTerms()`, never written
     into the sentence here.
   - `keys/mail.ts`: `optout.confirmed` — `fixedBy: "REQ-011 c3"`;
     `optout.invalid` — `fixedBy: "REQ-011 c3"`.
5. Fill `keys/bands.ts` with the thirteen band and severity words. These are the
   one exception to "no string is written here", and they are **transcriptions of
   approved artifacts, not choices** (rule 1.2 — never invent a customer fact;
   copying a recorded owner ruling is not inventing one). Each entry carries the
   artifact it was transcribed from in a comment above it:
   - from **BP-019 decision 6** (owner ruling, 2026-08-31):
     `band.winnability.winnable` → `Winnable`;
     `band.winnability.reach` → `Reach`;
     `band.winnability.notYet` → `Not yet` — **unhyphenated**, and the note that
     the hyphenated `'not-yet'` is the internal handle and never the rendered
     term; `band.rivalSize.near` → `Similar size`;
     `band.rivalSize.middle` → `Larger`; `band.rivalSize.far` → `Much larger`;
     `severity.low` → `Minor`; `severity.mid` → `Worth fixing`;
     `severity.high` → `Critical`. Each `fixedBy` is `"REQ-047 c10"`,
     `"REQ-096 c2"` or `"REQ-009 c8"` as BP-019 records.
   - from **REQ-004 criterion 1**, whose own words they are:
     `band.score.invisible` → `Invisible`; `band.score.hard-to-find` →
     `Hard to find`; `band.score.findable` → `Findable`;
     `band.score.dominant` → `Dominant`. `fixedBy: "REQ-004 c1"`.
   None of the thirteen is in `OWNER_OWED`: they have values, so `copy()` returns
   them.
6. Create `copy.ts`. Implementation order inside the function: (a) if
   `OWNER_OWED` contains the key, throw `Error` whose message contains the key
   and the word `owner-owed`; (b) if any value in `COPY_META[key].slots` is
   `'measured'`, throw `Error` whose message contains the key; (c) substitute
   named slots from `vars` and return. Any slot named in `COPY_META[key].slots`
   and absent from `vars` throws naming the slot — a half-substituted sentence is
   the blank REQ-093 exists to prevent. The function takes no session, reader or
   persona argument and reads no module-level mutable state.
7. Create `index.ts` re-exporting exactly the symbols in `## Interfaces`.
8. Write `tests/presentation/copy/registry.test.ts` and
   `tests/presentation/copy/copy.test.ts` per the test plan. Watch each fail
   before the source file it exercises exists (constitution §8).

## Test plan
| Acceptance criterion (verbatim from REQ-093) | Test |
|---|---|
| 1. "Given any text the product renders or sends to a person — whether or not they are a customer: a verdict, an explanation, a label, a heading, an empty state, an alert, or anything else it says in its own voice — when they read it, then it is a written string in the product and no part of it was produced by a language model, unless criterion 2 or criterion 3 admits it. The kinds named here are illustrative, not a closed list: the criterion holds for every text the product renders or sends, on every surface it speaks on — a screen and a mail alike — including surfaces added after this requirement." | `registry.test.ts` → **"COPY is the only source of a product sentence"**: (a) `COPY` is frozen — a write to an existing key and an addition of a new key both throw in strict mode; (b) every value in `COPY` traces to a string literal present in one partition source read from disk, so no value was computed at load; (c) the module's transitive import graph, read from the source files, contains no path under `src/lib/llm/`. Discriminates: adding a getter-backed key, or an import of `src/lib/llm/`, fails it. The surface-wide half of this criterion is WO-044's sweep. |
| 4. "Given the product explains a number, verdict or reason, when the explanation renders, then every value inside it was read from a measurement the product stored for that domain, carrying the date of the scan it came from — never estimated, never recomputed for display, and never produced by a language model — and the words around those values do not change with who is reading." | `copy.test.ts` → **"copy() refuses a measured key and takes no reader"**: (a) `copy()` on a key whose meta declares a `'measured'` slot throws and the message names the key; (b) `copy.length === 2` and the exported signature accepts no third argument, so no session, reader or persona can vary the words; (c) two calls with identical arguments return byte-identical strings. The type-level half is WO-042's. |
| 5. "Given the product's language models are unavailable, when anyone opens a ReachKit surface, then every word in the product's own voice renders unchanged, and page content already written still renders from what was stored." | `registry.test.ts` → **"the registry renders with every model unavailable"**: import the module in a context where every export of `src/lib/llm/` is replaced by a throwing stub, and assert every non-owner-owed key returns its literal through `copy()` unchanged — the thirteen band and severity words included, which is the first time this assertion has anything to render. Discriminates: any lazy fetch or model call on the read path throws. The stored-page half is WO-043's. |

Additional tests this WO owns, not inherited from a criterion:
- `registry.test.ts` → **"the partition list is closed and total"** (BP-020
  decision 5): `registry.ts` imports exactly twelve partitions; every file under
  `keys/` is imported by it; every key in `COPY` traces to exactly one partition;
  and no partition file imports another partition or any surface. A `keys/` file
  nobody imports fails it — the failure mode decision 5's rejected
  "directory the build globs" alternative could not catch.
- `registry.test.ts` → **"COPY_META is total over CopyKey"**: a
  `@ts-expect-error` fixture adding a key to a partition as a bare string rather
  than a `[string, CopyMeta]` pair, asserting the compile error (BP-020 `## Error
  & edge behavior`, second bullet; under decision 5 a key without meta is
  unrepresentable rather than merely checked, and this is the test of that).
- `registry.test.ts` → **"owner-owed and empty agree both ways"**: every key in
  `OWNER_OWED` has `COPY[key] === ''`, and every key with `COPY[key] === ''` is
  in `OWNER_OWED`. Count assertion: the test states how many keys are owner-owed
  and how many are filled (rule 5.5 — a silent index reads as nothing to report).
- `registry.test.ts` → **"the thirteen band words are the ruled words"**: each of
  the nine asserted against BP-019 decision 6's recorded value and each of the
  four against REQ-004 criterion 1's, quoted in the test body (rule 5.3). Also
  asserts `band.winnability.notYet` renders `Not yet` and never `Not-yet` — the
  transcription note BP-019 decision 6 deliberately did not smooth.
- `copy.test.ts` → **"an owner-owed key throws rather than rendering blank"**:
  `copy()` on each `OWNER_OWED` key throws with the key in the message.
- `copy.test.ts` → **"a missing key is a compile error"**: `@ts-expect-error`
  fixture calling `copy('not-a-key')`.

## Out of scope
- `explain()` and the measured-slot type machinery — WO-042.
- `GeneratedText`, `PageIdentity`, `renderGenerated`, `renderQuestion` — WO-043.
- The string-literal sweep and the generated-text flow gate — WO-044.
- `BAND_LABELS`, `SCORE_BANDS`, `SEVERITY` and `renderMeasured` — BP-019's maps,
  not this node's. This WO writes the thirteen **strings** they resolve to and
  none of the three maps; the maps carry `CopyKey`s and are BP-019's to declare.
- Filling any empty partition. Eight of the twelve ship empty on purpose; the
  block that owns each module fills its own file.
- Writing any customer-visible string other than the thirteen transcribed in
  Steps 5. Every other seeded key is owner-owed and empty.
- Any repo-root file (`package.json`, `tsconfig.json`, lint or CI config).

## Decision taken under rule 1.1
**The empty-value-plus-throw representation.** `COPY`'s declared type is
`Readonly<Record<CopyKey, string>>`, so a key whose string the owner has not yet
supplied has no representable "absent" value. Three options: omit the key (then
every downstream module referencing it fails to compile and cannot be built or
tested at all), give it a placeholder sentence (forbidden — the owner owns
customer-visible strings, and a placeholder is the thing REQ-091 criterion 2
forbids on a screen), or give it `''` and make reading it throw. **Chosen: `''`
plus `OWNER_OWED` plus a throw.** Derivation: the key is an internal name (the
system's, per constitution §1), the empty value is not a string a reader can ever
see because `copy()` refuses it, and the failure is loud at the call site rather
than blank on a screen. Cost of reversing: one line per key — delete the
`OWNER_OWED` entry and paste the owner's sentence in; no consumer changes.

**The twelfth partition, `offer.ts`.** BP-020 decision 5 names eleven partitions
by module. The seven BP-001-owned surfaces cut on 2026-08-31 (WO-241..WO-247)
include `/pricing`, whose sentences are BP-030's `PRICE_COPY_KEYS` and BP-031's
`OfferTerms` keys — a module with no partition in that list, because the list was
written before those work orders existed. Options: put them in `settings.ts` or
`setup.ts` (wrong module, and the next reader looking for the price sentence has
two places to look), let WO-241 add a partition (then WO-241 modifies
`registry.ts`, which decision 5 bounds to one line but which is still a second
work order writing this file), or declare the twelfth here. **Chosen: declare it
here**, where the partition list is written once anyway, so `registry.ts` still
has exactly one author. The two opt-out lines WO-247 renders go in `mail.ts` —
BP-029 owns the opt-out and lives in `src/lib/mail/leads/**`, so that partition
is already the right module and no thirteenth is needed. Cost of reversing: one
import and one spread line. Reported to the architect for BP-020's interface
comment, which lists eleven.

## Rollback
Delete the eighteen files. Nothing imports them yet at the time this WO lands,
and no migration, schema or repo-root file is touched.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-020 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-093 as it stands on disk.
- **Bounded.** 17 files in the file plan (17 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — re-cut against BP-020 decision 5: `registry.ts` is now written once over twelve `copy/keys/` partition files instead of being appended to by every block, the nine ruled words are transcribed into `keys/bands.ts` with REQ-004 c1's four, and the owner-owed `rests-on` row is discharged.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-001); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — the twelve partition files, `registry.ts`, `copy.ts` and `index.ts` built per steps 1-7; `registry.test.ts` and `copy.test.ts` written and watched to fail before the source existed; ready to validate.
- 2026-09-03 failed — validator — TST-018: findings, not reject — 2 same-file test defects found by mutation (REQ-093 c4's measured-slot-throw assertion in `copy.test.ts` doesn't discriminate its own feature because the fixture supplies no `vars`; the REQ-093 c5 `vi.doMock("@/lib/llm", …)` block in `registry.test.ts` is dead code against a module path that doesn't exist yet) — next: implementer fixes both assertions in `tests/presentation/copy/copy.test.ts` and `tests/presentation/copy/registry.test.ts`, then re-validate.
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — TST-018 defect 1: `copy.test.ts`'s measured-slot test now supplies `vars` satisfying the declared slot and asserts on the distinguishing message, so the missing-vars throw can no longer masquerade as the measured-slot throw (watched fail on the reintroduced mutation, then reverted). Defect 2: removed the dead `vi.doMock("@/lib/llm", …)` apparatus from `registry.test.ts`'s c5 test — `src/lib/llm/` doesn't exist and nothing in the copy module's import graph reaches it — and kept its real, discriminating assertions (13-key count, literal equality via `copy()`), grounded structurally by c1's zero-import-graph test. `npm run typecheck`, `npm run lint` (scoped) and the full suite (452/452) pass; commit `ee5eef9`; ready to re-validate.
- 2026-09-03 finished — validator — TST-019: pass — independently re-probed both TST-018 fixes by mutation (deleted and restored the measured-slot check; the c4 test now discriminates it, previously it did not); confirmed fix 2's removal is sound by injecting a fabricated `lib/llm` import and watching the retained c1 zero-import-graph test catch it. Full suite 452/452, typecheck and scoped lint clean, scope check clean (2 test files + WO's own `## Log`, nothing else). Both `rests-on` rows re-checked, remain `confirmed`; no row `open` on this WO.
- 2026-09-03 finished — validator — `/regress`: full suite (452/452), typecheck, build and test:layout (16/16) all clean; `factory-console impact WO-041` — 25 files, 2 blueprints (BP-019, BP-020), 0 importers, 1 requirement (REQ-093), re-checked live against current code; none of the nine named `done` WOs (WO-001, WO-002, WO-005, WO-006, WO-029, WO-030, WO-051, WO-267, WO-268) shares code with this WO's impact set and all their tests remain green. Regression line appended to `## TST-019`.

## Validation report (appended by validator)

## TST-018 — WO-041 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-041`, branch `wo/WO-041-copy-registry`, implementer HEAD `0170c8f` (single commit on top of `64b2204`). Working tree clean before and after this pass (the only file this pass touches is `WO-041.md`'s own `## Validation report` and `## Log`).
- Corpus-global id: `TST-018` — next unused after a corpus-wide scan (`grep -rhon "^## TST-[0-9]*" sdlc-factory/docs/work-orders/*.md`; highest existing is `TST-017`, WO-051).

### Scope check

`git show 0170c8f --stat`: exactly the 17 file-plan files (17 NEW) plus `WO-041.md`'s own `## Log` (2 lines). No repo-root file touched, matching `## Rollback`'s claim. `npm run typecheck`, `npm run lint` (scoped to the touched files) and the full suite all run clean from the worktree as committed.

### Criterion-by-criterion (WO's own `## Test plan`, 3 rows + WO-owned tests)

| # | Acceptance criterion | Test | Result |
|---|---|---|---|
| REQ-093 c1 | "no part of it was produced by a language model, unless criterion 2 or criterion 3 admits it" (registry half) | `registry.test.ts` → "COPY is the only source of a product sentence" (frozen, traces-to-literal, no-`lib/llm`-import) | PASS — reproduced. Discriminates: removing `Object.freeze` on `COPY` flips 3 tests red (the freeze test itself, plus 2 downstream literal-value tests once a write-then-read is attempted — verified directly); the import-graph test correctly asserts today's zero-external-import state and would flip on a real `lib/llm` specifier (independently probed below). |
| REQ-093 c4 | "every value inside it was read from a measurement... never produced by a language model... the words around those values do not change with who is reading" | `copy.test.ts` → "copy() refuses a measured key and takes no reader" | **PARTIAL** — 2 of 3 assertions discriminate (`copy.length === 2` flips if the signature grows a third parameter; the byte-identical-output check flips under a mutated substitution step). The **measured-slot-throw assertion does not discriminate**: deleting the `'measured'`-slot check in `copy.ts` entirely (mutation, reverted) leaves this test green, because the test calls `copyFn("fixture.measured")` with **no `vars`**, so the *general* "slot named in `meta.slots` and absent from `vars`" throw (step 6c) fires instead and its message also contains the key `"fixture.measured"` — the assertion only checks `.toThrow("fixture.measured")`, which both code paths satisfy. Supplying `vars: { amount: "x" }` in the fixture would let the mutation through undetected under the *current* wrong-vars framing, i.e. it would properly discriminate (confirmed by hand: with `vars` supplied and the check removed, the mutated function returns a substituted string instead of throwing). This is a **coverage gap on this WO's own test-plan row**, not one beyond scope — see Defects below. |
| REQ-093 c5 | "every word in the product's own voice renders unchanged" with every model unavailable | `registry.test.ts` → "the registry renders with every model unavailable" | **PASS, but the `vi.doMock("@/lib/llm", …)` mechanism is dead code** — `src/lib/llm/` does not exist in this repo yet (confirmed: `ls src/lib/llm` → no such directory) and nothing in the copy module's import graph references `@/lib/llm` (confirmed by the c1 import-graph test's own comment: "today this module has no external import at all"). Deleting the entire `vi.doMock`/`vi.doUnmock` block (mutation, reverted) leaves the test's pass/fail outcome **unchanged** — the mock is never reached because nothing ever imports the mocked specifier. What the test actually verifies — that the 13 filled keys return their literal via `copy()`, with a count assertion (13) — is real and does discriminate (a `freeze`-mutation on `COPY` flips it, as above), and is otherwise redundant with "the thirteen band words are the ruled words". The criterion itself is genuinely satisfied, but structurally (zero import path to any model), not by the mocking mechanism the test's name and comment claim. Not a criterion failure; a misleading/vacuous test-quality defect — see Defects below. |

### Additional tests this WO owns (not inherited from a criterion)

| Test | Result | Mutation applied |
|---|---|---|
| "the partition list is closed and total" — exactly 12 `keys/` files, `registry.ts` imports all 12 and no 13th, every key traces to exactly one partition, no partition imports another | PASS | Added a duplicate key (`"price.amount"`) to `danger.ts` (already declared in `offer.ts`) — `"every key in COPY traces to exactly one partition"` correctly fails, naming both files. Reverted. |
| "COPY_META is total over CopyKey" (`@ts-expect-error` fixture) | PASS (typecheck-discharged) | Independently confirmed the fixture line does fail without `@ts-expect-error` (a bare string is not assignable to `readonly [string, CopyMeta]`) by removing the suppression comment and re-running `tsc --noEmit`: `error TS2578: Unused '@ts-expect-error' directive` disappears and a real `TS2322` appears instead when the object literal is left unguarded — confirms the fixture is live, not stale. |
| "owner-owed and empty agree both ways" + count assertion (30/13/43) | PASS | Verified the counts by direct enumeration of all 12 partition files' entries (30 empty-string entries, 13 non-empty) — matches. |
| "the thirteen band words are the ruled words" | PASS | Changed `band.winnability.notYet` from `"Not yet"` to `"Not-yet"` in `bands.ts` (mutation, reverted) — the dedicated "never `'Not-yet'`" assertion correctly fails. |
| "an owner-owed key throws rather than rendering blank" | PASS | Removed the `OWNER_OWED`-throw branch from `copy.ts` (mutation, reverted) — fails with `expected [Function] to throw an error`. Correctly localised. |
| "a missing key is a compile error" (`@ts-expect-error`) | PASS (typecheck-discharged) | Same class of check as `COPY_META` totality; not independently re-probed beyond confirming `tsc --noEmit` is clean on the committed fixture. |

### Watch-it-fail-first

Not independently re-run from a pre-source state (the module now exists and deleting 17 files to re-observe red is destructive to the worktree); the implementer's log and commit message state it was watched to fail first, and the committed tests import the module by its real path with no fallback/optional-chaining that would let a missing module pass silently — a `describe`/`it` block importing a nonexistent `src/lib/presentation/copy/index.ts` would throw at collection time, which is a hard red, not a soft one. Accepted on the structural argument, consistent with the log claim (rule 6.1).

### Mutation summary (constitution §8)

Eight independent, targeted mutations against the five source files, each reverted before the next: (1) remove `OWNER_OWED` throw — 1 test failed, correctly localised; (2) remove `'measured'`-slot throw — **0 tests failed, see REQ-093 c4 above**; (3) un-freeze `COPY` — 3 tests failed; (4) invert `band.winnability.notYet` — 1 test failed, correctly localised; (5) remove slot-substitution — 2 tests failed; (6) add a cross-partition duplicate key — 1 test failed, naming both files; (7) remove the entire `vi.doMock("@/lib/llm", …)` block — **0 tests failed, see REQ-093 c5 above**; (8) add a real dynamic `import("@/lib/llm")` inside `copy.ts` — every test in the file crashed with `ERR_MODULE_NOT_FOUND` (the module genuinely does not exist on disk), which is a cruder but real backstop: this repo cannot currently ship a working build that reaches into `src/lib/llm/` from this module at all. All eight mutations independently authored and applied by the validator, all reverted; `git status` confirms the worktree matches the implementer's committed content after this pass.

### Defects (findings — not a reject)

1. **`tests/presentation/copy/copy.test.ts`, "throws naming the key when its meta declares a 'measured' slot"** — does not discriminate the measured-slot check from the general missing-`vars` throw, because the fixture call supplies no `vars` and both throw paths embed the key in their message. Fix: call with `vars` that *would* satisfy every declared slot (e.g. `{ amount: "x" }`) so only the measured-slot check can produce the throw, or assert on distinguishing message content (e.g. `toThrow(/measured/)`) instead of the key alone. Implementer's to fix; re-validate the single test file after.
2. **`tests/presentation/copy/registry.test.ts`, the REQ-093 c5 block's `vi.doMock("@/lib/llm", …)`** — dead code against the current tree: `src/lib/llm/` doesn't exist yet and nothing in this module's import graph reaches for it, so the mock is never exercised (confirmed by deletion). The test's real, functioning assertions (13 non-owner-owed keys render their literal, with a count check) should stay; the mocking apparatus should either be removed as misleading until `src/lib/llm/` exists, or re-scoped to state plainly that it is a forward-looking guard with no bite today. Not blocking — the criterion is genuinely met structurally (zero imports) — but the test's docstring claim ("every export of `src/lib/llm/` replaced by a throwing stub") overstates what it currently does.

Neither defect is a reject: both are narrow, one-file fixes that do not touch the file plan, the interfaces, or any other test, and every other criterion and WO-owned test discriminates correctly under mutation.

### Independent verification of the five flagged judgment calls

1. **Type-only circular import (`keys/*.ts` ↔ `registry.ts`) — confirmed no runtime cycle.** Every `keys/*.ts` file uses `import type { CopyPartition } from "../registry.ts"` (grepped all 12 — no value import of `registry.ts` anywhere under `keys/`). `tsconfig.json` sets `isolatedModules: true`, which requires type-only imports to be always safely erasable per-file (not just under whole-program analysis). Directly compiled `bands.ts` in isolation with `tsc --isolatedModules`: the emitted `.js` carries **zero** import statements — the `import type` line is fully erased, confirming nothing survives to `require`/`import` at that end. `registry.js`'s emitted output still value-imports all 12 partitions with no back-edge. No runtime cycle exists; the implementer's claim is correct.
2. **No compile-time duplicate-key error from spreading partitions — confirmed correct, and the fallback is the right call.** Independently reproduced in an isolated `tsc --noEmit --strict` snippet: `{...A, ...B} as const satisfies Record<string, number>` with `A`/`B` sharing a key compiles with **no error** — TypeScript silently lets the later spread win, exactly as the implementer states (contradicting the WO's step 2 prose, which assumed a compile error). The runtime fallback the implementer wrote — pairwise-checking the 12 source partitions for a shared key before the spread runs — is proven to actually catch the case (mutation 6 above: injected a real duplicate, the test fails and names both files). This is the correct, evidenced resolution of the WO's own wrong assumption, not an unauthorized deviation — WO step 2 itself said "assert it in the test rather than resolving it by order" as its fallback instruction, which is exactly what was built.
3. **`{slotName}` placeholder syntax — genuinely an internal parameter, not an escalation.** No caller of `copy()` ever sees the placeholder string; it is consumed and replaced before the return value reaches anything outside `copy.ts`. It carries no customer-visible promise (constitution §1's table: "internal names, type members, module boundaries" are the system's to decide) and the WO's own comment states the reversal cost (one `.replace` call, no consumer change) per rule 1.1's requirement to record derivation and reversal cost. Correctly self-certified, correctly not escalated.
4. **`OWNER_OWED` excluded from `index.ts`'s barrel — matches the file plan's literal text.** File plan row for `index.ts` (WO-041.md line 57): "re-export `COPY`, `COPY_META`, `copy`, and the types `CopyKey`, `CopyMeta`, `CopyPartition`. Nothing else" — `OWNER_OWED` is not named. The blueprint's own `## Interfaces` section separates "Exposes" (BP-020's, excludes `OWNER_OWED`) from "Exposes additionally" (this WO's own addition, `## Interfaces` line 111-113, includes it) — Step 7's "re-exporting exactly the symbols in `## Interfaces`" is arguably ambiguous between the two, but the file plan row is the more specific, row-level instruction and the one the implementer followed. `index.ts` as committed matches the file plan table verbatim. Both test files correctly import `OWNER_OWED` from its declaring file (`registry.ts`) directly, with a comment stating why, consistent with this choice. Accepted — noted as a minor internal inconsistency between WO step 7's prose and the file plan's own table (worth a line in a future WO template pass, not a defect of this build).
5. **`vi.doMock("@/lib/llm", …)` against a non-existent module path — confirmed vacuous, per Defect 2 above.** Not meaningful as written; see Defects.

### Structure compliance

`src/lib/presentation/copy/**` matches `registry/structure.md`'s row for `src/lib/presentation/**` (BP-020's leaf: `copy/` and `generated/`) verbatim, and matches BP-020's own `code:` glob. `grep -rln "presentation/copy\|COPY_META\|export const COPY\b" --include="*.ts" src/ tests/` outside the two new directories: no hits — no duplicate capability. BP-020's `## Public interface` block matches this WO's `## Interfaces` block, and the committed `registry.ts`/`copy.ts`/`index.ts` match both verbatim (diffed directly).

### `rests-on` disposition (constitution rule 2.3b)

**WO-041's own rows:**
1. *"No source tree, package manifest, tsconfig or test runner exists on disk at cut time... every path in this file plan is NEW and its placement is provisional against a repo with no code"* — **confirmed**. The scaffold this row anticipated (`package.json`, `tsconfig.json`, `vitest.config.ts`, the `@/*` path alias, the `*.test.ts` runner) now exists, built by work orders outside this range as the row predicted, and this build is the first observation of whether the file plan's provisional placement actually holds against it: `npm run typecheck`, the full suite (452/452), and `npm run lint` all run clean with every file exactly where the file plan put it — no placement adjustment was needed. This WO added no repo-root file (confirmed by `git show --stat`). The claim holds; disposition set to `confirmed`.
2. *"Nine values in COPY were owner-owed... Discharged 2026-08-31"* — already `disposition: confirmed` in the frontmatter at cut time (BP-019 decision 6's owner ruling, transcribed verbatim into `keys/bands.ts` and independently checked against the requirement/decision text above). Left as-is; this pass adds no new evidence beyond re-confirming the nine (plus REQ-004 c1's four) words match their cited source verbatim.

Both rows now carry a non-`open` disposition — no row is left `open` on this work order.

**Upstream (BP-020, this WO's `implements` edge):** BP-020 carries three `rests-on` rows. Row 1 (the nine/four band words) was already `confirmed` at BP-020's own creation and this build's bands.ts transcription corroborates it further, but does not newly discharge it. Rows 2 and 3 — the path-glob conformance-suite claim and the bounded-model-columns claim — concern the surface-wide sweep (ADR-010, WO-044's scope, explicitly named `## Out of scope` here: "The string-literal sweep and the generated-text flow gate — WO-044"); this build implements only the registry itself and observes neither claim. Left `open`, untouched, per the instruction to disposition only what this build observed — WO-044's validator is the first stage positioned to observe them.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

findings

Structure, scope, interfaces and 6 of the 8 tests/mutation-checked behaviours are sound: `npm run typecheck`, `npm run lint` (scoped) and the full suite (452/452) all pass; all 17 files match the file plan exactly, one commit, no repo-root file touched; all five flagged judgment calls independently verified correct (the type-only-import erasure, the duplicate-key runtime fallback, the internal placeholder syntax, the barrel's literal exclusion of `OWNER_OWED`, and — separately — the `vi.doMock` mechanism confirmed vacuous as flagged). Two narrow, same-file test defects found by mutation and reported above: the REQ-093 c4 measured-slot-throw assertion doesn't discriminate its own feature (fixable by supplying `vars` in the fixture or asserting distinguishing message content), and the REQ-093 c5 `vi.doMock("@/lib/llm", …)` block is dead code against a module path that doesn't exist yet in this repo (the criterion is still genuinely met, structurally, by the zero-import-graph fact the c1 test already establishes — but the test claiming to prove c5 via mocking doesn't). Neither defect touches the file plan, an interface, or any other test; both are one-file, same-test fixes for the implementer. Both `rests-on` rows on this WO dispositioned (`confirmed`, `confirmed`); BP-020's rows 2 and 3 correctly left `open` — out of this build's observed scope, named to WO-044's validator.

No `Regression:` line follows: that record is `/regress`'s to write, a separate dispatch from this `/validate` pass. This WO does not carry one yet and should not reach `done` until the two defects above are fixed and this report — or a follow-up validation pass — reflects a clean re-check.

## TST-019 — WO-041 re-validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-041`, branch `wo/WO-041-copy-registry`, tip `d139312` (implementer fix `ee5eef9` on top of the `0170c8f` build TST-018 examined, plus a log-only commit `d139312`). Working tree clean before and after this pass, confirmed by `git status --short` immediately before writing this section and again after each mutation probe below.
- Corpus-global id: `TST-019` — next unused after a corpus-wide scan of `sdlc-factory/docs/work-orders/*.md` across the main repo (`/root/projects/reachkitv3`, highest `TST-017`) and every worktree under `/root/projects/reachkitv3-wt/WO-*/` (highest `TST-018`, this WO's own prior report); no other worktree or the main repo carries a `TST-018` or higher.
- Scope: this is a re-validation dispatched to close the two findings TST-018 raised. It does not re-walk criteria, structure compliance or the five flagged judgment calls TST-018 already cleared — those are unchanged (no file outside the two test files moved between `0170c8f` and `ee5eef9`) — it independently re-probes the two fixes, re-runs the full suite, and re-checks nothing else regressed.

### Scope check

`git show ee5eef9 --stat`: exactly 2 files changed (`tests/presentation/copy/copy.test.ts`, `tests/presentation/copy/registry.test.ts`), both test files named in TST-018's own defect list, no source file, no file-plan file. `git show d139312 --stat`: `WO-041.md` only (the implementer's `## Log` entry). No file outside the WO's own scope changed across either commit.

### Independent re-probe of fix 1 (REQ-093 c4 measured-slot-throw)

Mutation applied and reverted by the validator, independently of the implementer's own log claim:

1. Removed the `'measured'`-slot throw block from `src/lib/presentation/copy/copy.ts` (the `for (const slotKind of Object.values(meta.slots))` loop and its `throw`), leaving `OWNER_OWED` throw and slot-substitution intact.
2. Ran `npx vitest run tests/presentation/copy/copy.test.ts`: **fails** — `throws naming the key when its meta declares a 'measured' slot` → `AssertionError: expected [Function] to throw an error`, the other 6 tests in the file unaffected (correctly localised).
3. Restored `copy.ts` from the pre-mutation copy (`git diff --stat` confirmed a clean revert), re-ran the same file: **7/7 pass**.

Fix 1 discriminates: the test now fails when the code it covers is deleted and passes when it is restored, closing TST-018 defect 1.

### Independent re-probe of fix 2 (REQ-093 c5 dead-mock removal)

Confirmed `src/lib/llm/` still does not exist on disk (`ls src/lib/llm` → no such file or directory) and the removed `vi.doMock("@/lib/llm", …)` apparatus is gone from `registry.test.ts` (diffed `ee5eef9` directly). To confirm the claim that the retained c1 "zero-import-graph" test is what actually grounds REQ-093 c5's "no reach into a language model" property — not merely an assumption — the validator independently added a fabricated external import (`import { foo } from "@/lib/llm/thing";`) to the top of `copy.ts` and re-ran `tests/presentation/copy/registry.test.ts`: the c1 test **the module's transitive import graph contains no path under src/lib/llm/** fails with `expected '@/lib/llm/thing' not to match /lib\/llm/`, 14/15 other tests in the file unaffected. Reverted; re-ran the file clean (15/15).

This confirms fix 2's removal is sound on its own terms, not merely on the implementer's assertion: a real import reaching toward `lib/llm` from anywhere in this module's transitive graph is caught by the c1 test that survives, independent of whatever mock c5 used to carry. The c5 test's own retained assertions (13-key count, literal equality via `copy()`) are unchanged and still discriminate on freeze-mutation, per TST-018's mutation 3.

### Full suite

`npm run test` (`vitest run --project node --project ui --project db`): **21 test files, 452/452 passing**, 0 failures — same count TST-018 recorded, confirming no regression outside the two touched test files. `npm run typecheck`: clean. `npx eslint tests/presentation/copy/ src/lib/presentation/copy/`: clean.

### `rests-on` disposition (constitution rule 2.3b) — re-check

Both of WO-041's own `rests-on` rows carry `disposition: confirmed` in front-matter, set at TST-018 and unchanged since (front-matter is untouched by `ee5eef9` and `d139312`). This re-validation pass observed nothing that bears on either claim (no scaffold change, no new band-word transcription) and finds no reason to revise either disposition: both remain **confirmed**. No row on this WO is `open`. Upstream, BP-020's rows 2 and 3 remain correctly left `open` per TST-018's reasoning — this build still implements only the registry itself (WO-044's scope, unchanged) and this re-validation pass observed nothing new about the surface-wide sweep either.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

pass

Both TST-018 findings independently re-probed and confirmed closed: the REQ-093 c4 measured-slot-throw test now fails when its own check is deleted (previously it did not) and passes on restore; the REQ-093 c5 dead-mock removal is sound — the retained c1 zero-import-graph test independently confirmed to catch a real `lib/llm`-reaching import, which is what the removed mock was standing in for. Scope check: exactly the 2 test files plus the WO's own `## Log` changed across `ee5eef9` and `d139312`; no file-plan file, no other test, no repo-root file touched. Full suite 452/452, typecheck clean, lint clean. Both `rests-on` rows re-checked and remain `confirmed`; no row is `open` on this WO. TST-018's structure-compliance, criterion table (rows not touched by the fix) and five flagged judgment calls are unchanged by this pass and are not re-litigated here — see TST-018 above.

No `Regression:` line follows: that record is `/regress`'s, a separate dispatch from this `/validate` re-pass.

### Regression pass (`/regress`, 2026-09-03)

Dispatched separately from the `/validate` passes above, against TST-019's passing verdict. Worktree unchanged since TST-019 (tip still `ebe2c83`; `git status --short` clean before and after).

- **Full suite, re-run independently of TST-019's own run:** `npm test` (`vitest run --project node --project ui --project db`) — 21 test files, 452/452 passing, 0 failures; same count TST-018 and TST-019 recorded. `npm run typecheck` — clean. `npm run build` (`next build`) — compiles and prerenders clean, not run by either prior pass. `npm run test:layout` (`vitest run --project layout`) — 3 test files, 16/16 passing.
- **`factory-console impact WO-041`** (read-only query, run from the worktree root): 25 files → 2 blueprints → 0 importers → 1 requirement.
  - Files: the 17 file-plan files under `src/lib/presentation/copy/**` and `tests/presentation/copy/**`, this WO's own `WO-041.md`, `sdlc-factory/docs/registry/waves.md`, and the `.md` files of WO-051, WO-056, WO-057, WO-058, WO-062, WO-070 — all reached only through the `depends-on` chain WO-070→WO-062→{WO-051,WO-058}→WO-057→{WO-056,WO-267,WO-006,WO-051}, not through any commit touching their code; none of those six is `done` except WO-051, and none has shipped code that imports the copy module (confirmed below).
  - Blueprints: BP-019, BP-020 — both anchor this WO's commits, neither's `code:` glob shows drift.
  - Importers: 0 — `grep -rln "presentation/copy" --include="*.ts" --include="*.tsx" src/ tests/` outside `src/lib/presentation/copy/` and `tests/presentation/copy/` itself returns nothing. Nothing in the current tree consumes `COPY`, `COPY_META` or `copy()` yet, so there is no downstream call site this build could have broken.
  - Requirements: REQ-093 — the only requirement either blueprint `satisfies`.
- **Re-checked REQ-093's acceptance criteria against the current code**, not restated from TST-018/TST-019: criteria 1, 4 and 5 are the three this WO's own test plan covers and are re-verified live by re-running `tests/presentation/copy/registry.test.ts` and `tests/presentation/copy/copy.test.ts` in isolation (`npx vitest run` on both) — 22/22 passing, matching TST-019's count, with no code change since that pass to have altered the result. Criteria 2 and 3 govern `GeneratedText`/`renderGenerated` and the twelve tracked questions — this WO's own `## Out of scope` names both as WO-043's and outside this build — and the impact set confirms no code implementing them exists yet under the files this WO's commits touched; neither is re-checked here because neither is observable from this build, consistent with TST-018/019's own scoping of BP-020's rows 2 and 3.
- **The nine `done` work orders named for this check** (WO-001, WO-002, WO-005, WO-006, WO-029, WO-030, WO-051, WO-267, WO-268): none appears among the 25 impacted files' *code*, and only WO-051's own `.md` appears at all, reached solely through the `depends-on` chain above, not through any shared source file. Their own test files (`tests/account/columns.test.ts`, `tests/app/*.test.ts`, `tests/config/*.test.ts`, `tests/db/*.test.ts`, `tests/ui/fonts.test.ts`, `tests/ui/tokens.test.ts`, `tests/ui/layout/*.test.ts`) are part of the 452/452 and 16/16 counts above and all pass unchanged.

Regression: 25 files · REQ-093 re-checked — pass
