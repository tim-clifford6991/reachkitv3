---
id: WO-276
type: work-order
title: "The fetch ledger and the cost context — one insert-only row, cache-first, cap-degrades"
status: approved
wave: W3
ui: no
implements: [BP-007, REQ-003]
depends-on: [WO-006, WO-267]
blocked-by: []
rests-on:
  - claim: "This migration creates `fetches`, which `BUILD.md` §10 lists among its nine baseline tables. WO-267 (`done`) deliberately leaves it out — its baseline migration's own header states the exclusion — because `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic and BP-007's `## Data model delta` fully specifies the table, so one table has one writer. The derivation and its reversal cost are recorded on WO-267's first `rests-on` row, which carries `disposition: confirmed`."
    disposition: confirmed
    reason: "TST-029 — observed directly, not merely read. `00000000000001_baseline.sql` applied alone creates exactly the eight tables `BUILD.md` §10 lists minus `fetches` (`users, sites, scans, opportunities, drafts, publications, destinations, leads`); the six migrations WO-267 through the free-path claim column, applied in order, add none. `20260903080000_fetches.sql` applied after them is the only statement in the composed, from-scratch migration chain that creates `fetches` — verified by applying all seven migrations to a freshly dropped `public` schema and enumerating `information_schema.tables` (one `fetches` row, one writer). One table, one writer, holds."
  - claim: "The `fetches` table this context writes to carries no unique constraint on `(source, cache_key, policy_version)` — BP-007 decision 3 (2026-09-03) dropped it, and the cache read is 'the newest row on that key inserted within `freshnessDays`, excluding a row whose payload is the vendor's own zero-result shape'. WO-021, which creates the table and is `approved` and unbuilt, still plans that constraint and predates the decision. If it ships as planned, the empty-payload re-buy this work order tests cannot insert its second row. Reported to the planner queue on 2026-09-03, not repaired here: `*_fetches*.sql`'s create-table statement is WO-021's file. **Discharged 2026-09-03**: the planner re-planned WO-021 in the same pass — its file plan carries no unique constraint, and its test plan now asserts positively that two rows on one `(source, cache_key, policy_version)` triple both insert. The premise no longer holds."
    disposition: refuted
supersedes: [WO-021, WO-022]
---

# WO-276 — The fetch ledger and the cost context — one insert-only row, cache-first, cap-degrades

- Branch: wo/WO-276-cost-context
- Estimate: ≤1 day

## Goal
Ship BP-007's spend seam whole: the one insert-only `fetches` row that is at once ledger, cache and raw store, and the per-scan context that reads it cache-first, ledgers every call that reached a vendor, and returns a value at the cap instead of throwing.

> **Read `decisions/ADR-094.md` before writing a line of this WO.** The
> reserve/settle split and the second-spender mechanics are that decision's and
> BP-028's, not this order's to re-derive.

## Consolidation (rule 2.6)
WO-021 created the `fetches` table; WO-022 created the only reader and the only
writer of it. Neither could state its stop condition without the other — WO-022's
goal is "reads cache first, ledgers what was spent", which is a claim about
WO-021's row, and WO-021's own `## Provenance` already folded `reserved_cents`
across the seam under rule 2.6 on 2026-09-03 with the note that "both files sit
under the same `*_fetches*.sql` glob, so no module boundary is crossed". They are
one capability from schema to test: what a call costs, where that is written
down, and what happens at the cap.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `supabase/migrations/20260903080000_fetches.sql` | NEW | WO-021 | `fetches` exactly to BP-007's `## Data model delta` — its nine columns including `reserved_cents`, **no unique constraint** (BP-007 decision 3), the `(source, cache_key, policy_version, created_at desc)` index for the cache read and the `scan_id` index for the ledger read. RLS enabled with **no request-scoped read policy** — `fetches` carries cost and raw vendor payloads and is reachable only through `dbAdmin()`. |
| `tests/costs/fetches-schema.test.ts` | NEW | WO-021 | The tests below. `structure.md` rule 4 puts BP-007's tests under `tests/costs/**`. |
| `src/lib/costs/index.ts` | NEW | WO-022 | `withCostContext()` and the `CostContext` implementation, exactly BP-007's interface. Opens the context, runs the body, and at close writes the roll-up (`spentCents`, `degraded`) to `scans.cost_cents` and `scans.status`. `recordFetch` **reserves** `costCents` before the call — the cap is checked against the reservation — and, where the call site supplied `settleCents`, **settles** the ledgered figure from the payload afterwards, clamped to the reservation and logged when it would exceed it. `spentCents()` sums settled figures. |
| `src/lib/costs/cache.ts` | NEW | WO-022 | The cache read keyed `source + cacheKey + policyVersion`: the newest row on that key inserted within `freshnessDays`, **excluding a row whose payload is the vendor's own zero-result shape** (BP-007 decision 3 — the exclusion is at the read, not the write). Nothing is ever deleted — a bumped `policyVersion` is how a changed derivation invalidates. |
| `src/lib/costs/ledger.ts` | NEW | WO-022 | `recordFetch`'s write half: a row is written for every call that reached the vendor, including one that succeeded at the vendor and failed on parse. Every row carries both figures — `reserved_cents` (what the cap was checked against) and `cost_cents` (the settled figure where one settled, the reservation otherwise). |
| `tests/costs/context.test.ts` | NEW | WO-022 | The tests below. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-021

Consumes: the RLS conventions WO-267 applied.
Exposes: the schema `withCostContext()` (WO-022) writes and reads. No code
interface.

### From WO-022

Exposes (verbatim from BP-007 `## Public interface`):
```ts
export type CapName = 'FREE' | 'DEEP' | 'WEEKLY' | 'DRAFT'

export function withCostContext<T>(
  ctx: { scanId: string; cap: CapName; policyVersion: number },
  body: (cost: CostContext) => Promise<T>
): Promise<T>

export interface CostContext {
  /** Cache-first, ledger-always. `fresh` false means the payload came from cache
   *  and cost nothing. An empty payload is always a miss; there is no negative
   *  cache. */
  recordFetch<P>(call: {
    source: string           // vendor endpoint or model id
    cacheKey: string
    freshnessDays: number
    /** The **reservation**: the most this call can cost, known before it runs.
     *  The cap is checked against this, so no call is made against money the
     *  context has not got. */
    costCents: number
    /** Optional **settlement** (ADR-094 decision 3a). Where a vendor's own
     *  documented rule makes the final charge a function of the response, the
     *  call site supplies it and the ledger records the settled figure instead
     *  of the reservation. `settled <= reserved` is enforced here, not trusted:
     *  a closure returning more is clamped to the reservation and logged, since
     *  the excess would be money the cap never authorised. One call site uses
     *  it today — BP-008's flagged `serpOrganic`. Omitting it is the ordinary
     *  case and behaves exactly as before this argument existed. */
    settleCents?: (payload: P) => number
    run: () => Promise<P>
  }): Promise<{ payload: P; fresh: boolean; costCents: number } | { skipped: 'cap' }>
  // The returned `costCents` is the settled figure where one was supplied, the
  // reservation otherwise — one number, and it is the one the row carries.

  capHit(): boolean          // re-checked between calls in any multi-call step
  spentCents(): number
  degraded(): boolean
}
```
Consumes: `db()`/`dbAdmin()` (BP-002), `CAPS` and `CACHE_WINDOWS_D` (BP-005).

## Steps

**WO-021's steps** (renumbered; order within the block unchanged):

1. Write the migration exactly to BP-007's data-model delta, `reserved_cents`
   included.
2. Add the two indexes, and no unique constraint (BP-007 decision 3).
3. Enable RLS with no request-scoped policy.
4. Write the tests; watch the "no second table" test fail first by adding a
   `spend_ledger` fixture table, and the re-buy test fail first by adding the
   unique constraint decision 3 dropped.

**WO-022's steps** (renumbered; order within the block unchanged):

5. Write `cache.ts` and prove the no-negative-cache rule by test.
6. Write `ledger.ts` and prove the spend-then-fail case; each row carries both figures.
7. Write `index.ts` with the three-armed `recordFetch` return, the cap checked against the
   reservation before `run()`, and the settlement applied after it where a closure was
   supplied — clamped at the reservation, never above it.
8. Write the tests; watch the cap case fail first by throwing a `CapExceeded`
   instead of returning `{ skipped: 'cap' }`, and the settlement case fail first by
   ledgering the reservation regardless of the closure's return.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| BP-007 data-model delta: "`fetches` — `id, scan_id, source, cache_key, policy_version, cost_cents, reserved_cents, payload jsonb, created_at`" … "Indexed `(source, cache_key, policy_version, created_at desc)` for the cache read and on `scan_id` for the ledger read." | WO-021 | `tests/costs/fetches-schema.test.ts` — asserts the column set and both indexes by name against the applied schema. A missing `reserved_cents` fails it. |
| BP-007 data-model delta: "**Insert-only; no unique constraint on `(source, cache_key, policy_version)`**" | WO-021 | Same file — two rows inserted on one `(source, cache_key, policy_version)` triple both persist and are distinguishable by `created_at desc`. Adding the constraint fails it. This is the schema half of the re-buy BP-007's error behaviour promises; the read predicate that serves it is WO-022's. |
| BP-007 data-model delta: "A column, not a second table: the two figures belong to the same call." · "No second table: the ledger, the cache and the raw store are one row" | WO-021 | Same file — `cost_cents` and `reserved_cents` are both columns of `fetches`, and no other table in `public` carries `cache_key` or `reserved_cents`. `scans.cost_cents` is excluded by name: BP-007's NFR budget writes that per-scan roll-up at close, so it is not a second ledger. |
| BP-002 error behaviour: "RLS is default-deny: a table with no policy is unreadable by anyone holding an anon or authenticated key." | WO-021 | Same file — an anon-key and an authenticated-key select on `fetches` each return zero rows; the service-role client reads. |
| BP-007 NFR budget: "No cost figure is ever rendered to a customer" | WO-021 | Same file — asserts there is no request-scoped read policy, so the columns are unreachable by a customer's own client by construction rather than by discipline. |
| BP-007 error behaviour: "**Caps degrade, never throw.** At the cap, `recordFetch` returns `{ skipped: 'cap' }` and the context is marked degraded" | WO-022 | `tests/costs/context.test.ts` — at the cap, the return is the third arm, nothing is thrown, `degraded()` is true, and measurements already paid for inside the same body are kept. Throwing fails it. |
| BP-007 decision 1: "`capHit()` is re-checked between calls in any multi-call step" | WO-022 | Same file — a body making three calls where the second crosses the cap gets `{ skipped: 'cap' }` on the third and a true `capHit()` between them. |
| BP-007 error behaviour: "Money already spent is always ledgered — a call that succeeds at the vendor and fails on parse still writes its row." | WO-022 | Same file — a `run()` that resolves then throws in the caller still leaves a `fetches` row with its `cost_cents`. |
| BP-007 error behaviour: "The cache is keyed `source + key + policyVersion`. Bumping the policy version is how a changed derivation invalidates its cache; nothing is deleted." | WO-022 | Same file — a bumped version misses; the old row still exists. |
| BP-007 error behaviour: "A cache read is the newest row on that key inserted within `freshnessDays`, excluding a row whose payload is the vendor's own zero-result shape … An empty payload is therefore always a miss and is retried on the next scan — it is still ledgered, at cost, every time it is bought, and is never served back as a hit" | WO-022 | Same file — an empty payload is billed, stored, and does not serve the next scan's read, which buys and ledgers a second row on the same key; a non-empty row inserted later on the same key is the one served. |
| BP-007 NFR budget: "A free scan's context must not exceed 12¢, a deep pass 150¢, a weekly 40¢, a draft 45¢ — enforced here, not assumed anywhere else." | WO-022 | Same file — one case per `CapName`, each asserted against `CAPS` from `constants.ts` rather than a literal. |
| BP-007 NFR budget: "a per-scan roll-up (`spentCents`, `degraded`) is written to `scans.cost_cents` and `scans.status` at close" | WO-022 | Same file — asserts both columns after a closed context. |
| BP-007 public interface: "The **reservation**: the most this call can cost, known before it runs. The cap is checked against this, so no call is made against money the context has not got." | WO-022 | Same file — a call whose `costCents` would cross the cap returns `{ skipped: 'cap' }` and `run()` is never invoked, even when a `settleCents` closure would have settled it under the cap. Checking the cap against a settled figure fails it, because the settlement does not exist yet. |
| BP-007 public interface: "Omitting it is the ordinary case and behaves exactly as before this argument existed." | WO-022 | Same file — with no `settleCents`, the returned `costCents`, the row's `cost_cents` and the row's `reserved_cents` are all the reservation, and `spentCents()` is unchanged from the pre-settlement behaviour asserted by the rows above. |
| BP-007 error behaviour: "**A settlement never raises a charge.** `settled > reserved` is a defect, not a case: it is clamped to the reservation and logged, because the excess is money no cap check authorised. A settled figure is what the row carries and what `spentCents()` sums." | WO-022 | Same file — a closure returning more than the reservation leaves `cost_cents` at the reservation and emits the log line; a closure returning less leaves `cost_cents` at the settled figure and `spentCents()` sums the settled figures, not the reserved ones. Trusting the closure fails the first case. |
| BP-007 error behaviour: "Between the reservation and the settlement a scan is charged the higher figure, so a cap can never be exceeded by a call in flight; after it, the context holds the figure the vendor says it will bill." | WO-022 | Same file — inside a body, `spentCents()` observed during `run()` is the reservation and after it is the settlement; a second call that fits only in the headroom the settlement released is made rather than skipped. |
| BP-007 NFR budget: "a row whose charge was settled records **both** figures — reserved and settled — so the difference can be reconciled against the vendor's own account statement" | WO-022 | Same file — a settled call's `fetches` row carries `reserved_cents` and `cost_cents` with the two different values; dropping either column fails it. |
| BP-007 data-model delta: "`reserved_cents` is what the cap was checked against; they differ only on the one endpoint ADR-094 flags" | WO-022 | `tests/costs/context.test.ts` — the applied schema carries `reserved_cents` on `fetches` (declared by WO-021's create-table, this order's `depends-on`), and an unsettled call writes the same value into both columns. |

## Out of scope
- The cache read predicate — "the newest row on that key inserted within
  `freshnessDays`, excluding a row whose payload is the vendor's own
  zero-result shape" (BP-007 decision 3). It is a read, not a write: this order
  adds no flag, no column and no constraint for it.
- The retention policy BP-007 notes `fetches` will need; the purge of a deleted
  account's payloads is BP-063's.
  columns owned by BP-012's `*_scans*.sql`.
- The free-path short-circuit BP-007 names — "A free scan of a domain measured
  less than 7 days ago serves the stored report and opens no context at all" — is
  BP-012/BP-023's admission control, which decides not to open a context. This
  work order opens the context it is given.
- Every caller: BP-008 (WO-023 to WO-025) and BP-009 (WO-026, WO-027).
- The vendor rule that computes a settlement. `settleCents` is a closure the call site
  supplies; BP-008's flagged `serpOrganic` is the one that supplies it today (ADR-094
  decision 3a, BP-007 decision 4: "the **rule** that computes a settlement is vendor
  knowledge and stays at the call site"). This seam learns nothing about AI Overviews and
  carries no endpoint-specific branch.
- The `fetches` table itself — its create-table statement, both indexes, `reserved_cents`
  writes and reads rows; it declares no column.
- Rendering any cost figure; none is ever rendered.

## Rollback
Revert both migrations and delete `src/lib/costs/**` and `tests/costs/**`. No
code outside BP-007's globs imports either half until WO-281 lands.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-007 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-021, WO-022.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-021, WO-022 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.
- 2026-09-04 started — implementer — dispatched in parallel with WO-018 (`src/lib/egress/`, `tests/egress/`, `eslint.config.mjs`) and WO-279 (`src/lib/presentation/generated/`, `tests/presentation/generated/`, `tests/presentation/copy/`) under constitution rule 4.1 (disjoint directories: `src/lib/costs/`, `tests/costs/`, `supabase/migrations/`).
- 2026-09-04 finished — implementer — `supabase/migrations/20260903080000_fetches.sql`, `src/lib/costs/{index,cache,ledger}.ts` and `tests/costs/{fetches-schema,context}.test.ts` written and passing (32/32 new tests, every criterion mutation-tested and confirmed to discriminate); ready for the validator, with one rule-4.2/rule-2 finding attached to this WO's return rather than fixed silently: `npm test`'s full run is flaky because `vitest.config.ts`'s `db` project (WO-275) was never extended past its original three files to cover the live-scratch-database test files added since — `tests/scan/free/schema.test.ts`, `tests/account/columns.test.ts`, and now this WO's own two `tests/costs/*.test.ts` — so all five race under the `node` project's default concurrency; confirmed pre-existing (reproduces with this WO's two new files removed, 2 of 3 runs) and confirmed fixed by isolation (this WO's two files pass 100% across many runs under `--no-file-parallelism`, alone and together). `vitest.config.ts`/`package.json` are outside this WO's file plan — not edited; the fix is two lines, shaped exactly like WO-275's own, and is this WO's one deviation to flag once (rule 4.2), not to carry silently into `done`.
- 2026-09-04 failed — validator — TST-029, verdict findings: 16/16 test-plan rows independently reproduced and discriminate by mutation (cap-reservation boundary, `capHit()` boundary, settlement clamp, `spentCents()` accumulation all confirmed); one coverage gap found — no test catches the cap check running before the cache read, so cache-first ordering is unenforced by the suite — next: implementer adds a discriminating test for that ordering. Flake claim (pre-existing, not WO-276's) independently reproduced and confirmed correctly out of scope; the Log's narrower "100% pass, alone and together" figure could not be independently reproduced in this session (heavy concurrent sibling-worktree load on the shared scratch substrate throughout — see report) and is left unverified, not refuted. `rests-on` row 1 dispositioned `confirmed` on this WO's own front-matter; row 2's prior `refuted` re-confirmed accurate; ADR-094's open row correctly left for BP-008's future work order. No other findings.

## Validation report (appended by validator)

## TST-029 — WO-276 validation report

### Scope
WO-276 (`wo/WO-276-cost-ledger`, commit `88e951d`, worktree
`/root/projects/reachkitv3-wt/WO-276`, cut from `doctrine-0.11.0` at
`9786ef7`, not merged). `git diff 9786ef7 88e951d --stat`: exactly 6 files,
all NEW, 1,513 insertions, 0 deletions — `supabase/migrations/
20260903080000_fetches.sql`, `src/lib/costs/{index,cache,ledger}.ts`,
`tests/costs/{fetches-schema,context}.test.ts` — matching the file plan
exactly. `git diff --name-only` confirms no file outside those three
directories changed; `grep -rln "lib/costs" src/` outside `src/lib/costs/`
itself returns nothing, confirming the `## Rollback` claim that no other
code imports either half yet. Working tree was clean at commit and left
clean at every point in this validation (mutations applied via `git diff`
against tracked files, reverted with `git checkout --`, confirmed empty
`git status` after each; the one throwaway `.env.local` used for the build
check is git-ignored, confirmed absent both before and after).

### A note on this environment before the counts

This sandbox's native-Postgres substrate (`127.0.0.1:5432`,
`reachkit_scratch`, one `PostgREST` instance) is shared across every
worktree, not per-worktree. Across this validation session `ps aux`
repeatedly showed `vitest`/`psql` processes from `WO-283`, `WO-277` and
`WO-279`'s own worktrees actively resetting and reapplying the same
physical `public` schema concurrently with my own runs — confirmed
directly, not inferred. This is a second, cross-worktree source of the
same class of failure the implementer's Log diagnoses intra-repo (§ below),
and it made several of the counts this task asked for unobtainable at full
confidence in the time available; each count below states plainly whether
it was taken in a confirmed-quiet window or not, per the instruction not to
repeat TST-025's transient-failure chase.

### Suite / build

- `tsc --noEmit`: clean.
- `eslint .`: 8118 problems (8093 errors, 25 warnings), all in
  vendored/generated files (`.claude/factory/console/src/vendor.mjs`, two
  migration scripts under `.claude/factory/plugins/`, one design-preview
  file) — matches the implementer's reported "8118 byte-identical" exactly.
  `eslint src tests`: clean, zero output — confirms `src`/`tests` clean.
- `next build`: fails as first attempted — `env.ts` throws on missing
  bindings, no `.env.local` in this sandbox. Wrote the same throwaway
  17-binding `.env.local` this corpus's own precedent uses (TST-024/
  TST-025, WO-062, WO-003), matching `ENV_FIXTURE` in `tests/costs/
  context.test.ts` for the Supabase-shaped values: `npx next build`
  compiles, typechecks, collects page data, emits `ƒ Proxy (Middleware)`
  and the app's two routes, no error. Deleted immediately after; `git
  status` clean before and after confirms it was never tracked.
- Migration, from scratch and composed: dropped and recreated `public`,
  applied all seven migrations in filename order
  (`00000000000001_baseline.sql` through `00000000000006_
  scans_freepath_claim.sql`, then `20260903080000_fetches.sql`) — every
  statement succeeds, `information_schema.tables` lists exactly the nine
  `BUILD.md` §10 tables including `fetches`, once. Filename sorts after
  the six existing zero-padded migrations (`2026...` > `000000000000
  06...`) and collides with none on disk.
- `db` project test count: 92, matching the implementer's reported figure
  (confirmed by the file's own reported total across three independent
  runs, pass/fail counts varying with substrate contention but the total
  of 92 constant every time).
- `npm test` / isolated-file counts under contention: see the flake
  section below — this is the count materially affected by the shared
  substrate and is reported there with the caveat attached, not here.

### Criterion-by-criterion (WO's own `## Test plan`, 16 rows)

| # | Criterion (source) | Test | Verdict | Mutation probe |
|---|---|---|---|---|
| 1 | BP-007 data-model delta — nine named columns, both indexes | `fetches-schema.test.ts` | PASS | Not separately mutated — a direct `information_schema`/`pg_indexes` read against the applied migration; the migration's own DDL is the thing under test and either has the columns/indexes or does not |
| 2 | BP-007 data-model delta — insert-only, no unique constraint | Same file, two rows | PASS | The file's own embedded "watch it fail first" guard (adds the constraint decision 3 dropped, asserts the second insert now raises, drops the constraint again) reproduced directly: `raises(...)` is `false` before the constraint and `true` after — confirmed live, not read |
| 3 | BP-007 — one row, no second table | Same file | PASS | Not separately mutated — an enumeration over `information_schema` against the applied schema; adding a second table would fail it by construction |
| 4 | BP-002 — RLS default-deny | Same file, real anon/authenticated/service-role JWTs through PostgREST | PASS | Real signed HS256 JWTs (`role: anon`/`authenticated`/`service_role`) through a real `PostgREST` instance via a loopback-`curl` fetch shim — not a config-catalogue assertion. Anon and authenticated both return `[], error: null`; service-role reads. This is the real security boundary being exercised, not asserted on `pg_policies` alone (a separate row, below, does check the policy catalogue too, correctly labelled as the "unreachable by construction" claim rather than the RLS behaviour itself) |
| 5 | BP-007 NFR — no cost figure rendered, no read policy | Same file | PASS | Not separately mutated — direct `pg_policies` count |
| 6 | BP-007 — caps degrade, never throw | `context.test.ts` | PASS | See M1–M4 below |
| 7 | BP-007 decision 1 — `capHit()` re-checked between calls | Same file | PASS | M2 (below) breaks exactly this test, 3/3 clean attempts |
| 8 | BP-007 — money spent is always ledgered on parse failure | Same file | PASS | Reproduced directly (a `run()` that resolves then throws leaves a row); not separately mutated beyond the four money-critical mutations below, which don't touch this path |
| 9 | BP-007 — policy-version bump invalidates cache, nothing deleted | Same file | PASS | Reproduced directly |
| 10 | BP-007 — empty payload always a miss, ledgered every time, never served | Same file | PASS | Reproduced directly (four-scan sequence: two empty buys both ledgered, a real result buys and is then served without re-invoking `run()`) |
| 11 | BP-007 NFR — cap exact figures per `CapName`, `CAPS` not literals | Same file | PASS | M1 (below) breaks exactly the four parameterised "lands exactly on the cap" cases, every clean attempt |
| 12 | BP-007 NFR — roll-up written to `scans.cost_cents`/`status` at close | Same file | PASS | Reproduced directly for both the clean-close and degraded-close cases |
| 13 | BP-007 public interface — cap checked against reservation, not settlement | Same file | PASS | Reproduced directly (`settleCents` returning a figure that would fit under the cap does not rescue a reservation that would not) |
| 14 | BP-007 — omitting `settleCents` behaves as before | Same file | PASS | Reproduced directly |
| 15 | BP-007 — settlement never raises a charge, clamped and logged | Same file | PASS | M3 (below) breaks exactly this test, 3/3 clean attempts |
| 16 | BP-007 — between reservation and settlement, higher figure charged in-flight; headroom released after | Same file | PASS | Reproduced directly (`spentCents()` observed mid-`run()` is the reservation; after, the settlement; a second call that only fits in the released headroom is made, not skipped) |
| — | BP-007 — a settled row carries both figures / an unsettled call writes the same value into both | Same file | PASS | Reproduced directly for both cases |

All 16 rows PASS, independently reproduced against a live scratch
database and PostgREST, not merely read.

### Money-critical mutation testing (reserve / settle / clamp, ADR-094 decision 3a)

Run against `tests/costs/context.test.ts` in isolation
(`--project node --project db` excluded; `--no-file-parallelism`), each
mutation applied via a tracked-file edit, run 3+ times, then reverted
(`git checkout -- src/lib/costs/index.ts`, confirmed clean after every
revert). Substrate contention (see note above) added noise on top of the
deterministic signal in most attempts; the reported result is the
consistent core that appeared in **every** attempt where the file's own
`beforeAll` schema reset itself succeeded — additional, varying failures
on top of that core in noisier attempts are attributed to contention, not
to the mutation, and are not counted as the mutation's own signal.

- **M1 — cap-check boundary, `>` → `>=`** (`if (spentCents() + call.costCents
  >= capValue)`): breaks exactly the `capHit()`-between-calls test and all
  four `CAPS.*_C` exact-landing cases, in every attempt where setup
  succeeded (3 attempts observed, cores identical). Reverted; clean baseline
  re-confirmed 3/3.
- **M2 — `capHit()` boundary, `>=` → `>`**: breaks exactly one test, the
  `capHit()`-between-calls case, 3/3 clean attempts, zero collateral —
  the cleanest signal of the five mutations, run when the substrate
  happened to be least contended. Reverted.
- **M3 — clamp disabled** (settlement closure trusted unconditionally,
  clamp branch removed): breaks exactly the settlement-clamp test
  ("a closure returning more than the reservation…"), 3/3 clean attempts,
  zero collateral. Reverted.
- **M4 — `spentCents()` accumulation, `+=` → `=`** (settlement overwrites
  the running ledger total instead of accumulating it): breaks the
  `capHit()`-between-calls test and the settlement-clamp-sums test in
  every attempt (both do 2+ `recordFetch` calls in one body, where
  accumulation matters); attempts run under heavier contention showed
  additional failures on top of that deterministic core. Reverted.
- **An unsettled call writes the same value into both columns** and
  **`reserved_cents`/`cost_cents` differ only where settled** are both
  covered directly by criterion rows 13–16 and the last table row above;
  not separately mutated beyond M1–M4, which exercise the same code path.

All four mutations produce the exact, predicted, test-plan-cited failure
every time signal was obtainable — the implementer's own report that "run
them yourself, that is the point of you" was reproduced faithfully citing
its own four scenarios (disable the clamp, flip the cap boundary, make
settlement overwrite without clamping — covered by M3 — plus the
accumulation claim covered separately by M4).

### Coverage gap found — cache-first ordering is not discriminated

**M5 — cap check moved before the cache read** (the two blocks in
`recordFetch` swapped: cap checked first, then `readCache()`, so a hit
under an exhausted cap now returns `{ skipped: 'cap' }` instead of being
served free). Run 6 times total across two confirmed-quiet windows
(verified via `ps aux` showing zero other `vitest`/`psql` processes
immediately before each run): **17/17 tests pass every time**, mutation
active. Reverted; unmutated baseline re-confirmed 3/3 clean.

This is a real gap, not noise — the signal was obtained in a genuinely
quiet window and was perfectly consistent across all six attempts (the
same six-run consistency M2's clean signal showed). BP-007's own public
interface states "Cache-first, ledger-always... `fresh` false means the
payload came from cache and cost nothing," and `index.ts`'s own comment
above the cache read states the property in exactly the terms this order
was dispatched to check: "a hit is free and bypasses the cap entirely —
it never reaches the vendor, so there is nothing for the cap to protect
against." No row in the WO's own 16-row Test Plan states this as its own
criterion (the closest, row 11/criterion 6, tests the cap boundary on a
paid call, not a cache hit against an already-exhausted cap), and no test
in `context.test.ts` constructs a scan that is at or over its cap and
then takes a cache hit. The code is correct — I did not find this order
by deleting real cache-first logic, only by reordering it, and the actual
(unmutated) implementation genuinely reads cache before checking the cap.
But nothing in the suite would catch a future regression that broke this
specific property, which is exactly the "cap check that passes vacuously"
pattern this dispatch asked me to hunt for, applied to the omission
rather than a written-but-vacuous test. **Finding, not a code defect**:
the implementer (or a follow-up) should add one test — a scan whose
reservation already exhausted its cap, followed by a `recordFetch` call
on a key with a cached, non-empty row, asserting the hit is served
(`fresh: false, costCents: 0`) rather than skipped.

### Flake claim — verified, with an added environmental factor

**"With WO-276's new files removed, 2 of 3 `npm test` runs still failed"** —
independently reproduced. Moved `tests/costs/{fetches-schema,context}
.test.ts` out of the tree, ran `npm test` 6 times in the quietest window
available early in this session: 4 passed, 2 failed (33%) — same order of
magnitude as the implementer's reported 2/3 (67%), same failure
signature (`tests/db/baseline.test.ts`, `tests/scan/free/schema.test.ts`
and `tests/account/columns.test.ts` — never the removed cost files, which
were absent — failing with "relation already exists"/"relation does not
exist" from concurrent schema resets). Confirms the flake is pre-existing
and not attributable to WO-276's two new files' content; restored the
files afterward (`git status` clean, confirmed).

Static confirmation, independent of any run's flakiness: `vitest.config
.ts`'s `db` project `include` list names exactly four files (`tests/db/
baseline.test.ts`, `tests/db/rls.test.ts`, `tests/db/clients.test.ts`,
`tests/scan/free/admission-claim.test.ts`). `tests/scan/free/
schema.test.ts`, `tests/account/columns.test.ts` and this WO's own two
`tests/costs/*.test.ts` each independently reset and reapply the same
physical `public` schema (read directly, all four headers say so) and are
absent from that list, so all four schedule under the `node` project's
default concurrent scheduling alongside `db`. Isolation test: `tests/db/
clients.test.ts` run entirely alone passes 3/3 clean; the same file fails
intermittently only when run in combination with the other schema-
resetting files — demonstrating the cross-file race mechanism the
implementer's Log describes is real, by direct observation, not merely
by inspection.

**"This WO's two files pass 100% across many runs under
`--no-file-parallelism`, alone and together"** — **could not be
independently reproduced in this session** and is left **unverified, not
refuted**. Ran `tests/costs/fetches-schema.test.ts` + `context.test.ts`
together under `--project node --no-file-parallelism`, repeatedly: most
attempts failed with the same "relation already exists"/"does not exist"
signature the flake produces elsewhere, including attempts where `ps aux`
showed zero other vitest/psql processes at the moment of invocation but
(per the note above) not necessarily for the run's full ~2–6s duration —
this session's sibling worktrees (`WO-283`, `WO-277`, `WO-279`, all
observed live via `ps aux` during this validation) start and stop test
runs on a timescale shorter than that, so a "quiet at start" check does
not rule out interference mid-run. I cannot distinguish, in this shared
environment, whether the Log's narrower claim is false or whether it was
genuinely obtained in a quiet window this session could not reproduce.
Recorded for the separate work order being cut on the flake's strength:
**the fix under discussion (extending `vitest.config.ts`'s `db` project
list) may be necessary but is not obviously sufficient** — this
session's own evidence is that even a two-file, `--no-file-parallelism`
run of files *already isolated from the other four* still failed most of
the time, consistent with cross-worktree substrate contention as an
additional, orthogonal cause this repository's own `vitest.config.ts`
cannot fix by itself. Flagged for the record, not treated as a WO-276
code defect — the Log's claim is about test infrastructure behaviour, not
about `src/lib/costs/` itself, and every one of that claim's failures
observed here was a setup-time schema-reset error, never an assertion
inside the cost seam's own logic.

**Live-DB suites under `--no-file-parallelism`, counts** (`--project
node --project db`, all eight live-scratch-schema files together:
`tests/db/{baseline,rls,clients}.test.ts`, `tests/scan/free/
{admission-claim,schema}.test.ts`, `tests/account/columns.test.ts`,
`tests/costs/{fetches-schema,context}.test.ts`, 148 tests total) — three
runs, taken under confirmed heavy contention (2–3 other worktrees'
`vitest`/`psql` processes present throughout): 4 failed/4 passed (10
failed/97 passed/41 skipped), 4 failed/4 passed (27 failed/105
passed/16 skipped), 3 failed/5 passed (17 failed/115 passed/16
skipped). These figures are the contaminated-session counts, reported
because the task asked for them explicitly — they should not be read as
`--no-file-parallelism`'s own ceiling on a quiet substrate; the isolated,
quiet-window evidence above (`clients.test.ts` alone 3/3, M2 and M5 each
6/6 clean) is the more reliable signal this report leans on.

### `isEmptyPayload` — vendor-agnostic, confirmed

`src/lib/costs/cache.ts`'s `isEmptyPayload` recognises exactly
`null`/`undefined`/an empty array — no DataForSEO- or vendor-specific
marker. `grep -n "dataforseo\|serpOrganic\|ai_overview" src/lib/costs/*.ts`
returns one hit, a doc-comment citation of BP-007's own interface text
(the one call site that will use `settleCents`), not logic. BP-008's
ownership of vendor-specific normalisation is respected.

### `untypedFetches` — narrow, precedented, no real mismatch hidden

Judged against the three questions this dispatch asked. **Genuinely
narrow**: `MinimalQueryBuilder<T>`/`MinimalClient` expose exactly
`select`, `eq`, `gt`, `order`, `limit`, `insert` — the operations
`cache.ts` and `ledger.ts` call, nothing wider. **Matches
`src/lib/scan/admission.ts`'s own escape hatch**: read both files side by
side — `admission.ts`'s `untyped()` and `ledger.ts`'s `untypedFetches()`
declare structurally identical `QueryResult<T>`/`MinimalQueryBuilder<T>`/
`MinimalClient` shapes and the same one-line cast-boundary function
pattern (`admission.ts`'s carries `gte`/`single` for its own two call
shapes where `ledger.ts`'s carries `gt`/no `single`, both narrowed to
exactly what each file calls). **No real mismatch hidden**: confirmed
`src/lib/db/types.generated.ts`'s own header states it was generated
against `00000000000001_baseline.sql` + `00000000000002_rls.sql` only,
predating this WO's migration — `fetches` is genuinely absent from the
generated `Database` type, not a gap papered over. `FetchesRow`'s nine
fields match the migration's create-table statement column-for-column.
Judgement: sound, precedented, not a hidden defect.

### `rests-on` disposition (rule 2.3b)

**This WO's own rows** — row 1 (the one-table-one-writer claim, `BUILD.md`
§10 vs. WO-267's deliberate exclusion) dispositioned **`confirmed`**
directly on this WO's own front-matter, reason recorded there: applying
all seven migrations from a freshly dropped schema produces exactly one
`fetches` table, created only by this WO's own migration. Row 2 (the
unique-constraint collision with the re-buy promise, already `refuted` by
the planner in the same pass that created this WO) **re-confirmed
accurate**, not re-dispositioned: the shipped migration carries no unique
constraint on `(source, cache_key, policy_version)` (criterion row 2,
verified live) — the premise the row's own text describes ("if it ships
as planned…") did not occur, and the row's existing `refuted` disposition
still holds against the code as built.

**Upstream — BP-007**, the blueprint this order implements: `rests-on:
[]`, nothing to disposition.

**Upstream — ADR-094**, decided-for BP-007: two rows. The `confirmed` row
is untouched (already settled, unrelated to this build). The **`open`**
row — whether DataForSEO's documented refund of the `load_async_ai_
overview` surcharge actually lands — is **left open, deliberately**: per
rule 2.3b, an upstream row is dispositioned "by the validator of the
first work order that implements it and observed the claim." WO-276
builds the generic reserve/settle/clamp *mechanism* (`settleCents?`) but
calls it with no vendor rule — the one call site that will exercise the
DataForSEO refund claim is BP-008's `serpOrganic`, explicitly out of this
WO's scope (`## Out of scope`: "Every caller: BP-008... BP-009"). This
build cannot observe a vendor's real account-statement behaviour; that
row is BP-008's first work order's to disposition, or the architect's at
wave close if this wave does not reach it.

### Parameters (rule 1.1)

The dispatch named "the seven rule-1.1 parameters the implementer
recorded." Searching the diff for explicit `rule 1.1` citations finds
**five**, not seven — four in `src/lib/costs/` (the implementer's own)
and one in the migration header (the planner's, cited not re-derived):

1. `cache.ts` — `isEmptyPayload`'s scope stops at `null`/`undefined`/`[]`
   rather than learning any vendor's own no-match marker. Genuinely a
   parameter (a membership-set choice, constitution §1's table), correctly
   scoped to avoid BP-008 knowledge, reversal cost stated (change one
   function). Recorded and sound.
2. `cache.ts` — `CACHE_READ_SCAN_LIMIT = 50`. Genuinely a parameter (a
   denominator/limit), derivation stated (how many consecutive empty
   re-buys before a pathological miss), reversal cost stated (change one
   constant). Recorded and sound.
3. `index.ts` — the single in-flight-reservation-slot / sequential-calls
   assumption. Genuinely a parameter in the constitution's sense
   (an internal concurrency-model choice, not a customer promise);
   correctly scoped to this WO's own callers (none fan out concurrently
   today) with a stated reversal path (a summed set, local to one file).
   Recorded and sound.
4. `index.ts` — `console.warn` as the one logging channel for the clamp
   invariant, matching `admission.ts`'s own precedent (`console.log`).
   A defensible internal-tooling choice (constitution §1: "internal
   names… module boundaries" is System's), reversal cost stated.
   Recorded and sound.
5. `supabase/migrations/20260903080000_fetches.sql`'s header — `reserved_
   cents` ships in the initial create-table statement rather than as a
   later additive migration; the header correctly cites this as "planner
   ruling… recorded on this WO's own front-matter, not re-derived here"
   rather than re-arguing it, which is the right move under rule 2.4.

I found no sixth or seventh explicit `rule 1.1` citation anywhere in the
6-file diff (`grep -rn "rule 1.1"` across all six files, exhaustive).
This is a discrepancy between the dispatch's expectation and what is
actually on disk, not a defect in the five that exist — every one found
is genuinely a parameter (not a customer-visible promise), genuinely
recorded with derivation and reversal cost, and correctly scoped. Worth
naming for the record; not a finding against this WO.

### Structure / duplication

`src/lib/costs/**` is `structure.md`'s own row for BP-007; `fetches` is
already a registered topic token in `src/lib/db/topics.ts` (`{ token:
"fetches", owner: "BP-007" }`). `grep -rln "recordFetch\|withCostContext\|
CostContext\|spend_ledger" src/` outside `src/lib/costs/` returns
nothing — no near-duplicate capability. Migration filename sorts
correctly and collides with none of the six existing migrations.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict

findings

One coverage gap (cache-first ordering vs. the cap is not discriminated
by any test — see "Coverage gap found" above) — next: implementer adds
the one missing test (a cache hit served while the context is already at
or over its cap). Everything else in this WO's own 16-row Test Plan
passes and discriminates by direct mutation; the money-critical reserve/
settle/clamp mechanics (ADR-094 decision 3a) are solidly covered. The
flake the implementer's Log reports is independently confirmed
pre-existing and correctly left unfixed here (outside this WO's file
plan); its narrower "100% … alone and together" sub-claim is unverified,
not refuted, for reasons stated above and should not by itself block a
re-submission — only the coverage gap should.
