---
id: WO-276
type: work-order
title: "The fetch ledger and the cost context — one insert-only row, cache-first, cap-degrades"
status: approved
wave: W3
ui: no
implements: [BP-007, REQ-003]
depends-on: [WO-006, WO-267]
blocked-by: []
rests-on:
  - claim: "This migration creates `fetches`, which `BUILD.md` §10 lists among its nine baseline tables. WO-267 (`done`) deliberately leaves it out — its baseline migration's own header states the exclusion — because `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic and BP-007's `## Data model delta` fully specifies the table, so one table has one writer. The derivation and its reversal cost are recorded on WO-267's first `rests-on` row, which carries `disposition: confirmed`."
    disposition: open
  - claim: "The `fetches` table this context writes to carries no unique constraint on `(source, cache_key, policy_version)` — BP-007 decision 3 (2026-09-03) dropped it, and the cache read is 'the newest row on that key inserted within `freshnessDays`, excluding a row whose payload is the vendor's own zero-result shape'. WO-021, which creates the table and is `approved` and unbuilt, still plans that constraint and predates the decision. If it ships as planned, the empty-payload re-buy this work order tests cannot insert its second row. Reported to the planner queue on 2026-09-03, not repaired here: `*_fetches*.sql`'s create-table statement is WO-021's file. **Discharged 2026-09-03**: the planner re-planned WO-021 in the same pass — its file plan carries no unique constraint, and its test plan now asserts positively that two rows on one `(source, cache_key, policy_version)` triple both insert. The premise no longer holds."
    disposition: refuted
supersedes: [WO-021, WO-022]
---

# WO-276 — The fetch ledger and the cost context — one insert-only row, cache-first, cap-degrades

- Branch: wo/WO-276-cost-context
- Estimate: ≤1 day

## Goal
Ship BP-007's spend seam whole: the one insert-only `fetches` row that is at once ledger, cache and raw store, and the per-scan context that reads it cache-first, ledgers every call that reached a vendor, and returns a value at the cap instead of throwing.

> **Read `decisions/ADR-094.md` before writing a line of this WO.** The
> reserve/settle split and the second-spender mechanics are that decision's and
> BP-028's, not this order's to re-derive.

## Consolidation (rule 2.6)
WO-021 created the `fetches` table; WO-022 created the only reader and the only
writer of it. Neither could state its stop condition without the other — WO-022's
goal is "reads cache first, ledgers what was spent", which is a claim about
WO-021's row, and WO-021's own `## Provenance` already folded `reserved_cents`
across the seam under rule 2.6 on 2026-09-03 with the note that "both files sit
under the same `*_fetches*.sql` glob, so no module boundary is crossed". They are
one capability from schema to test: what a call costs, where that is written
down, and what happens at the cap.

## File plan

| File | New/Modify | From | Change |
|---|---|---|---|
| `supabase/migrations/20260903080000_fetches.sql` | NEW | WO-021 | `fetches` exactly to BP-007's `## Data model delta` — its nine columns including `reserved_cents`, **no unique constraint** (BP-007 decision 3), the `(source, cache_key, policy_version, created_at desc)` index for the cache read and the `scan_id` index for the ledger read. RLS enabled with **no request-scoped read policy** — `fetches` carries cost and raw vendor payloads and is reachable only through `dbAdmin()`. |
| `tests/costs/fetches-schema.test.ts` | NEW | WO-021 | The tests below. `structure.md` rule 4 puts BP-007's tests under `tests/costs/**`. |
| `src/lib/costs/index.ts` | NEW | WO-022 | `withCostContext()` and the `CostContext` implementation, exactly BP-007's interface. Opens the context, runs the body, and at close writes the roll-up (`spentCents`, `degraded`) to `scans.cost_cents` and `scans.status`. `recordFetch` **reserves** `costCents` before the call — the cap is checked against the reservation — and, where the call site supplied `settleCents`, **settles** the ledgered figure from the payload afterwards, clamped to the reservation and logged when it would exceed it. `spentCents()` sums settled figures. |
| `src/lib/costs/cache.ts` | NEW | WO-022 | The cache read keyed `source + cacheKey + policyVersion`: the newest row on that key inserted within `freshnessDays`, **excluding a row whose payload is the vendor's own zero-result shape** (BP-007 decision 3 — the exclusion is at the read, not the write). Nothing is ever deleted — a bumped `policyVersion` is how a changed derivation invalidates. |
| `src/lib/costs/ledger.ts` | NEW | WO-022 | `recordFetch`'s write half: a row is written for every call that reached the vendor, including one that succeeded at the vendor and failed on parse. Every row carries both figures — `reserved_cents` (what the cap was checked against) and `cost_cents` (the settled figure where one settled, the reservation otherwise). |
| `tests/costs/context.test.ts` | NEW | WO-022 | The tests below. |

## Interfaces

> The blocks below are the superseded orders' `## Interfaces` sections,
> carried verbatim. A reference to one of the orders named in `supersedes:`
> is a reference to a block of **this** work order, not to a separate one —
> the `From` column of the file plan and the block headings say which.

### From WO-021

Consumes: the RLS conventions WO-267 applied.
Exposes: the schema `withCostContext()` (WO-022) writes and reads. No code
interface.

### From WO-022

Exposes (verbatim from BP-007 `## Public interface`):
```ts
export type CapName = 'FREE' | 'DEEP' | 'WEEKLY' | 'DRAFT'

export function withCostContext<T>(
  ctx: { scanId: string; cap: CapName; policyVersion: number },
  body: (cost: CostContext) => Promise<T>
): Promise<T>

export interface CostContext {
  /** Cache-first, ledger-always. `fresh` false means the payload came from cache
   *  and cost nothing. An empty payload is always a miss; there is no negative
   *  cache. */
  recordFetch<P>(call: {
    source: string           // vendor endpoint or model id
    cacheKey: string
    freshnessDays: number
    /** The **reservation**: the most this call can cost, known before it runs.
     *  The cap is checked against this, so no call is made against money the
     *  context has not got. */
    costCents: number
    /** Optional **settlement** (ADR-094 decision 3a). Where a vendor's own
     *  documented rule makes the final charge a function of the response, the
     *  call site supplies it and the ledger records the settled figure instead
     *  of the reservation. `settled <= reserved` is enforced here, not trusted:
     *  a closure returning more is clamped to the reservation and logged, since
     *  the excess would be money the cap never authorised. One call site uses
     *  it today — BP-008's flagged `serpOrganic`. Omitting it is the ordinary
     *  case and behaves exactly as before this argument existed. */
    settleCents?: (payload: P) => number
    run: () => Promise<P>
  }): Promise<{ payload: P; fresh: boolean; costCents: number } | { skipped: 'cap' }>
  // The returned `costCents` is the settled figure where one was supplied, the
  // reservation otherwise — one number, and it is the one the row carries.

  capHit(): boolean          // re-checked between calls in any multi-call step
  spentCents(): number
  degraded(): boolean
}
```
Consumes: `db()`/`dbAdmin()` (BP-002), `CAPS` and `CACHE_WINDOWS_D` (BP-005).

## Steps

**WO-021's steps** (renumbered; order within the block unchanged):

1. Write the migration exactly to BP-007's data-model delta, `reserved_cents`
   included.
2. Add the two indexes, and no unique constraint (BP-007 decision 3).
3. Enable RLS with no request-scoped policy.
4. Write the tests; watch the "no second table" test fail first by adding a
   `spend_ledger` fixture table, and the re-buy test fail first by adding the
   unique constraint decision 3 dropped.

**WO-022's steps** (renumbered; order within the block unchanged):

5. Write `cache.ts` and prove the no-negative-cache rule by test.
6. Write `ledger.ts` and prove the spend-then-fail case; each row carries both figures.
7. Write `index.ts` with the three-armed `recordFetch` return, the cap checked against the
   reservation before `run()`, and the settlement applied after it where a closure was
   supplied — clamped at the reservation, never above it.
8. Write the tests; watch the cap case fail first by throwing a `CapExceeded`
   instead of returning `{ skipped: 'cap' }`, and the settlement case fail first by
   ledgering the reservation regardless of the closure's return.

## Test plan

> Every criterion below is carried verbatim from the work order named in
> the `From` column, re-checked against the requirement as it stands on
> disk on 2026-09-04. Line breaks are normalised to fit the table and no
> word is changed. No criterion any superseded order carried is dropped.

| Acceptance criterion (verbatim from REQ) | From | Test |
|---|---|---|
| BP-007 data-model delta: "`fetches` — `id, scan_id, source, cache_key, policy_version, cost_cents, reserved_cents, payload jsonb, created_at`" … "Indexed `(source, cache_key, policy_version, created_at desc)` for the cache read and on `scan_id` for the ledger read." | WO-021 | `tests/costs/fetches-schema.test.ts` — asserts the column set and both indexes by name against the applied schema. A missing `reserved_cents` fails it. |
| BP-007 data-model delta: "**Insert-only; no unique constraint on `(source, cache_key, policy_version)`**" | WO-021 | Same file — two rows inserted on one `(source, cache_key, policy_version)` triple both persist and are distinguishable by `created_at desc`. Adding the constraint fails it. This is the schema half of the re-buy BP-007's error behaviour promises; the read predicate that serves it is WO-022's. |
| BP-007 data-model delta: "A column, not a second table: the two figures belong to the same call." · "No second table: the ledger, the cache and the raw store are one row" | WO-021 | Same file — `cost_cents` and `reserved_cents` are both columns of `fetches`, and no other table in `public` carries `cache_key` or `reserved_cents`. `scans.cost_cents` is excluded by name: BP-007's NFR budget writes that per-scan roll-up at close, so it is not a second ledger. |
| BP-002 error behaviour: "RLS is default-deny: a table with no policy is unreadable by anyone holding an anon or authenticated key." | WO-021 | Same file — an anon-key and an authenticated-key select on `fetches` each return zero rows; the service-role client reads. |
| BP-007 NFR budget: "No cost figure is ever rendered to a customer" | WO-021 | Same file — asserts there is no request-scoped read policy, so the columns are unreachable by a customer's own client by construction rather than by discipline. |
| BP-007 error behaviour: "**Caps degrade, never throw.** At the cap, `recordFetch` returns `{ skipped: 'cap' }` and the context is marked degraded" | WO-022 | `tests/costs/context.test.ts` — at the cap, the return is the third arm, nothing is thrown, `degraded()` is true, and measurements already paid for inside the same body are kept. Throwing fails it. |
| BP-007 decision 1: "`capHit()` is re-checked between calls in any multi-call step" | WO-022 | Same file — a body making three calls where the second crosses the cap gets `{ skipped: 'cap' }` on the third and a true `capHit()` between them. |
| BP-007 error behaviour: "Money already spent is always ledgered — a call that succeeds at the vendor and fails on parse still writes its row." | WO-022 | Same file — a `run()` that resolves then throws in the caller still leaves a `fetches` row with its `cost_cents`. |
| BP-007 error behaviour: "The cache is keyed `source + key + policyVersion`. Bumping the policy version is how a changed derivation invalidates its cache; nothing is deleted." | WO-022 | Same file — a bumped version misses; the old row still exists. |
| BP-007 error behaviour: "A cache read is the newest row on that key inserted within `freshnessDays`, excluding a row whose payload is the vendor's own zero-result shape … An empty payload is therefore always a miss and is retried on the next scan — it is still ledgered, at cost, every time it is bought, and is never served back as a hit" | WO-022 | Same file — an empty payload is billed, stored, and does not serve the next scan's read, which buys and ledgers a second row on the same key; a non-empty row inserted later on the same key is the one served. |
| BP-007 NFR budget: "A free scan's context must not exceed 12¢, a deep pass 150¢, a weekly 40¢, a draft 45¢ — enforced here, not assumed anywhere else." | WO-022 | Same file — one case per `CapName`, each asserted against `CAPS` from `constants.ts` rather than a literal. |
| BP-007 NFR budget: "a per-scan roll-up (`spentCents`, `degraded`) is written to `scans.cost_cents` and `scans.status` at close" | WO-022 | Same file — asserts both columns after a closed context. |
| BP-007 public interface: "The **reservation**: the most this call can cost, known before it runs. The cap is checked against this, so no call is made against money the context has not got." | WO-022 | Same file — a call whose `costCents` would cross the cap returns `{ skipped: 'cap' }` and `run()` is never invoked, even when a `settleCents` closure would have settled it under the cap. Checking the cap against a settled figure fails it, because the settlement does not exist yet. |
| BP-007 public interface: "Omitting it is the ordinary case and behaves exactly as before this argument existed." | WO-022 | Same file — with no `settleCents`, the returned `costCents`, the row's `cost_cents` and the row's `reserved_cents` are all the reservation, and `spentCents()` is unchanged from the pre-settlement behaviour asserted by the rows above. |
| BP-007 error behaviour: "**A settlement never raises a charge.** `settled > reserved` is a defect, not a case: it is clamped to the reservation and logged, because the excess is money no cap check authorised. A settled figure is what the row carries and what `spentCents()` sums." | WO-022 | Same file — a closure returning more than the reservation leaves `cost_cents` at the reservation and emits the log line; a closure returning less leaves `cost_cents` at the settled figure and `spentCents()` sums the settled figures, not the reserved ones. Trusting the closure fails the first case. |
| BP-007 error behaviour: "Between the reservation and the settlement a scan is charged the higher figure, so a cap can never be exceeded by a call in flight; after it, the context holds the figure the vendor says it will bill." | WO-022 | Same file — inside a body, `spentCents()` observed during `run()` is the reservation and after it is the settlement; a second call that fits only in the headroom the settlement released is made rather than skipped. |
| BP-007 NFR budget: "a row whose charge was settled records **both** figures — reserved and settled — so the difference can be reconciled against the vendor's own account statement" | WO-022 | Same file — a settled call's `fetches` row carries `reserved_cents` and `cost_cents` with the two different values; dropping either column fails it. |
| BP-007 data-model delta: "`reserved_cents` is what the cap was checked against; they differ only on the one endpoint ADR-094 flags" | WO-022 | `tests/costs/context.test.ts` — the applied schema carries `reserved_cents` on `fetches` (declared by WO-021's create-table, this order's `depends-on`), and an unsettled call writes the same value into both columns. |

## Out of scope
- The cache read predicate — "the newest row on that key inserted within
  `freshnessDays`, excluding a row whose payload is the vendor's own
  zero-result shape" (BP-007 decision 3). It is a read, not a write: this order
  adds no flag, no column and no constraint for it.
- The retention policy BP-007 notes `fetches` will need; the purge of a deleted
  account's payloads is BP-063's.
  columns owned by BP-012's `*_scans*.sql`.
- The free-path short-circuit BP-007 names — "A free scan of a domain measured
  less than 7 days ago serves the stored report and opens no context at all" — is
  BP-012/BP-023's admission control, which decides not to open a context. This
  work order opens the context it is given.
- Every caller: BP-008 (WO-023 to WO-025) and BP-009 (WO-026, WO-027).
- The vendor rule that computes a settlement. `settleCents` is a closure the call site
  supplies; BP-008's flagged `serpOrganic` is the one that supplies it today (ADR-094
  decision 3a, BP-007 decision 4: "the **rule** that computes a settlement is vendor
  knowledge and stays at the call site"). This seam learns nothing about AI Overviews and
  carries no endpoint-specific branch.
- The `fetches` table itself — its create-table statement, both indexes, `reserved_cents`
  writes and reads rows; it declares no column.
- Rendering any cost figure; none is ever rendered.

## Rollback
Revert both migrations and delete `src/lib/costs/**` and `tests/costs/**`. No
code outside BP-007's globs imports either half until WO-281 lands.

## Self-certification
`status: approved` — set 2026-09-04 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-007 — all `status: approved`.
- **Criteria inherited, not paraphrased.** Every row of `## Test plan` quotes its requirement as it stands on disk on 2026-09-04, and every row the superseded orders carried is present.
- **Bounded.** Estimate ≤1 day; the file plan is the deduplicated union of WO-021, WO-022.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-04 created — planner — consolidation of WO-021, WO-022 under rule 2.6 (see `## Consolidation`); criteria and file plans carried verbatim, the replaced orders left on disk as `superseded`.

## Validation report (appended by validator)
