---
id: WO-081
type: work-order
title: "The correction state machine and CORRECTION_SCOPE"
status: approved
wave: W4
ui: no
risk: high
implements: [BP-028, REQ-094]
depends-on: [WO-001]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-081 — The correction state machine and CORRECTION_SCOPE

- Branch: wo/WO-081-correction-state
- Estimate: ≤1 day

## Goal
Implement `src/lib/market/coherence/state.ts` — the correction states, a transition function
total over states × events with one refusal that costs nothing, and the named scope constant
that says what a correction re-measures and what it carries forward.

**Risk: high — seam: a state machine that publishes.** `correction_state` is stored on the scan row and read back into the report's one notice, and this machine's refusals — `already_used`, `already_running` — are what a customer is told when a correction is declined; REQ-094 binds each outcome to a different written line. A totality or refusal test that survives deletion lets one submission be accepted twice, or a decline be reported under the wrong line. Mutation-tested (doctrine 0.13.2, rule 2b).

## The block is cleared: six members, and the table is written down
This WO carried `blocked-by: [BP-028]` because the five-member union as approved required one
(state, event) pair — `running + produced_no_report` — to yield `failed_once` on a first
attempt and `exhausted` on a retry, which a total function cannot do. **BP-028 decision 2a
(accepted 2026-08-31) split the running state**: `running` is the first attempt in flight,
`running_retry` is the second, entered by `failed_once + submitted`. The edge is discharged
and the plan below is cut against the six-member machine.

Decision 2a also records why the two alternatives this WO offered are not taken, and an
implementer must not reintroduce either:

- an `attempts` field on `ReportFacts` with one `running` state is **rejected** — it puts the
  machine's own memory outside the machine and is the integer `attempts` counter decision 2
  already rejected, arriving by a side door;
- redefining which state `produced_no_report` applies to (letting the retry run while the
  state stays `failed_once`) is **rejected** — it makes the retry run indistinguishable from a
  report sitting idle after one failure, so a second submission during the retry would be
  refused `already_used` when criterion 5 binds that outcome to a different written line. This
  node's `already_running` clause depends on the retry being a running state.

`scans.correction_state` admits one more string value and needs **no migration**: the column is
text and its value domain is this node's (BP-028's `## Data model delta`). `CORRECTION.retries:
1` still bounds the machine — a seventh state would mean a second retry, which is a pin change
and a requirement question, not something implemented here.

## Context an implementer needs
No source code exists on disk; both paths below are NEW. This module is **pure**: BP-028
decision 3 — "This node is pure and BP-012 calls into it, never the reverse". Putting the
re-measurement here would make `src/lib/market/ → src/lib/scan/` and close the cycle
`structure.md` rule 6 makes a build failure. `CORRECTION_SCOPE` exists precisely so the
boundary between what this node decides and what BP-012 executes is a value both sides can
be tested against.

This node is the canonical home of the **value domain** of `scans.correction_state` — the
column already exists in BP-002's baseline and BP-012's migration glob owns it. No migration
is written here.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/market/coherence/state.ts` | **NEW** | `CorrectionState` (six members), `CorrectionEvent`, `nextCorrectionState` and `CORRECTION_SCOPE`, exactly as BP-028's `## Public interface` states them. The transition table BP-028 writes out in that interface is the switch's source; it is transcribed, not re-derived. |
| `tests/market/coherence/state.test.ts` | **NEW** | The tests named in the test plan, including the exhaustive states × events table over all 6 × 4 pairs. |

## Interfaces
**Consumes**: nothing. No import outside this file's own type declarations.

**Exposes** — copied exactly from BP-028's `## Public interface`:

```ts
// src/lib/market/coherence/state.ts
/** Six members, not five. `running` and `running_retry` are both *running*
 *  states and differ only in which attempt is in flight, because
 *  `(running, produced_no_report)` must yield `failed_once` on a first attempt
 *  and `exhausted` on the retry — one pair, two results, which a total function
 *  cannot produce. Decision 2 records why the attempt count is in the state
 *  rather than in `ReportFacts`. */
export type CorrectionState =
  | 'none' | 'running' | 'failed_once' | 'running_retry' | 'used' | 'exhausted'
export type CorrectionEvent =
  | 'submitted' | 'refused_before_run' | 'produced_report' | 'produced_no_report'
export function nextCorrectionState(a: { current: CorrectionState; event: CorrectionEvent }):
  { next: CorrectionState } | { refused: 'already_running' | 'already_used' }
// The whole table, and the exhaustive switch is over exactly these pairs:
//   none         + submitted          -> running
//   running      + produced_report    -> used
//   running      + produced_no_report -> failed_once
//   failed_once  + submitted          -> running_retry
//   running_retry+ produced_report    -> used
//   running_retry+ produced_no_report -> exhausted
//   any          + refused_before_run -> current, unchanged (costs no attempt)
//   running | running_retry + submitted        -> refused 'already_running'
//   used | exhausted        + submitted        -> refused 'already_used'
//   used | exhausted | none | failed_once + produced_(no_)report -> unreachable,
//     and a compile error rather than a fall-through arm.

/** What a correction re-measures and what it carries forward. BP-012 reads this
 *  constant rather than deciding the scope itself, so no correction can re-read
 *  the domain's own pages by accident (REQ-094 criterion 3). */
export const CORRECTION_SCOPE: {
  reDerive:     readonly ['market', 'questions', 'ai_answers', 'google_presence', 'rivals', 'score']
  carryForward: readonly ['foundations', 'answerability', 'on_page', 'robots']
}
```

## Steps
1. Declare the two unions and the constant, `as const`, with the two tuples exactly as quoted.
   `CorrectionState` has **six** members in the order BP-028 writes them.
2. Implement `nextCorrectionState` as an exhaustive switch over every (state, event) pair,
   transcribed from BP-028's table. An unhandled pair must be a **compile error** (a
   `never`-typed exhaustiveness check), not a fall-through (BP-028 decision 2).
3. The nine named rows, exactly as the table states them — sixteen of the twenty-four pairs
   between them:
   - `none + submitted → running`.
   - `running + produced_report → used`.
   - `running + produced_no_report → failed_once`.
   - `failed_once + submitted → running_retry`.
   - `running_retry + produced_report → used`.
   - `running_retry + produced_no_report → exhausted`.
   - `<any state> + refused_before_run → { next: <that same state> }` — a refusal before the
     correction ran consumes neither attempt: nothing was measured and nothing was spent.
   - `running + submitted` and `running_retry + submitted` → `{ refused: 'already_running' }`.
     Both running members refuse the same way: a submission during the retry is
     `already_running`, **not** `already_used`, because the retry has not been spent until it
     returns (BP-028's `## Error & edge behavior`), and criterion 5 binds the two outcomes to
     different written lines.
   - `used + submitted` and `exhausted + submitted` → `{ refused: 'already_used' }`.
4. `produced_report` from either running member returns `used` **including where that report
   stopped early**: a correction whose re-measurement stopped early did produce a report
   (criterion 7's last sentence).
5. The pairs unreachable from a correct caller — `none`, `failed_once`, `used` and `exhausted`
   each with `produced_report` or `produced_no_report`, eight pairs in all — are enumerated in
   the switch, not caught by a `default`, and each returns `{ next: current }`.
   **Placement choice (rule 1.1).** BP-028's table marks these pairs "unreachable, and a
   compile error rather than a fall-through arm" but states no return value for them, while
   decision 2 requires the function to be **total** over states × events. `{ next: current }`
   is the only return that satisfies totality without inventing a transition: it advances
   nothing and spends nothing, so a caller that reached it by mistake loses no attempt.
   Reversing this is one arm-set of the switch and one test row; no stored value and no
   customer-visible consequence. Reported to the architect as a gap in the table, not asked.
6. Emit the observability field BP-028 names: each state transition with its event.
7. Write the tests; watch each fail first.

## Test plan
| Acceptance criterion (verbatim from REQ-094) | Test |
|---|---|
| 5. "Given a report whose one correction has been accepted and has produced a corrected report, when any visitor opens it, then no correction control is offered, one written line says a free scan allows one correction and that this one has been used, and the corrected report stays readable. Given a correction already running for that report, when a second is submitted, then it is refused in writing as a correction already under way rather than as one already used, and whoever was refused is left where the running correction leaves them: the corrected report if it produced one (criterion 4), or the control still available if it did not (criterion 7)." — the state limb; the two written lines are BP-019's and the offer is WO-082's. | `tests/market/coherence/state.test.ts › state/running-and-used-are-different-refusals` — a submit at `running` **and** a submit at `running_retry` each return `{ refused: 'already_running' }`; a submit at `used` or `exhausted` returns `{ refused: 'already_used' }`; the two values are never interchanged. `state/retry-in-flight-is-already-running-not-already-used` — the `running_retry` case named separately, because it is the one BP-028 decision 2a's alternative (b) would have broken. |
| 7. "Given a correction that ran and produced no report, when the reader returns to the report address, then the report stored before the correction is still there under its own measurement date and its original category (REQ-001 criterion 17), the reader is told in writing that the correction did not complete — in the one written line REQ-001 criterion 16 shows while it shows one, never a second line beside it — and the correction may be submitted once more on criterion 1's terms, that retry being the one control REQ-001 criterion 16 admits there. Given that retry also ran and produced no report, when the reader returns, then the report stands — its original category, its own measurement date, and one written line saying the correction could not be completed — and no correction control is offered for that report again. Given a correction refused before it ran, because free scanning is paused or switched off or a scan is already running for that network (REQ-003 criteria 7 and 8), when the reader is told which limit was met, then neither attempt has been used and the control still stands: nothing was measured and nothing was spent. A correction whose re-measurement stopped early does produce the stored report — incomplete, on REQ-003 criterion 11's terms — and that correction has been used." — the state limb. | `tests/market/coherence/state.test.ts › state/retry-is-spent-by-being-taken` — the full sequence `none` →(submitted)→ `running` →(produced_no_report)→ `failed_once` →(submitted)→ `running_retry` →(produced_no_report)→ `exhausted`, asserting each intermediate state by name, and a submit at `exhausted` is refused. `state/first-failure-is-not-exhaustion` — `running + produced_no_report` returns `failed_once` and never `exhausted`, which is the pair the five-member union could not separate. `state/early-stop-is-used` — `produced_report` from **either** running member returns `used` regardless of how much that report measured. `state/refusal-before-run-costs-nothing` — for every one of the six states, `{ event: 'refused_before_run' }` returns `{ next: <that same state> }`. |
| 3. "Given a reader who submits a corrected category, when the correction is accepted, then the market set, the questions, and every figure measured from them — the AI answers, the Google presence, and the drivers and score that rest on those — are measured again against the corrected category and the report re-renders from them, while what the scan read from the domain's own pages is not re-read and does not change. That re-measurement is the free scan it corrects still measuring: it spends no further scan allowance and opens no second spend ceiling (REQ-003 criteria 6 and 11), and its own wait is bounded as any scan's wait is, timed from the moment the correction was accepted (REQ-003 criteria 1, 2 and 5) — the time the reader took to decide counts against no bound. The corrected report is one scan's report throughout, carrying one measurement date, the moment that scan's measurement finished; every figure under that date comes from that one scan and none from another (REQ-004 criterion 12), and the figures carried forward unchanged from the domain's own pages were read less than 7 days before the correction started (criterion 1)." — the scope limb only: this constant fixes what a correction re-derives and what it carries forward, so no correction can re-read the domain's own pages by accident. The allowance, the wait, the single measurement date and the re-render are BP-012's, which reads this constant. | `tests/market/coherence/state.test.ts › scope/re-derives-six-carries-forward-four` — `CORRECTION_SCOPE.reDerive` equals `['market','questions','ai_answers','google_presence','rivals','score']` and `CORRECTION_SCOPE.carryForward` equals `['foundations','answerability','on_page','robots']`, and the two tuples are disjoint. |

Totality, required by BP-028 decision 2 ("an unhandled pair is a compile error, not a
fall-through"): `state/is-total` — a table-driven test over all **24** (state, event) pairs,
six states × four events, asserting each returns a value of the declared union; and a
type-level test that removing an arm from the switch fails to compile.

Table fidelity: `state/matches-the-declared-table` — the 24-row expectation table in the test
is transcribed from BP-028's `## Public interface` comment block and is asserted row for row,
so a transition changed in code without changing the blueprint fails here rather than passing
quietly.

Union shape: `state/has-exactly-six-members` — `CorrectionState`'s members are exactly
`'none'`, `'running'`, `'failed_once'`, `'running_retry'`, `'used'`, `'exhausted'`. A seventh
would mean a second retry, which `CORRECTION.retries: 1` does not admit.

Purity: `state/is-pure` — `state.ts` resolves no import at all outside its own module. In
particular it imports no `ReportFacts` and takes no attempt count: the machine's memory is the
state (BP-028 decision 2a, alternative (a) rejected).

Discrimination: collapsing `already_running` into `already_used` at `running_retry`, merging
`running_retry` back into `running`, or making `refused_before_run` advance a state, must each
fail a named test above.

## Out of scope
- The offer, the category on the report and the 7-day expiry — WO-082.
- Running the re-measurement, storing `scans.correction_state`, and
  `scans.supersedes_scan_id` (REQ-094 criterion 4's replacement) — all BP-012's.
- The migration for `scans.correction_state` — BP-002's baseline, BP-012's glob.
- The coherence verdict — WO-080.
- Amending BP-028: blueprints are the architect's (constitution §4). This WO implements the
  six-member machine decision 2a landed; it does not edit one.

## Rollback
Delete both files; WO-082 has not landed, so nothing imports `CorrectionState`.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-028 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 3 rows of `## Test plan` quote REQ-094 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — block cleared against BP-028 decision 2a; plan re-cut on the six-member `CorrectionState` with the declared transition table transcribed into the interface, steps and a 24-pair totality test
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-08-31 finished — planner — `depends-on: WO-001` added: this work order ships TypeScript under `src/` and a `tests/**` suite, and WO-001 is the only work order that creates `package.json`, `tsconfig.json`, `vitest.config.ts` and `tests/setup.ts`. Without the edge `/wave propose` could schedule it before a package manifest exists. WO-001 carries `depends-on: []`, so the edge adds no cycle.
- 2026-09-04 finished — planner — /wave propose: **W4**, MoSCoW **Must**; `risk: high` set with its seam in `## Goal` (a state machine that publishes, doctrine 0.13.2 rule 2b). Declaration-first ground: it declares `CorrectionState`, a member of `StoredReport`.
- 2026-09-04 started — implementer
- 2026-09-04 finished — implementer — `state.ts`/`state.test.ts` land: six-member `CorrectionState`, `CorrectionEvent`, `nextCorrectionState` (total, `assertNever`-guarded — verified removing an inner or outer switch arm fails `tsc`, not just at runtime), `CORRECTION_SCOPE`, per BP-028's public interface exactly. 15/15 scoped tests green, watched fail first (missing module). Mutation-tested: 5 hand mutations (merge failed_once/exhausted, collapse already_running/already_used at running_retry, advance state on refused_before_run, drop early-stop's move to used, alter an unreachable pair's value) each killed 1–4 tests and were reverted clean; two arm-removal mutations each failed `tsc` instead of the suite, proving totality is a compile-time property. Typecheck clean; scoped lint clean (repo lint 8115, none in these two files). Ready to validate.
- 2026-09-04 finished — validator — TST-045: all three test-plan rows PASS, 3 independent hand mutations killed (mutation required, `risk: high`), the compile-time totality claim independently reproduced (arm removal → real `tsc` TS2345, not a runtime fallthrough). `rests-on: []` — no row to disposition. Verdict: pass.
- 2026-09-04 finished — validator — /regress (W4 wave-close pass): full suite (7 independent runs), typecheck, lint and build re-run clean on tip `bb9373e`; `factory-console impact WO-081` re-checked, REQ-094 re-verified against unchanged code; the totality claim independently re-reproduced once more (arm removal → real `tsc` TS2345, reverted). Regression: pass.

## Validation report (appended by validator)

## TST-045 — WO-081 validation report

- Date: 2026-09-04
- Branch/HEAD: `wave/W3` @ `3caa2ae`
- Corpus-global id: `TST-045` — next unused after `TST-044` (WO-080, same dispatch).
- `risk: high` (WO-081 `## Goal`: "seam: a state machine that publishes") — mutation pass owed and performed below.

### Commands

| Command | Result |
|---|---|
| `npx vitest run --project node tests/market/coherence/state.test.ts` | 1 file, 15 tests, all pass |
| `npm run typecheck` | exit 0 clean at HEAD; separately, a scratch arm-removal mutation (below) reproduces a real TS2345 |
| `npm run lint` | 8115 baseline, none in `src/lib/market/coherence/state.ts` or its test |
| `git diff --stat 2c09b0e~1..f53c4ed` | exactly 2 new files plus the WO's own `## Log` — matches the file plan |

### Criterion-by-criterion (WO-081 `## Test plan`, 3 rows)

| # | Criterion (REQ-094, abbreviated) | Test | Result |
|---|---|---|---|
| 5 (state limb) | `already_running` and `already_used` are never interchanged; the retry-in-flight case is named separately | `state/running-and-used-are-different-refusals`, `state/retry-in-flight-is-already-running-not-already-used` | PASS |
| 7 (state limb) | the full failure/retry/exhaustion sequence; first failure ≠ exhaustion; an early-stopped report still counts as `used`; a refusal before run costs nothing | `state/retry-is-spent-by-being-taken`, `state/first-failure-is-not-exhaustion`, `state/early-stop-is-used`, `state/refusal-before-run-costs-nothing` | PASS |
| 3 (scope limb) | `CORRECTION_SCOPE` fixes exactly what re-derives and what carries forward, disjoint | `scope/re-derives-six-carries-forward-four` | PASS |

Plus the WO's own named totality, fidelity, union-shape, purity and discrimination tests, all passing: `state/is-total` (all 24 pairs), `state/matches-the-declared-table` (24-row transcription check), `state/has-exactly-six-members`, `state/is-pure` (no import at all, no `ReportFacts`), `state/discrimination` (3 assertions).

### Mutation testing — `risk: high`, required by doctrine 0.13.2

Three independent hand mutations against `src/lib/market/coherence/state.ts`, each run, observed to fail, then reverted (`git diff --stat` empty after each restore):

1. **Collapsed `running_retry`'s `submitted` refusal from `already_running` to `already_used`** (leaving `running` alone). Result: 4/15 fail, including both named `running-and-used-are-different-refusals` and `retry-in-flight-is-already-running-not-already-used`. **Kills.**
2. **Made `refused_before_run` advance state** (`running` instead of `current`). Result: 3/15 fail, including `state/refusal-before-run-costs-nothing` and the discrimination suite's own "must not advance any state" row. **Kills.**
3. **Merged `running_retry`'s `produced_no_report` outcome into `running`'s** (`failed_once` instead of `exhausted`). Result: 3/15 fail, including the discrimination suite's "merging running_retry back into running must fail" row and the totality/table-fidelity checks. **Kills.**

All three mutations named in the WO's own log and discrimination note were reproduced independently and all three kill. No vacuous test found.

### Totality claim — verified directly, not inherited

The implementer's return claims removing a switch arm fails `tsc` with a real compile error rather than falling through. I reproduced this myself: removed the `case "exhausted":` arm from the `submitted`-event inner switch (leaving `case "used":` alone to reach `assertNever(current)`), ran `npx tsc --noEmit`, and got:

```
src/lib/market/coherence/state.ts(89,30): error TS2345: Argument of type '"exhausted"' is not assignable to parameter of type 'never'.
```

This is a genuine compile-time guarantee, not a documentation claim: the `assertNever(x: never): never` guard (`state.ts:44-46`) means an un-narrowed union member reaching that call site is a type error at every one of the four inner switches, not just the one I removed. File restored; `git diff --stat` empty after restore.

### `rests-on` dispositions (constitution rule 2.3b)

WO-081's front matter carries `rests-on: []`. No row to disposition. This is not an omission left `open` — it is the WO's own recorded state, checked directly against the file, and constitution 2.3a's default-`open` schema has nothing to project here because no row was ever raised. No `open` row exists.

### Coverage gaps

None found against this WO's own test plan.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

pass

Regression: 21 files · REQ-094 re-checked — pass

`git diff --stat 3caa2ae..bb9373e -- src/ tests/` is empty: no code has moved since TST-045's own tip. Full suite independently re-run, 7 runs on tip `bb9373e`: 6/7 clean at 57 files / 1406 passed / 1 skipped, 1/7 the documented intermittent `safe-fetch` timer flake, unrelated. `npm run typecheck` clean; `npm run lint` 8115, none under `src/`/`tests/`; `npm run build` clean against a throwaway, gitignored `.env.local` (deleted after, tree byte-clean). `factory-console impact WO-081`: 21 files → 2 blueprints (BP-011, BP-028) → 0 importers → 1 requirement (REQ-094); `tests/market/coherence/state.test.ts` re-run in isolation, 15/15 passed against the unchanged `src/lib/market/coherence/state.ts`; the totality claim (arm removal → real `tsc` error, not a fall-through) independently re-reproduced once more this pass. `rests-on: []` — nothing to disposition, confirmed against the current front matter. No `done` work order's files disturbed by this WO's own impact set.
