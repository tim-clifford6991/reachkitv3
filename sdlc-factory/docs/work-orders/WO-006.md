---
id: WO-006
type: work-order
title: "constants.ts — every pinned number, grouped and frozen"
status: approved
ui: no
implements: [BP-005]
wave: W1
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "One value BP-005 decision 3 lists as deliberately undeclared stays undeclared here: the removal address REQ-002 criterion 1 requires the free report to name. It is a customer-visible string and the owner's under the decision-rights table; it is absent, not guessed (rule 1.2). Whether anything records receipt of a removal request is outstanding with it. The other four this row carried were ruled by the owner on 2026-08-31 (BP-005 decision 3, \"Discharged 2026-08-31 by owner ruling\"): the six band words and the three severity words landed in BP-019's `BAND_LABELS` and `SEVERITY` — not here, decision 2 stands — and `GOAL_VALUES.pages_published` landed here as 30."
    disposition: open
  - claim: "`AI_READER_AGENTS` and `PLATFORM_DOMAINS` are populated from quoted source clauses — REQ-059 criterion 4 and `BUILD.md` §6.1 respectively — never from memory. `BUILD.md` §6.1's `PLATFORM_DOMAINS` row ends in an ellipsis (\"… (closed list)\"), so the list it states is not complete on its face; the implementer takes the ten names §6.1 does state and raises the incompleteness rather than inventing an eleventh."
    disposition: open
supersedes: []
---

# WO-006 — constants.ts — every pinned number, grouped and frozen

- Branch: wo/WO-006-constants
- Estimate: ≤1 day

## Goal
Declare, once, every pinned number the specification and the corpus's forty-four
feature nodes ask for, grouped and frozen, so no coefficient, cap, floor,
denominator or threshold is ever written twice.

## Provenance of this file plan
**This repository holds no `src/`.** Both paths are NEW, anchored to BP-005's
`code:` glob `src/lib/config/**`.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/config/constants.ts` | NEW | Every export in BP-005's `## Public interface`, transcribed with its stated value and its source comment, one frozen `as const` group per source section: `PRICE_BOOK`, `CAPS`, `BATTERY`, `SERP_LOCATION`, `PLATFORM_DOMAINS`, `CACHE_WINDOWS_D`, `FREE_BOUNDS`, `TIMING`, `WINNABILITY`, `RIVAL_SIZE_BANDS`, `GENERATION`, `SUPPLY`, `VETO`, `RATE_LIMITS`, `RETENTION_D`, `NURTURE_H`, then the 2026-08-31 block: `SELECTION`, `COHERENCE`, `CORRECTION`, `SEVERITY_THRESHOLDS`, `AI_READER_AGENTS`, `FREE_RESCAN_WINDOW_D`, `FAILURE_COOLDOWN_H`, `HOURLY_WINDOW_H`, `DAILY_WINDOW_H`, `DEEP_HEARTBEAT_S`, `EFFORT_BY_TYPE`, `FIT_WEIGHT`, `SUPPLY_TARGET_DEPTH`, `SUPPLY_SHORT_BELOW`, `TOO_EARLY_WEEKS`, `SHINGLE_SIZE`, `NEAR_DUPLICATE_MAX`, `MACHINE_ADDRESS_PATTERNS`, `CLAIM_RECHECK_SWEEP_MAX`, `PUBLISH_RETRY_BACKOFF_MIN`, `DESTINATION_HEALTH_MAX_AGE_H`, `VERIFY`, `WEEK_START`, `WEEKLY_DUE_HOUR_LOCAL`, `SETUP_REMINDER_OFFSETS_H`, `EMAIL_CHANGE_TTL_H`, `SIGNIN_LINK_TTL_H`, `ERASURE_DAYS`, `DANGER_TICKET_TTL_MINUTES`, `HOSTED_RETENTION_DAYS`, `HOSTING_END_REMINDER_DAYS`, `MAINTENANCE_TICK_MINUTES`, `NURTURE_MAX_TOUCHES`, `SEQUENCE_START_DEADLINE_DAYS`, `FIRST_PAGE_RETRY_WINDOW_H`, `FIRST_PAGE_RETRY_MINUTES`, `GOAL_VALUES`. Two of those groups are the 2026-08-31 amendment and are transcribed with the rest: **`SCORE_BAND_BOUNDS`** — `{ invisible: 0, 'hard-to-find': 25, findable: 50, dominant: 75 }`, `BUILD.md` §5's four boundaries, which BP-024's `bandOf` already cited as pins in this file and which this file had no entry for — and **`GOAL_VALUES.pages_published: 30`**, no longer optional. Imports nothing. Holds no `CopyKey` and no customer-visible string: `SCORE_BAND_BOUNDS` carries the four *boundaries* and none of the four *words*, which are BP-019's `SCORE_BANDS` (decision 2). |
| `tests/config/constants.test.ts` | NEW | Structural tests only — frozenness, `as const`, no import, no string that reads as product copy. The value-against-source assertions are `tests/pins.test.ts` (WO-007) and are not duplicated here. |

## Interfaces
Exposes: BP-005's `## Public interface` block for `src/lib/config/constants.ts`,
transcribed exactly, including its comments and the traced-to-asker annotations.
Consumes: nothing. BP-005: "Imported by every other module; imports nothing."

## Steps
1. Transcribe every group from BP-005's `## Public interface`, keeping each
   source comment (`// BP-025 · REQ-006 · BUILD §6.7 step 3` and so on) — the
   comment is what WO-007 asserts against.
2. Populate `PLATFORM_DOMAINS` from `BUILD.md` §6.1's stated names only.
3. Populate `AI_READER_AGENTS` from REQ-059 criterion 4's stated membership,
   quoted in a comment above the array.
4. Declare `GOAL_VALUES` with all four values present, `pages_published: 30` —
   the owner's ruling of 2026-08-31, transcribed from BP-005's own note ("**Ruled
   2026-08-31: 30**, meaning a month of daily pages"), never re-derived.
5. Declare `SCORE_BAND_BOUNDS` with BP-005's four boundaries and its source
   comment quoting `BUILD.md` §5. Four keys, four numbers, no word.
6. Freeze every group; confirm `npm run typecheck` passes.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-005 carries
> `satisfies: []`. REQ-070 criterion 3's second half — a measurement or spend
> parameter's "value is identical for every customer" — is the requirement-side
> promise a frozen pin serves; its first half ("no control over it is offered")
> is BP-055's Settings surface, outside this block. The criterion is therefore
> cited, not inherited, and this work order claims no `implements:` edge to
> REQ-070.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-005 error behaviour: "Every constant is `as const` and frozen; nothing mutates a pin at runtime" | `tests/config/constants.test.ts` — asserts every exported group is frozen and that a write throws in strict mode. |
| BP-005 module boundary: "Imported by every other module; imports nothing." | Same file — parses the module and asserts its import list is empty. |
| BP-005 decision 3, "Not declared, deliberately: the removal address REQ-002 criterion 1 requires the report to name" | Same file — asserts the one still-owner-owed value is absent: no removal address anywhere in the file. Adding it fails the test, which is the point of the test. |
| BP-005 decision 2: "`WINNABILITY` and `RIVAL_SIZE_BANDS` hold the counts here; the six words live in BP-019's `BAND_LABELS`" — and decision 3's discharge, "not here — decision 2 stands" | Same file — asserts no winnability word, no rival-size word, no severity word and no score-band word appears in `constants.ts`, now that all nine exist in BP-019. The nine being ruled is what makes this test capable of failing; before the ruling it could only pass. |
| BP-005 `SCORE_BAND_BOUNDS` (added 2026-08-31): "Boundaries only — the four *words* are BP-019's `SCORE_BANDS`" | Same file — asserts `SCORE_BAND_BOUNDS` has exactly the four keys `invisible`, `hard-to-find`, `findable`, `dominant`, that every value is a number, and that no value is a string. The value-against-`BUILD.md`-§5 assertion is WO-007's. |
| BP-005 `GOAL_VALUES`: "It is no longer optional, and BP-038 renders that tile's goal like the other three." | Same file — asserts `GOAL_VALUES.pages_published === 30` and that the field is required, not optional. |
| BP-005 `GOAL_VALUES` note: "this file stays free of `CopyKey` and keeps importing nothing" | Same file — asserts no identifier named `CopyKey` appears. |
| BP-005 `AI_READER_AGENTS`: "An empty list fails the pins test." | Same file — asserts the array is non-empty; the membership assertion against REQ-059 criterion 4 is WO-007's. |

## Out of scope
- Asserting each value against its quoted source clause — WO-007.
- `env.ts` — WO-005.
- The removal address the owner still owes (listed in `rests-on`).
- The nine ruled words themselves. They exist now, and they live in BP-019's
  `BAND_LABELS`, `SCORE_BANDS` and `SEVERITY` and in the copy registry — never
  in this file (BP-005 decision 2).

## Rollback
Delete both files. Every later module imports this one, so a revert after WO-007
lands breaks the pins test first, by design.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-005 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 8 rows of `## Test plan` quote BP-005's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended to BP-005's 2026-08-31 state: `SCORE_BAND_BOUNDS` and `GOAL_VALUES.pages_published: 30` added to the plan, the five-value `rests-on` reduced to the removal address alone, and the owner-owed absence test re-cut as a decision-2 placement test.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 started — implementer
- 2026-09-03 finished — implementer — `constants.ts` and its structural test audited group-by-group against BP-005's interface (source comments kept, `SCORE_BAND_BOUNDS`/`AI_READER_AGENTS`/`PLATFORM_DOMAINS`/`GOAL_VALUES.pages_published` correct, no BP-019 word, no `CopyKey`, no import, removal address absent); 107/107 tests pass, frozenness and absence tests confirmed to fail-then-pass under mutation, typecheck and lint clean.

## Validation report (appended by validator)
