---
id: WO-006
type: work-order
title: "constants.ts — every pinned number, grouped and frozen"
status: approved
ui: no
implements: [BP-005]
wave: W1
depends-on: [WO-001]
blocked-by: []
rests-on:
  - claim: "One value BP-005 decision 3 lists as deliberately undeclared stays undeclared here: the removal address REQ-002 criterion 1 requires the free report to name. It is a customer-visible string and the owner's under the decision-rights table; it is absent, not guessed (rule 1.2). Whether anything records receipt of a removal request is outstanding with it. The other four this row carried were ruled by the owner on 2026-08-31 (BP-005 decision 3, \"Discharged 2026-08-31 by owner ruling\"): the six band words and the three severity words landed in BP-019's `BAND_LABELS` and `SEVERITY` — not here, decision 2 stands — and `GOAL_VALUES.pages_published` landed here as 30."
    disposition: confirmed
  - claim: "`AI_READER_AGENTS` and `PLATFORM_DOMAINS` are populated from quoted source clauses — REQ-059 criterion 4 and `BUILD.md` §6.1 respectively — never from memory. `BUILD.md` §6.1's `PLATFORM_DOMAINS` row ends in an ellipsis (\"… (closed list)\"), so the list it states is not complete on its face; the implementer takes the ten names §6.1 does state and raises the incompleteness rather than inventing an eleventh."
    disposition: confirmed
supersedes: []
---

# WO-006 — constants.ts — every pinned number, grouped and frozen

- Branch: wo/WO-006-constants
- Estimate: ≤1 day

## Goal
Declare, once, every pinned number the specification and the corpus's forty-four
feature nodes ask for, grouped and frozen, so no coefficient, cap, floor,
denominator or threshold is ever written twice.

## Provenance of this file plan
**This repository holds no `src/`.** Both paths are NEW, anchored to BP-005's
`code:` glob `src/lib/config/**`.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/config/constants.ts` | NEW | Every export in BP-005's `## Public interface`, transcribed with its stated value and its source comment, one frozen `as const` group per source section: `PRICE_BOOK`, `CAPS`, `BATTERY`, `SERP_LOCATION`, `PLATFORM_DOMAINS`, `CACHE_WINDOWS_D`, `FREE_BOUNDS`, `TIMING`, `WINNABILITY`, `RIVAL_SIZE_BANDS`, `GENERATION`, `SUPPLY`, `VETO`, `RATE_LIMITS`, `RETENTION_D`, `NURTURE_H`, then the 2026-08-31 block: `SELECTION`, `COHERENCE`, `CORRECTION`, `SEVERITY_THRESHOLDS`, `AI_READER_AGENTS`, `FREE_RESCAN_WINDOW_D`, `FAILURE_COOLDOWN_H`, `HOURLY_WINDOW_H`, `DAILY_WINDOW_H`, `DEEP_HEARTBEAT_S`, `EFFORT_BY_TYPE`, `FIT_WEIGHT`, `SUPPLY_TARGET_DEPTH`, `SUPPLY_SHORT_BELOW`, `TOO_EARLY_WEEKS`, `SHINGLE_SIZE`, `NEAR_DUPLICATE_MAX`, `MACHINE_ADDRESS_PATTERNS`, `CLAIM_RECHECK_SWEEP_MAX`, `PUBLISH_RETRY_BACKOFF_MIN`, `DESTINATION_HEALTH_MAX_AGE_H`, `VERIFY`, `WEEK_START`, `WEEKLY_DUE_HOUR_LOCAL`, `SETUP_REMINDER_OFFSETS_H`, `EMAIL_CHANGE_TTL_H`, `SIGNIN_LINK_TTL_H`, `ERASURE_DAYS`, `DANGER_TICKET_TTL_MINUTES`, `HOSTED_RETENTION_DAYS`, `HOSTING_END_REMINDER_DAYS`, `MAINTENANCE_TICK_MINUTES`, `NURTURE_MAX_TOUCHES`, `SEQUENCE_START_DEADLINE_DAYS`, `FIRST_PAGE_RETRY_WINDOW_H`, `FIRST_PAGE_RETRY_MINUTES`, `GOAL_VALUES`. Two of those groups are the 2026-08-31 amendment and are transcribed with the rest: **`SCORE_BAND_BOUNDS`** — `{ invisible: 0, 'hard-to-find': 25, findable: 50, dominant: 75 }`, `BUILD.md` §5's four boundaries, which BP-024's `bandOf` already cited as pins in this file and which this file had no entry for — and **`GOAL_VALUES.pages_published: 30`**, no longer optional. Imports nothing. Holds no `CopyKey` and no customer-visible string: `SCORE_BAND_BOUNDS` carries the four *boundaries* and none of the four *words*, which are BP-019's `SCORE_BANDS` (decision 2). |
| `tests/config/constants.test.ts` | NEW | Structural tests only — frozenness, `as const`, no import, no string that reads as product copy. The value-against-source assertions are `tests/pins.test.ts` (WO-007) and are not duplicated here. |

## Interfaces
Exposes: BP-005's `## Public interface` block for `src/lib/config/constants.ts`,
transcribed exactly, including its comments and the traced-to-asker annotations.
Consumes: nothing. BP-005: "Imported by every other module; imports nothing."

## Steps
1. Transcribe every group from BP-005's `## Public interface`, keeping each
   source comment (`// BP-025 · REQ-006 · BUILD §6.7 step 3` and so on) — the
   comment is what WO-007 asserts against.
2. Populate `PLATFORM_DOMAINS` from `BUILD.md` §6.1's stated names only.
3. Populate `AI_READER_AGENTS` from REQ-059 criterion 4's stated membership,
   quoted in a comment above the array.
4. Declare `GOAL_VALUES` with all four values present, `pages_published: 30` —
   the owner's ruling of 2026-08-31, transcribed from BP-005's own note ("**Ruled
   2026-08-31: 30**, meaning a month of daily pages"), never re-derived.
5. Declare `SCORE_BAND_BOUNDS` with BP-005's four boundaries and its source
   comment quoting `BUILD.md` §5. Four keys, four numbers, no word.
6. Freeze every group; confirm `npm run typecheck` passes.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-005 carries
> `satisfies: []`. REQ-070 criterion 3's second half — a measurement or spend
> parameter's "value is identical for every customer" — is the requirement-side
> promise a frozen pin serves; its first half ("no control over it is offered")
> is BP-055's Settings surface, outside this block. The criterion is therefore
> cited, not inherited, and this work order claims no `implements:` edge to
> REQ-070.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-005 error behaviour: "Every constant is `as const` and frozen; nothing mutates a pin at runtime" | `tests/config/constants.test.ts` — asserts every exported group is frozen and that a write throws in strict mode. |
| BP-005 module boundary: "Imported by every other module; imports nothing." | Same file — parses the module and asserts its import list is empty. |
| BP-005 decision 3, "Not declared, deliberately: the removal address REQ-002 criterion 1 requires the report to name" | Same file — asserts the one still-owner-owed value is absent: no removal address anywhere in the file. Adding it fails the test, which is the point of the test. |
| BP-005 decision 2: "`WINNABILITY` and `RIVAL_SIZE_BANDS` hold the counts here; the six words live in BP-019's `BAND_LABELS`" — and decision 3's discharge, "not here — decision 2 stands" | Same file — asserts no winnability word, no rival-size word, no severity word and no score-band word appears in `constants.ts`, now that all nine exist in BP-019. The nine being ruled is what makes this test capable of failing; before the ruling it could only pass. |
| BP-005 `SCORE_BAND_BOUNDS` (added 2026-08-31): "Boundaries only — the four *words* are BP-019's `SCORE_BANDS`" | Same file — asserts `SCORE_BAND_BOUNDS` has exactly the four keys `invisible`, `hard-to-find`, `findable`, `dominant`, that every value is a number, and that no value is a string. The value-against-`BUILD.md`-§5 assertion is WO-007's. |
| BP-005 `GOAL_VALUES`: "It is no longer optional, and BP-038 renders that tile's goal like the other three." | Same file — asserts `GOAL_VALUES.pages_published === 30` and that the field is required, not optional. |
| BP-005 `GOAL_VALUES` note: "this file stays free of `CopyKey` and keeps importing nothing" | Same file — asserts no identifier named `CopyKey` appears. |
| BP-005 `AI_READER_AGENTS`: "An empty list fails the pins test." | Same file — asserts the array is non-empty; the membership assertion against REQ-059 criterion 4 is WO-007's. |

## Out of scope
- Asserting each value against its quoted source clause — WO-007.
- `env.ts` — WO-005.
- The removal address the owner still owes (listed in `rests-on`).
- The nine ruled words themselves. They exist now, and they live in BP-019's
  `BAND_LABELS`, `SCORE_BANDS` and `SEVERITY` and in the copy registry — never
  in this file (BP-005 decision 2).

## Rollback
Delete both files. Every later module imports this one, so a revert after WO-007
lands breaks the pins test first, by design.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-005 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 8 rows of `## Test plan` quote BP-005's stated behaviour — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — amended to BP-005's 2026-08-31 state: `SCORE_BAND_BOUNDS` and `GOAL_VALUES.pages_published: 30` added to the plan, the five-value `rests-on` reduced to the removal address alone, and the owner-owed absence test re-cut as a decision-2 placement test.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 started — implementer
- 2026-09-03 finished — implementer — `constants.ts` and its structural test audited group-by-group against BP-005's interface (source comments kept, `SCORE_BAND_BOUNDS`/`AI_READER_AGENTS`/`PLATFORM_DOMAINS`/`GOAL_VALUES.pages_published` correct, no BP-019 word, no `CopyKey`, no import, removal address absent); 107/107 tests pass, frozenness and absence tests confirmed to fail-then-pass under mutation, typecheck and lint clean.
- 2026-09-03 finished — validator — all 8 test-plan rows pass and discriminate under mutation, full suite clean but for the known pre-existing `.claude/` failure, scope confined to the file plan; the WORDPRESS_STAMP_SLUG/price-pin omission is a planner-side gap, not a defect of this build — verdict pass, TST-003.
- 2026-09-03 finished — validator — regression on TST-003: full suite 143/144 pass (same pre-existing `.claude/` failure, WO-270); `factory-console impact WO-006` names 0 requirements (BP-005 `satisfies: []`), so the 8 BP-005 test-plan rows were re-checked against unchanged code instead, code confirmed unmoved since `016892e`, spot mutations on rows 1 and 6 re-confirmed to discriminate — `Regression: 3 files · none re-checked — pass`.

## Validation report (appended by validator)

## TST-003 — WO-006 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-006`, branch `wo/WO-006-constants`, HEAD `016892e`
- Corpus-global id: `TST-003` — `grep -h "^## TST-" sdlc-factory/docs/work-orders/*.md` returns `## TST-001 — WO-001 validation report` and `## TST-002 — WO-005 validation report` only; `TST-003` is the next unused id.

### Commands (run fresh from HEAD)

| Command | Result |
|---|---|
| `npx vitest run tests/config/constants.test.ts` | 1 test file, 107 tests, all pass |
| `npx vitest run` (full suite) | 4 test files: 143 pass, 1 fails |
| `npm run typecheck` (`tsc --noEmit`) | exit 0 |
| `npx eslint src/lib/config/constants.ts tests/config/constants.test.ts` | exit 0 |
| `git diff 61f1fd0...wo/WO-006-constants --stat` | 3 files: `sdlc-factory/docs/work-orders/WO-006.md` (Log only), `src/lib/config/constants.ts` (NEW), `tests/config/constants.test.ts` (NEW) |

The one full-suite failure — `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" (`expected ['.claude'] to deeply equal []`) — is **pre-existing, not this WO's**: `.claude/` was vendored in `61f1fd0` before this branch was cut, `structure.md` rule 7 has since been amended to admit it, and the test edit is its own work order (documented identically in WO-005's TST-002). WO-006's diff touches none of `tests/app/toolchain.test.ts`, `structure.md`, or `.claude/`.

### Criterion-by-criterion (WO-006 `## Test plan`, 8 rows) and mutation results

| # | Criterion (source, abbreviated) | Test | Result | Mutation probe (each restored after; post-restore diff against pre-mutation file is empty) |
|---|---|---|---|---|
| 1 | "Every constant is `as const` and frozen; nothing mutates a pin at runtime" | frozen + write-throws assertions over every export | PASS | Unfroze `PRICE_BOOK` (dropped `Object.freeze`) → both the frozenness test and the write-throws test for `PRICE_BOOK` fail. Discriminates. |
| 2 | "Imported by every other module; imports nothing" | asserts no `import` statement in source | PASS | Inserted `import path from "node:path";` at top of file → fails. Discriminates. |
| 3 | Removal address "not declared, deliberately" | no key matching `/removal/i`; no email-shaped literal anywhere in file | PASS | Inserted `// contact: support@reachkit.app` → the email-literal test fails. Discriminates. |
| 4 | No band/rival-size/severity/score-band word (all 9, BP-019 decision 2/3's discharge) | `it.each` over the 9 words, checked against every string reachable from every export | PASS | Added `export const _PROBE = "Dominant" as const;` → the "Dominant" case fails. Discriminates. |
| 5 | `SCORE_BAND_BOUNDS` — 4 keys, numbers only, no string | key-set equality + `typeof === "number"` over all 4 values | PASS | Changed `invisible: 0` to `invisible: "0"` → the typeof assertion fails. Discriminates. |
| 6 | `GOAL_VALUES.pages_published === 30`, required not optional | value equality + `hasOwnProperty` + not-undefined | PASS | Changed value to `31` → value-equality test fails. Separately, deleted the field entirely → both the presence test and (implicitly) the value test fail. Discriminates both ways. |
| 7 | File "stays free of `CopyKey`" | comment-stripped source regex for the bare identifier | PASS | Inserted `type CopyKey = string;` → fails. Discriminates. |
| 8 | `AI_READER_AGENTS` — "an empty list fails the pins test" | `.length > 0` | PASS | Emptied the array to `[]` → fails. Discriminates. |

All 8 rows PASS. All 8 discriminate: each mutation was the shape the assignment named (unfreeze a group; add an import; insert a removal-address-shaped string; insert a BP-019 word; make `SCORE_BAND_BOUNDS` carry a string; make `pages_published` optional or ≠ 30; add a `CopyKey` identifier; empty `AI_READER_AGENTS`), and every one broke its named test and no others unexpectedly. No vacuous test found (constitution §8).

### Additional probes

- **`PLATFORM_DOMAINS`**: exactly the ten names `BUILD.md` §6.1 states — `reddit, quora, youtube, wikipedia, g2, capterra, medium, linkedin, producthunt, stackoverflow` — same order, none invented, none dropped. The file's own comment quotes §6.1's row verbatim, ellipsis included, and does not claim the list is complete.
- **`AI_READER_AGENTS`**: exactly the six names REQ-059 criterion 4 states — `GPTBot, ClaudeBot, OAI-SearchBot, Claude-SearchBot, PerplexityBot, Google-Extended` — and the file's comment quotes criterion 4 verbatim ("it blocks no general search engine crawler and permits by name GPTBot, ClaudeBot, OAI-SearchBot, Claude-SearchBot, PerplexityBot and Google-Extended.").
- **Deep freeze**: nested objects are independently frozen, not just their top-level parent — direct probe (`Object.isFrozen`) confirms `SELECTION` and `SELECTION.intentWeights` both `true`, `SEVERITY_THRESHOLDS.blocked_readers` `true`, `MACHINE_ADDRESS_PATTERNS` and `VERIFY` both `true`; writing `SELECTION.intentWeights.decision = 999` throws (`Cannot assign to read only property`). The structural test suite itself only checks one level deep (a top-level key write per exported group); the deep-freeze property was verified by direct probe, not by the shipped test, and is noted as a coverage gap below.
- **ESM import**: `import * as constants from "../../src/lib/config/constants.ts"` resolves and runs cleanly under both `vitest` and `tsx`; this repo's `tsconfig.json` sets no `verbatimModuleSyntax`, so that specific named risk does not arise here.
- **Scope**: `git diff --name-only 61f1fd0...wo/WO-006-constants` returns exactly `sdlc-factory/docs/work-orders/WO-006.md`, `src/lib/config/constants.ts`, `tests/config/constants.test.ts` — nothing else changed. WO-006.md's own diff (`git diff ... -- sdlc-factory/docs/work-orders/WO-006.md`) is confined to three new `## Log` lines.
- **Structure / capability check**: `structure.md` line 27 and `capabilities.md` line 84 both already name `src/lib/config/constants.ts` under BP-005 as the one home for this capability; this WO adds no second home and duplicates nothing indexed.

### Transcription audit — `WORDPRESS_STAMP_SLUG` and the three price pins

The implementer's rule-4.2 flag (in the file's own header comment) has two halves, both independently verified against the texts, not taken on the implementer's word:

1. **BP-005's current `## Public interface` does list all four.** `WORDPRESS_STAMP_SLUG` — BP-005.md line 150, `export const WORDPRESS_STAMP_SLUG: string // BP-048 · REQ-060 c6 · owner-owed`. The three price pins — BP-005.md lines 174–176, `PRICE_EUR_CENTS: 4900`, `PRICE_CURRENCY: 'eur'`, `PRICE_INTERVAL: 'month'`, added under BP-005 decision 5 ("The three price pins are declared here" — Status: accepted (2026-08-31)), which states explicitly: "declare them, under the price group above."
2. **WO-006's own file-plan enumeration and test plan omit all four.** `grep -o "WORDPRESS_STAMP_SLUG\|PRICE_EUR_CENTS\|PRICE_CURRENCY\|PRICE_INTERVAL" sdlc-factory/docs/work-orders/WO-006.md` returns nothing: the file-plan's group enumeration (`## File plan` row 1) runs `PRICE_BOOK … GOAL_VALUES` and stops there; none of the four appears in the 8-row `## Test plan` either.

Both halves confirmed. This is a **planner-side gap**, not a defect of this build: the implementer built exactly what WO-006's own plan enumerated and flagged the omission once, in one place (the file's header comment, per rule 4.2), rather than adding the four constants on its own authority. It is recorded here as a finding **for the planner** — a plan amendment (this WO or a follow-on) is needed to declare all four, matching BP-005's current interface — and is not scored against this build, which rule 4.2 and the assignment both direct me to judge against its own plan.

### Coverage gaps

- Deep-freeze of nested groups (`SELECTION.intentWeights`, `SEVERITY_THRESHOLDS`'s three sub-objects) is real (confirmed by direct probe above) but not asserted by the shipped test suite, which only probes one level deep per exported group. Not a criterion-8 failure — none of the 8 test-plan rows claims nested freezing — but a gap a future WO-007-adjacent pass could close.
- Value-against-quoted-source assertions (the pins test proper, `tests/pins.test.ts`) are explicitly out of scope per this WO's own `## Test plan` header and `## Out of scope`, and belong to WO-007. Not scored here.
- The `WORDPRESS_STAMP_SLUG`/price-pin omission above — planner's, not this build's.

### `rests-on` dispositions (constitution rule 2.3b)

**WO-006's own two rows**, dispositioned by this build:

1. *Removal address / receipt-of-removal-request* — **confirmed**. The build demonstrably keeps the removal address undeclared: no exported key matches `/removal/i` and no email-shaped literal appears anywhere in the file, both mutation-tested above (row 3) and shown to discriminate. This is exactly BP-005 decision 3's requirement — absent, not guessed. The row's trailing clause, "whether anything records receipt of a removal request," names a capability outside `constants.ts` and outside BP-005 entirely; this build neither adds nor forecloses it, so that half is unchanged and correctly still outstanding — this disposition covers only what a `constants.ts` build can observe: the value stays out, on purpose, nothing was smuggled in its place.
2. *`AI_READER_AGENTS` and `PLATFORM_DOMAINS` populated from quoted source clauses, never memory* — **confirmed**. Verified directly against REQ-059 criterion 4 and `BUILD.md` §6.1 in the "Additional probes" section above: both arrays match their quoted sources exactly, the file's own comments quote each source verbatim (§6.1's ellipsis included), and no eleventh `PLATFORM_DOMAINS` name was invented.

**BP-005's `rests-on`**: front matter states `rests-on: []` — no rows exist to disposition. Noted, not skipped.

No `open` row remains on WO-006.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject." (`.claude/factory/plugins/sdlc-factory/skills/work-order-writing/SKILL.md`'s own `## Validation report` section states the TST-id and `Regression:` line grammar but not a separate verdict-word list, so the agent table is the artifact that defines it.)

### Verdict

pass

Regression: 3 files · none re-checked — pass

`/regress` run 2026-09-03. Full suite: 4 test files, 144 tests, 143 pass, 1 fail — the same pre-existing `tests/app/toolchain.test.ts` › "no top-level directory outside structure.md rule 7's set is present" failure named above (`.claude/` committed in `61f1fd0`, structure.md rule 7 amended since, its own work order, WO-270); no new failure. `factory-console impact WO-006` → `3 files → 1 blueprints → 0 importers → 0 requirements` (`sdlc-factory/docs/work-orders/WO-006.md`, `src/lib/config/constants.ts`, `tests/config/constants.test.ts` → BP-005 → no importers → no requirements, consistent with BP-005's own `satisfies: []`). Since the impact set names zero requirements, the 8 BP-005 rows of this WO's own `## Test plan` were re-checked against current code instead: `git log` confirms `src/lib/config/constants.ts` and `tests/config/constants.test.ts` carry no commit since `016892e`, the exact HEAD TST-003 validated, so the code is unchanged; `npx vitest run tests/config/constants.test.ts` reruns clean at 107/107. Independently re-ran targeted mutations on rows 1 and 6 against this HEAD: removing `Object.freeze` from `PRICE_BOOK` still fails both the frozenness assertion and the write-throws assertion (2 failures), and changing `GOAL_VALUES.pages_published` from 30 to 31 still fails the value-equality assertion; both mutations were reverted and `git status --porcelain` confirmed clean (empty diff) after each restore, and the full 107/107 pass again on the restored file. Rows 2–5, 7 and 8 rest on the same unchanged file and TST-003's own recorded mutations. No new `rests-on` row was raised by this pass; WO-006's two rows and BP-005's empty set stand as TST-003 dispositioned them.
