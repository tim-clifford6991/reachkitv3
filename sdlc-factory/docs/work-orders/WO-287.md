---
id: WO-287
type: work-order
title: "The report address's own sentences enter the copy registry — eighteen ruled strings under fixed keys, and the subject slot reaches renderMeasured"
status: approved
wave: W4
ui: no
risk: normal
implements: [BP-019, BP-020, REQ-001, REQ-002, REQ-003, REQ-004, REQ-094]
depends-on: [WO-041, WO-278]
blocked-by: []
rests-on:
  - claim: "The eighteen values are the owner's ruling of 2026-09-04 (sheet 2), recorded verbatim at `sdlc-factory/docs/registry/evidence/RULING-copy-2026-09-04.json`. No agent writes, re-wraps, re-punctuates or 'improves' any of them; the implementer copies each value out of that file."
    disposition: confirmed
    reason: "The evidence file was copied byte-for-byte from the orchestrator's machine-verified artifact and re-verified after the copy by this planner: 18 keys, 8 U+2019 apostrophes, 8 U+2014 em dashes, zero U+0027, slots {what} {address} {domain} {wait}. sha256 86207a6274e94466fc4d9432d8ea6c260baf26bdb89167a29223669eeb95cef9. This work order names keys and never a value."
  - claim: "Filling `unmeasured.undeterminable` and `unmeasured.not-attempted` breaks `renderMeasured` at run time unless its options object also carries the subject those two ruled lines declare a slot for."
    disposition: confirmed
    reason: "Observed directly in two files. `src/lib/presentation/copy/copy.ts` throws `copy(): \"<key>\" requires slot \"<slot>\", which was not supplied.` for any slot declared in `COPY_META[key].slots` and absent from `vars`. `src/lib/presentation/measured.ts` has exactly one such call — `copy(o.unmeasuredLine)` — and passes no `vars`. Today the two keys are owner-owed, so `copy()` throws earlier and the defect is invisible; supplying the values moves the throw past the owner-owed guard and onto every unmeasured render."
  - claim: "Widening `renderMeasured`'s options object with that required member errors in exactly two files and nowhere else in the tree."
    disposition: confirmed
    reason: "Type fan-out check, measured, not grepped. A scratch edit of `src/lib/presentation/measured.ts` alone, then the repo's own type-check command — `npx tsc --noEmit`, the `typecheck` script in `package.json`; `factory.config.json` sets no `typeCheck` key, so the command is derived from the project's own tooling (rule 1.1). Baseline 0 errors; after the edit, 14 errors, all TS2345 'Property … is missing': `src/lib/presentation/copy/explain.ts` (1) and `tests/presentation/measured.test.ts` (13). `tsconfig.json`'s `include` covers `tests/**`, so the test boundary is inside the check. The edit was reverted and the tree left clean."
  - claim: "No thirteenth `keys/*.ts` partition is needed or wanted for the thirteen new keys."
    disposition: confirmed
    reason: "`src/lib/presentation/copy/registry.ts`'s own header states the list is closed — 'The closed partition list, written once (BP-020 decision 5) … no thirteenth import, no re-export by name' — and `tests/presentation/copy/registry.test.ts` asserts it with `expect(importedKeyFiles).toHaveLength(12)`. WO-070 and WO-249 both landed new keys inside an existing partition on the same reasoning; this order is the third application, not a new one."
  - claim: "The ten `notice.*`/`control.*`/`copy-link.*` names are exactly the gap the design-guardian reported on WO-282's v1 preview, and naming them changes nothing else about that preview."
    disposition: confirmed
    reason: "WO-282 `## Log`, 2026-09-04 preview line, verbatim: 'Duty-2 self-review: PASS, one non-blocking gap reported (§7's copy keys for the notice/control/copy-link text have no fixed `CopyKey` name anywhere in the corpus yet — distinct from the `verdict.limiting.*`, `unmeasured.*` and `removal.*` keys, which are already named)'. The gap is a name, not a layout; no token, component or placement moves."
  - claim: "This order is the only place in the corpus where these eighteen values can land — no other work order claims `src/lib/presentation/copy/keys/**` for them."
    disposition: confirmed
    reason: "Sixteen work orders name `copy/keys/`; of those, WO-041, WO-070 and WO-278 are `done`, WO-248/WO-249 are `superseded`, and the remaining eleven (WO-045, WO-049, WO-118, WO-121, WO-162, WO-165, WO-167, WO-241, WO-247, WO-250, WO-258) carry no wave and name none of these eighteen keys — checked by grep for `removal.`, `notice.`, `control.` and `copy-link` across all eleven. WO-282's own file plan carries no `keys/*.ts` row and its own words are 'This WO creates the reference and the test; it writes no address'."
supersedes: []
---

# WO-287 — The report address's own sentences enter the copy registry — eighteen ruled strings under fixed keys, and the subject slot reaches renderMeasured

- Branch: wo/WO-287-ruled-copy-entries
- Estimate: ≤1 day

## Goal
Make every sentence the report address speaks resolvable from the copy registry: the eighteen strings the owner ruled on 2026-09-04 land under fixed `CopyKey` names with their slots declared and their `fixedBy` recorded, and `renderMeasured` gains the one member the two ruled unmeasured lines need to render at all.

**Risk: normal.** The order moves no money, reads no session, calls back to no third party and publishes to nothing. It touches none of the seams the charter names (`00-project.md`: the SSRF fetcher, the cost seam, Stripe webhook handling, the `sendEmail()` seam, the `llm()` seam). Its criteria are byte-comparison and slot-declaration assertions a plain test discriminates directly — a mis-typed apostrophe fails a byte-for-byte comparison against the ruling file outright rather than surviving it.

**`ui: no`.** No route, no component, no token, no layout: the files are the copy partitions and the `Measured<T>` renderer, all inside `src/lib/presentation/**`. This is WO-041's kind of order, not WO-070's — WO-070 was `ui: yes` because it built the landing screen around its eight strings, not because it entered them. The screen these eighteen sentences appear on is WO-282's, whose v1 preview is already signed.

## Slice (rule 2.6)
One capability, interface to test: the report address's sentences, from their key names through their slot declarations to the renderer that fills them. It is above the floor and it is not half of WO-282 — each states its goal and stop condition without citing the other. This order stops when the eighteen keys resolve and the registry's coverage counts read 17 owner-owed / 53 filled / 70 total; WO-282 stops when every `AddressState` arm renders with exactly one control and one removal address. WO-282 consumes `copy()` and `CopyKey` — an interface BP-019 declares, not this order — and its file plan names no partition file. Neither order can be merged into the other without merging a registry into a route.

WO-041 (the registry itself) and WO-278 (the five seeded keys) are both `status: done` and terminal; neither can be amended, so a scoped amendment was not available and a new order is the only home.

## Source of the values
`sdlc-factory/docs/registry/evidence/RULING-copy-2026-09-04.json` — the owner's ruling, byte for byte. Every value in this order's file plan is copied from that file. No value appears anywhere in this work order.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/presentation/copy/keys/report.ts` | Modify | Fill the three `verdict.limiting.*` values from the ruling file. Declare thirteen new entries in this same partition — `removal.address`, `removal.line.on-report`, `removal.line.removed`, `notice.incomplete`, `notice.measurement-failed`, `notice.correction-failed`, `notice.refused.network-limit`, `notice.refused.scan-running`, `control.rescan-age`, `control.rescan-incomplete`, `control.retry`, `control.correction-retry`, `copy-link.label` — each with the `slots` map and `fixedBy` this order's two tables state, and no `law` tag. Header note in the file's own established form, naming this order and the ruling's date. |
| `src/lib/presentation/copy/keys/laws.ts` | Modify | Fill `unmeasured.undeterminable` and `unmeasured.not-attempted` from the ruling file and add `what: "text"` to each one's `slots` map. Their `law: "unmeasured"` tags and their existing `fixedBy` values stand unchanged; `unmeasured.dash` is untouched. |
| `src/lib/presentation/measured.ts` | Modify | `renderMeasured`'s options object gains one required member naming what could not be measured, and the `copy(o.unmeasuredLine)` call supplies it as that line's slot. The two-branch shape, the dash branch and `DASH_KEY`'s non-export are unchanged. |
| `src/lib/presentation/copy/explain.ts` | Modify | The `renderMeasured` call in the measured-slot branch supplies the new member — the file's one TS2345 from the fan-out check. `explain()`'s own public signature already carries a non-measured `vars` channel; whether the subject travels that way or as its own argument is the implementer's. |
| `tests/presentation/measured.test.ts` | Modify | Thirteen `renderMeasured` call sites supply the new member (13 × TS2345). The two assertions that `unmeasured.undeterminable` is owner-owed and that the throw names the key are replaced by this order's own unmeasured-line suites — the key now has a value, so the old assertions no longer discriminate anything. |
| `tests/presentation/bands.test.ts` | Modify | The suite 'each value is a declared CopyKey listed in OWNER_OWED, and copy() throws naming it', over `LIMITING_LINES`, inverts: the three keys are no longer owner-owed and `copy()` returns each one's sentence. |
| `tests/presentation/copy/registry.test.ts` | Modify | The registry's own coverage counts (rule 5.5) move from 22 owner-owed / 35 filled / 57 total to 17 / 53 / 70, with the running derivation in the comment extended the way the three earlier rulings extended it. `expect(importedKeyFiles).toHaveLength(12)` is unchanged — no partition is added. |
| `tests/presentation/copy/ruled-copy.test.ts` | NEW | The entry-level suites named in the test plan below. |

## Where the thirteen live, and why
All thirteen are sentences of one surface — the public report address, `src/app/(public)/scan/[domain]/_address/**`, BP-022's node — and `keys/report.ts` is that surface's partition. `report.ts`'s own header records the precedent this follows: WO-070 put the landing page's eight keys there 'rather than in a thirteenth partition (constitution rule 2.2/BP-020 decision 5's own reasoning, applied the same way WO-249 applied it to `laws.ts`)'. A partition file has to earn itself; a thirteenth would also break the closed-list assertion in `registry.test.ts` for nothing.

The five that already exist keep their homes: `verdict.limiting.*` in `keys/report.ts`, `unmeasured.*` in `keys/laws.ts` beside the law tag that scopes them.

## Slot declarations, and their derivation (rule 1.1)
`CopyMeta.slots` takes `text | date | measured` (`src/lib/presentation/copy/registry.ts`). All four ruled slot names are `text`. Cost of reversing any one: one word in a `slots` map plus the caller's argument type — no customer-visible consequence, because the kind is never rendered; it only decides which function may fill the slot.

| Slot | Keys | Kind | Derivation |
|---|---|---|---|
| `what` | `unmeasured.undeterminable`, `unmeasured.not-attempted`, `notice.incomplete` | `text` | It names *what* was not measured — a driver's name or an `UnmeasuredPart` — never a measurement's value, so `measured` states the wrong thing about it. `measured` is also uninhabitable here, not merely wrong: `copy.ts` refuses any key declaring a measured slot ('use explain() instead'), and `renderMeasured` reaches these two keys through `copy(o.unmeasuredLine)`, so a measured slot would make the unmeasured branch throw for every unmeasured value. `date` is not a date. |
| `address` | `removal.line.on-report`, `removal.line.removed` | `text` | The value substituted is `copy('removal.address')` itself — one key, one address, which is what REQ-002 criterion 3's 'names the address criterion 1 names' requires. Not a measurement and not a date. |
| `domain` | `removal.line.removed` | `text` | The domain segment of the report address — a routing identifier, not a measurement. `measured` is unfillable on this arm besides: `AddressState`'s `{ kind: 'removed' }` carries no `StoredReport` and no `Measured<T>` at all (WO-286), and `explain()` requires a `Measured` with a scan date per slot. |
| `wait` | `notice.refused.network-limit`, `notice.refused.scan-running` | `text` | REQ-003's third `rests-on` row, `disposition: confirmed`: 'A free visitor has no account and therefore no stated timezone, so every time this requirement tells a visitor is an elapsed duration, never a clock time.' `date` is this registry's kind for a clock time — `stopped.work.resumes-on`'s `date` slot and `next-publish.scheduled`'s `at` slot are both instants. An elapsed duration formatted by the refusal's own producer is text. `measured` is out for the same reason as `domain`: the `refused` arm carries a `Refusal`, no `Measured<T>` and no scan date. |

**No `law` tag on any of the thirteen.** `CopyMeta.law`'s four arms are `next-publish`, `stopped-work`, `no-presence-yet` and `unmeasured`, and the conformance suites 'enumerate their own scope from this tag'. The `unmeasured` scope is the pair `renderMeasured`'s `unmeasuredLine` argument takes (`keys/laws.ts` header); `notice.incomplete` is REQ-001 criterion 14's report-level notice and is never passed to `renderMeasured`, so tagging it would widen that suite onto a key it cannot render. This is the posture `report.ts` already records for its eight landing keys.

## `fixedBy` — the criterion each sentence is fixed by
The five that exist were checked, not assumed, and are correct as written; they are not edited.

| Key | `fixedBy` | Ground |
|---|---|---|
| `verdict.limiting.foundations` | `REQ-004 c2` | already on disk — unchanged |
| `verdict.limiting.answerability` | `REQ-004 c2` | already on disk — unchanged |
| `verdict.limiting.presence` | `REQ-004 c2` | already on disk — unchanged |
| `unmeasured.undeterminable` | `REQ-004 c6` | already on disk — unchanged |
| `unmeasured.not-attempted` | `REQ-004 c9` | already on disk — unchanged |
| `removal.address` | `REQ-002 c1` | 'it names in writing the address to which a request to remove the report about that domain is sent' |
| `removal.line.on-report` | `REQ-002 c1` | the same criterion — the line is how the address is named in writing on a live report |
| `removal.line.removed` | `REQ-002 c3` | 'one written line says the report was removed at the domain owner's request and names the address criterion 1 names' |
| `notice.incomplete` | `REQ-001 c14` | 'one written line says it is incomplete and names what was not measured' |
| `notice.measurement-failed` | `REQ-001 c16` | 'one written line says what happened and a manual retry control is offered' |
| `notice.correction-failed` | `REQ-094 c7` | 'the reader is told in writing that the correction did not complete — in the one written line REQ-001 criterion 16 shows while it shows one, never a second line beside it' |
| `notice.refused.network-limit` | `REQ-003 c6` | 'they are refused with one written line saying how long until they may scan again' |
| `notice.refused.scan-running` | `REQ-003 c7` | 'they are refused in writing, told how long until they may scan' |
| `control.rescan-age` | `REQ-001 c15` | 'Given a stored report 7 days old or older … then a re-scan control is offered' |
| `control.rescan-incomplete` | `REQ-001 c14` | 'a re-scan control is offered beside that line however new the report is' |
| `control.retry` | `REQ-001 c16` | 'a manual retry control is offered' |
| `control.correction-retry` | `REQ-094 c7` | 'that retry being the one control REQ-001 criterion 16 admits there' |
| `copy-link.label` | `REQ-001 c7` | 'when they use the copy-link control' |

## Decisions taken under rule 1.1
1. **`notice.measurement-failed` is fixed by REQ-001 c16, not REQ-003 c4.** Both criteria state the same promise from different sides: REQ-003 c4 at the moment a scan ends, REQ-001 c16 at the report address a visitor later opens. The sentence is rendered from the `AddressNotice` arm `measurement_failed`, which `resolveAddress` produces and which WO-286's own test plan traces to REQ-001 c16. One claim, one home (rule 2.4): the address-side criterion holds it and REQ-003 c4 cites it. Cost of reversing: one string in one `fixedBy` field.
2. **`removal.line.on-report` is fixed by REQ-002 c1, not given a criterion of its own.** Criterion 1 requires the address to be *named in writing* on the report; the address key alone is a value with no sentence around it. Both keys therefore carry c1, and the test plan asserts they name the same address. Cost of reversing: one `fixedBy` field.
3. **`rescan` gets two keys, not one with a slot.** `AddressControl` is `{ kind: 'rescan'; because: 'incomplete' | 'age' }` (WO-286), and the owner ruled two distinct labels for it. Two keys keep the choice a total switch over `because` rather than a string built at the call site. Cost of reversing: one key.

## Interfaces
Consumes: BP-020's `COPY`, `COPY_META`, `CopyMeta`, `CopyPartition`, `copy()`, `explain()`, `OWNER_OWED`; BP-019's `renderMeasured` and `LIMITING_LINES`; `AddressControl` and `AddressNotice`'s arm names (WO-286) as the shape the `notice.*`/`control.*` keys are cut to — this order imports neither type.
Exposes: thirteen new `CopyKey` members, five keys that stop being owner-owed, and one widened `renderMeasured` options object.

## Steps
1. Copy the eighteen values out of `sdlc-factory/docs/registry/evidence/RULING-copy-2026-09-04.json` into the two partition files. Copy, never retype: an editor that straightens a quotation mark has broken this order.
2. Declare each entry's `slots` map and `fixedBy` exactly as this order's two tables state, and no `law` tag on the thirteen.
3. Widen `renderMeasured`'s options with the required subject member and supply it at the `copy(o.unmeasuredLine)` call.
4. Fix the two consumers the fan-out check named, then run the type-check and confirm it is clean.
5. Update the three affected suites and add the new one; watch the byte-exactness suite fail before the values land (§8).
6. Re-run the full suite, the type-check and the lint.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-002 c1 — "Given any report, when it renders, then it names in writing the address to which a request to remove the report about that domain is sent, readable by a visitor with no account, session, or payment." | `ruled-copy/removal · one key, one address` — `copy('removal.address')` returns the ruled value and does not throw; `removal.line.on-report` declares exactly `{ address: 'text' }`, `copy()` on it throws naming `address` when it is not supplied, and renders with no `{` marker left when it is; the rendered line contains `copy('removal.address')`. The rendering of that line on a report is WO-282's half. |
| REQ-002 c3 — "… in its place one written line says the report was removed at the domain owner's request and names the address criterion 1 names — until a request to that same address asks for that domain to be scannable again." | `ruled-copy/removal · the removed line takes both slots and names the same address` — `removal.line.removed` declares exactly `{ domain: 'text', address: 'text' }`; `copy()` throws naming `domain` when only `address` is supplied and naming `address` when only `domain` is; supplied both, the rendered line contains `copy('removal.address')` and no marker. |
| REQ-003 c6 — "… then it does not start, they are refused with one written line saying how long until they may scan again, and the refused scan, having never run, produces no report of its own." | `ruled-copy/notice · the network-limit refusal carries exactly one wait slot` — `notice.refused.network-limit` declares exactly `{ wait: 'text' }`; `copy()` throws naming `wait` when it is absent and renders with no marker left when it is supplied. |
| REQ-003 c7 — "… if the running scan is of the domain they asked for they are returned to it, and if it is not they are refused in writing, told how long until they may scan, and are never shown a scan or a report of a domain they did not ask for." | `ruled-copy/notice · the scan-running refusal is its own line` — `notice.refused.scan-running` declares exactly `{ wait: 'text' }`, renders the same way, and its value differs from `notice.refused.network-limit`'s, so the two causes are never one line. |
| REQ-004 c2 — "… then one written line beside it names the one driver, of the three the score is composed of, whose measurement holds the score down the most — and names it without stating that driver's value, its weight, or how the score is put together." | `ruled-copy/verdict · three lines, one per driver, and no value in any of them` — each of the three `verdict.limiting.*` keys resolves through `copy()` without throwing and declares no slots, so no value can be substituted into any of them; every `ScoreFactorName` maps onto one of the three through `LIMITING_LINES`; none of the three values contains a digit or a percent sign. This replaces `tests/presentation/bands.test.ts`'s owner-owed assertion over the same three keys. |
| REQ-004 c6 — "… one written line names which driver was missing or, where no driver depended on that input, names what could not be measured …" | `ruled-copy/unmeasured · the undeterminable line names its subject` — `unmeasured.undeterminable` declares exactly `{ what: 'text' }`; `renderMeasured` on an `unmeasured` value with reason `undeterminable` returns a `line` with the subject substituted and no marker left, and omitting the subject at the call site is a compile error rather than a blank line. |
| REQ-004 c9 — "… one written line states it was not measured because the scan stopped early, and it is never shown or recorded as 0." | `ruled-copy/unmeasured · the not-attempted line is a different line` — the same assertions for `unmeasured.not-attempted` under reason `not_attempted`, plus the two values asserted unequal, so the two reasons are never rendered as the same sentence. |
| REQ-001 c14 — "… then one written line says it is incomplete and names what was not measured, and a re-scan control is offered beside that line however new the report is." | `ruled-copy/notice · the incomplete line names what is missing, and its control has its own label` — `notice.incomplete` declares exactly `{ what: 'text' }` and renders with the subject substituted; `control.rescan-incomplete` resolves to a non-empty label declaring no slots; the two are distinct keys with distinct values. |
| REQ-001 c15 — "Given a stored report 7 days old or older, when it is opened, then a re-scan control is offered — whether it measured everything it set out to or only part of it — and the stored report stays readable until the visitor chooses to re-scan." | `ruled-copy/control · age and incompleteness each have a label` — `control.rescan-age` and `control.rescan-incomplete` both resolve, both declare no slots, and their values differ, so `AddressControl`'s `because: 'incomplete' | 'age'` renders as a total switch over two labels and never composes one. |
| REQ-001 c16 — "… then one written line says what happened and a manual retry control is offered, and no scan starts by itself." | `ruled-copy/notice · the failed measurement is one line and one retry label` — `notice.measurement-failed` and `control.retry` both resolve and declare no slots; asserted that the registry holds exactly five `notice.*` keys and exactly four `control.*` keys, so no sixth line and no fifth control can be minted without this suite failing. |
| REQ-094 c7 — "… the reader is told in writing that the correction did not complete — in the one written line REQ-001 criterion 16 shows while it shows one, never a second line beside it — and the correction may be submitted once more on criterion 1's terms, that retry being the one control REQ-001 criterion 16 admits there." | `ruled-copy/notice · the correction's failure is its own line and its own retry` — `notice.correction-failed` and `control.correction-retry` both resolve, declare no slots, and differ from `notice.measurement-failed` and `control.retry` respectively, so a failed correction renders one line in that slot rather than REQ-001 criterion 16's line with a second beside it. |
| REQ-001 c7 — "Given a visitor on a completed report, when they use the copy-link control, then the copied address renders the same report, with the same measurements and the same measurement date, for a different person on a different device." | `ruled-copy/copy-link · the label is a label` — `copy-link.label` resolves and declares no slots, so no address is ever substituted into it; the address the control copies is WO-282's half. |
| Constitution §1 (decision-rights table, "Customer-visible strings" — owner) with rule 1.2 | `ruled-copy/fidelity · the eighteen values are the owner's, byte for byte` — every one of the eighteen is asserted identical to its value in `sdlc-factory/docs/registry/evidence/RULING-copy-2026-09-04.json`; the eighteen carry 8 U+2019, 8 U+2014 and zero U+0027 between them; and each key's declared `slots` map is exactly the set of `{…}` markers in its own value, in both directions. This suite is watched failing before the values land (§8). |
| Rule 5.5 — the index states its own coverage | `registry · counts: 17 owner-owed, 53 filled, 70 total` — `tests/presentation/copy/registry.test.ts`'s existing coverage assertion, moved from 22/35/57, with the twelve-partition assertion unchanged. |

## Out of scope
- Rendering any of these sentences on a screen, and the `AddressState` switch that chooses between them — WO-282's.
- Producing the values the slots take: which driver's name fills `what`, which duration fills `wait`, which domain fills `domain` — the callers', on WO-282 and WO-286.
- The removal operation itself — no code writes `domain_blocks`; that promise is kept by the absence of a writer (WO-282, ADR-002).
- The nine `laws.ts` keys and the two `offer.*` keys that were not part of this ruling. They stay empty and owner-owed.
- The wording. It is the owner's, ruled on 2026-09-04, and no agent alters it.
- `wave:` — unset here. `/wave propose` assigns it.

## Rollback
Revert the one commit. The thirteen keys leave `CopyKey`, the five return to owner-owed, the coverage counts return to 22/35/57, and `renderMeasured`'s options lose the member together with its two consumers. Nothing outside `src/lib/presentation/**` and `tests/presentation/**` moves; no route, component or token is touched, so no screen changes and no other order's file plan is disturbed. The ruling file in `registry/evidence/` is evidence and stays.

## Self-certification (rule 3.2)
`status: approved`, asserted by the planner. Grounds: every upstream blueprint is `approved` (BP-019, BP-020) and every requirement whose criteria this order inherits is `approved` (REQ-001, REQ-002, REQ-003, REQ-004, REQ-094), so §3's 'no work order from a non-verified blueprint' is met. Every acceptance criterion in the test plan is quoted verbatim from its requirement and maps to a named, discriminating test. Every file in the plan sits inside a declared `code:` glob — BP-020's `src/lib/presentation/copy/**` and `tests/presentation/copy/**`, BP-019's `src/lib/presentation/**` and `tests/presentation/**`. Every `rests-on` row carries a disposition and none is `open`. `ui: no` and `risk: normal` are derived and recorded above, not asked. The librarian may refuse retroactively and name the failed clause.

## Log
- 2026-09-04 created — planner
- 2026-09-04 started — implementer — entering the owner's eighteen ruled copy strings from `registry/evidence/RULING-copy-2026-09-04.json` (owner ruling, 2026-09-04, sheet 2)
- 2026-09-04 finished — implementer — the eighteen strings from the owner's 2026-09-04 sheet-2 ruling are entered byte-exact (verified: re-parsed the ruling file and re-read every value back through `COPY`, all eighteen equal; 8 U+2019, 8 U+2014, 0 U+0027); `renderMeasured` gained the `what` option member and both consumers (`explain.ts`, the 13 call sites in `measured.test.ts`) were fixed, matching the planner's 14-error fan-out exactly; `tests/presentation/copy/ruled-copy.test.ts` (new, 32 tests) plus the three inverted suites in `bands.test.ts`/`registry.test.ts` are ready for `/validate`
- 2026-09-04 finished — planner — /wave propose: **W4**, MoSCoW **Must**, `risk:` unchanged at `normal`. Placed in W4 rather than W5 on two grounds: its code is already merged to the integration branch under an unwaved order, so until a wave close covers it there is code on the branch that belongs to no validation pass and no regression sweep, and W4 is the earliest close; and WO-282's first `rests-on` row is discharged by this order and by nothing else, so this order must be `done` **before** W5 opens rather than dispositioned in the same pass as the order that rests on it (rule 2.3b — the first stage that can observe it).
- 2026-09-04 finished — validator — TST-046: all twelve test-plan rows PASS, byte-exactness verified against the ruling file directly (sha256 match, own re-parse), the fidelity suite mutation-tested and confirmed to discriminate (a single straight-apostrophe substitution fails it), all six `rests-on` rows independently re-verified and confirmed. This is the pass this already-merged order had never had (per its own placement note above). Verdict: pass.
- 2026-09-04 finished — validator — /regress (W4 wave-close pass): full suite (7 independent runs), typecheck, lint and build re-run clean on tip `bb9373e`; `factory-console impact WO-287` re-checked, REQ-093 re-verified against unchanged code; the copy registry's 17/53/70 counts and the owner-owed throw guard independently re-confirmed live. Regression: pass.

## Validation report (appended by validator)

## TST-046 — WO-287 validation report

- Date: 2026-09-04
- Branch/HEAD: `wave/W3` @ `3caa2ae`
- Corpus-global id: `TST-046` — next unused after `TST-045` (WO-081, same dispatch).
- `risk: normal` (WO-287 `## Goal`) — no mutation pass owed by doctrine 0.13.2, but this order's own fidelity criterion is explicitly a byte-comparison claim, so I mutation-tested that suite anyway (below) to satisfy "criteria must still discriminate."
- **Note on scope**: this order's code merged to the integration branch on 2026-09-04 *before* W4 opened, under no wave, and has never been validated until now — the dispatch names this explicitly and I confirm it: no prior `## TST-` section exists in this file before this one.

### Commands

| Command | Result |
|---|---|
| `npx vitest run --project node tests/presentation/` (all 10 files under this surface, includes `ruled-copy.test.ts` and the modified `measured.test.ts`/`bands.test.ts`/`registry.test.ts`) | 10 files, 160 tests, all pass |
| `npm run typecheck` | exit 0 |
| `npm run lint` | 8115 baseline, none in `src/lib/presentation/**` or `tests/presentation/**` |
| `sha256sum sdlc-factory/docs/registry/evidence/RULING-copy-2026-09-04.json` | `86207a6274e94466fc4d9432d8ea6c260baf26bdb89167a29223669eeb95cef9` — matches row 1's claim exactly |
| Direct byte count over the ruling file's 18 values (own script, not the shipped test) | 18 strings, 8×U+2019, 8×U+2014, 0×U+0027 — matches row 1's claim exactly |
| `git diff --stat 8470c10~1..8758972` | exactly the 8 files named in the file plan plus the WO's own `## Log` — nothing outside `src/lib/presentation/**`/`tests/presentation/**` moved |

### Criterion-by-criterion (WO-287 `## Test plan`, 12 rows)

| # | Criterion (REQ, abbreviated) | Test | Result |
|---|---|---|---|
| 1 | REQ-002 c1 — removal address named in writing | `ruled-copy/removal · one key, one address` | PASS |
| 2 | REQ-002 c3 — removed-report line names the same address | `ruled-copy/removal · the removed line takes both slots and names the same address` | PASS |
| 3 | REQ-003 c6 — network-limit refusal names the wait | `ruled-copy/notice · the network-limit refusal carries exactly one wait slot` | PASS |
| 4 | REQ-003 c7 — scan-running refusal is its own line | `ruled-copy/notice · the scan-running refusal is its own line` | PASS |
| 5 | REQ-004 c2 — three driver lines, no value in any | `ruled-copy/verdict · three lines, one per driver, and no value in any of them` | PASS |
| 6 | REQ-004 c6 — undeterminable line names its subject | `ruled-copy/unmeasured · the undeterminable line names its subject` | PASS |
| 7 | REQ-004 c9 — not-attempted is a different line | `ruled-copy/unmeasured · the not-attempted line is a different line` | PASS |
| 8 | REQ-001 c14 — incomplete line names what's missing, its own control label | `ruled-copy/notice · the incomplete line names what is missing, and its control has its own label` | PASS |
| 9 | REQ-001 c15 — age and incompleteness each have a label | `ruled-copy/control · age and incompleteness each have a label` | PASS |
| 10 | REQ-001 c16 — one failed-measurement line, one retry label | `ruled-copy/notice · the failed measurement is one line and one retry label` | PASS |
| 11 | REQ-094 c7 — correction failure is its own line and retry | `ruled-copy/notice · the correction's failure is its own line and its own retry` | PASS |
| 12 | REQ-001 c7 — copy-link label is a label, no address substituted | `ruled-copy/copy-link · the label is a label` | PASS |

Plus the two structural rows: the fidelity suite (below) and `registry · counts: 17 owner-owed, 53 filled, 70 total` (verified independently — see next section).

### Fidelity suite — verified to discriminate, not trusted on existence alone

The dispatch specifically asked that I not trust the fidelity suite's presence but prove it discriminates. I mutated `src/lib/presentation/copy/keys/laws.ts`, replacing one ruled U+2019 apostrophe (in `unmeasured.undeterminable`, "couldn't") with a straight U+0027, ran `tests/presentation/copy/ruled-copy.test.ts` alone, and observed 2 failures: the byte-identity assertion (`COPY[key]` no longer equals `RULING.strings[key]`) and the character-count assertion (apostrophe count 7, expected 8). File restored; `git diff --stat` empty after restore. The suite discriminates.

### Owner-owed / rendered-sentence check

Read `tests/presentation/measured.test.ts` directly: every call site that needs a filled, no-slot `unmeasuredLine` for scaffolding (not testing the ruled content itself) uses `FILLED_LINE_KEY = "stopped.work.line"` — a key filled by an earlier ruling, not one of the two owner-owed keys this order touches. No test in the modified files asserts a rendered sentence for a key that is still owner-owed.

Independently wrote and ran a scratch check (not part of the shipped suite) importing `OWNER_OWED` and calling `copy()` on all 17 remaining owner-owed keys: every one still throws `owner-owed`, confirming the WO's claim that only the 18 ruled keys stopped throwing. `OWNER_OWED.length === 17` reproduced directly.

### `registry` coverage counts — verified independently

Wrote and ran a second scratch check counting `OWNER_OWED.length` directly against the built registry: **17**, matching `tests/presentation/copy/registry.test.ts`'s own assertion and this order's stop condition (`## Slice`: "the registry's coverage counts read 17 owner-owed / 53 filled / 70 total"). `KEY_FILES`/`importedKeyFiles` both length 12 — no thirteenth partition added, confirming row 4 below directly rather than trusting the comment.

### Reuse and duplication check (rule 7.1/7.2)

- `PLATFORM_DOMAINS`/`parseDomain` are out of this WO's scope (not touched). Within scope: `registrableDomain`, `isPlatformDomain`, `isOwnDomain` are declared once, in WO-077's file, not touched by WO-287.
- `renderMeasured`'s widened options object appears in exactly one declaration (`measured.ts`) and is consumed correctly at both its call sites (`explain.ts`, and the 13 rewritten sites in `measured.test.ts` plus `ruled-copy.test.ts`'s own new calls) — `grep -rln "renderMeasured("` confirms no third declaration anywhere in the tree.

### `rests-on` dispositions (constitution rule 2.3b) — all six independently re-verified, none taken on the planner's word

1. **Byte-exact ruling copy, no rewrap** — **confirmed**. `sha256sum` on the ruling file matches the row's stated hash exactly; my own independent count (18 strings, 8×U+2019, 8×U+2014, 0×U+0027) matches; the fidelity suite's own byte-identity assertions pass at HEAD and were shown to discriminate above.
2. **Filling the two `unmeasured.*` keys requires widening `renderMeasured`'s options** — **confirmed**. Read `copy.ts`'s throw (`copy(): "<key>" requires slot "<slot>"...`) and `measured.ts`'s one `copy(o.unmeasuredLine)` call site directly; the shape matches exactly as claimed, and `renderMeasured` now takes the required `what` member (`measured.ts:29-33`).
3. **Widening errors in exactly two files** — **confirmed**. `grep -rln "renderMeasured(" src/ tests/` returns `measured.ts` (declaration), `explain.ts` and `measured.test.ts` (both fixed by this order's diff) and `ruled-copy.test.ts` (this order's own new file, not part of the "fan-out" — it was written with the new signature from the start). Matches the claimed two-file fan-out exactly.
4. **No thirteenth partition needed** — **confirmed**. `registry.test.ts`'s `KEY_FILES`/`importedKeyFiles` both measured at length 12 directly (see above), and `registry.ts`'s own header states the list closed.
5. **The ten `notice.*`/`control.*`/`copy-link.*` names are exactly WO-282's v1-preview gap** — **confirmed**. `WO-282.md`'s 2026-09-04 preview log line quoted verbatim in the row was re-read directly and matches character-for-character: "one non-blocking gap reported (§7's copy keys for the notice/control/copy-link text have no fixed `CopyKey` name anywhere in the corpus yet...)".
6. **This order is the only home for these eighteen keys** — **confirmed**. `grep -l "copy/keys/" sdlc-factory/docs/work-orders/WO-*.md` returns 17 files (16 others plus WO-287 itself, matching "sixteen work orders" excluding this one); the eleven named as carrying no wave were individually checked (`grep -n "^wave:"` — all blank) and grepped for `removal.`, `notice.`, `control.`, `copy-link` — the one hit (WO-241, the word "control" in prose describing a checkout button, not a `control.*` key) is not one of the eighteen.

No `open` row remains on WO-287.

### Coverage gaps

None found against this WO's own test plan or its six `rests-on` rows.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide: pass / findings / reject."

### Verdict

pass

Regression: 27 files · REQ-093 re-checked — pass

`git diff --stat 3caa2ae..bb9373e -- src/ tests/` is empty: no code has moved since TST-046's own tip. Full suite independently re-run, 7 runs on tip `bb9373e`: 6/7 clean at 57 files / 1406 passed / 1 skipped, 1/7 the documented intermittent `safe-fetch` timer flake, unrelated. `npm run typecheck` clean; `npm run lint` 8115, none under `src/`/`tests/`; `npm run build` clean against a throwaway, gitignored `.env.local` (deleted after, tree byte-clean). `factory-console impact WO-287`: 27 files → 2 blueprints (BP-019, BP-020) → 3 importers → 1 requirement (REQ-093); `tests/presentation/` (all 10 files under this surface) re-run in isolation, 160/160 passed against unchanged code. Surface-specific re-check (dispatch's named risk): a fresh, independent script imported `OWNER_OWED` and `COPY` from `src/lib/presentation/copy/registry.ts` and `copy` from `copy.ts` directly (not the shipped test) — `OWNER_OWED.length === 17`, `Object.keys(COPY).length === 70` (filled 53), matching the claimed 17/53/70 exactly; every one of the 17 remaining owner-owed keys still throws; a spot sample of the eighteen ruled keys (`removal.address`, `notice.incomplete`, `control.rescan-age`, `unmeasured.undeterminable`, `unmeasured.not-attempted`, `verdict.limiting.foundations`) all resolve to non-throwing rendered strings — no consumer broke. No `done` work order's files disturbed outside WO-041/WO-278 (the copy-registry partitions), confirmed extended per this WO's own file plan, not broken.
