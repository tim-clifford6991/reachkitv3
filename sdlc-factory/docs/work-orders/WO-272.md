---
id: WO-272
type: work-order
title: "users.notify and sites.timezone — the two component-level columns of BP-017's delta that no leaf sub-token claims, migrated and asserted"
status: approved
ui: no
implements: [BP-017]
depends-on: [WO-267]
blocked-by: []
rests-on:
  - claim: "WO-267's `topicOf()` resolves a `users`- or `sites`-topic migration whose remaining name segments match no registered sub-token to the topic's owner, BP-017, rather than refusing it. WO-267's naming test states the rule as 'exactly one assigned topic token or one sub-token of an assigned topic; a name carrying none, or two, fails', and this order's two file names each carry one topic token and no sub-token. Derivation (rule 1.1): structure.md rule 3a says a component-level column needs no sub-token ('the sub-token list is open and grows with the leaves'), so no new token is minted here. Reversal cost: a file rename before any data exists, or two rows in rule 3a's list — either is cheap; observable by this order's validator against WO-267's applied test."
    disposition: confirmed
  - claim: "`sites.timezone` is nullable because the `sites` row exists from provisioning (WO-126) and the zone arrives only at first sign-in (REQ-073 criterion 1: 'the time zone is the one their browser reported at first sign-in'), and REQ-073 forbids a zone the customer did not state; in the interval between, nothing that reads the column runs — WO-211's ceiling check and WO-218's settings read both follow sign-in and setup. WO-211 and WO-218 rest on the column being 'present and non-null for every site'; this order narrows that to every site past first sign-in, and reports that no order in the corpus yet writes the browser's zone at first sign-in. Reversal cost: `set not null` after a backfill — cheap before customers exist."
    disposition: confirmed
supersedes: []
---

# WO-272 — users.notify and sites.timezone — the two component-level columns of BP-017's delta that no leaf sub-token claims, migrated and asserted

- Branch: wo/WO-272-account-component-columns
- Estimate: ≤1 day

## Goal
Add the two columns BP-017's `## Data model delta` names as its own and no leaf
migrates — `users.notify` and `sites.timezone` — under BP-017's topic globs,
each with one owner, so the orders that read them (WO-107, WO-211, WO-218) have
a column to read instead of a claim to rest on.

## Provenance of this file plan
**This repository holds no `supabase/` and no `tests/account/` on this branch**;
WO-267 is building the substrate in a sibling worktree. All three paths are NEW.
`supabase/migrations/*_users*.sql` and `*_sites*.sql` are BP-017's `code:` globs
and structure.md rule 3 makes `users` and `sites` BP-017's topics; every leaf
sub-token under them (`users_billing`, `users_provisioning`, `users_subscription`,
`users_identity`, `users_erasure`, `sites_provisioning`, `sites_hosting`,
`sites_erasure`) is claimed by a leaf order, and none of those claims either
column. `tests/account/**` is BP-017's; every leaf owns a sub-directory beneath
it, so a file at the root is the component's under structure.md rule 2.

BP-002's data-model index names the owner in so many words —
"| `users.notify jsonb` | REQ-075 | `users` · BP-017 (leaf: BP-059) |" and
"| `sites.timezone` | REQ-073 c3 | `sites` · BP-017 (leaf: BP-057) |" — and says
of that index "it is not a second declaration of the delta … where an entry and
its owning node disagree, the owning node is right". WO-267's baseline row lists
neither column. BP-059 decision 4 and BP-057's `## Data model delta` say the
baseline holds them; that is a reading of BP-002 that BP-002's own text refutes,
reported to the architect in this order's return and not relitigated here.

Of BP-017's `depends-on` — BP-002, BP-005, BP-006, BP-016 — this slice consumes
BP-002 alone (WO-267's `users` and `sites`); a `depends-on` edge to an order this
SQL never touches would be a fabricated edge, so none is written.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `supabase/migrations/<ts>_users_notify_column.sql` | NEW | `alter table users add column notify jsonb not null default '{}'::jsonb`, with `check (jsonb_typeof(notify) = 'object')`. The column only: its shape and meaning are BP-059's ("this node holds its shape and its meaning"). No foreign key, no cascade (ADR-051 point 2). |
| `supabase/migrations/<ts>_sites_timezone_column.sql` | NEW | `alter table sites add column timezone text null`. No default — REQ-073 criterion 1 forbids a zone the customer did not state; nullability derived in `rests-on` row 2. |
| `tests/account/columns.test.ts` | NEW | The schema assertions named in the test plan, against the applied migrations; no I/O beyond the scratch database. |

## Interfaces
Exposes (verbatim from BP-017 `## Data model delta`):

> `users` — as `BUILD.md` §10, plus `notify jsonb`, `pending_email`,
> `pending_email_token_hash`, `pending_email_sent_at`, `deleted_at`,
> `paid_through`, `vat_number`, `billing_country`.
> `sites` — as §10, plus `timezone`, `publishing_enabled`.

Of that list, this order ships exactly `notify jsonb` and `timezone`; every other
column is a leaf's under its sub-token (WO-116, WO-129, WO-135, WO-140).

Consumes: WO-267's baseline `users` and `sites` tables (BP-002).

## Steps
1. Write the two migrations, one topic token each, no sub-token.
2. Apply locally on top of WO-267's baseline and RLS files.
3. Write the test; watch the default row fail first by omitting `default '{}'`,
   and the object-check row fail first by omitting the `check`.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-017 carries
> `satisfies: []` and was cut from `BUILD.md` §13 and §4.7, not from a
> requirement. Acceptance is quoted from BP-017, BP-002, BP-059, REQ-073 and
> `structure.md`, each row naming its source.

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-017 `## Data model delta`: "`users` — as `BUILD.md` §10, plus `notify jsonb`, …" | `tests/account/columns.test.ts` › `users.notify exists as jsonb, not null, default {}` — `information_schema` row asserted on type, nullability and default; an insert omitting the column reads `{}`. Dropping the default fails the case. |
| BP-017 `## Data model delta`: "`sites` — as §10, plus `timezone`, `publishing_enabled`." | Same file › `sites.timezone exists as text and is nullable with no default` — an insert omitting it succeeds and reads `null`; `column_default is null`. Adding a default fails the case. |
| BP-059 `## Data model delta`: "The value is a sparse object over `NotifyKind`, default `{}`." | Same file › `a non-object notify value is refused` — an update to `[]`, `"x"` and `null` each raises; `{}` and `{"weekly": false}` are accepted. Removing the check fails the case. |
| BP-002 `## Data model delta`: "\| `users.notify jsonb` \| REQ-075 \| `users` · BP-017 (leaf: BP-059) \|" and "\| `sites.timezone` \| REQ-073 c3 \| `sites` · BP-017 (leaf: BP-057) \|" | Same file › `both migration names resolve to BP-017 and to no other node` — WO-267's `topicOf()` returns BP-017 for each file name, and each name passes `tests/db/migration-naming.test.ts`'s rule. This is the row `rests-on` row 1 is discharged by. |
| `structure.md` rule 3a: "A column added to `users`, `sites` or any other baseline table after the baseline is a migration under that table's token or a sub-token of it — never 'part of the baseline', which is one file that runs once." | Same file › `the baseline file is unmodified` — `00000000000001_baseline.sql`'s text contains neither `notify` nor `timezone`; the two columns arrive in the two files above and nowhere else. |
| REQ-073 criterion 1 (the clause BP-002's index cites as forcing the column): "where they have set nothing, the veto window is 24 hours and the time zone is the one their browser reported at first sign-in" | Same file › `no fallback zone is written by the schema` — a freshly provisioned `sites` row carries `timezone = null`, never `'UTC'` or the server's zone. |

## Out of scope
- Writing `sites.timezone` from the browser's report at first sign-in — no
  order in the corpus does it today (reported in this order's return; it is
  BP-061's or BP-033's, not BP-017's component seam).
- Reading or writing `users.notify` — WO-107 (BP-059).
- Reading `sites.timezone` — WO-211 (BP-045), WO-218 and WO-219 (BP-057).
- `sites.publishing_enabled` and every other `users`/`sites` column — the leaf
  sub-token orders WO-116, WO-129, WO-135, WO-140.
- Regenerating `src/lib/db/types.generated.ts` — BP-002's generated artefact;
  no sibling migration order claims it and this one follows that practice.

## Rollback
Delete the two migration files before apply, or `drop column` each on an
applied database; no order writes either column until WO-107 and WO-218 land,
and neither is on a read path WO-267's own tests exercise.

## Self-certification
`status: approved` — set 2026-09-03 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-017 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 6 rows of `## Test plan` quote BP-017, BP-002, BP-059, REQ-073 and `structure.md` as they stand on disk — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **Merge test (rule 2.6).** WO-107, WO-211 and WO-218 each state their goal and stop condition without citing this order — they rest on the column existing, as they rest on WO-267's tables existing; this order states its goal (two columns, one owner each, asserted) and its stop (the schema test) without citing any of them. Three orders, not one.
- **Reuse (rule 7.2).** `grep` of every `supabase/migrations/` row across `work-orders/` finds no file adding either column; WO-267's baseline row lists neither.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-09-03 created — planner
- 2026-09-03 started — implementer
- 2026-09-03 finished — implementer — `00000000000003_users_notify_column.sql` and `00000000000004_sites_timezone_column.sql` written; `topicOf()` resolves both to `{ token: 'users'|'sites', owner: 'BP-017' }` (no registered sub-token matches either name's remaining words), discharging `rests-on` row 1. `tests/account/columns.test.ts` (10 tests, all quoting the test-plan rows) applies baseline + the two new migrations via `psql` against `reachkit_scratch`, following `tests/db/baseline.test.ts`'s reset pattern; RLS not applied (out of scope for this delta). Watch-it-fail-first, both restored after (`git status --porcelain` clean of probes): (1) dropped `default '{}'` from the `notify` migration — observed 6/10 tests failed, including the direct "not null, default {}" assertion and every insert helper (`null value in column "notify" of relation "users" violates not-null constraint`); restored, 10/10 pass. (2) dropped the `check (jsonb_typeof(notify) = 'object')` constraint — observed 1/10 failed ("a non-object notify value is refused" — `[]`/`"x"`/`null` all inserted with no error); restored, 10/10 pass. Full suite `npx vitest run --no-file-parallelism`: 278/280 pass; the 2 failures are the pre-existing WO-270 `.claude` toolchain failure (untouched, as instructed) and `tests/db/migration-naming.test.ts` › "finds the two migrations this work order writes", which hardcodes the exact two-file WO-267 snapshot and necessarily fails once any order adds a migration — every other assertion in that file, including the per-file `topicOf()` check over all 4 files, passes; not fixed, per the WO's instruction not to edit `tests/db/**`, reported below instead. `npm run typecheck`: clean. `npx eslint tests/account/columns.test.ts` (the SQL files carry no matching ESLint config, expected): clean, 0 errors. Finding carried per the WO's provenance section: BP-059 decision 4 and BP-057's `## Data model delta` both say the baseline holds `users.notify`/`sites.timezone`, which BP-002's own text refutes ("where an entry and its owning node disagree, the owning node is right") — for the architect to settle, not relitigated here.
- 2026-09-03 failed — validator — TST-008: 6/6 test-plan rows PASS and independently discriminate under fresh mutation (own probes, restored) — WO-272's own delivered columns and test are correct. One confirmed defect blocks a clean merge: `tests/db/migration-naming.test.ts` › "finds the two migrations this work order writes" is newly red under the documented full-suite command as a direct, deterministic consequence of this WO's two new files landing in `supabase/migrations/` — a hardcoded two-file enumeration in a file WO-272 does not own and correctly did not touch (BP-002/WO-267's, per rule 7.1) — next: planner routes a fix (amend this WO's file plan to add `tests/db/migration-naming.test.ts` as a MODIFY replacing the exact-enumeration assertion with one that doesn't over-fit to a single snapshot, or spin a fast-follow WO under BP-002), then this WO returns for re-validation.
- 2026-09-03 brought current — librarian — merged doctrine-0.11.0 (WO-274's migration-naming membership fix); suite now 304/304 passed (blocker resolved); ready for re-validation.
- 2026-09-03 finished — validator — TST-010: pass. Re-validation following WO-274's fix (merged and `done` on `doctrine-0.11.0`, brought current onto this branch at `e754782`/`eebd62f`). Full suite now 304/304 (0 failures; was 278/280 at TST-008). All 6 of WO-272's own criteria re-verified against the current tree with fresh mutation probes distinct from TST-008's; all discriminate. Migration-naming coexistence confirmed: `tests/db/migration-naming.test.ts` is 15/15 in isolation and inside the full run, its enumeration case now a `toContain` membership check (WO-274) that WO-272's two files no longer trip. Scope still exactly the 3 planned files (verified against WO-272's own non-merge commits, not a raw branch-to-root diff). Both `rests-on` rows remain `disposition: confirmed`, neither reverted to `open`.
- 2026-09-03 finished — validator — regression pass on TST-010: branch reconfirmed current with `doctrine-0.11.0`; substrate consumers (`baseline.test.ts`, `rls.test.ts`, `clients.test.ts`, `migration-naming.test.ts`) 87/87; RLS re-checked against the real database with a fresh probe applying all four migrations and exercising PostgREST directly — anon reads 0 rows, cross-user reads/writes affect 0 rows, own-row read/write succeeds, no widened access; migrations re-apply cleanly from scratch (`ON_ERROR_STOP=1`, all four in order on a dropped `public` schema); WO-107/WO-211/WO-218/WO-273 named as the columns' intended readers, all `approved` and unimplemented — no live reader today; full suite 304/304, typecheck clean. `Regression:` line recorded on TST-010.

## Validation report (appended by validator)

## TST-008 — WO-272 validation report

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-272`, branch `wo/WO-272-account-component-columns`, HEAD `a8ab3b0`
- Corpus-global id: `TST-008` — `grep -rh "^## TST-" sdlc-factory/docs/work-orders/*.md` (both this worktree and the `doctrine-0.11.0` root checkout) returns `TST-001`…`TST-005` only; `TST-008` is the next unused id.
- `ui: no` — no `Placement:` line follows (design-guardian's UI-fit pass does not run on this WO).
- Substrate: native Postgres 18 at `127.0.0.1:5432` (`reachkit_scratch`), PostgREST on `:3001`/`:3002`, postgres-meta on `:8090` — the substrate WO-267 built, already live; no `supabase start` attempted.

### Commands (fresh from HEAD)

| Command | Result |
|---|---|
| `npx vitest run tests/account/columns.test.ts --no-file-parallelism` | 10/10 pass |
| `npx vitest run --no-file-parallelism` (full suite) | 10 files, 280 tests: 278 pass, 2 fail (1 pre-existing WO-270 `.claude`; 1 new — see Defect below) |
| `npm run typecheck` | exit 0 |
| `npx eslint tests/account/columns.test.ts` | exit 0 |
| `git diff --name-only ad7bbfe a8ab3b0` | exactly the 3 planned files + `WO-272.md` |

### Criterion-by-criterion (WO-272 `## Test plan`, 6 rows) and mutation results

| # | Criterion (source, abbreviated) | Test | Result | Mutation probe (fresh, independent of the implementer's own) |
|---|---|---|---|---|
| 1 | BP-017 — `users.notify` jsonb, not null, default `{}` | `columns.test.ts` › "users.notify exists as jsonb, not null, default {}" + "an insert omitting the column reads {}" | PASS | Removed `default '{}'::jsonb` from the migration, re-applied fresh (not the implementer's own restore): 6/10 tests fail, including both rows above (`null value in column "notify" ... violates not-null constraint`). Restored; `git diff` empty; 10/10 re-pass. Discriminates. |
| 2 | BP-017 — `sites.timezone` text, nullable, no default | `columns.test.ts` › "sites.timezone exists as text and is nullable with no default" + "an insert omitting it succeeds and reads null" | PASS | Changed the column to `text not null default 'UTC'`, re-applied: 3/10 fail — the nullability assertion (`expected [['UTC']] to equal [['text','YES','']]`... shape), the insert-reads-null assertion, and the REQ-073 no-fallback-zone assertion (received `'UTC'`, expected `'<null>'`). Restored; `git diff` empty; 10/10 re-pass. Discriminates. |
| 3 | BP-059 — non-object `notify` value refused (`check` constraint) | `columns.test.ts` › "a non-object notify value is refused" | PASS | Removed the `check (jsonb_typeof(notify) = 'object')` constraint entirely, re-applied: exactly 1/10 fails — this row (`[]`, `"x"`, `null` all now insert with no error). Restored; `git diff` empty; 10/10 re-pass. Discriminates. |
| 4 | BP-002 data-model index — both migration names resolve to BP-017 via `topicOf()`, and pass `migration-naming.test.ts`'s per-file rule | `columns.test.ts` › "both migration names resolve to BP-017 and to no other node" | PASS | Read `src/lib/db/topics.ts` directly (unchanged by this WO's diff — confirmed via `git diff ad7bbfe a8ab3b0 -- src/lib/db/topics.ts`, empty): no `users_*`/`sites_*` sub-token in `MIGRATION_SUBTOKENS` matches either filename's remaining words, so both fall through to the parent topic, owner `BP-017`. Independently re-ran `tests/db/migration-naming.test.ts -t "carries exactly one assigned topic token"`: 4/4 pass, including both new files. See Defect below for a *different*, unrelated assertion in the same file that this WO's new files do trip. |
| 5 | `structure.md` rule 3a — the baseline file is never re-opened; the two columns arrive only in the two new migrations | `columns.test.ts` › "the baseline file is unmodified" + "the two columns arrive in the two migrations above and nowhere else" | PASS | Directly grepped `00000000000001_baseline.sql` for `notify`/`timezone`: no match, confirming the shipped file, independent of the test's own read. |
| 6 | REQ-073 c1 — no fallback zone written by the schema | `columns.test.ts` › "no fallback zone is written by the schema" | PASS | Same mutation as row 2 (`default 'UTC'`) fails this row too (`expected [['<null>']] to equal [['UTC']]`... received `'UTC'`, and the explicit `.not.toBe("UTC")` assertion fails). Discriminates. |

**All 6 rows PASS and discriminate** under fresh, independent mutation (different edits than the implementer's own shipped probes, all restored — `git status --porcelain` empty after every probe, confirmed before and after each).

### Defect — `tests/db/migration-naming.test.ts` newly red under the documented full-suite command

**This is a defect of the record/full-suite-health kind, not of WO-272's own delivered code** — say so first, per this report's own instructions: WO-272's two migrations and its own test file (`columns.test.ts`) are entirely correct (all 6 rows above PASS and discriminate). The defect is in a file WO-272 does not own, did not plan to touch, and correctly did not touch.

`tests/db/migration-naming.test.ts` › "finds the two migrations this work order writes" (line 75) hardcodes:
```ts
expect(files.sort()).toEqual([
  "00000000000001_baseline.sql",
  "00000000000002_rls.sql",
]);
```
This enumerates **every** file in `supabase/migrations/`, not just `users`/`sites`-topic ones. WO-272's two new files (`00000000000003_users_notify_column.sql`, `00000000000004_sites_timezone_column.sql`) are correctly placed under BP-017's own topic tokens, but their mere presence in the shared directory makes this specific assertion fail — independently reproduced fresh (not the implementer's own report taken on faith): ran `npx vitest run tests/db/migration-naming.test.ts --no-file-parallelism` on HEAD `a8ab3b0`, result `1 failed | 14 passed (15)`, the one failure being exactly this row. Every other assertion in the same file — including the per-file `it.each(files)("%s carries exactly one assigned topic token")` rule, which is what `rests-on` row 1 actually rests on — passes for all 4 files (re-run in isolation: `4 passed | 11 skipped`).

This assertion is structurally guaranteed to fail for the *next* migration any WO adds, regardless of topic — a brittleness inherent to `tests/db/migration-naming.test.ts` as WO-267 shipped it (a snapshot of the directory at the moment WO-267 branched, asserted as exact and permanent), not something WO-272 introduced by miswriting its own files. `tests/db/**` is BP-002's (rule 7.1, single owner per capability), and WO-272's approved file plan lists 3 files, all NEW, 0 MODIFY — none under `tests/db/**`; editing it here would itself be a scope violation of this WO's own self-certified bound (rule 2.6).

**Why this still blocks a clean verdict:** the *documented* full-suite command (`npx vitest run --no-file-parallelism`) is red for a reason directly, deterministically caused by this WO's own files landing — this is not the same shape as WO-267's own Deviation 2 (an *undocumented* command flaking on a pre-existing property unrelated to that WO's new files); here the gating command itself newly fails. `git diff --name-only ad7bbfe a8ab3b0` — the merge-base immediately before this WO — confirms `tests/db/migration-naming.test.ts` is untouched by this diff and the full suite was green (net of the one pre-existing WO-270 failure) at that commit.

**Fix path (not this WO's implementer's to take):** the planner amends this WO's file plan (or opens a fast-follow WO under BP-002/WO-267's ownership) to add `tests/db/migration-naming.test.ts` as a MODIFY, replacing the exact-enumeration assertion with one that does not over-fit to a single snapshot in time — e.g., asserting the two known baseline files are present as a subset, or dropping the enumeration in favor of the per-file topic-token rule the rest of the file already carries.

### `rests-on` dispositions (constitution rule 2.3b)

**Row 1** — *`topicOf()` resolves a `users`/`sites`-topic migration with no matching sub-token to BP-017, rather than refusing it* — **confirmed**. Directly observed above (criterion 4): both new filenames resolve to `{ token, owner: 'BP-017' }`, and `migration-naming.test.ts`'s own per-file rule (the rule this claim is actually about) passes for both, independently re-run in isolation. The one failing assertion in that file is a different, unrelated hardcoded enumeration (see Defect) and does not bear on whether `topicOf()` refuses these names — it does not.

**Row 2** — *`sites.timezone` is nullable because nothing reads it before first sign-in, and no order in the corpus yet writes the browser's zone* — **confirmed**. The shipped shape (`text null`, no default) is directly verified against the applied schema (criteria 2 and 6). The report that no order yet writes the zone also holds as of this build: WO-273 (`status: approved`, "The first-sign-in time zone post") is the order that would write it, but is unimplemented — its own `## Provenance` states "This repository holds no `src/`", and `src/app/(account)/adopt-timezone.tsx` does not exist on this branch or on `doctrine-0.11.0`. WO-273's existence corroborates rather than refutes the claim: its own file plan states `adoptBrowserTimezone` "sets `sites.timezone` only while it is null," which is exactly the nullable-until-written shape this migration ships, and it lists "the `sites.timezone` column — WO-272 (BP-017)" as out of its own scope, i.e., WO-273 is written to consume this shape, not to have already closed the gap.

**BP-017's own two open `rests-on` rows** (Stripe customer-portal VAT correction; Stripe Checkout billing-address capture) are unrelated to this build — `users.notify`/`sites.timezone` touch neither claim — and are correctly left `open`, not dispositioned here.

### Scope

`git diff --name-only ad7bbfe a8ab3b0` returns exactly `sdlc-factory/docs/work-orders/WO-272.md`, `supabase/migrations/00000000000003_users_notify_column.sql`, `supabase/migrations/00000000000004_sites_timezone_column.sql`, `tests/account/columns.test.ts` — the 3 planned files plus this WO's own record. No stray files; `capabilities.md` line 97 already names this exact capability under BP-017 with no other entry claiming either column (`grep -rn "add column notify\|add column timezone"` across `supabase/migrations/` and `work-orders/` returns only this WO's own rows).

### Coverage gaps

- The full-suite defect above — confirmed real, routed to the planner, not a defect of WO-272's own delivered files.
- RLS is not exercised against these two columns by this WO (by design — `00000000000002_rls.sql` is not applied in `columns.test.ts`'s setup, and BP-017's delta is schema-shape, not policy). No `rests-on` row or acceptance criterion claims otherwise.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

findings

WO-272's own delivered work — two migrations and `tests/account/columns.test.ts` — is correct: all 6 test-plan rows PASS and independently discriminate under fresh mutation (different edits than the implementer's own, all restored, `git status --porcelain` clean throughout), scope is exactly the 3 planned files, typecheck and lint are clean, and both `rests-on` rows are confirmed. One defect blocks a clean merge, and it is a record/full-suite-health defect, not a defect of this WO's own code (say so first, per this report's own convention): `tests/db/migration-naming.test.ts`'s hardcoded two-file enumeration — a leftover WO-267 self-check outside WO-272's file plan and outside BP-017's ownership — is newly red under the documented full-suite command as a direct, deterministic consequence of this WO's files landing in the shared `supabase/migrations/` directory. **Next:** the planner, not this WO's implementer, routes the fix — amend the file plan (or a fast-follow WO under BP-002) to replace the brittle assertion in `tests/db/migration-naming.test.ts` with one that survives future migrations — after which this WO returns for re-validation.

## TST-010 — WO-272 re-validation report (Deviation resolved by WO-274)

- Date: 2026-09-03
- Worktree: `/root/projects/reachkitv3-wt/WO-272`, branch `wo/WO-272-account-component-columns`, HEAD `eebd62f`
- Corpus-global id: `TST-010` — `grep -rh "^## TST-" sdlc-factory/docs/work-orders/*.md` across the `doctrine-0.11.0` root checkout **and every live `wo/*` worktree** (`WO-001`, `WO-005`, `WO-006`, `WO-267`, `WO-269`, `WO-270`, `WO-272`, `WO-274`) returns a combined max of `TST-009` (this branch's own TST-008 and WO-274's TST-009 are the two highest); `TST-010` is the next unused id, corpus-wide.
- `ui: no` — no `Placement:` line follows (design-guardian's UI-fit pass does not run on this WO).
- Substrate: native Postgres 18 at `127.0.0.1:5432` (`reachkit_scratch`), PostgREST on `:3001`/`:3002`, postgres-meta on `:8090` — already live; no `supabase start` attempted; no `npm install` needed (`node_modules` already present and current).
- Scope of this pass: does TST-008's single finding (the migration-naming enumeration false-positive) actually clear now that WO-274 is merged and `done` on `doctrine-0.11.0`, and does WO-272's own delivered work still hold on the current tree? Full re-verification of both, not a carry-forward.

### 1. The finding, confirmed cleared

`npx vitest run --no-file-parallelism` on this branch (HEAD `eebd62f`, current with `doctrine-0.11.0` through WO-267/WO-269/WO-270/WO-274, all `done`):

```
Test Files  15 passed (15)
     Tests  304 passed (304)
```

At TST-008 the same documented command reported **10 files, 280 tests: 278 pass, 2 fail** (1 pre-existing WO-270 `.claude` toolchain failure, since fixed by WO-270's own `done` merge; 1 the migration-naming enumeration this report is about). Both are gone: the full suite is **304/304, 0 failures** — a number I ran myself, not one taken from the implementer's or the librarian's log line. The file/test-count growth (10→15 files, 280→304 tests) is the merged content of WO-267/WO-269/WO-270/WO-274 landing on this branch, not new work by WO-272.

Isolated: `npx vitest run tests/db/migration-naming.test.ts --no-file-parallelism` → **15/15 pass** (TST-008 recorded `1 failed | 14 passed (15)` on the same file at that point).

### 2. WO-272's own six criteria — re-verified against the current tree, fresh mutation probes distinct from TST-008's

`tests/account/columns.test.ts` in isolation: `npx vitest run tests/account/columns.test.ts --no-file-parallelism` → **10/10 pass**.

| # | Criterion (source, abbreviated) | Result | Fresh mutation probe (different edit than TST-008's own) |
|---|---|---|---|
| 1 | BP-017 — `users.notify` jsonb, not null, default `{}` | PASS | TST-008 dropped the `default`; **here** the migration was changed to drop `not null` instead, keeping the default (`add column notify jsonb default '{}'::jsonb;`). The "not null, default {}" assertion fails (`"NO"` → `"YES"`), 9/10 pass, 1 fail. Restored (`git diff`/`git status --porcelain` empty); 10/10 re-pass. Discriminates. |
| 2 | BP-017 — `sites.timezone` text, nullable, no default | PASS | TST-008 made the column `not null default 'UTC'`; **here** it was changed to `text default 'Europe/London'` — nullable, but with a default, and a non-`'UTC'` value (rules out a probe that would only ever catch the literal string `'UTC'`). The "no default"/"reads null" assertions fail (`'Europe/London'` received where `'<null>'`/empty expected); 3/10 fail. Restored; 10/10 re-pass. Discriminates. |
| 3 | BP-059 — non-object `notify` value refused (`check` constraint) | PASS | TST-008 removed the `check` constraint entirely; **here** it was weakened instead of removed — `check (jsonb_typeof(notify) in ('object','array'))`, still a real constraint. `[]` now inserts without error; exactly 1/10 fails (the "a non-object notify value is refused" row). Restored; 10/10 re-pass. Discriminates. |
| 4 | BP-002 index — both migration names resolve to BP-017 via `topicOf()`, and pass `migration-naming.test.ts`'s per-file rule | PASS | TST-008 read `topics.ts` directly without mutating it. **Here**, `src/lib/db/topics.ts`'s `users` topic entry was mutated from `{ token: "users", owner: "BP-017" }` to `owner: "BP-099"` — a wrong-but-non-null owner, which a looser "resolves to something" assertion would miss. The row-4 assertion fails on the exact object (`toEqual` catches the wrong owner). Restored; 10/10 re-pass. Discriminates — proves the test checks the *specific* owner id, not merely non-null resolution. |
| 5 | `structure.md` 3a — baseline file never re-opened; the two columns arrive only in the two new migrations | PASS | TST-008 only grepped the shipped baseline file (no mutation). **Here**, the word `notify` was inserted as a SQL comment inside `00000000000001_baseline.sql`'s `create table users (...)` body. The "the baseline file is unmodified" assertion fails (`not.toMatch(/\bnotify\b/)`). Restored; 10/10 re-pass. Discriminates. |
| 6 | REQ-073 c1 — no fallback zone written by the schema | PASS | Same mutation as row 2 (`default 'Europe/London'`, not `'UTC'`) fails this row too — both the `toEqual([["<null>"]])` and the explicit `.not.toBe("UTC")` assertions, the latter proving the test isn't merely checking against the literal string `'UTC'`. Discriminates. |

All six rows PASS and discriminate under fresh, independent mutation — every probe distinct from TST-008's own (different columns/constraints/files touched, or the same file mutated in a different direction), all restored and confirmed clean via `git status --porcelain` and `git diff` (empty) after each, before moving to the next.

### 3. Migration-naming interaction — specifically re-checked

`git diff a8ab3b0..HEAD -- tests/db/migration-naming.test.ts` shows the file is now WO-274's fix, not WO-272's own diff. Read directly (current text, lines 74–80):

```ts
it("contains the two baseline migrations WO-267 writes", () => {
  expect(files).toContain("00000000000001_baseline.sql");
  expect(files).toContain("00000000000002_rls.sql");
});

it.each(files)("%s carries exactly one assigned topic token", (file) => {
  expect(topicOf(file)).not.toBeNull();
});
```

The exact `toEqual([...two files...])` enumeration TST-008 named as the defect is gone, replaced by a `toContain` membership check (WO-274) — it no longer asserts the directory holds *only* those two files, so WO-272's `00000000000003_users_notify_column.sql` and `00000000000004_sites_timezone_column.sql` landing alongside them no longer trips it. Confirmed empirically, not just by reading the diff: the whole file is 15/15 green (§1 above), and the per-file `it.each` rule — the assertion WO-272's own `rests-on` row 1 actually rests on — passes for all four files in the directory, including WO-272's two. This is the specific interaction TST-008 flagged and this re-validation exists to clear; it is cleared.

### 4. Scope — re-checked against WO-272's own commits, not a raw branch diff

A raw `git diff --name-only <old>..HEAD` on this branch is dominated by files WO-267/WO-269/WO-270/WO-274 brought in through the merge (`.claude/factory/**`, `src/ui/layout/**`, `tests/ui/**`, blueprints, requirements, the generated registry, etc.) — none of that is WO-272's own change, so that comparison is the wrong instrument here. Checked instead against WO-272's own non-merge commits:

- `1dd6daf` (planner, WO doc) — `sdlc-factory/docs/work-orders/WO-272.md` only.
- `a8ab3b0` (implementer) — `sdlc-factory/docs/work-orders/WO-272.md`, `supabase/migrations/00000000000003_users_notify_column.sql`, `supabase/migrations/00000000000004_sites_timezone_column.sql`, `tests/account/columns.test.ts`.
- `46d2a7e` (TST-008) — `sdlc-factory/docs/work-orders/WO-272.md` only.
- `306c208` (id renumber) — `sdlc-factory/docs/work-orders/WO-272.md` only.
- `e754782`/`eebd62f` — merge/bring-current commits, no conflicting hunks in WO-272's own files; `git status --porcelain` clean on this HEAD.

Exactly the two migrations, `tests/account/columns.test.ts`, and WO-272's own record — nothing outside the approved file plan. No scope finding.

### 5. `rests-on` dispositions (constitution rule 2.3b) — re-confirmed, not re-opened

Read directly from this branch's current front-matter:

```yaml
rests-on:
  - claim: "WO-267's `topicOf()` resolves a `users`- or `sites`-topic migration ... to the topic's owner, BP-017 ..."
    disposition: confirmed
  - claim: "`sites.timezone` is nullable because the `sites` row exists from provisioning ... "
    disposition: confirmed
```

Both rows carry `disposition: confirmed`, set by TST-008 and untouched since. Neither has reverted to `open`. Re-confirmed on this pass, not merely carried forward unread:

- **Row 1** (topicOf resolves to BP-017, doesn't refuse): re-observed directly in §2 row 4 above — both filenames still resolve to `{ token, owner: 'BP-017' }` on the current tree, and my own mutation (wrong owner id) proves the assertion is live and specific, not vacuous. Still holds.
- **Row 2** (`sites.timezone` nullable, nothing reads it before first sign-in): the shipped shape (`text null`, no default) is unchanged and re-verified in §2 rows 2/6. WO-273 remains `approved`, unimplemented on this branch and on `doctrine-0.11.0` (`src/app/(account)/adopt-timezone.tsx` does not exist in either); its own file plan still lists "the `sites.timezone` column — WO-272 (BP-017)" as out of its own scope. The claim's factual basis — no order in the corpus yet writes the browser's zone — is unchanged since TST-008. Still holds.

Neither row is this pass's to newly disposition (TST-008 already discharged both); this section is the re-confirmation the re-validation brief asked for.

### Coverage gaps

- Same residual note as TST-008's, unaffected by this pass: RLS is not exercised against these two columns (by design — `00000000000002_rls.sql` is not applied in `columns.test.ts`'s setup; BP-017's delta is schema-shape, not policy). No criterion or `rests-on` row claims otherwise.
- Not re-examined here (unchanged since TST-008, not this WO's files): BP-017's own two other open `rests-on` rows (Stripe customer-portal VAT correction; Stripe Checkout billing-address capture) — unrelated to `users.notify`/`sites.timezone`.

### Verdict vocabulary note

`sdlc-factory/CLAUDE.md` §4's agent table, validator row, states the vocabulary this verdict is drawn from: "May decide: pass / findings / reject."

### Verdict

pass

TST-008's single finding — `tests/db/migration-naming.test.ts`'s hardcoded two-file enumeration tripping on WO-272's two correctly-named migrations — is cleared: WO-274 (merged, `done` on `doctrine-0.11.0`) replaced the exact enumeration with a `toContain` membership check, independently confirmed here to no longer trip on WO-272's files while the per-file `topicOf()` rule WO-272's `rests-on` row 1 actually depends on still passes for all four migrations. Full suite, run fresh: **304/304, 0 failures** (was 278/280 at TST-008; both prior failures — the WO-270 `.claude` one and this one — are gone). All six of WO-272's own test-plan rows re-verified against the current tree with fresh mutation probes distinct from TST-008's own (different columns, different constraint edits, a wrong-owner-id mutation on `topics.ts`, a direct baseline-file mutation) — all discriminate. Scope re-checked against WO-272's own non-merge commits: exactly the two migrations, `tests/account/columns.test.ts`, and this WO's own record. Both `rests-on` rows remain `disposition: confirmed`; neither reverted to `open`. Typecheck and lint clean (`npm run typecheck` exit 0; `npx eslint tests/account/columns.test.ts` exit 0). **Next:** none for this WO's validation — ready for `/regress` and the librarian's `done` gate (regression record and, per rule 2.3b, both `rests-on` rows already dispositioned, so no `open` row blocks `done`).

**Regression pass, appended per `/regress`'s dispatch (constitution §3, "its latest validation section states what else was re-checked, not just the new work").** Branch reconfirmed current with `doctrine-0.11.0` (`git merge-base --is-ancestor doctrine-0.11.0 HEAD` — true; HEAD `cb3e59f`, no commits landed on `doctrine-0.11.0` since this branch last merged it). This is a schema change (two new columns on `users`/`sites`), a wider blast radius than a test-only order — treated accordingly:

1. **Substrate consumers re-run.** `npx vitest run tests/db/baseline.test.ts tests/db/rls.test.ts tests/db/clients.test.ts tests/db/migration-naming.test.ts --no-file-parallelism`: **4 files, 87/87 pass.** `factory-console impact WO-272 .`: `47 files → 2 blueprints (BP-002, BP-017) → 0 importers → 0 requirements` — no source module yet imports the two new columns.
2. **RLS re-checked against the real database, not the migration text** — a standalone probe (`/tmp/.../rls-probe.mjs`, deleted after this run) reset the schema from scratch, applied all four migrations including WO-272's two, seeded two users/sites via `service_role`, and exercised PostgREST directly with signed JWTs: (a) `anon` reads 0 rows from `/users` and `/sites` (no `anon` policy on either table — unchanged); (b) authenticated user A's `GET /users`/`GET /sites` returns exactly their own row, including their own `notify`/`timezone` value, never user B's; (c) a direct `id=eq.<B>` filter and a `PATCH` to user B's `notify` / site B's `timezone` from user A's JWT both affect 0 rows, and B's values are confirmed unchanged afterward via a `service_role` read; (d) user A can read and write their own `notify`/`timezone`. All 15 assertions passed. The two new columns inherit the existing table-level RLS policies (`users_select_own`/`users_update_own`, `sites_select_own`/`sites_update_own`) — no column-level grant restricts them (`00000000000001_baseline.sql`'s `grant select, insert, update, delete` is table-level) and no anon/non-owner access was widened.
3. **Migrations re-applied from scratch**, not incrementally: `drop schema public cascade; create schema public; grant usage ...` then all four migrations in order (`00000000000001` → `00000000000004`) via `psql -v ON_ERROR_STOP=1 -f`, individually, on the now-empty `public` schema — no error at any step; `information_schema.columns` confirms `sites.timezone` (`text`, nullable, no default) and `users.notify` (`jsonb`, not null, default `'{}'::jsonb`) land exactly as specified. Schema reset to clean afterward for the suite.
4. **Consumers named, none live.** WO-107 (`users.notify`, BP-059), WO-211 and WO-218 (`sites.timezone`, BP-045/BP-057), and WO-273 (`sites.timezone`'s first-sign-in writer) are all `status: approved`, none `done`. Their planned files — `src/lib/mail/notifications/store.ts`, `src/lib/publish/ceilings/index.ts`, `src/lib/publish/settings/settings.ts`, `src/app/api/account/timezone/route.ts`, `src/app/(account)/adopt-timezone.tsx` — do not exist on this branch or on `doctrine-0.11.0`. The two columns have no live reader or writer today; stated here per rule 5.5 rather than left as silence.
5. **Full suite and typecheck, fresh:** `npx vitest run --no-file-parallelism` → **15 files, 304/304 pass** (matches TST-010's own number, re-run independently rather than taken on faith). `npm run typecheck` → clean, exit 0.

Regression: 4 files (`sdlc-factory/docs/work-orders/WO-272.md`, `supabase/migrations/00000000000003_users_notify_column.sql`, `supabase/migrations/00000000000004_sites_timezone_column.sql`, `tests/account/columns.test.ts`) · none (BP-017 carries `satisfies: []`; no REQ is named on this WO) re-checked — pass
