---
id: WO-058
type: work-order
title: "claimFreeScanSlot — the single consumption point, in one transaction"
status: approved
ui: no
implements: [BP-023, REQ-001, REQ-003]
depends-on: [WO-267, WO-057]
blocked-by: []
rests-on: []
supersedes: []
wave: W2
---

# WO-058 — `claimFreeScanSlot` — the single consumption point, in one transaction

- Branch: wo/WO-058-admission-claim
- Estimate: ≤1 day

## Goal
Make starting a free scan a single transaction that re-evaluates the same
admission order and inserts the `scans` row that *is* the in-flight record, so
two simultaneous submissions can only ever resolve to one scan.

## File plan

> Every path below is **NEW** except `src/lib/scan/admission.ts`, which WO-057
> creates and this WO extends. Verified 2026-08-31: this repo carries no `src/`,
> so even the "modify" row is a file that does not exist yet at planning time.
> Placement per `structure.md`; provisional against a repo with no code.

| File | New/Modify | Change |
|---|---|---|
| `src/lib/scan/admission.ts` | Modify (created by WO-057) | Adds `claimFreeScanSlot` only. Touches no existing export: `admitFreeScan` keeps its signature and its side-effect-free contract (BP-023 decision 5). The claim runs one serialisable transaction that re-evaluates the same six steps and inserts the `scans` row with `network_hash`, `tier: 'free'`, `status: 'running'` and `from_incomplete_rescan`. |
| `tests/scan/free/admission-claim.test.ts` | NEW | The transaction, concurrency and once-only suites named in the test plan. |

## Interfaces

Consumes: WO-057's `Admission`, `NetworkKey`, and the six-step order it
implements; WO-051's `CanonicalDomain`; WO-056's `network_hash` and
`from_incomplete_rescan` columns; BP-002's `dbAdmin()`.

Exposes (verbatim from BP-023 `## Public interface`, `src/lib/scan/admission.ts`):

```ts
/** The consuming form. One transaction: re-evaluates the same order, inserts the
 *  `scans` row that *is* the in-flight record, and returns its id — so two
 *  simultaneous submissions cannot both pass. */
export function claimFreeScanSlot(a: {
  domain: CanonicalDomain; network: NetworkKey
  fromIncompleteRescan: boolean          // REQ-001 c14's once-only bound
}): Promise<{ claimed: true; scanId: string } | { claimed: false; refusal: Admission }>
```

## Steps
1. Write the failing suites first, the concurrency one included.
2. Extract WO-057's six steps into an internal, transaction-aware evaluator that
   both entry points call, so the order exists once in the file (rule 2.4).
3. Implement `claimFreeScanSlot` as one serialisable transaction: evaluate the
   order, and on `admit` insert the `scans` row and return its id. The in-flight
   bound is resolved by the `status = 'running'` predicate inside the same
   transaction — not by an application-held lock (BP-023's NFR budget).
4. Set `from_incomplete_rescan` from the parameter at insert time, and never
   anywhere else.
5. On refusal insert nothing: a refused scan produces no `scans` row at all
   (BP-023, "so it 'produces no report of its own'").
6. Confirm the fail-open handler from WO-057 still wraps the non-removal steps
   inside the transaction, and that a serialisation failure retries the
   transaction rather than admitting a second scan.

## Test plan

> Criteria are copied verbatim from `requirements/REQ-003.md` and
> `requirements/REQ-001.md`; line breaks are normalised to fit the table and no
> word is changed.

| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-003 c7 — "Given a scan is already running from the visitor's network, when another scan would start for them, then no second scan starts; if the running scan is of the domain they asked for they are returned to it, and if it is not they are refused in writing, told how long until they may scan, and are never shown a scan or a report of a domain they did not ask for." | `admission/claim · two simultaneous claims resolve to one scan` — N concurrent `claimFreeScanSlot` calls for one network key: exactly one returns `{ claimed: true }`, every other returns `{ claimed: false, refusal: { refuse: 'in_flight', … } }`, and the database holds exactly one `running` row. Run at N = 2 and N = 20. |
| REQ-003 c6 — "… when another scan would start for them, then it does not start, they are refused with one written line saying how long until they may scan again, and the refused scan, having never run, produces no report of its own." | `admission/claim · a refused claim writes nothing` — for each of the six refusal reasons, `claimFreeScanSlot` returns `{ claimed: false }` and the `scans` row count is unchanged, asserted before and after. |
| REQ-001 c14 — "… That offer is made once and does not chain: a report that is itself the product of a re-scan started from it carries the same written line and no re-scan control, and stays readable at its address, until its age earns a control under criterion 15. A re-scan that produced no report at all leaves the offer standing (criteria 16 and 17)." | `admission/claim · from_incomplete_rescan is set at claim time and only there` — a claim with `fromIncompleteRescan: true` stores `true` on the new row; a claim with `false` stores `false`; and a source assertion that `from_incomplete_rescan` is written in exactly one statement in `src/lib/scan/admission.ts` and nowhere else in `src/`. The control this column drives is WO-061's. |
| REQ-003 c9 — "Given the scan limiter itself is unavailable, when a visitor starts a scan, then the scan proceeds." | `admission/claim · a counting error still claims` — with each counting read inside the transaction stubbed to throw in turn, the claim succeeds and a `running` row exists; with the `domain_blocks` read stubbed to throw, it refuses `removed` and no row exists. |
| REQ-003 c12 — "… once the limit has passed the same address starts a scan for them, unless that domain's report was removed at its owner's request (REQ-002 criterion 3), when it never does …" | `admission/claim · the limit passing re-opens the claim, removal never does` — advancing a frozen clock past the hourly window lets a previously refused key claim; a removed domain refuses at every point on the clock, before and after every window. |

## Out of scope
- The scan pipeline the claimed id feeds — BP-012's `runScan`, another node's.
- The 90-second and spend ceilings — WO-059.
- The stage stream — WO-060.
- The written refusal lines — BP-019's, another node's.

## Rollback
Revert `claimFreeScanSlot` and delete its test file. `admitFreeScan` is
independent of it and keeps working; no schema change is involved.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-023 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote REQ-001, REQ-003 as they stand on disk.
- **Bounded.** 2 files in the file plan (1 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 1 cross-block depends-on edge added (WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267
- 2026-09-03 failed — validator — TST-022, verdict findings: `claimFreeScanSlot` itself, the migration-vs-transaction deviation and the narrowed WO-057 assertion all independently confirmed sound (a code finding, not a record finding); but `npm test` fails reproducibly (3/3 runs) because `tests/scan/free/admission-claim.test.ts`'s live-substrate concurrency suite is not isolated from vitest's default file parallelism and collides with two already-unprotected live-substrate files under the shared scratch database — next: implementer, fold that file into the `db` vitest project (`vitest.config.ts`, mirroring WO-275) or redesign its schema reset to be non-destructive of tables outside its own scope, then resubmit for validation.
- 2026-09-03 started — implementer — refusal check passed (`status: approved`, self-certified 2026-08-31); resuming from TST-022's findings verdict to close the test-isolation defect it found. Worktree `/root/projects/reachkitv3-wt/WO-058`, substrate confirmed running (postgres, postgrest, pgmeta).
- 2026-09-03 finished — implementer — closed TST-022's finding: folded `tests/scan/free/admission-claim.test.ts` into the `db` vitest project (`vitest.config.ts` — added to that project's `include`, added to `node`'s `exclude`), mirroring WO-275's own precedent exactly (a dedicated, `fileParallelism: false` project, not a non-destructive schema-reset redesign) rather than diverging from it. `npm test` (the documented gate command): 5/5 consecutive runs green, 24 files / 508 passed / 1 skipped / 0 failed every run (was reliably red, 3/3, before this fix). `tests/scan/free/schema.test.ts` and `tests/account/columns.test.ts` re-verified individually (both pass in isolation) and inside all 5 full-suite runs — untouched, in scope, and no longer collide with `admission-claim.test.ts` now that it runs in the serialized `db` project after `node`/`ui`/`layout` drain (WO-275's own established scheduling). `npm run typecheck`: clean. 2-file diff (`vitest.config.ts`, plus this WO's own `## Log`) — no other file touched. Ready for `/validate`.
- 2026-09-03 finished — validator — TST-023, verdict pass: independently re-ran `npm test` 5 consecutive times (not taken on the implementer's count) — 5/5 green, 24 files / 508 passed / 1 skipped / 0 failed every run, reversing TST-022's reproducible 3/3-red finding; confirmed `tests/scan/free/schema.test.ts` and `tests/account/columns.test.ts` still pass individually and inside every full-suite run; confirmed the `vitest.config.ts` diff is exactly the 2-line fold into the `db` project (nothing over-broad, nothing else touched); confirmed `src/lib/scan/admission.ts`, the migration, and `admission-claim.test.ts` are byte-identical to TST-022's own validated tip, and independently re-ran the partial-unique-index mutation to reconfirm the concurrency proof holds. TST-022's 5 test-plan rows and its deviation review stand unchanged.

## Validation report (appended by validator)

## TST-022

### Scope
WO-058 (`wo/WO-058-admission-claim`, commit `b1a7b60`, worktree
`/root/projects/reachkitv3-wt/WO-058`), validated against its own `## Test
plan`, `## Interfaces`, `## Steps` and `## Out of scope`, and against
constitution rule 4.2 (the implementer's own three flagged deviations) and
rule 2.3b (`rests-on` disposition). `git diff 26f74d1..b1a7b60 --stat`:
5 files — `src/lib/scan/admission.ts` (MODIFY, in file plan),
`tests/scan/free/admission-claim.test.ts` (NEW, 647 lines, in file plan),
`supabase/migrations/00000000000006_scans_freepath_claim.sql` (NEW, 52
lines, **not** in file plan — flagged deviation 1),
`tests/scan/free/admission-check.test.ts` (MODIFY, 27 lines — WO-057's own
file, **not** in file plan — flagged deviation 2), and this WO's own `##
Log` (+2 lines). Working tree was clean at commit; `node_modules` in the
worktree is a stale symlink artifact unrelated to this WO (see Suite/build).

### Suite / build
- `tsc --noEmit`: clean.
- `eslint .`: 8118 pre-existing problems (8093 errors, 25 warnings) —
  identical count confirmed by running the same command against
  `26f74d1` (pre-WO-058) in a scratch checkout; `eslint src/lib/scan/
  admission.ts tests/scan/free/admission-claim.test.ts
  tests/scan/free/admission-check.test.ts` in isolation: clean, zero
  output. Nothing attributable to this WO.
- `next build` in the given worktree: **fails** —
  `TurbopackInternalError: Symlink [project]/node_modules is invalid, it
  points out of the filesystem root`. Root-caused to the worktree's own
  `node_modules` being a symlink to `/root/projects/reachkitv3/node_modules`
  (`readlink` confirms), which Turbopack's sandbox refuses to resolve from
  a worktree root — **an environment artifact, not a WO-058 defect**:
  `npm run build` against the main checkout's own real (non-symlinked)
  `node_modules`, same commit's code, compiles, typechecks and prerenders
  cleanly.
- `npm test` (`vitest run --project node --project ui --project db`,
  the documented gate command, WO-275's own `db` project honoured):
  **fails, reproducibly, 3/3 consecutive runs** — 2 test files fail each
  run (`tests/scan/free/schema.test.ts` and/or `tests/account/
  columns.test.ts`, which one varies by run — 11, 1, and 11 failing
  assertions respectively across the three runs), with errors of the
  shape `relation "scans" does not exist` and table-list mismatches — the
  signature of the shared scratch database's schema being mid-rebuild
  when another file's query runs. **Root cause and isolation, independently
  reproduced (see "New finding" below): this WO's own new file.**

### Criterion-by-criterion

| Criterion (verbatim, per WO's own table) | Test | Verdict | Mutation probe |
|---|---|---|---|
| REQ-003 c7 — in-flight: no second scan starts, same-domain returns to it, different-domain refuses in writing with a wait | `admission/claim · two simultaneous claims resolve to one scan` (mocked lost-race + winning-claim pair) and the live-substrate concurrency suite (N=2, N=20) | PASS | Removed the partial unique index from the migration: N=2 → 2 winners (expected 1), N=20 → 14 winners (expected 1), both fail deterministically, 3/3 isolated re-runs of the unmutated code pass 20/20. Inverted `isUniqueViolation` to always `false`: both concurrency tests fail (`duplicate key value violates unique constraint`, uncaught) — confirms the conflict-to-refusal path is load-bearing, not decorative. |
| REQ-003 c6 — a refusal writes one line, never a report, having never run | `admission/claim · a refused claim writes nothing` (6 refusal reasons, `it.each`) | PASS | Removed the `if ("refuse" in result) return` short-circuit before the insert: 6/6 refusal-writes-nothing tests fail (an insert lands for every refusal reason) — confirmed live. |
| REQ-001 c14 — `from_incomplete_rescan` set once, at claim time, nowhere else | 2 behavioural tests (`true`/`false` stored) + 1 source-scan test (exactly one write site in `admission.ts`, none elsewhere in `src/`) | PASS | Hard-coded the insert's `from_incomplete_rescan` to a constant `false`: the behavioural `true`-storing test fails independently of the source-scan test (both catch it, neither is a decorative duplicate of the other), confirmed live. |
| REQ-003 c9 — limiter unavailable → scan proceeds; `domain_blocks` unavailable → refuses `removed` | `admission/claim · a counting error still claims` (4 steps) + `a domain_blocks read that throws refuses removed and inserts no row` | PASS | Made the `removed` step fail open instead of refuse (`catch { removed = false }`): the domain_blocks-throws test fails, returning `{ claimed: true }` instead of `{ claimed: false, refusal: { refuse: "removed" } }` — confirmed live, matches BP-023's stated asymmetry (limiter fails open, removal never does). |
| REQ-003 c12 — the limit passing re-opens the claim; a removed domain never does, at any point on the clock | `admission/claim · the limit passing re-opens the claim, removal never does` (2 tests) | PASS | Not independently re-mutated in this pass (time-bounded); coverage inherited from WO-057's own established mutation of the identical shared order — `evaluateAdmission` is the same six-step function `admitFreeScan` calls, and TST-021 already confirmed live that reordering the `removed` step below `cooldown` breaks exactly the test claiming to cover it. `claimFreeScanSlot` calls the same function, so the same guarantee carries over; the two new tests here exercise the hourly-boundary and removed-precedence behaviour specifically through the claim path's insert/no-insert outcome, which the c6/c9 mutation probes above already establish is wired correctly to `evaluateAdmission`'s result. |

All 5 rows of the file plan's `## Test plan` are covered by tests confirmed
to discriminate by direct mutation (4 of 5 independently re-run in this
pass; the 5th inherits a live-confirmed mutation of the exact shared code
path it also exercises).

### Deviation review (constitution rule 4.2) — the implementer's three flags

**1. A migration in place of a literal client-driven transaction — independently verified, legitimate.**
- **(a) Cannot be done as a literal transaction from this client, confirmed.**
  `src/lib/db/index.ts` (BP-002's own public interface) declares exactly
  `db()` and `dbAdmin()`, both `createClient<Database>(...)` —
  `@supabase/supabase-js` PostgREST clients. PostgREST is stateless HTTP:
  each `.from(...)` call is one request and one implicit server-side
  transaction: there is no client-side `BEGIN`/`COMMIT` spanning two
  separate `.from(...)` round trips through this client, and neither
  `.rpc()` (a stored procedure — a migration function body) nor a raw `pg`
  connection is in this WO's file plan or reachable from `dbAdmin()`'s own
  declared surface. Confirmed by reading the actual client construction,
  not taken on the comment's word.
- **(b) The partial unique index enforces the claimed property, confirmed
  by reading the SQL.** `create unique index
  idx_scans_one_running_per_network on scans (network_hash) where status =
  'running' and network_hash is not null;` — a unique index scoped to rows
  where `status = 'running'`, so Postgres refuses a second `running` row
  for the same `network_hash` atomically, regardless of what evaluated in
  application code beforehand, and stops constraining a network the moment
  its row leaves `running` (finishes or fails), which is exactly the
  transient in-flight bound BP-023 describes. `network_hash is not null`
  correctly excludes paid-tier rows (`network_hash` is `null` there, and
  Postgres never treats two `null`s as equal for uniqueness) without
  depending on `status='running'` never being a paid-tier value.
- **(c) The concurrency test genuinely proves it — run independently, not
  just read.** N=2 and N=20 against the live substrate (real PostgreSQL 18
  + PostgREST at `127.0.0.1:3001`, real `@supabase/supabase-js`, an async
  `curl`-based loopback fetch so requests actually overlap): 3/3 isolated
  runs, exactly 1 `claimed: true` and n−1 `in_flight` refusals, exactly one
  `running` row in the database, every time. Mutation-verified in both
  directions above (index removed → fails; conflict-detection disabled →
  fails). **This is the most consequential deviation and it holds**: the
  mutual-exclusion property BP-023's NFR budget names ("concurrent claims
  from one network resolve to one `running` row... not by a lock the
  application holds") is delivered, atomically, by the database constraint
  — a stronger guarantee in one respect than a client-driven serialisable
  transaction would even need, since it holds regardless of the calling
  transaction's isolation level (there is none) and survives any future
  caller that reaches this table directly.

**2. The WO-057 assertion narrowing — honest, confirmed.** WO-057's
original "no write method appears anywhere in this file" is strictly
false once WO-058 adds `claimFreeScanSlot`'s own insert; the narrowed
assertion (`"domain_blocks"` appears exactly once in the file, inside
`isRemoved`) is a source-level regex over the **entire file** (both
functions), so it still catches a write to `domain_blocks` introduced
anywhere in the file, including inside `claimFreeScanSlot`. Combined with
the file's own call-tracking check (no `insert`/`update`/`upsert`/`delete`
call ever lands on the mocked `domain_blocks` table across live
`admitFreeScan` calls), the original claim's *intent* — `domain_blocks` is
never written by this module — is fully preserved; only the accidentally-
broader original wording (no write method *at all*) is narrowed, and only
because it is now literally false. Not a weakening of coverage.

**3. File-plan coverage.** All 5 test-plan rows covered per the table
above; see also the two additional flagged deviations (this section,
items 1–2) which are the only files touched outside the literal 2-file
plan, both justified and independently confirmed.

### New finding — the full suite does not pass reliably (not flagged by the implementer)

Not one of the implementer's three flagged items. Found independently by
running the documented gate command (`npm test`) itself, as instructed.

**Evidence, reproduced and isolated:**
1. `npm test` on `b1a7b60` (this WO's tip): 3/3 runs red, 2 files failing
   each time (`tests/scan/free/schema.test.ts` and/or `tests/account/
   columns.test.ts`).
2. `npm test` on `26f74d1` (the immediate parent, pre-WO-058, same
   command): 3/3 runs green, 23 files, 487 passed, 1 skipped — confirms
   this is **not** a pre-existing flake independent of this WO.
3. Isolating test: with `tests/scan/free/admission-claim.test.ts` moved
   out of the tree (nothing else changed, still on `b1a7b60`'s code),
   `npm test` returns to 3/3 green (23 files, 488 passed, 1 skipped).
   Restored afterward; working tree confirmed clean.

**Root cause.** `tests/scan/free/admission-claim.test.ts`'s live-substrate
concurrency suite runs `drop schema public cascade` / reapplies 3
migrations in `beforeAll`, and `drop schema public cascade` again in
`afterAll`, against the one shared scratch Postgres database — while
scheduled under vitest's default-parallel `node` project, the same
project `tests/scan/free/schema.test.ts` and `tests/account/
columns.test.ts` also run under. Those two files already carry header
comments claiming they need `--no-file-parallelism` "alongside
`tests/db/*`" (a pre-existing gap: WO-275, the WO already dedicated to
exactly this class of problem, scoped its fix to exactly 3 files —
`tests/db/baseline.test.ts`, `rls.test.ts`, `clients.test.ts` — and its
own `## Out of scope` does not name `schema.test.ts` or `columns.test.ts`).
WO-058 adds a *third*, more destructive live-substrate file
(`drop schema... cascade` is more destructive than the reset-and-reapply
the other two do, and it reapplies only 3 of the repo's migrations, not
the full baseline) into that same unprotected pool, which is what tips
the pre-existing latent gap into a reliably-reproducing failure.

**Attribution.** The underlying architectural gap (two files not yet
folded into the `db` project) predates this WO and is not this WO's to
close from scratch. What is this WO's: shipping a new file that (a) is
not isolated from vitest's default parallelism despite doing the same
class of destructive live-substrate work the project's own established
convention (`tests/db/baseline.test.ts`'s header, and WO-275 itself)
requires isolation for, and (b) was not flagged — rule 4.2's "say so once"
was exercised for the transaction/migration deviation and the assertion
narrowing, but not for this. The result is that `npm test`, the
documented gate command, does not pass on this branch — the condition
this validation is explicitly asked to confirm.

**What would clear it** (implementer's, or a scoped follow-up either
`blocked-by`'s this WO): fold `tests/scan/free/admission-claim.test.ts`
into the `db` vitest project (`vitest.config.ts`, `fileParallelism:
false`) alongside the 3 files WO-275 already isolated — mirroring that
WO's own precedent exactly — or redesign the concurrency suite's setup to
avoid a whole-schema `drop ... cascade` (e.g. idempotent per-migration
apply, checking existence, rather than tearing down tables outside its
own scope) so it can coexist with the two already-unprotected files under
default parallelism. Either touches `vitest.config.ts`, outside this WO's
literal 2-file plan — the same situation `tests/db/baseline.test.ts`'s own
header names and WO-275 later resolved; the correct move is to take the
narrow fix here (it is squarely this WO's own new file's reliability) or
open a same-day follow-up and `blocked-by` it, not to ship silently
red.

### rests-on disposition (rule 2.3b)

**WO-058's own `rests-on: []`** — no rows carried, nothing to disposition;
the `done`-gate precondition is trivially met (no `open` row exists to
block it).

**Upstream — BP-023 and REQ-003 (this WO's `implements` set; REQ-001's
`rests-on: []` carries nothing to disposition):**

WO-057 was the first WO to implement BP-023/REQ-003, and TST-021 (its own
validation report) *narrated* dispositions for 3 of BP-023's 4 rows and 1
of REQ-003's 2 open rows — but never wrote them into those artifacts' own
`rests-on:` front matter; all 4 rows were still `disposition: open` on
disk at the start of this pass (confirmed by reading both files before
editing). This is a record defect in that prior validation (the argument
was made, the disposition was never persisted to the row it was about —
exactly the authored-vs-derived-index gap rule 5.1/5.5 exist to prevent),
not this build's to leave uncorrected a second time. WO-058 independently
re-observes the same underlying, unchanged mechanism (`evaluateAdmission`
is the same six-step order WO-057 wrote, called by both `admitFreeScan`
and `claimFreeScanSlot`; `admission-claim.test.ts`'s own hourly/in-flight/
concurrency suites exercise it again), so this pass persists the
dispositions now, citing both the original TST-021 argument and this
build's own re-observation:

- **BP-023 row 2** (network-address counting shares one allowance/slot) →
  **confirmed**. Persisted to `BP-023.md`.
- **BP-023 row 3** (`x-forwarded-for` trust) → **undischargeable** — a
  claim about the deployment platform's own reverse-proxy behaviour,
  unobservable from inside this repository by any test either WO builds.
  Persisted to `BP-023.md`.
- **BP-023 row 4** (BP-005 gains the four constants) → **confirmed** —
  re-verified directly: `src/lib/config/constants.ts` still declares
  exactly the four named constants; WO-058's own diff declares none of its
  own. Persisted to `BP-023.md`.
- **BP-023 row 1** (the 60 s p95 vendor SLA) — **left open**, deliberately:
  neither WO-057 nor WO-058 calls a vendor endpoint or times a battery;
  this build did not observe that claim. Unchanged.
- **REQ-003 row 2** (same network-counting claim as BP-023 row 2) →
  **confirmed**, same reasoning. Persisted to `REQ-003.md`.
- **REQ-003 row 1** (vendor SLA, same claim as BP-023 row 1) — **left
  open**, same reasoning. Unchanged. REQ-003 row 3 was already `confirmed`
  on disk; untouched.

One row remains `open` on each of BP-023 and REQ-003 (the vendor-SLA row,
same claim in both places) — deliberate, not an oversight: this build
touches no vendor call and times no battery, so it cannot be the build
that discharges that claim.

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict
findings

WO-058's actual deliverable — `claimFreeScanSlot`, the extracted shared
evaluator, the partial-unique-index consumption mechanism, and all 5
test-plan rows — is correct, thoroughly and independently verified by
direct mutation (not just read), and both of the implementer's own flagged
deviations (the migration-in-place-of-a-transaction, and the narrowed
WO-057 assertion) are honest, correctly scoped, and hold up under
independent scrutiny — the migration-vs-transaction deviation in
particular is confirmed sound: PostgREST genuinely cannot give this client
a literal spanning transaction, and the partial unique index delivers the
same mutual-exclusion property BP-023's NFR budget names, atomically, by a
mechanism independently proven (by mutation, twice) to be load-bearing
rather than decorative. **This is a code finding, not a record finding —
send it back to the implementer, not to this report.** The defect is
narrow and precisely bounded: `tests/scan/free/admission-claim.test.ts`'s
own live-substrate concurrency suite is not isolated from vitest's default
file parallelism, and collides with two already-unprotected live-substrate
files (`tests/scan/free/schema.test.ts`, `tests/account/columns.test.ts`)
under the shared scratch database, making `npm test` — the documented gate
command — fail reproducibly (3/3 runs) on this branch; the immediate
parent commit passes the identical command reliably (3/3 runs), and
removing only this WO's new file restores that reliability, isolating the
cause to this WO's own shipped change. Fix path stated above. No other
scope, structure or convention defect found: no files outside the 2-file
plan plus the two flagged deviations were touched; `tests/scan/free/**`
is BP-023's own governed path; no duplicate capability introduced.

`/regress` does not apply to this section: its own dispatch condition is a
WO whose latest `## TST-###` already carries a *passing* verdict, and this
one does not — no `Regression:` line is added here.

## TST-023

### Scope
Re-validation of WO-058 (`wo/WO-058-admission-claim`, tip commit
`f3ee789`, worktree `/root/projects/reachkitv3-wt/WO-058`), dispatched to
close the single finding TST-022 left open: `npm test` failing
reproducibly (3/3) from a test-isolation collision, not a correctness
defect. `git diff aeffc42..f3ee789 --stat`: `vitest.config.ts` (2 lines)
and this WO's own `## Log` (2 lines) — nothing else. `git diff
aeffc42..f3ee789 -- tests/scan/free/admission-claim.test.ts
supabase/migrations/00000000000006_scans_freepath_claim.sql
src/lib/scan/admission.ts` is empty (confirmed directly): the file this
WO's `claimFreeScanSlot` deliverable lives in, its migration, and its
own concurrency suite are byte-identical to what TST-022 already
mutation-tested. `BP-023.md` and `REQ-003.md`'s `rests-on` edits were
already part of `aeffc42` (TST-022's own committed report), not new in
this range.

### Suite / build
- `npm run typecheck` (`tsc --noEmit`): clean.
- `npm test` (`vitest run --project node --project ui --project db`, the
  documented gate command): run independently **5 times** in this pass
  (not taken on the implementer's own count) — **5/5 consecutive green**,
  24 test files / 508 passed / 1 skipped / 0 failed, every run. This
  reverses TST-022's reproducible 3/3-red finding on the immediately
  preceding commit.
- `tests/scan/free/schema.test.ts` and `tests/account/columns.test.ts`,
  the two files TST-022 found colliding with the new suite: each run
  individually under `--project node` — both pass in isolation
  (13 passed/1 skipped, and 10 passed, respectively) — and both pass
  inside every one of the 5 full-suite runs above. Their own
  `--no-file-parallelism` header comments are unchanged and neither file
  was moved; the pre-existing gap TST-022 named (they are not yet folded
  into the `db` project themselves) is confirmed still present but is not
  this WO's to close — see "vitest.config.ts diff" below.

### vitest.config.ts diff — confirmed exact, not over-broad
```diff
             "tests/db/baseline.test.ts",
             "tests/db/rls.test.ts",
             "tests/db/clients.test.ts",
+            "tests/scan/free/admission-claim.test.ts",
```
applied twice: once to the `node` project's `exclude` array (removing
the file from the default-parallel pool that collided with it), once to
the `db` project's `include` array (the project WO-275 already
established as `fileParallelism: false`, serialized, and scheduled after
`node`/`ui`/`layout` drain). Confirmed by reading `vitest.config.ts` in
full: no other project, array, or flag touched — `schema.test.ts` and
`columns.test.ts` are not added to `db`'s `include` (they remain the
pre-existing, out-of-scope gap TST-022 attributed to before this WO), no
project's `fileParallelism` default is changed, and no other test file
is moved. This is exactly WO-275's own established precedent (a
dedicated, non-parallel project for destructive live-substrate suites)
applied to one additional file, not a broader policy change and not a
redesign of the suite's schema-reset strategy (the alternative TST-022
also offered was not taken, which is fine — either was acceptable).

### Independent mutation re-confirmation
Since the code is byte-identical to what TST-022 already mutation-tested
(confirmed above), a full re-run of all four mutation probes would be
redundant motion, not new evidence. One spot-check was run directly in
this pass for its own confirmation, not inherited: the partial unique
index (`idx_scans_one_running_per_network`) was removed from
`supabase/migrations/00000000000006_scans_freepath_claim.sql` and
`admission/claim · two simultaneous claims resolve to one scan` was run
against the live substrate — both N=2 and N=20 failed deterministically
(2 winners and 15 winners respectively, expected 1 in both), reproducing
TST-022's own finding exactly. The migration was restored byte-for-byte
(`git diff --stat` on the file confirmed clean) and the same test passed
again, then the full `npm test` suite was re-run clean once more. The
remaining three TST-022 mutation probes (refusal-writes-nothing
short-circuit, `from_incomplete_rescan` hard-coding, `removed`
fail-open) were not independently re-run in this pass — no code in
their path changed, and TST-022 already confirmed each live, by direct
mutation, not by inspection.

### Criterion-by-criterion (re-confirmed)

| Criterion (verbatim, per WO's own table) | TST-022 verdict | Re-confirmed this pass |
|---|---|---|
| REQ-003 c7 — in-flight, one scan starts | PASS | PASS — code unchanged; unique-index mutation independently re-run, fails as before |
| REQ-003 c6 — a refusal writes nothing | PASS | PASS — code unchanged (byte-identical diff) |
| REQ-001 c14 — `from_incomplete_rescan` set once, at claim time | PASS | PASS — code unchanged (byte-identical diff) |
| REQ-003 c9 — limiter unavailable proceeds, `domain_blocks` unavailable refuses `removed` | PASS | PASS — code unchanged (byte-identical diff) |
| REQ-003 c12 — the limit passing re-opens the claim, removal never does | PASS | PASS — code unchanged (byte-identical diff); same inherited-coverage reasoning as TST-022 |

All 5 rows hold. Nothing about `admission.ts`'s or the migration's logic
changed between TST-022 and this pass — the only change in scope is the
2-line `vitest.config.ts` fix and this WO's own `## Log`.

### Deviation review — unchanged from TST-022
Both of the implementer's originally flagged deviations (the migration
standing in for a literal client-driven transaction; the narrowed WO-057
assertion) were already independently confirmed sound in TST-022 against
code that has not changed since. Not re-litigated here (rule 2.4 — one
claim, one home; TST-022 is that home).

### Scope check
`git diff b1a7b60..f3ee789 --stat` (the full range since TST-022's own
validated tip): `BP-023.md`, `REQ-003.md` (both `aeffc42`, TST-022's own
`rests-on` persistence — accounted for, not new), `WO-058.md` (the
validation report itself, plus the log), `vitest.config.ts` (2 lines,
`f3ee789`). No file outside this WO's own record and its one narrow
config fix was touched. `vitest.config.ts` is not under `BP-023`'s own
`code:` glob, but a project-wide test-runner config is not any single
blueprint's governed path — the same exception TST-022 already reasoned
through for the migration file, applied here to the config file WO-275's
own precedent already established as the right place for this class of
fix.

### rests-on disposition (rule 2.3b)
WO-058's own `rests-on: []` — nothing to disposition, unchanged from
TST-022. Upstream (BP-023, REQ-003) dispositions were already persisted
by TST-022 in `aeffc42`, confirmed present and untouched by this pass
(`BP-023.md` and `REQ-003.md` do not appear in `git diff aeffc42..f3ee789
--stat`, above). One row remains deliberately `open` on each (the vendor
SLA row) — unchanged, and this pass observes nothing that discharges it
either (no vendor call, no timed battery in this range).

### Verdict vocabulary note
`sdlc-factory/CLAUDE.md` §4's agent table, validator row: "May decide:
pass / findings / reject."

### Verdict
pass

The single finding TST-022 raised — `npm test` failing reproducibly
(3/3) from `tests/scan/free/admission-claim.test.ts`'s live-substrate
concurrency suite colliding with two already-unprotected live-substrate
files under vitest's default `node` parallelism — is closed. Independently
confirmed, not taken on the implementer's report: 5/5 consecutive `npm
test` runs green (24 files / 508 passed / 1 skipped / 0 failed, every
run), `tests/scan/free/schema.test.ts` and `tests/account/columns.test.ts`
individually and inside every full-suite run, and the `vitest.config.ts`
diff read line-by-line and confirmed to be exactly the 2-line fold into
the `db` project TST-022 described — nothing broader, no other file
touched. The correctness findings TST-022 reached — `claimFreeScanSlot`
itself, the migration-in-place-of-a-transaction deviation, the narrowed
WO-057 assertion, and all 5 test-plan rows — stand unchanged because the
code they were reached against is byte-identical in this range
(confirmed directly, not assumed), and one of TST-022's own mutation
probes (the partial unique index) was independently re-run in this pass
and reproduces the identical result. This is a record update to a
previously-sound build closing a previously-open process defect, not new
correctness work. `WO-058` now has a passing verdict on its latest
`## TST-###` section and, per constitution rule 3 (§3's gate list), still
needs a `/regress` pass before `done` (this section is not that pass and
carries no `Regression:` line — its own preconditions were not met until
now).
