---
id: WO-161
type: work-order
title: "The pages-published tile: its delta, or its goal of 30 and what reaching it means"
status: approved
ui: yes
implements: [BP-038, REQ-041]
depends-on: [WO-006, WO-041, WO-156, WO-249]
blocked-by: []
rests-on:
  - claim: "The goal value for the Pages published headline number was the owner's and was outstanding (BP-038 decision 2 and its open question). Discharged 2026-08-31 by owner ruling — 30, meaning a month of daily pages. The value is pinned in BP-005's GOAL_VALUES, BP-038's open question is closed, and this tile renders its goal exactly as the other three do."
    disposition: confirmed
  - claim: "The what-reaching-it-means line behind GOALS.pages_published.meansKey is an owner-owed string in BP-019's registry; this WO renders the key and tests it, never the string"
    disposition: open
supersedes: []
---

# WO-161 — The pages-published tile: its delta, or its goal of 30 and what reaching it means

- Branch: wo/WO-161-overview-pages-published
- Estimate: ≤1 day
- Preview: sdlc-factory/docs/design/previews/WO-161.html
- Signed-off: <date>

## Goal
Render the pages-published tile so that it leads with exactly one headline
number carrying its change since the previous measurement, or — where there is
none — its goal of 30 with the meaning key beside it, and never appears bare.

## File plan
> Every path below is **NEW** (no `src/` on disk, checked 2026-08-31). Placement
> follows `structure.md` and BP-038's `code:` globs; provisional.

| File | New/Modify | Change |
|---|---|---|
| `src/app/(account)/app/overview/PagesPublishedTile.tsx` | NEW | Renders `Module<number>` for pages published: the headline through `renderMeasured` with its delta since the previous measurement, and where there is no previous measurement, `GOALS.pages_published` — the value `30` read from BP-005's `GOAL_VALUES` through WO-156's `GOALS`, with its `meansKey` rendered through `copy()`. The number is **read, never written here**: this file contains no numeric literal for the goal, so a change to the pin moves the tile and a change to the tile cannot move the pin. |
| `tests/app/overview/pages-published.test.tsx` | NEW | Delta rendering; the goal-and-meaning render where no previous measurement exists; the never-bare property; and the discriminating case that the rendered goal equals `GOAL_VALUES.pages_published` rather than any literal in this file — change the pin and the assertion follows it. |

## Interfaces
Consumes (verbatim from BP-038 `## Public interface`):
```ts
export type GoalKey = 'searches_appeared_in' | 'ai_answers' | 'score' | 'pages_published'
/** The pairing lives here; the numbers are BP-005's `GOAL_VALUES` and are never
 *  written in this module. All four now carry a value: `pages_published` was
 *  ruled by the owner on 2026-08-31 at **30**, meaning a month of daily pages,
 *  so `value` is no longer optional and no tile renders a goalless headline. */
export const GOALS: Readonly<Record<GoalKey, { value: number; meansKey: CopyKey }>>
```
and `renderMeasured()` / `copy()` (BP-019).

Exposes: `PagesPublishedTile` inside the overview module; no new cross-node
interface.

## Steps
1. **Precondition (rule 7.3):** signed preview. As of 2026-08-31
   `docs/design/tokens.md` is `Status: ready` and `components.md` registers 24
   components, every row `proposed` and reaching `approved` only when a preview
   carrying it is signed off. The preview, not the registration, is the gate.
2. Write the failing tests first.
3. Render the headline with its delta since the previous measurement.
4. Where there is no previous measurement, render `GOALS.pages_published.value`
   beside the headline with `copy(GOALS.pages_published.meansKey)`. **Still do
   not write the number or the sentence in this file**: the value is BP-005's
   pin (`GOAL_VALUES.pages_published`, ruled 30 on 2026-08-31) and the sentence
   is the owner's, in BP-019's registry. What the ruling changed is that the
   value exists; where it lives did not change.
5. Add the discriminating test: the rendered goal is read from
   `GOAL_VALUES.pages_published`, so changing the pin changes the render and a
   literal `30` anywhere under `src/app/(account)/app/overview/` fails the test.
6. Assert the never-bare property directly: with no delta and the goal stubbed
   away the tile must not render, because `HeadlineNumber.goal` is required and
   `GOALS`' `value` is not optional (WO-156).

## Test plan
| Acceptance criterion (verbatim from REQ-041) | Test |
|---|---|
| 4. Given a headline number on Overview — the single number a module leads with, of which every module shows exactly one and never two — when it is rendered, then it carries either its change since the previous measurement or the goal it is moving toward, and never appears bare. Every headline number has a goal: a fixed value the product defines for that number, the same for every customer, shown with the number and what reaching it means. Where there is no previous measurement to compare against, the number carries its goal. A value a module shows alongside its headline number, for comparison or context, is not itself a headline number and this criterion does not bind it. | `tests/app/overview/pages-published.test.tsx › the tile renders exactly one headline carrying its delta`, `(same file) › with no previous measurement the headline carries its goal of GOAL_VALUES.pages_published and the meaning key beside it`, `(same file) › the headline never renders bare`, `(same file) › the rendered goal follows the pin and no literal number appears in the tile` |

## Out of scope
- The goal **value** itself: `GOAL_VALUES.pages_published = 30` is BP-005's pin
  and its `tests/pins.test.ts` row, not this file's.
- The sentence behind `meansKey` — BP-019's registry; the owner's words. This WO
  renders the key.
- `GOALS` itself — WO-156 declares it and wires it to `GOAL_VALUES`.
- The other three tiles (WO-160) and every other module.
- Dropping the tile: `BUILD.md` §4.5 specifies it and BP-038 decision 2 rejects
  dropping it.

## Rollback
Revert the branch; the tile is a read-only render.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-038 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 1 row of `## Test plan` quote REQ-041 as it stands on disk.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.
- **`ui: yes`.** Rule 7.3's preview sign-off holds this work order's *implementation*, not its approval (§3), and the `Signed-off:` bullet is unfilled until the design-guardian returns the preview.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — pages_published goal ruling folded in: the tile now renders the ruled goal of 30 with its meaning key, the held rests-on row is discharged, and the WO is complete against REQ-041 c4 rather than partial.
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 3 cross-block depends-on edges added (WO-006, WO-041, WO-249); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.

## Validation report (appended by validator)
