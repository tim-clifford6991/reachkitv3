---
id: WO-021
type: work-order
title: "fetches — ledger, cache and raw store in one table"
status: approved
ui: no
implements: [BP-007]
depends-on: [WO-267]
blocked-by: []
rests-on:
  - claim: "This migration creates `fetches`, which `BUILD.md` §10 lists among its nine baseline tables. WO-267 (`done`) deliberately leaves it out — its baseline migration's own header states the exclusion — because `structure.md` rule 3 makes `*_fetches*.sql` BP-007's topic and BP-007's `## Data model delta` fully specifies the table, so one table has one writer. The derivation and its reversal cost are recorded on WO-267's first `rests-on` row, which carries `disposition: confirmed`."
    disposition: open
supersedes: []
---

# WO-021 — fetches — ledger, cache and raw store in one table

- Branch: wo/WO-021-fetches
- Estimate: ≤1 day

## Goal
Create the one row that is at once the spend ledger, the cache and the raw
store: insert-only, with the index the cache read uses and the index the ledger
read uses.

## Provenance of this file plan
`supabase/` now exists and carries six applied migrations through
`00000000000006_scans_freepath_claim.sql`; the `…004` slot this order planned
before WO-267 landed is taken by `00000000000004_sites_timezone_column.sql`.
The filename below follows the corpus's newer timestamped convention
(`20260901090000_opportunities_core.sql`), which sorts after every `0000…`
file. Path anchored to BP-007's `code:` glob `supabase/migrations/*_fetches*.sql`.
`tests/costs/` is still new.

**`reserved_cents` ships in this create-table, not in WO-022's additive
migration** (ruling 2026-09-03, rule 1.1). Derivation: BP-007's `## Data model
delta` names the column in `fetches`' own column list; both orders are unbuilt
and no `fetches` row exists anywhere, so an additive migration would alter a
table created moments earlier in the same unapplied set — ceremony against an
empty schema, and a second statement of one column (rule 2.4). Both files sit
under the same `*_fetches*.sql` glob, so no module boundary is crossed, and
WO-022's `depends-on: WO-021` already fixes the order of application. Rule 2.6
is unaffected: this order's stop condition stays the applied schema, WO-022's
stays the seam's behaviour — a column moving into the create-table moves no
behaviour, and WO-022 still asserts the column from the seam's side.
Reversal cost: if ADR-094 decision 3a is reversed before this ships, one column
is deleted from one unapplied statement (zero cost today); after it ships, a
`drop column` migration under this same glob — the same cost the separate
migration would have had.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `supabase/migrations/20260903080000_fetches.sql` | NEW | `fetches` exactly to BP-007's `## Data model delta` — its nine columns including `reserved_cents`, **no unique constraint** (BP-007 decision 3), the `(source, cache_key, policy_version, created_at desc)` index for the cache read and the `scan_id` index for the ledger read. RLS enabled with **no request-scoped read policy** — `fetches` carries cost and raw vendor payloads and is reachable only through `dbAdmin()`. |
| `tests/costs/fetches-schema.test.ts` | NEW | The tests below. `structure.md` rule 4 puts BP-007's tests under `tests/costs/**`. |

## Interfaces
Consumes: the RLS conventions WO-267 applied.
Exposes: the schema `withCostContext()` (WO-022) writes and reads. No code
interface.

## Steps
1. Write the migration exactly to BP-007's data-model delta, `reserved_cents`
   included.
2. Add the two indexes, and no unique constraint (BP-007 decision 3).
3. Enable RLS with no request-scoped policy.
4. Write the tests; watch the "no second table" test fail first by adding a
   `spend_ledger` fixture table, and the re-buy test fail first by adding the
   unique constraint decision 3 dropped.

## Test plan
> **Criterion source: the blueprint, not a requirement.** BP-007 carries
> `satisfies: []` and was cut from `BUILD.md` §6.5. Every row below quotes
> BP-007 as it stands on disk after its 2026-09-03 amendments (decision 3;
> decision 4 / ADR-094 decision 3a).

| Acceptance criterion (source quoted) | Test |
|---|---|
| BP-007 data-model delta: "`fetches` — `id, scan_id, source, cache_key, policy_version, cost_cents, reserved_cents, payload jsonb, created_at`" … "Indexed `(source, cache_key, policy_version, created_at desc)` for the cache read and on `scan_id` for the ledger read." | `tests/costs/fetches-schema.test.ts` — asserts the column set and both indexes by name against the applied schema. A missing `reserved_cents` fails it. |
| BP-007 data-model delta: "**Insert-only; no unique constraint on `(source, cache_key, policy_version)`**" | Same file — two rows inserted on one `(source, cache_key, policy_version)` triple both persist and are distinguishable by `created_at desc`. Adding the constraint fails it. This is the schema half of the re-buy BP-007's error behaviour promises; the read predicate that serves it is WO-022's. |
| BP-007 data-model delta: "A column, not a second table: the two figures belong to the same call." · "No second table: the ledger, the cache and the raw store are one row" | Same file — `cost_cents` and `reserved_cents` are both columns of `fetches`, and no other table in `public` carries `cache_key` or `reserved_cents`. `scans.cost_cents` is excluded by name: BP-007's NFR budget writes that per-scan roll-up at close, so it is not a second ledger. |
| BP-002 error behaviour: "RLS is default-deny: a table with no policy is unreadable by anyone holding an anon or authenticated key." | Same file — an anon-key and an authenticated-key select on `fetches` each return zero rows; the service-role client reads. |
| BP-007 NFR budget: "No cost figure is ever rendered to a customer" | Same file — asserts there is no request-scoped read policy, so the columns are unreachable by a customer's own client by construction rather than by discipline. |

## Out of scope
- `withCostContext()`, `recordFetch()`, `capHit()` — WO-022.
- The cache read predicate — "the newest row on that key inserted within
  `freshnessDays`, excluding a row whose payload is the vendor's own
  zero-result shape" (BP-007 decision 3). It is a read, not a write: this order
  adds no flag, no column and no constraint for it.
- The retention policy BP-007 notes `fetches` will need; the purge of a deleted
  account's payloads is BP-063's.
- `scans.cost_cents` and `scans.status` — written at context close by WO-022,
  columns owned by BP-012's `*_scans*.sql`.

## Rollback
Drop the migration. WO-022 has nothing to write to.

## Self-certification
`status: approved` — set 2026-08-31, re-certified 2026-09-03 after the re-plan below, by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-007 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 5 rows of `## Test plan` quote BP-007 (and BP-002's RLS bullet) as they stand on disk after BP-007's 2026-09-03 amendments — this node carries no requirement of its own and cites, never inherits.
- **Bounded.** 2 files in the file plan (2 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: no cross-block depends-on edge left to add; status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-010 → WO-267
- 2026-09-03 re-planned — planner — brought current against BP-007's 2026-09-03 amendments. The `unique (source, cache_key, policy_version)` constraint is gone from the file plan and the test plan (BP-007 decision 3), replaced by the plain `(source, cache_key, policy_version, created_at desc)` index and a positive re-buy assertion — two rows on one key must insert, which is what WO-022's empty-payload re-buy needs. `reserved_cents` folded into the create-table by ruling (rule 1.1, derivation and reversal cost in ## Provenance); WO-022's additive migration deleted to match and its `rests-on` row refuted. Migration renamed `00000000000004_fetches.sql` → `20260903080000_fetches.sql`: WO-267's applied set took the `…004` slot. Test plan re-quoted verbatim from BP-007 and BP-002 on disk; the old "no other table carries a `cost_cents` column" assertion corrected — `scans.cost_cents` is BP-007's own roll-up and would have failed it. `rests-on` row repointed WO-009 → WO-267, left `open` for this order's validator.

## Validation report (appended by validator)
