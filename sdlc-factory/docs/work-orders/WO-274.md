---
id: WO-274
type: work-order
title: "tests/db/migration-naming.test.ts's directory-enumeration case asserts membership, not an exact two-file snapshot"
status: approved
ui: no
implements: [BP-002]
depends-on: [WO-267]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-274 — tests/db/migration-naming.test.ts's directory-enumeration case asserts membership, not an exact two-file snapshot

- Branch: wo/WO-274-migration-naming-enumeration
- Estimate: ≤1 day (well under; one assertion in one existing file)

## Goal
Replace the exact-equality directory listing in
`tests/db/migration-naming.test.ts`'s `"finds the two migrations this work
order writes"` case with a membership check, so the case keeps proving
WO-267's baseline and RLS migrations are present without failing every time
any other order correctly adds a new, correctly-named migration file under
its own topic token.

## Provenance
`tests/db/migration-naming.test.ts` is WO-267's file, `tests/db/**` is
BP-002's by `structure.md` rule 4's `code:` glob (`supabase/**` ·
`src/lib/db/**` → BP-002, `structure.md` line 24: "every other migration
file is owned by the node whose topic or sub-token it carries" — the test
itself is the registry check, and the registry check is BP-002's). No
other order's file plan claims this file (`grep -rn
"tests/db/migration-naming.test.ts" sdlc-factory/docs/work-orders/*.md`
returns only WO-267's plan and mentions in WO-272's provenance/log/report,
none of which claim it as their own file to modify) — reuse check clear
(rule 7.2).

**Why this is its own order, not a merge into the order that found it
(rule 2.6's merge test).** WO-272 (BP-017) discovered the brittleness as a
side effect of correctly adding two new `users`/`sites`-topic migrations —
its own validator report (`## TST-006`) names the defect and explicitly
routes the fix to the planner rather than to its own implementer, because
`tests/db/**` is outside WO-272's file plan and outside BP-017's ownership
(rule 7.1). This order's goal and stop condition are stated entirely in
terms of `tests/db/migration-naming.test.ts`'s own behavior — "the case
must keep passing for the two migrations it names as more, correctly-named,
migrations are added" — and are true and checkable with zero reference to
WO-272, WO-272's columns, or BP-017: the fix is exercised here with a
synthetic third fixture file, not with WO-272's own migrations. WO-272 is
cited above only as *how the defect was found*, not as a fact this order's
stop condition needs. That is the distinction the merge test turns on, so
this is its own order, cut under BP-002 where the file is owned (rule
7.1) rather than admitted into WO-272's file plan, which would hand one
capability — the migration-naming registry check — two owners (BP-002's
WO-267 and BP-017's WO-272), exactly what rule 7.1 forbids.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `tests/db/migration-naming.test.ts` | MODIFY | In the `describe("supabase/migrations/ — every applied migration file names exactly one topic or sub-token", …)` block: replace the `"finds the two migrations this work order writes"` case's `expect(files.sort()).toEqual([...two names...])` with an assertion that `files` **contains** both `00000000000001_baseline.sql` and `00000000000002_rls.sql`, without asserting `files`'s length or exact membership beyond those two. Rename the case to describe membership, not an exact find. No other case in the file changes; the per-file `it.each(files)("%s carries exactly one assigned topic token")` row is untouched — it already scales to any number of files. |

## Interfaces
Consumes: `src/lib/db/topics.ts`'s `topicOf()` (BP-002, unchanged) — same as
today. Exposes: nothing; this is a test.

## Steps
1. Watch it fail first, on the tree as it stands today (only the two
   baseline files exist): the current case already passes trivially here
   (two files, matches exactly) — so the fail-first case is constructed,
   not observed: drop a third fixture file into `supabase/migrations/`
   (e.g. a copy of `00000000000001_baseline.sql` renamed to a `leads`-topic
   name) and confirm the *current* exact-equality assertion fails with
   `received` naming three files, reproducing WO-272 TST-006's finding
   locally rather than on WO-272's own branch.
2. Replace the assertion with the membership form.
3. Re-run with the fixture file still present: the new case passes. Remove
   the fixture; the case still passes (two files, both present, subset
   check trivially true). Delete a baseline file instead (temporarily) and
   confirm the new case now fails, naming the missing file — the case
   still discriminates loss of either baseline migration, which is the
   property worth keeping.
4. `npx vitest run --no-file-parallelism`, `npm run typecheck`, `npx eslint
   tests/db/migration-naming.test.ts`.

## Test plan
> **Criterion source: the file's own registry-check purpose (BP-002
> decision 2 and `structure.md` rule 2/3/3a), not a requirement.** BP-002
> carries `satisfies: []`; this order changes no behavior BP-002's
> `## Data model delta` or `## Public interface` states, only the
> discipline of one assertion inside the test that enforces BP-002
> decision 2, so acceptance is quoted from WO-272's own validator report
> (`## TST-006`, this branch's discovery of the defect) and from
> `structure.md`, each row naming its source.

| Acceptance criterion (source quoted) | Test |
|---|---|
| WO-272 `## TST-006` (Defect section): "This assertion is structurally guaranteed to fail for the *next* migration any WO adds, regardless of topic — a brittleness inherent to `tests/db/migration-naming.test.ts` as WO-267 shipped it (a snapshot of the directory at the moment WO-267 branched, asserted as exact and permanent)." | `tests/db/migration-naming.test.ts` › the renamed membership case — passes with a third, correctly-named fixture migration present (constructed in Steps 1 and 3), which is exactly the shape that failed under the old assertion. Restoring the old `toEqual` form against the same fixture reproduces the TST-006 failure, confirming the fix addresses the named defect. |
| `structure.md` rule 2/3/3a (unchanged, carried forward — not this order's row to re-assert, cited to show what stays true): the per-file `it.each(files)("%s carries exactly one assigned topic token")` case already scales per-file and is untouched by this order. | Same file — that case is not modified; its own PASS is unaffected by this order's one-line change (confirmed by running the file's full suite before and after). |
| BP-002 decision 2 (unchanged, carried forward, not re-asserted): "A migration whose name carries no assigned topic token fails the registry check." | Unaffected rows in the same file (the unassigned-name and two-token cases) — not touched by this order's file plan; re-run clean as part of Step 4's full-file run, proving the fix didn't loosen the registry check itself. |
| This order's own stop condition — the case still discriminates loss of a named baseline migration | Same file — deleting either `00000000000001_baseline.sql` or `00000000000002_rls.sql` (temporarily, in Step 3) fails the membership case, naming the missing file. Restoring it re-passes. |

## Out of scope
- Any change to `topicOf()`, `MIGRATION_TOPICS`, `MIGRATION_SUBTOKENS`, or
  any other case in `tests/db/migration-naming.test.ts` — the per-file
  `it.each` rule, the unassigned-name case, the two-token case, the
  sub-token cases, and the `config.toml` companion assertions are all
  unchanged.
- WO-272's own migrations, columns, or test file — this order does not
  read or depend on `wo/WO-272-account-component-columns`; it is built and
  verified against `doctrine-0.11.0` alone, with a synthetic fixture
  standing in for "any future correctly-named migration."
- Re-validating WO-272 — once this order merges to `doctrine-0.11.0` and
  WO-272 merges on top of (or is rebased onto) it, WO-272's own
  re-validation pass re-runs the full suite and confirms green; that pass
  belongs to WO-272's validator, not to this order.

## Rollback
Revert the one-file, one-assertion edit; the case returns to exact-equality
form, which is the state that TST-006 found brittle. No production code,
no migration, no schema touched.

## Self-certification
`status: approved` — set 2026-09-03 by the planner under constitution rule
3.2: an assertion with its grounds stated, which the librarian audits and
may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-002 — `status: approved`;
  `tests/db/**` is in its `code:` globs (`structure.md` line 24).
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan`
  quote WO-272's `## TST-006` validator report and `structure.md`/BP-002
  decision 2 as they stand on disk; this order carries no requirement of
  its own and cites, never restates, BP-002's decision.
- **Bounded.** 1 file in the file plan (0 NEW, 1 MODIFY), estimate ≤1 day.
- **Stop condition stated.** The one row names the test and the two
  mutations (extra file present; baseline file missing) that decide it;
  `## Rollback` states how to undo the change.
- **Merge test (rule 2.6).** Argued in `## Provenance` above: this order's
  goal and stop condition are stated and checkable with a synthetic
  fixture, citing WO-272 only as provenance of discovery, not as a fact
  the stop condition needs. It is its own order.
- **Single owner (rule 7.1).** `tests/db/migration-naming.test.ts` stays
  BP-002's only; this order does not give WO-272/BP-017 a claim on it.
- **Reuse (rule 7.2).** `grep` above finds no other order's file plan
  claiming this file; this order extends WO-267's existing test, mints no
  second one.

Not asserted here: validation, regression and merge. Those are the `done`
gate's, and the validator's and librarian's to set.

## Sequencing consequence
WO-272 (`wo/WO-272-account-component-columns`, `## TST-006`, verdict
`findings`) cannot reach `done` while `npx vitest run
--no-file-parallelism` is red — constitution §10 and the librarian's `done`
gate both require it. This order is what must land on `doctrine-0.11.0`
first; once it merges, WO-272 (already correct in its own 6/6 criteria per
TST-006) rebases onto or merges past it, re-runs the documented full-suite
command, and returns to the validator for re-validation with the suite
green. No other order sits between WO-274 and that re-validation.

## Log
- 2026-09-03 created — planner — routed from WO-272's `## TST-006` finding: `tests/db/migration-naming.test.ts`'s exact-enumeration case is BP-002's (WO-267's file, `tests/db/**` under BP-002's `code:` glob per `structure.md` rule 4) and outside WO-272's file plan and BP-017's ownership (rule 7.1); cut as its own order rather than admitted into WO-272's file plan, per the merge test argued in `## Provenance`.

## Validation report (appended by validator)
> Once this WO reaches `status: done`, its LATEST `## TST-###` section must
> also carry a `Regression:` line — what was re-checked alongside the new
> work, not just the new work itself: `Regression: <n> files ·
> <REQ-… list | none> re-checked — pass` or `Regression: <n> files ·
> <REQ-… list | none> re-checked — findings: <one line>`. Missing it is
> `tst-without-regression` (warn).
