---
id: WO-114
type: work-order
title: "scheduleSequence and advanceSequences — sweep, then release, then send"
status: approved
ui: no
implements: [BP-029, REQ-010]
depends-on: [WO-006, WO-267, WO-106, WO-109, WO-112, WO-113]
blocked-by: []
rests-on: []
supersedes: []
---

# WO-114 — scheduleSequence and advanceSequences — sweep, then release, then send

- Branch: wo/WO-114-nurture-sequence

- Estimate: ≤1 day

## Goal
Run at most one sequence per address at a time, in the order the pages were
delivered, dropping any that has waited more than seven days, and send at most
three touches per domain.

## Provisional against an empty repo
**No `src/` exists.** All files NEW, inside BP-029's `src/lib/mail/leads/**`
glob.

Reuse check (rule 7.2): `registry/capabilities.md` row "Bounded per-domain
follow-up sequencing — `src/lib/mail/leads/` — BP-029 (seam declared by BP-016) —
`scheduleSequence()` · `advanceSequences()` — consumers BP-003". Nothing exists.
This WO consumes `sendEmail()` (WO-106), `buildNurture()` (WO-113),
`isSuppressed()` (WO-112) and the schema (WO-109), and re-implements none.

**Pins are BP-005's and imported, never redeclared** (`structure.md` rule 5;
BP-029 says so in its own words): `NURTURE_TOUCH_HOURS = [24, 72, 168]`,
`NURTURE_MAX_TOUCHES = 3`, `SEQUENCE_START_DEADLINE_DAYS = 7`. Each is asserted in
`tests/pins.test.ts`, which is BP-005's file and which this WO does not touch. A
literal `24`, `72`, `168`, `3` or `7` anywhere in this module is a defect.

## Read ADR-041 before "fixing" a lead who received no follow-ups
Two of the three landmines are this file's:
1. **A waiting sequence past its deadline is dropped forever.** The sweep runs
   **before** the release step, on **every** invocation, so the terminal state is
   reached even on a run that releases nothing. There is no code path from
   `dropped` to `running`. Releasing them "because they only missed their slot" is
   one line and breaks the 14-day bound REQ-010 c12 states.
2. **A re-delivery starts nothing.** `scheduleSequence` inserts nothing when the
   unique index (WO-109) already holds a sequence for `(lower(email), domain)`.
   The second `leads` row keeps a null `sequence_state` forever. It reads as a
   lost enqueue and is not one.
The 14-day guarantee is **arithmetic over two pins**, not a separate check: 7 days
of waiting plus a last touch at 168 hours. Do not add a third check that could
disagree with them.

## File plan
| File | New/Modify | Change |
|---|---|---|
| `src/lib/mail/leads/sequence.ts` | NEW | `scheduleSequence()` — refuses outright for an address in `email_suppressions` (`isSuppressed`, WO-112), otherwise inserts `sequence_state = 'waiting'` with `page_delivered_at = deliveredAt`; a unique violation from WO-109's index is swallowed as success-with-no-effect, not retried and not raised. `advanceSequences(now)` — in this order and no other: **(1) sweep** every `waiting` row whose `page_delivered_at + SEQUENCE_START_DEADLINE_DAYS < now` to `dropped`, stamping `dropped_at`; **(2) release** for each address with no `running` sequence the oldest surviving `waiting` row by `page_delivered_at`, setting `running`, `sequence_started_at = now`, `touch_count = 0`, `next_touch_at = now + NURTURE_TOUCH_HOURS[0]`, and skipping any suppressed address; **(3) send** every `running` row whose `next_touch_at <= now`, calling `sendEmail` with `buildNurture` at touch `touch_count + 1`, then advancing `touch_count` and setting `next_touch_at` from `NURTURE_TOUCH_HOURS[touch_count]` or `finished` at `NURTURE_MAX_TOUCHES`. Returns `{ dropped, released, sent }`. |
| `tests/mail/leads/sequence.test.ts` | NEW | `scheduleSequence`: the suppressed refusal and the re-delivery no-op. |
| `tests/mail/leads/advance.test.ts` | NEW | The three-phase order, the touch schedule, the 14-day bound, the one-at-a-time and ordering invariants. |

## Interfaces
**Exposes** (verbatim from BP-029 `## Public interface`):
```ts
// src/lib/mail/leads/sequence.ts — criteria 9 to 13
/** BP-016's declared entry point, realised here. Keyed by (lower(email), domain):
 *  a second delivery of the same domain's page to the same address starts
 *  nothing (criterion 13). Starts nothing at all for a suppressed address
 *  (criterion 11's last limb). */
export function scheduleSequence(a: { email: string; domain: string; deliveredAt: Date }): Promise<void>

/** The lead/nurture job body (BP-003). Sweeps first, then releases, then sends:
 *  a waiting sequence past its own deadline is dropped even on an invocation
 *  that releases nothing, so the stored state is never a lie about what is due. */
export function advanceSequences(now: Date): Promise<{
  dropped: number; released: number; sent: number
}>
```
**Consumes**: `sendEmail()` (WO-106, BP-053); `buildNurture()` (WO-113);
`isSuppressed()` (WO-112); the `leads` schema and its three indexes (WO-109);
`NURTURE_TOUCH_HOURS`, `NURTURE_MAX_TOUCHES`, `SEQUENCE_START_DEADLINE_DAYS`
(BP-005); `dbAdmin()` (BP-002).

**Not exposed here**: `suppressAddress()`, which BP-029's interface prints under
this file's comment header but which WO-112 already ships alongside the store it
writes. Recorded so the two WOs do not both create it; no interface changes.

## Steps
1. Write both test files first and watch them fail.
2. `scheduleSequence` first — it is the only writer of `sequence_state = 'waiting'`.
3. `advanceSequences` as three named internal steps called in a fixed order from
   one function, so the order is visible in one place and a reviewer can see the
   sweep precede the release.
4. Log lead id, domain, state transition, touch index, drop reason and suppression
   outcome; never an address in clear text (BP-029 NFR budget). Every `dropped`
   row's log line names REQ-010 c12 and ADR-041, per that ADR's consequences.
5. Mutation checks: (a) move the sweep after the release and confirm the
   deadline test fails on an invocation that releases nothing; (b) delete the
   `NURTURE_MAX_TOUCHES` stop and confirm a fourth touch is sent and the c9 test
   fails; (c) release a `dropped` row and confirm the 14-day bound test fails.

## Test plan
| Acceptance criterion (verbatim from REQ) | Test |
|---|---|
| REQ-010 c9 — "Given a lead who received the free page for a domain and has not subscribed, when time passes, then at most three follow-up emails are sent for that domain, at 24, 72 and 168 hours after that domain's sequence begins (criterion 12), and none afterwards." | `tests/mail/leads/advance.test.ts` → `a released sequence sends exactly three touches, at +24h, +72h and +168h from sequence_started_at, and nothing at +336h` — a clock-driven fixture stepping through the whole life, asserting the count is three and the offsets come from `NURTURE_TOUCH_HOURS` rather than from literals in this module. |
| REQ-010 c12 — "Given a domain's page has been delivered to an address, when that domain's sequence begins, then it begins no earlier than the delivery of that page and never while another sequence to that address is running or still waiting to run: at most one sequence to an address is ever running, and sequences to one address run in the order their pages were delivered, however many domains that address has. A sequence that has not begun within 7 days of its own page's delivery is dropped and never sent, so no follow-up about a domain ever reaches an address more than 14 days after that domain's page was delivered to it. A sequence ended by a subscribe (criterion 10) or an opt-out (criterion 11) releases nothing — those end the waiting sequences too, and no waiting sequence ever begins after either." | `tests/mail/leads/advance.test.ts` → five named cases: `no sequence starts before its own page_delivered_at`; `an address with four domains never has two running at once`; `sequences release in page_delivered_at order`; `a waiting sequence is dropped at day 7 on an invocation that releases nothing, and no later invocation can release it` (the sweep-before-release property, and ADR-041 landmine 1); `no touch is ever sent more than 14 days after its own page_delivered_at` — asserted across a four-domain fixture run day by day for 30 days, which is the bound stated as arithmetic on the two pins. The last sentence's store half is WO-112's; here it is asserted as `after suppressAddress, advanceSequences releases nothing for that address on any later invocation`. |
| REQ-010 c13 — "Given an address that has a sequence for a domain — running, waiting to begin, or already finished — when that domain's page is delivered to that address again, for any reason, then no second sequence for that domain is started and that domain's touches to that address stay capped at the three of criterion 9." | `tests/mail/leads/sequence.test.ts` → `scheduleSequence for a (email, domain) that already has any sequence_state is a no-op and raises nothing` — four cases, one per surviving state (`running`, `waiting`, `finished`, `stopped`), plus `the total touches for that domain across both leads rows is still three`. The index that makes it unrepresentable is WO-109's; this asserts the caller's behaviour against it (ADR-041 landmine 2). |
| REQ-010 c11 — "Given any of these emails, when it is opened, then it carries a working opt-out, and using it stops all future follow-up email to that address — every remaining touch of the running sequence, every sequence for every other domain, and any sequence a future delivery would otherwise start." | `tests/mail/leads/sequence.test.ts` → `scheduleSequence starts nothing for a suppressed address` — **the last limb only** ("any sequence a future delivery would otherwise start"). The remaining-touches and other-domains limbs are WO-112's; the carries-limb is WO-113's. |

## Out of scope
- The `lead/nurture` job trigger and its schedule — BP-003's, which "calls
  `advanceSequences()` and holds no logic of its own" (BP-029).
- Delivering the first page (WO-115). A sequence is only ever scheduled from a
  delivery that already happened.
- Suppression writes and the opt-out token (WO-112).
- Any customer-visible string (REQ-093 c1); the nurture copy keys are WO-113's.
- Any change to the touch cadence, the deadline or the cap — those are BP-005
  pins, and changing one is a change to `tests/pins.test.ts` and to ADR-041.

## Rollback
Delete `src/lib/mail/leads/sequence.ts` and its two tests. WO-109's schema stands;
rows already in `running` stop advancing, which is a halt, not a violation — no
mail is sent that should not be.

## Self-certification
`status: approved` — set 2026-08-31 by the planner under constitution rule 3.2: an assertion with its grounds stated, which the librarian audits and may refuse retroactively. Grounds, against §3's gate list:

- **Cut from a verified blueprint.** BP-029 — `status: approved`.
- **Criteria inherited, not paraphrased.** The 4 rows of `## Test plan` quote REQ-010 as it stands on disk.
- **Bounded.** 3 files in the file plan (3 NEW, 0 MODIFY), estimate ≤1 day.
- **Stop condition stated.** Every row names the test that decides it, and `## Rollback` states how to undo the change.

Not asserted here: validation, regression and merge. Those are the `done` gate's, and the validator's and librarian's to set.

## Log
- 2026-08-31 created — planner
- 2026-08-31 finished — planner — corpus-wide pass over all 238 work orders: 2 cross-block depends-on edges added (WO-006, WO-011); status self-certified `approved` (rule 3.2), grounds in ## Self-certification.
- 2026-09-02 finished — planner — depends-on repointed after consolidation: WO-011 → WO-267

## Validation report (appended by validator)
